# Synthetic example

The fixture for `uv-pbr@0.2.0` is deliberately synthetic and contains no user image, model weight or network asset. Runtime uses the bundled, provenance-tracked CC0-derived texture pack only after its manifest and hashes validate.
