# Synthetic example

The fixture for `hard-surface-detail@0.1.0` is deliberately synthetic and contains no user image, model weight or external asset. It is used only to exercise declarative validation.
