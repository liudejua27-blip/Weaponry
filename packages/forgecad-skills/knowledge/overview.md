# ForgeCAD MVP Skill registry

The registry is a typed planning boundary between Codex's visual reasoning and
the local Runtime. It describes ten staged capabilities; it is not a model,
prompt pack or executable plugin. `img2threejs` contributes the staged
inventory/compare workflow idea, while `img2css` contributes only bounded
color-region preview vocabulary.
