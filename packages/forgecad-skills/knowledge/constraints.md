- Every plan is bound to an admitted `ReferenceEvidence` hash.
- Hidden surfaces, scale and physical function remain explicit unknowns.
- Operators are pre-registered Rust IDs; a Skill cannot carry code.
- Recipes are acyclic, bounded and deterministic; no network, filesystem,
  environment, secret, URL, JavaScript, CSS or model call is permitted.
- A Skill benchmark receipt is evidence about the Skill contract, not proof of
  high-quality geometry or similarity.
