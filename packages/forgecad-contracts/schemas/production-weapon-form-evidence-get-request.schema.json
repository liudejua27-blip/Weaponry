{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://forgecad.local/contracts/production-weapon-form-evidence-get-request.schema.json",
  "title": "ProductionWeaponFormEvidenceGetRequest@1",
  "description": "Read-only lookup for one immutable six-view ProductionWeaponFormEvidence@1 record.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "form_evidence_id", "session_id", "project_id", "candidate_id"],
  "properties": {
    "schema_version": {"const": "ProductionWeaponFormEvidenceGetRequest@1"},
    "form_evidence_id": {"$ref": "#/$defs/id"},
    "session_id": {"$ref": "#/$defs/id"},
    "project_id": {"$ref": "#/$defs/id"},
    "candidate_id": {"$ref": "#/$defs/id"}
  },
  "$defs": {
    "id": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9_.-]{0,127}$"}
  }
}
