{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://forgecad.local/contracts/candidate-reject-result.schema.json",
  "title": "ForgeCAD Candidate Reject Result",
  "type": "object",
  "required": ["schema_version", "candidate_id", "project_id", "state", "approval_receipt_id", "request_sha256", "replayed"],
  "properties": {
    "schema_version": {"const": "CandidateRejectResult@1"},
    "candidate_id": {"type": "string", "pattern": "^[A-Za-z0-9_.-]{1,128}$"},
    "project_id": {"type": "string", "pattern": "^[A-Za-z0-9_.-]{1,128}$"},
    "state": {"const": "rejected"},
    "approval_receipt_id": {"type": "string", "pattern": "^receipt-[A-Za-z0-9]{32}$"},
    "request_sha256": {"type": "string", "pattern": "^[a-f0-9]{64}$"},
    "replayed": {"type": "boolean"}
  },
  "additionalProperties": false
}
