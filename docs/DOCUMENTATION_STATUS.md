# ForgeCAD 当前状态账本

2026-08-22 `CandidateAnimationVfxQuality@2` source/structural gate：Contracts **402**；Store focused **3/3**、Store full **112/112**；Runtime focused **6/6**、同源码同 cohort Runtime full **354 passed / 0 failed / 22 ignored**（376 total，115.40s）；MCP focused **4/4**、full **152 passed / 0 failed / 0 ignored**（2.49s）；contracts/runtime/store/MCP joint cargo check **PASS**。旧 `GEOMETRY_WORKER_PROTOCOL` 报告来自 stale Worker binary，已由同源码 Geometry/Render Worker 重建后清除。尚无真实 `Attachment@3 + Quality@2` public full-chain positive fixture，durable end-to-end=`NOT_RUN`/`BLOCKED_FIXTURE_CHAIN`；当前仅 `structural_only`，visual/artistic/commercial FPS=`NOT_PROVEN`，human/engine=`NOT_RUN`，不推进 stage/confirm/version/export。证据：`docs/evidence/mcp010f/candidate-animation-vfx-quality-v2-durable-source-gate-20260822.json`。

2026-08-22 `CandidateMaterialSurfaceQuality@1` public positive fixture：`Geometry → CandidateTopologyQuality@1 → AppearanceProgram@3 → TextureBuild@2 → SurfaceBake@1 → AppearanceSourceLineage@1 → CandidateMaterialSurfaceQuality@1` 的 `prepare → same-key replay → get → Runtime drop/reopen → restart get` 通过 **1/1（111.72s）**；Runtime focused **5/5**、Store full **74/74**、Contracts **350**。CAS inventory unchanged；stable `artifact_id` 与 GLB object SHA-256、MaterialPack CAS kind 精确区分，合法 UV/tangent rebuild 不计入 geometry-preservation 漂移。该结果仅为 `structural_only`；V2 animated-socket-particles 仍无完整 public `prepare → Store → restart get`，durable end-to-end=`NOT_RUN`/`BLOCKED_FIXTURE_CHAIN`；visual/commercial=`NOT_PROVEN`，human/engine=`NOT_RUN`，stage/confirm/version/export=false。证据：`docs/evidence/mcp010f/candidate-material-surface-quality-public-positive-source-gate-20260822.json`。

最终同 cohort 修订口径：强制 build cohort `724278fe8f6777c8b3d07bc5058208aee90aa5c700db5f9284d6297126fa79f6` 下 material focused **5/5（112.63s）**；Runtime full **310 passed / 0 failed / 20 ignored**（330 total，201.91s），且 public material fixture 明确在该 full run 内执行。此前 **111.72s** 仅为 public fixture 单测时长；两者都只支持 `structural_only`，不提升 visual/commercial、human/engine 或 stage/confirm/version/export 状态。

2026-08-22 `FictionalEnergyVfxAnimatedSocketParticlesSequence@2` 双候选 source slice：Contracts **350**；Store V2 focused **2/2**、Store full **74/74**；Runtime V2 仅低层 focused **6/6**、cargo check **PASS**；MCP V2 **3/3**；同 cohort `724278fe8f6777c8b3d07bc5058208aee90aa5c700db5f9284d6297126fa79f6` Runtime full **309 passed / 0 failed / 20 ignored**（191.06s）、MCP full **128 passed / 0 failed / 0 ignored**（1.93s），这些是全量回归，不是 V2 public `prepare → Store → restart get` 正向 fixture。V1/V2 隔离；V2 仅证明 1..16 frame、geometry/appearance 双 candidate/delivery/AnchorSet bridge 以及 Store FK/reachability/idempotence/conflict/rollback 的结构面。完整双候选 public Runtime `prepare → Store → restart get` 正向 fixture 尚不存在，durable end-to-end=`NOT_RUN` / `BLOCKED_FIXTURE_CHAIN`，不能声称正向 durable。该 slice 为 `structural_only`；visual/commercial=`NOT_PROVEN`，human/engine=`NOT_RUN`，stage/confirm/version/export=false。证据：`docs/evidence/mcp010f/fictional-energy-vfx-animated-socket-particles-v2-dual-candidate-source-gate-20260822.json`。

2026-08-22 `FGC-MCP010F-TRAILS-21` 当前为 source `in_progress`：已形成动态 animated socket Trails 与 TrailsBloom 的 closed Contracts、versioned Render Worker/Protocol、Store parent/frame 事务，以及 Runtime/MCP prepare/get focused source 实现；新增 animated GLB socket transform projection 的 Runtime-owned durable/replay/restart read-only source Gate。当前源真值为 Contracts **344**、**26/26 active operators**、MCP **79 read + 59 write = 138 tools**；同 cohort `9cc7c11b8309ee7cc76df3d67794c4692c81ff0e8ef064248955ad511b8ca388` Runtime full lib **307/307**（0 failed，另 20 ignored）、binary **1/1**，合计 **308 pass**，Store full **72/72**、显式固定 cohort 的 MCP full **125/125**。该 projection Gate 不等于 animated-socket Particles/Trails/TrailsBloom 正向集成；动态 VFX 链仍缺完整正向 durable `prepare → Store → restart get` 集成测试和独立回执，因此不得宣称 durable end-to-end PASS。整体保持 `structural_only`，不推进 stage/confirm/version/export，视觉/艺术/商业 FPS=`NOT_PROVEN`，人评/引擎=`NOT_RUN`。证据：`docs/evidence/mcp010f/game-weapon-animated-glb-socket-transform-projection-source-gate-20260822.json`。

2026-08-21 `FGC-MCP010F-ANIMATION-VFX-QUALITY-19` durable structural report 子门通过：新增 `CandidateAnimationVfxQuality@1`，只读重验 material-surface V2 head/quality、delivery LOD0、AnchorSet、既有 animation clip/GLB receipt/animated socket，以及 VFX profile→sequence→frame→bloom→particles→trails→trail-bloom；全部验证后才写唯一 report CAS，restart get 纯读。该子门完成时的历史快照为 **319 schemas / 26/26 active operators / 74 read + 54 write = 128 tools**；Store full **55/55**、Runtime full **284 pass / 0 fail / 20 ignored**、同 cohort MCP full **110/110**。VFX 结论为 sidecar-only，故 `vfx_glb_socket_attachment=false`、`validator_status=failed`、`hard_gate_passed=false`，报告不会推进 stage；视觉/艺术/商业 FPS 仍 `NOT_PROVEN`，人评/引擎仍 `NOT_RUN`，且不 confirm/version/export。证据：`docs/evidence/mcp010f/candidate-animation-vfx-quality-durable-source-gate-20260821.json`。

2026-08-21 `FGC-MCP010F-PRODUCTION-STAGE-V2-18` source/runtime/Store/MCP durable structural 子门通过：`ProductionStageTransition@2` 经显式 approval，把 immutable topology root 原子推进为 distinct material-surface head，并精确绑定 V1 parent、通过的 topology/material quality、双 candidate/artifact/state、参考/相机证据与 receipt；Store transaction、幂等/冲突/stale 拒绝、历史 restart get 和 reservation rollback 均 fail-closed，V1 head 保持不变。当前 **314 schemas / 26/26 active operators / 73 read + 53 write = 126 tools**；Contracts 314、Store full 51/51、Runtime full 277 pass / 0 fail / 20 ignored、同构建双 Worker MCP full 107/107。`CandidateMaterialSurfaceQuality@1` prepare 本身仍不推进 stage，只有独立 V2 可提升 exact passed binding；material-surface head 仍为 `structural_only`，不等于 PBR likeness、艺术/真人验收、Commercial FPS、引擎 round-trip、confirm/version/export。总体仍为 `QUALITY_TARGET_NOT_MET` / `INCOMPLETE_TRUTH_BINDING` / camera `MISMATCH` / `BLOCKED_REFERENCE_COVERAGE`。证据：`docs/evidence/mcp010f/production-stage-transition-v2-durable-source-gate-20260821.json`、`docs/evidence/mcp010f/candidate-material-surface-quality-durable-source-gate-20260821.json`。

2026-08-21 `FGC-MCP010F-TOPOLOGY-QUALITY-16` source/runtime/Store/MCP focused 子门通过：在 durable 生产轴 `draft → gray-model → topology → material-surface → animation-vfx → game-delivery` 上，除既有 `draft → gray-model` 外，已新增 candidate-wide `CandidateTopologyQuality@1` 与 `gray-model → topology`。Runtime 从 exact candidate GLB、GeometryCandidateEvidence 与 ArtifactReadback@2 重算全部 Part 的 bounded 拓扑快照和结构指标；Store 只拥有一个 topology report CAS，blocked 报告留痕但不推进 head，passing report 才允许 approval-gated stage transition。当前 **303 schemas / 26/26 active operators / 71 read + 51 write = 122 tools**。定向证据为 Contracts 303、Store topology 4/4、Store full 45/45、Runtime topology 2/2、Agentic session 11/11、MCP topology 3/3；2K AppearanceProgram@3 现使用固定 120 秒 wall+CPU Worker profile，lineage fixture canonical preimage 已按合同修正，补建同 target Worker 后 Runtime full 为 **268 pass / 0 fail / 20 ignored**。此 Gate 仍只是 evaluated triangulated GLB 的结构质量：每 Part 512 faces 上限，authoring cage 仅 partial/not-available，edge flow、artistic/visual quality 固定 `NOT_PROVEN`；总体仍为 `QUALITY_TARGET_NOT_MET` / `INCOMPLETE_TRUTH_BINDING` / camera `MISMATCH` / `BLOCKED_REFERENCE_COVERAGE`。证据：`docs/evidence/mcp010f/candidate-topology-quality-durable-source-gate-20260821.json`。

2026-08-21 `FGC-MCP010F-BEVEL-CANDIDATE-14` source/runtime/Store/MCP 结构子门通过：`GeometryModifierApplyRequest@2 → Result@2` 已把现有单边 `bevel@2` 从独立 Worker 能力接到 exact current-head candidate。Runtime 重验 candidate/CAS/program/readback/topology/catalog/config/Part terminal/stable edge，派生 reviewable candidate 并保持非目标 Part 绑定；Store 在同一事务内复验 source/head/derived/sidecar，支持幂等与重启读回。固定同一相机的 source/derived 各重复两次 512×512 九 AOV：各自 byte-exact，silhouette IoU、bbox、centroid 均通过有界非退化门，normal 或 wireframe detail pass 确认发生变化。当前 **293 schemas / 69 read + 49 write = 118 tools**。这是 `structural_only` 的可见细节接线，不是任意 edge-chain bevel、参考 likeness、PBR、人评、确认/版本/导出或商业游戏武器质量通过；总体仍为 `QUALITY_TARGET_NOT_MET` / `INCOMPLETE_TRUTH_BINDING` / camera `MISMATCH` / `BLOCKED_REFERENCE_COVERAGE`。证据：`docs/evidence/mcp010f/candidate-bound-bevel-v2-source-gate-20260821.json`。

2026-08-21 `GAME-ENGINE-IMPORT-13` evidence-only conformance PASS：真实 Godot `4.7.2.stable.official.ed1daf0bf` headless importer 聚合读取 4 个 GLB scene（LOD0/LOD1/LOD2/animated）；三档 triangles 为 `304 > 176 > 112`，每个 scene 均为 5 meshes、相同 materials、6 个 non-rendering sockets 且 parent/local TRS exact。animated scene 的 10 个 glTF channels 在 Godot 中优化为 2 个 semantic tracks，cross-loader 在 t0/half-duration 的 socket TRS exact，两个命名 socket follow 通过；collision sidecar readback 通过 5 个 `BoxShape3D`。static 与 animated 输入来自独立 Runtime source cohorts；本证据不产生 Runtime durable Godot link。`commercial_engine_roundtrip=false`，Unity/Unreal、physics/hitbox、visual/human、confirm/export 均 `NOT_RUN/false`；quality 仍 `structural_only`，总体 `QUALITY_TARGET_NOT_MET` / `INCOMPLETE_TRUTH_BINDING` / camera `MISMATCH` / `BLOCKED_REFERENCE_COVERAGE` 不变。当前 **291 schemas / 69 read + 49 write = 118 tools**。

2026-08-21 `GAME-ANIMATED-GLB-SOCKETS-12` source/runtime/MCP durable 结构子门通过：`game_weapon_animated_glb_socket_prepare/get` 将 `MechanicalAnimationGlbReceipt@1` 的 candidate-bound animated LOD0 source 与 delivery LOD0、AnchorSet 精确绑定，在派生 animated GLB 中物化六个稳定命名 empty socket nodes；源/派生 animation projection、animations/channels/samplers、renderable projection、source static projection 与 BIN bytes 均 exact。Runtime-owned CAS 恰好两个：派生 animated socket GLB 与新 receipt；source animated artifact、animation receipt、delivery LOD0 与 AnchorSet 只是精确引用。Three.js r185 `GLTFLoader` source/derived parse、10 tracks projection 与 half-duration 两个 socket parent-follow 通过。当前 **290 schemas / 26/26 active operators / 69 read + 49 write = 118 tools**。这是 structural-only；Unity/Unreal/Godot、runtime pivot、functional weapon semantics、human、confirm/version/export 与 visual quality 均未运行或未通过，保留 `QUALITY_TARGET_NOT_MET` / `INCOMPLETE_TRUTH_BINDING` / camera `MISMATCH` / `BLOCKED_REFERENCE_COVERAGE`。

2026-08-21 `GAME-GLB-SOCKETS-11` source/runtime durable 结构子门通过：`game_weapon_glb_socket_prepare/get` 对 delivery 的 LOD0/1/2 各派生一份 GLB，每份物化六个稳定命名、无渲染内容的 anchor nodes，并保持源 renderable projection 与 BIN chunk byte-exact。四个自有 CAS、SQLite parent+exact-three-child FK、幂等/冲突拒绝、限定回滚和 Runtime 重启后源/派生 GLB 双重解析均通过；Three.js r185 `GLTFLoader` 已实际解析三份派生 GLB 并复验节点、父级与 local TRS。现行 **284 schemas / 26/26 active operators / 68 read + 48 write = 116 tools**。这只是非功能性游戏美术资产的结构能力；Unity/Unreal/Godot、动画 socket、运行时 pivot、视觉质量、真人、确认/版本/导出均未通过，总体 `QUALITY_TARGET_NOT_MET` / `INCOMPLETE_TRUTH_BINDING` / camera `MISMATCH` / `BLOCKED_REFERENCE_COVERAGE` 不变。

2026-08-21 `VFX-TYPED-TRAILS-BLOOM-10` source/runtime durable 同 cohort 子门通过：`fictional_energy_vfx_trails_bloom_prepare/get` 从精确绑定的 typed trails 重建两条轨迹，以固定阈值/增益/半径/强度/钳位/双 pass kernel 输出独立 `trail-emissive-source` 与 `trail-bloom-contribution` RGBA8 review passes；原三张 trail passes、基础九 AOV、原 HDR Bloom 与 particle passes 前后 byte-exact。CAS/SQLite immutable link、冲突 fail-closed、幂等与 Runtime 重启回读均通过；forced durable 1/1 PASS（152.61 秒），独立 Worker 1/1 PASS（27.88 秒）。当前为 **278 schemas / 26/26 active operators / 67 read + 47 write = 114 tools**。旧 trails receipt 明确 `bloom_input=false`，新 receipt 明确 `trail_bloom_input=true`。这不是 FP16 HDR、sidecar anchor 不是 GLB socket，商业引擎 roundtrip、foreign-cohort 专门 Gate、人评、确认/版本/导出均未运行；总体 `QUALITY_TARGET_NOT_MET` / `INCOMPLETE_TRUTH_BINDING` / camera `MISMATCH` / `BLOCKED_REFERENCE_COVERAGE` 不变。

2026-08-21 `VFX-TYPED-TRAILS-09` source 子门通过：`fictional_energy_vfx_trails_prepare/get` 将 1–4 个严格更早的 durable particle frames 与当前 particle frame 组成两条 Runtime-owned typed trails，输出独立 `trail-color` / `trail-id` / `trail-depth` PNG。当前显示/遮挡严格绑定 current base/Bloom；历史帧保留各自 tick 的 base/Bloom lineage，并强制同项目、候选、artifact、AnchorSet、camera、RenderProfile、Worker cohort、node inventory、owner transform 与 particle policies。forced cohort durable Gate 1/1 PASS（135.47 秒），独立 Worker Gate 1/1 PASS（25.17 秒），覆盖非法字段和未来历史 tick 零写入、双 Worker 字节一致、依赖 byte-exact 不污染、幂等和 Runtime 重启精确回读。当前为 **271 schemas / 26/26 active operators / 66 read + 46 write = 112 tools**。V1 明确不把 trails 输入 Bloom，parent trail ID 由其 segments 共享；GLB socket、商业引擎回读与视觉质量仍未通过，总体 `QUALITY_TARGET_NOT_MET` / `INCOMPLETE_TRUTH_BINDING` / camera `MISMATCH` / `BLOCKED_REFERENCE_COVERAGE` 不变。

2026-08-21 `VFX-FRAME-02` 当前仅完成 Render Core/Worker 执行子门：闭集 `render_glb_vfx_frame` 以 MaterialZone name + stable material ID 双重校验临时 emissive override，同 cohort 隔离测试证明 beauty 实际改变、其余八 AOV 不变且九 AOV 重放字节一致。现行 schema/tool 计数不变。typed frame sampling、Appearance LOD lineage、CAS receipt、GLB anchor/socket 尚未连通，所以总体 `QUALITY_TARGET_NOT_MET` / `INCOMPLETE_TRUTH_BINDING` / camera `MISMATCH` / `BLOCKED_REFERENCE_COVERAGE` 不变。

2026-08-21 当前最新 source truth：新增 `fictional_energy_vfx_prepare/get`，把精确 delivery、weapon AnchorSet 与 allowlisted 2K MaterialPack 绑定为 Runtime-owned durable `FictionalEnergyVfxProfile@1`；raw stdio、幂等、重启 readback、负数拒绝和 Three.js r185 线性采样数学通过。当前为 **227 schemas / 26/26 active operators / 58 read + 40 write = 98 tools**。该 profile 只证明静态 emissive 材质定义存在和 sampled emissive intent 可持久化，不证明 GLB MaterialZone 已绑定、材质动画已渲染、Bloom/粒子/拖尾或 Godot/Unity/Unreal 回读；`QUALITY_TARGET_NOT_MET`、`INCOMPLETE_TRUTH_BINDING`、camera `MISMATCH`、`BLOCKED_REFERENCE_COVERAGE` 不变。

2026-08-21 当前最新 source truth：默认只读 `game_asset_lod_derive` 已能从精确绑定的 GeometryProgram 临时派生 LOD0/1/2，仅降低白名单 typed tessellation 参数；本轮 raw stdio 回读 256/128/64 triangles，每档固定 Worker 双回放字节一致，Part/MaterialZone 绑定稳定，无 Runtime/CAS/candidate/version 写入，最终 MCP 包络 9019 bytes。返回的 GeometryProgram 变体是 `structural_only` transient preview，须显式 materialize 后才能进入 candidate-bound delivery。当前为 **215 schemas / 26/26 active operators / 56 read + 38 write = 94 tools**。这不是通用 mesh decimation/QEM/重拓扑，也未证明外观保持、Unity/Unreal/Godot LODGroup、用户外部 export 或视觉/真人/HQ360；`QUALITY_TARGET_NOT_MET`、`INCOMPLETE_TRUTH_BINDING`、camera `MISMATCH` 和 `BLOCKED_REFERENCE_COVERAGE` 不变。

2026-08-21 上一 durable delivery source slice：MAT-04 2K surface lowering 与 `mechanical_animation_glb_prepare` 保持已通过的结构 Gate；`game_asset_delivery_prepare` 现把 3 层 authored candidate-bound GLB、LOD2 Part AABB、四个 JSON sidecar 与可选动画固化为 Runtime-owned `GameAssetDeliveryLink`，Store 同事务标记可达，冲突 retarget fail closed，`game_asset_delivery_get` 在 Runtime 重启后重新读取 CAS 并校验全部绑定。Three.js r185 GLTFLoader 已真实解析 LOD0/1/2、回算三角形、核对稳定材质名和碰撞 AABB，并由 AnimationMixer 采样 4 条刚性轨道。现行计数为 **215 schemas / 26/26 active operators / 56 read + 38 write = 94 tools**。该 delivery prepare 本身不生成 LOD；LOD1/2 动画、正式 GC inventory、Unity/Unreal/Godot、引擎 LODGroup/碰撞组件、用户外部 export、视觉/真人/HQ360 仍未完成；`QUALITY_TARGET_NOT_MET`、`INCOMPLETE_TRUTH_BINDING`、camera `MISMATCH` 和 `BLOCKED_REFERENCE_COVERAGE` 不变。

2026-08-20 当前最新 MAT-03 source structural 能力：新增闭集 `forgecad-fictional-energy-weapon-2k@1.0.0` 与 `MaterialPackManifest@2` / `TextureBuildReceipt@2`。固定 Geometry Worker 从已准入的 7 张 512px CC0 输入按 Catmull-Rom + 固定 64px integer semantic microdetail 确定性派生 2048×2048 输出；实际 GLB BIN 嵌入 5 张被材质引用的 PNG（baseColor、OpenGL+Y normal、metallicRoughness），strict readback 逐张解析 PNG/IEND、去除仅 0–3 字节 GLB padding、解码尺寸并对 manifest/GLB receipt 哈希和语义，任何字节篡改失败。same-cohort optimized raw stdio 为 3 Parts / 524 triangles / 5 actual 2K PNG / 9 AOV / external URI 0 PASS，CAS 中的 TextureBuildReceipt@2 绑定 7 source inputs 与 5 physical outputs。普通几何仍受声明的 10s budget；只有精确闭集 2K pack 获得独立 120s texture-build wall budget，Worker 全局 10s CPU hard gate未放宽，优化构建实际通过。精确状态仅为 `PASS_SOURCE_STRUCTURAL_EMBEDDED_2K_PBR_V1`：synthetic microdetail 不是 geometry normal/AO bake，不是 decal/wear/clearcoat texture、视觉 PBR likeness、真人、当前用户 candidate、package/engine/export-restart/HQ360 PASS，`QUALITY_TARGET_NOT_MET` 不变。receipt：`docs/evidence/mcp010f/fictional-energy-weapon-2k-source-gate-20260820.json`。

2026-08-20 当前最新 MAT-02 source structural 能力：专用 `forgecad-fictional-energy-weapon` 路径现在使用 artifact-wide `connected-dominant-axis-islands@1` UV allocator，2048 坐标图集、固定 8 texel padding；同 source、同投影且无正面积重叠的共享边保持完全相同 UV，复杂曲面重叠、投影变化、source 边界和 Boolean 面确定性拆为显式 seam。GLB primitive 绑定 chart IDs/hash，strict readback 从实际 TEXCOORD_0 accessor/BIN 校验 chart coverage、范围、padding、共享边连续性和正面积 overlap；BIN tamper 被拒绝。Worker 64/64、Runtime fallback 235/0/12 ignored、专用同 cohort raw stdio 524 triangles/319 charts/9 AOV/external URI=0 PASS；旧 robot 512 triangle-grid 路径回归不变。这里的 2048 只指 UV atlas coordinate/padding policy，实际纹理仍为 512px；2K texture、normal/AO bake、decal/wear/clearcoat、视觉/人评/package/engine/export-restart/HQ360 未完成，`QUALITY_TARGET_NOT_MET` 不变。receipt：`docs/evidence/mcp010f/fictional-energy-weapon-continuous-uv-source-gate-20260820.json`。

2026-08-20 当前最新原子能力为专用 `forgecad-fictional-energy-weapon@1.0.0` MaterialPack 的 source structural admission：`AppearanceProgram@2`、Worker protocol、Runtime、MCP 与 GLB lowering 现在按同一 closed pack ID / canonical manifest hash 绑定，MaterialZone 的 `part_ids` 必须与几何 Part outputs 完全一致；7 个 512×512 CC0 纹理、7 个武器材质定义、embedded-only/external-URI=0、Runtime GLB pack identity readback 和 caller path/URL/plugin fail-closed 均通过。隔离 raw stdio 已真实用该 pack 完成 `appearance_prepare` 并返回 9 AOV；这是专用材质路由与结构性 PBR plumbing，不是最终材质完成。连续 UV、2K、normal/AO bake、decal、wear、clearcoat texture、当前用户 candidate、视觉/人评/package/live/export-restart/HQ360 均未推进，`QUALITY_TARGET_NOT_MET` 保持。receipt：`docs/evidence/mcp010f/fictional-energy-weapon-material-pack-source-gate-20260820.json`。

2026-08-20 当前最新原子能力为 active `forgecad.geometry.bevel@2`：它只接受一个 direct `authoring-mesh@1` 输入和一个稳定 source edge ID，在 closed、connected、outward、planar、strictly convex triangle/quad solid 及端点 valence=3 的 P0 范围内执行有界 1–4 segment/profile bevel。Worker 对 segments=1/3 的 closed cube 分别生成 16/24 triangles，strict readback、solid、determinism、edge/width/profile variation、boundary/non-manifold/multi-edge/oversize/unknown executable field fail-closed 均通过。当前口径为 195 schemas、26/26 active operators、54 read + 36 write = 90 MCP tools；Luna 最终复签 P0=0/P1=0。它尚未进入 Modifier/Agentic/Repair、当前武器 candidate、视觉/PBR、人评、package/live、export-restart 或 HQ360。receipt：`docs/evidence/mcp010f/fictional-energy-weapon-bevel-v2-source-gate-20260820.json`。

2026-08-20 `energy-core@1` source/Skill structural slice：固定 Geometry Worker 以 closed typed component 生成 guard ring、mechanical ring、emitter core 或 mechanical backplate；一个 operator node 只产生一个 semantic Part，四节点 fixture 为 4 connected components / 768 triangles，boundary=0、non-manifold=0、winding=0。负 inner radius 和 solid 的任意非零 inner radius 均 fail closed，MCP/Runtime Agentic allowlist 与 outer/inner radius、depth、position、rotation typed patch 已闭合。该 slice 当时为 195 schemas、25/25 operators、54 read + 36 write = 90 MCP tools；Worker 60/60、Runtime 233/0/12 ignored、MCP 86/86 和 raw stdio 25/25 Gate PASS。该 slice 只是合法非功能性科幻视觉组件语法，当前武器 candidate 未重编译，PBR、人评、package/live、export-restart 和 HQ360 未推进。receipt：`docs/evidence/mcp010f/fictional-energy-weapon-energy-core-v1-source-gate-20260820.json`。

2026-08-20 此前 `vent-array@2` source/Skill structural slice：在保留 `vent-array@1` 历史盒体阵列语义的前提下，固定 Geometry Worker 新增零输入、闭合 typed grammar，生成一个 connected/watertight 的开槽面壳和同一 Part 内的后置 closed backing sub-solid；每个槽前后均有参数驱动的 bevel 深度环，`backing_gap_m` 真实决定背层位置。逐槽贯穿、单连通面壳、312-triangle 精确预算、boundary/non-manifold、strict GLB readback、lineage、determinism、关系/未知字段/预算负门、Runtime Skill trust 与 12-bundle 完整性均通过。该 slice 当时为 195 schemas、23/23 operators、54 read + 36 write = 90 MCP tools。backing 不是独立 semantic Part；当前武器 candidate 未重编译或视觉验收，PBR、人评、package/live、export-restart 与 HQ360 状态不变。receipt：`docs/evidence/mcp010f/fictional-energy-weapon-vent-array-v2-source-gate-20260820.json`。

2026-08-20 此前 `panel@2` source/Skill structural slice：在保留 `panel@1` 兼容语义的前提下，`GeometryProgram@2` 与固定 Geometry Worker 新增闭合矩形硬表面 grammar，真实生成 recessed floor、border band、分段内外 bevel 与双组 support loops。关系越界、未知字段、非有限值、输入 arity、预算和 catalog/hash 漂移 fail closed；strict GLB readback、source-node lineage、Worker 46/46、Runtime Skill trust 4/4 与 12 个声明式 Skill 完整性已通过。该 slice 当时为 195 schemas、22/22 operators、54 read + 36 write = 90 MCP tools。这仅是合法非功能性科幻游戏资产的 source structural capability，尚未做同候选视觉、PBR、人评、package/live、export-restart 或 HQ360 Gate。receipt：`docs/evidence/mcp010f/fictional-energy-weapon-panel-v2-source-gate-20260820.json`。

2026-08-20 Provenance Graph Viewer source slice：新增 closed `ViewerProvenanceGraphRequest@1 → ViewerProvenanceGraph@1`，Runtime 按 exact project/candidate/candidate-state/artifact 重新校验 GeometryProgram/Operator DAG/GLB/ArtifactReadback/GeometryQuality，可选合并已存在且完整校验的 Reference/RenderSet/9 AOV/Compare/Quality 与 immutable MechanicalAnimationClip 摘要。图固定 64 nodes / 128 edges / 1 MiB，完整或失败，Viewer 节点选择与键盘导航只在内存中；不新增 MCP 工具、SQLite/CAS 写入或质量重算。现行总量为 195 schemas、26/26 operators、54 read + 36 write = 90 MCP tools；Modifier Apply/Boolean preview/Subdivision sidecar 的专用持久历史尚无 bounded list，因此明确标记为未投影，不伪造 provenance。这仍只是 source/read-only structural inspector，package/live UI E2E、VoiceOver、visual/human/PBR/export-restart/HQ360 均未升级。

2026-08-20 Mechanical Animation Viewer follow-up source slice：在既有 inventory/detail 之上新增认证只读的 scheduled single-tick frame preview。Runtime 仍执行既有 exact clip/candidate/artifact/cohort 绑定与双 Geometry Worker transient replay；Viewer 只接受完整 hash 绑定的 Part delta，并要求 GLB 对每个预期 Part 恰有一个 identity node owner。embedded animation、Bone/SkinnedMesh、未知/重复/嵌套/nonidentity owner 与旧异步响应全部 fail closed；Viewer 不本地求值姿态，不自动连续播放，不调用 prepare/confirm，也不写 Runtime。该结果仅为 `PASS_SOURCE_READ_ONLY_VERIFIED_DISCRETE_RIGID_FRAME`，package/live UI、VoiceOver、真人/视觉门仍未运行。

2026-08-19 当前 Blender clean-room candidate-bound Modifier Apply source/focused slice：新增 closed `GeometryModifierApplyRequest@1 → GeometryModifierApplyResult@1`，但不增加 MCP 工具；它沿用 opt-in `geometry_prepare`。Runtime 从 durable current-head candidate/CAS 重建 source program/readback，source/derived 双 Worker replay，仅替换一个 stable direct-source Part terminal；Store 用 bounded same-key single-flight、CAS reservation 和最终事务内的 source/derived/sidecar/Part 二次校验原子提交 reviewable candidate/Job/event/audit/evidence/idempotency。Store 24、Runtime 240、MCP 86 tests 与 raw stdio 重启 receipt PASS。当前状态仅为 `PASS_SOURCE_STRUCTURAL_APPROVAL_GATED_CANDIDATE_BOUND_APPLY`；不 confirm/version/export，不改写 `QUALITY_TARGET_NOT_MET`、`INCOMPLETE_TRUTH_BINDING`、camera `MISMATCH` 或 HQ360 blocked 事实。

2026-08-19 historical Blender clean-room Authoring Mesh Edit Prepare source/focused slice：新增 closed `AuthoringMeshEditPrepareRequest@1 → AuthoringMeshEditPrepare@1` 与显式 write-opt-in `authoring_mesh_edit_prepare`。Runtime 重新执行 exact preview，经固定 Geometry Worker、strict GLB readback 与同 cohort 门后，在一个 Store transaction 中提交 reviewable candidate、Job/event/audit、GeometryCandidateEvidence 和幂等回放记录；只把本次新建的临时 CAS 对象纳入失败回滚。成功不 confirm、不建 version、不 export；stale head、跨项目、幂等键漂移、CAS kind/size、Python/path/URL/plugin 字段均 fail closed。当前仅为 `PASS_SOURCE_STRUCTURAL_APPROVAL_GATED_STAGED_CANDIDATE`，不代表完整 mesh editor、BMesh/Blender/Python/plugin parity 或视觉 PASS。

2026-08-19 historical Blender clean-room Authoring Topology/Edit Preview source/focused slice：新增 `AuthoringTopology@1`、`AuthoringMeshEditPreview@1` 与默认只读 `authoring_topology_get`/`authoring_mesh_edit_preview`。Runtime 对 candidate、durable evidence、GeometryProgram object、ArtifactReadback 与固定 Worker GLB 做 exact 重验后，返回单一 direct `authoring-mesh@1` Part 的 source V/E/Loop/Face；preview 仅允许 sorted translate 与 boundary triangle/planar-convex-quad single-face extrude，并用 transient derived program 双 Worker replay/strict readback。公开请求只用 `geometry_prepare` 可获得字段；evidence canonical tamper、stale/cross-scope、interior/non-planar/concave、1 MiB drift及 Python/path/URL/env/plugin/network均 fail closed。source/raw stdio 为 `PASS_SOURCE_STRUCTURAL_RAW_STDIO`，不代表持久 mesh editor、BMesh/Python/plugin parity 或视觉 PASS。receipt：`docs/evidence/mcp010f/blender-authoring-topology-edit-preview-source-gate-20260819.json`。

2026-08-19 historical Blender clean-room Authoring Mesh source/focused slice：新增 active `forgecad.geometry.authoring-mesh@1`，以 ForgeCAD 自有 closed `GeometryProgram@2` 参数显式承载 stable opaque Vertex/Edge/Loop/triangle-or-quad Face ID；固定 Geometry Worker 重验字典序、引用闭包、loop 绕向、edge incidence、非流形、重复/悬空/未使用/退化元素和全部资源上限，再确定性三角化为 GLB。Worker 42 tests、workspace check、11-Part/622-triangle Runtime/MCP raw stdio `geometry_prepare → ArtifactReadback@2` 与 Python 字段负测 PASS；不引入 Blender/BMesh/Python/plugin/runtime dependency。该 slice 尚无 candidate-bound AuthoringTopology read model、translate/extrude edit preview、selection/history 或通用 BMesh；只是 `PASS_SOURCE_STRUCTURAL_AUTHORING_MESH_OPERATOR_ONLY`，不改变视觉质量失败。receipt：`docs/evidence/mcp010f/blender-authoring-mesh-source-gate-20260819.json`。

数值口径：当前 source 仅以本文顶部最新段落和本轮 source/runtime/MCP 证据的 **344 schemas / 26/26 active operators / 79 read + 59 write = 138 tools** 为准；下文出现的 334/134、319/128、290/118、284/116、278/114、271/112、264/110、257/108、231/100、229/99、227/98、221/96、215/94、210/92、204/91、201/90、197/90、195/90、193/90、191/90、187/89、177/84、175/83、173/82、170/80、168/79、166/78、164/78、162/77、160/76 均是 historical prior snapshot，即使旧句仍含“当前/current”也不得解读为现行机器真值；Stage0 marker、current-benchmark 与 compiled source-tool summary 已由主验收同步为同一口径。

2026-08-19 historical source slice — `MechanicalAnimationClip@1` 通过显式 opt-in Runtime prepare 将 bounded rigid pose sequence 固化为 immutable CAS clip 与 SQLite Link；写前实际 Geometry Worker 对 source artifact 做两次 full-GLB byte-exact replay，并要求非空 same cohort。默认只读 get 可跨 Runtime 重启回读；preview 仅允许 immutable schedule 内一个 tick，双 Worker transient replay，且不写 candidate/version/CAS。该 slice 当时为 187 Schema、20/20 active operators、54 read + 35 write = 89；它不是 Blender Armature/skin/IK/NLA/F-Curve/timeline/GLB animation/Python ecosystem，也不改变现有视觉失败与不完整绑定。

2026-08-19 当前 Blender clean-room Render Evidence Replay source/focused slice：新增 closed `RenderEvidenceReplayRequest@1 → RenderEvidenceReplay@1` 与默认只读 `render_evidence_replay_get`。Runtime 先重读 exact candidate/artifact/ArtifactReadback/program/reference/camera/RenderSet/comparison/quality 完整性链，再从 GLB 字节重做 strict readback，调用实际 fixed Render Worker 两次；只有 source/first/repeat 的同 cohort、同 RenderProfile 与有序九 AOV 原始 PNG 字节及 decoded RGBA8 pixel 全部完全一致才返回 PASS。整个流程仅内存 materialization，不写 CAS、SQLite、candidate 或 version，整个 MCP response 受 1 MiB 硬门约束。该结论仅是当前 source 同 cohort 的 structural repeat-byte-exact 证据；材质只能说 artifact-embedded，不证明 authored AppearanceProgram、跨平台确定性、Cycles/EEVEE/OCIO parity、视觉质量或人评。

2026-08-19 historical Blender clean-room Mechanical Pose Geometry Preview source/focused slice：新增 closed `MechanicalPoseGeometryPreviewRequest@1 → MechanicalPoseGeometryPreview@1` 与默认只读 `mechanical_pose_geometry_preview`。Runtime 从 exact candidate/artifact/readback/program/catalog/config/rest/action 绑定计算每个刚性 Part 的 `PoseWorld × inverse(RestWorld)`，只在证明最终 Part output 具有唯一纯 source 归属后，向 transient derived GeometryProgram 追加 `transform@2` sink，再经 fixed Geometry Worker 编译和 strict GLB readback；派生 GLB 只返回 hash/readback，不写 CAS、SQLite、candidate 或 version。Quaternion 仅 lowering 到现有 Worker XYZ Euler，并在 gimbal 附近 fail closed。该 slice 当时为 175 schemas、20/20 operators、49+34=83；现行整体口径见本文顶部 191/90。caller-authored rest frame 不等于原资产 rig/pivot provenance，且不改变 `QUALITY_TARGET_NOT_MET` / `INCOMPLETE_TRUTH_BINDING` / camera `MISMATCH` / `BLOCKED_REFERENCE_COVERAGE`。

2026-08-19 historical Blender clean-room Subdivision artifact-lineage sidecar source/focused slice：新增三个 closed contracts、显式 opt-in `subdivision_artifact_lineage_prepare` 与默认只读 `subdivision_artifact_lineage_sidecar_get`。prepare 只在既有 reconstructed projection 已完成 strict readback 与 fixed-Worker full-GLB byte replay 后，把 canonical sidecar 写入 Runtime CAS，并以独立 SQLite link 绑定 request/project/candidate/artifact/readback/evidence/node/lineage/binding hash；Link 写入/幂等复用与 CAS reachable 标记在同一 SQLite transaction 内，Link 提交失败时只回滚本次新建、仍 temporary 且未引用的 sidecar。这里不宣称 filesystem CAS 与 SQLite 构成跨介质原子事务，通用 GC 仍留给 MCP011。相同请求幂等，不同请求或跨 candidate 漂移 fail closed；getter 重启可读且不懒写。该 slice 当时为 173 schemas、20/20 operators、48+34=82；sidecar 仍是 candidate-local structural lineage，不提供 glTF V/E/C 或跨版本 ID，也不改变 `QUALITY_TARGET_NOT_MET` / `INCOMPLETE_TRUTH_BINDING` / camera `MISMATCH` / `BLOCKED_REFERENCE_COVERAGE`。

2026-08-19 historical Blender clean-room Subdivision artifact-lineage source/focused slice：新增 closed `SubdivisionArtifactLineageRequest@1 → SubdivisionArtifactLineageProjection@1` 与默认只读 `subdivision_artifact_lineage_get`。Runtime 从 durable `GeometryCandidateEvidence@1` 解析 exact project/candidate/artifact/program/readback/catalog/config 绑定，重验 strict `ArtifactReadback@2`，并让 fixed Geometry Worker 对持久化 program 做完整 GLB byte-for-byte replay；只有 replay 与 CAS artifact 完全相同、target `subd-cage@2` 是唯一 direct source primitive、triangle count/order 与 root lineage 对齐时，才将 evaluated quad 映射到 source-primitive-local triangles。投影重启可重建且 getter 零写入，但不是持久 CAS sidecar，不提供 glTF vertex/edge/corner identity 或跨版本稳定 ID。该 slice 当时为 170 schemas、20/20 operators、47+33=80；视觉账本仍为 `QUALITY_TARGET_NOT_MET` / `INCOMPLETE_TRUTH_BINDING` / camera `MISMATCH` / `BLOCKED_REFERENCE_COVERAGE`。

2026-08-19 historical Blender clean-room Subdivision root-lineage source/focused slice：新增两个 closed schemas 和默认只读 `subdivision_topology_lineage_preview`。实际 fixed Geometry Worker 产生 `control-root-to-evaluated-quad-topology@1` 映射，Runtime/MCP 对 exact program/catalog/node、规则网格计数、crease chain、root coverage、canonical、25,000-element 与完整 response 1 MiB 独立 fail closed；candidate/version/CAS 不变。最大 16×16 level2 的 22,802-element 投影仍在 wire budget 内。`complete=true` 只表示声明的 root-lineage preview 无截断；结果明确无 corner/per-level child path/influence weight、无 artifact/readback/GLB identity、无跨版本稳定性。该 historical slice 当时为 168 schemas、20/20 active operators、46 read + 33 opt-in write = 79 tools；现行真值见顶部 191/54/90。`QUALITY_TARGET_NOT_MET` / `INCOMPLETE_TRUTH_BINDING` / camera `MISMATCH` / `BLOCKED_REFERENCE_COVERAGE` 不变。

2026-08-19 historical Blender clean-room crease-aware Subdivision source/focused slice：新增 active `forgecad.geometry.subd-cage@2`、closed `SubdivisionCreaseEvaluationRequest@1` / `Result@1`，并同步 GeometryProgram、OperatorCatalog、Agentic action/critic/repair 与 first-party `hard-surface-detail` Skill cohort。新 Worker 仅执行 3..16 规则开放网格、1..2 level、整数 edge sharpness 1..2、最多 128 条排序唯一 interior edge 和 `edge-only` boundary；actual Worker golden、deterministic GLB、Runtime `geometry_prepare → strict ArtifactReadback@2` 与 MCP read-only authoring round-trip 均 PASS。只读 branch 仍不编译、不写 candidate/CAS/version，真实编译必须显式走 `geometry_prepare`；Modifier Stack v1 不接纳 @2。该 slice 当时为 166 schemas、20/20 active operators、45 read + 33 opt-in write = 78 tools。source Gate 不等于 Blender/OpenSubdiv parity、package/live、Render/AOV、视觉、人评、export/restart 或 HQ360 PASS；质量仍 `QUALITY_TARGET_NOT_MET` / `INCOMPLETE_TRUTH_BINDING` / camera `MISMATCH` / `BLOCKED_REFERENCE_COVERAGE`。receipt：`docs/evidence/mcp010f/blender-subdivision-crease-source-gate-20260819.json`。

2026-08-19 historical Blender clean-room Boolean Operand Lineage source slice：新增 closed `BooleanOperandLineageRequest@1` / `BooleanOperandLineage@1` 与默认只读 `boolean_operand_lineage_preview`。固定 Geometry Worker 对指定 Boolean 节点返回连续 left/right operand 与 evaluated-face runs，Runtime 独立重算 program canonical、target Boolean operation、左右输入及递归 source lineage，并重验 catalog、覆盖、计数、run hash、canonical 和完整 MCP wire 1 MiB 上限；candidate/version/CAS 不变。该 ID 不是原始 authoring face ID，跨 program 变更不稳定，且未写入当前 GLB；不升级视觉质量。该 slice 当时机器真值为 164 schemas、19/19 active operators、45 read + 33 opt-in write = 78 tools。独立 receipt：`docs/evidence/mcp010f/blender-boolean-operand-lineage-source-gate-20260819.json`。

Render Evidence Integrity 独立验收补强：Runtime 现在还必须重读 artifact GLB 与 reference image 原始 CAS bytes，对齐 CAS metadata/hash/size；`ArtifactReadback@2.artifact_id` 必须等于 artifact hash；QualityReport 的八项 observed/direction/threshold/status、visual status、hard gate 和 benchmark eligibility 必须从同一 comparison 重新推导并精确相等。输出 Schema 已封闭 source-byte rows 和 metric-gate rows，JSON CAS 单对象上限为 1 MiB。

2026-08-19 historical Blender clean-room Render Evidence Integrity source slice：新增 closed `RenderEvidenceIntegrityRequest@1` / `RenderEvidenceIntegrity@1` 与默认只读 `render_evidence_integrity_get`。Runtime 对 exact project/candidate/artifact/ArtifactReadback/program/reference/camera/RenderSet/comparison/quality 指针 fail closed，重新读取 JSON CAS，区分 object/canonical hash，逐张重新计算九 AOV 与 comparison mask 的 bytes hash，并严格解码 512×512 RGBA8 PNG；相机只声明 `same_camera_verified`，不声明 fit provenance。该 slice 当时机器真值为 162 schemas、19/19 active operators、44 read + 33 opt-in write = 77 tools。该 historical cohort structural receipt 不修复 attempt35 的 `BT006=FAIL` / `BT009=MISSING`，不提升 `QUALITY_TARGET_NOT_MET`、人评、PBR、export/restart 或 360。receipt：`docs/evidence/mcp010f/blender-render-evidence-integrity-source-gate-20260819.json`。

2026-08-19 historical Blender clean-room Mechanical Pose Sequence Preview source slice：新增 closed `MechanicalPoseSequencePreviewRequest@1` / `MechanicalPoseSequencePreview@1`，复用默认只读 `mechanical_pose_evaluate` 对最多 16 个严格递增整数 tick 生成 deterministic local/world TRS samples；任一 sample、顺序、action 或 lineage 漂移整体 fail closed。该 slice 当时机器真值为 160 schemas、19/19 active operators、43 read + 33 opt-in write = 76 tools；未新增 tool/operator/Skill/Worker/dependency，不写 candidate/CAS/version，也不生成 armature、skin、timeline 或动画资产。receipt：`docs/evidence/mcp010f/blender-mechanical-pose-sequence-preview-source-gate-20260819.json`。该 Gate 仅为 `structural_only`，视觉与发布真值不变。

2026-08-18 historical Blender Parametric Group v2 source slice：新增 closed `ParametricDesignKitRequest@2` / `ParametricDesignKitProgram@2`，复用默认只读 `geometry_program_hash` 将三个不可变 first-party group template lowering 为现有 `GeometryProgram@2`。该 slice 当时机器真值为 158 schemas、19/19 active operators、43 read + 33 opt-in write = 76 tools；未新增 tool/operator/Skill/dependency，不执行 Python/插件/路径/URL/网络，不写 candidate/CAS/version。receipt：`docs/evidence/mcp010f/blender-parametric-group-v2-source-gate-20260818.json`。该 Gate 仅为 `structural_only`；不是 Blender Geometry Nodes runtime 或插件生态完成，视觉事实仍为 `QUALITY_TARGET_NOT_MET` / `INCOMPLETE_TRUTH_BINDING` / camera `MISMATCH` / `BLOCKED_REFERENCE_COVERAGE`。

Mechanical pose source receipt：`docs/evidence/mcp010f/blender-mechanical-pose-source-gate-20260818.json`。该历史 slice 当时为 156 schemas、19/19 active operators、43 read + 33 write = 76 tools。它实现 candidate-bound、read-only 的 rest hierarchy/有限 pose action/evaluated local-world TRS；不 materialize 几何，也不是 Armature、skin 或可播放动画资产。

RenderProfile/AOV historical source receipt：`docs/evidence/mcp010f/blender-render-profile-aov-lineage-source-gate-20260818.json`（canonical `7520b4e7504c5de58bd6794b69315ada495fe1cf7576f96f11c96a665cd01933`）。

三路 Luna 终审的 P1 已闭合：`RenderSet@2` Schema 与 Runtime exact-key/AOV-order/beauty=`srgb`/data=`data` 规则一致；Worker PNG 在进入 RenderSet 前必须实际解码为精确尺寸 RGBA8；Part-ID/Material-ID palette 只接受 index 0..255，超出时 Render Core fail closed。

2026-08-18 historical Blender render/AOV clean-room source slice：新增 closed `RenderProfile@1`，并让 `RenderSet@2`、Render Worker `render_glb` response 与 Runtime 精确绑定同一 fixed `forgecad-fixed-software@2` profile、canonical/AOV/color-pipeline/ID-palette hashes。仅 beauty 执行固定 linear-to-sRGB；silhouette/depth/normal/AO/part-ID/material-ID/wireframe/UV-stretch 均标为 non-color data，depth 明确是 reversed normalized RGBA8、不是 metric distance。该 slice 当时为 152 schemas、19/19 active operators、42 read + 33 write = 75 tools；未新增工具或状态写入，也未引入 Blender、Cycles、EEVEE、OCIO、GPU、EXR、透明 film、custom AOV 或 compositor。该 source/structural slice 不改变 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`INCOMPLETE_TRUTH_BINDING` 与 `BLOCKED_REFERENCE_COVERAGE`；package/live/visual/human 仍未运行。Subdivision v2 的 151-schema receipt 保持历史 cohort。

2026-08-18 historical Blender clean-room Modifier evaluation v2 source slice：新增 closed `GeometryModifierEvaluationRequest@2` / `GeometryModifierEvaluationSignature@1` / `GeometryModifierEvaluationResult@2`，复用默认只读 `geometry_program_hash`。Runtime 分离完整 authoring stack hash、有效 evaluation hash、GeometryProgram output hash、policy/catalog cohort、逐 stage cache key、dirty reason 与 deterministic reuse decision；`reuse_kind=semantic-signature-only`、`output_kind=geometry-program-canonical-sha256` 防止把它误读成持久 mesh cache。禁用项的参数或纯顺序变化保持 trace dirty，但不会改变有效 DAG、program hash 或 cache key；cache key 绑定实际 program output。previous signature 对 project/representation/Part/material/solid fail closed，source base operator/parameters 在 Schema 与 MCP 以 11 个 paired branches 绑定。上一份 signature 仅由调用方 round-trip，不是 durable provenance；本 slice 不编译 mesh、不持久缓存，focused test 证明 candidate/version 与 CAS inventory 不变；SQLite 全表/Job inventory 未直接测试，也不暴露 Runtime/Worker build cohort。该 slice 当时机器真值为 149 schemas、19/19 active operators、42 read + 33 opt-in write = 75 tools；focused contracts/Runtime/MCP/no-persistence/tamper Gate PASS，package/live/visual/human 未运行。receipt：`docs/evidence/mcp010f/blender-modifier-evaluation-v2-source-gate-20260818.json`。其后续 Subdivision v2 已由上一段完成，`FGC-MCP010F` 仍唯一 `in_progress`。

2026-08-18 Blender clean-room Bevel/Normal source slice：新增 active `forgecad.geometry.bevel@1` 与 `forgecad.geometry.normal-policy@1`；该 slice 当时为 146 schemas、19/19 active operators、42 read + 33 opt-in write = 75 tools。Bevel v1 只处理 direct source `primitive@2` box，固定 `all-source-box-edges`、segments 1..4、bounded profile/clamp；Normal v1 只做 corner-domain face-area × face-angle weighting 与显式 crease threshold。`source → bevel → normal-policy` 已通过 300-triangle deterministic GLB、source-box lineage 精确回读、strict readback、UV/MikkTSpace、Runtime/MCP/Skill 与负向 source/overlap/normal-before-Boolean Gate。它仍是 source/structural PASS，不是任意 topology Bevel、Blender parity、packaged/live、visual/PBR/human/export/360 PASS。receipt：`docs/evidence/mcp010f/blender-bevel-normal-source-gate-20260818.json`。

2026-08-18 Blender clean-room TopologySnapshot source slice：新增 closed `TopologySnapshotRequest@1` / `TopologySnapshot@1` 与默认只读 `topology_snapshot_get`。Runtime 从 candidate-bound、hard-gate-passed `ArtifactReadback@2` 对同一 GLB CAS bytes 投影单 Part 的完整 vertex/edge/face/corner、邻接、boundary/non-manifold/orientation、Part/source-node/MaterialZone 和 corner normal/UV/tangent；project/candidate/artifact/readback/program/catalog/config/policy hash 全绑定。每 Part 最多 512 faces/1536 elements，响应最多 1 MiB，超限不截断；ID 仅 `artifact-bound`，source lineage 仅 `partial-operator-node-only`，不宣称 authoring cage/BMesh/跨版本稳定 ID。primitive/SubD/Boolean、确定性、负向绑定/预算和 MCP closed-schema focused Gate PASS。该 slice 当时为 146 schemas、17 active operators、42 read + 33 opt-in write = 75 tools；当前 catalog 真值以上一段 19/19 为准。receipt：`docs/evidence/mcp010f/blender-topology-snapshot-source-gate-20260818.json`。视觉、package/live 与完整 Blender 能力不变。

2026-08-18 Blender reference-only clean-room Modifier Stack source slice：主代理已研究用户 Blender 对话，并由三路 Luna Max 分别审计官方几何、渲染/动画与 ForgeCAD 现状；官方研究冻结到 `blender/blender@72ccdd6e96ca119a1ffa3372559cc5654343b477`，未克隆、安装、执行或复制 Blender GPL 源码。当前新增 `GeometryModifierStackRequest@1` / `GeometryModifierStackProgram@1`，通过只读 `geometry_program_hash` 将 1..8 个有序、唯一 ID、可启停的 transform/mirror/array modifier lowering 到现有 `GeometryProgram@2` DAG；disabled stage 保留 trace 但不改变 effective hash，调用不写 candidate/CAS/SQLite/version。focused contracts/Runtime/MCP Gate PASS；该 slice 当时为 144 schemas、17 个 source-canonical active operators、41 read + 33 opt-in write = 74 tools。下文 136/138/139 schemas 与 16-operator 文案均是其日期对应的 historical cohort/receipt，不覆盖本段当前机器真值。receipt：`docs/evidence/mcp010f/blender-modifier-stack-source-gate-20260818.json`；路线与禁止边界见 `docs/BLENDER_CAPABILITY_ADAPTATION_PLAN.md`。该项仅 `structural_only`；通用 BMesh/Subdivision/Bevel/Normal、EEVEE/Cycles、动画骨骼、Python 生态、package/live/视觉/人评均未完成，质量继续 `QUALITY_TARGET_NOT_MET` / `INCOMPLETE_TRUTH_BINDING` / camera `MISMATCH` / `BLOCKED_REFERENCE_COVERAGE`。

Reference Visual Structure source receipt：`docs/evidence/mcp010f/reference-visual-structure-source-20260817.json`。

2026-08-17 Reference Visual Structure source Gate：`SilhouetteTarget@1` 新增可选、向后兼容的 `ReferenceVisualStructure@1`。它以 `global_contour_authority=true` 固定整件产品外轮廓为最高权威，内部只允许中性的视觉角色、continuity group、layer、共享/覆盖边界、深度证据和开放 line-flow；明确 `functional_part_claim_allowed=false`，不再强迫高科技产品套入枪托/弹夹等功能分类。区域可以重叠，细化全局轮廓后视觉结构 review 会自动回到 `unreviewed`。该对象由 Runtime 生成 canonical hash 并随 silhouette target 进入 CAS，Codex/Python 只提供归一化图像注释，不能直接成为几何真值。该 slice 当时的 focused Runtime/MCP 与 139-schema contract checks PASS，工具为 41 read + 33 opt-in write = 74。尚未将用户参考实际写入该合同，也未重建候选，visual likeness/PBR/human/export/HQ_360 仍 `NOT_RUN/BLOCKED`，现有质量保持 `QUALITY_TARGET_NOT_MET`。

2026-08-17 Fictional Energy Rifle Profile source Gate：新增 `FictionalEnergyRifleProfile@1` / `FictionalEnergyRiflePlan@1`，仅允许 `fictional-game-asset` 且强制 `nonfunctional_asset=true`；Profile 将 bounded macro intents 映射到现有六类 PDK kit/产品自有 Operator，Runtime 只展开已有 `ParametricDesignKitRequest@1` 计划，不写 candidate/CAS/SQLite/version。receipt：`docs/evidence/mcp010f/fictional-energy-rifle-profile-source-20260817.json`。这是 source/structural authoring aid，不是 geometry/readback、visual likeness、PBR、人评、export/restart 或 360 PASS；`HQ_360` 继续 `BLOCKED_REFERENCE_COVERAGE`。该 slice 当时为 138 contracts、41 read + 33 opt-in write = 74 tools；`FGC-MCP010F` 仍是唯一 `in_progress`。

2026-08-17 Weapon joint-multiview optimization source Gate：针对用户激光步枪多视图参考，`FGC-MCP010F` 新增 `SubjectCoordinateFrame@1`（+X 枪口、+Y 上、+Z 右）、`CameraCalibration@2` 正交投影、`CameraRigCalibration@1` 六向正交覆盖、`SilhouetteRig@2`、`OptimizationIntent@2`、`OptimizationEvaluation@2` 与 `OptimizationResult@2`。Runtime 现在以同一批次渲染所有视图，按 view/metric 计算加权与最差损失，并要求所有视图不退化、primary 严格改善、至少三视图严格改善、加权损失严格下降后才允许 staged promotion；MCP 只做 typed 适配与 canonical rebind。该 slice 当时为 136 contracts、41 read + 33 opt-in write = 74 tools。source-only receipt：`docs/evidence/mcp010f/weapon-joint-multiview-optimization-source-20260817.json`。focused tests、Render Core、MCP 与 Stage0 truth 通过；完整 Runtime lib 仍受隔离 Geometry/Render Worker 环境阻断（162 passed / 15 failed / 12 ignored）。没有真实武器参考多视图执行、PBR/人评/export-restart/360 证据，质量仍 `QUALITY_TARGET_NOT_MET`/`BLOCKED_REFERENCE_COVERAGE`。

2026-08-17 Weaponry laser-rifle reference intake：本轮用户授权多视图激光步枪参考（1491×1055，SHA-256=`1964704a62ed7a841b4d49c370b8d46f4626e201daad29092a9c39a40b4c4109`）已完成 Codex-side inventory；17 个细节经现有 16-entry OperatorCatalog 与离线 MaterialPack 校验通过（14 observed、1 inferred、2 unknown）。参考库存、验证回执和有界设计顺序见 `docs/evidence/mcp010f/weaponry-laser-rifle-reference-inventory-20260817.json`、`docs/evidence/mcp010f/weaponry-laser-rifle-reference-inventory-validation-20260817.json`、`docs/evidence/mcp010f/weaponry-laser-rifle-reference-intake-plan-20260817.json`。该项仍是 `reference-canvas` 编排状态：当前会话能力广告为可写但实际 callable tool surface 缺少 reference import/geometry prepare/appearance prepare/reference compare，故 `BLOCKED_LIVE_WRITE_TOOL_EXPOSURE_MISMATCH`，没有 Runtime project/candidate/CAS/Geometry/Appearance/confirm/version 写入，不能宣称 visual likeness/PBR/high-quality PASS；cyan emissive 与 gold accent 也保持 material gap。

2026-08-17 Weaponry Blockout 01 structural slice：原创模块化科幻步枪的 hash-free `GeometryProgram@2` draft 已通过同 cohort product-owned Geometry Worker 的 catalog/hash 校验、编译、GLB strict readback 与固定 Render Worker preview；12 semantic Parts / 17 nodes / 1932 triangles。该结果只保留 hash receipt，未进入 Runtime candidate/CAS/version，未导入用户参考；visual likeness、PBR、人评、export/restart 和 HQ_360 仍 `NOT_RUN/BLOCKED`。详见 `docs/evidence/mcp010f/weaponry-modular-rifle-blockout-receipt-20260817.json`。
2026-08-17 Weaponry Primary Form 02 structural slice：在同一 12 semantic Parts 和同 cohort Worker 上完成受限 profile/panel/joint primary-form 轮廓与 AppearanceProgram@1 材质区修正；25 nodes / 2780 triangles，GLB strict readback、UV/tangent、9 AOV 512×512 与 deterministic repeat PASS。该结果仍为离线 structural preview，未进入 Runtime candidate/CAS/version/confirm/export；没有用户武器参考，visual likeness、人评、生产导出和 HQ_360 继续 `NOT_RUN/BLOCKED`。详见 `docs/evidence/mcp010f/weaponry-modular-rifle-primary-form-optimization-receipt-20260817.json`。
2026-08-17 Weapon Detail Grammar + Close-up Detail Evidence structural slice：新增 Codex-side detail grammar 与局部近景计划，不激活产品 Skill、不写 Runtime/CAS。Primary Form 02 GLB 已完成 receiver/energy/barrel/stock/optic 六组 512×512 九 AOV 近景渲染；receiver、barrel、optic/vent 结构可观察，energy-core 仍 `OCCLUDED_NOT_OBSERVABLE`，stock 仅 `PARTIAL_FRAMING_STRUCTURAL_ONLY`。feature-line F1、detail-density、bevel consistency、human review 与 reference comparison 均 `NOT_RUN/BLOCKED`。详见 `docs/evidence/mcp010f/weaponry-detail-grammar-20260817.json`、`docs/evidence/mcp010f/weaponry-closeup-detail-plan-20260817.json`、`docs/evidence/mcp010f/weaponry-closeup-detail-evidence-receipt-20260817.json`。

2026-08-17 OperatorCatalog historical reconciliation：当前 source catalog 为 19 entries / 19 active；2026-08-17 package/isolated raw receipt 精确校验其 16-entry historical cohort，中间 17/17 也仅为后续 historical source slice；`boolean@1` 仅开放同一 Part 的 bounded union/difference/intersection，任意 mesh Boolean、跨 Part 或无 lineage/预算 Boolean 仍不可用。旧 13/12 receipt 不改写，只标为历史 cohort。raw receipt：`docs/evidence/mcp010f/current-operator-catalog-reconciliation-20260817.json`。

2026-08-17 raw probe socket-path repair：B/C/D/E/Optimization raw probes 改用短绝对临时路径，避免 macOS `/private/var` canonicalization 造成 Unix socket 超长、Runtime Ready 前启动失败；该项是测试可靠性修复，不代表 live Desktop 重启或视觉质量通过。

2026-08-17 MCP typed-error repair：`GEOMETRY_PROGRAM_HASH_REJECTED` 不再被 MCP 错误降级为 retryable `RUNTIME_UNAVAILABLE`；新增 `ipc_request_errors_keep_typed_codes_without_leaking_details` 回归通过。该项只修复错误分类与重试边界，质量仍 `QUALITY_TARGET_NOT_MET`。

2026-08-17 `FGC-MCP010F` orchestrator dispatch repair：Runtime IPC 已把 `design_stage_run_prepare`/`design_composition_prepare` 接入 typed dispatch，错误族以 bounded code 保持可诊断且不泄露用户文本；同 cohort `906c8decb7aec90e8854bb0b7eb4d650dd3895851b556a62a1c8d16557aa17c8` 的真实授权参考隔离 stage batch 通过 1 个 child ActionRun、`view_spec` 转发、Runtime `RuntimeParameterPatch@1` materialization 与 replay hash equality，source candidate 未变、version=0。严格视觉门仍 `QUALITY_TARGET_NOT_MET`，live Desktop restart、真实多视图、PBR、人评、export/restart、HQ_360 仍 `NOT_RUN/BLOCKED`。receipt：`docs/evidence/mcp010f/dev-app-install-orchestrator-dispatch-fix-20260817.json`、`docs/evidence/mcp010f/current-source-real-reference-orchestrator-dispatch-fix-20260817.json`。

2026-08-17 PDK v0 source Gate：新增 `ParametricDesignKitRequest@1`/`ParametricDesignKitProgram@1` 两个合同，并在只读 `geometry_program_hash` 上接入六类 Runtime-owned typed macro（housing/panel/frame、vent、joint、sensor）。宏展开固定 Operator Catalog、有限参数/几何关系、单节点/单 Part 输出和 exact source map；input/representation/program/canonical hash 均绑定。Runtime deterministic/no-persistence negative tests、MCP oneOf schema/read-only test、cargo check、129-schema checker 与完整 `script/test_mcp010f.sh` source Gate PASS。源码变更后 Dev.app 以 cohort `6f00a58a…ed50` 重建/安装，隔离 package probe 和 packaged PDK read-only round-trip PASS。receipt：`docs/evidence/mcp010f/parametric-design-kit-v0-source-gate-20260817.json`、`docs/evidence/mcp010f/dev-app-install-pdk-v0-20260817.json`、`docs/evidence/mcp010f/dev-app-probe-pdk-v0-20260817.json`、`docs/evidence/mcp010f/dev-app-probe-pdk-packaged-read-only-20260817.json`。当前仅为 structural authoring aid；没有 candidate/CAS/SQLite/version/visual compare，live Desktop restart 和真实质量门仍 `NOT_RUN/BLOCKED`。

2026-08-17 historical package-cohort raw manifest/OptimizationJob recheck：以最终 Dev.app cohort `ce45110e3a5e6eaa5b5283e61f430e2338c7f06a2d09f4e75d4a21cb924f6a86` 运行隔离 raw stdio probe；MCP/Runtime cohort exact-match，`tools/list` 已按生成的 source manifest 做 exact-name equality（41 read + 33 opt-in write = 74，manifest canonical `7ced259e…c526b609`），`OptimizationJob` 完成 `32 coarse + 4 mid + 3 final` 共 39 次评估，Job/result=`succeeded/done`，proposal=`blocked-no-improvement`、`strict_improvement=false`，未 confirm/version/export，`persistent_user_data_touched=false`。receipt：`docs/evidence/mcp010f/optimization-raw-manifest-dynamic-20260817-receipt-v2.json`。该收据只证明该历史 package cohort 的 manifest/Job/IPC 结构与 fail-closed 传输，不升级真实机器人 likeness、PBR、人评或 `QUALITY_TARGET_NOT_MET`。

2026-08-17 P2 typed surface-finish package：`hard-surface-finish-v1` 已由当前源码重建并安装为 Dev.app cohort `ce45110e3a5e6eaa5b5283e61f430e2338c7f06a2d09f4e75d4a21cb924f6a86`；同 cohort 组件、ad-hoc deep-strict、隔离 Runtime Ready、Ponytail preflight、project create/readback 与无持久用户数据副作用均 PASS。receipt：`docs/evidence/mcp010f/dev-app-install-parametric-surface-finish-20260817.json`、`docs/evidence/mcp010f/dev-app-probe-parametric-surface-finish-20260817.json`。Codex Desktop restart 仍 `NOT_RUN`；这不是 live authoring、Panel finish likeness、PBR、人评、export/restart 或 HQ_360 PASS。

2026-08-17 P2 typed surface-finish parameter slice：`RuntimeParameterPatch@1` 新增 `hard-surface-finish-v1`；Panel 的 `thickness_m/bevel_m` 与 SurfaceShell 的 `thickness_m` 现在可由单 Part typed action 有界修改，并在 Runtime 侧校验 Panel 尺寸关系，混合族/歧义节点/越界关系 fail closed。源代码、MCP schema、focused tests、129-schema/13-agentic-contract checker 和完整 MCP010F source Gate 通过；receipt：`docs/evidence/mcp010f/parametric-surface-finish-parameter-patch-source-gate-20260817.json`。该条目是 source-only capability，不提升当前 `QUALITY_TARGET_NOT_MET`，不声称 Parametric Design Kit v0、真人 PBR/likeness、package/live restart 或 HQ_360 已完成。

2026-08-17 P2 camera/observation binding follow-up：RenderSet@2 已嵌入并校验 `camera_object_sha256`，Runtime 生成的 RepairIntent 与真实探针均绑定同一 `observation_sha256`。cohort `18c9fb86cafc7e7baf2356e2efe9db404e6530fdeae54d33c0a9beba94fbae40` 的 Dev.app install 与真实授权参考隔离回归 PASS；RepairIntentRun 已完成 render/compare/evaluate 后因 `QUALITY_TARGET_NOT_MET` blocked，未 confirm/version/export，源 candidate 未变。receipt：`docs/evidence/mcp010f/real-reference-repair-intent-run-observation-cas-20260817.json`。

2026-08-17 P2 RepairIntent run source Gate：`repair_intent_run_prepare` 已把 CAS `RepairIntent@1`、观察/证据/参考/相机/候选绑定与既有 bounded `compile → readback → render → compare` 收口为 approval-gated、只产 staged candidate 的 Runtime action；调用方不能覆盖 CAS intent，结果明确 `source_candidate_unchanged=true`、`confirm_allowed=false`。合同/MCP boundary/Runtime focused/full source Gate 通过，最终 Dev.app packaged positive transport 也已通过；相机 CAS 与 observation hash 修复后，真实参考已完成 `render → compare → evaluate`，随后因严格视觉门 `QUALITY_TARGET_NOT_MET` blocked。receipt：`docs/evidence/mcp010f/repair-intent-run-source-gate-20260817.json` 与 `docs/evidence/mcp010f/real-reference-repair-intent-run-observation-cas-20260817.json`。`repair_apply_prepare`、confirm/version/export 与 live Desktop restart 仍未运行；质量继续 `QUALITY_TARGET_NOT_MET`。

2026-08-17 P2 package：Dev.app 已由当前源码安装为最终 cohort `18c9fb86cafc7e7baf2356e2efe9db404e6530fdeae54d33c0a9beba94fbae40`，install PASS；`docs/evidence/mcp010f/real-reference-repair-intent-run-observation-cas-20260817.json` 记录 `PASS_REPAIR_INTENT_RUN_REAL_REFERENCE`（transport 完成至 evaluate、quality blocked、无 source candidate mutation），Codex Desktop restart 仍 `NOT_RUN`。旧 cohort `ad4837…` receipt 按历史范围保留。

2026-08-17 core HQ reference coverage source Gate：完整/eligible 的 `ReferenceCanvas@1` 必须包含 `front/back/left/right/rear-three-quarter`；`perspective/top/material/detail` 不能替代 identity views。Runtime/schema/Agentic negative fixture/focused test 通过，receipt：`docs/evidence/mcp010f/core-hq-reference-coverage-contract-gate-20260817.json`。仍未取得真实多视图，HQ_360 继续 blocked。

2026-08-17 reference annotation readiness source Gate：自动 flood-fill 目标现在只允许作为观察/探索证据；只有显式 `user_confirmed=true` 的轮廓、observed Part region、至少 3 个 observed target/view landmarks、observed view regions 和 canonical camera hash 绑定，比较/QualityReport 才能标记 `READY_PARTIAL_VIEW`。自动目标、缺少用户确认或缺少观察字段一律输出 `BLOCKED_USER_CONFIRMATION_REQUIRED` 与 `QUALITY_TARGET_NOT_MET`。Runtime/contract focused tests、Agentic contract checker、MCP010F full Gate 与五项 release baseline 通过；receipt 为 `docs/evidence/mcp010f/reference-annotation-readiness-source-gate-20260817.json`。这是 source/readiness 边界，不是真人评审、真实 robot likeness、PBR、export/restart、Desktop restart 或 360 PASS。

2026-08-17 reference annotation readiness package：上述源码已构建并安全安装为 Dev.app cohort `896105d3ab204babd415738bf66f572ea5c4be41df1ee83a26da3deed6bb42c7`，保留旧包备份；同 cohort 隔离 Ready/project/preflight probe PASS，`persistent_user_data_touched=false`。现有 Codex Desktop 连接未重启，故 live authoring 仍 `NOT_RUN`。

2026-08-17 live mismatch after P1 package：只读 `capabilities_get` 仍来自旧 Runtime `41945cc48f600abb1601e82ed11ec116b5f6c55a6066f6777268c2d540c705c7`/旧 MCP `3e6debdabdb56dff44e630918553dcfbaf9d2ae0503c23d144307eed33c53b34`，与已安装 `896105d3…` 不一致；旧连接仍广告 write tools，故在用户安全重启前禁止任何设计写入。receipt：`docs/evidence/mcp010f/live-cohort-mismatch-after-annotation-package-20260817.json`。

2026-08-17 final-package isolated Primary Form trial：最终 `820a542fabe3503f0b8cf6c86e84f578e7b074d79d07401a9a3f865dc60709b4` 在同一 perspective/canonical camera 上执行 `pelvis → upper-arm-pair` 两步 bounded repair，receipt `docs/evidence/mcp010f/isolated-final-cohort-primary-form-pelvis-upper-arm-20260817.json`（SHA `da3de253f56b484e1c8d9f7f3e98ee32a9c1d6860cd204e123a1a85db4373713`）。每步 64 evaluations；`pelvis` accepted strict improvement，`upper-arm-pair` no improvement；无 confirm/version/export/持久用户数据。当前指标 IoU `0.661785435407`、Boundary F1 `0.249419639555`，质量仍 `QUALITY_TARGET_NOT_MET`，不存在 likeness/PBR/人评/360 PASS 结论。

2026-08-17 final-package isolated real-reference observation：最终 `820a542fabe3503f0b8cf6c86e84f578e7b074d79d07401a9a3f865dc60709b4` 在隔离 Runtime 上完成真实授权 perspective 的 `preflight → import → geometry/readback → ReferenceCanvas/DesignSpec → scene_observe → SilhouetteRig`，receipt `docs/evidence/mcp010f/isolated-final-cohort-observation-20260817.json`，SHA `68d156c662c717e762a1ee61a9a17995236c906a10f53a0ffd58035487ff0423`，状态 `PASS_BOUNDARY_ONLY`。binding=`bound`、无持久用户数据/无无关副作用；仅 perspective，视觉仍 `QUALITY_TARGET_NOT_MET`，PBR、人评、export/restart、VoiceOver、360 仍 `NOT_RUN/BLOCKED`。该 receipt 不代表 live Desktop 已重启，也不代表 likeness PASS。

2026-08-17 package alignment：当前源码/最新 Dev.app package cohort 为 `820a542fabe3503f0b8cf6c86e84f578e7b074d79d07401a9a3f865dc60709b4`，install/probe receipts 已通过；本轮同时加入 MCP 能力广告、`tools/list` 与 `tools/call` 的 cohort fail-closed，隔离 mismatch transport 已实测 41 个只读工具，仍只闭合 source→package structural boundary，Codex Desktop restart 仍 `NOT_RUN`。

2026-08-17 live cohort check：现场 `capabilities_get` 为 `build_cohort_match=false`；已安装 package 为 `820a542fabe3503f0b8cf6c86e84f578e7b074d79d07401a9a3f865dc60709b4`，现场旧 Runtime 为 `41945cc48f600abb1601e82ed11ec116b5f6c55a6066f6777268c2d540c705c7`，live MCP 为 `3e6debdabdb56dff44e630918553dcfbaf9d2ae0503c23d144307eed33c53b34`。preflight 只读 PASS，但设计动作在 Codex Desktop 重启并重新绑定前保持阻断。receipt：`docs/evidence/mcp010f/live-cohort-mismatch-capability-package-20260817.json`。

2026-08-17 authoring action boundary：默认 ReferenceCanvas/DesignSpec 只用于观察和未知记录；Repair、Primary Form 和其他 stage action 在 Runtime 编译/比较前必须消费显式 `bindings.status=bound`，并验证 per-view target/mask、ViewSpec canonical hash 与已知 camera。正向显式 authoring action、默认 fallback 负向回归、MCP010F full Gate 和 package/probe 通过；该边界不产生新的视觉质量、PBR、人评、export/restart 或 360 PASS。

2026-08-17 per-view ReferenceCanvas annotation source Gate：每个参考 view 可携带独立 `ReferenceViewSpec@1`、target/mask CAS hash 与 camera canonical hash；Runtime authoring/evaluation/cross-view compare 均做同 view/reference exact binding，RepairIntent 的 evaluation kind 集合与 `coverage.supplied_views` 必须一一对应，且每个 supplied kind 恰好对应一个 authored view card，duplicate/extra/missing kind 均 fail closed，projection 回读相应字段。contracts、negative fixture、Runtime/MCP focused tests、Dev.app package/probe 与 `script/test_mcp010f.sh` 通过；receipt 为 `docs/evidence/mcp010f/reference-coverage-exact-binding-20260817.json`。没有真实多视图用户参考的新 receipt，质量仍 `QUALITY_TARGET_NOT_MET`，`HQ_360=BLOCKED_REFERENCE_COVERAGE`，PBR、人评、export/restart、VoiceOver/UI E2E 仍 NOT_RUN/BLOCKED。

2026-08-17 ReferenceCanvas lineage hardening：`ReferenceCanvas@1` 现在必须携带 `bindings`，并由 Runtime fail-closed 校验 `target_sha256`、`camera_hash`/canonical hash、`evidence_sha256` 与当前 session/reference/CAS 的 exact lineage；unbound 文档只能显式保留全 null，不能伪装为已绑定。authoring canonicalizer 同时兼容整数/浮点 JSON 的数值等价重放，避免 Codex 序列化造成无意义 hash 漂移。contracts、Agentic fixture、source Gate、同 cohort Dev.app package/probe 与真实 explicit Codex authoring 均已通过；CAS-bound RepairIntentRun transport 另已通过但在 camera evidence gate blocked；receipt 为 `docs/evidence/mcp010f/supplemental/real-codex-cli-current-20260817-reference-lineage-primitive.json`，明确 `binding_status=bound`、`EXPLICIT_CODEX_AUTHORING`、`persistent_user_data_touched=false`。该 PASS 只覆盖参考/相机/DesignSpec/Observation lineage，不覆盖视觉 likeness。该 slice 当时源码为 144 Schema、41 read + 33 opt-in write = 74 tools；视觉仍 `QUALITY_TARGET_NOT_MET`，Stage 0 历史 `camera=MISMATCH`/`BLOCKED_INCOMPLETE_BINDING`、PBR、人评、export/restart/360 状态不变。

2026-08-17 Viewer candidate/export boundary Gate：新增 source checker 要求前端实际 Runtime 调用严格等于 8 个 read-only Viewer 命令，Tauri 注册表与之 exact-match；“保存对比快照”只允许本地 Canvas/data URL 下载，不得调用 Runtime write/export/confirm。显式 root Cargo test-only Worker feature 后，Tauri Viewer IPC/source tests 6 passed / 0 failed；不带 feature 的生产式测试仍会诚实返回 GEOMETRY_WORKER_UNAVAILABLE，不会用 fallback 冒充 Worker。该 Gate 不等于 packaged UI E2E、正式 artifact export、VoiceOver 或真人质量 PASS，后者仍 NOT_RUN/BLOCKED。

2026-08-17 typed pose-control source Gate：`SilhouetteRig@1`/`SilhouetteRigHashRequest@1` 现在支持 `rotation_x`/`rotation_y`/`rotation_z` 与 `radian` 单位；Runtime 将旋转限制在 ±2π，并只写入最终 `forgecad.geometry.transform@2` 输出节点，源 Part/镜像拓扑保持不变。Agentic Primary Form typed action 也接受 `head-rotation-y` 等受限参数；错误单位、越界值和非有限值 fail closed。4 个 Runtime SilhouetteRig、9 个 Runtime materialization、1 个 Agentic action、1 个 MCP schema focused tests 通过，`script/test_mcp010f.sh` source Gate 通过。为保持现有 `40 geometry` 预算，默认 detail Rig 仍为 36 controls，姿态控制目前是可调用的 typed materializer 能力，尚未加入默认自动搜索；没有新的真实 likeness receipt，质量仍 `QUALITY_TARGET_NOT_MET`，历史 Stage 0 `camera=MISMATCH`/`BLOCKED_INCOMPLETE_BINDING`、PBR、人评、export/restart/360 状态不变。

2026-08-16 ReferenceCanvas/DesignSpec 编排状态：视觉回合现在按 `target → camera → comparison → Runtime authoring → consolidated observation` 执行；脚本生成并提交 hash-bound `ReferenceCanvas@1` 与 `DesignSpec@1`，随后要求 `session.session.observation_sha256 == scene_observe_get.canonical_sha256`，避免 Codex 再从零散工具结果拼接现场。Agentic/ForgeCAD contracts、Python syntax、Visual Surface 与 MCP010F full source Gate 通过。真实 workspace-write boundary probe 未形成新的 typed receipt（先前 read-only 调试被 `project_create` 取消，后续隔离 host 超时并清理），所以此处只记 source/contract PASS；live authoring 仍 `BLOCKED/NOT_RUN`，质量仍 `QUALITY_TARGET_NOT_MET`，Stage 0 incomplete-binding、PBR、人评、export/restart/360 状态不变。

2026-08-16 current real-reference CADFit/Surface/Manifold status：同一隔离 cohort 完成真实授权机器人参考的 39 次 `128→256→512` `OptimizationJob`；camera producer round-trip、Runtime/Geometry/Render Worker、Critic、Part-error 与 product-owned Manifold Boolean lane 通过。`surface_control_point` materializer 修复后，多点候选确实发生 typed 写入并由 `surface_changed_control_point_counts` 回读；质量证据账本已绑定 receipt `docs/evidence/mcp010f/supplemental/optimization-job-real-reference-20260816-unified-objective-manifold-surface.json`。最终比较 `QUALITY_TARGET_NOT_MET`（IoU `0.756284113105`、Boundary F1 `0.331999375494`），不是 likeness/HQ PASS；proposal `blocked-no-improvement`，无 confirm/version/export，Stage 0 历史 incomplete-binding truth 保持不变。下一项是同 lineage 的 Viewer RenderSet/read-model projection 验证。

2026-08-16 current Viewer read-model status：同 cohort Viewer 在隔离 Runtime 上实际读回 1 project/1 candidate/0 version，candidate/artifact/reference/RenderSet/comparison/QualityReport exact-match；Runtime 质量仍 `QUALITY_TARGET_NOT_MET`、hard gate=false，Viewer read-only/no persistent side effect。receipt `docs/evidence/mcp010f/supplemental/viewer-read-model-real-reference-20260816.json` 已纳入 current-quality ledger；formal packaged UI E2E、VoiceOver、人评、export/restart/360 仍未运行或阻断。

2026-08-16 Manifold Boolean current raw Gate：当前隔离 Geometry Worker 通过真实 vendored Manifold C ABI/FFI 执行 bounded same-Part `union`、`difference`、`intersection`；MCP010D raw receipt `docs/evidence/mcp010d/raw-stdio-subd-cage-20260815.json` 记录 `16 entries / 16 active`、9 个 semantic Parts、588 triangles，三个 Geometry Worker Boolean/lineage/tangent focused tests 通过。该 Gate 只证明产品自有 Worker 的结构、lineage、预算与 transport；不开放任意 mesh Boolean，也不证明视觉/PBR likeness、export/restart、真人或 360 通过。历史收据中的 unavailable/intersection deferred 文案不覆盖当前源码状态。

2026-08-16 CADFit OptimizationJob raw stdio Gate：修复真实 Runtime IPC 漏挂 `optimization_job_prepare/get/resume` 后，补齐 Render Worker 的 `256px` mid-fidelity 合同，并让 synthetic fixture 为 Runtime-owned output `transform@2` 派生节点预留 `max_nodes`。当前源码/隔离二进制通过完整 `OptimizationJob`：`32 coarse + 4 mid@256 + 2 final proposal + 1 final baseline = 39` evaluations，`checkpoint_sequence=39`、`next_stage=done`、`job/result=succeeded`、`best_evaluation_fidelity=final`；本次 `proposal_status=blocked-no-improvement`、`strict_improvement=false`，所以没有 proposal candidate、confirm、version 或 export，`persistent_user_data_touched=false`。receipt 为 `docs/evidence/mcp010f/supplemental/optimization-job-raw-20260816-mid-resolution.json`。这是 Runtime/Geometry/Render Worker/Job/Checkpoint source+transport PASS，不是机器人 likeness 或高质量视觉 PASS；真实参考质量、人评、PBR、export/restart 与 360 状态不变。

2026-08-16 Primary Form async Job real Codex replay：在同一源码 cohort `4661b8d94fc2b72819cb09b1503ea5112a328a40edec7b227a8814b3194f6497` 上，真实 Codex 已完成 `primary_form_repair_job_prepare → job_get` 轮询 → `job_result_get`；Runtime-owned Job 实际执行 64 次有界评估，Geometry/Render Worker、strict readback 与 same-camera acceptance 均完成，`camera_binding=PASS_SILHOUETTE_FIT_TO_COMPARE`，source/proposal loss `0.465026451428 → 0.447063964053`，并准备 `staged_new_candidate`。最终轮廓指标为 IoU `0.756928086147`、Boundary F1 `0.392553090172`，但质量仍 `QUALITY_TARGET_NOT_MET`、hard gate=false、review=`needs_revision`；未 confirm/version/export，`persistent_user_data_touched=false`、无无关副作用，九 AOV 调用已完成。该 receipt `docs/evidence/mcp010f/supplemental/real-codex-cli-current-20260816-primary-form-async-job.json` 是 supplemental transport/convergence evidence，不替换 Stage 0 保留的历史 incomplete-binding benchmark，也不宣称视觉高质量通过。

2026-08-15 Agentic durable design-document readback：`AgenticSessionResult@1` 现在携带同一 session binding 下的 CAS-backed `ReferenceCanvas@1` 与 `DesignSpec@1`；Runtime 对两个文档执行 canonical/schema/project/reference/canvas 校验，任何 hash drift 都 fail closed。focused session/action regression 通过，未增加工具或视觉结论；live MCP 仍需重启，质量继续 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`。

2026-08-15 clean package cohort：提交 `cf329215` 后重新构建的 Dev.app 使用干净源码，四个 packaged executable 共享 cohort `aa0d6dcb…5808e`，`source_worktree_dirty=false`；隔离 Runtime `Ready`、project create、Ponytail preflight 与 packaged MCP010B raw stdio 均 PASS。该 package structural Gate 不等于 live MCP rebind 或视觉 likeness PASS，live session 仍需重启；质量继续为 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`。

2026-08-15 Primary Form adaptive local line search：Runtime 已修复大 Rig 失败路径中“无 reverse/fine 预算”的实际漂移。36-control detail Rig 现在在 `40 geometry` 内执行 `joint + 0.5 whole-proposal backtrack + 0.5/1.0 dominant-Part coupled hypotheses + 36/36 scalar coverage`；小 Rig 动态保留 `0.5/0.25` backtrack 与一个 coupled recovery。focused Runtime `22 passed`，receipt 为 `docs/evidence/mcp010f/primary-form-adaptive-line-search-20260815.json`。该 source/convergence Gate 不等于真实视觉收敛，质量仍 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`，人评/PBR/export-restart/360 未运行或阻断。

2026-08-15 live/package cohort alignment：当前 `abae43f3` 已重建并安装到用户级 `ForgeCAD Runtime Dev.app`，四个 packaged executable 共享 cohort `5a1f108a…e2dd2f`；ad-hoc deep-strict、四资源 allowlist、隔离 Runtime `Ready`、project create 与 Ponytail preflight probe 通过，`persistent_user_data_touched=false`。安装/probe receipt 为 `docs/evidence/mcp010f/dev-app-install-live-cohort-20260815.json` 与 `docs/evidence/mcp010f/dev-app-live-cohort-probe-20260815.json`。当前 Codex 进程中的已连接 MCP 仍是旧 `7f9e4c…ee518`/旧 manifest `05fca3…d4d0a`，因此不能把新包安装 PASS 当作 live authoring PASS；需要新 MCP 会话后再复跑真实参考闭环。视觉质量仍 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`。

2026-08-15 Primary Form resolution-consistent fit：`silhouette_fit_prepare` 的 Runtime-owned camera/geometry search 已与最终 same-camera acceptance 统一为 512×512 isolated Render Worker fit batch；普通 camera 粗搜保持 128×128 bounded accelerator。该修复解决 fit objective 与 QualityReport acceptance 的分辨率漂移，不新增 candidate/version/CAS 或真实视觉 likeness 证据；权威状态仍 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`，人评/PBR/export-restart/360 未运行或阻断。

2026-08-15 Viewer Agentic evidence binding hardening：Viewer 在已有完整视觉证据绑定时，不再接受 Runtime Agentic projection 缺少任一 artifact/reference/render-set/comparison/quality hash；完整等值才标记 projection ready，缺失/漂移为 `AGENTIC_EVIDENCE_BINDING_MISMATCH`。Viewer source Gate、Node behavior check、desktop typecheck/build 与 MCP010F full Gate 通过。无新增 Schema/tool/CAS 或真实视觉 receipt，`QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`、人评/PBR/export-restart/360 状态保持不变。

2026-08-15 Render Worker fallback boundary hardening：Runtime 的 perspective/fixed/fit-batch `render-core` fallback 从 `cfg(test)` 隐式路径收口到独立 `test-render-worker-fallback` feature；生产 `cargo check` 不启用该依赖，legacy Runtime/MCP 单测只有在命令行显式启用时才可使用 fallback。source ownership checker、Runtime full `122 passed / 0 failed / 12 ignored`、MCP `56 passed / 0 failed`、MCP010C/F 聚合 Gate 和 Stage 0 truth freshness 通过。该模块不新增视觉 likeness、PBR、人评、export/restart 或 360 证据，仍保留 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`。

2026-08-15 Agentic canonical observation MCP dispatch closure：`InProcess` adapter 现在强制把 `observation_sha256` 交给 Runtime 的 bound stage/critic/evidence projection；此前开发/测试路径会调用未绑定 stage/critic 方法，且缺少 visual-evidence 分支，可能重新构建碎片观察。新增 stale/missing hash regression，MCP 全量 `56 passed / 0 failed`；没有 Runtime/CAS 或工具数变化，也没有新的真实 likeness receipt。质量仍 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`，人评/PBR/export-restart/360 未运行或阻断。

2026-08-15 Primary Form output-level offset sink：Runtime 对 mirror/array 输出的 camera-plane offset/scale 采用复用的最终 `forgecad.geometry.transform@2` sink；不再把双侧 Part 的位移写入镜像前源节点，避免一次 typed offset 被解释成左右反向张开。mirror/direct-output materialization regressions 通过，Stage 0 Runtime source hash 已同步；没有新的真实 likeness receipt，`QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING` 与未运行的人评/PBR/export-restart/360 保持不变。

2026-08-15 Primary Form exact evaluation ledger：Runtime 的 64 evaluation ceiling 现在与真实 bounded schedule 一致，为 `40 geometry + 15 initial camera + 9 winner refit`；每个 refit camera row 在同一 source/proposal Render Worker batch 完成后都计入 `camera_evaluations`/`evaluations`，不因 proposal 被 strict same-camera gate 拒绝而漏计。focused budget 与 Primary Form fixture 通过，未引入新 Schema/tool/CAS 写入。该 source/convergence Gate 不产生新的真实 likeness receipt，当前仍为 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`INCOMPLETE_TRUTH_BINDING`/`BLOCKED_INCOMPLETE_BINDING`，人评/PBR/export-restart/360 未运行或阻断。

2026-08-15 Primary Form 单 Part orchestration source Gate：`scripts/probe_mcp010f_part_correction.py` 已从 Codex/Python 本地连续候选搜索收口为一次 Runtime-owned `primary_form_repair_prepare`；它先取得同相机 baseline compare 和唯一 canonical `scene_observe_get`，再提交 Runtime 返回 hash-bound `prepared`/`no_improvement` 结果。旧 fraction loop、局部 GeometryProgram patch、`silhouette_candidate_compare` 和本地 metric retention 已移除；`script/test_mcp010f.sh` full Gate、desktop typecheck/build 与 Runtime focused tests 通过。此处没有真实授权参考复跑，因此不更新 likeness 或 benchmark：当前仍 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`INCOMPLETE_TRUTH_BINDING`/`BLOCKED_INCOMPLETE_BINDING`，人评/PBR/export-restart/360 未运行或阻断；旧单部件 receipts 仅作历史记录。

2026-08-15 Primary Form profile-loft height sink Gate：Runtime 将 `profile-loft@1` 的 Rig `height` 绑定到 `profiles[*].height_m` 的真实纵向站点，并保留截面点深度；`profile-extrude` 路径未改变。7 个 materialization tests、Runtime 全量 `122 passed / 0 failed / 12 ignored`、MCP010F full Gate 与 Stage 0 checker 通过。该修复只关闭 typed 参数落点缺口，不升级视觉质量：当前仍为 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`INCOMPLETE_TRUTH_BINDING`/`BLOCKED_INCOMPLETE_BINDING`，人评/PBR/export-restart/360 未运行或阻断。

2026-08-15 Agentic durable lineage binding：`validate_observation_claims` 现在逐项校验 observation lineage 与当前 Candidate/Reference canonical records；session/checkpoint/action 不能拿缺失或漂移的参考/候选观察继续执行。exact-lineage、Agentic projection 和 bounded action 回归通过；无新增 Schema/tool/CAS 写入，视觉真值保持 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`。

2026-08-15 Agentic observation consolidation status：`AgenticSceneObserveResult@1` 现在由 Runtime 进程内 bounded hash cache 保持为一次完整只读快照；bound plan/critic/evidence/action 不再在同一 Runtime 会话重新构建观察。cache 只存在内存，重启后重建仍须通过 canonical hash/scope 校验；没有新增 Schema、tool 或 durable user-data 写入。focused Runtime tests 通过，真实视觉状态仍 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`。

2026-08-15 Primary Form composition-lineage status：`--part-contour-sequence` 现生成并校验 `ForgeCADPrimaryFormCompositionLineage@1` compact orchestration receipt；每个 serial step 绑定 source/observation/target/camera/Rig/intent/fit-camera hash，第二步起必须先通过 prefix lineage 校验才允许下一步消费 candidate，只有 `prepared + strict_improvement` 才推进，`no_improvement` 保留 source。该 projection 只属于 CLI 编排与证据投影，不写 Runtime/CAS，也不改变 Viewer Runtime-only QualityReport；缺少授权 PNG 时真实 composition 仍 `NOT_RUN/BLOCKED_REFERENCE_BYTES_UNAVAILABLE`，`QUALITY_TARGET_NOT_MET`、`camera=MISMATCH` 与 `BLOCKED_INCOMPLETE_BINDING` 保持不变。

2026-08-15 Primary Form modular composition source status：CLI `--part-contour-sequence` 已把多 Part 修复限定为 2–3 个 exact Part 的 serial Runtime-owned steps；每个 step 都重新读取 current candidate 的 consolidated observation，并把 staged candidate 的 candidate/program/job lineage 绑定到下一步，未接受 proposal 不得推进。Runtime continuation regression 与 Runtime full `117 passed / 0 failed / 12 ignored`、MCP010C source/raw Gate 通过；raw probe 由 Runtime-owned tool manifest summary 驱动计数。Stage 0 Runtime source hash 已同步，`check_mcp010f_stage0_truth.py` 通过；这不改变 `camera=MISMATCH`、`QUALITY_TARGET_NOT_MET`、`BLOCKED_INCOMPLETE_BINDING`。真实组合因没有用户授权 PNG 字节保持 `NOT_RUN/BLOCKED_REFERENCE_BYTES_UNAVAILABLE`，未 confirm/version/export。

2026-08-15 Hip-pair bounded candidate evidence：r21 `docs/evidence/mcp010f/supplemental/real-codex-cli-current-20260815-canonical-observation-hip-pair-r21.json` 在 current packaged cohort `726153a3…42ab5c` 通过 60-evaluation Runtime-owned fit 与 `PASS_SILHOUETTE_FIT_TO_COMPARE`；source/proposal loss `0.426350916959`/`0.426265642648`，严格接受并生成 staged candidate，但最终 compare 为 IoU `0.743473474034`、Boundary F1 `0.301145366407`、bbox `0.0234375`、centroid `0.021831234764`，仍 `QUALITY_TARGET_NOT_MET`、hard gate false。r19/r20/r21 均是独立 candidate trial，未 confirm/version/export；PBR、人评、360 仍未运行或阻断，不替换 Stage 0 provisional truth。

2026-08-15 Chest-shell bounded candidate evidence：r20 `docs/evidence/mcp010f/supplemental/real-codex-cli-current-20260815-canonical-observation-chest-shell-r20.json` 在 current packaged cohort `726153a3…42ab5c` 通过 61-evaluation Runtime-owned fit 与 `PASS_SILHOUETTE_FIT_TO_COMPARE`；source/proposal loss `0.426350916959`/`0.416406210214`，严格接受并生成 staged candidate，但最终 compare 为 IoU `0.739507057324`、Boundary F1 `0.314765400977`、bbox `0.013671875`、centroid `0.020515796935`，仍 `QUALITY_TARGET_NOT_MET`、hard gate false。该 receipt 未 confirm/version/export，PBR、人评、360 仍未运行或阻断，不替换 Stage 0 provisional truth。

2026-08-15 Upper-arm-right bounded candidate evidence：r19 `docs/evidence/mcp010f/supplemental/real-codex-cli-current-20260815-canonical-observation-upper-arm-right-r19.json` 在 current packaged cohort `726153a3…42ab5c` 通过 63-evaluation Runtime-owned fit 与 `PASS_SILHOUETTE_FIT_TO_COMPARE`；source/proposal loss `0.426350916959`/`0.423630597587`，严格接受并生成 staged candidate，但最终 compare 为 IoU `0.744938326895`、Boundary F1 `0.307109823312`、bbox `0.021484375`、centroid `0.021832396893`，仍 `QUALITY_TARGET_NOT_MET`、hard gate false。该 receipt 未 confirm/version/export，PBR、人评、360 仍未运行或阻断，不替换 Stage 0 provisional truth。

2026-08-15 Canonical observation pelvis trial and CLI camera retention：r16 因瞬时 `reference_get` IPC 失败保留为 `BLOCKED`；r17 进一步证明 `no_improvement` 的未采用 proposal camera 不应覆盖 source candidate 的 baseline compare camera。CLI probe 已修复该条件，r18 `docs/evidence/mcp010f/supplemental/real-codex-cli-current-20260815-canonical-observation-pelvis-r18.json` 在 current packaged cohort `726153a3…42ab5c` 通过 `PASS_CAMERA_FIT_TO_COMPARE`，canonical observation 携带 26 个 inferred Parts 且没有 `boundary_error_get`；pelvis Runtime fit 55 evaluations，source/proposal loss `0.435730135362`/`0.438891599478`，严格门失败并保留 source，最终 `QUALITY_TARGET_NOT_MET`。该 supplemental evidence 不替换 Stage 0 `camera=MISMATCH`/`BLOCKED_INCOMPLETE_BINDING` provisional truth，不产生 candidate/version/confirm/export。

2026-08-15 Canonical observation + inferred Part table closure：Runtime `silhouette_part_error` 现在接受 `SilhouetteTarget.parts=[]`，从同一 candidate-bound 512px silhouette/Part-ID boundary evidence 生成最多 64 个 `visibility=inferred` Part rows，并由 `AgenticSceneObserveResult@1.design_critic_report.primary_form_directive.part_error` 一次性携带。CLI 的 silhouette turn 改为 `target → camera_fit → baseline reference_compare → scene_observe → Rig`，不再在观察后追加 `boundary_error_get`。真实 r15 receipt `docs/evidence/mcp010f/supplemental/real-codex-cli-current-20260815-canonical-part-observation-r15.json`（cohort `726153a3…42ab5c`）返回 26 个 inferred rows，`boundary_error.source=canonical_observation`、`boundary_error_get` 调用数为 0；最终仍 `PASS_WITH_QUALITY_TARGET_NOT_MET`，IoU `0.739507057324`、Boundary F1 `0.314765400977`、hard gate false，未 confirm/version/export。该 supplemental receipt 关闭 Codex 观察碎片化证据缺口，但不替换 Stage 0 provisional truth，也不宣称视觉高质量通过。

2026-08-15 Primary Form right-shoulder bounded trial：current packaged cohort `26354e2f…a2029` 的真实 r11 receipt `docs/evidence/mcp010f/supplemental/real-codex-cli-current-20260815-part-contour-trial-r11.json` 对 `shoulder-armor-right` 完成 Runtime-owned exact Part contour → 63 次 bounded fit → Geometry/strict readback → Render Worker same-camera paired compare；source/proposal loss `0.426350916959`→`0.409541676181`，严格改善并准备 `staged_new_candidate`。但全局 compare 仍为 `silhouette_iou=0.749072115206`、`boundary_f1_4px=0.326405552646`，`QUALITY_TARGET_NOT_MET`、`quality_hard_gate_passed=false`、`visual_review=needs_revision`；未 confirm/version/export，未写持久用户数据且无无关副作用。该 receipt 是 supplemental current-cohort evidence，未提升 Stage 0 provisional benchmark truth；人评/PBR/export-restart/360 仍未运行或阻断。

2026-08-15 Primary Form exact-side sink alias hardening：Runtime `materialize_rig_geometry_program` 现在只按固定 `*-armor-left/right ↔ *-left/right` 规则解析单侧 detail sink；`shoulder-armor-left` proposal 不会因实际输出命名为 `shoulder-left` 而静默零改动，也不会触及右侧。新增 exact alias/asymmetric materialization 与既有 bilateral/scope focused regressions 全部通过；无新真实视觉 receipt，`QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING` 与人评/PBR/export-restart/360 未运行或阻断状态不变。

2026-08-15 Primary Form exact single-Part candidate trial：Runtime 为既有 `primary_form_repair_prepare`/Job 增加可选 `part_id`，从 Codex 的 bilateral Rig 派生 Runtime-owned exact Part Rig；连续搜索只接收该 Part 的 bounded controls，随后仍由同一最终 camera、Geometry Worker、严格 GLB readback、Render Worker source/proposal compare 决定是否 staged。r10 receipt `docs/evidence/mcp010f/supplemental/real-codex-cli-current-20260815-part-contour-trial-r10.json` 在 cohort `d2b67cd8…02a2f1` 实际完成 `shoulder-armor-left` trial：`retained_source`/`no_improvement`，source loss `0.426350916959`、proposal loss `0.425568832154`，未创建 candidate/version；后续 `part_contour_fit_prepare` 返回 `proposal_ready`。r10 顶层因 probe 无关副作用为 `BLOCKED`，quality `NOT_RUN`，不升级 `QUALITY_TARGET_NOT_MET`。

2026-08-15 Primary Form 单 Part contour/semantic asymmetry Gate：Runtime 的 `part_contour_fit_prepare` 现在按固定 bilateral alias 规则把显式 `hip-left`/`hip-right` Part-ID 聚合到 `hip-pair`，同时保留精确 `shoulder-armor-left` 这类单侧 Part 的独立 mask、boundary error 和 typed Rig scope；显式单侧 `width/height/offset_x/offset_y` 只影响该侧 primitive，不把连续参数搜索交还 Codex。真实 Codex CLI supplemental receipt `docs/evidence/mcp010f/supplemental/real-codex-cli-current-20260815-part-contour-r8.json` 使用同 cohort `dd78d216…e5088`，26 Parts、`part_contour_fit_prepare=proposal_ready`、`persistent_user_data_touched=false`、无无关副作用；本次为 `PASS_BOUNDARY_ONLY`，part error `23.073555066079px`、全局 IoU `0.743548116218`、Boundary F1 `0.300929937747`，`quality_visual_status=NOT_RUN`，不升级历史 `QUALITY_TARGET_NOT_MET`，也不产生 candidate/version。

2026-08-15 Primary Form 36-control Rig 真实复放：新 packaged cohort `106a2889…67b08e` 的 r7 supplemental receipt `docs/evidence/mcp010f/supplemental/real-codex-cli-current-20260815-primary-form-runtime-owned-r7.json` 通过 current-cohort camera/Render Worker/Viewer read-model 绑定，但 56 次 bounded fit 未改善 authored source；fit loss `0.40710507361`→`0.413276643194`，`acceptance_status=retained_source`、`candidate_state=unchanged`，顶层 `BLOCKED` 由无关 side-effect 账本触发，未写入持久用户数据。最终 compare 仍为 `silhouette_iou=0.746739614479`、`boundary_f1_4px=0.34284083431`、`quality_visual_status=QUALITY_TARGET_NOT_MET`；36-control 参数扩展是 Runtime/Worker 收敛输入补强，不是视觉质量通过。

2026-08-15 Primary Form same-camera acceptance 与真实复放：Runtime 在 geometry-winner camera refit 中对 source/proposal 使用同一最终相机做 paired Render Worker compare，并只允许严格 source loss 改善进入 staged candidate；search-only 的 bounded loss 过渡不会泄漏到最终接受门。packaged cohort `e7e466f6…b7cc` 的 r6 复放保留 authored source：source loss `0.407105`、proposal loss `0.412983`、`acceptance_status=retained_source`、`candidate_state=unchanged`；r6 顶层因无关副作用标为 `BLOCKED`，不作为完成 transport。r4/r5/r6 原始 receipt 归档于 `docs/evidence/mcp010f/supplemental/`。相机到 compare、三组件 cohort、Render Worker 与 Viewer read-model 绑定通过，但质量仍为 `QUALITY_TARGET_NOT_MET`，不得宣称 Primary Form 已视觉收敛。

2026-08-15 QualityReport 与 packaged F structural gate 已修正/验证：Runtime `AppearancePrepare` 在 `visual_status=not-run` 时写入 `hard_gate_passed=false`，与 `QualityReport@2` validator 和 Viewer Runtime-only quality authority 对齐；同一 packaged cohort `fee79807…` 的隔离 raw probe PASS，覆盖 26 parts/4704 triangles、7 material zones、九 AOV、embedded textures、Render Worker cohort 和 Viewer read-model。该结果不包含真实 likeness、人评或用户参考 compare，现有 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING` 及 PBR/export-restart/360 未运行或阻断不变。

2026-08-15 Primary Form 多尺度 geometry convergence 已落地：Runtime-owned `GeometryProgram` bounded search 在 `max_evaluations=64` 时使用 `40 geometry + 15 initial camera + 9 geometry-winner refit`，并把后续 coordinate probe 步长固定为 `1.0 → 0.5 → 0.25`；小预算仍按相同硬上限归一化。该修复让 26-control detail Rig 在一次完整 evidence-directed pass 后继续 deterministic reverse/fine refinement，Codex 不承担连续参数搜索。focused tests 通过；没有新的真实视觉 receipt，当前仍为 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING`，人评/PBR/export-restart/360 未运行或阻断。

2026-08-15 历史 cohort — Primary Form asynchronous convergence Job 已落地：新增 `primary_form_repair_job_prepare`，Runtime 先原子写入 `RuntimeJob@1`/queued event，再在独立 Runtime execution context 中复用现有 `primary_form_repair_prepare` 的 Geometry Worker、strict readback、Render Worker 和 same-camera acceptance；终态结果写入 CAS，`job_get`/`job_events_read`/`job_result_get` 可回读，Job 不 confirm/version/export。该 cohort 工具面为 125 Schema、41 read + 32 opt-in write = 73；当前源码以本账本顶部的 191 Schema、21/21 active operators、54 read + 36 opt-in write = 90 为准，真实质量仍 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING`。

2026-08-15 Render Worker cohort-to-RenderSet evidence 已落地：Runtime 保留 isolated sibling Render Worker 的 `build_cohort_sha256`，并将其绑定到 `RenderSet@2` 的 `render_worker_build_cohort_sha256`/`render_worker_binding_status`；cohort/status 漂移在 Runtime readback fail closed。source build 无 cohort 时显式为 `cohort_unavailable`，packaged same-cohort 才能为 `same_cohort_verified`；Viewer 只读显示该状态，质量门仍只来自 Runtime `QualityReport@2`。该模块通过 Runtime C regression、contracts、Viewer typecheck 与 MCP010C/E probe syntax，不产生新的 likeness、人评、PBR、export/restart 或 360 证据。

2026-08-15 canonical observation durable handoff 已落地：`DesignSession@1` 和 `DesignCheckpoint@1` 新增必需 `observation_sha256`，均绑定同一 `AgenticSceneObserveResult@1.canonical_sha256`；Runtime 在 session/checkpoint/action 的读写边界重新生成观察并对 stale/mismatch fail closed。contracts、Store/Runtime 校验、receipt checker、nested projection checker、隔离 Runtime/MCP restart probe 和 `forgecad-runtime` 全量 `105 passed / 12 ignored` 通过。该模块只消除 observation hash 粒度不一致，不产生新的 likeness 证据、不执行 Repair、不改变 Viewer read-only quality authority；当前仍为 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING`，人评/PBR/export-restart/360 未运行或阻断。

2026-08-15 RepairIntent observation binding 已落地：`RepairIntent@1` 新增必需 `observation_sha256`，Runtime restore prepare 将其绑定到 session/checkpoint 的同一 canonical observation，隔离 Runtime/MCP 重启 receipt 证明读回一致；contracts、Runtime receipt checker、projection checker 通过。该模块只补齐 durable Repair intent 的 hash lineage，不执行 Repair 或完整 orchestrator，也不产生新的视觉质量证据；当前质量和未运行门保持原状。

2026-08-15 Primary Form local-group convergence 已落地：Runtime 在 evidence joint proposal 失败时，先按 Part 将同一局部的 width/height/offset 作为一个 bounded hypothesis 评估，再使用剩余预算做 coordinate refinement；成功候选从同一 incumbent 继续组合，避免单坐标贪心把可共同修复的局部形体拆散。新增 Runtime group ordering/composition focused regression；没有新的真实 likeness evidence，Stage 0 仍为 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING`。

2026-08-15 Primary Form ranking snapshot 已落地：camera batch、geometry probes、geometry-winner refit 不再把带 landmark 的 transient loss 与不带 landmark 的 public baseline 混排；Runtime 为两者建立同一 candidate-bound ranking snapshot，公开 `SilhouetteFitResult@1` 仍保持固定五项 metrics。新增 regression，Runtime 全量 `105 passed / 12 ignored`；该模块没有新的 likeness 证据，当前仍 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING`，人评/PBR/export-restart/360 未运行或阻断。

2026-08-15 observation/camera handoff 已落地：MCP 的 `design_action_run_prepare` schema 要求 Codex 提供 observation hash，Runtime 将其纳入 action input binding、重新生成并校验观察；stale observation 和未命中的 session camera hash 均 fail closed。Primary Form action 复用同一 candidate/target-bound camera fit evidence，不再把 default camera 当作隐式 handoff。该模块有 MCP schema/负向 fixture、Runtime stale-observation 和 action round-trip 回归；它不产生新的 likeness 证据，当前 benchmark 仍 `BLOCKED_INCOMPLETE_BINDING`、camera `MISMATCH`、visible-view `QUALITY_TARGET_NOT_MET`，人评/PBR/export-restart/360 未运行或阻断。

2026-08-15 Runtime quality-authority projection：Runtime 新增统一的 `visible_view_gate_checks`，由 Quality gate 布尔判断和 Agentic critic 的 failed metrics 共同消费；删除 Agentic critic 内重复阈值，避免 Viewer/Agentic projection 与 Runtime 质量结论漂移。focused Runtime、完整 `104 passed / 12 ignored` 和 MCP010F source Gate 通过。此模块不产生新的 likeness 证据，Stage 0 继续为 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING`，human/PBR/export-restart/360 未运行或阻断。

2026-08-15 Primary Form metric-priority 修复：当前质量顺序已经从“Chamfer/IoU weighted loss 主导”改为 Runtime-owned priority retention：先比较 `boundary_f1_4px`，再比较 `silhouette_iou`、bbox/centroid、landmark coverage/NME 和 regions；同级浮点噪声才回退到稳定 scalar loss。camera fit、Primary Form geometry/refit、same-camera acceptance、silhouette candidate compare 共用该 helper，新增 `primary_form_metric_ordering_keeps_boundary_ahead_of_iou` regression，focused Primary Form 14 passed。该模块没有新的真实视觉 receipt、Schema/tool count 或 Viewer quality authority 变化；Stage 0 继续为 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING`，human/PBR/export-restart/360 未运行或阻断。

2026-08-15 Primary Form 双向 boundary projection：Runtime 将同一 candidate-bound silhouette 的 boundary evidence 从单向 target→model 扩为有界的 target→model 与 model→target；模型侧多余/外扩边缘因此进入 Part-ID coverage 和 bounded Rig sweep，而不是被 target 点的 nearest projection 静默丢失。新增 `boundary_error_segments_include_model_owned_excess_edges` regression，`forgecad-runtime` 全量 `103 passed / 12 ignored`；没有 Schema、工具数或 Viewer quality authority 变化，也没有新的真实视觉 receipt。Stage 0 仍为 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING`，human/PBR/export-restart/360 继续未运行或阻断。

2026-08-15 Render Worker ownership 状态：Geometry Worker 现在只负责 Geometry compile 与通用 fixed sibling transport；`render_worker.rs` 独占 `forgecad-render-worker` binary identity 和 typed Render Worker adapter。新增同 cohort isolated conformance tests（九 AOV、512px PNG、重复确定性、拒绝 GeometryProgram），普通 Runtime 全量 `102 passed / 12 ignored`，source checker PASS。该模块没有新的真实 reference/likeness receipt，不改变 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING` 或 human/PBR/export-restart/360 未运行事实。

2026-08-15 Primary Form evidence-magnitude handoff：Runtime 在第一轮 coordinate probes 中优先使用同一 boundary observation 对当前坐标给出的 bounded proposal magnitude，单次最多半个 authored bound；当前已处于 proposal 或进入反向 pass 时仍使用小步。该模块让一次聚合观察能对 secondary Part 产生有意义的有限修正，Codex 不承担连续搜索。新增 regression，`forgecad-runtime` 全量 102 passed、10 ignored；没有新的真实 likeness receipt，不改变 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING` 或 human/PBR/export-restart/360 未运行状态。

2026-08-15 Primary Form boundary evidence coverage：Runtime 将 boundary observation 的固定 top-N 选择改为“每个有 `part_id` 的可见 Part 先取一条最高误差 evidence，再按距离补齐”，最大仍为 64 条，最终顺序仍为距离降序且确定性。这样 Codex 的一次聚合观察不会被 shin 等单一部件占满，后续 bounded multi-Part Rig probes 能读取 head/hand 等 secondary Part 的真实方向证据。新增 coverage regression，`forgecad-runtime` 全量 101 passed、10 ignored；无新的真实 likeness receipt，不改变 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING` 或 human/PBR/export-restart/360 未运行状态。

2026-08-15 Primary Form proposal-direction retention：Runtime 保留完整 boundary evidence proposal 作为后续坐标探测的方向来源；dominant Part 的局部 seed 只作用于首个联合 probe，secondary Part 不再使用参数索引猜方向。显式 `-1/0/+1` regression 和完整 `forgecad-runtime` 通过（100 passed、10 ignored）。该 source/convergence 修复没有新视觉 receipt，不改变 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING`、human/PBR/export-restart/360 未运行状态。

2026-08-15 Primary Form bounded multi-Part sweep：修复 Runtime 在 dominant boundary Part 存在时把完整 Rig coordinate schedule 过滤成单一 Part 的缺口。dominant Part 仍只用于 seed proposal；后续 fixed bounded probes 覆盖所有 supplied typed Rig controls，新增 seed/sweep regression，`forgecad-runtime` 全量 100 passed、10 ignored。无新的真实 likeness receipt，不改变 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING`、human/PBR/export-restart/360 未运行状态；Agentic action-run 仍是单 Part。

2026-08-15 Primary Form 局部 Part 偏移标定：Runtime 将显式 Part envelope proposal 的米制 `offset_x/offset_y` 统一换算到相机平面世界尺度，并修正 image-Y/down 与 camera-plane/up 的符号；缺少 camera 的 legacy proposal 不再用 Rig step 猜测米制位移。新增 focused regression，`forgecad-runtime` 全部 109 个单元测试通过（99 passed、10 个 worker isolation tests 按要求 ignored）。这是 source/convergence 修复，没有新的授权机器人视觉 receipt，不改变 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING`、human/PBR/export-restart/360 未运行状态。

2026-08-15 Primary Form same-camera retention：Runtime 在 staged candidate 之前用最终 camera 和 512px Render Worker 对 source/proposal 做同目标 full-resolution weighted-loss compare；未严格改善则返回 `PrimaryFormAcceptance@1.retained_source`，不创建 candidate、不覆盖 source VisualEvidence。contracts、Runtime focused regression 与 Stage 0 checker 通过；本模块不升级真实质量，当前仍 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING`。

2026-08-15 Agentic observation→action receipt binding：`DesignActionRun@1` 新增必需的 `observation_sha256`；Runtime action 只记录本回合唯一 `AgenticSceneObserveResult@1` 的 canonical hash，Store/CAS readback、schema positive/negative fixture 与 Runtime idempotent regression 均保持一致。该模块让 Codex 的观察、candidate-bound evidence 和一次 bounded action 可被同一 hash 追溯，但不改变视觉质量状态或解锁后续 stage。

2026-08-15 Primary Form 收敛证据与 action-run 契约对齐：`SilhouetteFitResult@1` 现在由 Runtime 明确保留 baseline/selected camera、metrics、loss、strict-improvement 以及 camera/geometry evaluation 分解；`design_action_run` 的 evaluate stage 同时引用 Primary Form result hash 与 QualityReport hash，Codex 只消费一份 Runtime-owned 收敛投影。`DesignActionRun@1` stage schema 已与 Store 实际字段对齐（`output_sha256`/`summary_sha256`/`error_code`/`skipped` 等），新增 Agentic 正负 fixture 防止旧 `hash/reason` 契约回流。contracts、Agentic/MCP、Stage 0 与 Runtime action/silhouette focused tests 通过；质量仍 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING`，没有升级人评/PBR/export-restart/360。

2026-08-15 历史 cohort — Agentic bounded action-run slice：Runtime 新增 `design_action_run_prepare`/`design_action_run_get`，当时只接受已批准、单 Part、`primary-form` 的 bounded repair intent；Runtime 在一次 action run 内复用 `SilhouetteRig@1`、既有 `primary_form_repair_prepare`、Geometry/Render Worker、strict readback 与 candidate-bound compare，并将不可变结果写入 SQLite/CAS。MCP 只有在 session scope、project/candidate/reference binding 和显式 write opt-in 同时满足时才转发；结果锁定 `confirm`/`export`，`runtime_write=false`、`persistent_user_data_touched=false`。Runtime focused idempotent/readback test、MCP source/manifest 和 Stage 0 已通过；该切片不等于通用多阶段 orchestrator、Repair 应用、durable/reference/DesignSpec 完整 producer 或视觉质量 PASS。该历史 cohort 为 125 Schema、41 read + 32 opt-in write = 73 tools；当前口径以本账本顶部的 191 Schema、21/21 active operators、54 read + 36 opt-in write = 90 tools 为准，`QUALITY_TARGET_NOT_MET`、`BLOCKED_INCOMPLETE_BINDING`、camera `MISMATCH` 保持不变。

2026-08-15 Primary Form 自动目标单 Part 归因修复：`part_contour_fit_prepare` 在没有用户显式 Part contour 时，现复用同一 Render Worker 的 silhouette + Part-ID boundary segments，生成选定 Part 的局部 envelope/error，不再把整张人体轮廓错误地归给单个 Part。单 Part probe 新增 `--target-mode automatic` 和 `shin-pair → shin-left` 的受限 typed patch route；current cohort `77ccce85…b9d4` 的真实授权参考隔离 receipt 为 `docs/evidence/mcp010f/part-correction-current-20260815-shin-auto.json`。Runtime proposal 为 `shin-width=+0.181818`、`shin-height=-0.25`、`shin-offset-x=-0.029121`、`shin-offset-y=+0.039908`，但五个候选没有同时避免 IoU/Boundary/bbox/centroid 回归，receipt 明确 `BASELINE_RETAINED_NO_NON_REGRESSING_SINGLE_PART_CANDIDATE`；baseline IoU `0.741047`、Boundary F1 `0.328765`，仍 `QUALITY_TARGET_NOT_MET`。该模块没有 confirm/export 或持久用户数据写入，不升级 camera/benchmark/human/PBR/export-restart/360 门。

2026-08-15 Primary Form action budget repair：真实 current-cohort 的修复前 receipt `docs/evidence/mcp010f/primary-form-budget-pre-fix-real-codex-20260815.json` 明确记录请求 `max_evaluations=64`、但 `primary_form_repair.fit_evaluations=24`；根因是 Runtime action 外层 cap 仍为 24，与 26-control detail schedule 不一致。源码现已恢复同一 Runtime bounded ceiling `64`，并将 optimizer normalization 抽成 helper；Runtime 端到端 fixture 用 action 请求 64 实际消耗 `63` 次（首个 authored baseline 不计入，仍在 63–64 bounded schedule 内），新增/Primary Form focused tests 通过。新 Dev.app cohort `c521bf28…c4a5` 已安装并通过 package/resource 校验；修复后的真实 CLI 尝试在 geometry hash/prepare authoring sequence 阶段阻断，receipt `docs/evidence/mcp010f/primary-form-budget-current-cohort-blocked-20260815.json` 没有 Primary Form 或视觉指标，因此不升级任何质量门。修复前 receipt 的比较 IoU `0.745514`、Boundary F1 `0.347176` 仍为 `QUALITY_TARGET_NOT_MET`，human/PBR/export-restart/360 继续未运行或阻断。

2026-08-15 packaged Render Worker landing：同一 source revision `cb1a6981…f805d` 重新构建并安装 Dev.app；MCP、Runtime、Geometry Worker 与 Render Worker 共用 cohort `aa5eaaa2…5827`，四个资源均通过 ad-hoc deep-strict 签名/包校验。隔离 packaged Runtime 通过同包兄弟 Render Worker 完成九 AOV、`512x512-perspective-zbuffer-deterministic`、两次确定性 hash 复跑、candidate-bound compare 和 MCP image-block transport，且 `persistent_user_data_touched=false`。证据为 `docs/evidence/mcp010f/dev-app-install-render-worker-20260815.json`、`dev-app-package-verify-render-worker-20260815.json`、`packaged-render-worker-raw-20260815.json`；raw probe 使用 synthetic reference，故只证明 packaged resource/process/protocol landing，`structural_visual_claim=NOT_CLAIMED`，质量仍为 `QUALITY_TARGET_NOT_MET`，human/PBR/export-restart/360 不升级。

2026-08-14 Primary Form 联合 proposal 回退修复：当 candidate-bound dominant Part 的一次性 width/height/offset 联合提案未严格优于 authored baseline 时，Runtime 在继续坐标探测前按 `1.0 → 0.5 → 0.25` 的固定比例回退重试；仍受同一 bounded budget、Rig bounds、Geometry Worker、strict GLB readback 和隔离 Render Worker 约束。focused/full Runtime 与 contracts 通过；没有新的授权参考字节或视觉 receipt，质量真值仍为 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING`。

2026-08-14 Primary Form bilateral Part-ID projection repair：Runtime boundary proposal 现在把 Worker 的固定 `*-left`/`*-right` Part-ID 聚合到 Rig 的 `*-pair` semantic Part，再计算同一局部 envelope 的 width/height/offset proposal；此前只能选中 dominant pair，但 proposal 可能保持 authored baseline。pair alias focused regression、full Runtime、MCP010C/F source Gate 通过；没有新的授权参考字节或视觉 receipt，质量真值不变。

2026-08-14 Primary Form 单 Part action scope：Runtime 现在先从 candidate-bound Part-ID boundary evidence 选出主导 Part，再把本次 bounded proposal 的可变范围锁定到该 Part；其余参数恢复 authored baseline，避免一次动作耦合多个轮廓误差。`DesignCriticReport@1.primary_form_directive.repair_operation` 同步直接指向 `primary_form_repair_prepare`，Codex 不再把只读 `silhouette_fit_prepare` 当作连续搜索入口。focused/full Runtime、Agentic contract、MCP010C/F source 和 Render Worker boundary Gate 通过；没有新的授权参考字节或视觉 receipt，故 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING` 及 human/PBR/export-restart/360 状态不变。

2026-08-14 Primary Form Part-priority convergence repair：Runtime 现在从 candidate-bound Part-ID boundary segments 聚合主导 Part 的误差分数，并把它作为 bounded Rig 坐标排序的第一优先级；无 Part-ID evidence 时回退到现有提案改变量排序。该修复保持 Geometry/Render Worker、strict readback、authored-baseline fallback 和 Codex 单动作边界不变；新增 Runtime regression 与 MCP010F source Gate 通过。真实视觉复验因授权参考原图字节未保留而 `BLOCKED_REFERENCE_BYTES_NOT_AVAILABLE`，当前仍 `QUALITY_TARGET_NOT_MET`，没有新的 likeness 或 benchmark 证据。

2026-08-14 Primary Form 首轮全控制覆盖修复：detail probe 的 26-control `SilhouetteRig@1` 现在提交 `max_evaluations=64`；Runtime 的三段有界预算为 `32 geometry + 16 initial-camera + 16 geometry-winner-camera-refit`，几何阶段的初始证据提案之后完整覆盖 26 个控制。该修复仍是 Runtime/source 收敛能力，不是 Codex 连续搜索或视觉质量证据；26-control focused regression、full Runtime 回归、MCP010C/F source Gate 通过。没有新的授权机器人 likeness receipt，当前仍为 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`，human/PBR/export-restart/360 未运行或阻断。

2026-08-14 Primary Form 单动作 transport 已有新的 current-cohort 真实 receipt：`real-codex-cli-current-20260814-primary-form-runtime-owned-r3.json` 在一次聚合 observation 后只执行 `primary_form_repair_prepare`，Runtime 内部完成 bounded fit、Geometry/Render Worker 和 candidate-bound compare，CLI 未重复 compare。该 transport 的 camera binding 为 `PASS_SILHOUETTE_FIT_TO_COMPARE`，但视觉状态仍是 `QUALITY_TARGET_NOT_MET`，因此不改变正式质量、人评/PBR/export-restart/360 的未运行账本。

2026-08-14 历史 cohort — Primary Form 肢段尺度/装配控制扩展：该时点 detail probe 声明 26 个有界 Rig 控制，并使用 `36 read + 23 opt-in write = 59` 工具清单。该数字只适用于当时 receipt；当前源码数字以本账本顶部的 191 Schema / 21/21 active operators / 54 read + 36 write = 90 为准。其质量仍为 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`。

2026-08-14 历史 cohort — Viewer 真值边界修复：Runtime `visual_evidence` 统一检查 candidate artifact、RenderSet、comparison report、QualityReport、reference/target 与 camera 的同 hash lineage；当时的 optimizer schema 验证口径为 59 个工具。该数字不覆盖当前 74-tool manifest；Viewer 继续只显示 Runtime `QualityReport@2`，本修复不升级真实机器人质量状态。

2026-08-13 历史架构重规划快照：新增 `docs/ADR/0026-agentic-design-runtime.md` 与 `docs/FORGECAD_AGENTIC_DESIGN_RUNTIME_PLAN.md`，并在当时记录 102 Schema / 59 tools。当前源码数字以本账本顶部的 191 Schema / 21/21 active operators / 90 tools 为准；Agentic 单动作 orchestrator、Repair 实际应用和完整视觉闭环仍未完成，真实机器人仍 `QUALITY_TARGET_NOT_MET`。

2026-08-14 Primary Form 收敛模块化修复：Runtime 现在把声明的 bounded evaluation budget 拆成 `geometry + initial-camera + geometry-winner-camera-refit` 三段（例如 `max_evaluations=24` 为 `12/6/6`），并以 Runtime-owned deterministic schedule 在第二轮方向试探前覆盖全部当前 Rig 参数；几何胜者会在同一最终 Geometry Worker 输出上重新拟合局部相机邻域，三段总和不超过请求预算。Geometry/Render Worker 边界、strict readback 和 authored-baseline fallback 保持不变。CLI 以 target/observation/fit 三阶段回执保留单次 canonical `scene_observe_get`，Viewer checker 已对齐实际 `error-console` 只读异常投影。budget/coverage/refit focused tests、MCP010C、MCP010F、Tauri check 和 desktop build 通过；本轮没有重新运行授权机器人 likeness，仍不得升级 `QUALITY_TARGET_NOT_MET`、`MISMATCH` 或 benchmark eligibility。

2026-08-14 Primary Form evidence projection follow-up：landmark annotations now produce only fixed Part-owned, camera-calibrated `offset_x/offset_y` proposals when the selected Render Worker Part-ID pass is present; no free-form region or hidden Part is guessed. The current detail probe declares 20 bounded Rig parameters and `max_evaluations=32` is allocated as `21 geometry + 11 camera` inside Runtime. Runtime full regression, Viewer typecheck/build and MCP010F source Gate pass；补充真实复跑 receipt `docs/evidence/mcp010f/primary-form-cas-rerun-20260814.json` 已完成 current-cohort fit/compare/AOV/review/quality 与 packaged Viewer exact lineage binding，但视觉仍 `QUALITY_TARGET_NOT_MET`，且不改写 Stage 0 冻结的 `camera=MISMATCH` provisional truth、benchmark eligibility 或未运行的 human/PBR/export-restart/360 门。
2026-08-14 Primary Form objective alignment 修复：Geometry trial 与 camera trial 现在统一使用含 landmark coverage 的 Runtime loss；新增 focused regression 防止几何变体通过 contour-only loss 绕过 landmark 证据。完整 Runtime 回归 `86 passed / 10 ignored`；一次新 cohort Codex 复跑在 authoring sequence 阻断，未生成新的视觉质量 receipt，不改变既有 `QUALITY_TARGET_NOT_MET`、Stage 0 provisional truth 或 benchmark eligibility。

2026-08-14 Primary Form 提案边界修复：严格改善 authored baseline 的几何试算现在以 `selected_geometry_program` 返回到 `SilhouetteFitResult@1`，并由 Runtime 再次通过 Geometry Worker hash/contract 校验；没有严格改善则为 `null`。该只读 typed proposal 可在用户批准后直接进入 `geometry_prepare`，减少 Codex 的参数重建和重复连续搜索；contracts、focused Runtime silhouette-fit、MCP010F source Gate 通过，真实视觉质量仍 `QUALITY_TARGET_NOT_MET`。

2026-08-14 Primary Form bounded schedule 修复：camera coordinate-descent 在单个候选批次没有改善时不再提前 `break`，而是继续消费声明的 Runtime-owned 有界 schedule，确保后续 roll/FOV/distance/target-offset/global-scale 轴仍被检查；只有预算耗尽后才结束该轮。此前 `max_evaluations=24` 的 camera remainder 可能被错误截成 16，已由 Runtime focused regression 固定为完整 `evaluations=8`（8-camera fixture）。这是收敛路径修复，不是新的视觉证据；本轮仍无授权机器人重跑，`QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING` 及 human/PBR/export-restart/360 未运行事实不变。

2026-08-13 模块化修复状态：Render Worker 已成为隔离一次性 JSONL 进程，Runtime 的固定渲染、GLB 渲染和 fit-batch 路径经该进程执行；Primary Form 的连续参数搜索收归 Runtime 的有界、确定性坐标邻域，Codex 只提供一次 typed proposal；Viewer 不再从 comparison metrics 本地推导质量门，只显示 Runtime Agentic projection 和 candidate-bound `QualityReport@2`。`script/test_mcp010c.sh` 与 `script/test_mcp010f.sh` 聚合门通过，真实 C raw stdio 的 Render Worker/九 AOV/比较/评审链路通过；这些结果仅证明模块和结构闭环，不能升级为机器人 likeness、PBR、人评或 360 PASS。

2026-08-14 F 回归状态：`silhouette_fit_prepare` 现在复用 winner camera-fit probe 的 transient silhouette/Part-ID 证据，且 Primary Form 只有在 Worker 评估严格改善时才替换 authored baseline；fresh Runtime focused regression 已证明 winner `CameraCalibrationRef@1` 进入 `reference_compare_prepare` 后 camera hash/canonical hash 不漂移。旧 attempt35 的真实 receipt 仍保留为 `camera=MISMATCH`、`QUALITY_TARGET_NOT_MET`、`BLOCKED_INCOMPLETE_BINDING`，需要新的同 cohort 真实参考闭环才能更新机器真值；本轮修复不把 source regression 写成视觉 likeness PASS。

2026-08-14 相机绑定加固状态：Runtime 在 128px bounded coarse search 后，对最多五个排序候选加 authored base camera 通过同一个隔离 Render Worker 批量执行 512px verification，内部使用完整 silhouette/landmark loss，外部继续返回 `CameraFitResult@1` 的 compact metrics。010C/010F 聚合回归、focused tests 和四个 build identity 已通过；此前两次停在 host `reference_get` setup 的重试按历史阻断记录保留。

2026-08-14 Primary Form / Viewer 模块边界更新：Runtime 的 camera-fit loss 已把 landmark coverage 纳入排序，并在固定预算内加入 authored base camera 的 Runtime-owned framing candidates；Codex 仍只提交一次 typed proposal，不负责连续参数搜索。r1 的 camera-cache compare 阻断与 r2 的 `reference_get` setup 阻断均按 fail-closed receipt 保留；r3 已成为最新 completed transport，验证 CLI 消费 Runtime candidate-bound visual evidence，不从 comparison metrics 重算阈值或阶段。Viewer source、typecheck/build、Runtime focused tests 和 `script/test_mcp010f.sh` 均通过。

2026-08-14 当前真实 Codex transport：`scripts/probe_mcp010c_codex_cli.py` 的最新 receipt `docs/evidence/mcp010f/real-codex-cli-current-20260814-primary-form-runtime-owned-r3.json` 使用同一 source-built cohort，10 个 Codex turn 全部退出码 0；它完成单次 `scene_observe_get`、Runtime-owned `primary_form_repair_prepare`、九 AOV、typed review、quality readback，camera binding 为 `PASS_SILHOUETTE_FIT_TO_COMPARE`，IoU `0.749122`、Boundary F1 `0.347623`，仍 `QUALITY_TARGET_NOT_MET`。该 receipt 已进入 run inventory 和 Stage 0 latest pointer，但未晋升 provisional observation/benchmark；脱敏事件没有观察到 image-block consumption，PBR、人评、confirm/export、restart hash 和 360 仍未运行/阻断。

2026-08-14 观察编排状态：真实 Codex silhouette-first source probe 已接入同一视觉回合的 canonical `scene_observe_get`，顺序固定为 `silhouette_target_get → scene_observe_get → camera_fit_prepare → silhouette_rig_hash`；脚本 fail closed 校验 Agentic projection 的 project/candidate/read-only/canonical hash，并把 observation hash 纳入脱敏 receipt。该 source route 尚未用新的授权机器人 PNG 重跑，不能升级旧 `MISMATCH`、`QUALITY_TARGET_NOT_MET` 或 benchmark eligibility。

2026-08-14 模块修复状态：Render Worker `--isolated-once` 现在严格消费一个有界 typed request 后退出，双 payload 负向回归已纳入 `script/test_mcp010c.sh`；Primary Form proposal 按 X/Y bbox 轴分别计算 width/height ratio，scale 使用两轴几何平均，并在 Runtime 内保持 authored baseline fallback 与严格改善晋级。Viewer 直接显示 Runtime `QualityReport@2.hard_gate_passed`，不再叠加本地 visual status/metric 谓词；Viewer source checker 已覆盖该边界。010C/010F 聚合回归、Viewer typecheck/build 和 focused Primary Form regression 通过，但真实机器人与 benchmark 真值仍为 `QUALITY_TARGET_NOT_MET`、`camera=MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`；本轮不新增视觉质量、packaged same-observation、人评、VoiceOver、PBR likeness、export/restart 或 360 证据。

Agentic Design Runtime 当前状态为 `部分实现/DURABLE_PREPARE_READBACK_PASS`：**历史 125-contract cohort** 已进入当时的 contracts manifest；该历史切片当时为 138 个 schema；现行源码真值为 191 个 schema、21/21 active operators。Runtime 提供 Agentic 只读投影和受批准 session/checkpoint/RepairIntent prepare/readback；Mechanical pose 是另一条不持久化的 candidate-bound read projection。当前切片仍不包含通用单动作 orchestrator、Repair 应用、Armature/skin、durable/reference/DesignSpec 的完整 producer conformance、packaged 同 cohort和真人门。

2026-08-13 历史 GitHub 研究授权记录：用户允许 Luna 对 build123d、BlenderMCP、CadQuery、Manifold 和 MaterialX 在精确 revision 下进行选择性源文件研究/复刻。该记录中的 Manifold receipt 当时仍为 `boolean@1 unavailable`；当前 bounded Manifold C ABI/FFI adoption 已另有 2026-08-15/16 receipt，任意 mesh Boolean 仍 unavailable。BlenderMCP 的 Blender Python、`exec()`、socket、遥测和资产网络能力仍被禁止。

2026-08-13 历史 Ponytail preflight 配置快照：新增 first-party `ponytail-preflight@0.1.0`，使 active registry 为 12 个 Skill；该旧 cohort 记录 102 Schema、59 tools 和 `boolean@1 unavailable`。当前 Skill 仍必须先读，当前工具/Boolean 数字以本账本顶部与 MCP010D current Gate 为准；没有安装或执行上游 Node package、hook 或 MCP server。

2026-08-13 隔离动作：已将三个不在 active registry 的 superseded Bundle（`reference-to-typed-plan@0.1.0`、`hard-surface-detail@0.1.0`、`uv-pbr@0.1.0`）移至 `packages/forgecad-skills/archive/superseded/`。当前 `hard-surface-detail@0.2.0` 和 `uv-pbr@0.2.0` 保持 active；archive manifest 保存三个旧 Bundle 的原始 tree hash 与替代关系，Runtime build 只嵌入 registry 声明的 Bundle，`scripts/check_mcp006_skills.py` 会拒绝未注册版本回到 `bundles/**`。归档内容只保留历史 provenance，不进入当前 12 个 active Skill，也不作为 Runtime 能力证据。

2026-08-13 轮廓优先增量：自动 mask→contour 已改为有向栅格边界追踪并通过重建 IoU > 0.94 回归；带 15 个图像地标的真实 Codex detail 回合已完成同 cohort target/camera/Rig/fit/compare/九 AOV/review/quality transport，但最终 silhouette IoU 0.685417、boundary F1 0.272115、landmark coverage 0.666667、NME 0.134407，与 attempt35 的 provisional retained observation 对照更低，仍为 `QUALITY_TARGET_NOT_MET`。随后 Runtime 将已知机器人 landmark 映射到固定 Part-ID anchor，并让 camera/Rig/geometry trial/candidate compare/持久 reference comparison 共用同一瞬时损失；最新完成传输 `docs/evidence/mcp010f/real-codex-cli-semantic-landmark-compare-20260813.json` 的 IoU 0.741047、Boundary F1 0.328765、landmark coverage 0.139498、NME 0.087015 反映指标语义变化，仍为 `QUALITY_TARGET_NOT_MET`，未晋升为 benchmark。最新尝试 `docs/evidence/mcp010f/real-codex-cli-semantic-aligned-fast-20260813.json` 在三次有界 operator discovery 后 `BLOCKED`，没有质量指标。这些都是目标函数/编排改进，不是 likeness、PBR、人评或 360°通过。

当前账本校正：源码为 191 个 JSON Schema、21/21 active operators、54 个默认只读工具和 36 个显式 write 工具（共 90）；下文旧数量只在明确标注的历史 receipt 语境中保留。

2026-08-14 最新模块化闭环：真实同 cohort detail receipt `docs/evidence/mcp010f/real-codex-cli-current-20260814-same-cohort.json` 已完成 preflight、单次 `scene_observe_get`、camera/Rig、Primary Form fit、compare、九 AOV、typed review 和 quality readback。此前暴露的 `selected_parameters` 合同错误已在 Runtime 结果边界修复为三字段 compact projection，并有 focused regression。该次 fit 使用 16 次 bounded evaluations，fit/compare camera canonical/hash 一致；指标为 silhouette IoU `0.690952`、boundary F1 `0.256758`，仍为 `QUALITY_TARGET_NOT_MET`。由于一个 Codex ephemeral turn 返回码为 1，receipt 按 fail-closed 保持 `BLOCKED`；44 个 typed MCP 调用完成、无额外副作用，因此只记录为当前修复 transport evidence，不替代 provisional truth 或 benchmark。

Stage 0 当前真值：唯一 `in_progress` 是 `FGC-MCP010F`，机器可读入口为 `docs/evidence/mcp010f/current-benchmark-truth.json`。attempt35 只保留为 `provisional retained observation`，视觉状态为 `QUALITY_TARGET_NOT_MET`，benchmark eligibility 为 `BLOCKED_INCOMPLETE_BINDING`：其 `camera_fit_prepare` 与 `reference_compare_prepare` 相机为 `MISMATCH`。packaged Viewer 的 current-cohort read-model binding 已单独记录为 `PASS_CURRENT_COHORT_BOUND_READ_MODEL`，但不把 r3 staged candidate 或 attempt35 晋升为 benchmark；source/raw/transport/build/AX smoke 只按各自层级记账，不等于视觉、人评或 packaged E2E PASS。运行顺序以 `docs/evidence/mcp010f/real-codex-run-inventory.json` 为准。

<!-- forgecad-stage0: schemas=402 schema_set_sha256=2f31c744134257e4d455cfc801a9be9e5c38ae81b1bc7fabd80d2002f968c4c7 read_tools=90 write_tools=69 total_tools=159 task=FGC-MCP010F observation=QUALITY_TARGET_NOT_MET eligibility=BLOCKED_INCOMPLETE_BINDING evidence=INCOMPLETE_TRUTH_BINDING camera=MISMATCH packaged=PASS_CURRENT_COHORT_BOUND_READ_MODEL latest_attempt=real-codex-cli-current-20260815-b37-complete-auto-v3.json latest_completed=real-codex-cli-current-20260815-b37-complete-auto-v3.json -->

本轮 contour-fit Runtime 增量已通过 focused tests：`silhouette_fit_prepare` 对带 V2 geometry evidence 的候选现在会读取候选绑定的 GeometryProgram，按 `optimizer.step_fraction` 生成受限 Rig 变体，逐一经固定 Worker 编译、严格 GLB 回读和选定相机渲染，并返回 `geometry_evaluations`、`parameter_deltas` 与 best-so-far metrics。它仍是只读试算，不写 CAS、candidate、version；真实用户 PNG 的既有 likeness receipt 不被改写，严格轮廓门仍未通过。

本轮新增的 V1 材质兼容基线已记录：15 Parts、548 triangles、三种 typed material zone、四个 256x256 fixed-render metadata pass 和 UV/tangent/GLB 回读通过；limited aspect proxy 为 0.4662（阈值 0.55），候选已拒绝，未创建 version/export。它是材质 plumbing/回读证据，不是 V2 PBR 或视觉相似度证据，详见 `docs/evidence/mcp010b/real-reference-v1-appearance-baseline.json`。

最新 `5143ac3b…6e61` primitive-blockout Dev.app 还完成了一次隔离真实 Codex CLI 的 MCP009 兼容回归：项目/授权参考、15-Part geometry、appearance/readback、limited quality、approval-bound confirm、version 和 CAS-backed MVP GLB export 十二调用全部完成；详见 `docs/evidence/mcp010b/dev-app-primitive-codex-cli-mcp009-appearance-export.json`。该程序是确定性 MVP 测试输入，图片只用于参考授权 admission，因此不能解释为图像条件生成、PBR V2 或视觉相似度通过。

随后将 primitive-blockout 的 Bundle 知识更新为“视觉质量路径遇到 limited 必须停止；仅用户明确选择时才允许 STRUCTURAL_BLOCKOUT MVP 导出”，并重建为最新 `d9c23b…ac0bd` Dev.app。该包的 ad-hoc/package、隔离 raw V2、active Skill 与真实 Codex CLI V2 structural receipts 均通过；用户完整重启后，d9 已成为当前 live cohort。live Desktop 结构激活证明 32 工具、Runtime/doctor Ready、MCP/Runtime cohort 一致、catalog/hash、active Skill 和临时项目只读回读；它仍不是视觉、PBR 或 360°证据。

MCP010C readiness audit 已更新：当前源码已提供 512×512 perspective、scene transform、z-buffer、确定性九 AOV、local reference comparison、MCP image block 和 Codex/human review；source-focused Gate 为 `PASS_WITH_UNRUN_VISUAL_GATES`。同 cohort 真实 Codex CLI 已完成六 turn/32 MCP 调用（setup/readback、V2 hash/prepare、compare、九 AOV、typed review/quality），其 camera auto-fit 修复前用户授权机器人 PNG 结果为 `QUALITY_TARGET_NOT_MET`（silhouette IoU 0.5132、boundary F1 0.1441、bbox edge error 0.1074）。当前重跑结果为 IoU 0.6623、boundary F1 0.2418、bbox edge error 0.0566、centroid error 0.0135，仍不是 likeness PASS；完整链路已重跑且结果已脱敏保存。Viewer compare source implementation、同 cohort packaged CLI read-model 与原生窗口结构探针已通过只读 IPC/构建/投影测试，但 Tauri packaged Viewer 控件交互/无障碍、真实 PBR likeness/纹理审美、人评阈值、export/restart hash 和 360°仍 `NOT_RUN/BLOCKED`，详见 `docs/MCP010C_READINESS_AUDIT.md`。

补充：当前 Dev.app 的安装、包验证、隔离探针、九 AOV raw C、packaged Codex CLI compare/review transport、同 cohort packaged E 的用户参考→detail→AssetPack→compare 结构传输，以及 F 的 packaged Viewer CLI read-model/原生窗口结构探针和 2026-08-12 frontmost native-window smoke 已通过；本轮轮廓草图更新后又完成同 cohort Dev.app 的 ad-hoc deep-strict 重建、隔离 Ready/project 探针和原生窗口启动验证。随后用 Computer Use 读取打包 WebKit AX 树并实际操作 AOV、Home/End、overlay/flicker、轮廓画布、差异热图和爆炸图，核心控件 smoke 通过。这些 receipt 证明 packaged C/E/F 的结构/传输/窗口链路和 Viewer 核心控件，不等于 VoiceOver 正式无障碍、真人评审或机器人 likeness；PBR likeness、人评阈值、export/restart hash 和 360°仍保持 `NOT_RUN/BLOCKED`。

F 当前新增的 `scripts/build_mcp010f_fit_plan.py` 已在用户授权机器人 PNG 的 candidate-bound comparison 上实际运行：修复 `region-mask-iou-v2` 后验证 comparison/view/catalog hash，并按 `reference-canvas → silhouette-blockout → landmark-structure → semantic-part-fill → surface-detail → uv-pbr → final` 生成门控修正意图。Viewer 额外提供一键 `轮廓画布`，直接选择同一 candidate 的 silhouette AOV 与 overlay，并在浏览器内存中显示与 Runtime `mask-2` 同源的受限边界连通 flood-fill reference-contour aid，不创建第二套 Runtime mask 或 camera。轮廓门未通过时只输出 silhouette 单部件动作，landmark/form/material 保持锁定；当前 helper 还把已知 image-space region 映射为一个 `primary_part_id`、只读 supporting Parts、MaterialZone hints 和按 Part 分组的 Operator hints；未知 region 保留为 unmapped，不允许 Codex 猜部件。Viewer source 现在同步显示第一处失败门的 read-only correction queue，给出失败指标、阶段和单 Part/单 MaterialZone 约束，但不生成几何参数、不调用写工具；视觉解锁只读取同一 candidate 的 `QualityReport@2.visual_status + hard_gate_passed`，不会把结构 candidate 的 `quality_hard_gate_passed` 当成视觉通过。当前 surface-linework/material-zoned source baseline 的 region median IoU `0.8694`、critical region min `0.6663` 已通过区域子门，但 silhouette IoU `0.7410`、boundary F1 `0.3288`、landmark coverage `0.7333` 仍使整体为 `QUALITY_TARGET_NOT_MET`。该辅助器只整理已有 metric/landmark/region 证据，不写 Runtime/CAS；`fit-plan-semantic-part-map-source.json` 和 `viewer-contour-first-panel-source.json` 记录源 Gate，8-zone material refinement 保持同一几何指标并仅改善材质分区可读性。rounded-panel/3368-triangle receipts 作为历史保留，当前 linework 与 material-zoned receipts 见 `docs/evidence/mcp010f/surface-linework-real-reference.json` 与 `docs/evidence/mcp010f/surface-zones-real-reference.json`，不构成 likeness 或人评通过。
在此基础上新增 `surface-lineflow` 编排计划器：它消费已通过的 inventory 校验、实时 `OperatorCatalog@1` 和离线 AssetPack，按当前失败门输出最多五个带 `line_flow`/Operator/MaterialZone 绑定的单 Part 意图。2026-08-12 用户图实跑为五个 `silhouette-blockout` 动作；下肢/脚部因裁切保持 deferred，结构、表面和 UV/PBR 仍锁定。证据为 `docs/evidence/mcp010f/surface-lineflow-plan-20260812.json`，它不写 Runtime/CAS，也不产生新的质量结果。
当前 Part 轮廓归因也已收紧：`SilhouetteTarget@1.parts` 必须使用唯一、非重叠且落在 contour 内的区间；`part_contour_fit_prepare` 只对有 observed/inferred slice 的语义 Part 计算对应目标边界，unknown 或缺失 slice 才保守回退整图。该 source hardening 只改善局部误差归因，不改变真实机器人 `QUALITY_TARGET_NOT_MET`。

2026-08-13 新增轻量 `scripts/probe_mcp010f_part_correction.py`：它使用 Viewer 实际 `chest-shell` contour draft，在隔离 Runtime 中建立局部 target，调用 `part_contour_fit_prepare`，再对单一 chest panel 运行有界 height/width/offset 候选和固定 reference comparison。对照的 provisional observation 为 silhouette IoU `0.741047`、boundary F1 `0.328765`；最高 IoU 候选为 `0.745895`/`0.330265`，Runtime loss winner 为 `0.745135`/`0.340045`。这证明了“局部建议→候选→比较→best-so-far”传输闭环，但仍远低于 `0.90` 轮廓门，状态为 `PASS_TRANSPORT_WITH_METRICS`，不是质量通过或 benchmark；未 confirm/export，未触碰用户持久数据，fresh source binary 未提供可验证 cohort identity。
Runtime 同步补上了 Part-aware Rig proposal：当 target 含 typed Part slice 时，`silhouette_fit_prepare` 在选定相机做一次 bounded Part-ID readback，为匹配 `part_id` 的 width/height/scale/offset 参数计算局部 envelope/centroid proposal；无 slice 的 target 继续走全身 fallback。合成局部优先回归、Runtime 全测试和新的 source receipt `part-correction-source-20260813-rig2.json` 通过；它只改善误差归因，不改变真实图片的 `QUALITY_TARGET_NOT_MET`，也不允许 confirm/export。
Runtime 另新增只读 `silhouette_part_error_get`，按 candidate/target/render-set hash 返回每个声明 Part 的局部 envelope、边界误差、质心/宽高比和推荐修正 Part；多 Part focused regression 与真实 chest-shell source receipt `part-correction-source-20260813-part-error.json` 通过。该表只改善 Luna 的局部归因，不代表视觉 likeness 通过。
新增的 `scripts/validate_mcp010f_reference_inventory.py` 已接入 F Gate。它在 geometry 写入前对当前机器人 inventory 做标准库校验，并要求 supplied catalog 中的 Operator 全部 active、材质 ID 全部存在于离线 AssetPack；当前验证为 10 个 detail、8 observed、1 inferred、1 unknown、7 个 active Operator，故意加入 `boolean@1` 的负向输入会 fail closed。该 hash-only receipt 只属于 Codex 编排，不是 Runtime GeometryProgram canonical hash。

本轮同 cohort packaged Codex CLI 复跑的 `real-codex-cli-current-20260812-packaged-contour.json` 已完成真实参考→几何→九 AOV→比较→typed review transport，但结果为 `QUALITY_TARGET_NOT_MET`（boundary F1 `0.2418`、silhouette IoU `0.6623`、bbox edge error `0.0566`）。该探针提交的 `ReferenceViewSpec@1` 没有 image-derived landmarks/visible regions，因此 `landmark_coverage=0` 与 `region_median_iou=0` 被记录为 `incomplete_visual_intake`，不是模型局部错误。新增 `scripts/build_mcp010f_contour_correction.py` 和 `contour-correction-current-20260812.json` 将下一轮固定为“先视觉 intake、再单一轮廓 Part/Operator 修正”，并在轮廓/形体门通过前锁定 landmark、表面细节和 UV/PBR；它不调用 MCP、不写 Runtime/CAS，也不产生新 QualityReport。

随后按用户要求在实际打包 Viewer 中选择 `chest-shell` 绘制并复制了两份 candidate-bound 轮廓草图；两份草图都通过 `validate_mcp010f_contour_draft.py`，并在隔离 Runtime 中执行了第二份草图对应的单 Part `chest-wedge-mild` 修正。`geometry_program_hash → geometry_prepare → appearance_prepare → reference_compare_prepare → quality_get` 的传输、hash 绑定和回读通过，但结果为 `QUALITY_TARGET_NOT_MET`：silhouette IoU `0.7399`、boundary F1 `0.3263`、landmark coverage `0.6667`、region median IoU `0.8487`；相对 attempt35 provisional observation 的 IoU `0.7410`、boundary F1 `0.3288`、coverage `0.7333`、region median `0.8694` 均未改善，因此拒绝晋级。此次仅使用隔离临时数据根，未确认 candidate、未创建 version/export、未修改用户持久数据；证据为 `docs/evidence/mcp010f/contour-execution-actual-20260812.json` 及其 draft/intent receipts。

随后用同一用户授权 PNG 和 `docs/evidence/mcp010e/robot-reference-intake.json`（15 个可见 landmark、8 个 visible region）重跑了真实 packaged Codex CLI：`real-codex-cli-current-20260812-packaged-intake.json`。这次不再把零标注误判为几何失败；区域填充已经达到 `region_median_iou=0.8466`、`critical_region_min_iou=0.5216`，真正未达标的是外轮廓与比例（`silhouette_iou=0.6623`、`boundary_f1_4px=0.2418`、`bbox_edge_error=0.0566`、`landmark_coverage=0.4667`、`landmark_nme=0.3367`）。对应的 `contour-correction-current-20260812-intake.json` 只解锁 silhouette-blockout 单变量修正，继续锁定 landmark/form/material。该历史 receipt 的工具面为 102 Schema、36 read + 23 opt-in write = 59；当前源码数量以顶部账本为准。新增 `CameraCalibrationRef@1` 只传 Runtime-owned camera hash pair，避免 Codex 跨 turn 复制完整浮点相机。

attempt32 是保留的 primitive route 历史 receipt。历史 attempt35 receipt `docs/evidence/mcp010f/real-codex-cli-silhouette-first-20260813-attempt35-detail-camera-ref.json` 已完成 11 个短 turn 的 detail 轮廓优先传输：reference/mask、V2 geometry hash/prepare、target、camera fit、Rig hash、silhouette fit、candidate-bound readback、compare、boundary error、九个 AOV、typed visual review 和 quality。MCP/Runtime/Worker cohort 均为该次构建的 `74749cc7…e1c3`，输出 26 Parts、4704 triangles、validator passed；结果为 `PASS_WITH_QUALITY_TARGET_NOT_MET`，指标为 silhouette IoU `0.741047`、boundary F1 `0.328765`、bbox edge error `0.007813`、centroid error `0.007878`、landmark coverage `0.733333`、landmark NME `0.134536`、region median `0.869403`、critical region min `0.666289`。它只作为 provisional retained observation；因 fit/compare camera 不一致且未绑定 packaged Viewer，benchmark eligibility 为 `BLOCKED_INCOMPLETE_BINDING`，不能称为 best 或合格 benchmark。Runtime fit proposal 为 `no_improvement`、IoU `0.698340`，仍是 read-only 建议，不是新 candidate。attempt33/34 的 full-camera canonical hash 漂移失败保留为负向证据；human/PBR/confirm/export/360 仍未运行，未触碰用户持久数据。camera search 仍内部评估 64 次。

历史 CameraCalibrationRef 里程碑 `docs/evidence/mcp010f/real-codex-cli-camera-ref-20260813-attempt5.json` 使用同一授权 PNG、同 cohort `e968c9ef…6980` 和 `CameraCalibrationRef@1` 完成了 13-turn detail transport。Runtime 成功根据 `camera_hash + canonical_sha256 + target_sha256` 解析精确相机，并完成九 AOV、typed review 和 quality readback；结果仍为 `PASS_WITH_QUALITY_TARGET_NOT_MET`：26 Parts/4704 triangles，silhouette IoU `0.698465`、boundary F1 `0.281074`、bbox edge error `0.037109`、centroid `0.049908`、landmark coverage `0.666667`、landmark NME `0.201432`、region median `0.771619`、critical region min `0.675106`。它不是当前最新完成传输，也不构成 benchmark；材质、confirm/export、human review 和 360 继续锁定。

随后用同一授权 PNG 和同 cohort `f087efba…bb2e62` 运行了新的 `docs/evidence/mcp010f/real-codex-cli-rig-fit-20260813.json`。这次 `silhouette_fit_prepare` 不再只有参数 proposal：它完成 8 次受限评估，其中包含候选绑定 V2 GeometryProgram 的真实 Worker 编译、严格 GLB 回读和固定相机渲染，并返回 `geometry_evaluations`/`parameter_deltas`；随后完成九 AOV、typed review 和 quality readback。选定 fit 的 IoU/F1 为 `0.700586/0.278263`，最终比较为 `0.698465/0.281074`，bbox/centroid 为 `0.037109/0.049908`，landmark coverage/NME 为 `0.666667/0.201432`，region/critical 为 `0.771619/0.675106`。状态仍为 `PASS_WITH_QUALITY_TARGET_NOT_MET`；其指标低于 attempt35 provisional observation，但两者均不是合格 benchmark。无 confirm/export、无 PBR 解锁、无用户持久数据写入。

随后修正了 Rig 物化的直接缺口：`SilhouetteRig@1` 从 5 个全局参数扩展到头、胸、肩甲、上臂、前臂、骨盆、大腿和小腿的 12 个宽/高语义参数；Runtime 沿 `part_outputs[].input_node_ids` 递归追踪镜像/变换/面板/profile/vent/joint 的真实源节点，并由固定 Worker 逐候选编译和渲染。针对性 Runtime 测试与 source build 通过。新的同 cohort 真实 CLI 尝试 `real-codex-cli-rig-fit-expanded-20260813.json` 已完成参考、V2 几何、相机、Rig、真实几何试算、比较、九 AOV 和质量读取，但 Codex 在 review turn 额外尝试了被禁止的 human-review 工具，导致该 receipt 保守记为 `BLOCKED`；它不覆盖 attempt35 provisional observation，也不构成视觉通过。严格 visible-view 门仍未达到，当前没有合格 benchmark，后续继续按单 Part/局部 target 修正。

随后重跑同一 12 参数 DAG-aware Rig 路径，并修复了 review turn 的工具漂移恢复逻辑；receipt `docs/evidence/mcp010f/real-codex-cli-rig-fit-review-recovery-20260813.json` 的 reference、camera、target、Worker、compare、九 AOV、typed review 和 quality transport 均完成，MCP/Runtime/Worker 为同一 cohort。该次结果为 `PASS_WITH_QUALITY_TARGET_NOT_MET`，IoU `0.698465`、boundary F1 `0.281074`、bbox edge error `0.037109`、centroid error `0.049908`、landmark coverage `0.666667`、landmark NME `0.201432`、region median `0.771619`、critical-region minimum `0.675106`，与 attempt35 provisional observation 对照更低，因此不晋级、不确认、不导出。失败 human-review 尝试只作为拒绝证据保留，恢复后的 typed review 不被误报为 human approval；该回合无 PBR、无持久用户数据写入。

为验证细节 Operator 和 AssetPack 是否能改善实际结果，又通过当前 Dev.app 同级 `forgecad-mcp`/`forgecad-runtime`/`forgecad-geometry-worker` 跑了 `real-detail-assetpack-current-20260812-baa39a.json`：26 Parts、4704 triangles、7 个实际 MaterialZone、9 个 AOV、ArtifactReadback@2 与离线材质传输均通过；比较指标提升到 `silhouette_iou=0.7410`、`boundary_f1_4px=0.3288`、`bbox_edge_error=0.0078`、`landmark_coverage=0.7333`、`region_median_iou=0.8694`。这证明“轮廓/结构 Operator 先于材质”是有效方向，但仍为 `QUALITY_TARGET_NOT_MET`，且 embedded texture URI count 为 0；不能宣称高质量 likeness 或 PBR 视觉保真。

随后把 `scripts/probe_mcp010c_codex_cli.py` 增加为显式 `--geometry-route detail`，让真实 Codex 在 authoring turn 读取 live Operator Catalog 后调用受限 profile/panel/vent/revolve/joint/mirror/transform/tube/array 组合；默认 primitive 路径保持不变。`real-codex-cli-current-20260812-packaged-detail-intake.json` 已完成 6 个 Codex turn、26 Parts/4704 triangles、9 AOV、typed review 和 candidate-bound quality transport，指标与 raw detail receipt 一致，但仍 `QUALITY_TARGET_NOT_MET`，没有 appearance write、human review 或 confirm/export。这证明 Codex 能调用 D 的细节 Operator，不证明图片到高质量模型已经完成。

Viewer source 还将这套顺序映射为只读阶段面板：缺少 candidate-bound metrics 时保持 `reference-canvas`，轮廓未通过时显示 `silhouette-blockout` 并将结构/形体/表面材质置为锁定，只有累计门通过才提示进入 UV/PBR。隔离 Vite browser DOM smoke 已实际验证 9 个 AOV、3 种比较模式、轮廓画布、热图/flicker 控件、无 metrics 时的空 correction queue、AOV tablist 的 Arrow/Home/End 键盘导航与 roving focus，以及四点指针描线、闭合预览、点级撤销和归一化点集复制。Viewer 轮廓点集现在形成 candidate-bound 的 `ForgeCADViewerContourDraft@2`，只存在内存，标记 `transient_only` 和 `runtime_write:false`，并带有 project/candidate/reference/artifact/RenderSet/comparison hash 与选定 Part/MaterialZone；不会进入 Runtime/CAS。新增 `scripts/validate_mcp010f_contour_draft.py`，在 Codex 生成 `change_prepare` 前拒绝自交/越界/过小点集和 stale hash，并在未选 Part 时保持 `CONTOUR_DRAFT_BOUND_PART_SELECTION_REQUIRED`。当前 detail intake receipt 已用胸甲 polygon 实际验证为 `READY_FOR_SINGLE_PART_CONTOUR_EDIT`，结果保存在 `docs/evidence/mcp010f/contour-intent-current-20260812.json`；它仍只是 transient 编排证据，不代表几何指标改善或质量通过。Viewer 视觉状态现在只接受同一 candidate 的 `viewer_visual_evidence.quality_report`，缺失时不回退到通用 quality 或 render hash，保持 `not-run`。该面板仍是 transient UI guidance，以 Runtime `ReferenceComparisonReport@1` / `QualityReport` 为真值。`script/test_mcp010f.sh` 的 Viewer source/build Gate 已覆盖其写边界和 contour validator 负向测试，正式 packaged UI 控件交互与无障碍仍 `NOT_RUN`。

第二轮真实单变量几何调优已完成并全部保留为拒绝证据：`asymmetric-stance` 将右肩上提、左臂下落后取得局部最佳 silhouette IoU `0.7490`、region median IoU `0.8684`，但 boundary F1 `0.2381`、bbox edge error `0.0195` 和 landmark coverage `0.7333` 仍失败；`head-wedge`、`tapered-lower`、`asymmetric-armor` 没有改善全局门。随后 panel@1 采用固定四段圆角轮廓，得到历史 20 Parts/3944-triangle baseline；再叠加六类受限表面线流层，当前 source baseline 为 26 Parts/4704 triangles，silhouette IoU `0.7410`、boundary F1 `0.3288`、bbox edge error `0.0078`、centroid error `0.0079`、landmark coverage `0.7333`、region median IoU `0.8694`、critical-region minimum `0.6663`；8-zone material refinement 保持这些 geometry/comparison 指标不变并改善材质-ID可读性，变化指标全部改善或持平，但全局仍 `QUALITY_TARGET_NOT_MET`。rounded-panel、linework 与 material-zoned receipts 分别见 `docs/evidence/mcp010f/rounded-panel-real-reference.json`、`docs/evidence/mcp010f/surface-linework-real-reference.json` 和 `docs/evidence/mcp010f/surface-zones-real-reference.json`。追加的 chest-shell 浅斜切 profile-extrude 实验把 boundary F1 提升到 `0.3338`、silhouette IoU 提升到 `0.7433`，但 landmark coverage 降到 `0.6667`、region median IoU 降到 `0.8478`，因此不替换 baseline，详见 `docs/evidence/mcp010f/chest-wedge-linework-real-reference-rejected.json`。2026-08-12 的同 cohort sweep 又测试了 sleek-armor、tapered-shells、long-limbs、helmet-visor；tapered-shells 的 landmark NME 降至 `0.0698`、region median IoU 升至 `0.8823`，但 boundary F1 仅 `0.2792`，四个变体均保持 `QUALITY_TARGET_NOT_MET`，没有自动晋级。
组合 `curved-tapered` 进一步把胸甲 profile-loft 与下肢渐缩叠加，得到 20 Parts/3320 triangles；严格回读通过但 silhouette IoU `0.7359`、boundary F1 `0.2786`、region median IoU `0.8553` 均退化，beauty 仍呈 blockout-like，因此仅作为负向证据保留在 `docs/evidence/mcp010f/experiment-curved-tapered-bb5c01.json`，不改变默认 `surface-linework → armor-shell-zones` 路线。
随后又做了一次仅替换 chest-shell 的浅斜切 profile-extrude 实验：boundary F1 提升至 `0.3338`、silhouette IoU 提升至 `0.7433`，但 landmark coverage 降至 `0.6667`、region median IoU 降至 `0.8478`，因此不晋级；详见 `docs/evidence/mcp010f/chest-wedge-linework-real-reference-rejected.json`。

补充：肩甲外轮廓的单变量校准没有替换上述基线。较大 shoulder-contour 变体将 landmark NME 降至 `0.0700`、region median IoU 提升至 `0.8764`，但 boundary F1 降至 `0.3275`；更小 shoulder-contour-tiny 也未恢复边界门。两者分别记录为 `NOT_PROMOTED/REJECTED`，不能作为确认或导出依据。

同一轮还用真实用户 PNG 跑了完整 Part-sink `three-quarter` yaw：20 Parts/3944 triangles 的 `-0.30 rad` 变体把 silhouette IoU 降至 `0.6578`、boundary F1 降至 `0.2821`、landmark coverage 降至 `0.6000`，已拒绝；这验证了当前“保持模型不旋转、由 calibrated camera 负责取景”的选择。新增胸甲宽度单变量实验只把 chest-shell 从 1.66 m 扩至 1.78 m；region median IoU 提升至 0.8833，但 boundary F1 从 0.3288 降至 0.3258，centroid error 与 critical-region minimum 也变差，已拒绝并保留 `docs/evidence/mcp010f/chest-width-mild-real-reference-rejected.json`。

本轮又加入了一个不改变拓扑的曲面显示增量：`profile-loft@1`、`revolve@1`、`tube-sweep@1`、`joint-stack@1` 对重合且法线相容的曲面顶点做受限平滑，panel/cap 锐边保持不变。真实 PNG 回读确认固定 beauty 的曲面着色更连续，但 silhouette、boundary、landmark 和 region 指标与圆角面板基线相同，因此只保留为 `retain_as_render_quality_increment_without_metric_promotion`，不替换当前候选、不允许 confirm/export；脱敏 receipt 见 `docs/evidence/mcp010f/smooth-curved-normals-real-reference.json`。单图 observed/inferred/unknown 与下一轮修正边界已冻结在 `docs/evidence/mcp010f/reference-detail-inventory-real-reference.json`。

版本：2026-08-12 · 分支：`main` · 工作树包含 MCP010B/C/D/E/F 受控修改 · `FGC-MCP005–MCP010A done（MVP host golden path + Dev activation）`；`MCP010B blocked/deferred（Darwin OS hard cap NOT_RUN）`；`MCP010C source-focused PASS_WITH_UNRUN_VISUAL_GATES；首次真实机器人 compare QUALITY_TARGET_NOT_MET`；`MCP010D source-focused + packaged D structural PASS_WITH_DEFERRED_BOOLEAN_AND_VISUAL_GATES`；`MCP010E source-focused + packaged structural PASS_WITH_UNRUN_EXTERNAL_AND_VISUAL_GATES（xatlas/Validator/视觉未运行）`；`MCP010F source + packaged read-model PASS_WITH_UNRUN_UI_AND_HUMAN_GATES（Tauri Viewer UI/人评/360 未运行）`

## 2026-08-12 历史快照（当前以本文顶部 2026-08-18 状态为准）

旧桌面工作台、内置模型/Provider、App Server/Protocol、Python Agent、旧脚本、旧合同和旧迁移目录已经从工作树删除；新 Viewer、Runtime、MCP、Worker 协议、Runtime V1 migration、完整首批 contracts、CAS、SQLite repository、OS process lock 和 authenticated local IPC 已写入。`WushenForgeLibrary` 未修改，reset 归档位于 `/tmp/forgecad-mcp001-20260807`。

当前代码提供单用户 Runtime 与 MCP004 事务基座：MCP 实现 `2025-11-25` canonical 与 Codex `2025-06-18` compatibility 初始化、只读 tools/resources、显式 opt-in write、URI/合同 fail closed；Runtime 已具备 SQLite/CAS、candidate/Job/audit、approval/idempotent confirm/reject、restore-as-new-version、path-free diagnostic export 和 authenticated IPC。`forgecad-mcp` 拥有 stdio 并异步启动/连接同一数据根的共享 Runtime；缺失、启动失败或 ready 后崩溃时保持 stdio，最多一次有界重启。短时 launcher 文件锁只负责启动选主，Runtime 自己在 migration 前持有的 `runtime.writer.lock` 才是唯一写者硬边界；正常 MCP 适配器退出不应终止已经 Ready 的共享 Runtime，显式 shutdown/update 才停止它。MCP010A/010B 的历史 package/live receipts仍按 cohort 保存。当前总源合同 191、21/21 active operators，默认 54 read + 36 opt-in write = 90 tools；B structural source Gate、C fixed renderer/九 AOV/reference compare/review raw Gate、D typed hard-surface operator raw Gate 和 E AssetPack/UV/PBR/MikkTSpace raw Gate均已通过。F 新增 hash-bound SilhouetteTarget、`CameraCalibrationRef@1`、37+top-3×9 coarse-to-local camera fit、64px 内部二值 transient raster（128px 合同输出，最终质量仍回到 512px）、Runtime-owned Rig hash（避免 Codex 本地重算 canonical JSON）、directional boundary error 和 `silhouette_part_error_get` 多 Part 误差表 source slice；Agentic observe/plan projection、durable session/checkpoint/RepairIntent prepare/readback 与 CAS-bound RepairIntentRun 另有 Runtime/Viewer 和隔离重启证据；Fictional Energy Rifle Profile/Plan 仅提供 bounded nonfunctional source authoring；真实机器人仍未达标，不证明用户 likeness、PBR likeness、Viewer/package/live C/D/E、人评或 360。MVP 仍不使用 TTL lease/heartbeat、daemon 或 broker。真实 Codex CLI diagnostic write 与 reference→CAS GLB host golden path、Viewer read model 均已有历史 evidence。

`MCP005 done` 表示真实 Codex CLI 的授权图片附件进入 CAS；`MCP006 done` 表示十个历史 first-party declarative Skill Bundle 和安全合同收口；MCP010B 当前源码新增并验证 `primitive-blockout@0.2.0` active overlay，MCP010D 又使 `hard-surface-detail@0.2.0` 在真实 Operator consumer 与 immutable bundle 验证通过后 active；不改写 MCP006 历史 receipt。`MCP007 done` 提供有界多 Part GeometryProgram → GLB；`MCP008 done` 增加 hash-bound UV/tangent/PBR、固定四 pass render 和真实 GLB Viewer；`MCP009 done` 增加 limited quality、stable-Part `change_prepare`、immutable confirm/restore 和 CAS-backed `mvp-glb` receipt，并已有真实 Codex CLI 的十二调用 host golden-path PASS。MCP009 的 reference compare 仍明确是 aspect-ratio limited，不是 silhouette/landmark/region 完成。首个机器人视觉/真人门属于 MCP010B–F；复杂 Job 恢复属于 MCP011；通用第三方生命周期属于 MCP012；签名/notarization、packaged Desktop write 和跨类别门属于 MCP013。未运行项不得写成 PASS。Desktop 只显示 Runtime Viewer，不提供聊天、上传、模型选择或 API Key。

用户已批准 MCP010 高质量硬表面升级轨道，FGC-MCP010A 已完成。权威重排、MCP/Runtime/Geometry Worker/Viewer 离线构建、用户级 ad-hoc Dev.app 安装、Runtime `Ready`、隔离项目创建、真实 Codex CLI 诊断写链、用户配置切换和第二次真实 Codex Desktop Gate 均有 PASS evidence。第一次用户重启后的真实 Codex Desktop Gate 已实际运行并 `FAIL`，该历史 receipt 保留；修复后第二次重启观察到 30 个工具、`Ready`、`doctor` ready、cohort match、临时 `project_create` 和 `project_get` readback。最新 cohort 为 `7a8fddf99c57893db93fe1bdd98ab65302bd890d191026495cbbc63ae4652064`。

当前唯一 `in_progress` 为 `FGC-MCP010F`。当前源码有 191 个 JSON Schema、21/21 active operators（MCP006 历史合同、MCP010B/C/D/E/F 增量、Agentic contract family、Fictional Energy Rifle Profile/Plan、Subdivision 与 Authoring Mesh contracts）；默认读表面是 54 个工具，显式 write opt-in 后是 54 read + 36 write = 90 个工具。除既有轮廓/材质/渲染只读工具外，Agentic projection tools 为 `scene_observe_get`、`design_stage_plan_get`、`critic_report_get`、`visual_evidence_bundle_get`，durable tools 为 `session_create_or_resume`、`session_get`、`checkpoint_prepare`、`checkpoint_get`、`checkpoint_restore_prepare`，action-run tools 为 `design_action_run_get`、`design_action_run_prepare`、`repair_intent_run_prepare`；读工具读取 Runtime evidence，写工具包含 approval-gated session/checkpoint/RepairIntent prepare、`repair_intent_run_prepare` staged run、`primary_form_repair_prepare` staged prepare 和单 Part `primary-form` action-run，不执行通用 orchestrator。`operator_catalog_get`、`geometry_program_hash`、`silhouette_rig_hash`、`material_pack_get`、`render_pass_get`、`silhouette_target_get`、`camera_fit_prepare`、`silhouette_fit_prepare`、`part_contour_fit_prepare`、`silhouette_part_error_get`、`silhouette_candidate_compare`、`subdivision_artifact_lineage_get`、`session_get`、`checkpoint_get`、`design_action_run_get` 是默认只读；`CameraCalibrationRef@1` 只作为 `SilhouetteFitIntent@1.base_camera` 的 Runtime-owned hash 引用，轮廓 target 和 Agentic prepare/action-run 需要显式 write opt-in。新增 visual evidence target binding 与 `primary_form_directive` 仍是 Runtime-owned read-only projection，不能变成 Viewer 第二质量门；C/D/E/F source Gate、Agentic projection/durable receipt 与 Profile source Gate 的结构事实仍与真实用户 likeness、PBR、Viewer UI、人评和 360 分开记录。当前一次性 Agentic observation 还内含 Runtime 生成的 Part-error table、推荐修复焦点和 RepairIntent scope；没有 typed Part slice 时明确返回 unknown/null，不让 Codex 重建这层诊断。
最新 E PBR renderer Dev.app cohort `77d4bff511a21f094a4875ccc4967b429441172adc008771e47dafc1797f2a73` 已完成 ad-hoc 安装、包校验和隔离用户参考图传输；固定 beauty 已实际采样嵌入 normal/metallic-roughness/AO/emissive 通道并使用固定 key/fill/rim 光照，但本次 receipt 未做完整 Desktop restart，机器人视觉结果仍 `QUALITY_TARGET_NOT_MET`，不能写成 PBR likeness。

当前授权单图的 V1 transition baseline 已实际到达 project/reference/geometry/appearance/quality/confirm/version 前缀，但在 export 前由 Codex 结束 turn，故记录为 `BLOCKED`，不冒充完整 host、V2 或视觉质量 PASS。隔离 source-built 真实 Codex CLI V2 structural receipts、Dev.app structural receipts、C raw receipt 和 C 真实 CLI receipt 均按其范围保留；C receipt 只证明九 AOV/比较/评审的结构链路，attempt35 provisional retained observation 明确记录 `QUALITY_TARGET_NOT_MET` 且 benchmark eligibility 为 `BLOCKED_INCOMPLETE_BINDING`，不证明图片相似度、PBR、用户人评、导出、restore 或 360°。高细节 Operator、离线 AssetPack、Skill `0.2.0`、PBR V2 纹理以及 F Viewer source surface 与 packaged CLI read-model Gate 已通过；F 还通过 Runtime 哈希绑定轮廓目标、`CameraCalibrationRef@1`、37 个覆盖全局尺度的粗候选加局部探针相机拟合和方向性边界误差 source tests；Agentic projection 与 durable session/checkpoint/RepairIntent prepare/readback 也已通过合同/重启 probe；xatlas/Validator、同一 observation 绑定的 Tauri packaged Viewer E2E/C/D/E/F、视觉 PBR、独立人评、export/restart hash 和 360 继续是后续目标；当前为 191 Schema、21/21 active operators，历史 177/152/144/52/50/44 contract receipts 必须按历史范围保留。

MCP010A 已完成一轮可恢复旧代码清理：先建立并验证 Git bundle、tracked/local archive、worktree patch 与 SHA-256 清单，再删除旧 Provider/Planner/CSG 评估、packaged Agent sidecar 和旧 Gate07 配置，终止 5 个孤儿 Python Agent，隔离旧虚拟环境、Host/Proxy、本地旧安装包、日志、缓存和 7 组 stale endpoint；无用 Rust API、依赖和旧 Tauri resource protocol 同步移除。两份 standalone Host receipt 仅以 `SUPERSEDED` 状态保存在 `docs/evidence/archive/`。`WushenForgeLibrary`、Runtime V1 数据、Codex 历史及约 2.5 GiB 可能含用户 GLB/PNG 的 `output/` 均未修改。第一次 Desktop live Gate 使用的旧 cohort `e5fd7da79576fd022894838c5ab9b0532b7aef735abc42b86f3283e43532ea91` 只保留为历史失败上下文；修复后的最终开发包已按 cohort `7a8fddf99c57893db93fe1bdd98ab65302bd890d191026495cbbc63ae4652064` 重建、安装并通过包验证、隔离探针和第二次 Desktop live Gate。详见 `docs/evidence/mcp010a/legacy-cleanup.json`、`codex-desktop-post-restart-failed.json`、`codex-desktop-post-restart-success.json`、`dev-app-install.json` 与 `dev-app-probe.json`。

## 能力账本

| 能力 | 状态 | 证据/缺口 |
|---|---|---|
| Codex-only 产品边界 | 已实现 | ADR-0025、Viewer 文案、无模型依赖；P0 Codex Desktop/CLI 宿主兼容 PASS，IDE 为未来兼容 |
| 旧产品硬切 | 已实现 | 删除搜索、恢复包、文档/CI Gate 和新骨架 evidence 已通过；后续任务不得恢复旧入口 |
| MCP010A 旧代码清理 | PASS（可恢复） | 旧入口/评估/孤儿进程/本地残留已清理，standalone Host receipt 仅归档；用户输出与旧 Library 保留；`docs/evidence/mcp010a/legacy-cleanup.json` |
| 可恢复 reset | 已实现 | 分支、tracked diff、untracked/Library archive、恢复读取验证；`docs/evidence/mcp001/manifest.json` |
| Runtime contracts | MCP006 功能核心 + MCP010B/C/D/E/F/Agentic source-focused PASS | MCP006 历史 44 个 JSON Schema；MCP010B/C/D/E/F、Agentic contract family 与 Blender clean-room 增量后当前源码共 191、21/21 active operators。C 的 `ReferenceViewSpec@1`、`CameraCalibration@1`、`RenderSet@2`、comparison/review/human/quality、E 的 AssetPack/Appearance V2、F 的 silhouette target/camera/camera-ref/Rig/Rig hash/SDF/Part/candidate compare，以及 Agentic 的 session/checkpoint/RepairIntent/ReferenceCanvas binding 合同已在对应 producer/consumer 或 isolated receipt 中使用；不等于真实用户视觉质量、PBR likeness、纹理审美、Repair 执行或 360°结论 |
| SQLite Runtime V1 | 已实现（MVP functional core） | Project/Candidate/Version/Snapshot/Job/Event/Audit/Object/ReferenceEvidence/ExportManifest、approval 和 write_idempotency 表；reference CAS admission、typed artifact lineage 与 MCP004–009 原子事务共用 Runtime 单写者；GC/checkpoint 等 post-MVP 治理仍未实现 |
| CAS | 已实现（MCP002 范围） | SHA-256、临时文件+fsync+原子 rename、容量/缺失/篡改/metadata mismatch、备份恢复通过；GC/reachability policy 待 MCP011 |
| Runtime 单写者 | 已实现（单用户 MVP）；共享启动回归 PASS | Runtime migration 前取得 OS 独占 `runtime.writer.lock`；第二 Runtime 返回 `RUNTIME_BUSY`；进程退出自动释放锁。短时 launcher 锁仅启动选主，不能替代 writer lock；适配器退出后复用同一 Ready Runtime、idle owner/passive takeover 与 rogue pre-auth client 隔离回归已在最终源码 PASS |
| authenticated local IPC | 已实现（MCP004 核心范围） | Unix socket 0600、token hash/常量时间比较、错误 token fail closed；candidate/restore/diagnostic-export transaction 已通过 authenticated IPC focused E2E；P0 packaged wiring 待 MCP013，IDE 不在当前 P0 |
| MCP stdio/resources | 已实现（MCP003 P0 只读 + MCP004–009 MVP + MCP010C/E/F source raw + Agentic projection/durable prepare/action-run） | 当前源码默认 54 个只读工具；authenticated/dynamic IPC + explicit opt-in 时为 90 工具（54 read + 36 write）。F 新增 `silhouette_target_get`、`camera_fit_prepare`、`silhouette_rig_hash`、`silhouette_fit_prepare`、`part_contour_fit_prepare`、`silhouette_part_error_get`、`silhouette_candidate_compare`、`boundary_error_get`、`subdivision_artifact_lineage_get` 与两个 target prepare 工具；Agentic 新增四个 projection tool、`session_get`/`checkpoint_get` readback、`design_action_run_get`、`repair_intent_run_prepare` 和 approval-gated session/checkpoint/Primary Form action prepare；`render_pass_get` 通过 MCP image block 返回 CAS PNG，不隐式生成 render。真实用户 likeness、packaged/live C/F Desktop Gate、人评和 360 仍 NOT_RUN |
| Runtime Viewer | 已实现（只读 GLB + reference/AOV compare source） | Tauri `viewer_read_model`/`viewer_artifact_bytes`/`viewer_reference_bytes`/`viewer_render_pass`/`viewer_visual_evidence` 只通过 authenticated IPC 读取项目、候选、参考图、RenderSet、九 AOV 和质量元数据；Three.js/compare 面仅建立临时 scene，不启动 Runtime、不打开 SQLite、不写入；同 cohort packaged CLI read-model 与原生窗口结构探针已通过，Tauri packaged Viewer 控件/无障碍 E2E 仍 NOT_RUN |
| Agentic Design Runtime | projection source/read-only PASS；durable prepare/readback PASS；Primary Form bounded action-run source/runtime PASS；通用 orchestrator/Repair NOT_IMPLEMENTED | Agentic contract family、4 个 read-only projection tool、5 个 durable session/checkpoint prepare/readback tool，以及 `design_action_run_prepare`/`design_action_run_get` 的单 Part `primary-form` slice 已通过各自 Gate；action run 仍锁定 confirm/export 且不写 candidate/version/user data，不能把它当通用多阶段 orchestrator、Repair execution、packaged same-cohort 或视觉 PASS；不改变 `QUALITY_TARGET_NOT_MET` |
| 废弃项隔离 | 已实现（当前树审计范围） | 旧 Provider/Agent/workbench 已由 MCP010A 清理或私有隔离；standalone Host receipt 在 `docs/evidence/archive/`；三个 superseded Skill Bundle 均在 `packages/forgecad-skills/archive/`，由 archive manifest、Skill Gate 和 Runtime build registry filter 约束。后续废弃项仍按 `DEPRECATED_ISOLATION_PLAN.md` 处理 |
| Codex 参考导入 | 已实现（MCP005） | `docs/evidence/mcp005/manifest.json`；PNG/JPEG、limits、authorized root/symlink/path boundary、CAS readback、真实 Codex CLI E2E PASS；Desktop bridge `NOT_RUN / unavailable` |
| Geometry/Render workers | MCP007–009 bounded + MCP010C fixed renderer/source ownership + MCP010D/E source and packaged structural PASS | product-owned Worker 支持 legacy four-pass compatibility、C 的 512×512 perspective/z-buffer/九 PNG AOV、D 的 profile/loft/revolve/sweep/transform/mirror/array/panel/vent/joint/part-output，以及 E 的 512px UV atlas、固定 mikktspace 0.3.0 和 embedded glTF PBR textures；当前 `render_fixed` 先由 Geometry Worker 产出 GLB，再由不依赖 Geometry Worker crate 的 Render Worker 消费；无状态 renderer implementation 位于 `apps/render-core`，窄 façade、source ownership checker 和隔离负向回归已通过；F Viewer source 与 packaged CLI read-model 只读消费这些 AOV；同 cohort packaged D/E raw structural probes已通过；boolean、xatlas、Khronos Validator、Tauri packaged Viewer UI/C、真实视觉/人评仍未完成 |
| GeometryProgram@2 / strict GLB readback | source-focused PASS；B OS memory hard cap deferred | 52-contract V2 geometry/readback、Worker isolation、restore hardening 和 closed GLB profile Gate 已通过；Darwin 512 MiB OS 总内存硬门仍 NOT_RUN，不阻塞 C 的固定 renderer source path |
| Skill Bundle | MCP006 done（development-only）+ MCP010B/D/E active overlays | `packages/forgecad-skills/registry.json` 与历史独立 Bundle，加上 `primitive-blockout@0.2.0`、`hard-surface-detail@0.2.0` 和 `uv-pbr@0.2.0` V2 Bundle；Runtime `skill_list/skill_get`、资源、trust hash、安全 allowlist、Recipe canonical hash、DAG/单位/finite/预算 validator、AssetPack LICENSE/NOTICE、SPDX SBOM/provenance；D/E Bundle 只声明受限 Operator，不携带可执行脚本；distribution signature/revocation 延后 MCP012–013 |
| 几何/轮廓/比例/局部修改 | 已实现（MVP bounded） | 14 部件 fixture、finite/index/lineage/budget/GLB readback PASS；`change_prepare` 仅允许一个稳定 Part 的 typed recompile，不声称通用 mesh delta；真实参考比例仍未完成 |
| UV/PBR/纹理/材质 | MCP010E source + packaged structural PASS_WITH_UNRUN_VISUAL_GATES | Appearance Compiler 输出 512px bounded UV atlas、固定 mikktspace tangent、MaterialZone、embedded PNG baseColor/metallic-roughness/normal/AO/emissive、clearcoat/emissive strength；当前 packaged E 用户参考结构探针通过；xatlas/UDIM/完整色彩管理/视觉 PBR/人评仍未运行 |
| 参考比较/视觉评审 | 部分实现（source-focused PASS_WITH_UNRUN_VISUAL_GATES） | C 已持久化 candidate-bound mask/metrics、RenderSet@2、Codex typed review、HumanVisualReviewReceipt@1 和 QualityReport@2；真实 Codex CLI 已完成六 turn/32-call compare/render/review transport，但机器人 likeness threshold 为 `FAIL_QUALITY_TARGET_NOT_MET`。Viewer compare source implementation、packaged CLI read-model 与原生窗口结构探针已通过只读 IPC/构建/投影测试；Tauri packaged Viewer 控件/无障碍、独立人评阈值和 360°仍 `NOT_RUN/BLOCKED` |
| MCP010 V2 质量轨道 | 010A done；010B deferred；010C source-focused；010D/E source + packaged structural PASS；010F contour-first source slice in_progress | 当前源码 191 contracts、21/21 active operators、54 read + 36 opt-in write = 90、固定 renderer/九 AOV/reference compare/review、D hard-surface operator/Skill、E AssetPack/UV/PBR/MikkTSpace，以及 F SilhouetteTarget/camera-ref/camera-fit/Runtime-owned Primary Form/Rig-hash/boundary-error/Subdivision artifact-lineage source Gate；Agentic projection/durable prepare/readback 与 CAS-bound RepairIntentRun receipt 已独立通过；attempt35 仍为 `provisional retained observation`，状态 `QUALITY_TARGET_NOT_MET`、benchmark eligibility `BLOCKED_INCOMPLETE_BINDING`；attempt5 是历史 CameraCalibrationRef 里程碑；当前最新 package/transport receipt 为 `docs/evidence/mcp010f/real-reference-repair-intent-run-observation-cas-20260817.json`，RepairIntentRun 已完成 evaluate 但严格视觉门仍 blocked，真实 explicit authoring receipt 尚未作为 PASS，视觉质量仍未达标且未晋升；人评、PBR、export/restart hash 和 HQ_360 仍分别 `NOT_RUN/BLOCKED` |
| Candidate/approval/confirm/reject/restore | 已实现（MVP host golden path） | prepare/Job/approval/idempotency/stale/hash/quality/expiry/reject、`change_prepare`、immutable confirm、restore-as-new-version、MCP009 regression PASS；真实 Codex CLI 已完成 appearance→quality→approval→version→CAS GLB export 十二调用 receipt；MCP010A Desktop 最小 activation write 已 PASS，完整 restore/change 与 Desktop 3D write 仍分别按独立 Gate 记录 |
| 导出 | 已实现（CAS-backed MVP GLB） | confirmed quality-passing version 才能 `format=glb/profile=mvp-glb`；confirm 返回 output hash 和 approval receipt；不写任意本机路径，filesystem/package export 留 MCP013 |

## 明确未运行/阻断

- MCP010A 历史 source cohort 与 MCP010B 50/52-contract/Dev.app receipts均按历史范围保留。该历史段当时总源合同 136；B structural source Gate、C `script/test_mcp010c.sh` fixed renderer/九 AOV/reference comparison/review/raw Gate、D hard-surface Operator Gate、E `script/test_mcp010e.sh` AssetPack/UV/PBR/MikkTSpace raw Gate 和 Agentic projection/durable prepare/readback/CAS-bound RepairIntentRun receipt均 PASS。真实 Codex CLI C 六 turn/32-call receipt、attempt35 provisional observation、历史 semantic receipts 与当前 package/transport receipt 均已保存；`ReferenceCanvas@1.bindings`/Runtime target-camera-evidence fail-closed source Gate 已通过，最新 RepairIntentRun 已在同 cohort 完成 render/compare/evaluate 后按 `QUALITY_TARGET_NOT_MET` blocked，真实 explicit authoring receipt 尚未作为视觉 PASS；当前质量仍为 `QUALITY_TARGET_NOT_MET`，未晋升为视觉基准；C/E/F raw/CLI/Agentic receipt 仍只证明结构/局部绑定/传输；Darwin OS 总内存硬门、第三方 xatlas/Validator、真实用户 likeness、packaged Viewer UI/accessibility、PBR 人评阈值、export/restart hash 和 360仍未运行；运行顺序见 `docs/evidence/mcp010f/real-codex-run-inventory.json`；
- `docs/evidence/mcp003/host-matrix.json` 中 protocol adapter、Codex CLI discovery/connection/read-only/mismatch、Codex Desktop discovery/connection/read-only/真实 `initialize.protocolVersion=2025-06-18` 均为 PASS；Desktop override 真实结果为 `HOST_OVERRIDE_IGNORED / NOT_APPLICABLE`；IDE acceptance 保持 `OPTIONAL_NOT_IN_SCOPE`，Computer Use 仅作为 BLOCKED 自动化 surface 证据；
- MCP005 已证明真实 Codex CLI 能把用户授权 PNG 路径的字节传给本地 MCP 并写入 CAS；当前 Codex Desktop bridge 不能提供可验证附件字节，记录 `NOT_RUN / unavailable`。IDE 图片字节仍是未来兼容范围；
- MCP007 geometry、MCP008 appearance/render 和 MCP009 host golden-path evidence 分别见 `docs/evidence/mcp007/`、`mcp008/`、`mcp009/`；真实 Codex CLI 已通过授权图片、geometry/readback、appearance/PBR/fixed-render、quality、confirm/version 和 CAS-only GLB export。`change_prepare`、restore、完整 Desktop 3D write、packaged/production write、silhouette/landmark/region similarity、human score 和 Tauri distribution signing/notarization 仍 `NOT_RUN/BLOCKED`。MCP010A 已通过最小 Desktop activation write probe（临时 `project_create`/readback）；签名诊断的历史事实保留在 `docs/evidence/mcp004/macos-signing-diagnostic.json`；
- MCP010A 的首次 Desktop 重启 receipt 仍位于 `docs/evidence/mcp010a/codex-desktop-post-restart-failed.json`，状态为 `FAIL`，不得改写；第二次成功 receipt 位于 `docs/evidence/mcp010a/codex-desktop-post-restart-success.json`，状态为 `PASS`。010B structural source Gate已通过但 Darwin OS hard cap deferred；010C/D/E source Gate 已通过；010F 当前为 source + packaged read-model `in_progress`，其 Tauri packaged Viewer UI、独立人评和 360 子门仍未运行；
- MCP010B 的 V2 source/CLI structural receipts只覆盖未确认的结构 blockout；Worker isolation/accepted-result peak-RSS gate已通过，但 Darwin 512 MiB OS memory hard cap仍 NOT_RUN。MCP010C 的真实 Codex CLI receipt 已完成六 turn/32-call、九 AOV/reference compare/review transport，但 quality target 未通过；Viewer compare source implementation 已通过本地构建/IPC 测试。MCP010E source raw 已通过离线 AssetPack/PBR/纹理与 MikkTSpace，当前 packaged E 用户参考结构 transport 也已通过；MCP010F source raw 与同 cohort packaged CLI read-model 已通过 Viewer 控件边界、TypeScript/Vite/Tauri 构建和只读 IPC 检查；本轮 attempt28 又完成了 source-built 真实 Codex 的完整 silhouette-first 12-turn transport，但仍为 `PASS_WITH_QUALITY_TARGET_NOT_MET`（primitive route、无 image-derived landmark/region intake），不可替代视觉 likeness；xatlas/Validator、Tauri packaged Viewer UI/C、真人评阈值或 export/restart hash 仍 `NOT_RUN/BLOCKED`；
- 不得将旧 U004/C/E/K/F 测试、旧 GLB 或旧截图当作新产品证据。
本轮轮廓门已统一为当前 Runtime/Viewer/fit-plan 的严格可见视图策略：silhouette IoU 与 Boundary F1 均需 `>=0.90`，bbox/centroid `<=0.02`，landmark coverage `>=0.80`、NME `<=0.03`，region/critical IoU `>=0.85`。历史 0.72/0.75 receipts 保持原样并明确为 exploratory/失败证据；机器人基线仍未达到新门，不能进入表面细节或确认。

2026-08-13 F Viewer binding 子任务：`RuntimeViewer.tsx` 已对 candidate/project/artifact/reference/RenderSet/visual evidence 实施严格同 lineage 校验；跨候选回退、缺失 evidence 和 payload hash 不一致均 fail closed。证据为 `docs/evidence/mcp010f/viewer-candidate-binding-source-20260813.json`，不改变 packaged、人评、PBR、export/restart、360 或真实机器人 `QUALITY_TARGET_NOT_MET` 状态。

2026-08-13 F 合同对齐复核：确认 `QualityReport@2` 不含 `project_id`，Viewer 改用 `ViewerVisualEvidence@1` 外层 project envelope，并要求同 candidate 的 RenderSet、ComparisonReport、artifact/reference hash 和 AOV pass 绑定；无效 payload 保持未运行。source checker、无 `QualityReport.project_id` 正向 fixture、跨候选 RenderSet/缺 reference hash 负向 fixture、TypeScript/Vite/Tauri 与安全门禁均通过。

2026-08-13 F 单部件修正复核：`scripts/probe_mcp010f_part_correction.py` 已强制在设计工具前读取 first-party `ponytail-preflight@0.1.0`，并由静态顺序 Gate 验证；真实隔离探针使用图像派生 `chest-shell` contour 和 Runtime Part 误差表完成五个单变量候选。receipt `docs/evidence/mcp010f/part-correction-source-20260813-followup.json` 的最高 silhouette IoU 为 `0.745895`、Boundary F1 为 `0.330265`，Runtime loss winner 的 IoU 为 `0.745135`，均未达到 `0.90` 严格轮廓门。状态为 `PASS_TRANSPORT_WITH_METRICS` 而非视觉质量通过；无用户持久数据写入、confirm 或 export。F 继续保持 `in_progress`，camera `MISMATCH`、`INCOMPLETE_TRUTH_BINDING`、packaged Viewer UI、人评、PBR、export/restart 和 360 仍按独立 Gate 记录。

2026-08-13 F 多 Part 单部件复核：探针保留 `chest-shell` 路由并新增 `shoulder-armor-left/right` 的参数化 patch route；使用图像派生的右肩 contour 和 `shoulder-contour-mild` 完成 `shoulder-armor-right` 的 Runtime Part-error/proposal/五候选比较。receipt `docs/evidence/mcp010f/part-correction-source-20260813-shoulder-right.json` 的最佳 silhouette IoU 为 `0.744471`、Boundary F1 为 `0.327606`，全部仍为 `QUALITY_TARGET_NOT_MET`。这确认多 Part 归因路径可执行，但没有改善当前基线；draft 仍是 transient annotation，无用户持久数据写入、confirm 或 export。

2026-08-13 F 左肩单部件复核：同一有界探针使用图像派生的 `shoulder-armor-left` contour 读取局部 Part-error/proposal 并完成五候选比较。receipt `docs/evidence/mcp010f/part-correction-source-20260813-shoulder-left.json` 的最佳 silhouette IoU 为 `0.742468`、Boundary F1 为 `0.327530`，未改善肩甲基线，保持 `QUALITY_TARGET_NOT_MET`；draft 为 transient annotation，无持久用户数据写入、confirm 或 export。

2026-08-14 Agentic observation binding repair：`scene_observe_get` 在没有 active snapshot 时不再从多个候选中静默取列表第一项；零候选继续返回 unknown、单候选保留有界兼容回退、多个候选必须显式 `candidate_id` 或 active snapshot，否则返回 `AGENTIC_BINDING_FAIL_CLOSED`。Runtime focused regression 为 2 passed / 0 failed，receipt 为 `docs/evidence/mcp010f/agentic-observation-binding-20260814.json`。这是观察上下文和候选 lineage 的 source PASS，不改变 `QUALITY_TARGET_NOT_MET`、`BLOCKED_INCOMPLETE_BINDING`、camera `MISMATCH` 或 Viewer 质量权威边界。
