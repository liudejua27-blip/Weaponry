# ForgeCAD 当前状态账本

版本：2026-08-09 · 分支：`main` · 工作树 clean · `FGC-MCP005–FGC-MCP010A done（MVP host golden path + Dev activation）`；`MCP010B–F blocked`

## 当前事实

旧桌面工作台、内置模型/Provider、App Server/Protocol、Python Agent、旧脚本、旧合同和旧迁移目录已经从工作树删除；新 Viewer、Runtime、MCP、Worker 协议、Runtime V1 migration、完整首批 contracts、CAS、SQLite repository、OS process lock 和 authenticated local IPC 已写入。`WushenForgeLibrary` 未修改，reset 归档位于 `/tmp/forgecad-mcp001-20260807`。

当前代码提供单用户 Runtime 与 MCP004 事务基座：MCP 实现 `2025-11-25` canonical 与 Codex `2025-06-18` compatibility 初始化、只读 tools/resources、显式 opt-in write、URI/合同 fail closed；Runtime 已具备 SQLite/CAS、candidate/Job/audit、approval/idempotent confirm/reject、restore-as-new-version、path-free diagnostic export 和 authenticated IPC。`forgecad-mcp` 拥有 stdio 并异步启动/连接同一数据根的共享 Runtime；缺失、启动失败或 ready 后崩溃时保持 stdio，最多一次有界重启。短时 launcher 文件锁只负责启动选主，Runtime 自己在 migration 前持有的 `runtime.writer.lock` 才是唯一写者硬边界；正常 MCP 适配器退出不应终止已经 Ready 的共享 Runtime，显式 shutdown/update 才停止它。共享生命周期修复在最终源码的 MCP 26/26、Runtime 30/30 与 lifecycle Gate 中 PASS；同 cohort 开发包已重建并通过隔离探针，第二次 Desktop 重启也已证明 30 个工具、Runtime Ready、doctor、cohort 和临时项目 readback。MVP 仍不使用 TTL lease/heartbeat、daemon 或 broker。真实 Codex CLI diagnostic write 与 reference→CAS GLB host golden path、Viewer read model 均已有历史 evidence。

`MCP005 done` 表示真实 Codex CLI 的授权图片附件进入 CAS；`MCP006 done` 表示十个 first-party declarative Skill Bundle 和安全合同收口；`MCP007 done` 提供有界多 Part GeometryProgram → GLB；`MCP008 done` 增加 hash-bound UV/tangent/PBR、固定四 pass render 和真实 GLB Viewer；`MCP009 done` 增加 limited quality、stable-Part `change_prepare`、immutable confirm/restore 和 CAS-backed `mvp-glb` receipt，并已有真实 Codex CLI 的十二调用 host golden-path PASS。MCP009 的 reference compare 仍明确是 aspect-ratio limited，不是 silhouette/landmark/region 完成。首个机器人视觉/真人门属于 MCP010B–F；复杂 Job 恢复属于 MCP011；通用第三方生命周期属于 MCP012；签名/notarization、packaged Desktop write 和跨类别门属于 MCP013。未运行项不得写成 PASS。Desktop 只显示 Runtime Viewer，不提供聊天、上传、模型选择或 API Key。

用户已批准 MCP010 高质量硬表面升级轨道，FGC-MCP010A 已完成。权威重排、MCP/Runtime/Geometry Worker/Viewer 离线构建、用户级 ad-hoc Dev.app 安装、Runtime `Ready`、隔离项目创建、真实 Codex CLI 诊断写链、用户配置切换和第二次真实 Codex Desktop Gate 均有 PASS evidence。第一次用户重启后的真实 Codex Desktop Gate 已实际运行并 `FAIL`，该历史 receipt 保留；修复后第二次重启观察到 30 个工具、`Ready`、`doctor` ready、cohort match、临时 `project_create` 和 `project_get` readback。最新 cohort 为 `7a8fddf99c57893db93fe1bdd98ab65302bd890d191026495cbbc63ae4652064`；`GeometryProgram@2`、九 AOV、参考像素指标、高细节 Operator、离线 AssetPack、Skill `0.2.0` 和 Viewer compare 仍全部是 `目标设计 / unavailable`；当前事实仍是 44 个 Schema、17 个只读 + 13 个写工具、十个 `0.1.0` Skill Bundle。

MCP010A 已完成一轮可恢复旧代码清理：先建立并验证 Git bundle、tracked/local archive、worktree patch 与 SHA-256 清单，再删除旧 Provider/Planner/CSG 评估、packaged Agent sidecar 和旧 Gate07 配置，终止 5 个孤儿 Python Agent，隔离旧虚拟环境、Host/Proxy、本地旧安装包、日志、缓存和 7 组 stale endpoint；无用 Rust API、依赖和旧 Tauri resource protocol 同步移除。两份 standalone Host receipt 仅以 `SUPERSEDED` 状态保存在 `docs/evidence/archive/`。`WushenForgeLibrary`、Runtime V1 数据、Codex 历史及约 2.5 GiB 可能含用户 GLB/PNG 的 `output/` 均未修改。第一次 Desktop live Gate 使用的旧 cohort `e5fd7da79576fd022894838c5ab9b0532b7aef735abc42b86f3283e43532ea91` 只保留为历史失败上下文；修复后的最终开发包已按 cohort `7a8fddf99c57893db93fe1bdd98ab65302bd890d191026495cbbc63ae4652064` 重建、安装并通过包验证、隔离探针和第二次 Desktop live Gate。详见 `docs/evidence/mcp010a/legacy-cleanup.json`、`codex-desktop-post-restart-failed.json`、`codex-desktop-post-restart-success.json`、`dev-app-install.json` 与 `dev-app-probe.json`。

## 能力账本

| 能力 | 状态 | 证据/缺口 |
|---|---|---|
| Codex-only 产品边界 | 已实现 | ADR-0025、Viewer 文案、无模型依赖；P0 Codex Desktop/CLI 宿主兼容 PASS，IDE 为未来兼容 |
| 旧产品硬切 | 已实现 | 删除搜索、恢复包、文档/CI Gate 和新骨架 evidence 已通过；后续任务不得恢复旧入口 |
| MCP010A 旧代码清理 | PASS（可恢复） | 旧入口/评估/孤儿进程/本地残留已清理，standalone Host receipt 仅归档；用户输出与旧 Library 保留；`docs/evidence/mcp010a/legacy-cleanup.json` |
| 可恢复 reset | 已实现 | 分支、tracked diff、untracked/Library archive、恢复读取验证；`docs/evidence/mcp001/manifest.json` |
| Runtime contracts | 已实现（MVP functional core） | 44 个 JSON Schema、Rust records、canonical hash、Appearance/Change/Artifact/Render/Quality/Export records；manifest 无漂移；reference similarity schema 仍只允许明确 limited implementation |
| SQLite Runtime V1 | 已实现（MVP functional core） | Project/Candidate/Version/Snapshot/Job/Event/Audit/Object/ReferenceEvidence/ExportManifest、approval 和 write_idempotency 表；reference CAS admission、typed artifact lineage 与 MCP004–009 原子事务共用 Runtime 单写者；GC/checkpoint 等 post-MVP 治理仍未实现 |
| CAS | 已实现（MCP002 范围） | SHA-256、临时文件+fsync+原子 rename、容量/缺失/篡改/metadata mismatch、备份恢复通过；GC/reachability policy 待 MCP011 |
| Runtime 单写者 | 已实现（单用户 MVP）；共享启动回归 PASS | Runtime migration 前取得 OS 独占 `runtime.writer.lock`；第二 Runtime 返回 `RUNTIME_BUSY`；进程退出自动释放锁。短时 launcher 锁仅启动选主，不能替代 writer lock；适配器退出后复用同一 Ready Runtime、idle owner/passive takeover 与 rogue pre-auth client 隔离回归已在最终源码 PASS |
| authenticated local IPC | 已实现（MCP004 核心范围） | Unix socket 0600、token hash/常量时间比较、错误 token fail closed；candidate/restore/diagnostic-export transaction 已通过 authenticated IPC focused E2E；P0 packaged wiring 待 MCP013，IDE 不在当前 P0 |
| MCP stdio/resources | 已实现（MCP003 P0 只读 + MCP004–009 MVP local opt-in wire）；MCP010A lifecycle/activation PASS | 默认 MCP `2025-11-25` canonical + Codex `2025-06-18` compatibility initialize、只读 tools（含 runtime_status/doctor）、forgecad-mcp 内置异步 Runtime supervisor、Starting/Ready/Degraded/Restarting/Busy、一次有界 restart、Runtime 缺失/崩溃时 stdio survival、resources/list/read/templates、annotations、URI/contract fail closed；真实 Codex CLI reference→geometry→appearance→quality→confirm→CAS GLB 已 PASS。第一次 MCP010A Desktop live Gate 因 stale handoff、无活 Runtime 和 write opt-in 未继承而 FAIL；修复 tests、当前同 cohort build、package verify、隔离探针与第二次 Desktop 重启后的 30 工具/Ready/cohort/project readback 均 PASS，成功 receipt 已保存 |
| Runtime Viewer | 已实现（只读 GLB read model + canvas） | Tauri `viewer_read_model`/`viewer_artifact_bytes` 只通过 authenticated IPC 读取项目、候选、GLB bytes、版本和当前快照；Three.js 仅建立临时 scene，不启动 Runtime、不打开 SQLite、不写入 |
| Codex 参考导入 | 已实现（MCP005） | `docs/evidence/mcp005/manifest.json`；PNG/JPEG、limits、authorized root/symlink/path boundary、CAS readback、真实 Codex CLI E2E PASS；Desktop bridge `NOT_RUN / unavailable` |
| Geometry/Render workers | 已实现（bounded MVP） | product-owned worker 支持 canonical box/cylinder/sphere、UV/tangent/material lowering 和确定性 beauty/silhouette/normal/part-ID 软件 render；profile/boolean/全 AOV 不在 MVP |
| Skill Bundle | MCP006 done（development-only） | `packages/forgecad-skills/registry.json` 与十个独立 `bundles/<skill_id>/0.1.0`；Runtime `skill_list/skill_get`、资源、trust hash、安全 allowlist、Recipe canonical hash、DAG/单位/finite/预算 validator、合成 fixture/receipt、LICENSE/NOTICE、SPDX SBOM/provenance；真实 Codex CLI 只读 registry E2E PASS；distribution signature/revocation 延后 MCP012–013 |
| 几何/轮廓/比例/局部修改 | 已实现（MVP bounded） | 14 部件 fixture、finite/index/lineage/budget/GLB readback PASS；`change_prepare` 仅允许一个稳定 Part 的 typed recompile，不声称通用 mesh delta；真实参考比例仍未完成 |
| UV/PBR/纹理/材质 | 已实现（MVP bounded） | Appearance Compiler 输出 glTF metallic-roughness、UV、tangent、MaterialZone；当前无纹理烘焙/UDIM/完整色彩管理 |
| 参考比较/视觉评审 | 部分实现（明确 limited） | `quality_get` 有 candidate/GLB/PBR/fixed-render hard checks 和 aspect-ratio compare；silhouette IoU、landmark、region、Codex typed review 和用户评分仍 `NOT_RUN` |
| MCP010 V2 质量轨道 | 010A done（Desktop attempt 1 FAIL 保留；attempt 2 PASS） | 首轮 Desktop 重启实时读回 FAIL，receipt 原样保留；修复、最终 `release:mvp`、cohort Dev.app 重建、ad-hoc deep-strict、package verify、隔离 probe 和第二次 Desktop live Gate 均 PASS。010B–F 继续 blocked/unavailable |
| Candidate/approval/confirm/reject/restore | 已实现（MVP host golden path） | prepare/Job/approval/idempotency/stale/hash/quality/expiry/reject、`change_prepare`、immutable confirm、restore-as-new-version、MCP009 regression PASS；真实 Codex CLI 已完成 appearance→quality→approval→version→CAS GLB export 十二调用 receipt；MCP010A Desktop 最小 activation write 已 PASS，完整 restore/change 与 Desktop 3D write 仍分别按独立 Gate 记录 |
| 导出 | 已实现（CAS-backed MVP GLB） | confirmed quality-passing version 才能 `format=glb/profile=mvp-glb`；confirm 返回 output hash 和 approval receipt；不写任意本机路径，filesystem/package export 留 MCP013 |

## 明确未运行/阻断

- 最终修复源码的 `script/test_mcp004.sh` 已 PASS（MCP 26/26 + shared lifecycle），`npm run release:mvp` 已 exit 0（Runtime 30/30、MCP 26/26、44 contracts、MCP005–009、Viewer/Tauri、docs/security）。cohort `7a8fddf99c57893db93fe1bdd98ab65302bd890d191026495cbbc63ae4652064` 的 Dev.app 安装、ad-hoc deep-strict、`package:verify`、隔离 probe 与第二次真实 Desktop live Gate 也已 PASS；真实 Codex CLI write/host golden path 已有历史 PASS，clean production packaged Tauri E2E、像素/区域视觉和真人评分仍明确未运行；此前一次旧 Tauri target exit 130 作为历史运行记录保留；
- `docs/evidence/mcp003/host-matrix.json` 中 protocol adapter、Codex CLI discovery/connection/read-only/mismatch、Codex Desktop discovery/connection/read-only/真实 `initialize.protocolVersion=2025-06-18` 均为 PASS；Desktop override 真实结果为 `HOST_OVERRIDE_IGNORED / NOT_APPLICABLE`；IDE acceptance 保持 `OPTIONAL_NOT_IN_SCOPE`，Computer Use 仅作为 BLOCKED 自动化 surface 证据；
- MCP005 已证明真实 Codex CLI 能把用户授权 PNG 路径的字节传给本地 MCP 并写入 CAS；当前 Codex Desktop bridge 不能提供可验证附件字节，记录 `NOT_RUN / unavailable`。IDE 图片字节仍是未来兼容范围；
- MCP007 geometry、MCP008 appearance/render 和 MCP009 host golden-path evidence 分别见 `docs/evidence/mcp007/`、`mcp008/`、`mcp009/`；真实 Codex CLI 已通过授权图片、geometry/readback、appearance/PBR/fixed-render、quality、confirm/version 和 CAS-only GLB export。`change_prepare`、restore、完整 Desktop 3D write、packaged/production write、silhouette/landmark/region similarity、human score 和 Tauri distribution signing/notarization 仍 `NOT_RUN/BLOCKED`。MCP010A 已通过最小 Desktop activation write probe（临时 `project_create`/readback）；签名诊断的历史事实保留在 `docs/evidence/mcp004/macos-signing-diagnostic.json`；
- MCP010A 的首次 Desktop 重启 receipt 仍位于 `docs/evidence/mcp010a/codex-desktop-post-restart-failed.json`，状态为 `FAIL`，不得改写；第二次成功 receipt 位于 `docs/evidence/mcp010a/codex-desktop-post-restart-success.json`，状态为 `PASS`。因此 010A 已结束，010B–F 保持 `blocked`，须由后续 Goal 显式领取；
- 不得将旧 U004/C/E/K/F 测试、旧 GLB 或旧截图当作新产品证据。
