# Weaponry 权威状态与版本真值

> 2026-08-30 `WPN-KNIFE-BRIEF-RUNTIME-001` 权威增量：Knife Production Brief 已成为
> `reference_intake` 下的 Runtime-owned durable aggregate。初始 intake 可保存 `conflicted/BLOCKED`，
> 但不会创建 candidate、推进 Stage、confirm、version 或 export；successor 必须引用 exact parent
> ID/hash，并原样保留 reference lineage、source claim map 与 conflict identity，只有 resolution、授权、coverage
> 和 acceptance gate 状态可变；required gates、promotion labels 与布尔政策仍冻结。当前 source 是 662 schemas / 11 façades / 127 active operations / 127 closed
> request schemas；compatibility 131/95/226 不变。Dragonfang isolated live intake、immutable successor
> replay 与 Runtime restart exact-hash readback 已 PASS；商业质量仍 `NOT_PROVEN`。
> 本原子 final architecture-fast same-cohort=`ef4a5ff42c608a288fccdebdadbd3fbb65bf06798b87d16e0e43baaf695a58b8`，
> `107/0/0`、195s、`source_drift=false`；未重跑完整 Runtime qualification，不能把 fast suite 或
> source/focused evidence 写成 Dragonfang High、视觉或商业验收。

> 2026-08-30 contracts-only successor fixture：`dragonfang-kukri-brief-resolved-001` 通过 closed
> schema/checker，parent ID/hash 精确绑定 `dragonfang-kukri-brief` /
> `27c8893f4ff982ff90ad62b21df18e00b30db691b8d9d169ddb8cbff2235d514`，三条 source identity claims、所有
> source claim objects 和 conflict identity 原样保留。它冻结“尼泊尔-屠龙”、Hero `25k–45k`、4K
> authoring master、2K shipping 和 Unreal `5.6-or-later`，但 authorization 仍为 `source-asserted-not-runtime-bound`，不
> 是 Runtime/Store/CAS live 记录；isolated live probe 已从该模板生成新的 runtime-bound canonical
> successor 并持久化。Brief@1 的 `resolved_width/height` 与 `shipping_width/height` 已分别
> 绑定 retained `hero`/`production` claims。acceptance 的 status/gate_statuses/blocking_reasons 已作为 resolution-owned 投影，
> 但 required_gates、promotion_labels 与四项布尔政策仍冻结。live successor 的 K0=`pass`，
> authoring eligibility=`ELIGIBLE`，`WPN-KNIFE-HIGH-001` 已领取；其余视图、engine validation、human
> review 门仍阻断，High mesh、视觉和商业质量没有晋级。

> 2026-08-30 final combined architecture source truth：Authoring、Evaluation、Surface、Presentation、Delivery 五域均已由 direct typed Router/service 承接；这表示当前路由边界已闭合，不表示 Runtime/Store 五域源码与 record family 已物理抽空。Runtime/Store/MCP final root 为 `52,542 / 79,841 / 1,081` 行，MCP `agentic_write_tools.rs=22,800`，Runtime root modules=`92`。
> 当前源码元数据：`schema_count=658`、`schema_set_sha256=29784beef684ae4334bfc2983f19fec25694c632ed11e0840bd12b0e9838f0f1`、`runtime_source_sha256=085714f60445ed831809564d5324424aed9a734f7dec3c782a90876fa1c5d708`、`truth_canonical_sha256=6f90c5fcb2fb2218b04c871d260964623729da6ba4adf9b7cfe1d5082c154cc3`；source-only compatibility summary `cohort=null`、`131/95/226`、SHA-256=`1eb6cf5125e4d72aa2e8eef0139ff11de8c69b615d47cb66f70b666fb83377ca`。
> 默认 11 façade 的 active request Schema 为 **125/125**，blocked=0，Runtime fallback=0；中央 capability maturity 仍有 `Partial`。Store 新增 `GameAssetDeliveryLinkRecord` borrowed repository；`ApprovalLifecycle`、game weapon `socket/anchor`、`ReadModel/QualityEvidence` 与其余 Presentation repository 仍未抽取。226 legacy registry 已 feature 隔离，但大量 compatibility handler 仍编译。
> 最终 architecture-fast same-cohort=`265914b6699d101eb69030947c2419e26e7a99ceef52a63a3c834989af88f28c`，`87 passed / 0 failed / 0 ignored`，182s，`source_drift=false`；local receipt SHA-256=`6487663b3aed0a0c80a63ebad7ff6c344f1fd0ccc283f4a52f7ed3e703fc74f8`。本轮未重跑 full Runtime qualification，前一完整 `554 passed / 0 failed / 37 ignored` 基线保持不变。
> 本轮仅完成架构 source/fast regression，无用户数据变更、无视觉或商业 promotion；没有提升 High→Low→UV→Bake、材质、FPS、引擎、人审或商业质量。下一原子严格为 `WPN-ARCH-MCP-SPLIT-001 → WPN-ARCH-RETIRE-001`；不可变历史 receipts 未改写，mutable Stage0 current-source truth 已按最终源码重建并通过 checker。

> 2026-08-30 `WPN-ARCH-COMPAT-001` 权威增量：默认 MCP 与历史 226-tool 实现已在编译图上物理隔离。
> fresh dep-info 为 default=6 MCP sources、compat=39；默认 `main.rs`=1,081 行，显式
> `compat_main.rs`=20,513 行，兼容 manifest 仍为 131/95/226。默认 11 façade 对应 125 个 active
> operation，但 request Schema closure 只有 12/125，其余 113 个仍由 Runtime parser fail closed，故
> 当前参数合同状态必须写为 `PARTIAL`，不能宣称所有 façade 已完全 closed。Runtime `lib.rs`=52,854 行、Store
> `lib.rs`=80,878 行；Surface 15 个 active operation 已由 direct typed service 承接，但 Evaluation、Presentation、
> Delivery 与其余 legacy-dispatch 域仍未完成物理拆分；视觉和商业质量真值不变。

> 2026-08-30 `WPN-ARCH-SURFACE-001` current-source truth：`surface_pipeline` active=15（8 read / 7 write），
> exact operations 由 `packages/forgecad-contracts/profiles/weaponry-knife-p0.json:122-162` 固定；Runtime
> 通过 `surface_service` direct typed route，只有 `production_weapon_retopology_cage_source_bundle_prepare/get`
> 是 compatibility-only alias。中央 `formal_high_low_cage_bake` mapping 为 `Partial`，首个物理 Store seam 是
> 借用 `&Store` 的 `SurfaceRepository`，不复制 SQLite/migration/CAS owner；新 bake materialization producer
> unavailable，不能产生新的 materialized bake。Surface repository extraction 只覆盖首个 formal aggregate，
> Evaluation/Presentation/Delivery 仍 legacy。Runtime `lib.rs`=52,854、Store `lib.rs`=80,878、MCP
> `main.rs`=1,081、Runtime root modules=92；active schema=12/125（Runtime-validated=113），MCP
> default=22/22、compat=230/230。无用户数据触碰、无视觉或商业 promotion，历史 receipts 不改写。

> 2026-08-30 `WPN-ARCH-BASELINE-FAST-003` 权威增量：fresh Runtime/Geometry/High/Render 四身份
> architecture-fast gate 在 cohort=`21911f161f4433ac0f40e3da5d47d0018b14fb8329eafe2792eec650fc29692f`
> 下为 53 passed / 0 failed / 0 ignored、194s≤900s、无 source drift；三类 Codex host
> 与 Runtime IPC timeout 均为 180s。37 ignored inventory 分为 21 platform-limited、10 fixture-required、
> 3 historical-compatibility、3 real-coverage-gap；current-cohort execution=`0/37`，不得写成覆盖通过。
> 完整 `554/0/37` 昂贵路径保持独立；local receipt SHA-256=`595a42990c22efe716e390e8bbf58b168c13f18fbe7ad6004b0c85507f5e1e2c`；
> 四个非 Authoring 域、巨型根、视觉和商业质量均未因此晋级。

> 2026-08-30 `WPN-ARCH-BASELINE-002` 权威结论：`done_same_cohort_runtime_full_pass`。已建立 fresh-target 四身份
> same-cohort Runtime full harness；timeout/art-decision/composite 三项旧失败已清，后续暴露的
> High Worker 可达性与 Authoring topology/identity 问题已定向 PASS。最终 source
> cohort=`a6f28cdef0528decbab7cee341f2b3ae4c06cd1adb522a86c9de63d416ef9ff9`，
> Runtime full=`554 passed / 0 failed / 37 ignored`（591 total），libtest=`10022.89s`、harness=`10147.37s`，
> 四身份一致且测试前后无 source drift。
> 这只是架构回归基线；不改变 High/Low/UV/Bake、视觉、人审、引擎和 commercial=`NOT_PROVEN`。

> 2026-08-30 `WPN-ARCH-MCP-ROUTER-001` 权威增量：默认 Knife façade 先由 profile 验证，再引用
> `forgecad-contracts::weaponry_domain_map` 的 domain/execution-target 真值，通过
> `Runtime::invoke_weaponry_operation` 或 `weaponry_domain_operation` IPC 执行。兼容注册表没有恢复为
> 默认 Action Space；`runtime_status` 只由 MCP supervisor 投影，Runtime 不拥有该状态。当前不能宣称
> 五域重构完成：Evaluation、Presentation、Delivery 仍回落旧 Runtime dispatch；Surface 已由 direct typed service
> 承接 15 个 active operation，但其 formal high-low-cage-bake 仍仅为 `Partial` mapping + borrowed repository；Store 仅
> transaction、CurveGraph、EvaluatedMesh 三个 family 进入 borrowed AuthoringRepository；旧 handlers、
> 巨型根仍存在。本 Router 轮次的 18 项旧基线失败已由上方
> `WPN-ARCH-BASELINE-002` 的 554/0/37 same-cohort full 结果取代；商业质量账本不变。

> 2026-08-29 `WPN-KNIFE-CURVE-001` 当前状态为
> `done_source_durable_evaluated_mesh_structural_no_downstream_quality`：manifest/schema=`589`，
> Profile 投影为 `586`；旧 MCP `218/218`、默认 11 façade、显式 compatibility `226` 和
> `KnifeCurveModifierGraph@1` compatibility replay 均保持。Core evaluated-mesh=`17/17 PASS`、
> Store full=`169/169 PASS`、Runtime `knife_curve_`=`6/6 PASS`（evaluated public=`4/4`）、
> MCP full same-cohort=`221/221 PASS`（evaluated=`3/3`）。这些只证明 source/structural/durable
> evaluated-mesh；没有创建或晋级 durable candidate/version。GLB/High/Low/UV/Bake/视觉/人审/引擎均
> `NOT_RUN`，commercial=`NOT_PROVEN`，旧 receipt 未改写。

> EvaluatedMesh parent lookup 由 Runtime 从 source revision + ModifierGraph/5 个 semantic hashes
> 派生，不接受 caller-facing `curve_graph_lookup_key`；Result 回报派生 lookup，旧
> `KnifeCurveModifierGraph@1` contract 保持兼容。sample/evaluated positions 固定 1nm 量化。

> 2026-08-29 刀类当前机器真值：`WPN-KNIFE-PROFILE-001` 已闭合 11 个默认
> bounded façade 和显式 226-tool compatibility replay。Curve + ModifierGraph 公共纵切已持久化
> CurveSet/SampleSet/Graph/Dependency/Recompute 五类对象与语义 hash，但
> `evaluation_status=curve-sampled-modifier-recompute-planned-no-mesh@1`。它没有 evaluated mesh、
> GLB 或图像；High/Low/UV/Bake、材质、FPS、引擎与人审仍未完成，不得从结构 PASS
> 推导刀类商业 PASS。

> 2026-08-29 当前 source-only 机器真值为 **586 schemas / compatibility 131 read + 95 opt-in write = 226 tools / default 11 façades**。独立编译 summary、源码 parser、manifest/schema content set 和 Stage 0 checker 已统一，旧 receipts 未改写。公共 Curve/ModifierGraph 纵切已接入 Core/Store/CAS/Runtime/MCP，但评估网格和商业验收仍不存在，质量继续为 `QUALITY_TARGET_NOT_MET / commercial=NOT_PROVEN`。

> 2026-08-29 Weaponry Authoring 真值：`AuthoringMeshTransaction@1` 只允许 SplitEdge、MoveVertices、FaceExtrude 的有序 closed journal，生成元素只能引用更早 command。Runtime 派生 child IDs/hashes，两次执行 pure kernel 比对确定性后才 staging CAS；Store 在一个 SQLite transaction 中公布所有 child revisions、aggregate receipt、幂等绑定与 reachability。该原子可见性不等于文件系统 CAS 与 SQLite 的分布式 ACID；CAS 失败通过 reservation cleanup + 零可见数据库行闭合。

> 2026-08-29 `FPS-PRODUCTION-04BI-HERO-UV-DURABLE`：Runtime-owned 4096 Hero UV 已绑定 source Low `28bba154…596e`，layout=`d1778e47…f0a6`、link=`dd57a9c9…4d32`，double replay 与 restart exact=true；UV0/UV1 overlap/OOB=0，32px mip padding、seam/hard-edge congruence 与 Mikk replay structural PASS。它不推进 Stage，也不代表 artist unwrap、geometric bake、视觉、人评或引擎通过；`QUALITY_TARGET_NOT_MET / commercial=NOT_PROVEN` 保持。

> 2026-08-29 `FPS-PRODUCTION-04BH-APPEARANCE-V3`：reviewable derived candidate=`candidate-79ec4472262847eb9be8c70cdbd2dfbe` / artifact=`68897ddd…d91e` 已由 Runtime 生成，绑定 `AppearanceProgram@3`=`f30ab7e2…c326`、decal/wear/clearcoat layer stack=`6355fcb5…31fd`、2K TextureBuild、approved-camera 九 AOV 与 Beauty=`7adfe096…0c7`。该派生候选不改写当前 ProductionStage head；其 surface bake 是 evaluated self-surface，不是正式 High→Low Cage projection。人评、引擎、confirm/version/export 均未发生，`QUALITY_TARGET_NOT_MET / commercial=NOT_PROVEN` 不变。

> 2026-08-29 PBR 美术渲染增量：Runtime 已从冻结 High 源生成 `candidate-c83f67…04a4` 与 `aed91a0c…361d`，4 个 MaterialZone、embedded CC0 texture pack、UV/tangent/GLB readback 和批准后 3/4 相机九 AOV 均为 same-cohort structural PASS；Beauty=`99ce1bb3…17f2`。权威边界不变：该候选未确认，正式 Hero UV durable、High→Low Cage Bake、AppearanceProgram@3 分层层图、真人审稿和商业引擎验证均未完成，视觉比较仍 `QUALITY_TARGET_NOT_MET`，商业质量仍 `NOT_PROVEN`。

> 2026-08-29 权威增量：用户接受 wide candidate 进入生产迭代，新增非晋级 source bundle `c73271da…cd58`。其 High/Low/Cage 分别为 `3caa4a87…d7596` / `28bba154…596e` / `338cabf1…a1c3`，Low 为 1818 tris，Runtime restart exact=true。该 bundle 不替换项目 Stage head、不 confirm candidate、不创建版本；4096 Hero UV 仅完成 Worker preflight，因 224 tris stretch 超限且 durable record 未创建，UV/Bake/PBR/human/engine/export 继续分别记录为 `PREFLIGHT_REPAIR_REQUIRED/NOT_RUN`。

> 2026-08-29 权威增量：`FPS-FORM-04BE-U` 保留旧 exact no-regression 失败事实，另新增用户授权的 core-raster absolute tradeoff=`0.01`。该 successor policy 下选中 wide `candidate-50b6981546f74bca9c2ca774ac5c1b00` / state `b70f01f6…55c` / artifact `3caa4a87…d7596` 作为下一 Form repair 基线，但它未确认、未创建版本，也未取代当前已持久化的权威父候选。`proposal_form_art_evidence_ready=false`，Stage=`camera-calibrated`，FormQualityV2/secondary=`NOT_CREATED`，High/Low/UV/Bake/PBR/human/engine/export 仍未推进。

> 2026-08-29 权威增量：`FPS-FORM-04BE-T` 产生四个未确认 layered candidates；它们的 `right.trigger-void` 均已打开，最佳 wide candidate=`candidate-988c9424c9244796a6f2fe2715f3e784`、IoU=`637‰`、aggregate score 提升，但 exact six-view no-regression=false，不能替换当前权威父候选。权威 candidate/state/artifact/program 仍为 `candidate-6f6ddeff…cf8a` / `1a0bf325…60d2` / `1039baef…15c80` / `a9d447e5…e7f11`。FormQualityV2=`NOT_CREATED`，appearance/UV/PBR/High→Low→Bake、人评、引擎、confirm/version/export 均未推进。

> 2026-08-28 `FPS-FORM-04BE-L` 权威增量：用户明确授权 `receiver-upper` 单 Part 试验；4 个 closed profile 已由 Runtime 唯一物化并完成 strict GLB、54 AOV 与 restart exact。视觉门全部失败，故权威 candidate/state/artifact/program 继续为 `candidate-6f6ddeff…cf8a` / `1a0bf325…60d2` / `1039baef…15c80` / `a9d447e5…e7f11`。下一 `FPS-FORM-04BE-M` 仅做只读遮挡归因；质量仍 `QUALITY_TARGET_NOT_MET`。

> 2026-08-28 `FPS-FORM-04BE-J/K` 权威增量：现有 composite proposal 合同不新增 Schema/tool，只将闭合 profile 枚举扩展为 4 个真孔 `@1` 与 4 个相机映射真孔 `@2`。Runtime 物化 8 个 reviewable candidate；8/8 strict GLB、8×54 AOV、8/8 restart exact PASS，但视觉选择 8/8 rejected。权威 candidate/state/artifact/program 仍为 `candidate-6f6ddeff…cf8a` / `1a0bf325…60d2` / `1039baef…15c80` / `a9d447e5…e7f11`。下一 `FPS-FORM-04BE-L` 仅做只读遮挡归因；Stage/secondary/FormQualityV2/version/export 不变，质量仍 `QUALITY_TARGET_NOT_MET`。

> 2026-08-28 `FPS-FORM-04BE-I` 权威增量：现有 composite proposal 合同仅扩展 4 个产品注册 `side-panel-a` profile，未新增 Schema 或 MCP tool；Runtime 唯一物化 4 个 reviewable candidate 并绑定同源六视图证据。source cohort=`8bc7308d…ae37`，4×strict GLB + 4×54 AOV + restart exact readback PASS，但所有 `CrossView.non_regressing=false` 且 left trigger void sealed。权威选择仍是 parent candidate=`candidate-6f6ddeff…cf8a` / program=`a9d447e5…e7f11`；step 2 未授权，Stage/secondary/FormQualityV2/version/export 不变，质量仍 `QUALITY_TARGET_NOT_MET`。

> 2026-08-28 `FPS-FORM-04BE-H` 权威增量：新增严格只读、Runtime-owned、hash-bound 的顺序双 Part aperture sensitivity plan。真实 D1/restart canonical=`fe7c8ecf…680b61`，两次会话均先读 `ponytail-preflight@0.1.0`，SQLite/CAS 零写。绑定 proposal GeometryProgram=`a9d447e5…e7f11`；先 `side-panel-a`、后条件性 `receiver-upper`，总试验预算 8，本工具实际执行 0。公共面为 **583 schemas / 130 read + 94 opt-in write = 224 tools**；candidate/Stage/secondary/version/export 不变，质量仍 `QUALITY_TARGET_NOT_MET`。

> 2026-08-28 `FPS-FORM-04BE-G` 权威增量：新增 `ProductionWeaponFormArtVisibilityCalibrationGetRequest/Result@1` 与 1 个默认只读 MCP surface；Runtime 唯一负责重读 04BE-F diagnostic、before/after FormArt/GLB、ReferenceCanvas 与批准 CameraRig，并由隔离 Render Worker 派生 pixel→triangle/source/depth/Part-ID/silhouette attribution。真实 D1/restart canonical=`3d3cd762…e7196`，SQLite/CAS 零写。结果固定 `TWO_VIEW_SIDE_APERTURE_OCCLUDERS_CALIBRATED_REPAIR_PLAN_ONLY / QUALITY_TARGET_NOT_MET / FormQualityV2=NOT_CREATED`；只授权后续 plan，不授权 geometry。公共面为 **581 schemas / 129 read + 94 opt-in write = 223 tools**；candidate=`candidate-6f6ddeff…cf8a`、Stage=`camera-calibrated`、secondary/version/export 均保持原值。

> 2026-08-28 `FPS-FORM-04BE-E` 权威增量：现有 composite proposal 合同新增一个固定 registered profile 分支，Runtime 新增 rear-stock half-Y/flat-Z 纯 mutator；未新增 MCP 工具或 Schema 数量。真实 D1 新增 proposal candidate=`candidate-6f6ddeff…cf8a` 与 composite evidence sidecar，绑定 original source、approved RigV2/rear3q camera=`9d8e590e…3abb`、fresh baseline=`2e983648…ae99`、CrossView=`64eb80b5…43ea`、FormArt=`0af3ded0…5bff`。真实状态固定为 `SIX_VIEW_EVIDENCE_BOUND_NOT_PROMOTED / QUALITY_TARGET_NOT_MET`；FormQualityV2 row、secondary Stage、version、export 均不存在。公共合同面保持 **577 schemas / 127 read + 94 opt-in write = 221 tools**。

> 2026-08-28 `FPS-FORM-04BE-D` 权威增量：新增 2 个 closed contracts 和 1 个默认只读 MCP tool；没有新增 Store 表、candidate、version、artifact 或 CAS object。真实 D1 plan canonical=`d6f74060…85fd` 在 Runtime 重启后 exact equal，业务表 canonical=`62bb5823…a9c6` 与 CAS tree=`dc47c325…6d07` 前后相同。plan 只登记 `rear-stock-owner-void-half-y-flat-z@1` 下一修复，不构成 RepairIntent 执行或艺术批准。当前合同面 **577 schemas / 127 read + 94 opt-in write = 221 tools**；Stage=`camera-calibrated`、secondary=`NOT_CREATED`、FormQualityV2 未创建，`QUALITY_TARGET_NOT_MET / commercial=NOT_PROVEN` 不变。

> 2026-08-28 `FPS-FORM-04BE-C` 权威增量：新增 `ProductionWeaponFormArtCompositeEvidenceRecord@1` sidecar 1 条，绑定 parent canonical=`d64e4589…03d8`、final candidate=`candidate-f4a7d…06fa`、same-cohort baseline=`ba5e08dc…b32a`、CrossView=`c93ccb2c…02f4`、proposal FormArt=`e1240c5f…47e`、receipt=`7fb0a11d…5106`、record canonical=`86043db0…0496`。隔离重启 GET 返回同一 hash 且零写；6 视图 Part-ID 都 observed，但 CrossView left 回归且 strict owner-void 未过，因此 sidecar 仅为 `SIX_VIEW_EVIDENCE_BOUND_NOT_PROMOTED`。当前合同面 **575 schemas / 126 read + 94 opt-in write = 220 tools**；FormQualityV2 row 未创建，Stage=`camera-calibrated`，secondary/version/export 未创建，`QUALITY_TARGET_NOT_MET / commercial=NOT_PROVEN`不变。

> 2026-08-28 `FPS-FORM-04BE-B` 权威增量：真实 D1 新增 composite link 1 条，绑定 proposal=`fps-form-04be-b-composite-v1`、final candidate=`candidate-f4a7d…06fa`、state=`da53deaa…54aa`、artifact=`e301dd8c…e007`、readback object=`75b52b0f…0621`、composed GeometryProgram=`ca7db8da…2386`、receipt=`ef122106…a18f`、record canonical=`d64e4589…03d8`。Runtime 重启 GET 对这些 hash 全部一致；prepare 写入、GET 零写。当前合同面 **572 schemas / 125 read + 93 opt-in write = 218 tools**。六视图/FormArt/FormQualityV2 row 尚未为该 candidate 创建，Stage=`camera-calibrated`，`candidate_confirm_allowed=false`，secondary/version/export 未创建，视觉真值仍 `QUALITY_TARGET_NOT_MET / commercial=NOT_PROVEN`。

> 2026-08-28 `FPS-FORM-04BE-A` 权威增量：当前合同面 569 schemas；新增 `ProductionWeaponFormArtCompositeProposalPlan@1` 与 Runtime source composition seam，original 04AZ visual scope 和 current 04BB geometry base 不再混同。1–8 个 operation 必须有序、stable source-node/Part 互斥且使用注册 profile；focused 2/2 与 Worker compile 证明 rear-stock prior edit 保留、trigger aperture 追加、23 Part 不变。此 gate 没有 Store/CAS current-base proof、candidate、durable link/restart GET、RenderSet 或 Stage 写入；FormQualityV2 count=0、Stage=`camera-calibrated`、version/export=0，视觉真值仍 `QUALITY_TARGET_NOT_MET / commercial=NOT_PROVEN`。

> 2026-08-28 `FPS-FORM-04BA` 权威增量：04AZ-R2 同一 Runtime 已新增 1 条 BoundaryBridge AuthoringMesh child、1 个 reviewable derived candidate 与 1 条 proposal FormArt evidence；derived artifact=`18f92e53…d89c`、readback=`1e080c73…0b7`、six-view bundle=`866cb442…b5ab`。该 proposal 的结构 hard gate 可读不等于视觉晋级：proposal evidence ready=false、FormQualityV2 count=0、Stage head canonical 仍 `526fab54…cbc6`，version/export count 均为 0。视觉真值继续为 `QUALITY_TARGET_NOT_MET / commercial=NOT_PROVEN`。

> 2026-08-28 `FPS-FORM-04AZ` 权威增量：04AY-R2 数据库现为 1 Session / 1 Stage V3 head / 1 CameraLock / 1 registration lineage / 1 fresh FormArt baseline。RigV2 rear3q camera hash=`9d8e590e…3abb`、canonical=`efffde77…1d33`，reference rotation=0、stock-left/muzzle-right、world +Y upright、Runtime orbit=180 的 semantic proof PASS；`4fa162d6…d43` 仅是 orbit 前的 GeometryProgram-space registered camera。fresh baseline=`93a53f18…8860`、cohort=`36795624…e5d`、6 views/54 AOV、same-cohort Worker 与 restart readback PASS。此结构证据不改视觉真值：`QUALITY_TARGET_NOT_MET / commercial=NOT_PROVEN`，Stage=`camera-calibrated`，secondary/High/Low/UV/Bake/human/engine/confirm/version/export 均未推进。

> 2026-08-28 `FPS-FORM-04AY` 权威增量：Runtime source 现内嵌并验证 `ProductionWeaponD1ReviewedFormProfile@1`，其内容仅为用户批准的 reference annotations，不是候选质量证据。fresh durable DB 已实证 1 Session / 1 Stage V3 / 1 CameraLock / 1 FormEvidence / 1 FormArt / 1 legacy FormQuality；registration lineage、fresh FormArt baseline、FormQualityV2 均为 0。相机真值为：参考 rotation=0、stock-left/muzzle-right、upright、Runtime orbit=180 已执行，但 fresh rear-three-quarter hash=`4fa162d6…d43` 与批准期望 `9d8e590e…3abb` 不同，状态 `BLOCKED_SEPARATE_REFERENCE_REGISTRATION`。视觉仍 `QUALITY_TARGET_NOT_MET`，不得宣称商业级。

> 2026-08-27 `FPS-FORM-04AX` 权威真值：原 04AU 一次性对象图、IDs 与 hashes 未恢复；历史会话中的完整 24-node typed GeometryProgram 已被重建为 Runtime 编译内置的 closed first-party D1 seed。新持久 Runtime DB/CAS 的 reference-bound candidate=`candidate-1c14…23fd`，artifact=`47b7…ce2`，strict readback=`23 Parts / 1876 triangles / validator passed`，Runtime drop/reopen 后 Store/CAS/hash equality=`PASS`。这只证明新的 durable reference→geometry candidate closure，不证明旧视觉证据或商业质量；新 seed 的 Session/ReferenceCanvas/DesignSpec、CameraLock/registration/RigV2、六视图 RenderSet、FormEvidence、FormArt 与 fresh baseline 均 `NOT_CREATED/NOT_RUN`。几何执行为 test fallback 固定产品代码，packaged/live same-cohort=`NOT_RUN`。唯一 `in_progress` 仍为 `FGC-MCP010F`；secondary=`NOT_CREATED`、quality=`QUALITY_TARGET_NOT_MET`、commercial=`NOT_PROVEN`，未 confirm/version/export，High/Low/UV/Bake/FPS/engine/human 未推进。

> 2026-08-27 `FPS-FORM-04AW` 权威真值：owner-reviewed-void GET 当前是 Runtime-derived read-only/non-durable projection，其结果合同已与实现统一为 `restart_hash_verified=false`；此前 Schema 的 `true` 是会阻断真实 GET 的错误声明，不是质量证据。568-schema checker 与 Contracts/Runtime/MCP compile PASS。04AU 一次性 D1 Store/CAS 没有可恢复的 GeometryProgram、candidate/artifact、六视图 AOV、FormArt、baseline 或 lineage 完整闭包，因此 real-D1 projection 仍为 `NOT_RUN_SOURCE_DATABASE_UNAVAILABLE`。Stage=`camera-calibrated`、secondary=`NOT_CREATED`、quality=`QUALITY_TARGET_NOT_MET`、commercial=`NOT_PROVEN`，未 confirm/version/export，High/Low/UV/Bake/FPS/engine/human 均未推进。

> 2026-08-27 `FPS-FORM-04AV` 权威真值：当前源码面为 **568 schemas / 124 read + 92 opt-in write = 216 tools**。`ProductionWeaponOwnerReviewedVoidCalibrationProjection@1` 及默认只读 GET 已经实现；Runtime 只从 durable FormArt/fresh baseline/approved CameraLock lineage/RigV2/Part-ID/depth/reviewed target 派生 left/right/rear-three-quarter 的精确 `rear-stock` 身份校准，无 Store/CAS/Worker/Stage 副作用。calibration eligibility 与 strict owner-void 零侵入为两个独立事实。Contracts/Store/Runtime/MCP compile 与 568-schema checker PASS；因 04AU 一次性 D1 Store/CAS 不可用，real-D1 projection=`NOT_RUN_SOURCE_DATABASE_UNAVAILABLE`、binding=`NOT_PROVEN`、strict owner-void=`NOT_PROVEN`、new shape=`NO`。因此 Stage 仍 `camera-calibrated`，secondary=`NOT_CREATED`，quality=`QUALITY_TARGET_NOT_MET`，commercial=`NOT_PROVEN`，High/Low/UV/Bake/FPS/engine/human/confirm/version/export 均没有被本轮推进。

> 2026-08-27 reviewable candidate 权威增量：`FpsPresentationPackage@2` 的精确 weapon `AuthoringMesh@2` revision 已由 Runtime 编译为 durable `reviewable` structural candidate，并通过 2,916-triangle strict GLB readback、幂等重放及 Runtime 重启后 binding/hash 回读。weapon、arms、Socket、Rig 与 clips 仍绑定于同一 editable composite；候选仍为 `structural_only`。`secondary_form_approved=false`，Formal High 为 `BLOCKED_SECONDARY_FORM_APPROVAL`，Low/UV/Bake、FPS、engine、human、confirm/version/export 均未运行，故权威质量仍为 `QUALITY_TARGET_NOT_MET / commercial=NOT_PROVEN`。

> 2026-08-27 `FPS-FORM-04AU` 权威真值：用户批准的 rear-three-quarter `rotation=0 / stock-left-muzzle-right / upright / Runtime orbit=180 / camera=9d8e590e…3abb` 已在同一真实 D1 构建批次 `5e1e4754…8804` 重放。fresh baseline=`9c43c46e…c0d` 完成 6 views × 9 AOV 与重启回读；唯一 `AuthoringMeshRearStockVoidRailBow@1` prepare 生成 durable child `amrev-fc303c…4d5b`、1900-triangle/23-Part candidate `candidate-3c2a…4987`、strict geometry readback、proposal FormArt Store 回读及 Runtime 重启后同请求幂等回放。结构执行通过，但视觉门明确拒绝：CrossView=`REJECTED_ANY_METRIC_REGRESSION`，Part-ID 六视图 observed，negative-space/line-flow/strict owner-void 未全通过；批准相机下的 owner 证据为 `STRICT_REGISTERED_CAMERA_OWNER_VOID_NOT_ELIGIBLE / PART_BINDING_CALIBRATION_BLOCKED`。因此仍为 Stage=`camera-calibrated`、secondary=`NOT_CREATED`、quality=`QUALITY_TARGET_NOT_MET`、commercial=`NOT_PROVEN`，不运行 FormQualityV2，不 confirm/version/export，也不进入 High/Low/UV/Bake。下一原子先闭合 registered-camera 的 `rear-stock` owner→reviewed-void 绑定校准，再决定是否领取一个新的 bounded rail-shape。证据：`docs/evidence/mcp010f/production-weapon-rear-stock-void-rail-bow-real-d1-gate-04au-20260827.json`。

> 2026-08-27 `FPS-FORM-04AT`：已接受并固定后 3/4 `rotation=0 / stock-left-muzzle-right / upright / Runtime orbit=180 / camera=9d8e590e…3abb`。04AR/04AS 复盘证明失败来自 `rear-stock` 上梁的 owner intrusion 与错误 line-flow，而非 notch 宽高；因此停止 U-frame/size sweep，新增 product-owned `AuthoringMeshRearStockVoidRailBow@1` 内核。Runtime 自动解析 void-facing source-local axis 和两条 depth-symmetric longitudinal edges，在 `s=0/.25/.5/.75/1` 应用 `d=0/.030/.045/.030/0m`，调用方不能传 vertex IDs/raw mesh/script；outer endpoints、lower beam、rear cap、PartOutput 与 source lineage 保持不变。Contracts/Store/Runtime compile 与 `git diff --check` PASS，source cohort=`575070e6…f768`。当前只到 source kernel：proposal/MCP/durable real-D1/fresh baseline/six-view/FormQualityV2 均未运行，Stage=`camera-calibrated`、secondary=`NOT_CREATED`、quality=`QUALITY_TARGET_NOT_MET`，无 confirm/version/export。证据：`docs/evidence/mcp010f/production-weapon-rear-stock-void-rail-bow-kernel-source-gate-04at-20260827.json`。

> 2026-08-27 `FPS-FORM-04AS`：`FormQualityV2` 新鲜基线适配器已在 Contracts、MCP、Runtime 与 Store 收口。它明确分离 source scope（当前 Stage head、CameraLock、same-cohort fresh baseline、registration lineage、RigV2）与 evaluation scope（distinct proposal candidate、proposal CrossView、proposal-side Part-ID/negative-space/line-flow）；所有调用字段均由 Runtime 从 durable evidence 重派生并由 Store 独立回读验证，legacy 模式不得夹带 proposal scope。538-schema checker、Contracts/Store/Runtime/MCP compile、四组件 same-cohort build identity 均 PASS，source cohort=`acf10c3b…173`。这只是 source/compile gate：当前真实 D1 `candidate-9127…fdc8b` 仍为 `REJECTED_REGRESSION`，未用新 adapter 重跑，Stage=`camera-calibrated`、secondary=`NOT_CREATED`、quality=`QUALITY_TARGET_NOT_MET`，无 confirm/version/export。下一原子是基于批准相机设计新的 bounded `rear-stock` art-shape，只有 proposal evidence=`READY` 且 fresh FormQualityV2 真实运行通过后才允许推进 Stage。证据：`docs/evidence/mcp010f/production-weapon-form-quality-v2-fresh-baseline-adapter-source-gate-04as-20260827.json`。

> 2026-08-27 `FPS-FORM-04AR` 权威真值：真实 D1 已持久化用户批准的 `rotation=0 / stock-left-muzzle-right / upright / orbit=180 / camera=9d8e590e…3abb` registration lineage，并在 real-D1 execution cohort `55c50eb6…876e1f` 生成 restart-verified fresh 6-view/9-AOV baseline。唯一 `rear-stock 620×680` OpenFrameNotch 产生 durable child、派生 candidate/artifact 与 Store-indexed proposal FormArt，但六视图为 `NOT_IMPROVED / REJECTED_REGRESSION`；Part-ID 全 observed，negative-space/line-flow/strict owner-void 不全通过。复核后的 closed-schema sync 与四组件 build identity 属于新 source cohort `07c53f8c…797ca`，未重跑真实 D1，不改写 `55c50eb6…876e1f` 历史回执。`FormQualityV2` fresh-baseline adapter 仍为 `BLOCKED_REQUIRED`，因此权威状态仍是 Stage=`camera-calibrated`、secondary=`NOT_CREATED`、quality=`QUALITY_TARGET_NOT_MET`、commercial=`NOT_PROVEN`，无 confirm/version/export。

> 2026-08-27 `FPS-FORM-04AP/04AQ` 权威增量：FormArt batch 正常失败/重放立即保守收口，same-key 六视图在 Runtime 内 single-flight；typed approval context 绑定 `0° / 枪托左枪口右 / 正立 +Y / orbit 180° / camera 9d8e590e…3abb` 及完整源 scope，过期或 proof/hash 不一致 fail closed。commit/replay/get 已统一重验 current cohort、candidate/artifact/base version、promotable lineage/receipt、RigV2 六视图与 durable ReferenceCanvas。source cohort=`e58bc668…031873`，538 schemas / 206 tools，source compile/Stage0 PASS；真实 D1 仍零写，Stage=`camera-calibrated`、secondary=`NOT_CREATED`、quality=`QUALITY_TARGET_NOT_MET`、commercial=`NOT_PROVEN`。

> 2026-08-27 `FPS-FORM-04AO` 当前权威增量：fresh FormArt 的 camera/9 AOV/RenderSet/mask/comparison/quality/view/parent 全派生 CAS 已统一进入 Store-owned durable batch，hash 在 CAS put 前被 owner 预声明；启动恢复只隔离本 batch 中过期、无 live owner、无 durable link 的 temporary 元数据，不删除 CAS bytes。baseline replay/get 现强制重新验证当前 cohort、promotable registration lineage/receipt、RigV2 固定六视图和 exact durable ReferenceCanvas，验证成功后 get 才可声明 restart hash verified。当前 source cohort=`dbdccfaa1e9fec54cc7e0d10ce30dc70d9c45265f600a30cd3de3cb571bc49e3`，四组件构建 PASS；公共面仍 **538 schemas / 118 read + 88 opt-in write = 206 tools**。真实 D1 零写，Stage=`camera-calibrated`、secondary=`NOT_CREATED`、quality=`QUALITY_TARGET_NOT_MET`、commercial=`NOT_PROVEN`。证据：`docs/evidence/mcp010f/production-weapon-form-art-baseline-durable-cas-replay-gate-04ao-20260827.json`。

> 2026-08-27 `FPS-FORM-04AL` 当前增量：Runtime-owned durable fresh six-view baseline producer 已接通合同、Store、Runtime 与 MCP `prepare/get`；每个视图绑定 approved registration lineage / RigV2、fresh same-cohort 512×512 九 AOV、camera/mask/compare/quality 与完整 CAS reachability，并以单事务持久化。精确状态为 `PASS_SOURCE_COMPILE_DURABLE_PRODUCER_NOT_RUN_REAL_D1`；真实 D1、orientation approval、fresh baseline、notch、secondary、Stage/confirm/version/export 均未执行。当前公共面 **538 schemas / 118 read + 88 opt-in write = 206 tools**，视觉仍 `QUALITY_TARGET_NOT_MET`。

> 2026-08-27 `FPS-FORM-04AK` current truth：新增只读 fresh-baseline preflight，验证 approved lineage、RigV2、candidate/artifact scope 与固定六视图，且拒绝调用方注入 camera/hash/render/path。producer 尚不存在，权威状态为 `FRESH_BASELINE_MATERIALIZER_UNAVAILABLE`，真实 D1 零写、旧 FormArt 不复用。proposal 原子性已收紧为“内存 child → Worker/candidate/FormArt/secondary gate → durable child”，CAS 临时 receipt 可失败清理；最终 child commit 失败后的下游补偿尚未完成。当前 **533 schemas / 117 read + 87 write = 204 tools**，Stage=`camera-calibrated`、secondary=`NOT_CREATED`、quality=`QUALITY_TARGET_NOT_MET`。

> 2026-08-27 `FPS-FORM-04AJ` current truth：620/680 open-frame notch 只有 deterministic zero-write proposal identity，prepare eligibility=false。Runtime 允许验证历史 FormArt/RenderSet 的 immutable bytes、canonical、camera/reference/candidate binding，但 active comparison 必须使用当前 Runtime/Worker cohort 的 fresh baseline；跨 cohort 在 durable child/candidate 写入前 fail closed。CameraLock lineage 的 Store-error cleanup 已补齐 ApprovalReceipt CAS；同时确认 notch request 尚无直接 registration-lineage 字段，因此 fresh FormArt 必须先证明实际消费 approved camera lineage，未证明前不得 prepare。当前仍为 Stage=`camera-calibrated`、secondary=`NOT_CREATED`、quality=`QUALITY_TARGET_NOT_MET`，无 confirm/version/export。

> 2026-08-26 `FPS-FORM-04AI` current truth：默认只读 projection successor 已取代“调用方提交 camera orbit”的半成品预检。真实 D1 isolated Runtime 将 `rotation=0 / stock-left-muzzle-right` 固定推导为 `orbit=180`，camera=`9d8e590e…5b3abb`，upright proof=`0ad9c90c…6ae8f`，且 CAS `1509→1509`、durable lineage 仍为 `0`。因此 camera projection=`READY`，authority=`BLOCKED_USER_APPROVAL_REQUIRED`，Stage/secondary/quality 不变。该轮为 531/116+87；当前公共面为 **533 schemas / 117 read + 87 write = 204 tools**。

> 2026-08-26 `FPS-FORM-04AH` current source truth：rear-three-quarter 概念板与 ReferenceViewSpec 均为正立 `rotation=0° / stock-left-muzzle-right`；当前 registered render 为正立但 `muzzle-left-stock-right`，所以 diagnostic `rotate-180` 不是可批准方向。CameraLock child prepare 已新增独立 semantic screen order 与 canonical `0/180` camera-orbit choice；Runtime 生成最终相机并投影 stock/muzzle source anchors，以 world `+Y` screen-up proof fail closed，并强制 promotable lineage 的 orientation provenance 精确绑定 Runtime 写入 CAS 的 `ApprovalReceipt@1`。合同仍 529、工具仍 115 read +87 opt-in write=202，Contracts/Store/Runtime/MCP compile PASS；orientation-specific user receipt、真实 D1 lineage prepare/restart、`620/680` notch 仍 `NOT_RUN`，Stage=`camera-calibrated`、secondary=`NOT_CREATED`、quality=`QUALITY_TARGET_NOT_MET`。

> 2026-08-26 最新权威事实：合同面是 **529 schemas / 115 read + 87 opt-in write = 202 tools**。`AuthoringMeshOpenFrameNotch@1` 已通过真实 D1 Runtime 执行，durable child/CAS/SQLite/restart、单 `rear-stock` lowering、strict GLB readback 与 fresh proposal FormArt 均存在。该事实只是 `PASS_STRUCTURAL_EXECUTION`；`700/700` 候选明确 `REJECTED_VISUAL_REGRESSION`，总质量仍 `QUALITY_TARGET_NOT_MET`，Stage/secondary/confirm/version/export 未改变。

> 2026-08-26 最新权威真值：**528 schemas / 28 operator entries / 115 read + 87 opt-in write = 202 tools**。真实 D1 `rear-stock` 已完成 stable-ID `MoveVertices` child revision、Runtime-only 单 source-node lowering、1876-triangle strict GLB readback、immutable six-view replay，以及 proposal FormArt CAS/Store/replay/restart readback。Z taper 方案总分从 `3.095222022337167` 到 `3.097260047417`，旧 strict-Pareto 仍为 regression，新 Pareto 仅称 `reviewable_tradeoff`。内容 Gate 因 owner/open-void、negative-space、line-flow 未全通过而 `BLOCKED_PROPOSAL_FORM_ART_EVIDENCE`；Stage=`camera-calibrated`、secondary=`NOT_CREATED`、quality=`QUALITY_TARGET_NOT_MET`、commercial=`NOT_PROVEN`，无 confirm/version/export。

> 04AG 状态语义：`ProductionWeaponFormArtProposalEvidence@1` 已注册并完成真实 D1 durable CAS + SQLite Store/replay/restart readback。receipt=`1f591bb7…fde3`、canonical=`26ab9fcd…bd87`、identity=`3b0da665…ae58` 在同输入与重启后保持稳定。该 transport/durability slice 为 PASS，但证据内容明确是 `BLOCKED_PROPOSAL_FORM_ART_EVIDENCE`：Part-ID 六视图 observed，negative-space/line-flow 未全部 resolved，left/right/rear-three-quarter 的 `rear-stock` owner/open-void 严格门为 false。`REVIEWABLE_TRADEOFF` 不能转换为 PASS；`secondary=NOT_CREATED`、`quality=QUALITY_TARGET_NOT_MET`、Stage=`camera-calibrated`，无 confirm/version/export。

> 2026-08-26 `FPS-AUTHORING-MESH-V2-02` 最新真值：当前 source 为 **527 schemas / 28 operator entries / 114 read + 86 opt-in write = 200 tools**。真实 D1 `rear-stock` 已完成 candidate-owned primitive→typed `AuthoringMeshRevision@2` genesis、CAS/SQLite durable write 与 Runtime restart exact readback；revision 内含可验证的 candidate/Part/source/material/operator/program/artifact/readback/transform 绑定，8 vertices / 6 quads。该回执只证明真实源网格进入 ForgeCAD authoring authority，不证明任何美术编辑、High、视觉或商业质量。Stage=`camera-calibrated`、secondary=`NOT_CREATED`、quality=`QUALITY_TARGET_NOT_MET`、engine/human=`NOT_RUN`、commercial=`NOT_PROVEN`，且未 confirm/version/export。

> 2026-08-26 `FPS-FORM-04AF` 最新真值：当前 source 为 **527 schemas / 28 operator entries / 114 read + 85 opt-in write = 199 tools**。真实 D1 的单 `rear-stock` 型面重建已走完六视图评估，但 left/right/rear-three-quarter 回退，故 `rejected-regression / confirm_allowed=false`；source candidate 不被改写。`AuthoringMesh@2` 持久化 genesis→split-edge→Runtime restart exact readback=`PASS_STRUCTURAL_ONLY`，仍未绑定真实 D1/High。CameraLock lineage preflight 将诊断性 180° 与用户诚意批准分离，当前 `ready=false / ORIENTATION_SPECIFIC_USER_RECEIPT_REQUIRED`。因此 Stage=`camera-calibrated`、secondary=`NOT_CREATED`、quality=`QUALITY_TARGET_NOT_MET`、commercial=`NOT_PROVEN`。

> 2026-08-26 `FPS-FORM-04AE` 当前权威口径：**525 schemas / 28 operator entries / 112 read + 84 opt-in write = 196 MCP tools**。CameraLock additive child 的 source vertical slice已可编译；本轮又增加 AuthoringMesh V2 稳定半边 ID/不可变 revision DAG/局部 split-edge、Native High evaluator、严格 High↔Low↔Cage 与 bake fail-closed validator、以及闭合 `MaterialLayerGraph@1` plan seam。它们当前均只属于 `PASS_SOURCE_COMPILE_ONLY`：Manifold 仅 feature-gated compile/link，OpenSubdiv 仍未采用，正式 bake producer、纹理求值、Runtime/MCP durable 总接线与视觉执行尚未完成。orientation-specific user approval 仍未提供，不得写入诊断性 180°；Stage=`camera-calibrated`、secondary=`NOT_CREATED`、quality=`QUALITY_TARGET_NOT_MET`。证据：`commercial-weapon-native-authoring-high-bake-material-source-compile-gate-20260826.json`。下方较小计数均为历史 cohort，不覆盖本条。

> 2026-08-26 `FPS-FORM-04AD` 权威增量：当前合同面为 **518 schemas / 111 read + 83 opt-in write = 194 tools**。新增 `ProductionWeaponSemanticLandmarkOrdering@1` 只表达 Runtime-derived 的 3D source/subject-axis 顺序，明确 `target_landmark_arrays_present=false / metrics=NOT_PRESENT`，不得冒充 2D landmark；`ProductionWeaponAuthoredViewOrientation@1` 将诊断变换与用户方向回执分开；`RegisteredCameraRigCalibration@2` 只有绑定 promotable authored rear3q receipt 才能物化。定向 Contracts/Runtime/MCP compile 与 518-schema checker PASS。真实 D1 尚无 orientation-specific user receipt，因此保持 `BLOCKED_AUTHORED_REAR_THREE_QUARTER_ORIENTATION`、Stage=`camera-calibrated`、secondary=`NOT_CREATED`、quality=`QUALITY_TARGET_NOT_MET`，不 confirm/version/export。旧 `@1` 保持历史真值；durable 落点采用 CameraLock 的 additive child lineage，不复制/自动升级整张旧记录。

> 2026-08-26 `FPS-FORM-04AC` 权威增量：已编译通过一个不新增 MCP tool 的 RuntimeParameterPatch strategy：`rear-stock-profile-reconstruction-v1`。它只物化 product-owned 三站位闭合型面，并校验 `changed_nodes == {rear-stock}`、PartOutputs exact unchanged、source primitive→`profile-loft@2`；caller-supplied GeometryProgram/外部 RepairIntent 不得使用该 strategy。这只是 source 能力；旧 `CameraLock@1` 与诊断性 rear3q 180° 不得构成 acceptance parent。无 fresh candidate/render/FormArt/human receipt，故资产真值仍为 baseline active、`QUALITY_TARGET_NOT_MET`。

> 2026-08-26 `FPS-FORM-04AB` 真实 D1 更正完成：Runtime 从 candidate-owned GeometryProgram 派生 subject→registered camera registration，ReferenceCanvas、CameraLock 校验、FormEvidence、FormArt、raster attribution 与 CrossView 统一消费可重放的 registered cameras。fresh durable run 1/1 PASS（30 candidates、1 CameraLock、1 FormEvidence、1 FormArt、6 RenderSets/54 AOV、restart hash PASS、0 confirm/version/export）。`left.open-stock-void` 的唯一最高影响源已从错误的 `muzzle-shroud` 回到 `rear-stock`（548 px；次高 `receiver-main` 114 px；muzzle-shroud 0 px），`repair_target_status=UNIQUE_HIGHEST_IMPACT_SOURCE_OBSERVED`。下一原子只允许一个 `rear-stock` source-node RepairIntent；Stage 仍 `camera-calibrated`，secondary=`NOT_CREATED`，quality=`QUALITY_TARGET_NOT_MET`。正式商业晋级前必须将 source ordering、authored orientation 与 registered rig 作为现有 CameraLock 的 durable child lineage 持久绑定；旧 `@1` 不改写、不自动升级。证据：`docs/evidence/mcp010f/production-weapon-real-d1-semantic-camera-relock-source-attribution-pass-20260826.json`。

> 2026-08-26 商业真值补充：`AuthoringMeshRevision`、`HighEvaluatedArtifact`、`LowAuthoringMesh`、`CageArtifact` 必须是独立 CAS/lineage 对象；GLB、自动 retopo、LOD 与压缩包均为派生物。最终批准同时要求 `HeroSourceAsset@1`、`FpsPresentationPackage@1`、`EngineDeliveryPackage@1`、`EngineValidationReceipt@1` 与 `HeroArtReviewReceipt@1` 绑定同一 candidate/export hash。当前没有这些完整对象，故商业状态保持 `NOT_PROVEN`。

本轮 FormArt 哈希绑定 raster attribution 证据：`docs/evidence/mcp010f/production-weapon-formart-hash-bound-raster-attribution-source-gate-20260826.json`；前序 Formal High transport/cleanup 证据继续保留为 `docs/evidence/mcp010f/production-weapon-raster-attribution-formal-high-transport-source-gate-20260826.json`。

2026-08-26 Formal High transport/cleanup 补充：隔离 raw stdio 已验证默认只暴露 `get`、preflight 可读、disabled-write typed rejection、显式 opt-in 后 `get/prepare` 可见，以及无合法 Stage source 时 `PRODUCTION_WEAPON_FORMAL_HIGH_SOURCE_TRANSITION_MISSING` 的 fail-closed 行为；reservation 失败的临时 SQLite metadata 与 CAS 文件 cleanup 聚焦测试 **1/1 PASS**。合法 positive prepare→replay→drop/reopen get 仍 `NOT_RUN`，因为现有真实 producer helper 无法生成通过 FormQuality@2 的 `secondary-form-approved` head；禁止 raw SQL 或伪 Stage。

2026-08-26 最新只读诊断增量：既有 `production_weapon_form_art_evidence_get` 可选择性执行闭合 `render_glb_raster_attribution`，不新增工具。Runtime 从 exact durable FormArt record 派生 candidate/artifact/readback、ReferenceCanvas/DesignSpec/SilhouetteTarget、CameraLock/CameraRig、FormEvidence view 与 RenderSet，不接受 caller camera 或 mask；输出同时绑定 triangle-id/source-table、target/projected/reviewed/expected-void mask、semantic Part/source node/lineage/material zone、mesh/primitive 与 Worker cohort hash。04AB 真实 D1 已以 registered-camera exact replay 返回唯一 `rear-stock/548px` source，能力状态为 `PASS_REAL_D1_UNIQUE_REAR_STOCK_SOURCE / ZERO_WRITE`；不 confirm/version/export，也不提高质量 Stage。

2026-08-26 早于 04AE 的 Formal High 历史切片：当时 manifest 为 **515 schemas**；Formal High 的 `get/prepare` 已接入 MCP，当时为 **111 read + 83 opt-in write = 194 tools**。Store 已持久化 public `idempotency_key` 并以 project/session/key 唯一约束实现 replay/conflict，MCP/Runtime/Store source compile 和聚焦测试通过。完整合法 secondary head 下的 positive materialize→restart、post-reservation cleanup、raw current-cohort transport、视觉/人评/引擎/package 仍 `NOT_RUN/NOT_PROVEN`。真实 D1 没有 secondary head、Formal High row、Stage/confirm/version/export 写入，visual 仍 `QUALITY_TARGET_NOT_MET`。

2026-08-26 Formal High scope 边界：internal input 不再携带 caller-supplied session/project；Runtime 从精确 Stage transition 派生 scope，并继续派生 candidate state、readback 与 receipt。公共 surface/full Runtime positive 状态不变。

2026-08-26 Formal High Store 最新真值：validated-row 原子事务已完成 commit、exact replay、conflict rollback、disk reopen 与 payload tamper detection，聚焦 fixture 1/1 PASS；该证据位于完整 source-lineage/CAS-content 验证之后的持久层，不能替代尚未运行的 full Runtime producer/restart。真实 D1 与质量状态不变。

2026-08-26 Formal High 最新 source 真值：internal materializer 使用 High-only 无环输入并由 Runtime 派生 candidate state、formal readback 与 receipt；public MCP surface 已暴露并保持 explicit write opt-in。完整 positive restart/cleanup 仍 `NOT_RUN/NOT_PROVEN`，所以真实 D1、Stage、visual 与 commercial 状态不变。

2026-08-26 最新 source 增量：Form 三边证据策略和 legacy current-head mapping 已修正；Formal High 的 distinct candidate factory、Store scoped-idempotent transaction、Runtime materializer 与 MCP public seam 已存在。尚未通过的是完整正向 restart/cleanup fixture，所以 capability仍 `NOT_PROVEN`。真实 D1 未写入、未晋级；Stage=`camera-calibrated`、secondary=`NOT_CREATED`、visual=`QUALITY_TARGET_NOT_MET`、commercial=`NOT_PROVEN`。

2026-08-26 Cage/Bake public seam 增量：Store 七子表、固定 Cage/Bake Worker、Formal High factory/Store/MCP/Runtime seam 已完成 source/focused。尚未通过的是完整 source-lineage/CAS positive materialize→drop/reopen/cleanup；当前 D1 的 Form 前置未通过且无 formal positive receipt，仍 fail closed、零写。formal High capability 和 Cage/Bake positive receipt 均 `NOT_RUN/NOT_PROVEN`，不提升任何质量或交付结论。


> 2026-08-26 最新权威 source 口径（取代下方 2026-08-25 的“最新/当前”计数）：**518 schemas / 28 operator entries / 111 read + 83 opt-in write = 194 MCP tools**。Low quad draft 的 current provenance 为 candidate-bound，仍为 `DRAFT_UNREVIEWED / structural_only / promotion_eligible=false`；其 prepare→同键重放→Runtime drop/reopen→get 在隔离 current-cohort fixture **1/1 PASS**。Hero UV 已有 **7 个 registered contracts**，并已接入 Store/Runtime/MCP public `hero_uv_durable_get/prepare`；其 prepare→同键重放→Runtime drop/reopen→get **1/1 PASS**，四个 Hero CAS roots 已纳入 linked/GC 判定。该 slice 仅为 `structural_only`，不是 artist unwrap、visual、human、engine、commercial 或 packaged PASS；proposal=`registered=false`。当前不推进 Stage、confirm、version 或 export；Stage=`camera-calibrated`、`secondary-form-approved=NOT_CREATED`、`FPS-HIGH-05=NOT_PASSED`、visual=`QUALITY_TARGET_NOT_MET`、HQ360=`BLOCKED_REFERENCE_COVERAGE`。证据：`docs/evidence/mcp010f/commercial-weapon-hero-uv-durable-restart-source-gate-20260826.json`。

> 2026-08-25 历史快照（已由上方 2026-08-26 权威口径取代）—权威补充：公共面为 **499 schemas / 28 operator entries / 107 read + 79 opt-in write = 186 tools**。Native High 当前源码以新 cohort 完成 Worker **6/6 + 7/7** 与 Runtime durable restart **1/1 PASS**；Low quad draft 与 Hero UV 仅为 Worker source producer，Art Director 矩阵仅为 Viewer read model。它们都不推进 Stage 或商业质量；proposal=`registered=false`、`FPS-HIGH-05=NOT_PASSED`、visual=`QUALITY_TARGET_NOT_MET`。证据：`docs/evidence/mcp010f/commercial-weapon-authoring-slices-source-gate-20260825.json`。

2026-08-25 Native High durable 历史权威补充（当前口径见顶部）：当前公共合同集合为 **499 schemas**；Native High 的 7 个 durable/GLB closed contracts与 Low quad draft 的 2 个 Worker contracts均已进入 manifest。Stable-ID/DetailGraph、embedded GLB strict readback、Runtime durable prepare/get、CAS/Store/restart 与公共 MCP source-focused positive chain已通过；新增 support-loop chamfer arc 也已在当前 cohort 重跑 restart 1/1。当前唯一可写者仍是 Runtime；Low quad draft 与 Hero UV 尚未接入 Runtime durable producer，source slices 不推进 ProductionStage、candidate confirm、version 或 export。`FPS-HIGH-05=NOT_PASSED`、visual=`QUALITY_TARGET_NOT_MET`、human/engine/distribution=`NOT_RUN`、Stage=`camera-calibrated`、HQ360=`BLOCKED_REFERENCE_COVERAGE`。

2026-08-25 `CQ-02-TYPED-TOPOLOGY-IDENTITY-LINEAGE`：`authoring_mesh_edit_preview → authoring_mesh_edit_prepare` 的 `split_edge / collapse_edge / dissolve_edge` proof 仍保持 source-element-only；下游 Runtime 现在只从 Store 的 exact candidate→idempotency response 恢复该 proof，并把 parent source identity 物化为 durable `AuthoringMeshIdentityLineage@1` child IDs、单调 tombstone 及 one-to-many/many-to-one relation，不接受 caller identity/proof arrays。真实 split/collapse/dissolve 已分别完成各自独立的完整持久化与 Runtime drop/reopen/get 重启链路，合计 **3/3 PASS**；Store `authoring_mesh_` **12/12**、MCP IdentityLineage **3/3**、490-schema checker与 Contracts/Store/Runtime/MCP 联合 compile PASS，工具数仍 **106 read + 78 write = 184**。general correspondence、evaluated retarget、完整 selection/undo history 与产品级 cross-version editor仍 `NOT_PROVEN`。Stage 保持 `camera-calibrated`，视觉=`QUALITY_TARGET_NOT_MET`，human/engine/distribution=`NOT_RUN`，HQ360=`BLOCKED_REFERENCE_COVERAGE`。新回执：`docs/evidence/mcp010f/authoring-mesh-typed-topology-identity-lineage-materialization-source-gate-20260825.json`；原 source-proof 回执继续作为上游证据。

2026-08-25 Native High 权威口径：Worker/GLB/Runtime CAS/Store/restart 与公共 MCP source-focused receipt 通过；没有 packaged/candidate-bound quality 或 Stage evidence，故 proposal 继续 `registered=false`，`FPS-HIGH-05=NOT_PASSED`。Low/Modifier 与 Stage=`camera-calibrated`、visual=`QUALITY_TARGET_NOT_MET`、human/engine/release/distribution=`NOT_RUN`、HQ360=`BLOCKED_REFERENCE_COVERAGE` 均不变。

2026-08-25 AuthoringMesh 历史 truth（当前口径见顶部）：当前公共合同集合为 **499 schemas**，28 operator entries、107 read + 79 write = 186 tools；`split/collapse/dissolve` 三条独立 full-chain 均完成 durable IdentityLineage 与 restart 回读，**3/3 PASS**；general correspondence、evaluated retarget、完整编辑历史和产品级 cross-version editor仍 `NOT_PROVEN`。`CQ-02`保持 queued/partial，`FGC-MCP010F`仍唯一 `in_progress`，Stage仍 `camera-calibrated`。此前 AuthoringMesh/Stage0 receipt 的 490-schema 数值保留为历史快照。

2026-08-25 `FPS-FORM-04Y/04Z` supplemental diagnostic truth：04Y hash-only preflight 的已捕获输出证明 left/right/rear-three-quarter 当前 rotation=`0/0/180°` 均为唯一 identity registration；但 owner intrusion=`302/492/587`、adjacency milli=`319/343/248`，严格 owner gate 仍为 false。04Z 对 upper-profile station 0/1 各做 `+0.020 m` 隔离后，intrusion=`302/481/587` 与 `302/492/587`；station 0 对 rear3q 仅改变 1 pixel，station 1 改变 143 pixels 但全部位于 reviewed region 外，二者 rear3q adjacency 均为 `248<250`，故两个方向都 `STOP_04Z_STATION_DIRECTION`。聚合 ignored fixture 在输出两段诊断后因本轮目标切换被中止（exit 130），所以本条不是完整 fixture PASS；只记录已捕获的 supplemental diagnostic。`QUALITY_TARGET_NOT_MET`、Stage=`camera-calibrated`、`secondary-form-approved=NOT_CREATED`、六视图/depth/confirm/version/export 状态不变。证据：`docs/evidence/mcp010f/production-weapon-real-d1-hash-only-registration-preflight-20260825.json`、`docs/evidence/mcp010f/production-weapon-real-d1-private-stock-upper-profile-station-isolation-20260825.json`。

2026-08-25 commercial-quality truth：当前同候选 2K 几何/材质演示只支持 `PARTIAL_SOURCE_PASS`，不支持 Hero Asset。High/Low/Cage 为 2,280/1,000/1,000 triangles；Low 非 artist-authored quad topology；Bake coverage=`0.3625035285949707`、ray miss=`45,386`、nearest fallback=`107,063`、cross-part hit=`3,982`、padding=`0`；Hero material 为 4 zones/6 embedded textures；Three.js consumer readback PASS，commercial engine/human/distribution=`NOT_RUN`。权威质量状态保持 `QUALITY_TARGET_NOT_MET`，Stage head 仍 `camera-calibrated`，`secondary-form-approved` 未创建。目标产品合同见 `COMMERCIAL_GAME_WEAPON_QUALITY_PLAN.md`；目标合同、算法评估和文档同步都不是 Runtime 真值。

## 当前商业级边界（2026-08-26）

ForgeCAD 当前仍是可验证高级灰模/技术管线，不是商业级资产生产软件。权威根因是上游资产真值与艺术生产链尚未闭合：AuthoringMesh 的专业 correspondence/edge-flow、可审的 Native High/Low、Hero UV、独立 Cage/Bake、Material Layer Graph、LOD/FPS 交付，以及 Art Director Viewer、EngineValidation、HeroArtReview 的独立门仍未全部形成同一 candidate/hash 的可回读证据。研究计划或源码结构不能覆盖这些缺口。

04AB 已把开放枪托误归因修正为 exact `rear-stock` source；但现有 Profile 外推呈现“owner-void overlap 改善、全局 silhouette 退化”的冲突，故任何单指标 Profile 候选均不得晋级。04AC 的权威接受条件是同一 source-node 的 owner-void 改善与六视图外轮廓/landmark 非回退同时成立；rear-three-quarter authored orientation 在 V2 durable parent 落地前保持独立 blocker。

商业生产能力只能由 ForgeCAD 内建的 typed Worker 承担：AuthoringMesh 固化拓扑与 Part identity；Native High/Low 固化高低模及 correspondence；Hero UV 固化 seam/density/tangent；Cage/Bake 固化逐射线诊断；Material Layer Graph 固化可追溯的 layer/mask/PBR；LOD 固化 LOD0/1/2、collision/socket；Art Director Viewer 只读呈现阶段/AOV/compare；EngineValidation 和 HeroArtReview 分别提供真实引擎与独立艺术家证据。所有模块都由 Runtime `prepare → readback/evaluate → approval → confirm` 管理，不能拥有第二真值、任意脚本或 DCC 运行依赖。

| 机器真值 | 当前状态 |
| --- | --- |
| 唯一任务 | `FGC-MCP010F` 是唯一 `in_progress` |
| Stage / secondary | `camera-calibrated` / `secondary-form-approved=NOT_CREATED` |
| High / Low / proposal | `FPS-HIGH-05=NOT_PASSED`；Low=`DRAFT_UNREVIEWED / structural_only / promotion_eligible=false`；proposal=`registered=false` |
| 质量与发布 | `QUALITY_TARGET_NOT_MET`；human/engine/distribution=`NOT_RUN`；HQ360=`BLOCKED_REFERENCE_COVERAGE` |
| 版本状态 | 无 confirm、version 或 export |

商业 11 组验收与 Runtime 19 状态只允许映射，不形成第二状态机；实际 Stage write 仍按 `ProductionStage@3` 的 1→19 顺序，并要求 `hero-art-review-approved → engine-validated → export-confirmed`。逐组当前真值如下：

| 商业质量组 | 当前权威状态 |
| --- | --- |
| 1 Art Direction / ReferenceViewSet | `camera-calibrated`；CrossView=`QUALITY_TARGET_NOT_MET`；五核心视图不完整，HQ360=`BLOCKED_REFERENCE_COVERAGE` |
| 2 AuthoringMesh | split/collapse/dissolve durable/restart **3/3 structural PASS**；general correspondence、evaluated retarget、完整 editor=`NOT_PROVEN` |
| 3 Native High | source/durable GLB/readback structural PASS；two-candidate resolver=`PASS_COMPILE_FOCUSED`；Runtime-owned derived High + formal `ProductionWeaponHighArtifact@1` positive receipt=`NOT_RUN`；`FPS-HIGH-05=NOT_PASSED` |
| 4 Low / Retopo | candidate-bound durable `DRAFT_UNREVIEWED / structural_only / promotion_eligible=false`；artist edge-flow/package=`NOT_RUN` |
| 5 Hero UV | public durable replay/restart **1/1 PASS**、4 CAS roots linked/GC；artist unwrap/package/engine=`NOT_RUN/NOT_PROVEN` |
| 6 Cage / Bake | fixed Cage/8-map/dilation/七记录 Store/MCP seam=`PASS_SOURCE`；真实 D1=`ZERO_WRITE`，formal positive receipt=`NOT_RUN`、quality=`NOT_PASSED` |
| 7 Material / Surface | 4 zones / 6 fixed-formula textures；`MaterialLayerGraph@1` commercial PBR=`NOT_PROVEN` |
| 8 LOD / Collision / Socket | 分散 structural slices；authored commercial LOD/collision/socket/performance=`NOT_RUN` |
| 9 Viewer / FPS / animation / VFX / audio | Viewer source/read-model smoke；正式 animation/VFX/audio/VoiceOver/human viewing=`NOT_RUN` |
| 10 Engine | Unreal/Unity commercial round-trip=`NOT_RUN`；Three.js/Godot evidence 不能替代 |
| 11 Independent Hero Art Review | `human=NOT_RUN`；无 `PASS_HUMAN_ART_REVIEW` |

上述结构性 Hero UV durable 事实仍只有 7 contracts、public `hero_uv_durable_get/prepare` 与 Runtime drop/reopen/get **1/1 PASS**，四个 CAS roots linked/GC；它不等于 artist unwrap、visual、human、engine、commercial 或 packaged PASS。

2026-08-25 `FPS-FORM-04X` real-D1 truth：private coupled stock-upper-profile boundary-translation screen **1/1 PASS（780.85s）**。`+0.020 m` intrusion=`302/472/587`、adjacency milli=`319/317/248`；`+0.040 m` intrusion=`296/416/587`、adjacency=`319/307/248`。两档三视图 selected transform 均为 `identity`，但均保留 nonzero intrusion 且 rear3q adjacency=`248<250`，formal owner gate=false，geometry direction=`STOPPED`。证据：`docs/evidence/mcp010f/production-weapon-real-d1-private-stock-upper-profile-boundary-translation-screen-20260825.json`（fixture log sha256=`19f4935b4a1078cb435773740455ac997bc92679494986a2c219b529e21082fb`）。该 PASS 仅表示 screen execution；quality=`NOT_PROVEN`、secondary=`BLOCKED`，六视图/depth/sink/stage/confirm/version/export 未推进。

2026-08-25 `FPS-FORM-04W` real-D1 truth：private lip continuation fixture **1/1 PASS（753.14s）**。`+0.025 m` intrusion=`246/300/573`、adjacency milli=`279/251/244`；`+0.045 m` intrusion=`186/268/550`、adjacency=`168/246/220`。虽然 intrusion 继续下降，但 adjacency 明显恶化且 rear3q 仍 `<250`，formal owner gate 均失败。按预设门 `stop_positive_extrapolation=true`，禁止继续 `+0.065/+0.085 m`。证据：`docs/evidence/mcp010f/production-weapon-real-d1-private-stock-upper-profile-lip-continuation-screen-20260825.json`。quality=`NOT_PROVEN`，无六视图/sink/stage/secondary/confirm/version/export。

2026-08-25 `FPS-FORM-04V` real-D1 truth：private lip_y 外推 fixture **1/1 PASS（727.10s）**。`-0.015 m` intrusion=`296/422/587`；`+0.005 m` 进一步降至 `280/362/585`，首次在 left/right/rear3q 同时得到 expected-void strong signal，identity/stable binding/rear-cap/lineage 保持。但三视图 intrusion 均非零，rear3q adjacency 仍 `248 milli < 250`，故两候选均 `BLOCKED_FORMAL_OWNER_GATE`。下一原子只允许最后一次 `+0.025/+0.045 m` private screen；若 rear3q adjacency 仍未过 250 则停止正向外推。证据：`docs/evidence/mcp010f/production-weapon-real-d1-private-stock-upper-profile-lip-extrapolation-screen-20260825.json`。

2026-08-25 `FPS-FORM-04U` real-D1 truth：逐像素 owner-mask 归属 fixture **1/1 PASS（700.95s）**。04R 内唇变化在 left/rear3q 分别影响 `142/119` 像素，但全部位于 reviewed region 外；left 已命中 boundary r1/r2/r4=`7/18/24`，rear3q r4=`12`。right 有 `18` 像素真正退出 expected void，overlap delta=`-18`。04S 更弱，04T 三视图均未触及 r4。因此下一原子只允许沿 04R lip_y 正向做一次 private bounded extrapolation。triangle attribution 仍 `UNAVAILABLE_CURRENT_RENDER_RASTERHIT`；该 PASS 仅为 zero-write 诊断，quality=`NOT_PROVEN`，无 sink/stage/secondary/confirm/version/export。证据：`docs/evidence/mcp010f/production-weapon-real-d1-stock-upper-pixel-owner-audit-20260825.json`。

2026-08-25 `FPS-FORM-04T` real-D1 truth：cap-facing inner-lip 第二坐标 `-0.405/-0.395 m` 真实 fixture **1/1 PASS（648.54s）**，但两档三视图均与 04Q baseline 完全相同：intrusion=`302/492/587`、adjacency px=`55/67/72`。候选均 `BLOCKED_OWNER_SCREEN`，该轴停止；quality=`NOT_PROVEN`、depth=`UNKNOWN`，无 sink/stage/secondary/confirm/version/export。证据：`docs/evidence/mcp010f/production-weapon-real-d1-private-stock-upper-profile-cap-lip-screen-20260825.json`。

2026-08-25 `FPS-FORM-04S` real-D1 truth：private upper-profile outer-shoulder 两档 screen 真实 fixture **1/1 PASS（621.10s）**，但均 `BLOCKED_OWNER_SCREEN`。`-0.085/-0.065 m` intrusion 分别为 `302/488/587` 与 `302/486/587`，只影响 right，left/rear3q 完全不变。identity/stable binding/rear-cap/lineage 保持，故停止 profile 第一坐标的 lip/shoulder 搜索，先审计能影响 left/rear3q 且不伪造 depth 的坐标轴。证据：`docs/evidence/mcp010f/production-weapon-real-d1-private-stock-upper-profile-shoulder-screen-20260825.json`。quality=`NOT_PROVEN`、depth=`UNKNOWN`，无 public sink/stage/secondary/confirm/version/export。

2026-08-25 `FPS-FORM-04R` real-D1 truth：固定 `0.85 m` inner span 的 private profile-lip 两档 screen 真实 fixture **1/1 PASS（595.63s）**，但两候选均 `BLOCKED_OWNER_SCREEN`。`lip_y=-0.035 m` 仅将 right intrusion 从 `492` 降至 `474`，left/rear3q 仍为 `302/587`；`-0.075 m` 使 right 回退至 `528`。三视图 identity、rear-cap、lineage 和 stable part/source/material/solid identity 保持；raw binding 仅因 triangle count 变化而不 exact。停止 lip-only 搜索，下一步只允许能影响 left/rear3q 的私有单轴 2D profile screen。证据：`docs/evidence/mcp010f/production-weapon-real-d1-private-stock-upper-profile-lip-screen-20260825.json`。quality=`NOT_PROVEN`、depth=`UNKNOWN`，无 public sink/stage/secondary/confirm/version/export。

2026-08-25 `FPS-FORM-04Q` real-D1 truth：私有 closed upper profile `profile-loft@2` 两档 screen 真实 fixture **1/1 PASS（568.68s）**，证明固定 Operator/GLB/readback 可执行，但两候选均 `BLOCKED_OWNER_SCREEN`。`0.85 m` intrusion=`302/492/587`、`0.75 m`=`302/516/587`；三视图 identity 均保持，只有 right 略改善，left/rear3q 不变。raw PartBinding 行因 triangle count 随 Operator 替换而不 exact，后续改用 stable identity projection；本轮候选已被 owner screen 阻断，不影响 promotion 结论。六视图未运行、public sink unavailable；证据：`docs/evidence/mcp010f/production-weapon-real-d1-private-stock-upper-profile-screen-20260825.json`。quality=`NOT_PROVEN`、depth=`UNKNOWN`，无 stage/secondary/confirm/version/export。

2026-08-25 `FPS-FORM-04P` real-D1 truth：私有 `stock-upper-inner-span` 两档 screen 真实 fixture **1/1 PASS（545.10s）**，但两候选均 `BLOCKED_OWNER_SCREEN`。固定 rear-cap-facing endpoint 后，`0.85 m` intrusion=`191/390/428`、adjacency=`267/358/213`；`0.75 m` intrusion=`117/246/250`、adjacency=`209/266/151`。两档都保持 exact upper-X-only、Part/source、rear-cap 和 registered lineage，但 rear3q discovery 均为 `vertical-flip`，六视图未运行。停止继续收缩单轴 inner-span；下一原子必须是 closed upper-frame 2D profile design。证据：`docs/evidence/mcp010f/production-weapon-real-d1-private-stock-upper-inner-span-screen-20260825.json`。public sink unavailable，quality=`NOT_PROVEN`、depth=`UNKNOWN`，无 stage/secondary/confirm/version/export。

2026-08-25 `FPS-FORM-04O` real-D1 diagnostic truth：真实 fixture **1/1 PASS（517.50s）**。Runtime 用临时 split Part-ID 将语义 `rear-stock` 精确分解为 upper/lower 两个 source，三视图均满足并集逐像素等于原 owner、互不重叠、geometry nodes/rear-cap/registered lineage 不变。left/right/rear3q 的全部 owner intrusion `302/560/587` 均来自 upper beam；lower beam 三视图 intrusion 全为 `0`。因此停止对称双梁搜索，下一步只允许私有、有界、单变量 upper-beam screen。split IDs 不是 public Part/sink，证据：`docs/evidence/mcp010f/production-weapon-real-d1-stock-source-attribution-20260825.json`。quality=`NOT_PROVEN`、depth=`UNKNOWN`，无 stage/secondary/confirm/version/export。

2026-08-25 `FPS-FORM-04N` real-D1 truth：私有 `stock-plane-position` 对称 screen 真实 fixture **1/1 PASS（504.80s）**，但两个候选均 `BLOCKED_OWNER_SCREEN`。`-0.10 m` 使 intrusion 从基线 `302/560/587` 变差为 `419/731/740`；`+0.10 m` 降为 `191/390/428`，但 rear3q adjacency 仅 `213` 且 discovery winner 变为 `vertical-flip`。两侧 exact two-beam X delta、rear-cap、Part/source 与 registered lineage 均稳定；因 owner screen 未过，六视图 non-regression 未运行。`stock-plane-position` 仍非 public sink，单轴平移路线停止。证据：`docs/evidence/mcp010f/production-weapon-real-d1-private-stock-plane-screen-20260825.json`。quality=`NOT_PROVEN`、depth=`UNKNOWN`，无 stage/secondary/confirm/version/export。

2026-08-25 `FPS-FORM-04M` real-D1 diagnostic truth：真实 fixture **1/1 PASS（477.36s）**，严格绑定已从错误的“唯一 discovery-eligible 候选”修正为“唯一 ranked identity 候选”，且零侵入门没有放宽。left/right/rear3q 仍分别侵入 `302/560/587` pixels，adjacency=`319/420/248` milli，状态仍为 `BLOCKED_OWNER_VOID_ACCEPTANCE`。新增的有符号 bbox/centroid 只读诊断显示左右视图呈镜像横向残差，但无法仅从 image-X 推断 registered world-X 正负；下一步只允许私有 `stock-plane-position` 对称 `-0.10/+0.10 m` A/B，不开放 public sink。证据：`docs/evidence/mcp010f/production-weapon-real-d1-owner-ranked-directional-diagnostic-20260825.json`。depth=`UNKNOWN`、stage/secondary/confirm/version/export 均未推进。

2026-08-25 `FPS-FORM-04K` real-D1 truth：真实 fixture **1/1 PASS（375.39s）**，但视觉门结果为 `BLOCKED_OWNER_VOID_ACCEPTANCE`。Runtime 现将 `ReferenceViewSpec@1.image.rotation_degrees` 应用于 crop-local mask 与 reviewed contour；rear-three-quarter 以 authored `180°` reference registration 直接得到 registered-camera Part-ID 的 `identity` discovery，三个视图均保持 `post_render_transform=identity`。严格 raw intrusion 仍为 left `302/1018`、right `560/1623`、rear-three-quarter `587/3792`，因此 all-views=false、promotable=false、depth=`UNKNOWN`、stage=`camera-calibrated`、secondary=`NOT_CREATED`，无 confirm/version/export。该 rotation 只证明 mask/contour/owner-void diagnostic，landmark/region 全质量链仍 `NOT_PROVEN`。证据：`docs/evidence/mcp010f/production-weapon-real-d1-registered-owner-void-diagnostic-20260825.json`。

2026-08-25 `FPS-FORM-04J` real-D1 truth：真实 fixture **1/1 PASS（360.46s）**。fresh Runtime 已对 durable `ProductionCameraLock@1` 做 restart/CAS readback，并从其精确 CameraRig CAS 生成 transient `RegisteredCameraRigCalibration@1`；同一 candidate/state/artifact、GeometryProgram raw/canonical、7 个相机与唯一 primary=`left` 全部 exact link，cross-candidate fail closed，注册段零新增 CAS。证据：`docs/evidence/mcp010f/production-weapon-real-d1-registered-camera-lock-readonly-link-20260825.json`。该 PASS 仅为只读 sibling lineage：rear-three-quarter reference registration 仍 `BLOCKED_SEPARATE_REFERENCE_REGISTRATION`，depth=`UNKNOWN`，CrossView=`QUALITY_TARGET_NOT_MET`，stage=`camera-calibrated`，secondary=`NOT_CREATED`，无 confirm/version/export。

2026-08-25 `FPS-FORM-04I` 历史 source truth（不覆盖 2026-08-26 顶部真值）：新增公开但非持久化的 `ProductionWeaponSubjectFrameRegistration@1` 与 `RegisteredCameraRigCalibration@1`；当时为 **474 schemas / 28 operator entries / 103 read + 76 write = 179 tools**。注册只允许 identity 或精确 `yaw-180-y`，并绑定 candidate state、artifact、GeometryProgram raw/canonical、operator catalog、CameraRig raw/canonical 与 registration canonical；合同检查 **474/474**、相机注册单测 **8/8** 通过。边界为 `PASS_PUBLIC_CONTRACT_SOURCE_NON_DURABLE`：Store/CAS/MCP 未接入，真实 D1 CameraLock/FormQuality 未重放，rear-three-quarter reference registration 仍 blocked，owner-void acceptance=`NOT_RUN`，depth=`UNKNOWN`，stage=`camera-calibrated`，secondary=`NOT_CREATED`，无 confirm/version/export。证据：`docs/evidence/mcp010f/production-weapon-registered-camera-rig-contract-source-gate-20260825.json`。

2026-08-25 `FPS-FORM-04H` source truth：正确 D1 source object `8f196f...e1fdf` 的 exact muzzle/stock/side anchors 证明其 GeometryProgram frame 相对 canonical SubjectFrame 为右手系 `yaw-180-y`。私有 `ProductionWeaponSubjectFrameRegistration@1` projection canonical hash 为 `19b6cd...267fe`，synthetic **7/7** 与真实 source **1/1（0.05s）**通过，7 sinks 前后不变。权威边界仍是 `PASS_SOURCE_READ_ONLY_PROJECTION`：没有 public/durable registration contract、没有真实 registered CameraLock/FormQuality 重放、没有 owner-void acceptance；rear-three-quarter reference registration 仍 blocked，depth=`UNKNOWN`，stage=`camera-calibrated`，secondary=`NOT_CREATED`，无 confirm/version/export。

2026-08-25 `FPS-FORM-04G-REGION-PART-ID-BINDING` supplemental truth：真实 D1 **1/1 PASS（371.07s）**，`docs/evidence/mcp010f/production-weapon-form-region-part-binding-supplemental-20260824.json` 证明 left/right=`horizontal-flip`、rear-three-quarter=`vertical-flip` 是 baseline/trial 一致的唯一有限 image-space 候选。它不构成 authored orientation/registration truth，故 machine truth 保持 `BLOCKED_AUTHORED_ORIENTATION_OR_REGISTRATION`、`diagnostic_only=true`、`promotable=false`；FormArt canonical、user-confirmed contours、depth=`UNKNOWN`、stage=`camera-calibrated`、secondary=`NOT_CREATED` 均不变，无 confirm/version/export。

2026-08-24 `FPS-FORM-PART-ID-AUDIT-04F` supplemental truth：`docs/evidence/mcp010f/production-weapon-form-part-id-audit-supplemental-20260824.json` 为 supplemental diagnostic only，状态 `BLOCKED_OWNER_BINDING`。`stock-open-frame-angle=0.12` mutator 改变了 left/right/rear-three-quarter 的 `rear-stock` Part-ID mask，但 reviewed region↔Part owner binding 均未 ready；FormArt canonical unchanged，depth=`UNKNOWN`，baseline retained。该回执不改 current benchmark truth、schema/MCP public contracts/manifest 或 Stage0 hashes，也没有 stage/confirm/version/export。

2026-08-24 current bounded-correction truth：六视图有向 bbox 诊断已记录。`receiver-envelope-height=0.90` 在 left/right/rear-three-quarter silhouette IoU 分别回退 `-0.005793775531/-0.003489678528/-0.000874537504`；`receiver-envelope-width=0.90` 在 top 改善 `+0.002570143137`，但 rear-three-quarter 回退 `-0.001420189109`；`stock-open-frame-clearance=0.22 m` 在 left/right/rear3q 回退 `-0.000293201195/-0.000205655818/-0.000426056732`，`stock-open-frame-angle=0.12 rad` 回退 `-0.000011178188/-0.00000371142/-0.000089696154`。两个 stock 试验均保持 depth/rear-cap 不变，但 frozen-camera evaluator 中 left/right/rear3q open-stock void 四项指标均与基线完全相同，无严格改善；组合接受门为外轮廓与内孔均不回退且各有严格改善。四个 pure typed 修正均 `accepted=false`，原基线候选保留；下一原子是 reviewed region/Part-ID 绑定审计。Runtime→Worker JSON 数值往返 canonical 回归及真实 D1 全链保持 PASS。机器真值仍为 `QUALITY_TARGET_NOT_MET`，stage=`camera-calibrated`，`secondary-form-approved=NOT_CREATED`，confirm/version/export=false。

2026-08-24 current D1 confirmation truth：V4 的 25 条 line-flow `runtime_kind_candidate` / `continuity_group_id` 已由用户 exact 全量确认；`rear3q.open-stock-void` 已按用户授权形成保守、封闭、无自交且位于 reviewed outer contour 内的二维 subtract polygon。Runtime 已将全参考 mask、25 line flows 与 6 negative spaces 确定性投影到六个 crop-local 512 视图，对越界/空 mask fail closed，再用 bounded `CameraCalibration@2` 在不改几何的前提下固化六视图相机。同一真实 D1 candidate 的 6 RenderSets/54 AOV、durable FormEvidence、durable FormArt、same-candidate CrossView、FormQuality@2 zero-write preflight 与 restart readback 均通过结构/持久化 Gate；六视图 IoU 为 `0.4383/0.4328/0.2158/0.2133/0.4269/0.2779`，但机器真值仍全部 `QUALITY_TARGET_NOT_MET`。rear crop 扩展只为保留参考中可见的下方撑杆，所有 depth 仍为 `UNKNOWN`；FormArt target observation 仍被 line-flow 与 left negative-space 指标阻断。这不是候选真人视觉批准，也没有 secondary stage/confirm/version/export，`secondary-form-approved` 继续 blocked。

2026-08-23 艺术决策真值：六视图 FormArt 现已有 Runtime-owned 只读 assembly 决策投影，结果固定为 5 个语义组/10 个 Gate，重启后结果完全一致且无 CAS/SQLite 写入。typed descriptor resolver 已让 receiver-envelope、muzzle-axis 在当前 GeometryProgram 可解析时 `READY_FOR_SEARCH`；stock-open-frame、trigger-void、rail-spine 仍 `BLOCKED_PARAMETER_SINK`，整体 parameter-sink gate 仍 `BLOCKED`。negative-space source gate 要求 left/right/rear-three-quarter 各有 exact trigger-void + open-stock-void observed rows（front/back/top 为 user-confirmed + not-applicable + empty），但真实 04A 仍 bbox / `mask_operation=none`，所以仍 `BLOCKED_NEGATIVE_SPACE`；line-flow 与 first-person profile 仍阻断。真实 D1 sink fixture/restart 本轮未运行；这些缺口满足前不得启动 candidate search/阶段推进/High-Low，当前真值仍是 `QUALITY_TARGET_NOT_MET` / `visual NOT_PROVEN`。

2026-08-24 当前 source truth：**472 schemas / 28 operator entries / 103 read + 76 write = 179 tools**。真实 `FPS-FORM-EVIDENCE-04A` 已完成 CameraLock、`camera-calibrated` Stage、六视图/54 AOV FormEvidence、reviewed-structure FormArt，以及同 candidate CrossView durable replay/restart。CrossView 六视图均 `QUALITY_TARGET_NOT_MET`，仅证明 non-regressing、无 strict improvement，hard gate=false。产品支持 8 个 typed sinks；真实 loft+box / box+cylinder+cylinder / open-stock GeometryProgram 的只读 parameter registry 精确解析 7 个 typed sinks、5 个 unavailable，`receiver-envelope-shoulder` 与 trigger/rail 继续 fail-closed，stock-open-frame 两个 sink 保持 depth/rear-cap 不变。legacy FormQuality 已成为 durable structural-only parent（hard gate=false，replay/restart 等价）；FormQuality@2 预检零写且仅剩 CrossView hard gate 与 FormArt target observation 两项 blocker，其他五项均 ready。Stage head 仍 `camera-calibrated`，无 blockout/primary/secondary head。High/Low/Cage source bundle虽 durable，仍没有 secondary head binding、artist-authored quad/edge-flow、formal three-artifact producer 或 Bake；Hero UV、人审、引擎与 export 均 `NOT_RUN`。

历史 source-intake 与七 crop/六 contour fixtures 仍作为来源先决证据保留，但当前权威结论由 2026-08-24 真实重放取代：六个 visual structures 为 user-confirmed、depth unknown；bbox-only open-frame 不构成 subtract polygon，negative-space 仍 unknown。legacy FormQuality durable parent 已补齐但 hard gate=false；CrossView 真实结果仍为 `QUALITY_TARGET_NOT_MET`，FormArt target observation 仍 blocked，所以 `secondary-form-approved` 继续 `BLOCKED`，不得把 fixture PASS、durable parent 或 sink 可执行解释为视觉质量通过。

2026-08-22 `CandidateAnimationVfxQuality@2` source/structural gate：Contracts **402**；Store focused **3/3**、Store full **112/112**；Runtime focused **6/6**、同源码同 cohort Runtime full **354 passed / 0 failed / 22 ignored**（376 total，115.40s）；MCP focused **4/4**、full **152 passed / 0 failed / 0 ignored**（2.49s）；contracts/runtime/store/MCP joint cargo check **PASS**。旧 `GEOMETRY_WORKER_PROTOCOL` 报告来自 stale Worker binary，已由同源码 Geometry/Render Worker 重建后清除。尚无真实 `Attachment@3 + Quality@2` public full-chain positive fixture，durable end-to-end=`NOT_RUN`/`BLOCKED_FIXTURE_CHAIN`；当前仅 `structural_only`，visual/artistic/commercial FPS=`NOT_PROVEN`，human/engine=`NOT_RUN`，不推进 stage/confirm/version/export。证据：`docs/evidence/mcp010f/candidate-animation-vfx-quality-v2-durable-source-gate-20260822.json`。

2026-08-22 `CandidateMaterialSurfaceQuality@1` public positive fixture：`Geometry → CandidateTopologyQuality@1 → AppearanceProgram@3 → TextureBuild@2 → SurfaceBake@1 → AppearanceSourceLineage@1 → CandidateMaterialSurfaceQuality@1` 的 `prepare → same-key replay → get → Runtime drop/reopen → restart get` 通过 **1/1（111.72s）**；Runtime focused **5/5**、Store full **74/74**、Contracts **350**。CAS inventory unchanged；stable `artifact_id` 与 GLB object SHA-256、MaterialPack CAS kind 精确区分，合法 UV/tangent rebuild 不计入 geometry-preservation 漂移。该结果仅为 `structural_only`；V2 animated-socket-particles 仍无完整 public `prepare → Store → restart get`，durable end-to-end=`NOT_RUN`/`BLOCKED_FIXTURE_CHAIN`；visual/commercial=`NOT_PROVEN`，human/engine=`NOT_RUN`，stage/confirm/version/export=false。证据：`docs/evidence/mcp010f/candidate-material-surface-quality-public-positive-source-gate-20260822.json`。

最终同 cohort 修订口径：强制 build cohort `724278fe8f6777c8b3d07bc5058208aee90aa5c700db5f9284d6297126fa79f6` 下 material focused **5/5（112.63s）**；Runtime full **310 passed / 0 failed / 20 ignored**（330 total，201.91s），且 public material fixture 明确在该 full run 内执行。此前 **111.72s** 仅为 public fixture 单测时长；两者都只支持 `structural_only`，不提升 visual/commercial、human/engine 或 stage/confirm/version/export 状态。

2026-08-22 `FictionalEnergyVfxAnimatedSocketParticlesSequence@2` 双候选 source slice：Contracts **350**；Store V2 focused **2/2**、Store full **74/74**；Runtime V2 仅低层 focused **6/6**、cargo check **PASS**；MCP V2 **3/3**；同 cohort `724278fe8f6777c8b3d07bc5058208aee90aa5c700db5f9284d6297126fa79f6` Runtime full **309 passed / 0 failed / 20 ignored**（191.06s）、MCP full **128 passed / 0 failed / 0 ignored**（1.93s），这些是全量回归，不是 V2 public `prepare → Store → restart get` 正向 fixture。V1/V2 隔离；V2 仅证明 1..16 frame、geometry/appearance 双 candidate/delivery/AnchorSet bridge 以及 Store FK/reachability/idempotence/conflict/rollback 的结构面。完整双候选 public Runtime `prepare → Store → restart get` 正向 fixture 尚不存在，durable end-to-end=`NOT_RUN` / `BLOCKED_FIXTURE_CHAIN`，不能声称正向 durable。该 slice 为 `structural_only`；visual/commercial=`NOT_PROVEN`，human/engine=`NOT_RUN`，stage/confirm/version/export=false。证据：`docs/evidence/mcp010f/fictional-energy-vfx-animated-socket-particles-v2-dual-candidate-source-gate-20260822.json`。

2026-08-22 `FGC-MCP010F-TRAILS-21` 当前为 source `in_progress`：已形成动态 animated socket Trails 与 TrailsBloom 的 closed Contracts、versioned Render Worker/Protocol、Store parent/frame 事务，以及 Runtime/MCP prepare/get focused source 实现；新增 animated GLB socket transform projection 的 Runtime-owned durable/replay/restart read-only source Gate。当前源真值为 Contracts **344**、**26/26 active operators**、MCP **79 read + 59 write = 138 tools**；同 cohort `9cc7c11b8309ee7cc76df3d67794c4692c81ff0e8ef064248955ad511b8ca388` Runtime full lib **307/307**（0 failed，另 20 ignored）、binary **1/1**，合计 **308 pass**，Store full **72/72**、显式固定 cohort 的 MCP full **125/125**。该 projection Gate 不等于 animated-socket Particles/Trails/TrailsBloom 正向集成；动态 VFX 链仍缺完整正向 durable `prepare → Store → restart get` 集成测试和独立回执，因此不得宣称 durable end-to-end PASS。整体保持 `structural_only`，不推进 stage/confirm/version/export，视觉/艺术/商业 FPS=`NOT_PROVEN`，人评/引擎=`NOT_RUN`。证据：`docs/evidence/mcp010f/game-weapon-animated-glb-socket-transform-projection-source-gate-20260822.json`。

2026-08-21 `CandidateAnimationVfxQuality@1` durable structural report 子门已完成：Runtime/Store/MCP 精确重验 material-surface V2 head、delivery/AnchorSet、既有 animation clip/GLB receipt/animated socket 和完整 VFX stack，且只在全部只读校验后写一个有界 JSON report。该子门完成时的历史快照为 **319 schemas / 26/26 active operators / 74 read + 54 write = 128 tools**；Store 55/55、Runtime 284 executed pass + 20 ignored、MCP 110/110。它的真实结论是 blocked：VFX 为 sidecar-only，`vfx_glb_socket_attachment=false`、hard gate failed，不推进生产 head；visual/artistic/commercial FPS=`NOT_PROVEN`，human/engine=`NOT_RUN`，无 confirm/version/export。

2026-08-21 当前最新 source 原子能力为 Runtime-owned `ProductionStageTransition@2`：经显式 approval，将 immutable topology root 与通过的 `CandidateMaterialSurfaceQuality@1` exact binding 原子提升为 distinct material-surface head；Runtime/Store/MCP 精确绑定 V1 parent、双 candidate/artifact/state、topology/material report CAS、reference/camera evidence 和 receipt，支持历史 restart get、幂等、冲突/stale 拒绝与限定 rollback，且不改写 V1 head。现行 **314 schemas / 26/26 active operators / 73 read + 53 write = 126 tools**；MCP full 同构建 107/107，Runtime full 277 executed pass + 20 ignored，Store full 51/51。该 head 仍是 `structural_only`：无参考绑定 PBR likeness、真人视觉、Commercial FPS 或 Unity/Unreal/Godot round-trip，也不 confirm/version/export；整体 `QUALITY_TARGET_NOT_MET` / `INCOMPLETE_TRUTH_BINDING` / camera `MISMATCH` / `BLOCKED_REFERENCE_COVERAGE` 不变。receipt：`docs/evidence/mcp010f/production-stage-transition-v2-durable-source-gate-20260821.json`。

2026-08-21 当前最新 source 原子能力为 candidate-bound `GeometryModifierApplyResult@2`：Runtime 只允许 exact current-head candidate 的 direct `authoring-mesh@1` 单 stable edge 进入 `bevel@2`，并在 Store 事务中重新绑定 source/head/derived/sidecar；同 cohort Geometry/Render Worker 已通过幂等、重启、v1 回归和固定同相机九 AOV 非退化 Gate。现行 **293 schemas / 69 read + 49 write = 118 tools**。该事实只证明 bounded visible-detail handoff，quality 仍 `structural_only`；不证明任意 edge-chain bevel、参考 likeness、PBR、人评、确认/版本/导出或商业游戏武器质量，整体 `QUALITY_TARGET_NOT_MET` / `INCOMPLETE_TRUTH_BINDING` / camera `MISMATCH` / `BLOCKED_REFERENCE_COVERAGE` 不变。

2026-08-21 `GAME-ENGINE-IMPORT-13` Godot evidence-only conformance PASS：实际 Godot `4.7.2.stable.official.ed1daf0bf` headless importer 聚合 4 个 GLB scene（LOD0/1/2/animated），三档 triangles `304 > 176 > 112`，每个 scene 5 meshes、相同 materials、6 个 non-rendering socket nodes 且 parent/local TRS exact。animated scene 的 10 glTF channels→2 Godot semantic tracks，cross-loader t0/half-duration socket TRS exact，两个 named socket follow；5 个 `BoxShape3D` collision sidecar readback exact。static 与 animated 由独立 Runtime source cohorts 提供；没有 Runtime durable Godot link。`commercial_engine_roundtrip=false`，Unity/Unreal、physics/hitbox、visual/human、confirm/export `NOT_RUN/false`，quality `structural_only`；整体 `QUALITY_TARGET_NOT_MET` / `INCOMPLETE_TRUTH_BINDING` / `MISMATCH` / `BLOCKED_REFERENCE_COVERAGE` 不变。当前 **291 schemas / 69 read + 49 write = 118 tools**。

2026-08-21 当前最新 source 原子能力是 Runtime-owned `GameWeaponAnimatedGlbSocketMaterializationReceipt@1`：`game_weapon_animated_glb_socket_prepare/get` 精确绑定 candidate/state、delivery manifest 的 LOD0、`MechanicalAnimationGlbReceipt@1`/animated artifact 与 AnchorSet，在 derived animated GLB 中物化六个稳定命名 empty socket nodes。源/派生 animation projection、animations/channels/samplers、renderable projection、source static projection 与 BIN bytes 均 exact；owned CAS 恰好两个（derived animated socket GLB + materialization receipt），inline readback digest 不是第三个 CAS/FK。Three.js r185 `GLTFLoader` 验证 10 tracks 的 source/derived clip projection exact，half-duration 有 2 个 socket 跟随 animated Part parent。现行 **290 schemas / 26/26 active operators / 69 read + 49 write = 118 tools**。该事实只证明 structural-only animated GLB socket materialization；Unity/Unreal/Godot、runtime pivot、functional weapon semantics、human、confirm/version/export 与 visual quality 未运行或未通过，整体仍 `QUALITY_TARGET_NOT_MET` / `INCOMPLETE_TRUTH_BINDING` / camera `MISMATCH` / `BLOCKED_REFERENCE_COVERAGE`。

2026-08-21 当前最新 source 原子能力是 Runtime-owned `GameWeaponGlbSocketMaterializationReceipt@1`：固定对 delivery 的 LOD0/1/2 生成三份派生 GLB，每份物化六个 stable named empty nodes，严格保持源 renderable projection 与 BIN chunk byte-exact；CAS/SQLite parent+exact-three-child durable link、幂等、冲突拒绝、限定回滚和 restart source/derived parse 均通过。Three.js r185 `GLTFLoader` 三 LOD 消费 Gate 通过。现行 **284 schemas / 26/26 active operators / 68 read + 48 write = 116 tools**。这证明结构化 GLB 节点消费，不证明 Unity/Unreal/Godot、动画 socket、运行时 pivot、视觉/人评、确认/版本/导出；总体门状态不升级。

2026-08-21 当前最新 source 原子能力是 Runtime-owned `FictionalEnergyVfxTrailsBloomFrameReceipt@1`：同一 candidate/artifact/camera/profile/cohort 的 typed trails 经固定 profile 派生独立 `trail-emissive-source` / `trail-bloom-contribution`，旧 trail 三 pass、基础九 AOV、原 HDR Bloom 与 particles 均 byte-exact 不变；四个自有 CAS 对象与 SQLite immutable link 支持幂等、冲突拒绝和 Runtime 重启复验。forced durable Gate 1/1 PASS（152.61 秒），isolated Worker Gate 1/1 PASS（27.88 秒）。现行 **278 schemas / 26/26 active operators / 67 read + 47 write = 114 tools**。其输出为 RGBA8 review evidence，不是 FP16；sidecar anchors 不是 GLB sockets，engine/foreign-cohort/human/confirm/version/export 未通过，总体门状态不升级。

2026-08-21 当前最新 source 原子能力是 Runtime-owned `FictionalEnergyVfxTrailsFrameReceipt@1`：严格有序的 1–4 个历史 particle frames 与 current particle frame 生成两条固定 typed trails，输出独立 `trail-color` / `trail-id` / `trail-depth`；opaque geometry 深度遮挡、稳定 parent trail ID 决胜、同 cohort 双 Worker 字节一致，CAS + SQLite immutable link 持久化并在 Runtime 重启后重验。current base/Bloom 与全部历史 particle lineage 均精确绑定，基础九 AOV、Bloom、particle passes 保持 byte-exact。forced durable Gate 1/1 PASS（135.47 秒），独立 Worker Gate 1/1 PASS（25.17 秒）。现行 **271 schemas / 26/26 active operators / 66 read + 46 write = 112 tools**。V1 不把 trails 输入 Bloom，sidecar anchors 不是 GLB sockets；Unity/Unreal/Godot、真人视觉和质量 Gate 未通过，总体门状态不升级。

2026-08-21 `VFX-FRAME-02` 底层执行子门已通过：Render Core/隔离 Render Worker 新增有界 `render_glb_vfx_frame`，只有同时匹配 glTF material name=`MaterialZone ID` 与 `extras.forgecad.material_id` 的目标才能接受临时 emissive override；0/8 强度对比只改变 beauty，其余八 AOV 字节不变，重放也逐字节一致。该路径不改 GLB/CAS/candidate。但 typed time-sampling 合同、Appearance LOD delivery 绑定、Runtime CAS frame receipt 仍未完成，AnchorSet 仍是 sidecar 而非 GLB socket；不得宣称完整材质动画、Bloom/粒子/拖尾或商业视觉 PASS。receipt：`docs/evidence/mcp010f/fictional-energy-vfx-frame-render-worker-source-gate-20260821.json`。

2026-08-21 当前 source truth：Runtime/MCP 新增 durable `FictionalEnergyVfxProfile@1`，精确绑定虚构非功能性游戏资产的 delivery、AnchorSet 和 allowlisted 2K MaterialPack；CAS/SQLite、幂等、重启读回、闭集采样、负值拒绝、raw stdio 与 Three.js r185 线性采样数学 Gate 已通过。当前为 **227 schemas / 26/26 operators / 58 read + 40 write = 98 tools**。它只持久化 sampled emissive intent；不证明 GLB MaterialZone 绑定，不渲染材质动画、Bloom、粒子或拖尾，也不证明 Unity/Unreal/Godot 或商业游戏资产完成度；总体仍为 `QUALITY_TARGET_NOT_MET` / `INCOMPLETE_TRUTH_BINDING` / camera `MISMATCH` / HQ360 blocked。

2026-08-21 当前 source truth：默认只读 `game_asset_lod_derive` 已提供受限自动 LOD 变体派生：精确绑定 GeometryProgram/candidate/artifact，仅降低白名单 typed tessellation 参数，Worker 双回放、Part/MaterialZone/AABB/三角形预算验收通过，无 Runtime/CAS/candidate/version 写入并要求后续 materialize。当前为 **215 schemas / 26/26 operators / 56 read + 38 write = 94 tools**。该结果仅是 `structural_only` transient preview，不是通用 mesh simplification/任意 GLB 减面，不证明外观保持、Unity/Unreal/Godot import、引擎 LODGroup/碰撞组件、确认/外部 export 或视觉质量 PASS；总体真值仍为 `QUALITY_TARGET_NOT_MET` / `INCOMPLETE_TRUTH_BINDING` / camera `MISMATCH` / HQ360 blocked。

2026-08-21 上一 durable delivery source truth：MAT-04 与标准 rigid glTF animation CAS prepare 保持已有结构 PASS；`game_asset_delivery_prepare` 的 authored LOD set、LOD2 POSITION 派生 AABB、四个 sidecar 与可选动画已进入 Runtime-owned durable `GameAssetDeliveryLink`，Store 同事务建立 reachability，冲突 retarget/cross-project fail closed；默认只读 `game_asset_delivery_get` 在重启后复验 CAS bytes 和完整绑定。Three.js r185 已真实解析 LOD0/1/2、回算 triangles、核对材质名/Part AABB并采样动画。当前为 **215 schemas / 26/26 operators / 56 read + 38 write = 94 tools**。这不是自动减面、Unity/Unreal/Godot import、引擎 LODGroup/碰撞组件、确认/外部 export 或视觉质量 PASS；总体真值仍为 `QUALITY_TARGET_NOT_MET` / `INCOMPLETE_TRUTH_BINDING` / camera `MISMATCH` / HQ360 blocked。

2026-08-20 当前 MAT-03 source truth：`forgecad-fictional-energy-weapon-2k` 已有 5 张实际嵌入 GLB 的 2048×2048 PNG，并由 strict reader 从 BIN 物理解码和 hash/dimension/semantic 校验；optimized same-cohort raw stdio 与 durable TextureBuildReceipt@2 PASS。派生的 fixed synthetic microdetail 明确不是 geometry normal/AO bake，也不升级视觉 PBR、真人、当前 candidate、package/engine/export-restart/HQ360；总体质量仍 `QUALITY_TARGET_NOT_MET`。

2026-08-20 当前 MAT-02 source truth：`forgecad-fictional-energy-weapon` AppearanceProgram 物化的 GLB 已有 product-owned artifact-wide continuous UV island policy，物理 TEXCOORD_0 与 chart assignment 通过 2048 atlas / 8 texel padding / shared-edge-or-explicit-seam / positive-area-overlap strict Gate；复杂投影不偷偷回退 legacy，而是确定性增加显式 seam。旧 robot 路径仍为历史 512 triangle grid。该结构事实不等于 2048 texture bytes、normal/AO bake、decal/wear/clearcoat、视觉 PBR、真人、engine/package/export 或 HQ360，所有总体质量阻断保持。

历史数值口径：本段的 **344 schemas / 26/26 active operators / 79 read + 59 write = 138 tools** 与下文更小数值均是 historical prior slice；现行机器真值只认本文顶部的 **490 / 28 / 106 + 78 = 184** 与 Stage0 marker。

2026-08-20 当前最新 source truth：专用 `forgecad-fictional-energy-weapon@1.0.0` MaterialPack 已进入 closed `AppearanceProgram@2` 选择与 MCP/Runtime/Worker/GLB 结构链路，pack ID/hash、7 个 512px CC0 texture bytes、7 个材质定义、exact Part/MaterialZone binding 与 embedded-only GLB 均可回读并 fail closed。该事实只证明 development-only source/isolated transport admission；连续 UV、2K、normal/AO bake、decal、wear、clearcoat texture、同候选视觉、人评和发布链仍未实现或未运行，因此 `QUALITY_TARGET_NOT_MET`、`INCOMPLETE_TRUTH_BINDING`、camera `MISMATCH` 与 HQ360 阻断不变。

2026-08-20 当前 source truth：active `forgecad.geometry.energy-core@1` 是零输入、closed typed、固定 Worker 执行的同心轴组件 operator；guard/mechanical ring 生成内外壁与顶底 annuli，emitter/backplate 生成 closed cylinder，一节点一 semantic Part。source structural PASS 不改变 `QUALITY_TARGET_NOT_MET`、`INCOMPLETE_TRUTH_BINDING`、camera `MISMATCH`、人评/PBR/package/export-restart 或 HQ360 阻断。

2026-08-19 当前 source truth：`authoring_mesh_edit_prepare` 是 Runtime-owned、显式 opt-in、requires-confirmation 的 staging surface。它不信任旧 preview，而是重新执行 exact bounded edit、固定 Worker compile 与 strict V2 readback，在 current-head 检查和 Store transaction 内提交新的 reviewable candidate、Job/evidence/audit/idempotency；不 confirm、不创建 version、不 export。该 structural staging PASS 不改变 `QUALITY_TARGET_NOT_MET`、`INCOMPLETE_TRUTH_BINDING`、camera `MISMATCH` 或 HQ360 阻断。

2026-08-19 当前 source truth：`authoring_topology_get` 与 `authoring_mesh_edit_preview` 是 Runtime-owned、candidate-bound、默认只读 surface。它们从已重验的 durable geometry evidence/CAS 派生单个 direct `authoring-mesh@1` Part source topology，只提供 transient sorted vertex translate 与 boundary triangle/planar-convex-quad single-face extrude；derived GeometryProgram 必须通过 fixed Worker 双 replay和 strict GLB readback，结果不进入 CAS/SQLite/candidate/version。真实 raw stdio 与负门 PASS 只证明 source structural capability；persistent editor、selection/history、BMesh/Python/plugin、package/live/visual/human/PBR/export-restart/HQ360 仍未实现或未运行。

2026-08-19 当前 source truth：`MechanicalAnimationClip@1` 已作为 Runtime-owned immutable CAS 对象与 SQLite Link 落地；显式 write opt-in 的 `mechanical_animation_clip_prepare` 在写入前要求 candidate/artifact/readback/program/catalog/config/rest/action/schedule 全绑定，并用实际 Geometry Worker 对 source GLB 做两次 full-byte replay，要求非空同 cohort。默认只读 `mechanical_animation_clip_get` 可跨 Runtime 重启回读，`mechanical_animation_clip_preview_get` 只接受 immutable schedule 内单 tick，并对 transient frame 做双 Worker exact replay；preview 不写 CAS/SQLite/candidate/version。该 slice 仍只是 caller-authored rigid Part action，不是 Armature、bone、skin、IK、NLA/F-Curve/driver/timeline、GLB animation channel、Blender add-on 或 Python runtime；结构 PASS 不改变 `QUALITY_TARGET_NOT_MET`、`INCOMPLETE_TRUTH_BINDING`、camera `MISMATCH` 或 HQ360 阻断。

2026-08-19 当前 source truth：`RenderEvidenceReplay@1` 是默认只读、candidate-bound 的 transient structural replay。Runtime 重读完整 integrity cohort，对 current GLB bytes 重做 strict `ArtifactReadback@2` 对齐，再调用实际 fixed Render Worker 两次；必须同 cohort、同 profile、固定有序九 AOV 且 persisted/first/repeat 的原始 PNG 与 decoded RGBA8 pixels 全部 exact。输出只含 hashes/sizes/status，CAS、SQLite、candidate、version 均不写。该事实仅限同 source cohort structural repeat，不推导视觉质量、PBR、跨平台 determinism、Cycles/EEVEE/OCIO 或人评。

2026-08-19 historical source slice：`MechanicalPoseGeometryPreview@1` 是默认只读、candidate-bound 的 transient authored-rig geometry preview。Runtime 只接受 exact durable geometry cohort 与 closed rest/action，计算 per-Part `posed-world × inverse(rest-world)`，拒绝跨 Part 重复 source、混合 Part output 与 Euler gimbal，派生新的 GeometryProgram/hash，通过 fixed Worker 编译和 strict GLB readback后只返回 artifact hash/readback；CAS、SQLite、candidate、version 不变。该 slice 当时为 175 schemas、20/20 operators、49+34=83；现行整体口径为 191/90。caller-authored rest frame 不是原资产 rig/pivot provenance；不得宣称 Armature/skin/animation、Blender/Cycles/EEVEE/Python parity 或视觉 PASS。

2026-08-19 当前 source truth：`SubdivisionArtifactLineageSidecar@1` 只能由显式 opt-in Runtime prepare 在 reconstructed projection 已完成 full-GLB byte replay 后创建；canonical JSON 存入 CAS，`SubdivisionArtifactLineageLink@1` 在 SQLite 绑定 exact request/project/candidate/artifact/readback/evidence/node/lineage/binding hashes。Link insert/idempotent return 与 CAS reachable promotion 是同一 SQLite transaction；提交失败只可清理当前调用新建、temporary、unlinked 的 sidecar，既有/linked/reachable CAS 不可删。filesystem CAS staging 与 SQLite 不构成跨介质原子事务，通用 reachability/GC 仍由 MCP011 负责。默认 getter 重启可读、验证 CAS canonical bytes 与 live durable bindings且零写入；相同 request 幂等，不同 request 或跨 candidate 绑定拒绝。该 slice 当时为 173 schemas、20/20 operators、48+34=82；此能力仍是 structural-only candidate-local lineage，不改变视觉与发布真值。

2026-08-19 当前 source truth：`SubdivisionArtifactLineageProjection@1` 在不修改 closed `ArtifactReadback@2` 的前提下，把 persisted `GeometryProgram@2` root lineage 绑定到 exact candidate GLB。Runtime 先通过 durable evidence/strict readback 重验，再由 fixed Worker 重编译并逐字节匹配完整 GLB，随后只在唯一 direct `subd-cage@2` primitive 上声明 `quad-q → local triangles 2q,2q+1`；任何 project/candidate/artifact/readback/program/catalog/config/source/count/order 漂移都拒绝。该 projection 重启可重建、读取零写入，但不是 durable CAS sidecar，不暴露 glTF vertex/edge/corner identity，不跨版本稳定。该 slice 当时为 170 schemas、20/20 operators、47+33=80；视觉与发布真值不变。

2026-08-19 historical Subdivision root-lineage source truth（当时 168/79 cohort）：新增 closed `SubdivisionTopologyLineageRequest@1 → SubdivisionTopologyLineage@1` 与默认只读 `subdivision_topology_lineage_preview`。固定 Geometry Worker 在执行同一 `subd-cage@2` evaluator 时生成 control vertex/edge/quad root → 最终 evaluated quad topology 的完整作用域内映射；Runtime 独立重算规则网格计数、crease control-edge identity、root coverage、连续 quad/triangle ranges、lineage/canonical hash 和 25,000-element/1 MiB 上限。3×3 level2 为 442 lineage elements，16×16 level2 最大包络为 22,802；超调用方预算整体拒绝且不截断。该投影明确 `cross_version_stable=false`、未绑定 artifact/readback、不是 GLB element ID，不含 per-level child path、corner domain 或 influence weights，也不写 SQLite/CAS/candidate/version。当时机器真值为 168 schemas、20/20 active operators、46 read + 33 opt-in write = 79 tools；现行口径见本文顶部，视觉、package/live、人评、PBR、export/restart 与 HQ360 真值不变。

2026-08-19 historical crease-aware Subdivision source truth：新增 product-owned `forgecad.geometry.subd-cage@2` 和 closed `SubdivisionCreaseEvaluationRequest/Result@1`。新 operator 只接受 3..16 × 3..16 规则开放 quad grid、1..2 级 uniform refinement、1..128 条 canonical interior edge，以及整数 `sharpness_levels=1..2`；边界固定为 `edge-only`，boundary edge 永远 sharp，但仅有两个 boundary neighbor 的顶点按 crease mask 求值而不被 corner-pin，三条及以上 sharp edge 的 junction 才固定为 corner。Worker 实际求值已通过 smooth/dart/two-edge crease/three-edge corner/level decay golden、deterministic GLB 与 strict readback；Runtime 另通过 `geometry_prepare` 产生 128-triangle review candidate/readback，但不创建 version。`geometry_program_hash` 的新 branch 仍只是 request-bound authoring/hash projection，零 candidate/CAS/version 写入，不能写成已编译 mesh。该 slice 当时为 166 schemas、20/20 active operators、45 read + 33 opt-in write = 78 tools；Blender GPL/OpenSubdiv 仅作冻结源码研究，未复制、链接、安装或运行；arbitrary topology、fractional/vertex crease、limit surface、adaptive、FVar UV、Modifier Stack 接入、package/live/render/visual/human/360 均未证明。receipt：`docs/evidence/mcp010f/blender-subdivision-crease-source-gate-20260819.json`。

2026-08-19 historical source truth：该 Boolean Operand Lineage slice 当时为 164 schemas、19/19 active operators、45 read + 33 opt-in write = 78 tools。`BooleanOperandLineageRequest@1 → BooleanOperandLineage@1` 是 fixed Geometry Worker 产生、Runtime 独立重算 program operation/operands/source lineage 并重验、MCP 默认只读的结构投影；它只证明一个 exact `GeometryProgram@2` Boolean 节点的 left/right operand 与 evaluated-face run 覆盖。evaluated face ID 不是原始 authoring face ID，跨 program 变更不稳定，也未持久化到当前 GLB；无 Runtime durable write，视觉仍 `QUALITY_TARGET_NOT_MET`。receipt：`docs/evidence/mcp010f/blender-boolean-operand-lineage-source-gate-20260819.json`。

2026-08-19 historical Render Evidence Integrity source truth：该 slice 当时为 162 schemas、19/19 active operators、44 read + 33 opt-in write = 77 tools。`RenderEvidenceIntegrityRequest@1 → RenderEvidenceIntegrity@1` 是 current candidate-bound、read-only 的 CAS/readback 完整性投影；它验证 object hash、canonical hash、camera identity、ArtifactReadback、九 AOV/mask 的 exact PNG bytes 和 quality threshold lineage，且固定 `runtime_write_performed=false`。它只形成新的 current-source 证据，绝不回填 attempt35，也不把 same-camera、结构完整性或 source Gate 写成视觉/真人验收。

2026-08-19 historical Mechanical Pose Sequence Preview source truth：该 slice 当时为 160 schemas、19/19 active operators、43 read + 33 opt-in write = 76 tools。`MechanicalPoseSequencePreviewRequest@1 → MechanicalPoseSequencePreview@1` 是 Runtime-owned、read-only、`structural_only` 的最多 16 tick 有序机械姿态预览；每个 sample 与 candidate/artifact/readback/program/catalog/config/rest/action/policy exact binding，candidate/CAS/version 不变。它不是 Armature、skin、timeline 或动画资产，也不改变 `QUALITY_TARGET_NOT_MET` / `INCOMPLETE_TRUTH_BINDING` / camera `MISMATCH`。receipt：`docs/evidence/mcp010f/blender-mechanical-pose-sequence-preview-source-gate-20260819.json`。

2026-08-18 historical Parametric Group v2 source truth：该 slice 当时为 158 schemas。`ParametricDesignKitRequest@2 → ParametricDesignKitProgram@2` 是 Runtime-owned、read-only、`structural_only` 的固定 first-party Geometry Group lowering；template/catalog/parameter/instance/program/source-map 哈希受绑，candidate/CAS/version 不变。receipt：`docs/evidence/mcp010f/blender-parametric-group-v2-source-gate-20260818.json`。

Mechanical pose source evidence：`docs/evidence/mcp010f/blender-mechanical-pose-source-gate-20260818.json`。它只证明 candidate/artifact/Part/source-node 绑定、closed rest hierarchy、有限 pose action 与确定性只读求值；不证明 Armature、skin、动画资产、Worker 几何 materialization、package/live 或视觉质量。

RenderProfile/AOV source evidence：`docs/evidence/mcp010f/blender-render-profile-aov-lineage-source-gate-20260818.json`。

Final-review authority：`RenderSet@2` Schema/Runtime exact rules 已一致，Worker AOV PNG 必须真实解码为精确 RGBA8 尺寸，Part-ID/Material-ID 的固定 palette 只覆盖并硬限 0..255；这三项均为 fail-closed source contract，不升级视觉质量。

2026-08-18 historical source truth：该 slice 当时 contract manifest 为 160 schemas，canonical OperatorCatalog 仍为 19/19 active，MCP 为 43 read + 33 opt-in write = 76 tools。最新 Mechanical pose slice 新增独立 `MechanicalRestFrame@1`、`MechanicalPoseAction@1` 与只读 `mechanical_pose_evaluate`；Runtime 绑定同一 candidate/artifact/readback/program/catalog/config 和完整 Part/source-node coverage，执行最多 64-link、深度 16 的无环层级与 fixed/revolute/prismatic 单自由度求值。该状态仅为 source/structural contract PASS；无 skin/IK/NLA/F-Curve/Worker materialization，也不改变 package/live/视觉或真人证据。RenderProfile/AOV 的 152-schema receipt 与 Subdivision v2 的 151-schema receipt均保持历史 slice。

2026-08-18 historical Modifier evaluation v2 source truth：该 slice 当时 contract manifest 为 149 schemas，canonical OperatorCatalog 为 19 entries / 19 active，MCP 为 42 read + 33 opt-in write = 75 tools。该增量通过默认只读 `geometry_program_hash` 返回 authoring/effective/program/policy/catalog/cache hash、逐 stage hash chain、dirty reason 与 deterministic semantic-signature reuse decision；cache key 绑定 GeometryProgram hash，previous signature 对 project/representation/Part/material/solid fail closed，base operator/parameters 由 11 个 paired Schema/MCP branches 约束。previous signature 只由调用方 round-trip，禁用项纯 trace 变化不改变有效 DAG/output/cache。focused test 证明 candidate/version/CAS inventory 不变；SQLite 全表与 Job inventory 未直接比较。该 PASS 只覆盖 `structural_only` source semantics；没有持久 mesh cache、真实 GLB/readback、Runtime/Worker build-cohort reuse、package/live 或视觉证明。此前 Bevel/Normal、TopologySnapshot 各自证据继续有效，但更早日期段落中的 136/138/139/144/146 schemas 与 16/17-entry catalog 均绑定其原 historical cohort/receipt，不能覆盖上方当前 Subdivision 段或 `docs/evidence/mcp010f/current-benchmark-truth.json`。receipt：`docs/evidence/mcp010f/blender-modifier-evaluation-v2-source-gate-20260818.json`。

2026-08-17 Fictional Energy Rifle Profile source truth：`FictionalEnergyRifleProfile@1`/`FictionalEnergyRiflePlan@1` 在该 slice 当时进入 138-schema source；仅接受原创 `fictional-game-asset` 且强制 `nonfunctional_asset=true`，bounded intent 只展开为既有 `ParametricDesignKitRequest@1`，不写 candidate/CAS/SQLite/version。receipt：`docs/evidence/mcp010f/fictional-energy-rifle-profile-source-20260817.json`。该 source/structural slice 不改写 Stage 0 历史观察、camera `MISMATCH`、`QUALITY_TARGET_NOT_MET`、`INCOMPLETE_TRUTH_BINDING` 或 `HQ_360=BLOCKED_REFERENCE_COVERAGE`，不构成视觉质量/PBR/人评/export/restart/360 PASS。

2026-08-17 `FGC-MCP010F` current orchestrator transport truth：Runtime IPC 已补齐 `design_stage_run_prepare`/`design_composition_prepare` typed dispatch，回归测试确认不会再把这两个已暴露方法折叠成 generic `INVALID_INPUT`；稳定错误族只输出 bounded code。Dev.app cohort `906c8decb7aec90e8854bb0b7eb4d650dd3895851b556a62a1c8d16557aa17c8` 的真实参考隔离 stage batch 已完成 1 个 child ActionRun，`view_spec` 与 observation 绑定转发，Runtime materialized typed parameter patch，batch/replay hash 相等，source candidate 未变、candidate/version=未确认/0。视觉真值保持 `QUALITY_TARGET_NOT_MET`，不是完整 orchestrator/Repair/高质量 PASS；live restart、多视图、PBR、人评、export/restart 与 360 仍 `NOT_RUN/BLOCKED`。receipt：`docs/evidence/mcp010f/dev-app-install-orchestrator-dispatch-fix-20260817.json`、`docs/evidence/mcp010f/current-source-real-reference-orchestrator-dispatch-fix-20260817.json`。

2026-08-17 PDK v0 source truth：当前只读 `geometry_program_hash` 可接受 `ParametricDesignKitRequest@1`，Runtime 对 housing/panel/frame、vent、joint、sensor 六类 intent 做有限数值/几何关系校验，展开为固定 Catalog 绑定的单节点 `GeometryProgram@2`，返回 `ParametricDesignKitProgram@1`、program/intent/input/source-map hashes。129-schema contract check、Runtime deterministic/no-persistence focused tests、MCP oneOf/read-only test、cargo check 与完整 `script/test_mcp010f.sh` source Gate PASS；源码变更后的 Dev.app 已以 cohort `6f00a58a…ed50` 重建/安装并通过隔离 package probe 与 packaged PDK read-only round-trip。receipt：`docs/evidence/mcp010f/parametric-design-kit-v0-source-gate-20260817.json`、`docs/evidence/mcp010f/dev-app-install-pdk-v0-20260817.json`、`docs/evidence/mcp010f/dev-app-probe-pdk-v0-20260817.json`、`docs/evidence/mcp010f/dev-app-probe-pdk-packaged-read-only-20260817.json`。这不是完整 PDK、CAD kernel、candidate/Repair/视觉/PBR 或插件安装；live Desktop restart、真实多视图、人评、export/restart、HQ_360 仍未运行或阻断。

2026-08-17 historical package-cohort raw manifest/OptimizationJob recheck：最终 Dev.app cohort `ce45110e3a5e6eaa5b5283e61f430e2338c7f06a2d09f4e75d4a21cb924f6a86` 的隔离 raw stdio probe 以生成的 source manifest 做 `tools/list` exact-name equality，MCP/Runtime cohort exact-match，该 package cohort 工具面为 41 read + 33 opt-in write = 74（manifest canonical `7ced259e…c526b609`）；`OptimizationJob` 通过 39 evaluations（32 coarse + 4 mid + 3 final），Job/result=`succeeded/done`，proposal=`blocked-no-improvement`、`strict_improvement=false`，无 candidate confirm/version/export、无持久用户数据副作用。receipt：`docs/evidence/mcp010f/optimization-raw-manifest-dynamic-20260817-receipt-v2.json`。这是该历史 package cohort 的结构/IPC recheck，不改写 Stage 0 的 camera/binding 与 `QUALITY_TARGET_NOT_MET` 视觉真值。

2026-08-17 P2 typed surface-finish package：当前源码已安装为 Dev.app cohort `ce45110e3a5e6eaa5b5283e61f430e2338c7f06a2d09f4e75d4a21cb924f6a86`；package install 与隔离 probe 均 PASS，组件 cohort exact-match，Runtime Ready、`ponytail-preflight@0.1.0`、project create/readback 和 `persistent_user_data_touched=false` 均已观察。receipt：`docs/evidence/mcp010f/dev-app-install-parametric-surface-finish-20260817.json`、`docs/evidence/mcp010f/dev-app-probe-parametric-surface-finish-20260817.json`。这仍是 source/package/isolated transport 证据，live Desktop restart、真实 finish likeness/PBR、人评、export/restart 和 HQ_360 继续未运行或阻断。

2026-08-17 P2 typed surface-finish parameter slice：Runtime `RuntimeParameterPatch@1` 新增 `hard-surface-finish-v1`，Panel 的 `thickness_m/bevel_m` 与 SurfaceShell 的 `thickness_m` 进入单 Part bounded action；Panel 尺寸关系、混合族、歧义节点和数值边界均 fail closed。source Gate receipt：`docs/evidence/mcp010f/parametric-surface-finish-parameter-patch-source-gate-20260817.json`。该能力仍为 source-only，未产生新的真实 likeness/PBR、人评、package/live restart 或 HQ_360 证据。

2026-08-17 P2 camera/observation binding follow-up：`RenderSet@2.camera_object_sha256` 已由 Runtime 在 comparison/appearance RenderSet 中写入并回读；RepairIntent@1 的 Runtime 参数补丁及真实参考探针均带同一 durable observation hash。cohort `18c9fb86cafc7e7baf2356e2efe9db404e6530fdeae54d33c0a9beba94fbae40` packaged/real-reference isolation PASS，RepairIntentRun 已完成 render/compare/evaluate，仍因 `QUALITY_TARGET_NOT_MET` blocked；无 confirm/version/export、source candidate 未变。receipt：`docs/evidence/mcp010f/real-reference-repair-intent-run-observation-cas-20260817.json`。

2026-08-17 P2 RepairIntent run source Gate：`repair_intent_run_prepare` 已形成 CAS intent → exact binding → bounded `compile/readback/render/compare` 的 Runtime-owned、approval-gated source slice；返回 staged candidate，不改变 source candidate，不允许 confirm/version/export。receipt：`docs/evidence/mcp010f/repair-intent-run-source-gate-20260817.json`，source/contract/full Gate 与最终 packaged positive transport PASS；相机 CAS 与 observation hash 修复后，真实参考已完成 evaluate 后按 `QUALITY_TARGET_NOT_MET` blocked。它不等于通用 orchestrator、Repair 应用或视觉质量通过，live Desktop restart 仍 `NOT_RUN`。

2026-08-17 P2 package state：最新 Dev.app cohort `18c9fb86cafc7e7baf2356e2efe9db404e6530fdeae54d33c0a9beba94fbae40` 安装 PASS；真实授权参考双会话 `RepairIntentRun` 已完成至 evaluate 后按严格视觉门 blocked，Codex Desktop restart 尚未执行；旧 cohort `ad4837…` receipt 按历史范围保留。

2026-08-17 core HQ reference coverage contract Gate：`ReferenceCanvas@1` 现在对 `coverage_status=complete`/`hq_360_status=eligible` 强制要求 `front/back/left/right/rear-three-quarter` 五个 identity views；`perspective/top/material/detail` 只能作为补充，不能替代核心视图。Runtime、schema、Agentic contract negative fixture 与 focused tests 通过，receipt：`docs/evidence/mcp010f/core-hq-reference-coverage-contract-gate-20260817.json`。这只收紧声明边界，不产生真实多视图或 HQ_360 PASS。

2026-08-17 reference annotation readiness：Runtime 将自动 flood-fill 与用户确认标注分离；`user_confirmed=true` 只有在显式 contour、observed Part regions、至少 3 个 observed landmarks、observed view regions 和 canonical camera exact binding 同时满足时，才允许 `benchmark_eligibility=READY_PARTIAL_VIEW`。否则保持 `BLOCKED_USER_CONFIRMATION_REQUIRED`/`QUALITY_TARGET_NOT_MET`；该字段不是独立真人评审。source/contract/full MCP010F/release baseline 通过，receipt：`docs/evidence/mcp010f/reference-annotation-readiness-source-gate-20260817.json`。当前 Desktop live cohort、真实 likeness、PBR、人评、export/restart 和 360 仍未通过。

2026-08-17 reference annotation readiness package：对应 Dev.app 已安全重建并安装为 cohort `896105d3ab204babd415738bf66f572ea5c4be41df1ee83a26da3deed6bb42c7`，install/probe/Ready/preflight/project isolation PASS，旧包保留；Codex Desktop restart gate 仍 `NOT_RUN`，不能将隔离 package probe 写成 live authoring 或 likeness 证据。

2026-08-17 live mismatch after P1 package：当前 `capabilities_get` 仍回读旧 Runtime/MCP cohort `41945cc48f600abb1601e82ed11ec116b5f6c55a6066f6777268c2d540c705c7`/`3e6debdabdb56dff44e630918553dcfbaf9d2ae0503c23d144307eed33c53b34`，`build_cohort_match=false`，旧连接仍广告 writes；在用户安全重启 Codex Desktop 前，只允许诊断读操作。receipt：`docs/evidence/mcp010f/live-cohort-mismatch-after-annotation-package-20260817.json`。

2026-08-17 final-package isolated Primary Form trial：最终 `820a542fabe3503f0b8cf6c86e84f578e7b074d79d07401a9a3f865dc60709b4` 在同一用户授权 perspective/canonical camera 上完成 `pelvis → upper-arm-pair` 两步 Runtime-owned bounded repair；receipt `docs/evidence/mcp010f/isolated-final-cohort-primary-form-pelvis-upper-arm-20260817.json`（SHA `da3de253f56b484e1c8d9f7f3e98ee32a9c1d6860cd204e123a1a85db4373713`）。64 evaluations/step；第一步 accepted strict improvement，第二步 retained source/no improvement；无 confirm/version/export/持久用户数据。当前比较 IoU `0.661785435407`、Boundary F1 `0.249419639555`，视觉仍 `QUALITY_TARGET_NOT_MET`，该 receipt 不代表高质量或 360。

2026-08-17 final-package isolated real-reference observation：最终 `820a542fabe3503f0b8cf6c86e84f578e7b074d79d07401a9a3f865dc60709b4` 四件套在临时 Runtime 上完成真实授权 perspective 的 durable `ReferenceCanvas@1`/`DesignSpec@1`、candidate-bound `AgenticSceneObserveResult@1`、camera 与 `SilhouetteRig` readback；receipt `docs/evidence/mcp010f/isolated-final-cohort-observation-20260817.json`（SHA `68d156c662c717e762a1ee61a9a17995236c906a10f53a0ffd58035487ff0423`）为 `PASS_BOUNDARY_ONLY`，explicit authoring binding=`bound`，无持久用户数据/无无关副作用。它只证明最终 package 的隔离 transport；`quality_visual_status=NOT_RUN`、`QUALITY_TARGET_NOT_MET`、单视图 `HQ_360=BLOCKED_REFERENCE_COVERAGE` 保持不变，Desktop live restart 仍未完成。

2026-08-17 package alignment after capability-surface hardening：最终用户级 Dev.app 已按当前脏源码重建并安装，package cohort `820a542fabe3503f0b8cf6c86e84f578e7b074d79d07401a9a3f865dc60709b4`；install receipt `docs/evidence/mcp010f/dev-app-install-capability-tools-list-fail-closed-v2-20260817.json`、isolated probe `docs/evidence/mcp010f/dev-app-probe-capability-tools-list-fail-closed-v2-20260817.json` 均 PASS，MCP/Runtime/Geometry/Render cohort exact-match，mismatch transport probe 证实只暴露 41 个 read-only 工具，Ponytail preflight/Ready/project readback PASS。该 package Gate 不代表当前已有 Codex Desktop session 已重启，`codex_desktop_restart_gate=NOT_RUN`；此前 `41945…`/`9c537…`/`cea852…`/`d56a…` receipts 按其原 cohort 保留，不能借新 package 改写视觉质量。

2026-08-17 live cohort diagnostic：当前 Codex Desktop stdio 仍返回 `build_cohort_match=false`；已安装 package 为 `820a542fabe3503f0b8cf6c86e84f578e7b074d79d07401a9a3f865dc60709b4`，现场旧 Runtime 为 `41945cc48f600abb1601e82ed11ec116b5f6c55a6066f6777268c2d540c705c7`，live MCP 为 `3e6debdabdb56dff44e630918553dcfbaf9d2ae0503c23d144307eed33c53b34`。`skill_get ponytail-preflight@0.1.0` 可只读 PASS，但在 MCP 重启并重新绑定前禁止开始新的设计动作；新版 source/package 已把能力广告、`tools/list` 与 `tools/call` 一并 fail-closed。诊断 receipt：`docs/evidence/mcp010f/live-cohort-mismatch-capability-package-20260817.json`。

2026-08-17 per-view annotation lineage source truth：`ReferenceCanvas@1` 的 view 级字段已接入 `ReferenceViewSpec@1`、silhouette target/mask CAS 与 camera canonical hash；Runtime 在 authoring、view evaluation 和跨视图 proposal compare 前 fail-closed 校验 view ID、reference ID/SHA、target/mask、camera 与 evidence lineage，RepairIntent 的 view evaluation kind 集合必须精确等于 `coverage.supplied_views`，projection 同步暴露这些 hash。当前 source/package Gate 通过，但没有新的真实多视图授权 receipt；仍以 `QUALITY_TARGET_NOT_MET`、单视图 `HQ_360=BLOCKED_REFERENCE_COVERAGE`、PBR/人评/export/restart/VoiceOver 未运行为准。

2026-08-17 explicit-authoring action boundary：`session_create_or_resume` 的默认 ReferenceCanvas/DesignSpec 只用于观察和未知记录；`design_action_run_prepare`、`repair_apply_prepare` 与 `repair_apply_confirm` 在任何几何/比较动作前都要求 Runtime-owned `bindings.status=bound`，并逐个验证 supplied view 的 target/mask、`ReferenceViewSpec@1` canonical hash 与 observed/inferred camera hash。显式 authoring 的 positive action 与默认 fallback 的 fail-closed negative regression 均通过；阻断路径不编译、不比较、不写 candidate/version。该边界不等于 Repair 已通用执行或视觉质量通过。

2026-08-17 ReferenceCanvas binding truth：当前源码/安装包 cohort 为 `820a542fabe3503f0b8cf6c86e84f578e7b074d79d07401a9a3f865dc60709b4`；更早的真实 explicit-authoring receipt `docs/evidence/mcp010f/supplemental/real-codex-cli-current-20260817-reference-lineage-primitive.json` 仍保留其原 cohort `d56a…`。`ReferenceCanvas@1` 的 `bindings` 已纳入 canonical/schema/readback；Runtime 对 target reference、camera hash/canonical hash、evidence CAS 与 session/reference project 逐项校验，unbound 文档不能携带半绑定字段。authoring canonical replay 对整数/浮点等价 JSON 做兼容归一化。source contracts、Agentic fixtures、最新 package/probe 通过，显式 authoring positive action 与默认 fallback fail-closed regression 通过；该 receipt/测试只证明 lineage/transport，不升级视觉质量。该 lineage slice 当时为 144 Schema、41 read + 33 opt-in write = 74 tools；当前计数以顶部 Stage 0 机器真值为准；质量仍 `QUALITY_TARGET_NOT_MET`，Stage 0 历史 `camera=MISMATCH`/`BLOCKED_INCOMPLETE_BINDING`、PBR、人评、export/restart/360 不变。

2026-08-17 exact authored coverage boundary：Runtime 现在要求每个 `coverage.supplied_views` kind 恰好对应一个 authored view card，并拒绝重复 kind、未声明的 extra kind 与缺失 kind；focused Runtime regression、MCP010F full source Gate、Dev.app cohort `820a542fabe3503f0b8cf6c86e84f578e7b074d79d07401a9a3f865dc60709b4` install/probe 均 PASS。receipt `docs/evidence/mcp010f/reference-coverage-exact-binding-capability-package-20260817.json` 只证明覆盖实体边界，不是真实多视图、likeness 或 HQ_360 PASS；真实授权 front/back/left/right/rear-three-quarter 仍 `NOT_RUN`。

2026-08-17 Viewer boundary source truth：Viewer 前端的 Runtime IPC command set 已由 source Gate 固定为 viewer_read_model、viewer_read_model_summary、viewer_artifact_bytes、viewer_reference_bytes、viewer_render_pass、viewer_visual_evidence、viewer_agentic_projection、viewer_agentic_session 八个只读命令；Tauri registration 与 allowlist exact-match。对比快照仅是本地 transient Canvas 下载，不能进入 Runtime export/candidate/version 写入链路。root package 新增显式 test-only Worker features，Viewer IPC/source tests 在 test-geometry-worker-fallback 下 6/6 通过；生产构建不启用 fallback。formal packaged UI E2E、artifact export、VoiceOver、人评、PBR、restart hash、360 仍未运行或阻断。

2026-08-17 typed pose-control source truth：`SilhouetteRig@1` 与 `SilhouetteRigHashRequest@1` 已扩展 `rotation_x`/`rotation_y`/`rotation_z`（单位 `radian`，范围 ±2π）；Runtime 的 materializer 将它们作为最终 `forgecad.geometry.transform@2` 的受限输出旋转，保留源几何、镜像与 Part lineage 不变，Primary Form action 同样通过 typed namespace 接入。错误单位/越界/非有限输入 fail closed；Runtime/MCP focused tests 与 `script/test_mcp010f.sh` source Gate PASS。默认 36-control detail Rig 和 40-geometry schedule 未改变，姿态控制尚未进入默认自动搜索；无新的真实视觉质量证据，仍 `QUALITY_TARGET_NOT_MET`，Stage 0 历史 `camera=MISMATCH`/`BLOCKED_INCOMPLETE_BINDING` 与 PBR、人评、export/restart、360 未运行或阻断状态保持不变。

2026-08-16 current-source Part namespace/pelvis correction：`probe_mcp010f_part_correction.py` 之前只接受胸壳、肩甲和小腿四类 Part，canonical observation 选出的最大边界误差 `pelvis` 因无 namespace 被错误阻断；现已显式注册 detail fixture 的全部语义 Part namespace。新 cohort `125df766029d3568c4a5724838ea342a18f29651566b625758cc6188d25059b6` 的 v2 receipt `docs/evidence/mcp010f/supplemental/part-correction-camera-binding-fix-pelvis-v2-20260816.json` 通过 64 evaluations、strict accepted 与 canonical camera exact-match，baseline→proposal IoU `0.741047132807 → 0.745160673820`、Boundary F1 `0.328765122610 → 0.334696163978`；candidate 仅 staged、视觉仍 `QUALITY_TARGET_NOT_MET`，无 confirm/version/export，persistent side effect=false。旧 blocker receipt 保留为负向诊断，不改写 Stage 0 历史质量与 incomplete-binding truth。

2026-08-16 current-source hip-pair trial：同一 namespace/camera-fix cohort 的 `hip-pair` automatic repair receipt `docs/evidence/mcp010f/supplemental/part-correction-camera-binding-fix-hip-20260816.json` 通过 64 evaluations、strict accepted 与 canonical camera exact-match，IoU `0.741047132807 → 0.742876384452`、Boundary F1 `0.328765122610 → 0.330228088626`；candidate staged、visual `QUALITY_TARGET_NOT_MET`、无 confirm/version/export。该 trial 与 pelvis/chest 隔离，不改写 retained best-known truth。

2026-08-16 current-source upper-arm-pair trial：同 cohort 的 `upper-arm-pair` automatic repair 通过 64 evaluations、strict accepted 与 canonical camera exact-match，但 IoU `0.741047132807 → 0.741042819691` 轻微回退，Boundary F1 `0.328765122610 → 0.330420176973`；receipt `docs/evidence/mcp010f/supplemental/part-correction-camera-binding-fix-upper-arm-20260816.json`，candidate staged、visual `QUALITY_TARGET_NOT_MET`、无 confirm/version/export，未晋升为 best-known。

2026-08-16 current-source shoulder-armor-pair trial：同 cohort 的 `shoulder-armor-pair` automatic repair 通过 64 evaluations、strict accepted 与 canonical camera exact-match，IoU `0.741047132807 → 0.744097146194`、Boundary F1 `0.328765122610 → 0.338944948938`；receipt `docs/evidence/mcp010f/supplemental/part-correction-camera-binding-fix-shoulder-armor-20260816.json`，candidate staged、visual `QUALITY_TARGET_NOT_MET`、无 confirm/version/export，未晋升为 confirmed candidate。

2026-08-16 current-source camera binding repair evidence：新 cohort `125df766029d3568c4a5724838ea342a18f29651566b625758cc6188d25059b6` 的 isolated `chest-shell` Runtime repair 已将 `PrimaryFormAcceptance@1.camera_hash`、candidate-bound RenderSet/VisualEvidence 与 comparison 全部绑定到 canonical observation camera `2cd35f435839f80e364a15a6930ffe37c2145925d6a5b455ccffd30873953736`；fit result 的 exploratory camera `04e27c1b…58ad0a` 仍仅作拟合证据。receipt `docs/evidence/mcp010f/supplemental/part-correction-camera-binding-fix-20260816.json` 为 `PASS_TRANSPORT_WITH_METRICS`、64 evaluations、strict accepted；最终 compare IoU `0.748146396048`、Boundary F1 `0.350344578024`，仍 `QUALITY_TARGET_NOT_MET`，candidate 为 staged、未 confirm/version/export。该 action-level receipt 只证明新源码边界修复，不改写 Stage 0 provisional observation 的历史 `BT006_CAMERA_BINDING=FAIL`/`camera=MISMATCH`/`BLOCKED_INCOMPLETE_BINDING`；同 cohort full Codex replay、正式 Viewer/VoiceOver、人评、PBR、export/restart、360 仍未运行或阻断。

2026-08-16 latest authoritative current-source real Codex ReferenceCanvas/Observation/Primary Form truth：MCP/Runtime/Geometry Worker/Render Worker 四个可执行文件的 cohort 均为 `d52261ffdf44e07a2a4762b16c80f433a824080f0530fad64bbd32a153eb9c04`。真实用户授权 PNG 的 durable `ReferenceCanvas@1`/`DesignSpec@1`、candidate-bound `AgenticSceneObserveResult@1` 与 `SilhouetteRig` 已完成 readback，session observation hash 与 scene observation hash exact-match；receipt 为 `docs/evidence/mcp010f/supplemental/real-codex-cli-current-20260816-primary-form-multiview-capable-v1.json`，`status=PASS_BOUNDARY_ONLY`、最终 candidate `candidate-7f6e16ddf8934699aa5594942fc2a7ba`、4704 triangles。随后同一 lineage 的 `pelvis → upper-arm-pair` 两步 Runtime-owned Primary Form repair 均 `accepted + strict_improvement`、64 evaluations/step，proposal loss 分别 `0.463981471487 → 0.460480624038`、`0.460480624038 → 0.458125428229`；最终 boundary-only compare 为 IoU `0.752731669182`、Boundary F1 `0.355021780326`、bbox edge `0.00390625`、centroid `0.001753436389`。`silhouette_gate=QUALITY_TARGET_NOT_MET`、`quality_visual_status=NOT_RUN`、无 AOV/review/confirm/version/export/PBR/人评证据；只有 `perspective` 视图，缺失 `front/back/left/right/rear-three-quarter`，所以 `hq_360=BLOCKED_REFERENCE_COVERAGE`，不能升级为 likeness/HQ PASS。

2026-08-16 authoritative supplemental Primary Form convergence truth：同一 current-source cohort 的 `chest-shell → shoulder-armor-pair` retry receipt 为 `docs/evidence/mcp010f/supplemental/real-codex-cli-current-20260816-primary-form-chest-shoulder-v3-retry.json`，最终 candidate `candidate-458cf0dc72fa4c5a8f9c64130b5318ab`。两步均由 Runtime-owned async Job 完成 64 evaluations、strict accepted；Geometry/Render Worker、9 AOV、typed visual review/quality readback 实际完成。最终 IoU `0.754862679391`、Boundary F1 `0.369927879976`、bbox edge `0.00390625`、centroid `0.002345583377`，是当前单视图 supplemental comparison 的 best-known 指标，但仍 `PASS_WITH_QUALITY_TARGET_NOT_MET`、`quality_hard_gate_passed=false`、review=`needs_revision`、visual claim 未声明，未 confirm/version/export。该 supplemental 不替换 durable ReferenceCanvas/Observation 的主 receipt，不解除缺少 `front/back/left/right/rear-three-quarter` 的 `BLOCKED_REFERENCE_COVERAGE`。

下方同日旧 cohort 条目作为历史快照保留，最新状态以上述 `d52261ff…` receipt 为准。

2026-08-16 historical cohort replay（superseded）：current real Codex ReferenceCanvas/Observation/Primary Form truth：当前源码 cohort `70badfd9d4e07a374aee994ad42604980ccf77e3f572b6d2926b46eccca6f72a` 的真实 Codex boundary probe 已返回 `PASS_BOUNDARY_ONLY`。Runtime 默认 authoring 产生并持久化 `ReferenceCanvas@1`/`DesignSpec@1`，`AgenticSessionResult@1` 的 observation hash 与同 candidate 的 `AgenticSceneObserveResult@1.canonical_sha256` 精确一致；receipt 为 `docs/evidence/mcp010f/supplemental/real-codex-cli-current-20260816-reference-canvas-observation-pass-v3.json`。同一 final candidate 上，Runtime-owned `pelvis → upper-arm-pair` 两步 Primary Form repair 均为 `accepted + strict_improvement`、64 evaluations/step；最终 boundary-only compare 为 IoU `0.752731669182`、Boundary F1 `0.355021780326`。该历史 cohort 不覆盖最新 `d52261ff…` receipt，也不改变视觉仍 `QUALITY_TARGET_NOT_MET` 的结论。

2026-08-16 current reference coverage truth：本轮只有用户授权的 `perspective` 单视图，缺少 `front/back/left/right/rear-three-quarter`；因此 `hq_360=BLOCKED_REFERENCE_COVERAGE`。authoring mode 是 `RUNTIME_DEFAULT_AUTHORING`，不是显式 Codex authoring PASS；一次 explicit payload drift 的负向诊断保留在 `docs/evidence/mcp010f/supplemental/real-codex-cli-current-20260816-reference-canvas-observation-primitive-split-v6.json`。该负向证据与当前成功的 Runtime default fallback 都没有触碰持久用户数据或候选确认。

2026-08-16 Agentic authoring context 状态：真实 Codex visual probe 的源码编排已把用户授权参考和同相机 comparison 固定为一次 Runtime-owned `ReferenceCanvas@1`/`DesignSpec@1` authoring context；`session_create_or_resume` 返回 durable documents 后，CLI 只接受与同一 `AgenticSceneObserveResult@1.canonical_sha256` 相等的 observation。合同/Runtime/MCP/source full Gate 通过。真实 workspace-write probe 因 Codex host 超时未返回 typed authoring receipt，先前 read-only 调试回合则在 `project_create` 被取消；两者均未触碰候选确认、版本、导出或持久用户数据。故 authoring producer 的 source truth 为 PASS，live transport 为 `BLOCKED/NOT_RUN`，视觉仍为 `QUALITY_TARGET_NOT_MET`，Stage 0 `camera=MISMATCH`/`BLOCKED_INCOMPLETE_BINDING` 与 PBR、人评、export/restart、360 未运行或阻断状态不变。

2026-08-16 latest real-reference state：当前源码 cohort 的真实机器人参考 `OptimizationJob` 已完成 39 次 `128/256/512` 多保真执行；统一 camera identity hash 为 `1faad5d08ba0d9d3b7dfb7d28c392d4cf7a23a7757d212d4d3c6c9c9a6adb655`，optimization/compare camera exact-match。`surface_control_point` materializer 现在实际写入 hash-bound `surface-shell@1.control_points` 的多个 index/axis，surface signal 为 `ready`；同一 receipt 的 Boolean backend 为 product-owned Manifold C ABI，candidate indices `[1..9]`。receipt `docs/evidence/mcp010f/supplemental/optimization-job-real-reference-20260816-unified-objective-manifold-surface.json` 结构与执行状态 `PASS`，但 `comparison_status=QUALITY_TARGET_NOT_MET`、`strict_improvement=false`、`proposal_status=blocked-no-improvement`、candidate_confirmed=false、version_count=0。该 supplemental truth 不能升级 likeness/HQ，也不覆盖 Stage 0 历史 `camera=MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`、人评/PBR/export-restart/360 未运行或阻断。

2026-08-16 Viewer read-model state：同 cohort Viewer 从隔离 Runtime 读回 1 个 project、1 个 candidate、0 个 version，并以 exact candidate/artifact/reference/RenderSet/comparison/QualityReport hash 绑定返回；`quality_visual_status=QUALITY_TARGET_NOT_MET`、`quality_hard_gate_passed=false`，Viewer 为 read-only。receipt `docs/evidence/mcp010f/supplemental/viewer-read-model-real-reference-20260816.json` 已由 current-quality ledger 校验；formal UI E2E、VoiceOver、人评、export/restart 与 360 仍未运行或阻断。

2026-08-16 Manifold Boolean current state：真实 vendored Manifold C ABI/FFI 已在隔离 Geometry Worker 中执行 bounded same-Part `union`、`difference`、`intersection`；MCP010D raw receipt 为 `16 entries / 16 active`、9 semantic Parts、588 triangles，Boolean lineage/tangent focused tests 通过。该状态仅表示 current source/transport/readback PASS；任意 mesh Boolean、视觉/PBR likeness、export/restart、真人和 360 仍未通过，旧历史收据的 intersection deferred 文案不覆盖它。

2026-08-16 CADFit OptimizationJob authoritative correction：真实 Runtime IPC 现在实际分发 `optimization_job_prepare/get/resume`，Render Worker fit batch 与 `OptimizationIntent@1` 的 `128/256/512` fidelity 对齐。隔离 raw receipt `docs/evidence/mcp010f/supplemental/optimization-job-raw-20260816-mid-resolution.json` 证明 39 次 bounded evaluation、39 个 checkpoint object、`next_stage=done`、`job/result=succeeded` 与 final baseline control；`proposal_status=blocked-no-improvement`、`strict_improvement=false`，因此没有 staged proposal、candidate confirmation、version 或 export。该 receipt 是合成 1px reference 的 transport/structural evidence，不得升级真实机器人视觉质量；Stage 0 的 `QUALITY_TARGET_NOT_MET`、`camera=MISMATCH`、`BLOCKED_INCOMPLETE_BINDING` 及 human/PBR/export-restart/360 状态保持不变。

2026-08-16 Primary Form async Job real Codex truth：当前源码 cohort `4661b8d94fc2b72819cb09b1503ea5112a328a40edec7b227a8814b3194f6497` 的真实 Codex 已实际完成 `primary_form_repair_job_prepare → job_get` 轮询 → `job_result_get`；Runtime-owned 64 evaluations、Geometry/Render Worker、strict readback 与 same-camera acceptance 通过，source/proposal loss `0.465026451428 → 0.447063964053`，camera binding `PASS_SILHOUETTE_FIT_TO_COMPARE`，准备 staged candidate。最终 IoU `0.756928086147`、Boundary F1 `0.392553090172`，仍 `QUALITY_TARGET_NOT_MET`、hard gate=false、review=`needs_revision`；未 confirm/version/export，`persistent_user_data_touched=false`、无无关副作用，九 AOV 调用完成。receipt `docs/evidence/mcp010f/supplemental/real-codex-cli-current-20260816-primary-form-async-job.json` 仅是 supplemental current-cohort transport/convergence truth，不替换 Stage 0 历史 `camera=MISMATCH`/`BLOCKED_INCOMPLETE_BINDING` benchmark，也不构成 HQ PASS。

2026-08-16 Appearance V2 camera identity truth：Runtime `prepare_appearance_candidate_v2` 不再把完整 CameraCalibration 对象哈希写入 `RenderSet@2.camera_hash`，而是通过 `camera_identity_hash` 校验 `CameraCalibration@1` 的 identity hash；canonical receipt hash 仍独立绑定完整相机回执。回归测试、source-built MCP010E raw stdio（AppearanceProgram@2、ArtifactReadback@2 hard gate、9 AOV、3 Parts、3 material zones、embedded texture URI=0）和 MCP010F full Gate 通过。该修复只收口 RenderSet/CameraCalibration evidence semantics；Stage 0 历史 provisional cohort 仍明确 `BT006_CAMERA_BINDING=FAIL`、`camera=MISMATCH`、`QUALITY_TARGET_NOT_MET`、`BLOCKED_INCOMPLETE_BINDING`，没有重写历史 receipt、没有 confirm/version/export，人评/PBR/export-restart/360 仍未运行或阻断。下一步是 current-source cohort 重建/重绑与真实 ReferenceCanvas→Primary Form receipt，不是直接推进视觉 PASS 或 Physical AI。

2026-08-15 Primary Form 512px fit raster alignment truth：提交 `220d6f0a` 将 Render Core 的 Primary Form 512px transient fit 改为与正式 512px compare 共用 1024 sample grid，128px exploratory path 仍保留半分辨率；Geometry Worker regression 已证明同一 camera 下 fit 的 `silhouette`/`part-id` PNG 与 formal renderer exact-match。clean Dev.app 安装 receipt `docs/evidence/mcp010f/supplemental/primary-form-fit-raster-alignment-dev-app-install-20260815.json` 的 source worktree clean，Runtime/MCP/Geometry Worker/Render Worker cohort 为 `3e6debd…c53b34`；真实复放 `docs/evidence/mcp010f/supplemental/real-codex-cli-current-20260815-primary-form-fit-raster-alignment.json` 完成 2 steps、每步 64 evaluations、均 `accepted + strict_improvement`，但 fit metrics（IoU `0.752731669182`、Boundary F1 `0.355021780326`）与顶层 final compare（IoU `0.745042364012`、Boundary F1 `0.334537343601`）仍不同，因为 receipt 明确 `silhouette_fit_camera_hash=27d180…c10c0c`、最终 `silhouette_camera_hash=8cd206…c84a535` 且 `canonical_compare_in_silhouette=false`；这暴露的是后续 camera binding 差异，不是把 renderer 采样对齐误写成质量通过。权威状态继续为 `QUALITY_TARGET_NOT_MET`、`hard_gate=false`、`visual_review=needs_revision`、PBR/人评/export-restart 未运行、360 `BLOCKED_REFERENCE_COVERAGE`，未 confirm/version/export；live MCP 仍待安全重绑。

2026-08-15 Primary Form canonical ReferenceViewSpec binding truth：`silhouette_fit_prepare` 与 `primary_form_repair_prepare` 现在可接收并校验 hash-bound `ReferenceViewSpec@1`；Runtime 的 transient Primary Form ranking 与最终 staged same-camera compare 使用同一 landmarks/regions 证据，避免 Codex 观察已经包含可见区域而内部搜索仍只看 landmarks。未提供 view spec 的旧 fixture/调用继续走 landmark-only compatibility fallback。提交 `1788f242` 后 clean Dev.app 安装回执 `docs/evidence/mcp010f/supplemental/primary-form-view-spec-dev-app-install-20260815.json` 确认 source revision/cohort `4c12eac9…edd5b`；真实复放 `docs/evidence/mcp010f/supplemental/real-codex-cli-current-20260815-primary-form-view-spec.json` 携带 15 landmarks、8 regions、`view_spec_sha256=79bbb0d8…8b65`，两步 Primary Form 均 `accepted + strict_improvement`、64 evaluations，最终 compare IoU `0.745151573065`、Boundary F1 `0.334457989922`，仍 `QUALITY_TARGET_NOT_MET`、`hard_gate=false`、`visual_review=needs_revision`。该 isolated receipt 的 camera fit→compare 为 PASS，但 Stage 0 冻结 benchmark 的 `camera=MISMATCH`、`BLOCKED_INCOMPLETE_BINDING` 仍不变；未 confirm/version/export，PBR/人评未运行，360 为 `BLOCKED_REFERENCE_COVERAGE`。

2026-08-15 Primary Form serial composition truth：修复真实 Codex retry 场景下以 compact call-id receipt 判断阶段顺序的误阻断；阶段校验现在消费 raw completed ForgeCAD MCP events，压缩调用只用于审计。新的同 cohort `78d03f2b…1a808` direct-MCP receipt `docs/evidence/mcp010f/supplemental/real-codex-cli-current-20260815-primary-form-sequence-pelvis-upper-arm.json` 完成 `pelvis → upper-arm-pair` 两个 Runtime-owned bounded repair steps：2/2 accepted，64 evaluations/step，`ForgeCADPrimaryFormCompositionLineage@1` 的 candidate/observation/target/camera/Rig/intent binding exact。最终 candidate 为 `candidate-65de4eba7341405fa448dafb24dbca84`，`silhouette_iou=0.751411042945`、`boundary_f1_4px=0.34965309767`、`bbox_edge_error=0.00390625`、`centroid_error=0.001862755047`，权威视觉状态仍 `QUALITY_TARGET_NOT_MET`、`quality_hard_gate_passed=false`、`visual_review=needs_revision`；九 AOV/typed review 传输完成，但 PBR/人评未运行、未 confirm/version/export、360 为 `BLOCKED_REFERENCE_COVERAGE`。该 receipt 只升级 serial composition/Runtime/Worker evidence，不升级 likeness、Stage 0 benchmark 或 Desktop live MCP。

2026-08-15 real reference replay on clean package：使用仍在 CAS 中的用户授权 PNG，通过 Dev.app 同 cohort `78d03f2b…1a808` 的隔离 MCP/Runtime/Geometry Worker/Render Worker 完成 detail + silhouette-first + Runtime-owned `primary_form_repair_prepare`。receipt `docs/evidence/mcp010f/supplemental/real-codex-cli-current-20260815-primary-form-runtime-replay.json`：26 Parts、4704 triangles、64 次 bounded evaluation，source candidate `candidate-1f6344e7f40040faa941fe544388226a` → staged candidate `candidate-696b85e3402f44fab22c771bdaf766b0`，Primary Form acceptance strict improvement；最终 `boundary_f1_4px=0.414236535223`、`silhouette_iou=0.74637831219`、`bbox_edge_error=0.03515625`、`centroid_error=0.007345230528`，`quality_visual_status=QUALITY_TARGET_NOT_MET`、`quality_hard_gate_passed=false`、`visual_review=needs_revision`。该回放未触碰持久用户数据，未 confirm/version/export，PBR 为 `NOT_RUN`，360 为 `BLOCKED_REFERENCE_COVERAGE`；它是当前 packaged direct-MCP 视觉证据，不等于 Desktop live MCP 已重绑，也不替换 Stage 0 provisional benchmark。

2026-08-15 Agentic durable design-document readback truth：Runtime session readback 现在从 session 保存的 CAS hashes 重新读取 `ReferenceCanvas@1` 与 `DesignSpec@1`，验证各自 canonical hash、schema、project/reference/canvas binding 后再返回；不会返回原图字节、路径或由 Codex 重建的替代文档。该模块只收口 durable observation context，不改变 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING` 或 live MCP `BLOCKED_RESTART_REQUIRED`。

2026-08-15 clean package truth：提交 `cf329215` 后 Dev.app 已按干净源码重建并安装，四个 packaged executable exact cohort 为 `aa0d6dcb…5808e`，`source_worktree_dirty=false`；ad-hoc deep-strict、隔离 Runtime Ready/project_create/preflight 与 packaged MCP010B raw stdio structural Gate PASS。该 package truth 不等于当前 Codex live MCP 已重绑，也不产生新的 candidate/RenderSet/视觉 likeness 结论；live session 仍需重启，权威质量继续为 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`。

2026-08-15 Primary Form adaptive local line-search truth：当前 36-control detail Rig 的失败路径不再把 `40` 个 geometry evaluation 全部消耗在 `joint + 2 backtracks + 1 group + 36 scalar` 的单向 schedule 上。Runtime 根据参数数量和硬预算安排大 Rig 的 `joint + 0.5 whole-proposal backtrack + 0.5/1.0 dominant-Part coupled recovery + 36/36 scalar coverage`；小 Rig 保留旧的 `0.5/0.25` whole-proposal backtrack 与单 coupled recovery。focused Runtime `22 passed`，证据 `primary-form-adaptive-line-search-20260815.json`；这只是 bounded convergence source Gate，不是视觉 likeness PASS，权威质量仍 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`，人评/PBR/export-restart/360 未运行或阻断。

2026-08-15 current-source package truth：提交 `a153dc02` 已重建并安装同源码 Dev.app，MCP/Runtime/Geometry Worker/Render Worker exact cohort 为 `04bbb4d6…ce81f3`；ad-hoc deep-strict、隔离 Runtime Ready/project/preflight 与 packaged MCP010B raw stdio structural Gate PASS，Skill registry 以 checked-in registry 精确为 12 项，工具面精确为 37 read + 24 opt-in write = 61，GeometryProgram@2 strict readback/candidate binding/负向 Part sink PASS，source/package manifest hash exact-match。现有 Codex Desktop MCP session 仍绑定旧 `7f9e4c…ee518`，故 `live_session_rebind=BLOCKED_RESTART_REQUIRED`；本次没有参考字节、candidate、RenderSet 或新的视觉质量结论。权威质量继续为 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`，人评/PBR/export-restart/360 未运行或阻断。

2026-08-15 Primary Form recovery coverage truth：当 joint evidence proposal 未严格改善时，Runtime 只保留 boundary-dominant Part 的一个 coupled recovery group，把剩余 geometry budget 留给 detail Rig 的全 36 个 scalar controls；64 上限仍为 `40 geometry + 15 initial camera + 9 winner refit`，没有扩大预算或把搜索交回 Codex。recovery receipt、focused Runtime/Agentic tests 与 MCP010F full Gate 通过；当前源码 hash 为 `531783a0…1ae53b`。该修复不产生新的真实视觉 receipt，package/live MCP 尚未重建到此源码，权威质量继续为 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`，人评/PBR/export-restart/360 未运行或阻断。

2026-08-15 live/package cohort truth：当前 `abae43f3` 源码已重建并安装用户级 Dev.app，四个 packaged executable 的 cohort 均为 `5a1f108a…e2dd2f`；ad-hoc deep-strict、资源 allowlist、隔离 Runtime Ready/project/preflight probe 通过，未触碰持久用户数据。新包 37 read + 24 opt-in write 的 manifest 与当前 source surface 一致。当前 Codex MCP 连接仍返回旧 `7f9e4c…ee518` cohort、旧 write surface 与旧 manifest `05fca3…d4d0a`，所以 live session rebind 仍 `BLOCKED_RESTART_REQUIRED`；该事实不改变当前 candidate/benchmark truth，也不产生视觉质量证据。权威质量继续为 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`。

2026-08-15 Primary Form resolution-consistent fit truth：Runtime-owned `silhouette_fit_prepare` 的 Primary Form camera/geometry search 现在使用 512×512 fit batch，并与最终 same-camera acceptance 对齐；只有普通 camera 粗搜保留 128×128 accelerator。此处只修复 objective/acceptance 分辨率漂移，未产生新的真实视觉 receipt或质量升级；`QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`、人评/PBR/export-restart/360 状态保持不变。

2026-08-15 Viewer Agentic evidence binding truth：Viewer 的 Agentic projection 只有在当前 visual evidence 已绑定时，才允许五个 Runtime evidence hash 全部 exact-match 后显示 ready；缺失或漂移返回 unavailable，不从 comparison metrics 或候选结构字段补造质量门。无视觉证据时仍可显示 unknown 的结构性 projection。该修复通过 Viewer source/Node/desktop build/MCP010F Gate，不新增视觉 receipt；真实状态仍 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`，人评/PBR/export-restart/360 未运行或阻断。

2026-08-15 Render Worker test-boundary truth：Runtime 的 `render-core` fallback 现在只在显式 `test-render-worker-fallback` feature 下编译；普通 `cfg(test)` 不再把进程内 renderer 当作 Worker 缺失时的隐式成功路径。无 feature 的 Runtime product check、source ownership checker、显式 fallback Runtime/MCP tests 与 MCP010C/F 聚合 Gate 通过。该状态只修正证据与测试边界，不改变真实视觉真值：`QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`，人评/PBR/export-restart/360 未运行或阻断；same-cohort packaged Worker receipt 仍是独立证据。

2026-08-15 Agentic canonical observation transport truth：MCP `InProcess` adapter 已修复 stage/critic/visual-evidence follow-up 的分发落点，三者现在都消费调用方提供的同一 `AgenticSceneObserveResult@1.canonical_sha256`；缺失或 stale hash 不再回退到未绑定观察。MCP 全量 56 tests 通过；该修复不新增 Runtime/CAS 数据、不改变 Schema/tool count，也没有新的视觉 receipt。权威质量仍 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`，人评/PBR/export-restart/360 未运行或阻断。

2026-08-15 Primary Form output-level offset sink truth：Runtime 对完整 Part output graph 的 camera-plane `offset_x/offset_y/offset_z/scale` 使用 Runtime-owned `forgecad.geometry.transform@2` sink；mirror/array 之前的源几何只承载局部尺寸控制。该修复防止 bilateral Part 的整体位移被镜像拓扑解释为左右张开/收拢，focused materialization regressions 与 Stage 0 source-truth freshness 通过。没有新的真实视觉 receipt，`QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`、人评/PBR/export-restart/360 状态不变。

2026-08-15 Primary Form evaluation ledger truth：Runtime 将 64 evaluation ceiling 对齐为 `40 geometry + 15 initial camera + 9 winner refit`，并对每个 refit camera row 做执行级计数；proposal 被 strict same-camera gate 拒绝时仍会计入，不再把 accepted rows 当作 evaluations。该修复仅影响 bounded search 的预算/receipt 一致性，没有新的真实视觉证据或质量升级；`QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`、人评/PBR/export-restart/360 状态保持不变。

2026-08-15 Primary Form profile-loft height truth：Runtime 的 typed Rig `height` 对 `forgecad.geometry.profile-loft@1` 现在只缩放 `profiles[*].height_m` 的纵向跨度，并以首站为稳定锚点；二维 profile points 的局部深度不再被误写。focused/full Runtime 与 MCP010F Gate 通过，Stage 0 Runtime source hash 已同步；没有新的真实视觉 receipt，权威状态仍为 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`，未解锁确认、PBR、人评、export/restart 或 360。

2026-08-15 Durable Agentic observation lineage truth：Runtime 在 durable session/checkpoint/action 的观察声明上增加 candidate/reference lineage 等值校验：观察必须同时绑定当前 candidate ID/hash、reference ID/object hash/canonical hash；不完整或状态漂移立即 fail closed。该校验只保护现有 Runtime-owned durable prepare/readback，不把 durable slice 扩写成完整 orchestrator 或 Repair execution；无新的视觉 receipt，`QUALITY_TARGET_NOT_MET`、`camera=MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`、人评/PBR/export-restart/360 状态不变。

2026-08-15 Agentic observation cache truth：Runtime 现在在进程内按 `AgenticSceneObserveResult@1.canonical_sha256` 缓存一次完整只读观察；bound plan/critic/visual-evidence/action follow-up 优先读取同一对象，不再在正常同一 Runtime 会话中重新拆分推导。缓存不是 SQLite/CAS 用户数据；Runtime 重启后仍允许一次重新构建，但必须通过请求 hash、project/candidate scope 与 canonical hash 校验。新增 cache/ambiguity regression 和 bounded action regression 通过；没有新的视觉 receipt，`QUALITY_TARGET_NOT_MET`、`camera=MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`、人评/PBR/export-restart/360 状态不变。

2026-08-15 Primary Form composition-lineage truth：`ForgeCADPrimaryFormCompositionLineage@1` 是 CLI 层的 hash-bound orchestration projection；每一步必须消费同一 current candidate 的 consolidated observation，完成第二步起先校验 prefix lineage 再推进 candidate，并且只允许 Runtime 返回 `prepared` 且严格改善时推进 staged candidate。raw events 仅保留 transport audit，projection 不写 Runtime/CAS、不产生 version/confirm/export，也不替代 Runtime-owned QualityReport。当前无授权 PNG，真实 composition 未运行；`QUALITY_TARGET_NOT_MET`、`camera=MISMATCH`、`BLOCKED_INCOMPLETE_BINDING` 仍为权威状态。

2026-08-15 Modular Primary Form continuation truth：当前源码的 `--part-contour-sequence` 只允许 2–3 个 exact Part，按 current candidate 重新绑定 consolidated observation、camera、baseline compare 与 Runtime-owned Rig 后执行一次 `primary_form_repair_prepare`；只有严格接受的 staged candidate 才会成为下一步 source，未改善结果保持原 candidate/camera。Runtime continuation regression、full Runtime `117 passed / 0 failed / 12 ignored` 与 MCP010C Gate 通过；raw stdio probe 的工具计数来自 `ForgeCADMcpToolManifestSummary@1`，不再硬编码。Stage 0 Runtime source hash 已同步且 checker 通过；该同步不改变 `camera=MISMATCH`、`QUALITY_TARGET_NOT_MET` 或 Viewer Runtime-only QualityReport authority。没有新的授权参考视觉 receipt，真实组合为 `NOT_RUN/BLOCKED_REFERENCE_BYTES_UNAVAILABLE`，未 confirm/version/export。

2026-08-15 Hip-pair staged candidate state：r21 `docs/evidence/mcp010f/supplemental/real-codex-cli-current-20260815-canonical-observation-hip-pair-r21.json` 在 cohort `726153a3…42ab5c` 通过 Runtime-owned 60-evaluation fit 与 `PASS_SILHOUETTE_FIT_TO_COMPARE`；source/proposal loss `0.426350916959`/`0.426265642648`，生成 staged `candidate-2dc5728da7124288b6ff79a0fb6b4f00`。同一 candidate 的最终 visual status 仍 `QUALITY_TARGET_NOT_MET`（IoU `0.743473474034`、Boundary F1 `0.301145366407`、bbox `0.0234375`、centroid `0.021831234764`，hard gate false），未 confirm/version/export。r19/r20/r21 相互独立，不能写成已合并模型；Stage 0 truth、Viewer Runtime-only authority、human/PBR/export-restart/360 状态不变。

2026-08-15 Chest-shell staged candidate state：r20 `docs/evidence/mcp010f/supplemental/real-codex-cli-current-20260815-canonical-observation-chest-shell-r20.json` 在 cohort `726153a3…42ab5c` 通过 Runtime-owned 61-evaluation fit 与 `PASS_SILHOUETTE_FIT_TO_COMPARE`；source/proposal loss `0.426350916959`/`0.416406210214`，生成 staged `candidate-d9d8cb24b1c5475d85b773c3ceb032c7`。同一 candidate 的最终 visual status 仍 `QUALITY_TARGET_NOT_MET`（IoU `0.739507057324`、Boundary F1 `0.314765400977`、bbox `0.013671875`、centroid `0.020515796935`，hard gate false），未 confirm/version/export。Stage 0 truth、Viewer Runtime-only authority、human/PBR/export-restart/360 状态不变；staged candidate 不等于 immutable version。

2026-08-15 Upper-arm-right staged candidate state：r19 `docs/evidence/mcp010f/supplemental/real-codex-cli-current-20260815-canonical-observation-upper-arm-right-r19.json` 在 cohort `726153a3…42ab5c` 通过 Runtime-owned 63-evaluation fit 与 `PASS_SILHOUETTE_FIT_TO_COMPARE`；source/proposal loss `0.426350916959`/`0.423630597587`，生成 staged `candidate-f1123cb13b624cb69493f825398b4423`。同一 candidate 的最终 visual status 仍 `QUALITY_TARGET_NOT_MET`（IoU `0.744938326895`、Boundary F1 `0.307109823312`、bbox `0.021484375`、centroid `0.021832396893`，hard gate false），未 confirm/version/export。Stage 0 truth、Viewer Runtime-only authority、human/PBR/export-restart/360 状态不变；staged candidate 不等于 immutable version。

2026-08-15 Pelvis bounded trial / camera binding reconciliation：r16 `BLOCKED`（瞬时 `reference_get` IPC），r17 `BLOCKED`（CLI 将 `no_improvement` proposal camera 错用于 source compare）均保留为独立 supplemental receipt；probe 已修复为仅在 `primary_form_repair_prepare=prepared` 时采用 repair camera。r18 `docs/evidence/mcp010f/supplemental/real-codex-cli-current-20260815-canonical-observation-pelvis-r18.json` 通过同 cohort canonical observation、26 inferred Part rows、`boundary_error_get=0` 与 `PASS_CAMERA_FIT_TO_COMPARE`；pelvis 55-evaluation proposal 未改善，source/proposal loss `0.435730135362`/`0.438891599478`，`no_improvement`/`retained_source`，最终 `QUALITY_TARGET_NOT_MET`，未 confirm/version/export。Stage 0 provisional truth、Viewer Runtime-only authority、human/PBR/export-restart/360 状态不变。

2026-08-15 Canonical observation + inferred Part table 状态：Runtime `silhouette_part_error` 对无显式 Part slice 的 target 生成同一 candidate/camera-bound 的 inferred Part error table；CLI 在同一 silhouette turn 内先完成 baseline compare，再读取一次 `AgenticSceneObserveResult@1`，不再发出独立 `boundary_error_get`。真实 r15 supplemental `docs/evidence/mcp010f/supplemental/real-codex-cli-current-20260815-canonical-part-observation-r15.json` 使用 cohort `726153a3…42ab5c`，canonical observation 含 26 个 inferred rows，`boundary_error_get` 为 0，推荐 Part 包含 `pelvis`、`upper-arm-right`、`chest-shell`；最终仍 `QUALITY_TARGET_NOT_MET`（IoU `0.739507057324`、Boundary F1 `0.314765400977`、hard gate false），未 confirm/version/export。它证明观察聚合与 Runtime 归因链路，不能改写 Stage 0 的 provisional `camera=MISMATCH`/`BLOCKED_INCOMPLETE_BINDING` 或升级 likeness。

2026-08-15 Primary Form right-shoulder bounded trial：current packaged cohort `26354e2f…a2029` 的真实 r11 receipt `docs/evidence/mcp010f/supplemental/real-codex-cli-current-20260815-part-contour-trial-r11.json` 对 `shoulder-armor-right` 完成 63 次 Runtime-owned bounded fit；same-camera source/proposal loss `0.426350916959`→`0.409541676181`，因此准备 staged candidate，但全局 `silhouette_iou=0.749072115206`、`boundary_f1_4px=0.326405552646` 仍为 `QUALITY_TARGET_NOT_MET`，且 `quality_hard_gate_passed=false`。没有 confirm/version/export、持久用户数据或无关副作用；r11 作为 supplemental current-cohort evidence 保留，不能替换 Stage 0 provisional truth，也不能宣称 Primary Form 已收敛。

2026-08-15 Primary Form exact-side sink alias 状态：Runtime 的固定 Part-ID projection 现在覆盖 `*-armor-left/right` 与显式 `*-left/right` sink 的单侧别名，且 side-aware；`shoulder-armor-left` 只会 materialize 到左侧，不会因 alias 规则污染右侧。新增 Runtime regression 通过；该 source fix 没有新的真实 likeness receipt，不改变 Stage 0 的 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING` 或未运行门。

2026-08-15 Primary Form exact single-Part trial 状态：`primary_form_repair_prepare`/Job 现在可接受可选 `part_id`，Runtime 会从 bilateral authoring Rig 派生 exact Part Rig，仅让该 Part 的 width/height/offset controls 进入 bounded search；主流程不再依赖预先存在的 Part RenderSet，source/proposal 仍由同一 Runtime-owned camera 与 Render Worker compare 做严格接受。r10 current cohort `d2b67cd8…02a2f1` 对 `shoulder-armor-left` 完成 trial，`acceptance_status=retained_source`、`candidate_state=unchanged`、`acceptance_strict_improvement=false`，source/proposal loss `0.426350916959`/`0.425568832154`；Part contour readback 为 `proposal_ready`。该 receipt 顶层 `BLOCKED` 仅来自 probe 无关副作用分类，未写持久用户数据、未 confirm/export，quality `NOT_RUN`，Stage 0 仍是 `QUALITY_TARGET_NOT_MET`、`BLOCKED_INCOMPLETE_BINDING`。

2026-08-15 Primary Form 单 Part contour/semantic asymmetry 状态：Runtime `part_contour_fit_prepare` 已关闭显式 bilateral Worker Part-ID 与 Rig Part 不一致的投影缺口：`hip-pair` 可读取 `hip-left`/`hip-right` 的合并 envelope，而精确 `shoulder-armor-left` 只读取左侧 mask/error；单侧 Rig 的 width/height/offset 参数保持 typed、bounded、read-only。真实 r8 receipt `docs/evidence/mcp010f/supplemental/real-codex-cli-current-20260815-part-contour-r8.json` 在 cohort `dd78d216…e5088` 上返回 `proposal_ready`，26 Parts、无持久化或无关副作用；它是 boundary-only 结构证据，`quality_visual_status=NOT_RUN`，不得改写 Stage 0 的 `QUALITY_TARGET_NOT_MET`、`BLOCKED_INCOMPLETE_BINDING` 或历史 benchmark。

2026-08-15 Primary Form 36-control Rig 真实复放状态：packaged cohort `106a2889…67b08e` 的 r7 supplemental receipt 记录 56 次 Runtime-owned bounded fit；新增 hip width/height、hip/pelvis/chest x-offset 与 hip y-offset 后，source loss `0.40710507361`、proposal loss `0.413276643194`，严格 acceptance 仍为 `retained_source`，`candidate_state=unchanged`，无持久用户数据写入。current-cohort camera binding、Render Worker 与 Viewer read-model 结构绑定通过，但 final `silhouette_iou=0.746739614479`、`boundary_f1_4px=0.34284083431`，`quality_visual_status=QUALITY_TARGET_NOT_MET`；r7 顶层 `BLOCKED` 只代表无关 side-effect 账本，不是质量或 transport PASS。

2026-08-15 Primary Form same-camera retention 真实验证：Runtime geometry-winner refit 现在先在同一 Runtime-owned final camera 上渲染 source GLB 与 proposal，再执行严格 `proposal_loss < source_loss` acceptance；search-only comparator 仅用于 bounded local-minimum exploration，最终 candidate/evidence 仍使用严格 acceptance。packaged cohort `e7e466f6…b7cc` 的 supplemental r6 receipt 记录 source loss `0.40710507361`、proposal loss `0.412982572291`、`acceptance_status=retained_source`、`acceptance_strict_improvement=false`、`candidate_state=unchanged`；顶层 `BLOCKED` 来自无关 side-effect 账本，未产生 candidate/version/confirm/export。真实 compare、camera binding、Render Worker cohort 与 Viewer read-model 结构门通过，视觉质量仍为 `QUALITY_TARGET_NOT_MET`。

2026-08-15 QualityReport 质量权威一致性与 packaged F 状态：`AppearancePrepare` 的结构性产物在 `visual_status=not-run` 时现在明确写入 `hard_gate_passed=false`，与 Runtime `QualityReport@2` validator 和 Viewer 只读消费规则一致；同一 packaged cohort `fee79807…` 的隔离 F probe 已验证 26 parts/4704 triangles、7 material zones、九 AOV、embedded textures、Render Worker cohort binding 与 Viewer read-model PASS。该证据仍是 structural/read-model PASS，不是 likeness 或视觉质量 PASS；真实质量继续为 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING`，人评/PBR likeness/export-restart/360 未运行或阻断。

2026-08-15 Primary Form 多尺度几何收敛状态：Runtime-owned `GeometryProgram` 搜索在固定 `max_evaluations=64` 上限内调整为 `geometry=40`、初始 camera=16、geometry-winner refit=8；较小预算按同一有界比例分配，三者总和仍不超过调用者上限。coordinate probe 的后续 pass 使用确定性 `1.0 → 0.5 → 0.25` 步长，形成一次完整 evidence-directed pass 后的 reverse/fine refinement，不把连续参数轨迹交给 Codex。该 source/focused 修复不改变 Schema、Worker/readback、same-camera acceptance、Viewer quality authority 或历史 receipt；当前仍为 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING`，人评/PBR/export-restart/360 未运行或阻断。

2026-08-15 camera-fit→compare handoff 状态：`reference_compare_prepare` 在没有显式 camera、但存在同一 `project_id/candidate_id/target_sha256` 的 Runtime camera-fit cache 时，复用该 `selected_camera`，不再重新从 default camera 推导 framing；无 cache/target 仍走原有 bounded fallback。新增自动复用 regression 通过。该源码 Gate 尚未替换历史真实 Codex receipt，因此当前账本仍保留 `camera=MISMATCH`、`QUALITY_TARGET_NOT_MET` 与 `BLOCKED_INCOMPLETE_BINDING`，不得写成视觉质量通过。

2026-08-15 Primary Form bounded continuation 状态：`primary_form_repair_prepare` 与异步 Job 的 Runtime optimizer 现在最多允许 2 个 continuation iterations；总 `max_evaluations` 仍为 64，第二轮只复用第一轮 Runtime incumbent 的相机/几何上下文。focused fixture 已验证 63–64 次 bounded evaluations 与 `iterations=2`，但没有新真实视觉 receipt，因此历史 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH` 和 benchmark incomplete binding 不变。

2026-08-15 Render Worker cohort-to-RenderSet 状态：Runtime sibling launcher 保留 isolated `forgecad-render-worker` 响应的 `build_cohort_sha256`，perspective `RenderSet@2` 写入 `render_worker_build_cohort_sha256` 与 `render_worker_binding_status`；Runtime 当前 cohort 不匹配、字段缺失或 status/值不一致时 fail closed。source build 的 null cohort 只能标记 `cohort_unavailable`，packaged same-cohort 才能标记 `same_cohort_verified`；Viewer 只读展示该信息，不推导 `QualityReport@2` 门。该结构/证据修复不产生新的 likeness、PBR、人评、export/restart 或 360 结论，Stage 0 仍为 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING`。

2026-08-15 canonical observation durable binding 状态：`DesignSession@1.observation_sha256` 与 `DesignCheckpoint@1.observation_sha256` 必须等于同一 Runtime 生成的 `AgenticSceneObserveResult@1.canonical_sha256`；`session_get`、checkpoint prepare/get/restore 和 `design_action_run_prepare` 会重新生成当前 observation，过期或跨 candidate/session 的 hash fail closed。Store payload 校验、Runtime focused/full tests、contracts、隔离 Runtime/MCP restart receipt 和 nested projection receipt 通过。该状态只证明观察证据的 durable lineage，不改变 Runtime 唯一写者、Viewer read-only authority、完整 durable/reference/DesignSpec producer 或 Repair 执行状态；Stage 0 继续为 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING`，人评/PBR/export-restart/360 未运行或阻断。

2026-08-15 RepairIntent durable binding 状态：`RepairIntent@1` 的 `observation_sha256` 由 Runtime 在 checkpoint restore prepare 时写入，并且必须等于 durable session 的 canonical observation hash；Store/CAS readback、schema positive/negative fixture、receipt checker 与隔离重启 probe 均通过。该字段只证明未来 Repair intent 使用哪一次观察，Repair 本身仍未执行；完整 orchestrator、真实 likeness、`QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING` 和人评/PBR/export-restart/360 状态均不变。

2026-08-15 Primary Form local-group convergence 状态：Runtime 的 bounded geometry search 在 joint proposal 未改善时，不再直接把所有剩余预算拆成互相独立的单坐标试探；它按 Runtime boundary ranking 形成同 Part 的耦合 width/height/offset candidate，并只在 candidate 严格改善时合并到当前 geometry incumbent。该修复没有改变 Schema、工具数、Viewer quality authority 或真实 receipt；`QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING` 和人评/PBR/export-restart/360 保持原状。

2026-08-15 Primary Form ranking snapshot 状态：Runtime 将 camera/geometry/refit 的 public contour metrics 与 transient landmark ranking metrics 分离保存，但所有 winner、baseline 和 strict-improvement 比较均使用同一完整 ranking snapshot；geometry trial 以 selected camera 的 snapshot 为局部 incumbent，最终 fit 仍对 authored base camera 做严格比较。该修复不改变 `SilhouetteFitResult@1` 五项公开 metrics、Runtime 唯一写者或 Viewer read-only authority；新增 snapshot regression，Runtime 全量 `105 passed / 12 ignored`。没有新的真实视觉 receipt，Stage 0 继续为 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING`。

2026-08-15 observation/camera binding 状态：Runtime action 只接受由 Codex 明确提交的 `observation_sha256`，并把它纳入 `DesignActionRun@1.input_sha256`；当前 Runtime observation canonical hash 不一致时返回 `AGENTIC_OBSERVATION_STALE`，不创建 action receipt。Primary Form 的 camera handoff 使用 session `camera_hash` 在同一 candidate/target-bound fit projection 中解析完整 `CameraCalibration`，未命中时返回 `AGENTIC_CAMERA_BINDING_MISMATCH`，不使用 default fallback。MCP/Runtime focused tests 通过；这只是观察和相机的证据绑定修复，不改变 Runtime 唯一写者、Viewer read-only authority 或 Stage 0 的 `QUALITY_TARGET_NOT_MET`、`MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`。

2026-08-15 Runtime quality-authority 状态：`visible_view_gate_checks` 集中持有 visible-view metric 的方向/阈值/状态，`visible_view_gate_passes` 与 Agentic critic projection 共享它；Agentic 不再自带第二套质量门。该修复保持 Runtime 唯一质量权威与 Viewer read-only projection boundary，focused/full Runtime 和 MCP010F source Gate 通过。没有新的真实视觉 receipt，Stage 0 仍为 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING`，人评/PBR/export-restart/360 仍未运行或阻断。

2026-08-15 Primary Form metric-priority 状态：Runtime 的 bounded fit 不再用 Chamfer/IoU 加权和单独决定 winner。`primary_form_metric_ordering` 以 boundary F1、silhouette IoU、bbox、centroid、landmark coverage/NME、region 顺序逐层比较；同一优先级完全相等时才使用 scalar loss 作为稳定 tie-break。该排序已接入 camera fit、Rig geometry/refit、same-camera acceptance 与 candidate compare，并新增 focused regression；完整 Runtime 预期 `104 passed / 12 ignored`。没有新的真实机器人视觉 receipt，Stage 0 仍为 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING`，人评/PBR/export-restart/360 仍未运行或阻断。

2026-08-15 Primary Form 双向边界证据状态：Runtime-owned `boundary_error_segments_for_masks` 现在对同一 target/model mask 以固定、有限采样同时建立 reference→model 与 model→reference correspondence；model-only excess edge 不再因单向 nearest projection 被漏掉。每条记录继续绑定同一 camera 与 Part-ID，并交给既有每部件覆盖和最多 64 条 deterministic selection；新增 regression，`forgecad-runtime` 全量 `103 passed / 12 ignored`。该 source/convergence Gate 没有新的真实机器人视觉 receipt，不改变 Runtime 唯一写者、Worker typed boundary、Viewer read-only authority 或 Stage 0 的 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING`；人评/PBR/export-restart/360 仍未运行或阻断。

2026-08-15 Render Worker ownership 状态：Runtime 通用 sibling launcher 现在由 `geometry_worker.rs` 的 `execute_sibling_worker` 提供，Render Worker binary identity、typed request projection 和 response adapter 全部归属 `render_worker.rs`；Geometry Worker 不再保留 `RENDER_WORKER_BINARY` 或 `execute_render_worker`。新增两个 macOS isolated tests 要求 source-built same-cohort `forgecad-render-worker`，覆盖九 AOV/512px PNG/确定性和 GeometryProgram 载荷拒绝；普通 Runtime 全量 `102 passed / 12 ignored`，boundary checker PASS。该结构 Gate 不生成新的视觉质量证据，Stage 0 仍保持 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING`，人评/PBR/export-restart/360 未运行或阻断。

2026-08-15 Agentic observation single-snapshot 状态：`design_stage_plan_get`、`critic_report_get` 和 `visual_evidence_bundle_get` 现在只能使用 `scene_observe_get` 返回的 canonical observation hash；Runtime 校验后直接切片同一 `AgenticSceneObserveResult@1`，不允许派生读工具重建第二份现场。Agentic evidence 统一为 `VisualEvidenceBundle@1`，Viewer 专用 `ViewerVisualEvidence@1` 不变。focused stale-hash/slice regression 通过；该边界只减少 Codex 观察漂移，不推进视觉质量、confirm/export、人评/PBR/restart 或 360。

2026-08-15 Primary Form evidence-magnitude handoff 状态：Runtime 第一轮坐标 probe 在当前值与 candidate-bound evidence proposal 不同的时候，复用该坐标的 evidence-derived magnitude；单步上限为 authored Rig span 的 50%，proposal 已到达或后续反向 pass 则使用原有小步。该模块只改变 Runtime 内部 bounded search 的步长，不改 schema、tool manifest、Runtime 唯一写者、Worker 协议或 Viewer quality authority。`forgecad-runtime` 全量 102 passed、10 ignored；没有新的真实视觉 receipt，Stage 0 仍为 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING`，human/PBR/export-restart/360 未运行或阻断。

2026-08-15 Primary Form boundary evidence coverage 状态：Runtime-owned `boundary_error_segments_for_masks` 现在在固定 64 条上限内先覆盖每个有 Part-ID 的可见部件，再按最大距离填充；输出仍按 distance descending，未归属 segment 只参与填充阶段。该修复让同一观察能同时提供 shin/head/hand 等部件的局部方向证据，供后续 bounded Rig sweep 使用，不改变 schema、tool manifest、Runtime 唯一写者、Viewer read-only authority 或 Agentic 单 Part action scope。coverage regression 与 `forgecad-runtime` 全量 101 passed、10 ignored；没有新的真实视觉 receipt，Stage 0 仍为 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING`，human/PBR/export-restart/360 未运行或阻断。

2026-08-15 Primary Form proposal-direction retention 状态：Runtime 现在将完整 candidate-bound boundary proposal 与 dominant-Part 的局部 probe-zero seed 分离；secondary Part coordinate probe 使用其 evidence-derived bounded direction，零改变量才回退到稳定方向。新增显式 `-1/0/+1` 回归，避免正零被误当成正向。全量 Runtime 为 100 passed、10 ignored；没有新的真实视觉 receipt，Stage 0 继续为 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING`，Agentic action-run 单 Part scope 和 Viewer read-only authority 不变。

2026-08-15 Primary Form bounded multi-Part sweep 状态：Runtime 仍让 candidate-bound dominant Part 只负责初始化局部 seed，以避免一次联合 proposal 耦合多个轮廓误差；后续 deterministic coordinate schedule 已移除 dominant-Part filter，会在固定 Geometry/Render Worker 预算内覆盖完整 typed Rig。新增 regression 验证 secondary Part 在 seed 后仍进入排序和 probe schedule；`forgecad-runtime` 全量为 100 passed、10 ignored。没有新的真实机器人视觉 receipt，Stage 0 继续为 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING`；Agentic action-run 的单 Part scope、Viewer read-only authority 和未运行的人评/PBR/export-restart/360 门不变。

2026-08-15 Primary Form local Part offset calibration 状态：Runtime 局部 Part envelope proposal 对米制 `offset_x/offset_y` 现在使用同一 camera calibration 的 world-per-normalized-screen scale；`offset_y` 按 image-Y 向下、camera-plane up 向上的坐标约定换算。`fit_rig_parameters_with_landmark_context` 将完整 camera 传入该 proposal；没有 camera 的 legacy `part_contour_fit_prepare` 不再猜测米制世界位移。相机标定/符号 focused regression 和 `forgecad-runtime` 全单测通过；没有 Schema、tool manifest 或真实视觉状态变化，Stage 0 仍为 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING`。

2026-08-15 Primary Form same-camera retention 状态：`primary_form_repair_prepare` 现在在创建 staged candidate 前，使用最终 Runtime-owned camera、同一 reference target 和同一 512px Render Worker 对 source GLB 与 proposed GeometryProgram 做 non-persisted full-resolution compare；只有 `proposal_loss < source_loss` 才进入正常 candidate/evidence transaction。`PrimaryFormAcceptance@1` 固化 source/proposal program hash、camera hash、两侧 loss 和 `accepted`/`retained_source`；未通过时保持 authored source，不覆盖 source VisualEvidence。该 Gate 只修复相机补偿/局部回归导致的错误晋级，未产生新的授权机器人视觉 receipt，当前仍为 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING`。

2026-08-15 Agentic observation→action binding 状态：`design_action_run_prepare` 只把当前 Runtime 生成并校验过的 `AgenticSceneObserveResult@1.canonical_sha256` 写入 `DesignActionRun@1.observation_sha256`；SQLite/CAS immutable readback 与合同均要求该 SHA-256。focused action round-trip 验证 receipt 与同 candidate 的 Runtime observation 相等，缺字段 fixture fail closed。该模块只修复 observation 被拆散后无法证明 action 使用哪一次观察的问题，不等于多阶段 orchestrator、Repair 应用或视觉质量通过；Stage 0 继续保留 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`。

2026-08-15 Primary Form 收敛证据状态：Runtime 现对每次 bounded fit 固化 authored baseline 与 selected winner 的 camera/metrics/loss、strict-improvement 和 camera/geometry evaluation 计数；action-run evaluate stage 通过 `result_sha256`、`output_sha256`、`summary_sha256` 形成 Primary Form→QualityReport→comparison 的单一证据链。`DesignActionRun@1` Schema 已对齐 Store 的实际 stage object，并通过 `skipped`/旧 `hash/reason` 负向回归。该修复不改变 candidate/version 写入边界，不把 Runtime source/focused PASS 写成视觉质量 PASS；Stage 0 仍为 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`。

2026-08-15 Render Worker Runtime adapter 状态：Runtime 侧新增 `render_worker.rs` typed adapter，Geometry Worker 只保留编译/worker transport，Primary Form 的初始 512px framing、full-resolution camera ranking、Geometry winner refit 和九 AOV render 均通过该 adapter 调用同一个 isolated sibling Render Worker。新增 source ownership checker 与 Runtime focused regressions 通过；该模块化修复不改写现有真实 reference，视觉仍为 `QUALITY_TARGET_NOT_MET`，`camera MISMATCH`、benchmark incomplete binding、human/PBR/export-restart/360 状态保持原账本。

2026-08-15 Agentic bounded action-run 状态：`design_action_run_prepare`/`design_action_run_get` 已进入当前 Runtime/MCP source slice。该切片只允许 approved、session/project/candidate/reference-bound、单 Part `primary-form` action；Runtime 在一次 action run 内复用既有 Primary Form bounded repair pipeline，写入 SQLite/CAS 的 `AgenticActionRunRecord` 并提供 immutable readback。结果锁定 `confirm`/`export`，`runtime_write=false`、`persistent_user_data_touched=false`，不会修改 candidate/version。focused Runtime idempotency/readback、MCP boundary/manifest 和 Stage 0 已通过；它不等于通用 orchestrator、Repair 应用、durable/reference/DesignSpec 完整 producer 或视觉质量通过。

2026-08-15 Primary Form 自动目标单 Part 状态：Runtime 已把没有显式 `SilhouetteTarget.parts` 的 automatic silhouette target 交给同一 Render Worker Part-ID boundary projection，仅对请求的 semantic Part 生成局部 envelope/error 和 bounded proposal；没有 hidden-side inference 或额外 Codex 参数搜索。current Dev.app cohort `77ccce85…b9d4` 的 `shin-pair` 隔离 receipt 已通过 preflight、MCP/Runtime cohort binding、Geometry/Render Worker transport 和五候选 compare，但严格单 Part retention 门保留 authored baseline（IoU `0.741047`、Boundary F1 `0.328765`），没有应用 candidate，仍为 `QUALITY_TARGET_NOT_MET`。该 supplemental receipt 不改写 Stage 0 provisional observation、camera `MISMATCH` 或 benchmark eligibility。

2026-08-15 Primary Form action budget truth：修复前真实 receipt `docs/evidence/mcp010f/primary-form-budget-pre-fix-real-codex-20260815.json` 暴露了 `max_evaluations=64 → fit_evaluations=24` 的 Runtime 外层截断；当前源码 cap 已恢复为 64，Runtime continuation 上限为 2，端到端 focused fixture 证明 `primary_form_repair_prepare` 在 64 请求下完成 63–64 bounded evaluations 且 `iterations=2`。Dev.app cohort `c521bf28…c4a5` 已安装；修复后 real-Codex receipt 在 authoring/hash/prepare 阶段阻断，没有新的视觉比较或质量结果。该源码修复不改写 retained observation，仍为 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`，human/PBR/export-restart/360 未运行或阻断。

2026-08-15 packaged Render Worker 状态：Dev.app 当前 cohort `aa5eaaa2…5827` 已将 MCP、Runtime、Geometry Worker、Render Worker 四个资源一起安装；Resource allowlist、ad-hoc deep-strict 签名、同 cohort identity 与 packaged raw stdio 均通过。Runtime 在 sibling Render Worker 进程边界内完成九 AOV、固定 renderer、两次 deterministic hash、compare 与 image-block transport，且未写持久用户数据。该状态只把 packaged resource/process/protocol boundary 记为 PASS；raw 输入为 synthetic reference，`QUALITY_TARGET_NOT_MET`、`structural_visual_claim=NOT_CLAIMED`、human/PBR/export-restart/360 未运行事实保持不变。

2026-08-14 Primary Form Part-priority 修复：Runtime 不再只按 Rig 提案改变量排序 bounded geometry probes；在已有同 candidate 的 Part-ID boundary evidence 时，先按聚合的 Part contour distance 优先覆盖主导可见误差 Part，再按参数 delta 与稳定 ID tie-break。无 Part evidence 仍使用原排序，所有值/边界/Worker 调用保持 Runtime-owned。focused/full Runtime 与 MCP010F source Gate PASS；新的授权参考视觉复验因原图字节不在当前 workspace 而阻断，不能升级 `QUALITY_TARGET_NOT_MET` 或 benchmark 状态。

2026-08-14 Primary Form 首轮全控制覆盖修订：detail route 的 26-control `SilhouetteRig@1` 现在通过 `max_evaluations=64` 进入 Runtime；GeometryProgram 路径的确定性预算为 `32 geometry + 16 initial-camera + 16 geometry-winner-camera-refit`，首轮几何试探覆盖初始证据提案和全部 26 个控制。该 source/convergence 修订有 Runtime focused/full regression，但尚未重新运行授权机器人 reference，因此不改变 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING` 或人评/PBR/export-restart/360 状态。

2026-08-14 Primary Form 单动作真实 transport：同 cohort receipt `docs/evidence/mcp010f/real-codex-cli-current-20260814-primary-form-runtime-owned-r3.json` 已验证 Codex 在一次 observation/camera/Rig 回合后只调用一次 `primary_form_repair_prepare`；Runtime 在该动作内完成 24 次 bounded fit、Geometry Worker 编译、严格 GLB 回读、隔离 Render Worker、candidate-bound compare 和九 AOV，随后 CLI 只读取 Runtime 返回的 visual evidence，不再重复发起 Codex compare。相机绑定为 `PASS_SILHOUETTE_FIT_TO_COMPARE`，compare IoU `0.749122`、Boundary F1 `0.347623`，仍为 `QUALITY_TARGET_NOT_MET`；这证明模块边界和证据链收口，不证明 likeness/high-quality PASS。

版本：2026-08-13
状态：MCP005–009 functional truth 已实现；FGC-MCP010A done；MCP010B structural truth source Gate 已通过但 Darwin OS memory hard cap deferred/NOT_RUN；MCP010C source-focused renderer/compare/review Gate、MCP010D hard-surface Operator/Skill Gate 与 MCP010E 离线 AssetPack/UV/PBR/MikkTSpace Gate 已通过，首次真实机器人 compare/review transport 已运行但 likeness threshold `FAIL_QUALITY_TARGET_NOT_MET`；MCP010F Viewer source Gate 已通过，当前 cohort packaged Viewer CLI read-model 已完成 exact project/candidate/artifact/reference/render-set/comparison lineage binding，正式 UI/accessibility、独立人评、360 仍 NOT_RUN/BLOCKED。当前 F 轮廓 slice 另提供 Runtime-owned `silhouette_part_error_get`：按 hash 绑定的多 Part table 归因局部边界误差，source focused PASS；它不改变当前真实图片 likeness 失败事实。ADR-0026 的 observe/plan projection 与受批准 durable `DesignSession`/`DesignCheckpoint`/`RepairIntent` prepare/readback 已成为当前 source/runtime/MCP/Viewer 证据层；单动作 orchestrator、Repair 应用、完整 producer/consumer conformance 与完整视觉门仍未完成。

Stage 0 权威快照（历史机器快照）：记录为 490 Schema、28 operator entries、106 read + 78 opt-in write = 184 tools；当前公共合同集合为 518 schemas、28 operator entries、111 read + 83 opt-in write = 194 tools，唯一 `in_progress` 为 `FGC-MCP010F`。机器可读事实入口是 `docs/evidence/mcp010f/current-benchmark-truth.json`；Hero UV durable receipt 为 `docs/evidence/mcp010f/commercial-weapon-hero-uv-durable-restart-source-gate-20260826.json`。当前 Low provenance 为 candidate-bound，Low=`DRAFT_UNREVIEWED / structural_only / promotion_eligible=false`；Hero UV 7 个 registered contracts 已接入 public `hero_uv_durable_get/prepare`，四个 Hero CAS roots 已纳入 linked/GC，durable restart **1/1 PASS**。这些 slice 仍为 `structural_only`，不是 artist unwrap、visual、human、engine、commercial 或 packaged PASS；proposal=`registered=false`、Stage=`camera-calibrated`、`secondary-form-approved=NOT_CREATED`、`FPS-HIGH-05=NOT_PASSED`、`QUALITY_TARGET_NOT_MET`、HQ360=`BLOCKED_REFERENCE_COVERAGE`，无 confirm/version/export。Mechanical pose 只是一条 candidate-bound read projection；attempt35 只称 `provisional retained observation`，benchmark eligibility 为 `BLOCKED_INCOMPLETE_BINDING`，fit/compare camera 判定 `MISMATCH`。packaged Viewer receipt 已绑定其自身 current-cohort read-model，但 UI/accessibility、image-block consumption、真实人评和 packaged E2E 仍未运行；source、raw transport、build、窗口结构或 AX smoke 也不构成视觉、人评或 packaged UI PASS。Runtime staged actions、Agentic receipts 与 Mechanical pose receipt 都只证明各自窄范围 source/readback slice，不改写上述质量事实。

2026-08-14 Worker 边界修订：`render_fixed` 的 GeometryProgram 编译已从 Render Worker 输入移回 Geometry Worker；Render Worker 只接受编译后 GLB 的 fixed/perspective/batch render façade，geometry compile payload 有隔离负向回归。无状态 renderer implementation 已拆到 `apps/render-core`，Render Worker 不再依赖 `forgecad-geometry-worker` crate，source ownership checker 与 MCP010C 回归已通过。该状态只把 process/protocol/source ownership boundary 记为 PASS，不升级 MCP010F、`QUALITY_TARGET_NOT_MET`、camera `MISMATCH` 或其他未运行的视觉/发布门。

2026-08-14 Primary Form evidence projection 修订：Runtime 在选中 camera 的同一 Part-ID readback 上消费固定 landmark→Part anchor，将投影误差映射为相机平面米制 offset，再以 typed transform/source geometry 试算；detail 编排使用 20 参数和 `32 → 21 geometry + 11 camera` 的 Runtime-owned bounded budget。该修复有 landmark ownership、camera-plane materialization 和完整 Runtime 回归证据，但尚未重跑授权机器人 reference，因此只改善收敛路径，不改写当前真实质量、camera binding 或 benchmark eligibility。

运行时间线以 `docs/evidence/mcp010f/real-codex-run-inventory.json` 为准：attempt5 是历史 CameraCalibrationRef 里程碑；当前最新完成 transport 与最新 attempt 均为 `docs/evidence/mcp010f/real-codex-cli-current-20260814-primary-form-runtime-owned-r3.json`，状态为 `PASS_WITH_QUALITY_TARGET_NOT_MET`，fit/compare camera binding 为 `PASS_SILHOUETTE_FIT_TO_COMPARE`，未晋升为 attempt35 provisional benchmark。历史 boundary-projection、semantic-landmark-compare 与 semantic-aligned-fast receipts 按原始状态保留。


<!-- forgecad-reference-source: input=ENV_AUTHORIZED_PNG original_sha256=1964704a62ed7a841b4d49c370b8d46f4626e201daad29092a9c39a40b4c4109 intake=PASS_SOURCE_SIX_REFERENCE_EVIDENCE_CAS views=6 worker=PASS_SAME_COHORT_SIX_FIXED_VIEWS target=USER_REFINED_USER_CONFIRMED_REVIEWED_STRUCTURE user_confirmed_crop=PASS_USER_CONFIRMED_SEVEN_CROPS contour=PASS_USER_CONFIRMED_SIX_IDENTITY_CONTOURS negative_space=BOUNDING_REGIONS_CONFIRMED_EXACT_SUBTRACT_UNKNOWN line_flow=EXPECTED_ROWS_DURABLE_MATCH_NOT_PROVEN camera_lock_fixture=PASS_REAL_DURABLE_REPLAY_RESTART form_art_fixture=PASS_REAL_DURABLE_NOT_PROVEN form_quality_v2_fixture=BLOCKED_ZERO_WRITE_MISSING_LEGACY_CROSS_VIEW secondary_form_approved=NOT_CREATED fixture=PASS_REAL_1_OF_1_108.07S -->

2026-08-14 Primary Form / observation module state：Runtime-owned fit now honors the declared bounded budget with geometry priority (`24 → 16 geometry + 8 camera`) and covers the full ranked Rig coordinate set before reverse-direction probes. The MCP010C/F source and worker-boundary gates pass, while current real-reference quality remains `QUALITY_TARGET_NOT_MET`; canonical observation is recorded as one bound `AgenticSceneObserveResult@1` stage, not a source of new likeness evidence.

2026-08-14 Primary Form proposal handoff state：`silhouette_fit_prepare` now exposes an optional `selected_geometry_program` only when a Runtime Geometry Worker trial strictly improves the authored baseline. The returned `GeometryProgram@2` is project/hash validated at the result boundary and remains read-only; it is intended for a later user-approved `geometry_prepare` call, not automatic candidate mutation or confirmation. Contracts and focused/source gates pass; this does not change `QUALITY_TARGET_NOT_MET`, camera `MISMATCH`, benchmark eligibility, or the unrun human/PBR/export/360 gates.

2026-08-14 Primary Form bounded schedule state：camera coordinate-descent no longer aborts when one local batch fails to improve the incumbent. It now consumes the remaining Runtime-owned bounded schedule so later roll/FOV/distance/target-offset/global-scale axes are evaluated before the declared budget is exhausted; a focused 8-camera fixture verifies all 8 evaluations are consumed. This repairs a real convergence truncation, but adds no visual-quality evidence and does not change `QUALITY_TARGET_NOT_MET`, camera `MISMATCH`, `BLOCKED_INCOMPLETE_BINDING`, or the unrun human/PBR/export/360 gates.

2026-08-14 Agentic visual evidence binding state：`VisualEvidenceRecord` now persists the nullable `target_sha256` used by a target-bound `reference_compare_prepare`, with an additive SQLite migration and project/reference validation on readback. `ViewerVisualEvidence@1` and the read-only `VisualEvidenceBundle@1` projection expose that same hash；`DesignCriticReport@1` now emits one fixed-priority `primary_form_directive` whose continuous-search owner and repair owner are both Runtime. Target-bound Runtime regression proves the hash survives SQLite/CAS → Viewer → `AgenticSceneObserveResult@1`；default legacy comparisons remain nullable. This reduces duplicated Codex observation/search context without executing Repair or changing Viewer quality authority；full Runtime 87 passed/10 ignored, Store 8 passed, contracts/Agentic contracts and `script/test_mcp010f.sh` passed. Real likeness remains `QUALITY_TARGET_NOT_MET`, camera `MISMATCH`, and benchmark eligibility `BLOCKED_INCOMPLETE_BINDING`.

## 1. 真值层级

1. **Runtime V1 SQLite + CAS**：项目、候选、版本、Job、Skill、审批和工件唯一持久真值；
2. **公开 JSON Schema + canonical serialization**：对象合法性和 hash 规则；
3. **`ActiveDesignSnapshot`**：当前项目状态的单一只读投影；
4. **Worker receipts/readback**：对特定输入和工件 hash 的事实；
5. **Render/Quality evidence**：对特定 candidate/version 的检查；
6. **MCP/Viewer projection**：可丢弃、可重建的展示；
7. **Codex 对话/自然语言**：意图与解释，不是产品状态。

GLB、图片、`.blend`、Three.js scene、prompt、Skill 文档和 Codex 评价都不能单独成为版本头。

MVP 具体规则：Reference truth 是 CAS 原始字节 + `ReferenceEvidence`，不是本机路径；Geometry truth 是 canonical `GeometryProgram` + worker receipt + mesh/GLB readback，不是 `.blend` 或 Viewer scene；Appearance truth 是 typed MaterialZone/AppearanceProgram；Render/Quality 只证明同一 candidate hash；导出是 confirmed version 的派生物，不反向成为版本头。

### 1.0.1 Agentic Design Runtime 目标真值

ADR-0026 引入的目标对象不改变现有真值层级。当前 observe/plan projection 由 Runtime 按需从现有 read model 派生，可丢弃、可重建；durable session/checkpoint slice 则由 Runtime 经过审批校验后写入 SQLite/CAS。两者仍不能替代 candidate/version 的唯一写者边界，也不把下列目标能力伪装成完整实现：

- `DesignSession@1` 当前已能在同一 project/candidate/reference/evidence lineage 下持久化 stage、失败门、下一步允许动作和 checkpoint 指针；单动作 orchestrator 与候选变更仍必须另行落到 Runtime candidate/version/job；
- `DesignCheckpoint@1` 当前已能持久化不可变失败/阶段检查点并在重启后读回；`checkpoint_restore_prepare` 只生成 CAS-bound `RepairIntent@1`，不执行 Repair、不改写历史；
- `SemanticSceneGraph@1` / `ModelUnderstandingBundle@1` 未来是只读理解投影，必须由 Runtime candidate、readback、RenderSet、QualityReport 和 source map 派生；
- `ReferenceCanvas@1` / `DesignSpec@1` 当前由授权 `ReferenceEvidence` 生成受限、hash-bound 的 session read model，覆盖不足和 unknown 会明确阻断；完整独立 producer/consumer conformance 仍未完成；
- Critic/Repair report 未来只记录 evidence-bound issue 和 bounded intent，不能跳过 compile/readback/render/compare；
- Parametric Design Kit 未来必须展开为 typed Geometry/Appearance contracts，不允许成为任意脚本或第二几何真值。

上述 durable 对象目前只在受限 prepare/readback receipt 的范围内计入当前能力账本；单动作 orchestrator、Repair 应用、完整 Visual Evidence conformance、packaged same-cohort 与视觉质量门仍不得宣称已实现。

### 1.1 MCP010 当前与目标真值

2026-08-14 当前真值修订：manifest/目录计数已为 102 个 JSON Schema；packaged Viewer receipt `docs/evidence/mcp010f/real-codex-cli-current-20260814-primary-form-coverage-bound-viewer.json` 已在其 cohort 下完成 packaged Viewer CLI read-model 的 exact project/candidate/artifact/reference/render-set/comparison lineage binding；全局最新 r3 transport 另见 `docs/evidence/mcp010f/real-codex-cli-current-20260814-primary-form-runtime-owned-r3.json`。两者均为 `QUALITY_TARGET_NOT_MET`、`quality_hard_gate_passed=false`，且 sanitized Codex events 未观察到 image-block consumption；UI/accessibility、真人评审、PBR likeness、export/restart hash 和 360°仍 `NOT_RUN/BLOCKED`。该绑定只补齐读模型 lineage，不把 attempt35 或 r3 staged candidate 晋升为 likeness/high-quality PASS。

MCP010B 当前源码增加 8 个合同，MCP010C 再增加 7 个合同，MCP010E 再增加 6 个合同，MCP010F 轮廓求解器新增 12 个合同（含 `CameraCalibrationRef@1`）及其余 CameraCalibration/target binding 合同，Agentic contract family 继续补充 projection/session/checkpoint/RepairIntent 合同，该段当时共 144 个 JSON Schema（MCP006 历史为 44）；现行总数见顶部 **515-schema** 真值。B 的 `GeometryProgram@2`/strict readback/restore evidence source Gate 已通过；Darwin 512 MiB OS memory hard cap 仍 deferred/NOT_RUN。C 的 reference/renderer/review 合同、E 的 AssetPack/Appearance V2 合同与 F 的 silhouette target/camera/Rig/SDF/Part/candidate compare 合同已由 Runtime/MCP producer/consumer 使用；Agentic projection 与 durable session/checkpoint/RepairIntent prepare/readback、CAS-bound RepairIntentRun request/result 也已通过合同/隔离重启 Gate；固定 512×512 perspective/z-buffer renderer、九 AOV、local mask/metrics、MCP image block、Codex/human review、离线 AssetPack、512px UV atlas、固定 mikktspace、embedded PBR texture、哈希绑定轮廓目标、扩展相机搜索、受限 Rig/SDF fit、候选比较和 Subdivision artifact-lineage read projection 的 source raw Gate 已通过。历史真实 Codex CLI C receipt 已完成六 turn/32-call transport；带 15 landmark/8 region intake 的 source-built silhouette-first attempt35 已完成 11-turn detail reference→mask/target→camera/Rig/fit→compare→boundary→九 AOV→typed review/quality transport，但只保留为 provisional retained observation，结果为 `QUALITY_TARGET_NOT_MET`（26 Parts/4704 triangles，IoU `0.741047`、boundary F1 `0.328765`、bbox edge error `0.007813`、landmark coverage `0.733333`、region median `0.869403`），不是 likeness PASS；其 camera-fit `354caf27…f95788` 与 reference-compare `8cd20605…a535` 为 `MISMATCH`，benchmark eligibility 为 `BLOCKED_INCOMPLETE_BINDING`。Runtime fit proposal 为 `no_improvement`、IoU `0.698340`，仍是 read-only 建议；`primary_form_repair_prepare` 另有 staged prepare/evaluate，但没有 confirm/version/export。attempt33/34 的完整相机 payload hash 漂移失败保留为负向证据；`CameraCalibrationRef@1` 已消除跨轮次浮点复制阻断，但不补齐 attempt35 的 compare 真值绑定。当前单 Part proposal 已改为使用选中 Part 的本地边界宽高和质心误差，而不是全身包围盒，且通过独立单测；这只改善编排信号，不证明新模型质量。脱敏证据见 `docs/evidence/mcp010f/real-codex-cli-silhouette-first-20260813-attempt35-detail-camera-ref.json`；same-observation packaged Viewer/human/PBR/export/360 仍未运行。MCP010A/010B 的历史 Dev.app receipts仍原样保留，不能替代 C/D/E/F packaged/live/Viewer evidence。C/E/F synthetic/raw/CLI transport 与 Agentic prepare/readback receipt 都不证明用户机器人 likeness、PBR likeness、独立人评、Repair execution 或 360°。

历史 CameraCalibrationRef 里程碑 `docs/evidence/mcp010f/real-codex-cli-camera-ref-20260813-attempt5.json` 使用同一授权 PNG、同 cohort `e968c9ef…6980`、26 Parts/4704 triangles、九 AOV 和 typed review，Runtime 通过 `camera_hash + canonical_sha256 + target_sha256` 解析精确相机。其 comparison 仍为 `QUALITY_TARGET_NOT_MET`（IoU `0.698465`、boundary F1 `0.281074`、bbox edge error `0.037109`、centroid `0.049908`、landmark coverage `0.666667`、landmark NME `0.201432`、region median `0.771619`、critical region min `0.675106`）。attempt5 不是当前最新完成传输，也不是 benchmark，不解锁材质、confirm/export、human review 或 360；用户持久数据未改变。

`AppearanceProgram@2`、PBR V2/纹理、Viewer 的 Part/MaterialZone/selection/explosion/heatmap、packaged E2E 和 first-party AssetPack 已由 MCP010E/F source Gate 部分落地。C 的 RenderSet@2/比较/评审已进入 Runtime-owned producer、CAS artifact、严格 readback、固定 render、QualityReport 和 evidence lineage；E 的 AssetPack/manifest/provenance、embedded textures、UV/tangent 和九 AOV 也已进入同一 Runtime/Worker source path；F 的 Viewer compare source surface 已通过本地 IPC/构建测试，但不等于 packaged/live Viewer Gate。当前 Runtime Skill registry 的 11 项中，`primitive-blockout@0.2.0`、`hard-surface-detail@0.2.0` 与 `uv-pbr@0.2.0` 在真实 consumer 和 immutable bundle 校验后 active；这不产生视觉 likeness 或 360°结论。Darwin 512 MiB OS memory hard cap、人评阈值、Viewer/package/live C/D/E/F 和 360 更不能由结构/PBR source PASS 推导。

最新 `d9c23b…ac0bd` 开发包记录了 Bundle 知识分支的校正：`limited` 只阻断视觉质量声明，`STRUCTURAL_BLOCKOUT` 仍需用户明确选择并经过相同 Runtime geometry/readback/approval 硬门。该包的 isolated raw/real-Codex V2 structural 通过；用户完整重启后它已成为当前 live Desktop cohort，并完成 32 工具、Ready/doctor、cohort/catalog/hash 与项目只读回读结构激活。

2026-08-13 的 F 轮廓优先增量已通过隔离 source transport probe：真实 Viewer `chest-shell` 草图被绑定为局部 target，`part_contour_fit_prepare` 生成建议，四个有界单 Part 变体由固定比较器筛选。最高 IoU 为 `0.745895`（provisional observation 对照值 `0.741047`），loss winner 为 IoU `0.745135`、Boundary F1 `0.340045`；两者都未达到严格 `0.90` 轮廓门，也未创建 candidate version/export。该证据只证明轻量纠偏编排和 bounded candidate 选择，不能产生合格 benchmark 或改写当前机器人 `QUALITY_TARGET_NOT_MET`；fresh source binary 的 build identity 为 null，故不宣称 cohort PASS。
Runtime 的 `silhouette_fit_prepare` 现在会在有 typed Part slice 时做一次选定相机 Part-ID readback，并对匹配参数使用局部 target/model envelope 与 centroid proposal；无 slice 时保持全身 fallback。合成局部优先单测、完整 Runtime 和新的 source receipt 均通过，但它仍只是 bounded reviewable proposal，不放宽质量门或确认/导出边界。

目标 `HumanVisualReviewReceipt` 只证明用户评分绑定到特定 reference/camera/render/candidate hash，不证明模型身份，也不能覆盖 Geometry/UV/PBR 硬门。当前单张参考只能产生 `PARTIAL_VISIBLE_VIEW_PASS`；`HQ_360_PASS` 在多视图完整前固定 blocked。

## 2. 核心对象

### Project

包含 project ID、名称、创建时间、policy/profile、active snapshot revision。无绝对本机路径和模型/Provider 信息。

### ReferenceEvidence

保存 CAS hash、MIME、尺寸、用户授权声明、导入方式、视图/相机 claims 和派生证据 lineage。原始绝对附件路径入 CAS 后丢弃。

### Candidate

未确认、可 GC 的完整构建单元，引用：base version、SubjectProfile、RepresentationPlan、AssemblyGraph、Geometry/Appearance programs、Skill receipts、artifacts/readback、RenderSet、QualityReport、SemanticChangeSet 和状态。

Candidate 状态：`prepared → compiling → evaluating → reviewable → confirmed | rejected | failed | expired`。只有 `reviewable` 且 hard gates 通过者可 confirm。

### DesignAssetVersion

不可变提交，至少包含：version ID、project、parent version、confirmed candidate hash、assembly/program/material/texture/artifact manifests、quality、approval、created_at 和 canonical digest。任何修改都创建新子版本。

### ActiveDesignSnapshot

```text
project_id
snapshot_revision
confirmed_version_id | null
review_candidate_id | null
runtime_capabilities_digest
selection_projection_revision | null
updated_at
```

Snapshot 不复制完整模型，不合并两套 `vN`，不按导出格式切换版本链。Viewer/localStorage 不能写它。

### RuntimeJob

持久 job ID、kind、project/candidate scope、request hash、state、event cursor、checkpoint/result/error refs 和取消状态。事件只追加、可重放；大内容只引用 CAS。

### SkillExecutionReceipt

绑定 Bundle/Recipe/Operator/Validator/asset/SBOM/signature hash、canonical input/output、预算和结果。不记录模型 prompt 或任意执行环境。

### ApprovalReceipt

由 Runtime 接收 Codex write approval 后创建，绑定 user-visible summary、tool、project、base version、prepared object ID/hash、quality report、expiry、decision 和 session。它不证明模型身份，只证明本地审批事务。

## 3. 写入不变量

- 只有 Runtime 进程持有 SQLite/CAS 写权限；启动时先取得 OS 独占 writer 文件锁，第二实例返回 `RUNTIME_BUSY`；
- `prepare` 不移动 confirmed head；
- `confirm` 在单一 SQLite 事务中校验 base/hash/quality/approval/idempotency、写版本、更新 snapshot、追加 audit；
- 同一 idempotency key + request hash 返回同一结果；同 key 不同 hash 拒绝；
- stale base 不自动 rebase 或 last-write-wins；
- rejected/failed/expired candidate 永不确认；
- 质量报告只能附着其 input artifact/candidate hash；
- export 只能引用 confirmed version，或明确标记为 unconfirmed diagnostic；
- CAS 对象以内容 hash 寻址，DB 事务提交前验证存在/尺寸/hash；
- GC 只能删除无 reachability 的临时候选工件，不能删除已确认版本、审批、audit 或其依赖。

## 4. 局部修改

`SemanticChangeSet` 必须引用 base version、Part/MaterialZone/source-map 稳定 ID 和 allowlisted operation。Runtime 校验 scope 后生成新 candidate，重新编译受影响 DAG 并复用未影响 hash。不能接受任意 JSON pointer、vertex buffer patch、脚本或路径。

Viewer selection 只是提示；prepare 时必须重新绑定当前 snapshot/part。Part 已不存在或版本漂移时返回 typed conflict。

## 5. Undo、Reject、Restore

- `undo/redo`：只作用于同一未确认 candidate 的 typed change stack；
- `reject`：终止 candidate，不改 confirmed version；
- `restore_prepare(version_id)`：从历史内容产生基于当前头的新 candidate；
- `restore_confirm`：批准后创建当前头的子版本，历史版本保持不变；
- 禁止移动数据库指针伪装新版本，禁止覆盖旧 GLB/CAS 对象。

## 6. 爆炸图

默认爆炸图是由 confirmed AssemblyGraph 派生的 `ExplodedViewPlan`。临时距离只存在 Viewer；保存计划必须产生 candidate/change/approval/version。Plan 引用稳定 Part ID，不能以渲染 primitive 顺序作为唯一身份。

## 7. 导出

`export_prepare` 生成 manifest 与 CAS-backed artifact reference，绑定 confirmed version、format/profile、artifact hashes、validator/readback、license/provenance 和 toolchain。MVP `glb/mvp-glb` 的 `export_confirm` 原子确认 receipt 并返回 `output_sha256`，不写任意本机路径；filesystem/package target 属 MCP013。导出目录不得成为版本真值。

如果 Viewer、candidate、quality、export 的 version/hash 不一致，导出 fail closed。导出包不包含绝对本机路径、secret、prompt、原始 Codex attachment path 或未授权资产。

## 8. 旧数据

旧 `ConceptVersion`、`ModuleGraph`、`AgentAssetVersion`、Thread/Turn/Item、Provider 和 migrations 仅属于只读归档。新 Runtime V1 不自动打开旧 DB，也不把旧 `vN` 投影为当前 snapshot。

一次性离线工具可以读取备份、校验旧工件、生成中立 export manifest，再由用户显式导入新项目。失败不修改旧库或新库。用户数据删除需要独立明确授权。

## 9. 重启与灾难恢复

重启时 Runtime：取得 OS writer 文件锁 → 验证 DB migration/version → CAS reachability → snapshot/version hashes。MVP 不使用 TTL lease、heartbeat 或 stale takeover；未完成 Job 的跨 MCP 会话恢复暂不承诺，无法安全恢复的 Job 转为 typed failure。已确认版本必须在 MCP/Viewer 不可用时仍可离线备份和校验。

2026-08-14 Agentic observation binding：没有 active snapshot 时，Runtime 仅在项目没有候选或只有一个候选时保留兼容读取；多个候选必须显式绑定 candidate ID，否则 `scene_observe_get` fail closed。该规则只保护 canonical observation lineage，不代表 Agentic orchestrator、Repair execution 或视觉质量通过；证据为 `docs/evidence/mcp010f/agentic-observation-binding-20260814.json`。
2026-08-25 `FPS-FORM-04L` real-D1 truth：同 cohort 真实 fixture **1/1 PASS（477.56s）**，但 `stock-open-frame-clearance` 闭集 `0.30/0.35/0.42` 全部 `BLOCKED`。left/right/rear3q raw intrusion 分别为 `356/583/635`、`377/569/713`、`374/503/837` pixels；0.30 还产生 right outer-silhouette 回退，0.35/0.42 虽不回退仍远非零侵入。Runtime 已验证每个 trial 除上下梁 `position_m[1]` 外整个 GeometryProgram 不变，因此 depth/size/rear-cap 不变；所有 reference/CameraLock/registered-rig/registration/Worker lineage 不变。不可推进 FormQuality/secondary/confirm/version/export。证据：`docs/evidence/mcp010f/production-weapon-real-d1-stock-clearance-trials-20260825.json`。

2026-08-27 foundation typed importer truth：当前源码仅执行三个固定 hash、Runtime 内嵌的 GLB 母版源，已经产生 ForgeCAD-owned compact topology/socket/rig/PBR/animation CAS 对象和 `FpsPresentationPackage@1` 草案；真实 source probe 与 Runtime prepare/replay/get hash revalidation 均通过。该状态只允许写成 `SOURCE_RUNTIME_STRUCTURAL_PASS`。完整 AuthoringMesh half-edge 按 Part 物化仍为 `AUTHORING_MESH_MATERIALIZATION_PENDING`，包状态为 `DRAFT_UNREVIEWED`，质量为 `structural_only`，没有 candidate confirm、version、export、engine roundtrip 或真人 Hero Art Review。证据：`docs/evidence/mcp010f/fps-foundation-typed-importer-source-runtime-gate-20260827.json`。

<!-- forgecad-stage0: schemas=662 schema_set_sha256=202e080ec378ddb294eb9c880079dcec5c910b27a1c679034ca34c5a880dcec6 read_tools=131 write_tools=95 total_tools=226 task=FGC-MCP010F observation=QUALITY_TARGET_NOT_MET eligibility=BLOCKED_INCOMPLETE_BINDING evidence=INCOMPLETE_TRUTH_BINDING camera=MISMATCH packaged=PASS_CURRENT_COHORT_BOUND_READ_MODEL latest_attempt=real-codex-cli-current-20260815-b37-complete-auto-v3.json latest_completed=real-codex-cli-current-20260815-b37-complete-auto-v3.json -->
# 2026-08-30 架构权威增量

`WPN-ARCH-MCP-ROUTER-001` 已完成 source-level typed Router 和 Authoring 第一物理切片：默认 Knife
façade 先由 profile 验证，再引用 `forgecad-contracts::weaponry_domain_map` 的 domain/execution-target
真值，通过 `Runtime::invoke_weaponry_operation` 或 `weaponry_domain_operation` IPC 执行。兼容注册表没有
恢复为默认 Action Space。`runtime_status` 只由 MCP supervisor 投影，Runtime 不拥有该状态。

当前不能宣称五域重构完成：Surface 15 个 active operation 已由 direct typed service 承接，但 Evaluation、Presentation、Delivery 仍回落到旧 Runtime dispatch；
Store 仅 transaction、CurveGraph、EvaluatedMesh 三个 family 进入 borrowed AuthoringRepository；旧 handlers、
Runtime/Store/MCP 巨型根仍存在。本 Router 快照的 18 项旧基线失败已由
`WPN-ARCH-BASELINE-002` 的 554 passed / 0 failed / 37 ignored same-cohort full 结果取代，商业质量账本不变。
