# ForgeCAD 当前交接

2026-08-22 `CandidateAnimationVfxQuality@2` source/structural gate：Contracts **402**；Store focused **3/3**、Store full **112/112**；Runtime focused **6/6**、同源码同 cohort Runtime full **354 passed / 0 failed / 22 ignored**（376 total，115.40s）；MCP focused **4/4**、full **152 passed / 0 failed / 0 ignored**（2.49s）；contracts/runtime/store/MCP joint cargo check **PASS**。旧 `GEOMETRY_WORKER_PROTOCOL` 报告来自 stale Worker binary，已由同源码 Geometry/Render Worker 重建后清除。尚无真实 `Attachment@3 + Quality@2` public full-chain positive fixture，durable end-to-end=`NOT_RUN`/`BLOCKED_FIXTURE_CHAIN`；当前仅 `structural_only`，visual/artistic/commercial FPS=`NOT_PROVEN`，human/engine=`NOT_RUN`，不推进 stage/confirm/version/export。证据：`docs/evidence/mcp010f/candidate-animation-vfx-quality-v2-durable-source-gate-20260822.json`。

2026-08-22 `CandidateMaterialSurfaceQuality@1` public positive fixture：`Geometry → CandidateTopologyQuality@1 → AppearanceProgram@3 → TextureBuild@2 → SurfaceBake@1 → AppearanceSourceLineage@1 → CandidateMaterialSurfaceQuality@1` 的 `prepare → same-key replay → get → Runtime drop/reopen → restart get` 通过 **1/1（111.72s）**；Runtime focused **5/5**、Store full **74/74**、Contracts **350**。CAS inventory unchanged；stable `artifact_id` 与 GLB object SHA-256、MaterialPack CAS kind 精确区分，合法 UV/tangent rebuild 不计入 geometry-preservation 漂移。该结果仅为 `structural_only`；V2 animated-socket-particles 仍无完整 public `prepare → Store → restart get`，durable end-to-end=`NOT_RUN`/`BLOCKED_FIXTURE_CHAIN`；visual/commercial=`NOT_PROVEN`，human/engine=`NOT_RUN`，stage/confirm/version/export=false。证据：`docs/evidence/mcp010f/candidate-material-surface-quality-public-positive-source-gate-20260822.json`。

最终同 cohort 修订口径：强制 build cohort `724278fe8f6777c8b3d07bc5058208aee90aa5c700db5f9284d6297126fa79f6` 下 material focused **5/5（112.63s）**；Runtime full **310 passed / 0 failed / 20 ignored**（330 total，201.91s），且 public material fixture 明确在该 full run 内执行。此前 **111.72s** 仅为 public fixture 单测时长；两者都只支持 `structural_only`，不提升 visual/commercial、human/engine 或 stage/confirm/version/export 状态。

2026-08-22 `FictionalEnergyVfxAnimatedSocketParticlesSequence@2` 双候选 source slice：Contracts **350**；Store V2 focused **2/2**、Store full **74/74**；Runtime V2 仅低层 focused **6/6**、cargo check **PASS**；MCP V2 **3/3**；同 cohort `724278fe8f6777c8b3d07bc5058208aee90aa5c700db5f9284d6297126fa79f6` Runtime full **309 passed / 0 failed / 20 ignored**（191.06s）、MCP full **128 passed / 0 failed / 0 ignored**（1.93s），这些是全量回归，不是 V2 public `prepare → Store → restart get` 正向 fixture。V1/V2 隔离；V2 仅证明 1..16 frame、geometry/appearance 双 candidate/delivery/AnchorSet bridge 以及 Store FK/reachability/idempotence/conflict/rollback 的结构面。完整双候选 public Runtime `prepare → Store → restart get` 正向 fixture 尚不存在，durable end-to-end=`NOT_RUN` / `BLOCKED_FIXTURE_CHAIN`，不能声称正向 durable。该 slice 为 `structural_only`；visual/commercial=`NOT_PROVEN`，human/engine=`NOT_RUN`，stage/confirm/version/export=false。证据：`docs/evidence/mcp010f/fictional-energy-vfx-animated-socket-particles-v2-dual-candidate-source-gate-20260822.json`。

2026-08-22 `FGC-MCP010F-TRAILS-21` 当前为 source `in_progress`：已形成动态 animated socket Trails 与 TrailsBloom 的 closed Contracts、versioned Render Worker/Protocol、Store parent/frame 事务，以及 Runtime/MCP prepare/get focused source 实现；新增 animated GLB socket transform projection 的 Runtime-owned durable/replay/restart read-only source Gate。当前源真值为 Contracts **344**、**26/26 active operators**、MCP **79 read + 59 write = 138 tools**；同 cohort `9cc7c11b8309ee7cc76df3d67794c4692c81ff0e8ef064248955ad511b8ca388` Runtime full lib **307/307**（0 failed，另 20 ignored）、binary **1/1**，合计 **308 pass**，Store full **72/72**、显式固定 cohort 的 MCP full **125/125**。该 projection Gate 不等于 animated-socket Particles/Trails/TrailsBloom 正向集成；动态 VFX 链仍缺完整正向 durable `prepare → Store → restart get` 集成测试和独立回执，因此不得宣称 durable end-to-end PASS。整体保持 `structural_only`，不推进 stage/confirm/version/export，视觉/艺术/商业 FPS=`NOT_PROVEN`，人评/引擎=`NOT_RUN`。证据：`docs/evidence/mcp010f/game-weapon-animated-glb-socket-transform-projection-source-gate-20260822.json`。

2026-08-21 Animation/VFX quality handoff：`CandidateAnimationVfxQuality@1` 已将 material-surface head、delivery/AnchorSet、既有动画 GLB/receipt/animated socket 与完整 VFX frame stack 收进一个 durable、可重启复验的 fail-closed report。Runtime 不调用 `mechanical_animation_glb_prepare`，只消费已有 source pair；所有 dependency 检查均在 CAS reservation 前完成。该子门完成时的历史快照为 **319 schemas / 74 read + 54 write = 128 tools**，Store **55/55**、Runtime **284/284 executed + 20 ignored**、MCP **110/110**。真实状态是 socket attachment 阻断：sidecar-only VFX 使 20 项 gate 中 `vfx_glb_socket_attachment=false`，所以不推进 animation-vfx；下一刀是逐帧六 socket transform 与 emitter/trail binding，不是直接声称商业品质。

2026-08-21 Production-stage V2 handoff：`FGC-MCP010F-PRODUCTION-STAGE-V2-18` 已把通过 topology quality 的 immutable root，经显式 approval 与通过的 `CandidateMaterialSurfaceQuality@1`，原子重绑定为 distinct material-surface head。`ProductionStageTransition@2` 精确绑定 V1 parent、双 candidate/artifact/state、topology/material report CAS、reference/camera 与 receipt；Store transaction、reservation rollback、same-ID replay、conflict/stale 拒绝和不依赖 mutable current head 的历史 restart get 通过，V1 head 不改写。当前 **314 schemas / 73 read + 53 write = 126 tools**；Store full **51/51**，Runtime full **277 pass / 0 fail / 20 ignored**，同构建双 Worker MCP full **107/107**。这是 `structural_only` stage promotion，不是材质视觉成品：无 PBR likeness、真人视觉、Commercial FPS 或 engine round-trip，且不 confirm/version/export。证据：`docs/evidence/mcp010f/production-stage-transition-v2-durable-source-gate-20260821.json`。

2026-08-21 Production/Topology handoff：`FGC-MCP010F-TOPOLOGY-QUALITY-16` 已在独立 durable 轴上完成 `draft → gray-model → topology`。新增 candidate-wide `CandidateTopologyQuality@1`，Runtime 复读 exact GLB/V2 readback 与全部 Part 快照，Store 事务化 report/reachability/replay，MCP 提供默认只读 GET 与显式 write opt-in PREPARE；失败报告不推进，passing report 才可由独立 approval-gated transition 推进。当前 **303 schemas / 26/26 active operators / 71 read + 51 write = 122 tools**；Runtime full 已收口为 **268 pass / 0 fail / 20 ignored**。它证明 bounded evaluated-triangle 结构门，不证明 authoring cage/edge flow/艺术质量；每 Part 超过 512 faces 会明确 BLOCKED。下一步不是把 Appearance 伪装成同一 candidate，而是以 `CandidateMaterialSurfaceQuality@1` 精确绑定 source topology candidate 与 derived appearance candidate 的几何保持、2K PBR/surface-bake 和视觉/真人独立状态，再用 V2 stage head 原子重绑定 candidate。证据：`docs/evidence/mcp010f/candidate-topology-quality-durable-source-gate-20260821.json`。

2026-08-21 `GAME-ENGINE-IMPORT-13` evidence-only handoff：真实 Godot `4.7.2.stable.official.ed1daf0bf` headless importer 已对 4 个 GLB scene（LOD0/1/2/animated）通过 conformance；三档 triangles `304 > 176 > 112`、5 meshes/相同 materials、每 scene 6 non-rendering sockets 的 parent/local TRS exact、10 glTF channels→2 Godot semantic tracks、cross-loader t0/half-duration TRS exact、两个 named socket follow 和 5 个 `BoxShape3D` sidecar readback 均 PASS。static/animated 输入来自独立 Runtime source cohorts；这是 evidence-only，不创建 Runtime durable Godot link。commercial engine 为 false，Unity/Unreal、physics/hitbox、visual/human、confirm/export 仍 `NOT_RUN/false`；quality `structural_only`，总体质量阻断不变。当前 **291 schemas / 69 read + 49 write = 118 tools**。

2026-08-21 Animated GLB socket handoff：`GAME-ANIMATED-GLB-SOCKETS-12` 已新增 `game_weapon_animated_glb_socket_prepare/get`，把 candidate-bound `MechanicalAnimationGlbReceipt@1` 的 animated LOD0 source、delivery LOD0 与 AnchorSet 精确绑定，并在派生 animated GLB 中物化六个稳定命名 empty socket nodes。源/派生 animation projection、channels、samplers、renderable projection、source static projection 与 BIN bytes exact；Runtime-owned CAS 恰好只有 derived animated socket GLB + 新 receipt 两个对象。Three.js r185 `GLTFLoader` 已验证 source/derived parse、10 tracks exact projection，以及 half-duration 两个 socket parent-follow。当前 **290 schemas / 69 read + 49 write = 118 tools**。这是 structural-only、非功能性游戏美术证据：Unity/Unreal/Godot、runtime pivot、functional weapon semantics、human、confirm/version/export 与 visual quality 均 `NOT_RUN/false`，总体仍 `QUALITY_TARGET_NOT_MET` / `INCOMPLETE_TRUTH_BINDING` / camera `MISMATCH` / `BLOCKED_REFERENCE_COVERAGE`。证据：`docs/evidence/mcp010f/game-weapon-animated-glb-socket-materialization-source-gate-20260821.json`、`docs/evidence/mcp010f/threejs-game-weapon-animated-glb-socket-consumer-20260821.json`。

2026-08-21 GLB socket handoff：`GAME-GLB-SOCKETS-11` 已新增 `game_weapon_glb_socket_prepare/get`，固定覆盖 LOD0/1/2，在每份派生 GLB 中物化六个稳定命名 empty nodes；源 renderable projection 与 BIN bytes 保持精确，四个 owned CAS、SQLite parent/children、幂等/冲突/回滚、重启复验通过。Three.js r185 `GLTFLoader` 已真实消费三份 GLB 并验证六节点、Part/scene 父级和 local TRS。当前 **284 schemas / 68 read + 48 write = 116 tools**。它尚不是 Unity/Unreal/Godot roundtrip、运行时 pivot 或商业品质武器；动画 socket、人评、确认/版本/导出与视觉质量仍未运行或未达标。证据：`docs/evidence/mcp010f/game-weapon-glb-socket-materialization-source-gate-20260821.json`。

2026-08-21 TrailsBloom handoff：`VFX-TYPED-TRAILS-BLOOM-10` 已把现有 typed trails 接入独立固定 Bloom 派生链，新增 `fictional_energy_vfx_trails_bloom_prepare/get`、`trail-emissive-source` 与 `trail-bloom-contribution`，并保持原 trail color/ID/depth、基础九 AOV、原 HDR Bloom 和 particle passes byte-exact。forced durable 1/1 PASS（152.61 秒），Worker 1/1 PASS（27.88 秒），当前 **278 schemas / 67 read + 47 write = 114 tools**。输出是 RGBA8 HDR-profile-derived review passes，不是 FP16；商业引擎、foreign cohort、人评、确认/版本/导出与最终质量仍未通过。下一原子切片可做 GLB socket materialization 或真实引擎 import，但不得把 sidecar anchor 或本结构 Gate 写成商业资产视觉 PASS。

2026-08-21 VFX typed trails handoff：`fictional_energy_vfx_trails_prepare/get` 已消费严格有序的 durable particle history，固定生成 `muzzle-trail` 与 `energy-core-trail` 两条 typed polylines，并输出独立 `trail-color` / `trail-id` / `trail-depth`。current base/Bloom 用于当前显示与 opaque depth，历史 particle frames 保留各自 tick 的 base/Bloom lineage；所有帧须同 candidate/artifact/camera/profile/cohort/AnchorSet/node/owner/policy。forced durable 1/1 PASS（135.47 秒），Worker 1/1 PASS（25.17 秒），基础九 AOV、Bloom 与全部 particle passes 在前后 byte-exact 不变。当前 **271 schemas / 66 read + 46 write = 112 tools**。V1 `bloom_input=false`，segments 共享 parent trail ID；GLB socket、真实引擎消费回读、真人视觉门与最终质量仍未达标。

2026-08-21 继续执行 `VFX-FRAME-02`：已收口第一个底层子门 `render_glb_vfx_frame`。Render Worker 不接受 material index、shader、script、URL 或 path，只接受 1–8 个有界 emissive override，由 Render Core 在 GLB 中精确解析 MaterialZone + material ID。same-cohort 隔离 Gate 已通过目标性和字节确定性。下一子门是新版 typed frame sample 与 AppearanceProgram/LOD0 artifact 真值绑定；现在不能把这项写成 VFX profile 已完整执行或《无畏契约》视觉对标完成。

2026-08-21 当前 handoff：`VFX-PROFILE-01` 已完成 durable `FictionalEnergyVfxProfile@1`：显式写 `fictional_energy_vfx_prepare` 与默认只读 `fictional_energy_vfx_get` 精确绑定 delivery、AnchorSet 和 allowlisted 2K MaterialPack，保存两条有界 emissive 采样意图并通过 raw stdio、幂等、重启 readback、负数拒绝及 Three.js r185 线性采样数学。现行计数 **227 schemas / 26/26 operators / 58 read + 40 write = 98**。静态 MaterialPack 定义存在不等于 GLB MaterialZone 绑定；材质动画、Bloom、粒子、拖尾以及 Unity/Unreal/Godot 均未执行，不能写成对标《无畏契约》的视觉 PASS。下一原子任务应先做 typed VFX frame 执行与固定帧 Render Core 回执。

2026-08-21 当前 handoff：`GAME-DELIVERY-03` 已新增默认只读 `game_asset_lod_derive`：从精确 candidate/artifact/program/catalog/readback 绑定的 GeometryProgram 出发，仅降低白名单 typed tessellation 参数，临时派生 256/128/64-triangle 三档程序，每档 Worker 双回放字节一致，且不写 CAS/candidate/version。现行计数 **215 schemas / 26/26 operators / 56 read + 38 write = 94**。这是需显式 materialize 的 transient structural preview，不是通用 mesh simplifier/任意 GLB 减面；外观保持、Unity/Unreal/Godot、引擎 LODGroup/碰撞组件、用户外部 export、视觉/真人/HQ360 仍未通过。下一原子任务是 engine-neutral 武器 anchor/pivot/socket sidecar。

2026-08-21 上一 durable delivery handoff：MAT-04 与 `ANIM-GLB-01` 的结构能力保持；`GAME-DELIVERY-02` 已把 authored LOD0/1/2、LOD2 gameplay-only AABB、四个 sidecar 与可选动画绑定到专用 SQLite `GameAssetDeliveryLink`，CAS 可达性、同 manifest 幂等、冲突 retarget、跨项目拒绝和 Runtime 重启 `game_asset_delivery_get` 复验均通过。Three.js r185 已解析全部三档 GLB，回算 268/140/76 triangles，核对材质名、Part AABB 与动画采样。现行计数 **215 schemas / 26/26 operators / 56 read + 38 write = 94**。当前精确局限：不是 Runtime 自动 LOD 减面，动画只绑定 LOD0，正式 GC inventory 与 Unity/Unreal/Godot、引擎 LODGroup/碰撞组件、用户外部 export、视觉/真人/HQ360 仍未通过。该段的后续原子任务 Runtime-owned 自动 LOD 派生已在上方新 handoff 完成，不得宣称《无畏契约》视觉对标完成。

2026-08-20 MAT-03 handoff：actual embedded 2K texture atom 已完成，精确状态 `PASS_SOURCE_STRUCTURAL_EMBEDDED_2K_PBR_V1`。新 closed pack `forgecad-fictional-energy-weapon-2k` 保留旧 robot/weapon 512 路径；发布构建 raw stdio 实际产生 5 张 2048×2048 GLB PNG，strict reader 与 probe 都从 BIN 解码并核对 receipt/manifest，TextureBuildReceipt@2 持久绑定 7 source inputs/5 outputs。下一原子任务是 candidate-bound normal/AO bake 与 layer stack；不得把 synthetic microdetail 写成 bake、decal/wear/clearcoat texture 或商业视觉完成。source receipt：`docs/evidence/mcp010f/fictional-energy-weapon-2k-source-gate-20260820.json`。

2026-08-20 MAT-02 handoff：专用武器 pack 已从 per-triangle UV 升级到 artifact-wide connected dominant-axis islands；同 chart 共享边 UV 连续，所有 chart 在一个 2048 坐标 atlas 内按 8 texel padding 分配，曲面投影 overlap 会确定性拆 seam。GLB extras 的 chart IDs/hash 只作绑定，strict readback 仍读取实际 accessor/BIN 并验证 coverage/bounds/padding/continuity/overlap；raw same-cohort 524-triangle fixture 为 319 charts、9 AOV、external URI 0。下一原子任务是实际 2048×2048 embedded texture outputs；不得把本 UV policy 写成 2K texture、bake 或 PBR likeness 完成。

2026-08-20 当前最新原子交接为专用 `forgecad-fictional-energy-weapon@1.0.0` MaterialPack：closed MCP `material_pack_get`、Runtime selection/receipt、Worker material lowering 与 GLB root/material extras 使用同一 pack ID/hash，7 个 512px CC0 textures 和 7 个武器材质定义已 embedded-only 落地；Part/MaterialZone exact binding、texture hash/dimensions、external URI=0 及 path/URL/plugin/unknown/mixed-pack 负门通过。隔离 raw stdio 已实际用专用包完成 `appearance_prepare` 与 9 AOV。这仍不是最终 PBR：连续 UV、2K、bake、decal、wear、clearcoat texture、当前武器 candidate、视觉/人评/引擎/package/export-restart/HQ360 未完成。receipt：`docs/evidence/mcp010f/fictional-energy-weapon-material-pack-source-gate-20260820.json`。

2026-08-20 当前最新原子能力为 active `forgecad.geometry.bevel@2`：固定 Worker 对 direct `authoring-mesh@1` 的单个稳定 source edge 执行有界 1–4 segment/profile bevel；P0 只接受 closed、connected、outward、planar、strictly convex triangle/quad solid 且所选边端点 valence=3。segments=1/3 closed cube 为 16/24 triangles，strict GLB readback、solid、determinism 与 boundary/non-manifold/multi-edge/oversize/unknown executable field fail-closed 已通过。当前口径为 195 schemas / 26/26 active operators / 54 read + 36 write = 90 tools；Luna 最终复签 P0=0/P1=0。它未进入 Modifier/Agentic/Repair，也不代表当前武器、PBR、人评、引擎导入或视觉质量通过。receipt：`docs/evidence/mcp010f/fictional-energy-weapon-bevel-v2-source-gate-20260820.json`。

2026-08-20 `forgecad.geometry.energy-core@1` structural slice：固定 Worker 以 guard-ring / mechanical-ring / emitter-core / mechanical-backplate 四种 closed component 生成有界同心轴 Part，四节点 fixture 为 4 components / 768 triangles，boundary/non-manifold/winding 均为 0。该 slice 当时口径为 195 schemas / 25/25 active operators / 54 read + 36 write = 90 tools；Worker 60/60、Runtime 233/0/12 ignored、MCP 86/86 和 raw stdio 25/25 Gate 通过。该 operator 是非功能性科幻视觉组件语法，不代表成品武器、PBR、人评、引擎导入或视觉质量通过。receipt：`docs/evidence/mcp010f/fictional-energy-weapon-energy-core-v1-source-gate-20260820.json`。

2026-08-20 此前原子能力为 active `forgecad.geometry.vent-array@2`：固定 Worker 直接生成单连通、闭合流形的贯穿开槽面壳，前后对称 bevel 环和后置 backing sub-solid 均由 closed parameters 驱动；312-triangle 精确预算、逐槽孔洞、component、strict GLB readback、lineage、determinism、v1 compatibility、Runtime/Skill trust Gate 通过。该 slice 当时为 195 schemas / 23/23 active operators / 54 read + 36 write = 90 tools。backing 仍属于同一 mesh/PartOutput，不是独立 semantic Part；该 source structural Gate 不代表武器成品、PBR、人评、引擎导入或视觉质量通过。

2026-08-20 此前原子能力为 active `forgecad.geometry.panel@2`：固定 Worker 以闭合 typed parameters 生成 recessed floor、border、分段 bevel 与支撑环，严格 GLB 回读、lineage、catalog/program hash、负向关系与预算 Gate 通过；`hard-surface-detail@0.2.0` 的合同副本、operator lock、manifest、SBOM/provenance/development trust 已重物化并通过 Runtime Skill trust。该 slice 当时为 195 schemas / 22/22 active operators / 54 read + 36 write = 90 tools。这是非功能性科幻资产的 source structural Gate，不代表武器成品、PBR、人评、引擎导入或视觉质量通过。

2026-08-20 当前 Provenance Graph Viewer source Gate：认证 Tauri/Runtime IPC 新增 exact project/candidate/candidate-state/artifact-bound 图投影，Runtime 复用 confirmation-grade V2 几何链校验，并只在已有证据完整时接入 visual 与 immutable animation 分支。Viewer 提供紧凑纵向树、键盘导航和 hash 详情；切换候选使 request generation 失效，旧 success/error/finally 不能覆盖新候选。当前 195 schemas、MCP 仍 54 read + 36 write = 90，无新写工具。该结果只能写为 `PASS_SOURCE_READ_ONLY_STRUCTURAL_PROVENANCE_GRAPH`；`QUALITY_TARGET_NOT_MET` / `INCOMPLETE_TRUTH_BINDING` / camera `MISMATCH` / package/live UI / VoiceOver / 人评 / PBR likeness / export-restart / HQ360 状态均不改变。

2026-08-20 当前 Mechanical Animation Viewer follow-up source Gate：Viewer 在 immutable clip inspector 上增加显式离散 tick 的 Runtime-evaluated rigid frame。Tauri 只读桥调用既有 `mechanical_animation_clip_preview_get`，前端重验 candidate/artifact/clip/rest/action/cohort/tick、双 Worker replay 与 transient artifact hash；GLB 必须为每个预期 Part 提供唯一 identity node owner，embedded animation、Bone/SkinnedMesh、未知/重复/嵌套/nonidentity owner fail closed。Viewer 不本地求值姿态、不自动播放、不写 Runtime；异步旧 success/error/finally 已有可执行竞态负测。状态仅为 `PASS_SOURCE_READ_ONLY_VERIFIED_DISCRETE_RIGID_FRAME`，不是 Armature/skin/IK/NLA/F-Curve/GLB animation/Python parity 或视觉质量通过。

2026-08-19 当前最新原子能力为 candidate-bound `GeometryModifierApplyRequest@1`：它沿用显式 write-opt-in `geometry_prepare`，从 durable current-head candidate/CAS 重建 source program/readback，source/derived 各做两次 same-cohort Worker replay，仅替换一个 stable direct-source Part 的 terminal，保持所有非目标 Part/source/material/solid/triangle binding。Store 完成 30 秒 bounded same-key single-flight、CAS reservation 和终态 source/derived/sidecar/Part 二次校验；raw stdio 已通过 identical replay 与 Runtime/MCP 重启回读。该 2026-08-19 slice 当时为 191 schemas / 21/21 active operators / 54 read + 36 write = 90 tools；现行计数见本文顶部。该能力只产生 approval-gated reviewable candidate 与 immutable sidecar，不 confirm/version/export，不是 Blender Depsgraph/BMesh/Python/plugin parity 或视觉 PASS。

2026-08-19 historical 原子能力为显式 write-opt-in `authoring_mesh_edit_prepare`：它只接受 exact `authoring_mesh_edit_preview` 回执及 current head，在 Runtime 内重新计算 edit 并通过固定 Worker、strict readback 和 same-cohort Gate；Store 原子提交 reviewable candidate、Job/event/audit、V2 geometry evidence 与幂等结果。重复请求精确回放，键复用、stale head、跨 scope、CAS 漂移和可执行字段失败且不留下可见 candidate/version；成功也不 confirm/version/export。该 2026-08-19 slice 当时为 191 schemas / 21/21 active operators / 54 read + 36 write = 90 tools；现行计数见本文顶部；这是 approval-gated structural staging，不是 persistent editor、BMesh/Blender/Python/plugin parity 或视觉质量 PASS。

2026-08-19 historical 原子回执 `docs/evidence/mcp010f/blender-authoring-topology-edit-preview-source-gate-20260819.json`：默认只读 `authoring_topology_get` 从 candidate 的 durable V2 evidence 与有界 CAS 读取 exact `authoring-mesh@1` V/E/Loop/Face source topology；`authoring_mesh_edit_preview` 只支持 lexically sorted `translate_vertices` 与 boundary triangle/planar-convex-quad `single_face_extrude`，用 transient derived GeometryProgram 做 fixed Worker 双重 byte-exact replay 与 strict GLB readback，不写 candidate/version/CAS/SQLite。真实 raw stdio、closed Schema、1 MiB、stale/tamper/cross-scope 和 Python/path/URL/env/plugin/network 负门均 PASS。

数值口径：当前 source 为 **344 schemas / 26/26 active operators / 79 read + 59 write = 138 tools**；本文后续 334/134、319/128、290/118、284/116、271/112、264/110、257/108、231/100、229/99、227/98、221/96、215/94、210/92、204/91、201/90、197/90、195/90、193/90、191/90、187/89、177/84、175/83、173/82、170/80、168/79、166/78、164/78、162/77、160/76 只是 historical prior snapshot，不得用作现行能力计数。

2026-08-19 当前 Mechanical Animation Clip source Gate：显式 write-opt-in `mechanical_animation_clip_prepare` 将 bounded caller-authored rigid action 写为 Runtime-owned immutable CAS clip 和 SQLite Link，写入前由实际 Geometry Worker 对 source GLB 做两次 full-byte replay，并强制非空 same cohort；默认只读 get 可跨 Runtime 重启回读，preview 只允许 immutable schedule 内单 tick并执行 transient 双 Worker replay，且不写 candidate/version/CAS。receipt：`docs/evidence/mcp010f/blender-mechanical-animation-clip-source-gate-20260819.json`。该能力不是 Armature、skin、IK、NLA/F-Curve/driver/timeline、GLB animation channel、Blender add-on 或 Python runtime，视觉质量仍未通过。

2026-08-19 当前 Render Evidence Replay source Gate：默认只读 `render_evidence_replay_get` 对 exact integrity-bound candidate 的 GLB 先重做 Runtime strict readback，再通过实际 fixed Render Worker 同 cohort 连续渲染两次，对持久 RenderSet、first replay、repeat replay 的九 AOV 原始 PNG 字节与 decoded RGBA8 pixels 做 exact comparison。Gate 强制实际 Geometry/Render Worker 同 cohort 正路径，并验证 candidate/version/CAS inventory 无变化。返回只含 hash/size/status，不含 PNG/base64；材质绑定仅为 artifact-embedded-materials-only。这不是跨平台 determinism、Cycles/EEVEE/OCIO、PBR likeness、视觉质量或人评 PASS。

2026-08-19 当前 Mechanical Pose Geometry Preview source Gate：默认只读 `mechanical_pose_geometry_preview` 对 hash-bound caller-authored rigid rest/action 求值，按 `PoseWorld × inverse(RestWorld)` 生成每 Part delta，要求每个最终 Part output 只追溯到该 Part 的唯一 source nodes，再编译 transient derived GeometryProgram 并严格回读 GLB。它不写 CAS/SQLite/candidate/version，不把 derived GLB 暴露为 durable Artifact；Euler lowering 在 gimbal 附近拒绝。caller-authored rest frame 未证明原资产 rig/pivot/domain，故这只是 authored-rig structural preview，不是 Armature/skin/animation、Blender parity 或视觉 PASS。receipt：`docs/evidence/mcp010f/blender-mechanical-pose-geometry-preview-source-gate-20260819.json`。

2026-08-19 historical Subdivision artifact-lineage sidecar source Gate：`subdivision_artifact_lineage_prepare` 是显式 opt-in Runtime 写工具；它先复用 exact artifact reconstructed projection，再将 immutable `SubdivisionArtifactLineageSidecar@1` 写入 CAS，并以 `SubdivisionArtifactLineageLink@1` 绑定 exact request/candidate/artifact/readback/evidence/node hashes。Link 与 CAS reachable 提升同属一个 SQLite transaction；失败清理只允许本次 `created_new`、temporary、unlinked sidecar，不删除既有/linked/reachable CAS。filesystem CAS staging 与 SQLite 不宣称跨介质原子，MCP011 通用 GC 仍未实现。`subdivision_artifact_lineage_sidecar_get` 默认只读、重启可读且绝不 backfill；幂等重复返回同一 link，冲突或 CAS 篡改 fail closed。该 slice 当时为 173 schemas、20/20 operators、48 read + 34 write = 82；视觉与 package/live/human/360 状态不变。

2026-08-19 historical Subdivision artifact-lineage source Gate：新增只读 `subdivision_artifact_lineage_get`。它从候选的 durable V2 evidence 和 CAS 读回 persisted GeometryProgram/GLB/ArtifactReadback，重跑 strict confirmation chain，再由 fixed Worker 重编译并要求完整 GLB bytes 与 artifact 完全相同；仅对唯一 direct `subd-cage@2` source primitive 输出 control-root lineage 与 evaluated quad → primitive-local two-triangle mapping。该结果重启可重建、无 read-side write，但明确不是 persisted sidecar，不提供 glTF vertex/edge/corner ID、跨版本 ID 或视觉结论。该 slice 当时为 170 schemas、20/20 operators、47+33=80。

2026-08-19 historical Subdivision root-lineage source Gate（当时 168/79 cohort）：新增 `subdivision_topology_lineage_preview` 与 `SubdivisionTopologyLineageRequest/Result@1`。Worker 使用真实 `subd-cage@2` refinement 产生 control root → evaluated quad topology 映射；Runtime 重算并验证全部 counts/ranges/root coverage/crease chain/hash，MCP 只返回 bounded summary + 完整 structuredContent。最大 16×16 level2 为 22,802 elements 且低于 1 MiB；预算不足整体拒绝。该结果不是 artifact/GLB lineage，不含 corner、per-level child path 或 influence weights，不跨版本稳定，也不写产品状态；其 168 schemas、20/20 active operators、46 read + 33 opt-in write = 79 仅属于该 historical cohort，视觉/人评/package/live 状态不变。

2026-08-19 historical crease-aware Subdivision source Gate：`subd-cage@2` 是 ForgeCAD 自有、clean-room、实际由 Geometry Worker 执行的 3..16 规则开放网格折痕细分；整数 sharpness 1..2 按 level 衰减，最多 128 条 canonical interior edge，fixed `edge-only` boundary。`geometry_program_hash` 只生成 request-bound authoring program，真实 mesh 必须经 `geometry_prepare`，并已通过 deterministic 128-triangle GLB、strict readback 和 no-version prepare；Modifier Stack v1 未接纳 @2。该 slice 当时为 166 schemas、20/20 active operators、45 read + 33 opt-in write = 78 tools。该 Gate 仅为 source/structural PASS，不证明 Blender/OpenSubdiv parity、package/live、视觉、人评、PBR、export/restart 或 360。receipt：`docs/evidence/mcp010f/blender-subdivision-crease-source-gate-20260819.json`。

Boolean Operand Lineage 已形成独立只读 source slice：`boolean_operand_lineage_preview` 只调用 allowlist 内固定 Geometry Worker，输出 bounded left/right operand + evaluated-face runs；Runtime 不信任 Worker 自报语义，会从 exact GeometryProgram 独立重算 operation、左右输入与递归 source lineage，并对连续覆盖、计数、hash/canonical 与完整 MCP wire 1 MiB 上限 fail closed，测试证明 candidate/version/CAS 零写入。它不宣称 Blender 原始 authoring face lineage，不持久化到 GLB，也不改变当前视觉失败结论。receipt：`docs/evidence/mcp010f/blender-boolean-operand-lineage-source-gate-20260819.json`。

Luna Max 独立审查的 Render Evidence Integrity P1 已按源码闭环：新投影重读 reference/GLB CAS bytes，校验 ArtifactReadback artifact ID，并从 comparison 的八项 metrics 重新推导 QualityReport rows/status/hard-gate/eligibility；Schema consumer 不再能用任意八个 object 代替 threshold rows。

本轮 Render Evidence Integrity historical source receipt：`docs/evidence/mcp010f/blender-render-evidence-integrity-source-gate-20260819.json`。该 slice 当时为 162 schemas、19/19 active operators、44 read + 33 opt-in write = 77 tools。新只读 `render_evidence_integrity_get` 要求调用者提交 current candidate 的 exact ArtifactReadback/camera/RenderSet/comparison/quality object hashes，Runtime 逐对象和逐 AOV/mask 深度回读；`cas_object_sha256` 与 `bytes_sha256` 分开，九 AOV 固定顺序、RGBA8/512×512 与 beauty=sRGB/其余=data 全部 fail closed。该投影无 SQLite/CAS/candidate/version 写入，只证明 current source cohort 的完整性，不覆盖 attempt35 历史缺口。

本轮 Mechanical Pose Sequence Preview historical source receipt：`docs/evidence/mcp010f/blender-mechanical-pose-sequence-preview-source-gate-20260819.json`。该 slice 当时为 160 schemas、19/19 active operators、43 read + 33 opt-in write = 76 tools。现有只读 `mechanical_pose_evaluate` 新增序列分支，最多返回 16 个严格递增 tick 的 Runtime-recomputed local/world TRS samples；没有新 tool/operator/Skill/Worker/dependency，不生成 timeline/animation asset，不写 candidate/CAS/version。source/focused structural Gate PASS；package/live/Viewer/visual/human 仍 `NOT_RUN/QUALITY_TARGET_NOT_MET`。

上一原子项 Parametric Group v2 source receipt：`docs/evidence/mcp010f/blender-parametric-group-v2-source-gate-20260818.json`。该历史 slice 当时为 158 schemas、19/19 active operators、43 read + 33 opt-in write = 76 tools。`geometry_program_hash` 可将 rounded-box、mirrored-box、arrayed-cylinder 三个固定 first-party group template 确定性 lowering 为现有 GeometryProgram；template/catalog/parameters/instance/program/source-map 全部 hash 绑定。它默认只读，未新增 tool/operator/Skill/dependency，不允许嵌套 group、field、Python、动态插件、路径、URL 或网络。

上一原子项 Mechanical pose source receipt：`docs/evidence/mcp010f/blender-mechanical-pose-source-gate-20260818.json`。该历史 slice 当时为 156 schemas、19/19 active operators、43 read + 33 opt-in write = 76 tools；`mechanical_pose_evaluate` 为 candidate/artifact/readback/Part/source-node 全绑定的默认只读工具，支持有界 fixed/revolute/prismatic hierarchy 和整数 tick linear/clamp action。它不调用 Worker、不写状态、不生成几何或动画资产。

上一轮 RenderProfile/AOV source receipt：`docs/evidence/mcp010f/blender-render-profile-aov-lineage-source-gate-20260818.json`。

终审补强：RenderSet Schema/Runtime 的字段、AOV 顺序和逐 pass color space 已精确对齐；Runtime adapter 真实解码每个 PNG 并要求声明尺寸与 RGBA8 完全一致；Render Core 对 mesh/material index 强制 0..255 palette domain，溢出不渲染。

2026-08-18 historical RenderProfile/AOV lineage handoff：主代理验收三路 Luna Max 对 Blender/Cycles/EEVEE pass 与颜色管理语义、现有 Render Worker/Runtime 缺口的只读审计后，完成 ForgeCAD 自有 fixed `RenderProfile@1`。Render Worker `render_glb` response 返回完整 profile，Runtime 严格比对并让两个 `RenderSet@2` producer 复用同一 profile 与 canonical/AOV/color-pipeline/ID-palette hashes；beauty 是唯一 display-color pass，其余八个 AOV 均为 non-color data。该 slice 当时为 152 schemas、19 active operators、42 read + 33 opt-in write = 75 tools；无新 MCP tool、无持久写入、无 Blender/Cycles/EEVEE/OCIO/GPU/EXR parity。Subdivision v2 的 151-schema receipt 作为历史 slice 保留。

2026-08-18 historical Modifier evaluation v2 handoff：主代理已验收三路 Luna Max 的 Blender Modifier/Depsgraph、dirty/cache 与 ForgeCAD 边界审计，并以 clean-room typed contract 实现只读语义评价。`GeometryModifierEvaluationRequest@2` 接受可选的 canonical previous signature，返回完整/有效/GeometryProgram output/policy/catalog/cache hash、逐 stage chain、dirty reasons 和 `initial-miss | reusable | invalidated`；`reuse_kind=semantic-signature-only`，cache key 绑定 program hash。有效节点 ID 按 enabled chain 稳定生成，禁用项纯 trace 变化不制造 program/hash 失效；previous scope 的 project/representation/Part/material/solid 必须一致，base operator/parameters 已在 Schema/MCP 成对约束。上一签名不是 Runtime 持久状态；没有真实 mesh cache、GLB readback 或 build-cohort reuse 证明，focused no-write 证据覆盖 candidate/version/CAS inventory，未覆盖 SQLite 全表与 Job inventory。该 slice 当时机器真值为 149 schemas、19 active operators、42 read + 33 opt-in write = 75 tools；focused source Gate PASS，其后续 Subdivision v2 已由上一段完成。receipt：`docs/evidence/mcp010f/blender-modifier-evaluation-v2-source-gate-20260818.json`。视觉真值继续 `QUALITY_TARGET_NOT_MET` / `INCOMPLETE_TRUTH_BINDING` / camera `MISMATCH`，`HQ_360=BLOCKED_REFERENCE_COVERAGE`。

2026-08-18 Blender Bevel/Normal handoff：主代理验收三路 Luna Max 的 Blender 官方/GitHub 语义、Worker/Render/Runtime 边界与仓库漂移审计后，以 clean-room 方式实现 `bevel@1 + normal-policy@1`；冻结 Blender commit 不变，未复制/链接/执行 GPL/Python。Bevel v1 只接受 direct `primitive@2` box，生成全部 12 edge 的 bounded rounded box；Normal v1 将 mesh 展开为 corner domain，以 face-area × interior-corner-angle 加权并按 crease threshold 分离硬边。该 slice 当时为 146 schemas、19 active operators、42 read + 33 opt-in write = 75 tools；focused Worker/Runtime/MCP/Skill Gate PASS，package/live/render AOV/人评未运行。其下一原子项 Modifier evaluation v2 已由上一段完成；不得扩写为任意网格 Bevel、Blender Weighted Normal、Cycles/EEVEE、动画骨骼或 Python ecosystem 已完成。receipt：`docs/evidence/mcp010f/blender-bevel-normal-source-gate-20260818.json`。

2026-08-18 Blender TopologySnapshot handoff：主代理已验收三路 Luna Max 的 Blender Mesh/BMesh 语义、现有 Worker/Runtime 接口与安全 Gate 审计；clean-room 边界仍固定 Blender commit `72ccdd6e96ca119a1ffa3372559cc5654343b477`，未复制/链接/执行 Blender GPL/Python。新增 `topology_snapshot_get`，只从通过 `ArtifactReadback@2` 的 candidate-bound GLB 投影单 Part、最多 512 faces 的完整 V/E/F/C 与 corner shading attributes；所有 lineage/policy hash 绑定，超限不截断，无持久写入。ID 仅 artifact-bound，Boolean/SubD 只写 evaluated/partial lineage，不冒充 authoring topology。该 slice 当时为 146 schemas、17 active operators、42 read + 33 opt-in write = 75 tools；其“下一原子项”已由上一段 Bevel/Normal slice完成，当前 catalog 为 19/19。receipt：`docs/evidence/mcp010f/blender-topology-snapshot-source-gate-20260818.json`。视觉真值仍 `QUALITY_TARGET_NOT_MET` / `INCOMPLETE_TRUTH_BINDING` / camera `MISMATCH`。

2026-08-18 Blender clean-room handoff：三路 Luna Max 的官方/GitHub/仓库审计已由主代理验收；统一使用 Blender commit `72ccdd6e96ca119a1ffa3372559cc5654343b477`，Blender GPL 源码保持 reference-only。源码新增只读 `GeometryModifierStackRequest@1 → GeometryModifierStackProgram@1`：有序 transform/mirror/array modifier 可启停、ID 唯一、input/program/stage/canonical hash 绑定并 lowering 到现有 GeometryProgram@2；无 candidate/CAS/SQLite/version 写入。该 Modifier Stack slice 当时机器真值为 144 schemas、17 个 source-canonical active operators、41 read + 33 opt-in write = 74 tools；该后续 snapshot 当时为 191 schemas、21/21 active operators、54 read + 36 opt-in write = 90 tools；现行机器真值见本文顶部；下文更早的 136/138/139 与 16-operator 数字仅是 historical cohort/receipt。focused `contracts:check`、Runtime、MCP Gate PASS。后续 TopologySnapshot、bounded bevel/normal、Modifier evaluation v2、Subdivision v2、RenderProfile/AOV、Mechanical pose、Subdivision artifact-lineage 与 Authoring Mesh source slices 已按路线逐项完成；不得把路线图写成 Cycles/EEVEE/BMesh/Armature/skin/Python 已配置。source receipt：`docs/evidence/mcp010f/blender-modifier-stack-source-gate-20260818.json`，路线：`docs/BLENDER_CAPABILITY_ADAPTATION_PLAN.md`。视觉/人评/package 状态不变。

Reference Visual Structure source receipt：`docs/evidence/mcp010f/reference-visual-structure-source-20260817.json`。

2026-08-17 Reference Visual Structure historical slice handoff：该 slice 新增 `ReferenceVisualStructure@1` 并作为 `SilhouetteTarget@1.visual_structure` 可选字段由现有 `reference_mask_prepare`/`reference_mask_refine_prepare` 写入和 `silhouette_target_get` 回读。该结构以全局轮廓优先、视觉几何而非功能部件、允许区域 overlap/shared boundary、开放 line-flow 和 continuity group 为硬约束；Runtime 负责 policy constants、review status 与 canonical hash。该 slice 当时为 139 contracts、41 read + 33 opt-in write = 74 tools；现行计数见本 handoff 顶部。下一安全动作是用用户确认后的 normalized contour/regions/line-flow 创建新 target，再按 continuity group 生成 Primary Form profile；这不是 candidate、视觉、PBR、人评或 360 PASS。

2026-08-17 Fictional Energy Rifle Profile source handoff：`FictionalEnergyRifleProfile@1`/`FictionalEnergyRiflePlan@1` 已加入当前合同；Profile 只允许原创 `fictional-game-asset`、强制 `nonfunctional_asset=true`，并把 bounded intent 展开为现有 PDK 请求，禁止 candidate/CAS/SQLite/version 写入。receipt：`docs/evidence/mcp010f/fictional-energy-rifle-profile-source-20260817.json`。该 source/structural authoring aid 不改变 Stage 0 的 `QUALITY_TARGET_NOT_MET`、`INCOMPLETE_TRUTH_BINDING`、camera `MISMATCH` 或 `HQ_360=BLOCKED_REFERENCE_COVERAGE`；该 slice 当时为 144 contracts、41 read + 33 opt-in write = 74 tools，现行计数见本 handoff 顶部。

2026-08-17 Weapon joint-multiview optimization source Gate：已把激光步枪参考的下一步从继续堆 Detail Grammar 收敛为 Runtime-owned 相机/坐标/联合评估底座。新增固定 weapon frame、`CameraCalibration@2` orthographic projection、`CameraRigCalibration@1` 六向 identity coverage、`OptimizationIntent@2`/`OptimizationEvaluation@2`/`OptimizationResult@2`，并在一次 batch render 中对所有 view 做 weighted + worst loss 评估；promotion 必须满足所有视图 non-regressing、primary strict improvement、至少三视图 strict improvement 和 aggregate strict improvement。receipt：`docs/evidence/mcp010f/weapon-joint-multiview-optimization-source-20260817.json`。该 slice 当时为 136 contracts、41 read + 33 opt-in write = 74 tools；focused source Gate 与 Stage0 PASS。完整 Runtime suite 仍为 162 passed / 15 failed / 12 ignored，失败来自隔离 Geometry/Render Worker 不可用；尚未运行真实多视图参考、PBR、人评、export/restart 或 360，不能宣称高质量视觉通过。

2026-08-17 Weaponry laser-rifle reference intake：用户提供并授权了 SHA-256=`1964704a62ed7a841b4d49c370b8d46f4626e201daad29092a9c39a40b4c4109` 的 1491×1055 多视图科幻激光步枪参考板，覆盖 left/right/front/back/top/bottom、front/rear-three-quarter 及 receiver/muzzle/energy-core 三组近景。Codex-side `ForgeCADCodexReferenceInventory@1` 已记录 17 个细节（14 observed、1 inferred、2 unknown），并通过 OperatorCatalog/MaterialPack 校验；证据与验证回执为 `docs/evidence/mcp010f/weaponry-laser-rifle-reference-inventory-20260817.json`、`docs/evidence/mcp010f/weaponry-laser-rifle-reference-inventory-validation-20260817.json`，编排计划为 `docs/evidence/mcp010f/weaponry-laser-rifle-reference-intake-plan-20260817.json`。当前 `capabilities_get`/Runtime/doctor 显示 Ready，但实际会话工具面未暴露 reference import/project create/geometry prepare/appearance prepare/reference compare prepare，故状态保持 `BLOCKED_LIVE_WRITE_TOOL_EXPOSURE_MISMATCH`；没有创建 project/candidate、没有写 Geometry/Appearance/CAS、没有确认或质量 PASS。安全下一步是刷新/重启 MCP 会话后复核 exact tool manifest，再绑定参考并按 barrel→receiver→energy-core→grip→stock→muzzle 顺序进行有界比较；cyan emissive 与 gold accent 目前也没有可直接匹配的 active MaterialPack ID。

2026-08-17 Weaponry Blockout 01：在不改变 Runtime/MCP/Viewer 边界的前提下，使用同 cohort 的 product-owned Geometry Worker 编译原创模块化科幻步枪块体；12 个语义 Part、17 个 typed 节点、1932 triangles，OperatorCatalog/program/readback hash 已绑定，临时 GLB 严格回读与固定 Render Worker 预览通过。该切片没有 Runtime candidate/CAS/version/confirm/export，也没有用户武器参考，因此 visual likeness、PBR、人评与 360 均 `NOT_RUN/BLOCKED`。计划、hash-free GeometryProgram draft 与 receipt：`docs/evidence/mcp010f/weaponry-modular-rifle-blockout-plan-20260817.json`、`docs/evidence/mcp010f/weaponry-modular-rifle-geometry-program-draft-20260817.json`、`docs/evidence/mcp010f/weaponry-modular-rifle-blockout-receipt-20260817.json`。下一步是用户提供授权参考或确认原创 brief，并创建独立 Runtime project 后再走 `geometry_prepare`。

2026-08-17 Weaponry Primary Form 02：在保留同一 12 个语义 Part、同一 Operator Catalog 与 Worker cohort 的前提下，新增受限 profile/panel/joint 轮廓层级，GeometryProgram 从 17 节点/1932 triangles 提升为 25 节点/2780 triangles；编译、GLB strict readback、UV/tangent、9 个 512×512 AOV 和重复编译确定性均 PASS。另完成 6 个 AppearanceProgram@1 材质区的可读性修正，最终 GLB hash 为 `799f7ce0…bd221`。第一次 optic mount joint-stack 变体被 `NON_MANIFOLD_EDGE` 严格拒绝，已改为 typed panel 并复跑通过。该切片仍是离线 structural preview：没有 Runtime candidate/CAS/version/confirm/export，没有用户授权武器参考，visual likeness、人评、生产导出与 HQ_360 继续 `NOT_RUN/BLOCKED`。receipt：`docs/evidence/mcp010f/weaponry-modular-rifle-primary-form-optimization-receipt-20260817.json`；draft：`docs/evidence/mcp010f/weaponry-modular-rifle-primary-form-draft-20260817.json`、`docs/evidence/mcp010f/weaponry-modular-rifle-appearance-draft-20260817.json`。

2026-08-17 Weapon Detail Grammar + Close-up Detail Evidence：没有继续堆普通 primitive，新增 Codex-side `ForgeCADWeaponDetailGrammar@1` 编排草案，按 primary/secondary/tertiary/micro 分层，定义 panel-break、inset-groove、vent-port、mechanical-interface、cable-transition、ornament-relief、crystal-seat、emissive-channel 的边界、失败模式和 close-up 证据要求；未激活为产品 Skill、未写 Runtime/CAS。基于 Primary Form 02 GLB 生成 receiver/energy/barrel/stock/optic 六组 512×512 九 AOV 近景传输证据；receiver、barrel、optic/vent 可观察，energy-core 近景重拍后仍 `OCCLUDED_NOT_OBSERVABLE`，stock 为 `PARTIAL_FRAMING_STRUCTURAL_ONLY`。该结果是局部可观察性证据，不是 feature-line、PBR、人评或视觉 likeness 通过。文件：`docs/evidence/mcp010f/weaponry-detail-grammar-20260817.json`、`docs/evidence/mcp010f/weaponry-closeup-detail-plan-20260817.json`、`docs/evidence/mcp010f/weaponry-closeup-detail-evidence-receipt-20260817.json`。下一项最小修正是用户确认 brief/参考后，对 energy-core 做一个 bounded window/seat Part 提案。

2026-08-17 `FGC-MCP010F` orchestrator dispatch repair：Runtime IPC 现已把 `design_stage_run_prepare`/`design_composition_prepare` 接入 typed dispatch，并以 regression test 防止已暴露工具被错误包装为 `INVALID_INPUT`；`DESIGN_`/`REPAIR_`/`ACTION_` 等 bounded failure family 也以稳定码穿过 IPC。Dev.app 同 cohort `906c8decb7aec90e8854bb0b7eb4d650dd3895851b556a62a1c8d16557aa17c8` 的真实授权参考隔离 probe 通过：1 个 stage child ActionRun、`view_spec` 转发、Runtime 自动 `RuntimeParameterPatch@1`、batch/replay hash 一致、source candidate 未变、version=0。该证据仍是编排/传输 Gate；严格视觉结果为 `QUALITY_TARGET_NOT_MET`，live Desktop restart、真实多视图、PBR、人评、export/restart 与 `HQ_360` 仍未运行或阻断。receipt：`docs/evidence/mcp010f/dev-app-install-orchestrator-dispatch-fix-20260817.json`、`docs/evidence/mcp010f/current-source-real-reference-orchestrator-dispatch-fix-20260817.json`。

2026-08-17 PDK v0 source slice：Runtime 在现有只读 `geometry_program_hash` 边界新增 `ParametricDesignKitRequest@1` 分支，覆盖 housing/panel/frame、vent、joint、sensor 六类 typed macro。每次展开都校验 `representation_plan_sha256` 与 `input_sha256`，用固定 Operator Catalog 生成一个有界 `GeometryProgram@2`，并返回 exact `source_map`、`program_sha256` 与 `ParametricDesignKitProgram@1` canonical hash；Panel 厚度/倒角、vent 槽位、joint 环距、尺寸/位置/旋转/segment/count 均有有限值和几何关系 Gate。Runtime focused tests、MCP schema oneOf/read-only test、cargo check、129-schema checker 与完整 `script/test_mcp010f.sh` source Gate PASS。源码变更后的 Dev.app/package 已以 cohort `6f00a58a…ed50` 重建/安装，隔离 Ready/preflight/project probe 与 packaged PDK read-only round-trip PASS；receipt：`docs/evidence/mcp010f/parametric-design-kit-v0-source-gate-20260817.json`、`docs/evidence/mcp010f/dev-app-install-pdk-v0-20260817.json`、`docs/evidence/mcp010f/dev-app-probe-pdk-v0-20260817.json`、`docs/evidence/mcp010f/dev-app-probe-pdk-packaged-read-only-20260817.json`。这只是 structural authoring aid，未创建 candidate/CAS/SQLite/version，不替代 `geometry_prepare`、reference compare、PBR、人评、export/restart、live restart 或 HQ_360。

2026-08-17 historical package-cohort raw manifest/OptimizationJob recheck：最终 Dev.app cohort `ce45110e3a5e6eaa5b5283e61f430e2338c7f06a2d09f4e75d4a21cb924f6a86` 的隔离 raw stdio probe 已按生成 manifest 对 `tools/list` 做 exact-name equality（41 read + 33 opt-in write = 74，manifest canonical `7ced259e…c526b609`），MCP/Runtime cohort exact-match；`OptimizationJob` 完成 39 evaluations（32 coarse + 4 mid + 3 final），Job/result=`succeeded/done`，proposal=`blocked-no-improvement`，无 candidate confirm/version/export、无持久用户数据副作用。receipt：`docs/evidence/mcp010f/optimization-raw-manifest-dynamic-20260817-receipt-v2.json`。该项只关闭该 package cohort 的 manifest/transport 漂移，不升级真实视觉质量；live Desktop restart、PBR、人评、export/restart hash 和 HQ_360 仍未运行或阻断。

2026-08-17 P2 typed surface-finish package handoff：`hard-surface-finish-v1` 已安装为 Dev.app cohort `ce45110e3a5e6eaa5b5283e61f430e2338c7f06a2d09f4e75d4a21cb924f6a86`；install/probe、同 cohort Runtime Ready、Ponytail preflight、project readback 与无持久用户数据副作用均 PASS。receipt：`docs/evidence/mcp010f/dev-app-install-parametric-surface-finish-20260817.json`、`docs/evidence/mcp010f/dev-app-probe-parametric-surface-finish-20260817.json`。下一步必须由用户安全重启 Codex Desktop 后复核 live cohort，再提供 front/back/left/right/rear-three-quarter 授权参考；不要把隔离 package transport 当成 visual likeness/PBR/human/export/HQ_360 PASS。

2026-08-17 P2 camera/observation binding follow-up：最新 Dev.app cohort `18c9fb86cafc7e7baf2356e2efe9db404e6530fdeae54d33c0a9beba94fbae40` 已安装；RenderSet 相机 CAS 与 RepairIntent observation hash 均已绑定。真实授权参考的隔离 RepairIntentRun 已完整走完 compile/readback/render/compare/evaluate，结果因视觉严格门 `QUALITY_TARGET_NOT_MET` blocked，未写入 source candidate、未 confirm/version/export。receipt：`docs/evidence/mcp010f/real-reference-repair-intent-run-observation-cas-20260817.json`。live Codex Desktop 仍需用户安全重启后再复核。

2026-08-17 P2 RepairIntent run handoff：新工具 `repair_intent_run_prepare` 先读取并校验 CAS `RepairIntent@1`，再绑定当前 session/candidate/observation/source evidence/reference/camera，执行受限单 Part `bounded-repair` 的 `compile → readback → render → compare`。它只返回隔离 staged candidate，不能覆盖 intent，且 `confirm_allowed=false`、`source_candidate_unchanged=true`；后续 `repair_apply_prepare`/confirm 仍须独立审批。source/contract/full Gate 与最终 Dev.app 的 packaged positive transport PASS；相机 CAS 与 observation hash 修复后真实参考已完成 evaluate，随后按严格视觉门 `QUALITY_TARGET_NOT_MET` blocked，live restart、真实多视图与 likeness/PBR/human/export/360 仍未完成。

2026-08-17 P2 package handoff：最新 Dev.app cohort `18c9fb86cafc7e7baf2356e2efe9db404e6530fdeae54d33c0a9beba94fbae40` 已安装，install PASS；真实授权参考双 MCP 会话的 `repair_intent_run_prepare` transport receipt 为 `docs/evidence/mcp010f/real-reference-repair-intent-run-observation-cas-20260817.json`，已完成至 evaluate 后按严格视觉门 blocked、无 confirm/version/export。live restart 仍 `NOT_RUN`，不要把 transport PASS 写成视觉 PASS；旧 cohort `ad4837…` receipt 按历史范围保留。

2026-08-17 core-view coverage handoff：`coverage_status=complete` 且 `hq_360_status=eligible` 现在必须同时声明并提供 `front/back/left/right/rear-three-quarter`；perspective 等补充视图不能替代 rear-three-quarter。source/schema/negative fixture/focused tests 通过，receipt：`docs/evidence/mcp010f/core-hq-reference-coverage-contract-gate-20260817.json`。真实多视图授权参考仍待用户提供。

2026-08-17 annotation-readiness handoff：自动 flood-fill 只能进入观察/探索；要让单视图比较具备 `READY_PARTIAL_VIEW` 资格，必须由用户显式确认 contour，并同时提供 observed Part region、至少 3 个 observed target/view landmarks、observed view regions 与 canonical camera hash binding。缺任一项，Runtime 会输出 `BLOCKED_USER_CONFIRMATION_REQUIRED`/`QUALITY_TARGET_NOT_MET`，不会把自动标注提升为 benchmark。source/contract/MCP010F/release baseline 已通过，receipt：`docs/evidence/mcp010f/reference-annotation-readiness-source-gate-20260817.json`。这不替代真人评审，也不解除当前 Desktop restart、真实 likeness、PBR、export/restart 或 360 gates。

2026-08-17 annotation-readiness package handoff：P1 源码已构建/安装为 Dev.app cohort `896105d3ab204babd415738bf66f572ea5c4be41df1ee83a26da3deed6bb42c7`，保留旧包；同 cohort isolated probe 为 PASS，live Codex Desktop 仍未重启。用户下一步只需安全重启后先复核 `capabilities_get → skill_get ponytail-preflight@0.1.0 → tools/list`，再提供用户确认的多视图标注；在此之前不要进行写入设计动作。

2026-08-17 live mismatch after package handoff：当前 `capabilities_get` 仍来自旧 Runtime/MCP `41945c…`/`3e6deb…`，与 `896105d3…` 安装包不一致，且旧连接仍返回 writes enabled；不要调用任何设计/写入工具。只读诊断 receipt：`docs/evidence/mcp010f/live-cohort-mismatch-after-annotation-package-20260817.json`。

2026-08-17 final-package isolated Primary Form trial：最终 `820a542fabe3503f0b8cf6c86e84f578e7b074d79d07401a9a3f865dc60709b4` 在同一用户授权 perspective、同一 canonical camera 上完成 `pelvis → upper-arm-pair` 两步 Runtime-owned Primary Form job；receipt `docs/evidence/mcp010f/isolated-final-cohort-primary-form-pelvis-upper-arm-20260817.json`（SHA `da3de253f56b484e1c8d9f7f3e98ee32a9c1d6860cd204e123a1a85db4373713`）。两步各 64 evaluations，第一步 `accepted + strict_improvement` 并生成 staged candidate，第二步 `no_improvement/retained_source`；无 confirm/version/export、无持久用户数据/无无关副作用。该 receipt 只证明 bounded repair transport，当前比较仍 IoU `0.661785435407`、Boundary F1 `0.249419639555`、`QUALITY_TARGET_NOT_MET`，仅单视图且 360 继续 blocked。

2026-08-17 final-package isolated real-reference observation：使用最终 `820a542fabe3503f0b8cf6c86e84f578e7b074d79d07401a9a3f865dc60709b4` 的 MCP/Runtime/Geometry Worker/Render Worker，在隔离 Runtime 中完成真实用户授权 perspective 的 `preflight → import → geometry/readback → mask/camera → ReferenceCanvas/DesignSpec → scene_observe → SilhouetteRig`；receipt 为 `docs/evidence/mcp010f/isolated-final-cohort-observation-20260817.json`，SHA `68d156c662c717e762a1ee61a9a17995236c906a10f53a0ffd58035487ff0423`。结果 `PASS_BOUNDARY_ONLY`、`EXPLICIT_CODEX_AUTHORING`、binding=`bound`、无持久用户数据/无无关副作用；`quality_visual_status=NOT_RUN`、`silhouette_gate=QUALITY_TARGET_NOT_MET`，仅 perspective，`HQ_360=BLOCKED_REFERENCE_COVERAGE`。该隔离 receipt 证明最终包 transport，不解除当前 Desktop 旧 cohort 的 restart gate，也不宣称 likeness/PBR/人评/export/360 PASS。

2026-08-17 package alignment handoff：当前源码新增能力广告、`tools/list` 与 `tools/call` 的 cohort fail-closed 后，Dev.app 已重建到最新 cohort `820a542fabe3503f0b8cf6c86e84f578e7b074d79d07401a9a3f865dc60709b4`；install/probe receipts 为 `docs/evidence/mcp010f/dev-app-install-capability-tools-list-fail-closed-v2-20260817.json` 与 `docs/evidence/mcp010f/dev-app-probe-capability-tools-list-fail-closed-v2-20260817.json`，结构性 package Gate PASS，隔离 mismatch transport 证实 `tools/list=41` 且 `write_count=0`。Codex Desktop 现有 MCP 连接未重启，仍记 `NOT_RUN`；不要把 package receipt 写成 real multi-view/visual PASS。

2026-08-17 live cohort handoff：最新现场 `capabilities_get` 证明已安装 package cohort `820a542fabe3503f0b8cf6c86e84f578e7b074d79d07401a9a3f865dc60709b4` 与 live Runtime cohort `41945cc48f600abb1601e82ed11ec116b5f6c55a6066f6777268c2d540c705c7`、live MCP cohort `3e6debdabdb56dff44e630918553dcfbaf9d2ae0503c23d144307eed33c53b34` 不一致，故 `build_cohort_match=false`。`skill_get ponytail-preflight@0.1.0` 只读 PASS；新版 source/package 已把能力广告、`tools/list` 与 `tools/call` 一并 fail-closed，但当前旧连接仍不能用于设计动作。需要用户安全重启 Codex Desktop 后按 receipt 中顺序重查 capabilities、preflight、Skill list 和 Operator catalog。receipt：`docs/evidence/mcp010f/live-cohort-mismatch-capability-package-20260817.json`。

2026-08-17 next handoff — per-view annotation lineage：`ReferenceCanvas@1` view 级 `ReferenceViewSpec`、target/mask 与 camera canonical hash 已由 Runtime 校验并投影；cross-view comparison 按各自 target 读取，禁止跨视图回退到全局 mask，且每个 supplied view kind 现在必须恰好对应一个 authored view card，重复/extra/missing kind 均 fail closed。source/package/MCP010F Gate 通过，最新 package receipt 为 `docs/evidence/mcp010f/reference-coverage-exact-binding-capability-package-20260817.json`；尚无真实 front/back/left/right/rear-three-quarter 授权参考的新 receipt。Desktop restart、视觉 likeness、PBR、人评、export/restart、VoiceOver、360 仍未运行或阻断。下一步是用户提供完整多视图后，在同一 `reference_set_sha256` 下重跑 mask/landmark/region/camera 与 observation，再推进 RepairIntent/Primary Form。

2026-08-17 explicit-authoring action handoff：默认 `session_create_or_resume` 生成的保守 ReferenceCanvas/DesignSpec 现在只允许观察/未知记录；任何 Repair、Primary Form 或 stage action 在 Runtime 编译/比较前必须消费 `bindings.status=bound`，并验证每个 supplied view 的 target/mask、ViewSpec canonical hash 与已知 camera。显式 authoring action 正向回归和默认 fallback 负向回归均通过，阻断路径不写 candidate/version。该 boundary 仍不等于通用 Repair orchestrator、视觉 likeness 或高质量 PASS。

2026-08-17 ReferenceCanvas lineage handoff：当前源码/安装包最新 cohort 为 `820a542fabe3503f0b8cf6c86e84f578e7b074d79d07401a9a3f865dc60709b4`；较早的 explicit-authoring receipt 仍按其原 cohort `d56a…` 保留。最新 package/probe、MCP/Runtime/Geometry Worker/Render Worker、隔离 Ready、Ponytail preflight、project create/readback 均通过。`ReferenceCanvas@1.bindings` 已成为显式 target/camera/evidence lineage；Runtime 对 target reference、camera hash/canonical hash 和 evidence CAS 做 fail-closed 校验，并兼容数值等价的 JSON canonical replay。真实 Codex explicit-authoring receipt `docs/evidence/mcp010f/supplemental/real-codex-cli-current-20260817-reference-lineage-primitive.json` 已完成，`binding_status=bound`、`authoring_mode=EXPLICIT_CODEX_AUTHORING`、无持久用户数据/无无关副作用；这只是参考/相机/DesignSpec/Observation lineage PASS，不是 likeness PASS。Desktop restart gate 仍 `NOT_RUN`。该 slice 当时源码为 144 Schema、41 read + 33 opt-in write = 74 tools，视觉仍 `QUALITY_TARGET_NOT_MET`，单视图 `HQ_360=BLOCKED_REFERENCE_COVERAGE`，PBR/人评/export/restart/VoiceOver/UI E2E 仍未运行或阻断。

2026-08-17 Viewer handoff：Viewer 现在有显式 8-command read-only allowlist，前端调用和 Tauri command registration exact-match；local compare snapshot 不调用 Runtime，仅下载当前画布，正式 Runtime export/confirm 仍不在 Viewer。Tauri Viewer IPC/source tests 在显式 test-geometry-worker-fallback 下 6/6 PASS；无 feature 的回归会返回 GEOMETRY_WORKER_UNAVAILABLE，这是生产 boundary 的诚实失败而非可忽略警告。下一步仍需 packaged UI E2E/VoiceOver 与正式 artifact export/restart evidence；不能用 local PNG snapshot 代替 export receipt 或视觉质量门。

2026-08-17 next handoff — typed pose control：当前源码已把 `SilhouetteRig@1`/`SilhouetteRigHashRequest@1` 的 `rotation_x/y/z`（`radian`、±2π）接到 Runtime 最终 `forgecad.geometry.transform@2` sink；源 Part、镜像拓扑和 durable candidate 不被隐式改写。Primary Form action 可请求 `head-rotation-y` 等受限旋转，错误单位和越界值 fail closed。focused Runtime/MCP/Agentic tests 与 `script/test_mcp010f.sh` source Gate 通过；默认 detail Rig 仍 36 controls，自动搜索仍保持 40-geometry budget，姿态控制暂不自动加入搜索。没有新的真实 likeness receipt，仍以 `QUALITY_TARGET_NOT_MET`、历史 Stage 0 `camera=MISMATCH`/`BLOCKED_INCOMPLETE_BINDING` 为准；PBR、人评、export/restart、360 仍未运行或阻断。后续应在用户授权多视图和同一 reference-set lineage 下，单独验证姿态旋转对 silhouette 的真实收益，再决定是否纳入 bounded search。

2026-08-16 latest current-source real Codex handoff：MCP/Runtime/Geometry Worker/Render Worker 四个可执行文件已重建到同一 cohort `d52261ffdf44e07a2a4762b16c80f433a824080f0530fad64bbd32a153eb9c04`。真实 Codex 已走通 `ReferenceCanvas/DesignSpec → session_create_or_resume → scene_observe_get → SilhouetteRig`，session observation 与 canonical scene observation exact-match；本轮实际使用 `RUNTIME_DEFAULT_AUTHORING`，显式 Codex authoring 未作为 PASS 宣称。receipt 为 `docs/evidence/mcp010f/supplemental/real-codex-cli-current-20260816-primary-form-multiview-capable-v1.json`。随后同一 candidate lineage 完成 `pelvis → upper-arm-pair` 两个 Runtime-owned Primary Form steps，均 64 evaluations、strict improvement accepted，proposal loss 从 `0.463981471487` 逐步降至 `0.458125428229`，最终 boundary-only compare IoU `0.752731669182`、Boundary F1 `0.355021780326`。当前仍 `PASS_BOUNDARY_ONLY`/`QUALITY_TARGET_NOT_MET`，`quality_visual_status=NOT_RUN`，未 confirm/version/export、未运行 AOV/review/PBR/人评；单视图缺少 `front/back/left/right/rear-three-quarter`，360 必须继续 `BLOCKED_REFERENCE_COVERAGE`。下一步不是宣称高质量，而是取得用户授权的 front/back/left/right/rear-three-quarter 参考后，沿同一 reference-set lineage 重新做 multi-view observation/coverage，再决定 final AOV/typed review 和 detail/UV/PBR。

2026-08-16 current-source supplemental Primary Form convergence：同一 `d52261ff…` cohort 的重试已完成 `chest-shell → shoulder-armor-pair` 两个 Runtime-owned async repair steps；两步均 64 evaluations、`accepted + strict_improvement`，proposal loss `0.463981471487 → 0.453228014319 → 0.452434755498`，最终 candidate 为 `candidate-458cf0dc72fa4c5a8f9c64130b5318ab`。receipt `docs/evidence/mcp010f/supplemental/real-codex-cli-current-20260816-primary-form-chest-shoulder-v3-retry.json` 完成 Geometry/Render Worker、9 AOV、candidate-bound review/quality readback；当前最佳可比指标为 IoU `0.754862679391`、Boundary F1 `0.369927879976`、bbox `0.00390625`、centroid `0.002345583377`，相对前一 receipt 的 F1/IoU 有改善，但仍 `PASS_WITH_QUALITY_TARGET_NOT_MET`、`quality_hard_gate_passed=false`、review=`needs_revision`、visual claim 未声明，未 confirm/version/export。该 supplemental receipt 不能解除单视图 coverage：仍缺 `front/back/left/right/rear-three-quarter`，`HQ_360=BLOCKED_REFERENCE_COVERAGE`；下一步仍是取得用户授权多视图并沿同一 `reference_set_sha256` 重跑 coverage/observation。

下方同日旧 cohort 条目作为历史交接快照保留；接续工作以上述 `d52261ff…` receipt 为准。

2026-08-16 historical cohort handoff（superseded）：当前 real Codex handoff 使用旧 cohort `70badfd9d4e07a374aee994ad42604980ccf77e3f572b6d2926b46eccca6f72a`；该条目对应的 receipt 不覆盖最新 `d52261ff…` source cohort，视觉结论仍为 `PASS_BOUNDARY_ONLY`/`QUALITY_TARGET_NOT_MET`。

2026-08-16 ReferenceCanvas/DesignSpec authoring handoff：真实 Codex 视觉回合的编排层已收口为 `target → camera → baseline compare → session_create_or_resume(authoring_context) → scene_observe_get → Rig hash`。`authoring_context` 只接受由当前用户参考字节、授权声明、单视图 coverage/unknowns、`ReferenceViewSpec@1` 和 Runtime 生成的 canvas CAS hash 组成的 typed `ReferenceCanvas@1`/`DesignSpec@1`；Runtime 返回后会重新绑定 observation，CLI 要求 session 与 observation canonical hash exact-match。源码与 full MCP010F Gate 通过。真实 workspace-write boundary probe 未形成新 typed result：read-only 调试阶段 `project_create` 被取消，后续隔离 Codex host 超时后已安全清理；没有候选确认、版本、导出或持久用户数据变更。因此这只是 source/contract 编排 Gate，live authoring 仍 `BLOCKED/NOT_RUN`，不能升级视觉质量。

2026-08-16 unified real-reference CADFit/Surface/Manifold handoff：在同一隔离 cohort `2026081620260816…` 上，真实授权机器人参考完成 39 次 `128→256→512` 多保真 `OptimizationJob`，camera identity producer round-trip 与 optimization/compare camera hash exact-match；`surface-shell@1` 的 `surface_control_point` materializer 已按 typed index/axis 实际改变多点，Manifold C ABI/FFI Boolean lane `[1..9]` 真实执行。receipt `docs/evidence/mcp010f/supplemental/optimization-job-real-reference-20260816-unified-objective-manifold-surface.json` 为 `status=PASS`，但比较为 `QUALITY_TARGET_NOT_MET`（IoU `0.756284113105`、Boundary F1 `0.331999375494`、critical region min IoU `0.694630394531`），proposal `blocked-no-improvement`、candidate/version/confirm/export 均未发生。当前质量账本新增并校验该 receipt；它不替换 Stage 0 历史 `CAMERA_BINDING=FAIL`/`BLOCKED_INCOMPLETE_BINDING`，也不宣称视觉高质量通过。后续接 Viewer 的 candidate-bound RenderSet/read-model projection。

2026-08-16 Viewer current-cohort read-model handoff：同 cohort Viewer binary 在隔离 Runtime 上实际返回 `ForgeCADViewerReadModel@1`，1 project/1 candidate/0 version；candidate/artifact/reference/RenderSet/comparison/QualityReport exact-bound，Runtime 质量仍 `QUALITY_TARGET_NOT_MET`、hard gate=false，Viewer read-only 且没有持久化副作用。receipt `docs/evidence/mcp010f/supplemental/viewer-read-model-real-reference-20260816.json` 已纳入 current quality ledger；这不是 formal packaged UI E2E、VoiceOver、人评、export/restart 或 360 PASS。

2026-08-16 Manifold Boolean handoff：当前源和 isolated raw Gate 已证明 vendored Manifold C ABI/FFI Worker 的 bounded same-Part `union`、`difference`、`intersection` 均可执行；该 historical raw receipt catalog 为 `16 entries / 16 active`，其后 17/17 也已成为历史 slice，当前源码为 `19 entries / 19 active`，MCP010D raw 读回为 9 semantic Parts/588 triangles，Boolean curved-mesh lineage 与 residual tangent focused tests 通过。它仍是结构/运输能力，不是任意 mesh Boolean、视觉/PBR likeness、export/restart、真人或 360 PASS；历史 adoption receipt 中的 deferred intersection 只代表旧快照。

2026-08-16 CADFit OptimizationJob raw stdio 当前真值：真实 Runtime IPC 已接通 `optimization_job_prepare/get/resume`，Render Worker fit batch 已与 `OptimizationIntent@1` 的 `128 → 256 → 512` 多保真合同对齐。隔离 raw receipt `docs/evidence/mcp010f/supplemental/optimization-job-raw-20260816-mid-resolution.json` 完成 39 次评估（32 coarse、4 mid、3 final 含 baseline），结果 `succeeded/done`，`best_evaluation_fidelity=final`，proposal 因无严格改善被安全阻断；没有 candidate confirm、version、export 或用户持久数据写入。该证据证明 CADFit 的 Runtime-owned 搜索、checkpoint、Geometry Worker 和 Render Worker 运输链已落地，不证明真实参考 likeness 或高质量通过。

2026-08-16 Primary Form async Job real Codex handoff：当前源码 cohort `4661b8d94fc2b72819cb09b1503ea5112a328a40edec7b227a8814b3194f6497` 的真实 Codex replay 已实际走通 `primary_form_repair_job_prepare → job_get` 轮询 → `job_result_get`，证明 64-evaluation Primary Form 不再受单次 MCP 55 秒 IPC deadline 阻断；Runtime 仍独占连续参数搜索、Geometry/Render Worker、strict GLB readback、same-camera acceptance 和 staged candidate。该 receipt `docs/evidence/mcp010f/supplemental/real-codex-cli-current-20260816-primary-form-async-job.json` 的 source/proposal loss 为 `0.465026451428 → 0.447063964053`，camera binding 为 `PASS_SILHOUETTE_FIT_TO_COMPARE`，IoU/F1 为 `0.756928086147`/`0.392553090172`，但仍 `QUALITY_TARGET_NOT_MET`、hard gate=false、review=`needs_revision`，未 confirm/version/export。它是 supplemental current-cohort transport evidence；Stage 0 仍保留历史 `camera=MISMATCH`/`BLOCKED_INCOMPLETE_BINDING`，不得把该单次 staged improvement写成 HQ PASS。下一步应把同一 observation/camera/reference lineage 接到既有 CADFit `optimization_job_prepare/resume` 的 128/256/512 多保真 Job，再核对 RenderSet/Viewer 投影。

2026-08-16 Appearance V2 camera identity handoff：`prepare_appearance_candidate_v2` 已修复 RenderSet 相机绑定语义漂移：以前写入完整 CameraCalibration 对象哈希，现在由 Runtime `camera_identity_hash` 校验并写入 `CameraCalibration@1.camera_hash`。回归测试验证 identity hash、canonical receipt hash 和完整对象 hash 不可混用；source-built MCP010E raw stdio PASS（AppearanceProgram@2、ArtifactReadback@2 hard gate、9 AOV、3 Parts、3 material zones、embedded texture URI=0），MCP010F full Gate 退出码 0。Stage 0 仅更新当前 Runtime source digest，历史 provisional receipt 不变，仍为 `BT006_CAMERA_BINDING=FAIL`、`camera=MISMATCH`、`QUALITY_TARGET_NOT_MET`、`BLOCKED_INCOMPLETE_BINDING`；未 confirm/version/export，人评/PBR/export-restart/360 仍未运行或阻断。下一步先用当前源码重建/重绑真实 MCP cohort，重新取得 ReferenceCanvas→Primary Form 的 candidate-bound receipt，再进入 CADFit OptimizationJob。

2026-08-15 Primary Form 512px fit raster alignment handoff：提交 `220d6f0a` 后 Render Core 的 Primary Form 512px transient fit 与 formal compare 共用 1024 sample grid，128px exploratory path 仍为低成本路径；Geometry Worker exact PNG regression、MCP010F full Gate 与同源码 Dev.app 安装通过。安装 receipt `docs/evidence/mcp010f/supplemental/primary-form-fit-raster-alignment-dev-app-install-20260815.json` 与真实 receipt `docs/evidence/mcp010f/supplemental/real-codex-cli-current-20260815-primary-form-fit-raster-alignment.json` 绑定 cohort `3e6debd…c53b34`，真实 serial replay 为 `pelvis → upper-arm-pair`、2/2 accepted、64 evaluations/step。fit metrics 为 IoU `0.752731669182`、Boundary F1 `0.355021780326`；最终 compare 为 IoU `0.745042364012`、Boundary F1 `0.334537343601`。该差异不是 renderer 采样漂移：receipt 明确 fit camera `27d180…c10c0c` 与最终 camera `8cd206…c84a535` 不同，且 `canonical_compare_in_silhouette=false`；下一模块应收口 final compare 的 camera binding。质量仍 `QUALITY_TARGET_NOT_MET`、hard gate false、`visual_review=needs_revision`，未 confirm/version/export，PBR/人评/export-restart 未运行、360 `BLOCKED_REFERENCE_COVERAGE`；当前 live MCP 仍为旧会话，安全重绑 pending。

2026-08-15 Primary Form canonical ReferenceViewSpec handoff：本轮修复把 Codex consolidated observation 中的 hash-bound `ReferenceViewSpec@1` 传入 `silhouette_fit_prepare`/`primary_form_repair_prepare`；Runtime 先验证 reference/project binding，再让 bounded Primary Form ranking 与最终 same-camera acceptance 使用同一 landmarks/regions。MCP canonicalizer 兼容整数/浮点 JSON 重放，旧调用无 view spec 时保留 landmark-only fallback。提交 `1788f242` 后 clean Dev.app 安装回执 `docs/evidence/mcp010f/supplemental/primary-form-view-spec-dev-app-install-20260815.json` 与真实复放 `docs/evidence/mcp010f/supplemental/real-codex-cli-current-20260815-primary-form-view-spec.json` 均确认 cohort `4c12eac9…edd5b`；真实 receipt 的 15 landmarks、8 regions、`view_spec_sha256=79bbb0d8…8b65` 被带入两步 `primary_form_repair_prepare`，两步均 `accepted + strict_improvement`、64 evaluations，最终 compare IoU `0.745151573065`、Boundary F1 `0.334457989922`。这不是视觉 likeness PASS：`QUALITY_TARGET_NOT_MET`、`hard_gate=false`、`visual_review=needs_revision`，Stage 0 `camera=MISMATCH`/`BLOCKED_INCOMPLETE_BINDING`、PBR/人评/export-restart/360 状态不变，未 confirm/version/export。

2026-08-15 Primary Form serial composition handoff：修复 probe 在 Codex retry 后使用 compact call-id projection 进行阶段顺序判断的误阻断；`call_sequence`/`all_completed` 现在只消费原始、已完成的 ForgeCAD MCP events，compact `mcp_calls` 仅保留 transport receipt。修复后的真实 direct-MCP receipt `docs/evidence/mcp010f/supplemental/real-codex-cli-current-20260815-primary-form-sequence-pelvis-upper-arm.json` 在同 cohort `78d03f2b…1a808` 完成 `pelvis → upper-arm-pair` 两步 serial Primary Form：两步均 64 evaluations、`accepted + strict_improvement`，candidate lineage 为 `candidate-e330ef0ad50a4779bb9f55fc22cc657d → candidate-84dc8d6229374faa9188f08693136a10 → candidate-65de4eba7341405fa448dafb24dbca84`。最终 IoU `0.751411042945`、Boundary F1 `0.34965309767`、bbox `0.00390625`、centroid `0.001862755047`，仍 `QUALITY_TARGET_NOT_MET`、hard gate false；九 AOV/review transport 完成，未 confirm/version/export，PBR/人评未运行，360 仍 blocked。前一轮真实阻断 receipt 以 `...-blocked-retry-gate.json` 保留，证明是编排门误判而非 Runtime/Worker 故障。

2026-08-15 real Primary Form replay handoff：新 Dev.app cohort `78d03f2b…1a808` 已通过隔离 direct MCP 完成真实授权参考的 detail + silhouette-first 闭环。`docs/evidence/mcp010f/supplemental/real-codex-cli-current-20260815-primary-form-runtime-replay.json` 记录 Runtime-owned Primary Form 64 evaluations，26 Parts/4704 triangles，source `candidate-1f6344e7f40040faa941fe544388226a` → staged `candidate-696b85e3402f44fab22c771bdaf766b0`，strict acceptance 为 true；最终视觉指标 `boundary_f1_4px=0.414236535223`、`silhouette_iou=0.74637831219`、`bbox_edge_error=0.03515625`、`centroid_error=0.007345230528`，仍 `QUALITY_TARGET_NOT_MET`、`visual_review=needs_revision`、`hard_gate=false`。这关闭了“新 Runtime/Render Worker 是否真实执行”的证据缺口，但没有关闭 likeness：未 confirm/version/export，PBR `NOT_RUN`，HQ 360 `BLOCKED_REFERENCE_COVERAGE`；Desktop live MCP 重启重绑仍需后续处理。

2026-08-15 Agentic durable design-document readback：`AgenticSessionResult@1` 现在在 session create/resume/get/lookup 的同一 Runtime 结果中返回 hash-bound `ReferenceCanvas@1` 与 `DesignSpec@1`；Runtime 重新读取 CAS、校验 schema/canonical hash、project/reference/canvas binding，缺失或漂移 fail closed，不返回原图字节或路径。focused bounded action/session regression 通过，receipt 为 `docs/evidence/mcp010f/agentic-design-documents-readback-20260815.json`。该模块解决重启后 Codex 只能拿到 opaque document IDs 的观察碎片化，不升级视觉质量；live MCP 仍旧 cohort，需重启/重新建立。

2026-08-15 clean package handoff：提交 `cf329215` 后重新构建并安装 `ForgeCAD Runtime Dev.app`，四个 packaged executable 共用 cohort `aa0d6dcb…5808e`，`source_worktree_dirty=false`；ad-hoc deep-strict、隔离 Runtime Ready/project_create/preflight 和 packaged MCP010B raw stdio 均 PASS。证据为 `docs/evidence/mcp010f/dev-app-install-adaptive-line-search-clean-20260815.json`、`dev-app-probe-adaptive-line-search-clean-20260815.json`、`packaged-adaptive-line-search-clean-raw-stdio-20260815.json`。该 package Gate 仍不是 live MCP 或视觉 likeness PASS；当前仍 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`，live session 需重启/重新建立。

2026-08-15 Primary Form adaptive local line-search handoff：Runtime 已修复 36-control detail Rig 的失败路径预算缺口：大 Rig 在 `40 geometry` 内执行 `joint`、一个 `0.5` whole-proposal backtrack、boundary-dominant Part 的 `0.5/1.0` coupled hypotheses，再保证 `36/36` scalar probe；小 Rig 保留 `0.5/0.25` backtrack 与单 coupled recovery。focused Runtime `22 passed`，source receipt 为 `docs/evidence/mcp010f/primary-form-adaptive-line-search-20260815.json`。没有新的授权参考或视觉 likeness receipt，当前仍 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`，未 confirm/version/export；package/live MCP 需按新源码重建和重启后再做真实复放。

2026-08-15 current-source package handoff：`a153dc02` 已重建并安装同源码 Dev.app，MCP/Runtime/Geometry Worker/Render Worker cohort `04bbb4d6…ce81f3` exact-match；ad-hoc deep-strict、隔离 Runtime Ready/project/preflight 和 packaged MCP010B raw stdio PASS（12 项 Skill registry、37 read + 24 opt-in write = 61 工具、manifest hash exact-match、GeometryProgram@2 strict readback、candidate binding、Part sink negative Gate）。`scripts/probe_mcp010b_raw_stdio.py` 已按 preflight contract 先读取 `ponytail-preflight@0.1.0`，并从 checked-in registry 计算 Skill 集合，避免固定数量漂移。当前 Codex Desktop live session 仍为旧 `7f9e4c…ee518`，开始 live authoring 仍需重启/重新建立 MCP session；没有把 package PASS 写成 live visual PASS，也没有新的 reference/candidate/RenderSet receipt。质量继续为 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`，未 confirm/version/export。

2026-08-15 Primary Form recovery coverage handoff：Runtime 的 64-evaluation Primary Form search 在 joint proposal 未严格改善时，不再为所有 Part 消耗 group recovery；它保留 boundary-dominant Part 的一个 coupled width/height/offset hypothesis，然后把剩余 geometry budget 用于 detail Rig 全 36 个 scalar controls 的一次 bounded probe。`primary_form_recovery_reserves_full_scalar_control_coverage`、Primary Form/Agentic integration tests 与 `script/test_mcp010f.sh` full Gate PASS，receipt 为 `docs/evidence/mcp010f/primary-form-recovery-coverage-20260815.json`。这是 source/convergence boundary 修复，不是视觉 likeness PASS；当前源码 hash `531783a0…1ae53b` 尚未重新安装到 Dev.app/live MCP，当前仍 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`，未 confirm/version/export。

2026-08-15 live/package cohort handoff：`abae43f3` 已按同一源码 revision 重建并安装用户级 Dev.app；MCP/Runtime/Geometry Worker/Render Worker cohort 为 `5a1f108a…e2dd2f`，package deep-strict、resource allowlist、隔离 Runtime Ready/project/preflight probe 通过，且 `persistent_user_data_touched=false`。新包工具面为 37 read + 24 opt-in write。当前对话已有的 MCP 连接仍绑定旧 `7f9e4c…ee518` cohort/旧 manifest `05fca3…d4d0a`，要开始 live authoring 必须新建 MCP 会话；因此没有把 package receipt 绑定到任何机器人 candidate 或视觉 benchmark。当前质量仍 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`。

2026-08-15 Primary Form resolution-consistent fit handoff：修复 Primary Form 搜索与最终接受门使用不同分辨率的问题。`silhouette_fit_prepare` 的相机邻域与 Geometry Worker proposal 现在走 512×512 isolated Render Worker fit batch；普通 camera-fit 仍保留 128×128 粗搜。Runtime focused/full tests、MCP010F 聚合门与 product workspace check 已通过；没有新的 likeness receipt，真实质量账本继续为 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`。

2026-08-15 Viewer Agentic evidence binding handoff：Viewer 已有 candidate-bound visual evidence 时，`agentic-design.ts` 的 normalizer 现在要求五个 Runtime evidence hash 逐项完整匹配；缺 hash 或 hash drift 会返回 `AGENTIC_EVIDENCE_BINDING_MISMATCH`，不再将缺失字段视为可兼容的 ready projection。没有视觉绑定时保留 unknown/结构状态。source Gate、Node behavior、desktop build 与 MCP010F full Gate 通过；没有新的真实 likeness receipt，当前仍 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`。

2026-08-15 Render Worker fail-closed boundary handoff：Runtime 的 `render-core` fallback 已不再由 `cfg(test)` 自动启用；`test-render-worker-fallback` 是唯一 Render 回退 feature，`test-geometry-worker-fallback` 仅作为 legacy 显式组合。无 feature product `cargo check`、source boundary checker、显式 fallback Runtime/MCP tests、MCP010C/F full Gate 通过。后续若要声称 Render Worker 真实落地，仍须运行 same-cohort sibling/package probe；当前质量账本不变：`QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`，人评/PBR/export-restart/360 未运行或阻断。

2026-08-15 Agentic canonical observation MCP dispatch handoff：修复 `InProcess` adapter 的观察绑定回退。`design_stage_plan_get`、`critic_report_get`、`visual_evidence_bundle_get` 现在必须把调用方提交的 `observation_sha256` 交给 Runtime bound projection；缺失/stale hash 立即 fail closed，与 authenticated IPC 路径一致。MCP 全量 `56 passed / 0 failed`；没有新的 Runtime/CAS、likeness 或 Viewer 质量证据，当前仍 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`。

2026-08-15 Primary Form output-level offset sink handoff：Runtime 修复了 `offset_x/offset_y/offset_z/scale` 在 mirror/array 拓扑上的落点错误。现在 width/height/depth 仍作用于源几何节点；整体 camera-plane 位移/缩放则在 Part 输出后的复用 `forgecad.geometry.transform@2` 上物化，避免双侧镜像把一次整体偏移变成左右相反的间距变化。mirror/direct-output materialization regressions 通过，Stage 0 Runtime hash 已同步；没有新的真实 likeness receipt，当前仍 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`，未 confirm/version/export。

2026-08-15 Primary Form exact evaluation ledger handoff：Runtime 修复了 bounded search 的执行计数与阶段预算漂移。64 上限现在固定为 `40 geometry + 15 initial camera + 9 winner refit`，与实际 15 个初始 camera variants 对齐；每个 refit camera row 都在 source/proposal batch 渲染后计入 `camera_refit_evaluations`，即使 strict same-camera acceptance 保留 authored source，也不会再把一次真实 Worker evaluation 从总数中漏掉。focused budget 与 Primary Form fixture 通过，fixture 现要求 `evaluations=64`。这只修复 Runtime 收敛账本，不代表视觉收敛或质量通过；没有新的授权参考 receipt，仍保留 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`INCOMPLETE_TRUTH_BINDING`/`BLOCKED_INCOMPLETE_BINDING`，人评/PBR/export-restart/360 未运行或阻断。

2026-08-15 Primary Form 单 Part orchestration boundary handoff：重构 `scripts/probe_mcp010f_part_correction.py`，现在只建立一个 authored baseline，按 `camera_fit_prepare → 同相机 reference_compare_prepare → scene_observe_get → silhouette_rig_hash` 固定顺序取得候选绑定观察，然后提交一次 `primary_form_repair_prepare`。连续参数搜索、Geometry Worker 编译、strict GLB readback、Render Worker paired compare 和 same-camera retention 全部由 Runtime 执行；脚本不再 patch GeometryProgram、不再生成 `0.4/0.7/1.0` fraction candidates、不再调用 `silhouette_candidate_compare` 或本地接受判定。精确 Part Rig 使用 `meter` offset sink，避免旧 ratio offset 语义漂移；结果只保留 Runtime typed `prepared`/`no_improvement` 与待批准 staged candidate，永不 confirm/export。`script/test_mcp010f.sh` full Gate 通过；没有新的授权参考/likeness receipt，当前仍 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`INCOMPLETE_TRUTH_BINDING`/`BLOCKED_INCOMPLETE_BINDING`，人评/PBR/export-restart/360 未运行或阻断。旧 `part-correction-source-20260813*.json` 仍按历史保留，不代表新探针行为。

2026-08-15 Primary Form profile-loft height sink handoff：`materialize_rig_geometry_program` 已按 operator-aware typed sink 修复 `profile-loft@1` 的 `height` 语义：目标高度按 profile station span 写入 `height_m`，首站保持锚定，截面点深度保持不变；`profile-extrude` 的二维高度缩放保持原行为。新增 regression、Rig materialization 7 tests、Runtime `122 passed / 0 failed / 12 ignored`、MCP010F full Gate 与 Stage 0 checker 通过。该模块没有新真实参考/likeness receipt，当前仍 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`INCOMPLETE_TRUTH_BINDING`/`BLOCKED_INCOMPLETE_BINDING`；后续仍需用户授权参考字节和真实视觉闭环，不能把 source Gate 写成高质量 PASS。

2026-08-15 Durable observation lineage handoff：本轮把 Agentic durable prepare/readback 的 binding 从 project/candidate scope 扩展为 exact candidate/reference lineage。`session_*`、`checkpoint_*` 和 bounded action 在使用观察时会核对 candidate ID/hash、reference ID/object hash/canonical hash；缺失/漂移不会继续执行。focused exact-lineage、Agentic projection 与 bounded action tests PASS。该修复没有改变 Runtime 唯一写者、Viewer 只读边界或当前视觉真值；下一步仍是用真实授权参考复跑视觉闭环，不能把结构 Gate 写成 likeness PASS。

2026-08-15 Agentic observation handoff：Runtime 进程内新增以 `AgenticSceneObserveResult@1.canonical_sha256` 为 key 的 bounded read-only cache；同一次 `scene_observe_get` 后的 plan/critic/evidence/action follow-up 读取原观察对象，缓存丢失时才重建并严格做 hash/scope 校验。它不写 SQLite/CAS，也不替代 durable session/checkpoint；新增 ambiguity/cache regression 与 bounded action regression 通过。没有新的授权 PNG 或 likeness 证据，质量仍为 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING`。

2026-08-15 Primary Form composition-lineage handoff：`--part-contour-sequence` 现在生成并校验 `ForgeCADPrimaryFormCompositionLineage@1` compact orchestration receipt；每一步绑定 candidate/observation/target/camera/Rig/intent hashes，完成第二步起先消费已校验的 prefix lineage 再允许下一 candidate，只有 `prepared + strict_improvement` 才推进，`no_improvement` 保持 source。raw Codex/MCP events 仍仅用于 transport audit，Codex 后续决策消费 lineage projection，不从散乱事件重建状态。该 projection 不写 Runtime/CAS、不改变 Viewer Runtime-only QualityReport，真实 composition 仍需用户授权 PNG，当前视觉质量仍 `QUALITY_TARGET_NOT_MET`。

2026-08-15 Primary Form bilateral landmark-mask handoff：修复 landmark 证据投影只按 exact Part-ID 解码的缺口；`knee-pair`/`hand-pair` 等语义 Rig Part 现在会在 Runtime 内把 Render Worker 的 `*-left`/`*-right` Part-ID 聚合成同一 landmark mask，再按 left/right anchor 计算覆盖与误差。新增双侧地标回归测试；该修复只恢复 Part-owned observation，不产生新的真实 likeness receipt，`QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`、人评/PBR/export-restart/360 未运行或阻断保持不变。

2026-08-15 Primary Form bilateral local-envelope handoff：修复局部 proposal 仍按 exact Part-ID 查找的缺口；`hip-left`/`hip-right` target 与 Render Worker envelope 现在会通过固定 alias 合并后再计算 pair Rig 的 width/height/offset 提案，避免已选中的双侧 Part 退回 whole-body bbox。新增 local-envelope merge regression 与 Primary Form 相关测试通过；该修复只改善 Runtime 收敛输入，没有新的真实 likeness receipt，`QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`、人评/PBR/export-restart/360 未运行或阻断保持不变。

2026-08-15 Primary Form bilateral boundary ranking handoff：修复 Runtime bounded probe 排序只按字符串精确匹配的缺口；Render Worker 的 `shin-left`/`shin-right` 现在会通过固定 alias 关系聚合到 Rig 的 `shin-pair` 控制，再参与 boundary-priority 排序。新增 bilateral ranking regression，Primary Form 相关 21 个 Runtime tests 与 MCP010F full Gate 通过。该修复只改善 Runtime 收敛输入，不产生新的真实 likeness receipt；`QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`、人评/PBR/export-restart/360 未运行或阻断保持不变。

2026-08-15 Modular composition boundary handoff：CLI 新增 `--part-contour-sequence`，将 2–3 个 exact Part repair 串在一个 project 内；每一步先对当前 candidate 重新读取 consolidated target/camera/baseline compare/canonical observation/Rig，再执行唯一一次 Runtime-owned bounded Primary Form repair。accepted staged candidate 才能成为下一步 source，`no_improvement` 不覆盖 source camera；Runtime continuation regression 与 full Runtime `117 passed / 0 failed / 12 ignored`、MCP010C Gate 通过。raw stdio manifest 断言已改为读取 `forgecad-mcp --tool-manifest-summary`，避免工具数漂移。Stage 0 Runtime source hash 已同步，`check_mcp010f_stage0_truth.py` 通过；质量真值仍为 `camera=MISMATCH` / `QUALITY_TARGET_NOT_MET` / `BLOCKED_INCOMPLETE_BINDING`。授权参考 PNG 字节仍不可用，真实 composition 尚未运行，当前仍 `BLOCKED_REFERENCE_BYTES_UNAVAILABLE`，无 confirm/version/export。

2026-08-15 Hip-pair bounded trial handoff：r21 `docs/evidence/mcp010f/supplemental/real-codex-cli-current-20260815-canonical-observation-hip-pair-r21.json` 在 cohort `726153a3…42ab5c` 完成 60 次 Runtime-owned fit，source/proposal loss `0.426350916959`/`0.426265642648` 严格改善，`candidate-2dc5728da7124288b6ff79a0fb6b4f00` 为 `staged_new_candidate`，camera binding `PASS_SILHOUETTE_FIT_TO_COMPARE`。最终 compare 仍为 `QUALITY_TARGET_NOT_MET`（IoU `0.743473474034`、Boundary F1 `0.301145366407`、bbox `0.0234375`、centroid `0.021831234764`、hard gate false），没有 confirm/version/export。r19/r20/r21 是独立 trial，不是已合并的多 Part 版本；下一步先选择 boundary-first 最佳 baseline，再决定是否允许组合。

2026-08-15 Chest-shell bounded trial handoff：r20 `docs/evidence/mcp010f/supplemental/real-codex-cli-current-20260815-canonical-observation-chest-shell-r20.json` 在 cohort `726153a3…42ab5c` 完成 61 次 Runtime-owned fit，source/proposal loss `0.426350916959`/`0.416406210214` 严格改善，`candidate-d9d8cb24b1c5475d85b773c3ceb032c7` 为 `staged_new_candidate`，camera binding `PASS_SILHOUETTE_FIT_TO_COMPARE`。最终 compare 仍为 `QUALITY_TARGET_NOT_MET`（IoU `0.739507057324`、Boundary F1 `0.314765400977`、bbox `0.013671875`、centroid `0.020515796935`、hard gate false），没有 confirm/version/export；下一步评估 `hip-pair`，不能把 staged candidate 当成已确认版本。

2026-08-15 Upper-arm-right bounded trial handoff：r19 `docs/evidence/mcp010f/supplemental/real-codex-cli-current-20260815-canonical-observation-upper-arm-right-r19.json` 在 cohort `726153a3…42ab5c` 完成 63 次 Runtime-owned fit，source/proposal loss `0.426350916959`/`0.423630597587` 严格改善，`candidate-f1123cb13b624cb69493f825398b4423` 为 `staged_new_candidate`，camera binding `PASS_SILHOUETTE_FIT_TO_COMPARE`。最终 candidate-bound compare 仍为 `QUALITY_TARGET_NOT_MET`（IoU `0.744938326895`、Boundary F1 `0.307109823312`、bbox `0.021484375`、centroid `0.021832396893`、hard gate false），没有 confirm/version/export；下一步按推荐表继续单 Part trial，不能把 staged candidate 当成已确认版本。

2026-08-15 Pelvis bounded trial handoff：r16 `BLOCKED` 记录了首次 `reference_get` 瞬时 IPC 失败；r17 保留了真实 camera-handoff defect——`primary_form_repair_prepare` 返回 `no_improvement` 时，未采用的 proposal camera 不应覆盖 source candidate 的 baseline compare camera。CLI 已修复该边界，r18 `docs/evidence/mcp010f/supplemental/real-codex-cli-current-20260815-canonical-observation-pelvis-r18.json` 在 cohort `726153a3…42ab5c` 通过 `camera_binding_status=PASS_CAMERA_FIT_TO_COMPARE`，同一 canonical observation 返回 26 个 inferred Part rows，`canonical_compare_in_silhouette=true`、`boundary_error_get=0`。pelvis 55 次 Runtime-owned fit 没有严格改善：source/proposal loss `0.435730135362`/`0.438891599478`，`retained_source`/`no_improvement`，最终 `QUALITY_TARGET_NOT_MET`，未产生新 candidate/version、未 confirm/export。下一步继续按推荐 Part 逐个 bounded repair；Stage 0 provisional truth 不变，不能宣称高质量通过。

2026-08-15 Canonical observation handoff：当前 cohort `726153a3…42ab5c` 的真实 r15 receipt `docs/evidence/mcp010f/supplemental/real-codex-cli-current-20260815-canonical-part-observation-r15.json` 已验证 silhouette turn 为 `target → camera_fit → baseline compare → scene_observe → Rig`；`scene_observe_get` 的同一 `AgenticSceneObserveResult@1` 携带 26 个 `visibility=inferred` Part error rows，`boundary_error.source=canonical_observation`，`boundary_error_get` 为 0。Runtime 仍拥有 compare、bounded Primary Form、Geometry/Render Worker 和 quality authority；Codex 只提交 typed intent。r15 最终 `PASS_WITH_QUALITY_TARGET_NOT_MET`，IoU `0.739507057324`、Boundary F1 `0.314765400977`、hard gate false，未 confirm/version/export。下一步继续按 canonical table 的推荐 Part 逐个做 bounded trial；不得回到 Codex 连续参数搜索，也不得把 supplemental r15 当作视觉质量 PASS。

2026-08-15 Primary Form right-shoulder trial handoff：同一 packaged cohort `26354e2f…a2029` 的真实 r11 receipt 已完成 `shoulder-armor-right` exact Part contour 与 Runtime-owned bounded repair；63 次内部评估在固定 camera 下把 source/proposal loss 从 `0.426350916959` 降到 `0.409541676181`，严格 acceptance 生成 staged candidate。最终全局 compare 仍未过门（IoU `0.749072115206`、Boundary F1 `0.326405552646`、`QUALITY_TARGET_NOT_MET`、`visual_review=needs_revision`），没有 confirm/version/export。下一步应继续按 canonical observation 选择剩余高误差 Part，并修复多 Part 观察归因/合并；每次仍只允许一个 Runtime-owned bounded Part trial，不能让 Codex 接管连续搜索，也不能把 supplemental receipt 提升为 Stage 0 benchmark truth。

2026-08-15 Primary Form exact-side sink alias handoff：Runtime 已补齐固定 `*-armor-left/right ↔ *-left/right` 的 side-aware sink projection；这保证 exact `shoulder-armor-left` trial 在显式 `shoulder-left` 几何输出上实际改变左侧，同时保留右侧不变。6 个 materialization/alias regression 与 exact-side scope regression 通过；没有新的真实视觉 receipt，下一步仍应验证其他高误差 Part，并继续使用同一 strict same-camera acceptance，不能放宽质量门或让 Codex 接管连续搜索。

2026-08-15 Primary Form exact single-Part candidate trial handoff：Runtime 已将 `part_id` 接入既有 `primary_form_repair_prepare`/Job，而不是新增第二套连续搜索；它把 bilateral authoring Rig 投影为 exact `shoulder-armor-left` scope，Primary Form 自己完成 bounded fit → Geometry Worker → strict readback → same-camera Render Worker source/proposal acceptance。真实 r10 receipt `docs/evidence/mcp010f/supplemental/real-codex-cli-current-20260815-part-contour-trial-r10.json` 证明 trial 实际执行并保留 source：source/proposal loss `0.426350916959`/`0.425568832154`，但严格联合门为 false，未产生 candidate/version；随后 Part contour readback 为 `proposal_ready`。receipt 顶层 `BLOCKED` 来自无关 side-effect ledger，不能写成 transport 或视觉 PASS；quality 仍 `NOT_RUN`/历史 `QUALITY_TARGET_NOT_MET`。下一步继续用同一 acceptance 验证其他高误差 Part，不能放宽门或让 Codex 接管连续参数。

2026-08-15 Primary Form 单 Part contour/semantic asymmetry handoff：Runtime 已将 `part_contour_fit_prepare` 收口为 exact Part-aware、alias-aware 的只读提案：`hip-pair` 可从显式左右 Part-ID 聚合 boundary evidence，而 `shoulder-armor-left` 这类精确 Part 只改变自身的 bounded width/height/offset 控制。真实 r8 receipt `docs/evidence/mcp010f/supplemental/real-codex-cli-current-20260815-part-contour-r8.json` 在同 cohort `dd78d216…e5088` 中按 `silhouette_rig_hash → part_contour_fit_prepare` 完成，返回 `proposal_ready`、26 Parts、无持久化数据或无关副作用；本次是 `PASS_BOUNDARY_ONLY`，不代表 proposal 被接受，也没有运行 likeness/quality gate。下一步应把该单 Part 提案接入 Runtime-owned bounded candidate trial，并继续以同一相机和严格非回归 acceptance 判定，不能让 Codex 扩展成连续搜索。

2026-08-15 Primary Form 36-control Rig 复放 handoff：packaged cohort `106a2889…67b08e` 的 r7 supplemental real-Codex receipt 已完成 56 次 Runtime-owned bounded fit，并验证新增 hip/pelvis/chest 有界控制会进入 GeometryProgram materialization；source/proposal loss 为 `0.40710507361`/`0.413276643194`，故 Runtime 保留 source，未创建 candidate/version，`candidate_state=unchanged`。camera/Render Worker/Viewer read-model 绑定通过，但 `quality_visual_status=QUALITY_TARGET_NOT_MET`，final IoU `0.746739614479`、boundary F1 `0.34284083431`；下一模块应基于这些部件误差继续单 Part contour/semantic asymmetry 设计，不能放宽 acceptance 或让 Codex 接管连续搜索。

2026-08-15 Primary Form same-camera acceptance handoff：Runtime 的 geometry-winner refit 现在让 source/proposal 共享最终 Runtime-owned camera，并由 isolated Render Worker 做 paired compare；只有严格 `proposal_loss < source_loss` 才会准备 candidate，search-only 的 bounded loss 过渡不会绕过最终门。packaged cohort `e7e466f6…b7cc` 的 supplemental r6 记录 source `0.40710507361`、proposal `0.412982572291`，因此 `retained_source`/`candidate_state=unchanged`；r6 顶层 `BLOCKED` 是无关 side-effect 分类，不是质量通过。r4/r5/r6 保存在 `docs/evidence/mcp010f/supplemental/`；真实视觉仍 `QUALITY_TARGET_NOT_MET`，下一轮应优先补强 hip/pelvis/chest 的有界 Rig controls，而不是放宽 acceptance threshold。

2026-08-15 QualityReport/packaged F handoff：`AppearancePrepare` 对 `visual_status=not-run` 的 `QualityReport@2` 现在绑定 `hard_gate_passed=false`，不再把结构性 AppearancePrepare 误写成视觉门通过；同一 packaged cohort `fee79807…` 的 F raw stdio probe 已 PASS，确认 26 parts/4704 triangles、7 material zones、九 AOV、embedded texture、Render Worker cohort 和 Viewer read-model lineage。该 probe 不运行用户参考 likeness 或人评，故不能提升真实质量状态；`QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING` 及 PBR/export-restart/360 未运行或阻断保持不变。

2026-08-15 Primary Form 多尺度收敛 handoff：Runtime-owned `GeometryProgram` bounded search 在 64 evaluation hard cap 内为 geometry 保留 40 次、初始 camera 保留 16 次、geometry-winner refit 保留 8 次；coordinate refinement 采用 `1.0 → 0.5 → 0.25` deterministic scale，避免 26-control detail Rig 只重复同一 coarse step。Codex 仍只提交 typed repair intent，不承担连续参数搜索；Geometry/Render Worker、strict readback、same-camera acceptance 和 Viewer read-only authority 不变。focused regression 已通过，但没有新真实 likeness receipt，当前仍为 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING`，人评/PBR/export-restart/360 未运行或阻断。

2026-08-15 camera-fit→compare handoff：Runtime 现在按 `project_id/candidate_id/target_sha256` 复用最近的 `camera_fit_prepare.selected_camera`；`reference_compare_prepare` 没有显式 camera 时不会再用 default camera 重新推导 framing。该 source regression 通过，但历史真实 Codex receipt 尚未重新运行，因此 `camera=MISMATCH`、`QUALITY_TARGET_NOT_MET` 和 benchmark incomplete binding 仍是当前事实；Viewer 继续只读消费 Runtime camera/quality lineage。

2026-08-15 Primary Form bounded continuation handoff：Runtime action/job 将 `max_iterations` 上限提升为 2，`max_evaluations` 仍固定为 64；第二轮只在 Runtime/Workers 内围绕第一轮 incumbent 继续 bounded camera/geometry coupling，Codex 仍只提交一次 typed intent。fixture 已验证 `iterations=2` 且总 evaluations 仍为 63–64；没有新的真实 likeness receipt，历史 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH` 和 benchmark incomplete binding 不变。

2026-08-15 canonical observation durable handoff：Runtime 现在把同一次 `AgenticSceneObserveResult@1.canonical_sha256` 写入 `DesignSession@1.observation_sha256` 与 `DesignCheckpoint@1.observation_sha256`；`session_get`、checkpoint prepare/get/restore 与 action run 会重新观察并拒绝 stale/mismatched hash，防止 Codex 把不同时间或不同 candidate 的零散观察拼到同一动作。隔离 Runtime/MCP 重启 probe、durable receipt checker、projection checker、contracts 和 `forgecad-runtime` 全量 `105 passed / 12 ignored` 通过。该 handoff 只修复观察 lineage 与 durable readback，不代表完整 Agentic orchestrator、Repair 执行或视觉质量 PASS；Stage 0 仍为 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING`，Viewer 仍只消费 Runtime read model，人评/PBR/export-restart/360 未运行或阻断。

2026-08-15 canonical observation read-slice handoff：Stage Plan、Critic 和 Agentic Visual Evidence 三个 follow-up read tool 现在要求 `scene_observe_get` 的 canonical observation hash；Runtime 在同一个 `AgenticSceneObserveResult@1` 上做 hash-bound slicing，拒绝 stale/missing hash，避免 Codex 从多个独立 projection 拼接现场。Agentic evidence 改为 `VisualEvidenceBundle@1`，`ViewerVisualEvidence@1` 仍只属于 Viewer transport；focused Runtime/MCP stale-binding regression 通过。该修复不执行 Repair、不改变视觉质量账本，仍为 `QUALITY_TARGET_NOT_MET`、`MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`。

2026-08-15 RepairIntent durable handoff：`checkpoint_restore_prepare` 现在生成带 `observation_sha256` 的 `RepairIntent@1`，该值必须与同一 durable session/checkpoint 的 observation hash 相等；`docs/evidence/mcp010f/agentic-runtime-session-checkpoint-20260815-observation-bound.json` 已由隔离 Runtime/MCP 重启 probe 产生并通过 Runtime receipt checker。该修复只完成 RepairIntent 的证据绑定与读回，不执行 Repair、不解锁完整 Agentic orchestrator，也不改变 `QUALITY_TARGET_NOT_MET`、`MISMATCH`、`BLOCKED_INCOMPLETE_BINDING` 或人评/PBR/export-restart/360 状态。

2026-08-15 Primary Form local-group convergence handoff：Runtime 在 joint evidence proposal 失败后，按候选 Part 的 boundary priority 先试 width/height/offset 的耦合局部修复，再继续 bounded coordinate refinement；每次仍由 Geometry Worker 编译、Render Worker 渲染并用同一 Runtime metric ordering 选择 incumbent，Codex 不接收连续参数轨迹。`forgecad-runtime` Primary Form focused group tests 通过；这只是收敛搜索结构修复，没有新的真实 likeness receipt，当前视觉质量与 incomplete-binding 状态不变。

2026-08-15 Primary Form ranking snapshot handoff：camera/geometry/refit 的 Runtime bounded search 现在对每个 render 同时保留公开五项 contour metrics 和带 candidate-bound landmark evidence 的内部 ranking snapshot；baseline 与候选经过同一投影后才比较，避免缺失字段被当成零 coverage。`SilhouetteFitResult@1` 不增加公开字段，Viewer 仍只消费 Runtime QualityReport/read model。新增 snapshot regression，Runtime 全量 `105 passed / 12 ignored`；这是收敛判定修复，不是 likeness PASS，Stage 0 仍为 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING`，人评/PBR/export-restart/360 未运行或阻断。

2026-08-15 observation/camera handoff：`design_action_run_prepare` 要求 Codex 把实际消费的 `AgenticSceneObserveResult@1.canonical_sha256` 作为 `observation_sha256` 送回；该 hash 进入 `input_sha256`，Runtime 重新生成当前观察并拒绝 stale/mismatch。Primary Form action 的 `base_camera` 现在由 Runtime 按 session `camera_hash` 从同一 candidate/target-bound camera-fit evidence 解析，不再静默使用 default camera；缺少精确 camera evidence 时 fail closed。MCP schema/缺字段 fixture、Runtime stale-observation 和 action round-trip focused tests 通过。该 handoff 只收口单次观察与相机 lineage，不是 orchestrator/Repair/视觉质量 PASS；Stage 0 仍为 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING`，Viewer 仍只消费 Runtime read model，人评/PBR/export-restart/360 未运行或阻断。

2026-08-15 Runtime quality-authority handoff：`visible_view_gate_checks` 现在是 Runtime visible-view metric gate 的唯一检查投影；Quality gate 布尔结果与 Agentic `DesignCriticReport@1.failed_metrics` 共用同一 Runtime helper，Agentic 不再复制阈值或独立判断通过/失败。`visible_view_gate_rejects_exploratory_thresholds_and_accepts_strict_metrics`、Runtime 全量 `104 passed / 12 ignored` 与 MCP010F source Gate 通过。此为质量权威边界修复，不是视觉质量通过；当前仍保留 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING`，Viewer UI/accessibility、人评、PBR、export/restart、360 未运行或阻断。

2026-08-15 Primary Form metric-priority handoff：Runtime 现在把可见质量门的 metric priority 真正用于 camera/geometry/refit/acceptance/candidate compare 的 winner retention，而不只在文档中列出顺序；`boundary_f1_4px` 先于 IoU，IoU 先于 bbox/centroid，再到 landmarks/regions，浮点噪声只在同一优先级相等时用 scalar loss 打破。新增边界优先 regression，Primary Form focused 14 passed；完整 Runtime 将从 103 增至 `104 passed / 12 ignored`。这是收敛判定修复，没有新的真实 likeness receipt，仍保留 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING` 与 human/PBR/export-restart/360 未运行或阻断。

2026-08-15 Primary Form 双向边界证据 handoff：Runtime 的同一 candidate-bound 观察现在保留 target boundary→model boundary 和 model boundary→target boundary 两个有限方向，后者用于暴露模型独有/错位边缘；两侧均复用最终 camera、Part-ID 与固定 Render Worker cohort，并在既有 per-Part coverage 后截断到最多 64 条。新增 regression 与完整 `forgecad-runtime` Gate 通过（`103 passed / 12 ignored`）。这是 Runtime 收敛证据覆盖修复，没有新的真实 likeness receipt，不升级 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING` 或 human/PBR/export-restart/360；Viewer 仍只消费 Runtime quality/read model。

2026-08-15 Render Worker ownership handoff：Runtime 现在由 `geometry_worker.rs` 提供不带产品身份的 `execute_sibling_worker` transport，`render_worker.rs` 自己声明 `forgecad-render-worker` 并负责 Render Worker typed response projection；Geometry Worker 不再含 Render Worker binary/adapter 符号。新增真实 sibling 隔离测试覆盖九 AOV、512×512 PNG、重复渲染确定性和 GeometryProgram 载荷拒绝，普通 `forgecad-runtime` 全量为 `102 passed / 12 ignored`，source ownership checker 通过。它只证明模块/进程边界落地，没有新的真实 likeness receipt，仍保留 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING` 与 human/PBR/export-restart/360 未运行或阻断。

2026-08-15 Primary Form evidence-magnitude handoff：Runtime bounded Rig 坐标探测的第一轮现在读取同一 candidate-bound boundary proposal 的有界改变量；若当前坐标已等于 proposal 或进入后续 pass，则回退到原有 `step_fraction` 小步，单次仍不超过 authored range 的 50%。新增 magnitude regression，`forgecad-runtime` 全量 102 passed、10 ignored。该修复减少“已观察但只微调一次”的收敛浪费，不把搜索交给 Codex、不改变单 Part Agentic action、Render Worker 或 Viewer read-only 边界；没有新的真实 likeness receipt，仍保留 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING` 和未运行的人评/PBR/export-restart/360。

2026-08-15 Primary Form boundary evidence coverage handoff：Runtime 的 boundary observation 现在先按可见 `part_id` 保留每个 Part 的最高误差 segment，再以全局距离补齐至固定上限 64，并最终保持距离降序与确定性输出。该模块修复同一 shin/head 误差占满 Codex 观察表、导致 secondary Part 无法进入 bounded sweep 的缺口；新增 coverage regression，`forgecad-runtime` 全量 101 passed、10 ignored。没有新的真实 likeness receipt，不升级 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING` 或 human/PBR/export-restart/360；MCP schema/tool count、Agentic 单 Part action contract 和 Viewer read-only authority 不变。

2026-08-15 Primary Form proposal-direction retention handoff：Runtime 把完整 boundary projection 与 dominant-Part probe-zero seed 分开保存；secondary Part 的后续 bounded coordinate probe 读取对应 proposal 的显式方向，只有真正零改变量才使用稳定 fallback。新增 `-1/0/+1` direction regression，`forgecad-runtime` 全量 100 passed、10 ignored。该修复继续保持 Runtime-owned continuous search、单次 Codex typed intent 和 Agentic 单 Part contract，没有新的真实 likeness receipt，不升级 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING` 或 human/PBR/export-restart/360。

2026-08-15 Primary Form bounded multi-Part sweep handoff：Runtime 仍以 candidate-bound 最大边界误差 Part 生成局部 seed，避免首个联合 proposal 耦合无关轮廓；但后续固定 coordinate schedule 不再把参数过滤成单一 Part，而是在同一 bounded Geometry/Render Worker 预算内遍历完整 typed Rig。新增 seed/sweep coverage regression，`forgecad-runtime` 为 100 passed、10 ignored。该修复针对“26-control 计划实际只搜索 dominant Part”的收敛缺口，没有新的真实 likeness receipt，不升级 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING` 或 human/PBR/export-restart/360；现有 Agentic action-run 仍是单 Part bounded contract。

2026-08-15 Primary Form local Part offset calibration handoff：Runtime 的局部 Part envelope proposal 现在把米制 `offset_x/offset_y` 转换为 camera-plane world meters，并统一 image-Y/down 与 camera-plane/up 的符号；没有 camera 的旧 `part_contour_fit_prepare` 路径对米制偏移返回 neutral，避免把 Rig step 当成世界尺度。新增相机标定/纵向符号 focused regression，`forgecad-runtime` 全部 109 个单元测试通过（99 passed、10 个隔离 worker 测试按要求 ignored）。该修复只作用于 Runtime 收敛输入，没有 Schema/工具数变化和新的真实 likeness receipt；继续保留 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING`、human/PBR/export-restart/360 未运行事实。

2026-08-15 Primary Form same-camera retention handoff：Runtime 现在先在最终选定 camera 上对 authored source 与 proposed GeometryProgram 做一次不写入 candidate/VisualEvidence 的 512px full-resolution compare；只有同一 `camera_fit_loss` 严格改善才调用 `geometry_prepare`。输出新增 `PrimaryFormAcceptance@1`，绑定 source candidate、proposal program、target/reference/camera hashes、source/proposal loss 和 `accepted`/`retained_source`。这修复了“transient fit 因 camera compensation 看似改善、staged compare 实际回归”的晋级缺口；contracts 与 focused Runtime regression 通过，但没有新的真实 likeness 证据，仍保留 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark incomplete binding 和未运行的 human/PBR/export-restart/360。

2026-08-15 Agentic observation→action receipt handoff：`design_action_run_prepare` 在通过 `validate_observation_claims` 后，必须把这次唯一 `AgenticSceneObserveResult@1.canonical_sha256` 固化为 `DesignActionRun@1.observation_sha256`；Store/CAS immutable readback 与合同都拒绝缺失或非法 hash。Runtime action focused round-trip 验证 receipt 与同 candidate 的观察一致。这收口 Codex 观察、candidate-bound evidence 和 bounded action 的同一证据链，但不执行 Repair、不改变 Viewer 的质量权威，也不升级 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark incomplete binding 或人评/PBR/export-restart/360 状态。

2026-08-15 Primary Form convergence receipt handoff：Runtime 的 `SilhouetteFitResult@1` 新增同一候选/目标下的 baseline 与 selected camera/metrics/loss、严格改善布尔值和 camera/geometry evaluation 分解；`DesignActionRun@1.evaluate` 现在同时回指 Primary Form result、QualityReport 和 comparison hash。`DesignActionRun@1` schema 已收口到 Store 实际 `output_sha256`/`summary_sha256`/`error_code`/`skipped` 字段，并有旧 `hash/reason` 负向 fixture。contracts、Agentic/MCP、Stage 0 和 Runtime focused tests 通过；这只修复证据/观察边界，不代表 Primary Form 已视觉收敛，当前仍 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark incomplete binding。

2026-08-15 Render Worker adapter handoff：Runtime 新增 `apps/desktop/src-tauri/crates/forgecad-runtime/src/render_worker.rs`，集中解析 fixed/perspective/batch Render Worker response；`geometry_worker.rs` 不再暴露 RenderPass 或 render protocol façade，只提供 Geometry compile、共享 sibling launcher 与 `execute_render_worker` transport seam。Primary Form 所有 camera/geometry refit render 已改走 adapter。`python3 scripts/check_render_worker_boundary.py`、Runtime check 与四项 focused tests 通过；这只证明模块/进程/协议边界，不产生新的 likeness 结果，仍保留 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH` 与未运行的人评/PBR/export-restart/360。

2026-08-15 Agentic bounded action-run handoff：Runtime 已落地 `design_action_run_prepare`/`design_action_run_get` 的最小写入切片。它要求 approved session 的 project/candidate/reference scope，只接受一个 Part 的 `primary-form` bounded repair；Runtime 负责 Rig 绑定、编译、strict GLB readback、Render Worker、candidate-bound compare 和 SQLite/CAS immutable receipt，MCP 做 typed scope/approval/manifest 适配。action result 明确锁定 `confirm`/`export`，不写 candidate/version 或用户持久数据；idempotent prepare/get focused Runtime test、MCP boundary/manifest、Stage 0 通过。不要把它写成通用 orchestrator、Repair 已执行或视觉 PASS；当前质量仍 `QUALITY_TARGET_NOT_MET`，benchmark `BLOCKED_INCOMPLETE_BINDING`，camera `MISMATCH`，F 仍唯一 `in_progress`。

2026-08-15 Primary Form 自动目标单 Part 归因 handoff：Runtime `part_contour_fit_prepare` 现在可在 automatic silhouette target 没有显式 Part slice 时，使用同一 candidate-bound Render Worker Part-ID boundary samples 投影局部 target envelope/error；这不推断 hidden geometry。`scripts/probe_mcp010f_part_correction.py` 已支持 `--target-mode automatic` 与 `shin-pair` 的镜像源节点 patch，并记录 MCP/Runtime build cohort。current cohort `77ccce85…b9d4` 的真实 reference receipt `docs/evidence/mcp010f/part-correction-current-20260815-shin-auto.json` 生成了有界小腿 proposal，但最佳 loss candidate 的 IoU `0.740801` / Boundary F1 `0.327508` 仍低于 authored baseline IoU `0.741047` / Boundary F1 `0.328765`；因此 `BASELINE_RETAINED_NO_NON_REGRESSING_SINGLE_PART_CANDIDATE`，没有 candidate 被接受、confirm 或 export。质量仍 `QUALITY_TARGET_NOT_MET`，camera/benchmark/human/PBR/export-restart/360 状态不变。

2026-08-15 Primary Form action budget repair handoff：修复前 current-cohort receipt `docs/evidence/mcp010f/primary-form-budget-pre-fix-real-codex-20260815.json` 证明 Codex 请求的 `max_evaluations=64` 被 `primary_form_repair_prepare` 外层 cap 静默压到 24；源码已恢复 action cap `64`，continuation 上限改为 2，连续搜索仍由 Runtime/Workers 持有。新增 optimizer normalization regression 与端到端 Runtime fixture 通过，64 请求在小 fixture 中实际完成 63–64 次 bounded fit。Dev.app cohort `c521bf28…c4a5` 已安装；修复后真实 CLI 尝试 `docs/evidence/mcp010f/primary-form-budget-current-cohort-blocked-20260815.json` 在 geometry hash/prepare authoring sequence 阶段阻断，未产生新的 Primary Form/compare 结果。保留 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING`，不宣称高质量或 likeness PASS。

2026-08-15 packaged Render Worker landing handoff：当前 source revision `cb1a6981…f805d` 的 Dev.app 已将 `forgecad-render-worker` 作为同 cohort Resource 实际打包、安装并通过 ad-hoc deep-strict package verify；MCP/Runtime/Geometry/Render 四个组件 cohort 均为 `aa5eaaa2…5827`。packaged Runtime 在短路径隔离临时根目录中通过兄弟 Render Worker 完成九 AOV、固定 512px renderer、两次 deterministic hash、compare 和 image-block transport，未触碰持久用户数据。三份 receipt 已纳入 MCP010F manifest；由于输入是 synthetic reference，receipt 只证明 packaged Worker 进程/资源/协议落地，仍是 `QUALITY_TARGET_NOT_MET`、`structural_visual_claim=NOT_CLAIMED`，不升级真人 likeness、PBR、人评、export/restart 或 360。

2026-08-14 Primary Form 联合 proposal 回退 handoff：Runtime 现在在同一 dominant Part 内对证据提案执行固定 `1.0 → 0.5 → 0.25` 回退；只有严格改善才更新 Geometry incumbent，之后才进入单坐标探测。回退值始终从 authored baseline 插值且不扩大 `SilhouetteRig@1` bounds，所有试算继续经 Geometry Worker、strict readback 和隔离 Render Worker。focused/full Runtime 与 contracts 通过；没有新的授权参考字节或视觉 receipt，仍保持 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`，不代表 Repair 已执行。

2026-08-14 Primary Form bilateral Part-ID projection handoff：Runtime 已把 Render Worker 的左右显式 Part-ID 聚合到同一 Rig `*-pair` semantic Part，再计算局部 envelope proposal；这补上了“主导 Part 已选中但 pair proposal 未被投影”的收敛缺口。focused/full Runtime、MCP010C/F source 与 Render Worker boundary Gate 通过；没有新的授权参考字节或视觉 receipt，当前仍为 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`。

2026-08-14 Primary Form 单 Part action scope handoff：Runtime 已把 candidate-bound boundary evidence 聚合出的主导 Part 设为单次 bounded repair 的唯一 mutable scope，并对其他 Part 恢复 authored baseline；critic projection 现在把 Codex-facing repair operation 固定为 `primary_form_repair_prepare`，避免重新暴露 `silhouette_fit_prepare` 连续搜索。focused/full Runtime、Agentic contract、MCP010C/F source 与 Render Worker boundary Gate 通过。当前没有新的授权参考字节或视觉 receipt，仍保持 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`，Repair 尚未执行。

2026-08-14 Primary Form Part-priority follow-up：`silhouette_fit_prepare` 已将 candidate-bound `boundary_error_segments_for_masks` 传入 Runtime-owned Rig ranking。排序优先级为 Part 聚合边界误差 → 当前参数 proposal delta → `parameter_id`，没有 Part-ID evidence 时回到既有排序；这只改变有限预算的坐标覆盖顺序，不暴露连续搜索给 Codex，也不改变 Viewer 质量权威。回归测试与 MCP010F source Gate 通过；新的真实视觉复验需要重新提供/授权参考原图，当前保留 r3 的 `QUALITY_TARGET_NOT_MET` 事实。

2026-08-14 Primary Form 首轮全控制覆盖修复：detail probe 的 `SilhouetteRig@1` 现有 26 个有界控制，CLI 将 `silhouette_fit_prepare` 的请求预算从 32 提升到 Runtime 合同允许的 64；有 GeometryProgram 时 Runtime 固定分配 `32 geometry + 16 initial-camera + 16 geometry-winner-camera-refit`，几何阶段包含初始证据提案后可完整遍历 26 个 Rig 控制，再进入反向方向探测。Codex 仍只提交一次 typed Rig，连续搜索和 Geometry/Render Worker 试算仍由 Runtime 持有。新增 26-control coverage regression 与 full Runtime 回归通过；本轮没有新的授权机器人视觉 receipt，当前仍为 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING`，human/PBR/export-restart/360 仍未运行或阻断。

2026-08-14 Primary Form 单动作真实 transport：`docs/evidence/mcp010f/real-codex-cli-current-20260814-primary-form-runtime-owned-r3.json` 在 current source-built cohort 完成 10 个 Codex turn、0 非零退出码；Codex 在聚合 observation 后只提交一次 `primary_form_repair_prepare`，没有独立 `silhouette_fit_prepare`，也没有重复 Codex `reference_compare_prepare`。Runtime-owned action 内完成 24 次 bounded fit、Geometry Worker/Render Worker 链和 candidate-bound compare，随后读取 Runtime visual evidence 并完成九 AOV/typed review。结果为 `PASS_WITH_QUALITY_TARGET_NOT_MET`，camera binding `PASS_SILHOUETTE_FIT_TO_COMPARE`，IoU `0.749122`、Boundary F1 `0.347623`；该 receipt 不升级 `QUALITY_TARGET_NOT_MET`、不 confirm/export，也不改变 human/PBR/restart/360 未运行事实。

2026-08-14 历史 cohort — Primary Form 肢段尺度/装配控制扩展：在既有 `SilhouetteRig@1` 的 typed `height` 与 `offset_y` 语义上，CLI detail probe 从 20 个有界控制扩展为 26 个；该时点 raw stdio 工具清单为 `36 read + 23 opt-in write = 59`，不覆盖当前 74-tool manifest。focused/full Runtime、MCP010C 与当时 Gate 通过；真实质量仍为 `QUALITY_TARGET_NOT_MET`，camera `MISMATCH`，benchmark `BLOCKED_INCOMPLETE_BINDING`。

2026-08-14 Viewer evidence lineage 收口：Runtime 的 `visual_evidence` 现在在返回 `ViewerVisualEvidence@1` 前，强制校验 candidate artifact、RenderSet、comparison report、QualityReport、reference/target 与 camera 的同一 hash lineage；缺失 comparison、错误 artifact、错误 target 或 camera 漂移均 fail closed。新增真实负向回归验证合法 RenderSet 被重绑到错误 artifact 时拒绝；同时修正 `primary_form_repair_prepare` 的 optimizer schema，使其使用 MCP 有界验证器支持的 `minimum` 正数下限。Viewer 仍只消费 Runtime `QualityReport@2`，本修复不改变机器人 `QUALITY_TARGET_NOT_MET`、`camera=MISMATCH`、`BLOCKED_INCOMPLETE_BINDING` 或 human/PBR/export-restart/360 未运行事实。

本轮按用户要求进行架构/模块/隔离重规划：新增 `docs/ADR/0026-agentic-design-runtime.md`、`docs/FORGECAD_AGENTIC_DESIGN_RUNTIME_PLAN.md`、`docs/ARCHITECTURE_MODULE_BOUNDARY.md` 与 `docs/DEPRECATED_ISOLATION_PLAN.md`。新方向是 Agentic Design Runtime：Codex/Pi-style thin harness + Runtime-owned DesignSession + SemanticSceneGraph/ModelUnderstandingBundle + ReferenceCanvas/DesignSpec + stage gates + Visual Evidence Bundle + Critic/Repair loop。第一阶段已交付 Runtime/MCP/Viewer 的只读 projection；真实 Runtime 的嵌套只读 projection producer/consumer conformance 已通过独立回执；本轮又交付受批准的 durable session/checkpoint/RepairIntent prepare/readback、重启验证和 Viewer durable lookup，并落地 MCP010F 窄范围 Primary Form 单动作 prepare/evaluate。durable/reference/DesignSpec 完整 producer、通用单动作 orchestrator 和 Repair 应用仍未完成。当前为 191 Schema、21/21 active operators、54 read + 36 opt-in write = 90 tools、唯一 `FGC-MCP010F in_progress`，机器人 `QUALITY_TARGET_NOT_MET` 事实不变。

2026-08-14 当前源码 cohort 的补充真实复跑已完成，receipt 保存在 `docs/evidence/mcp010f/primary-form-cas-rerun-20260814.json`。它使用 CAS 中同一授权参考、15 个 landmark 与 8 个区域，MCP/Runtime/Geometry/Render/Viewer cohort 均为 `b0f9cfa2…ab04`，11 个 Codex turn 全部零退出码；Primary Form fit `evaluations=32`、IoU `0.749616`，最终 compare IoU `0.746740`、Boundary F1 `0.342841`、bbox `0.003906`、centroid `0.002700`、landmark coverage `0.139498`、NME `0.075107`、region median `0.877606`、critical min `0.672417`。fit→compare camera binding 为 `PASS_SILHOUETTE_FIT_TO_COMPARE`，packaged Viewer exact lineage binding 为 PASS，但质量仍为 `QUALITY_TARGET_NOT_MET`，human/PBR/export-restart/360 仍 `NOT_RUN/BLOCKED`；该补充 receipt 不改写 Stage 0 冻结的 provisional observation、latest pointer 或 benchmark eligibility。

2026-08-14 Primary Form objective alignment 修复：Geometry trial 现在与 camera trial 共享同一 landmark/coverage-aware `camera_fit_loss`，不再用 contour-only loss 与带 landmark 惩罚的 loss 做不可比的严格改善判断；新增 focused objective regression，完整 Runtime 回归为 `86 passed / 10 ignored`。新 cohort 的真实 Codex 复跑在 authoring sequence 阻断，未产出新的质量结果，因此既有 `QUALITY_TARGET_NOT_MET` 与未运行门保持不变。

2026-08-14 Primary Form 提案边界修复：当 Runtime 的几何试算严格优于 authored baseline 时，`SilhouetteFitResult@1` 现在返回同一次 Geometry Worker 校验过的 `selected_geometry_program`；无严格改善时返回 `null`。后续可在用户批准后直接将该 typed V2 程序交给 `geometry_prepare`，不再让 Codex 重新拼装参数或重复连续搜索。该字段仍是只读提案，不创建候选、版本或确认；合同校验、focused silhouette-fit 回归和 MCP010F source Gate 通过，尚无新的真实机器人质量 receipt。

2026-08-14 Primary Form bounded schedule 修复：camera coordinate-descent 在局部批次没有改善时不再提前退出，因此声明的 bounded camera remainder 会继续遍历后续 roll/FOV/distance/target-offset/global-scale 轴；这修复了 `max_evaluations=24` 可能只执行 16 次、让后续轴永远不可见的实际截断。Runtime focused regression 已验证 8-camera fixture 完整消费 `evaluations=8`；Geometry/Render Worker、Runtime sole-writer 和 Viewer 只读质量边界未改变。本轮没有新的授权机器人视觉 receipt，仍保留 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING` 及未运行的 human/PBR/export-restart/360 门。

2026-08-14 Agentic observation consolidation 修复：`VisualEvidenceRecord` 新增 nullable target binding，target-bound compare 的 `target_sha256` 现在通过 Runtime SQLite/CAS、Viewer read model 和 Agentic aggregate 同 hash 回读；critic 把失败指标压缩到一个固定优先级 directive/Runtime-owned bounded intent，Codex 不再承担连续参数搜索。default comparison 兼容路径保持 nullable；target-bound regression、Runtime 87 passed/10 ignored、Store 8 passed、contracts/Agentic contracts 和 MCP010F Gate 均通过。该模块不执行 Repair、不写 candidate/version、不升级 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH` 或 `BLOCKED_INCOMPLETE_BINDING`。

2026-08-14 Primary Form 收敛/观察模块修复：`silhouette_fit_prepare` 不再把 `max_evaluations=24` 静默截成 camera/geometry 各 8 次；有 GeometryProgram 时按 `geometry=12 / initial-camera=6 / winner-camera-refit=6` 的 Runtime-owned bounded 预算执行，且坐标探测先覆盖完整当前 12 参数 Rig，再进入反向方向探测。几何胜者会在同一最终 Geometry Worker GLB 上通过隔离 Render Worker 重新拟合局部相机邻域，最终 camera/metrics 才交给 candidate-bound compare。每次几何试算仍经 Geometry Worker 编译、严格 GLB 回读和 Render Worker 单请求渲染，authored baseline 仍只有在严格改善时才替换。CLI receipt 新增 target/observation/fit 分阶段序列和 `AgenticSceneObserveResult@1` 绑定摘要；Viewer source checker 与当前统一 `error-console` 异常面板对齐。新增 budget/coverage/refit focused tests、`script/test_mcp010c.sh`、`script/test_mcp010f.sh`、Tauri check、TypeScript/Vite build 均通过；没有新增真实 likeness receipt，现有 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING` 与 360/human/PBR 未运行事实不变。

2026-08-14 Primary Form 第二轮修复：真实图像 landmark 不再只参与 transient loss；当 Render Worker 返回同一 candidate 的 Part-ID pass 时，Runtime 按固定 landmark→Part 词表计算部件锚点误差，把它转换为相机标定的相机平面米制 `offset_x/offset_y` typed proposal，再沿 camera right/up basis 编译 GeometryProgram。当前 detail probe 的 Rig 为 20 个有界参数、`max_evaluations=32` 时 Runtime 分配 `geometry=21 / camera=11`；Codex 仍只提交一次 Rig proposal，参数搜索和 Geometry/Render Worker 试算仍由 Runtime 持有。新增两项 Runtime focused test、完整 Runtime 回归和 MCP010F source Gate 均通过；本轮没有新的授权机器人端到端 receipt，因此不得升级现有 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH` 或 benchmark eligibility。

本轮也将三个已标记 superseded 且未出现在 active registry 的 Bundle 移入 `packages/forgecad-skills/archive/superseded/`：早期 `reference-to-typed-plan@0.1.0`，以及分别由 active `0.2.0` 替代的 `hard-surface-detail@0.1.0`、`uv-pbr@0.1.0`。archive manifest 保留每个 Bundle 的 pre-archive tree hash 和 replacement；Runtime build 只嵌入 registry 声明的 Bundle，`scripts/check_mcp006_skills.py` 会拒绝未注册版本回到 active `bundles/**`。它们只保留历史 provenance，不得被 `skill_list`、`registry.json`、Runtime build archive 或当前能力账本引用为 active。

本轮 contour-fit Runtime 已补齐真实 V2 Rig 试算：`silhouette_fit_prepare` 读取候选绑定 GeometryProgram，按 `step_fraction` 编译/回读/渲染有限几何变体，并返回 `geometry_evaluations` 与 `parameter_deltas`；该调用保持只读，不自动创建 candidate/version。

2026-08-13 模块化修复增量：Render Worker 已从 stub 变为隔离、一次性、逐行 JSON 协议进程；Runtime 的 render_fixed/render_glb/render_glb_fit_batch 现在经该进程边界执行，geometry worker 仍保持独立职责。Primary Form 的 `silhouette_fit_prepare` 已由 Runtime 持有有界 Rig 坐标下降、按证据排序的参数邻域和确定性预算，Codex 只提交一次 typed proposal，不再承担连续参数搜索。Viewer 已移除本地阈值/工作流推导，改为只显示 Runtime Agentic projection 与 candidate-bound `QualityReport@2`。`script/test_mcp010c.sh` 与 `script/test_mcp010f.sh` 聚合门均通过；真实 C raw stdio 也通过 Render Worker、九 AOV、比较/评审链路。该增量证明模块边界和结构闭环，不改变真实机器人 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、current-cohort Viewer CLI read-model exact lineage PASS、UI/accessibility/image-block consumption/人评/VoiceOver/360 未运行等事实。

2026-08-14 F 回归修复：`silhouette_fit_prepare` 现在保留每个实际 camera-fit probe 的 transient silhouette/Part-ID 证据，直接复用 winner 的同一视图，不再为 selected camera 追加未计入预算的重新渲染；Primary Form geometry search 以 authored baseline 为保底，只有严格改善才接受 Runtime proposal，避免“proposal 未收敛却替换基线”。新增 Runtime focused regression 还验证 `fit winner CameraCalibrationRef@1 → reference_compare_prepare` 在 fresh Runtime 内返回相同 `camera_hash/canonical_sha256`。这证明了新路径的 camera handoff 和 baseline fallback，但不会改写 attempt35 的历史 mismatch receipt；该用户参考仍需重新走同一 cohort 的真实端到端传输后才能改变 `BLOCKED_INCOMPLETE_BINDING`。

同轮又把 canonical observation 接入真实 Codex silhouette-first 编排：CLI source probe 的单个视觉回合强制 `silhouette_target_get → scene_observe_get → camera_fit_prepare → silhouette_rig_hash`，Runtime 返回的 `AgenticSceneObserveResult@1` 必须绑定同一 project/candidate、`read_only=true` 且带 canonical hash；脱敏 receipt 将记录 observation hash，防止 Codex 在相机/比例决策前拼接零散的 project、artifact、quality 读取。该 source 编排尚未用新的授权机器人 PNG 重跑，因此不能把它写成新的 likeness 或 benchmark evidence。

2026-08-14 模块边界收口：Render Worker 的 `--isolated-once` 已改为严格单请求生命周期，读取有界 stdin 后只解析并处理一个 typed request，第二个 JSON payload 不会在同一子进程中复用；`script/test_mcp010c.sh` 已加入双 payload 负向回归。Primary Form 的全局 bbox proposal 改为按 X/Y 轴分别计算 width/height ratio，scale 使用两轴几何平均，并保留 authored baseline 与“严格改善才替换”规则，避免连续搜索把一个轴的误差污染另一个轴。Viewer 的 `visualHardGatePassed` 现在直接显示 Runtime `QualityReport@2.hard_gate_passed`，source checker 拒绝本地 status/metric 二次谓词。`script/test_mcp010c.sh`、`script/test_mcp010f.sh`、Viewer typecheck/build 和 focused Primary Form regression 均通过；这些修复仍不改变 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING` 或 packaged/human/VoiceOver/export-restart/360 的未运行状态。

2026-08-14 同 cohort 真实闭环复跑又暴露并修复了一个 Runtime 合同缺陷：当 geometry probe 没有严格改善 authored baseline 时，`silhouette_fit_prepare` 曾把完整 `SilhouetteRig@1` parameter definition 写入 `SilhouetteFitResult@1.selected_parameters`，触发三字段 compact projection 校验失败。现在 Runtime 在结果边界统一压缩为 `parameter_id/part_id/value`，并有 focused regression；同一 fresh Runtime 的 detail route 真实 receipt `docs/evidence/mcp010f/real-codex-cli-current-20260814-same-cohort.json` 已完成 `scene_observe_get` 聚合观测、fit、compare、九 AOV、typed review 和 quality readback。该 receipt 的 MCP/Runtime/Worker cohort 均为 `1aae2448…`，fit/compare 相机 hash 与 canonical hash 一致，fit `evaluations=16`、silhouette IoU `0.690952`、Boundary F1 `0.256758`，仍为 `QUALITY_TARGET_NOT_MET`。由于 12 个 Codex turn 中有 1 个退出码为 1，探针按 fail-closed 记录为 `BLOCKED`；44 个 typed MCP 调用均完成且无额外副作用，因此该 receipt 只作为当前修复证据，不晋升 provisional benchmark 或高质量结论。

2026-08-14 相机拟合分辨率加固：为避免 `128×128` 粗搜的栅格 aliasing 把不一致的 camera 送入 512px compare，Runtime 对粗搜排序后的最多五个候选及 authored base camera 使用同一隔离 Render Worker 的 `render_glb_fit_batch` 一次性做 `512×512` verification，再按完整内部损失选 winner；公开 `CameraFitResult@1` 仍保持四项 compact metrics，未扩展合同。`script/test_mcp010c.sh` 与 `script/test_mcp010f.sh` 聚合门、Runtime focused tests 及四个最新 build identity 均通过。此前两次停在外部 host `reference_get` setup turn 的重试按历史阻断记录保留。

2026-08-14 当前真实 Codex 复跑已完成：setup turn 一次完成 `project_create → reference_import → reference_get`，随后由同一视觉回合调用单次 `scene_observe_get`，再完成 camera fit、16 次 Primary Form 有界评估、compare、九 AOV、typed review、quality readback，并验证 packaged Viewer exact project/candidate/artifact/reference/RenderSet/comparison binding。receipt `docs/evidence/mcp010f/real-codex-cli-current-20260814-viewer-bound.json` 使用同 cohort `d11e83cc…e07264`，11 个 Codex turn 全部退出码 0，fit→compare camera binding 为 `PASS_SILHOUETTE_FIT_TO_COMPARE`；但 IoU `0.688698`、Boundary F1 `0.248825`、bbox `0.035156`、centroid `0.042175`、landmark coverage `0.517270`、NME `0.153089`、region median `0.759490`、critical min `0.389423` 仍未达到严格质量门。该 receipt 已记录为最新 completed transport，但未替换 attempt35 provisional observation，也没有宣称 likeness/PBR PASS；脱敏 CLI 事件仍未观察到真实 Codex 消费返回 image blocks，正式人评、VoiceOver、PBR、confirm/export、restart hash 和 360 仍未运行/阻断。

2026-08-14 最新模块事实：r3 已成为 latest completed transport 与 latest attempt；r1 的 compare camera cache 绑定阻断、r2 的 `reference_get` setup 阻断均按原始 receipt 保留。r3 的 `primary_form_runtime_compare=true` 证明 Viewer/CLI 读取的是 Runtime-owned candidate-bound visual evidence，而不是再次推导质量；Runtime/Viewer 仍保持 `QualityReport@2` 唯一质量权威。r3 只达到 `QUALITY_TARGET_NOT_MET`，正式人评、PBR、export/restart、VoiceOver/accessibility 与 360 仍未完成。

2026-08-14 Render Worker 输入与源码边界收口：Runtime 的 `render_fixed` 先调用 Geometry Worker 编译，再只把有界 GLB 交给 Render Worker；Render Worker 通过窄化的 `render_worker_result` façade 拒绝 `geometry_program`/`appearance_program` 编译输入，保留 `render_glb` 与 `render_glb_fit_batch`。渲染实现已从 `forgecad-geometry-worker` 拆到无状态的 `apps/render-core`，Render Worker 不再依赖 Geometry Worker crate；`scripts/check_render_worker_boundary.py`、`render_worker_rejects_geometry_compile_payload` 与 `script/test_mcp010c.sh` 均通过。该结果将 process/protocol/source ownership boundary 记为 PASS，但不改变 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、packaged/live C/F、真实视觉、人评、PBR、export/restart 和 360 的独立状态。

更新时间：2026-08-14
分支：`main`；工作树包含受控 MCP010B/C/D/E/F 修改
任务状态：`FGC-MCP005–MCP010A done（MVP host golden path + Dev activation）`；`MCP010B blocked/deferred（Darwin OS hard cap NOT_RUN）`；`MCP010C source-focused PASS_WITH_UNRUN_VISUAL_GATES`；`MCP010D source-focused PASS_WITH_DEFERRED_BOOLEAN_AND_VISUAL_GATES（packaged D structural PASS）`；`MCP010E source-focused PASS_WITH_DEFERRED_EXTERNAL_AND_VISUAL_GATES（packaged E structural PASS）`；`MCP010F source + packaged read-model/core-control AX smoke in_progress（正式 VoiceOver/人评/360 NOT_RUN/BLOCKED）`
当前机器口径：191 Schema、21/21 active operators、54 read + 36 opt-in write = 90 tools；唯一 `in_progress` 是 `FGC-MCP010F`。Mechanical pose 与 Agentic observe/plan 均为只读 projection；`RenderSet@2` 额外绑定 Runtime 已验证的 Render Worker cohort/status，Viewer 只读显示，不从它重算质量门。统一证据入口为 `docs/evidence/mcp010f/current-benchmark-truth.json`，durable session/checkpoint/RepairIntent 证据为 `docs/evidence/mcp010f/agentic-runtime-session-checkpoint-20260813.json`。另有 `primary_form_repair_prepare`、`primary_form_repair_job_prepare` 与 `repair_intent_run_prepare` 的 Runtime focused/source/real-Codex evidence；它们只产生 staged candidate，不证明通用 orchestrator、Repair 应用或质量通过。attempt35 只称 `provisional retained observation`，视觉状态为 `QUALITY_TARGET_NOT_MET`，benchmark eligibility 为 `BLOCKED_INCOMPLETE_BINDING`，因为 camera-fit 选中相机 `354caf27…f95788` 与 reference-compare 相机 `8cd20605…a535` 为 `MISMATCH`；packaged Viewer current-cohort read-model binding 已单独通过，但尚未绑定 attempt35 的同一 candidate/artifact/RenderSet/comparison。它不是 best 或合格 benchmark；source/raw/transport/build/AX smoke 也不是视觉、人评或 packaged E2E PASS。
运行顺序以 `docs/evidence/mcp010f/real-codex-run-inventory.json` 为准：attempt5 是历史 CameraCalibrationRef 里程碑；最新完成传输与最新尝试均为 `docs/evidence/mcp010f/real-codex-cli-current-20260814-primary-form-runtime-owned-r3.json`，状态为 `PASS_WITH_QUALITY_TARGET_NOT_MET`，已纳入 Stage 0 latest pointer；它证明 Runtime-owned single-action fit/worker/compare transport，但未晋升 attempt35；r1 的 camera-cache 阻断、r2 的 setup 阻断和历史 semantic-landmark-compare/semantic-aligned-fast 仍按原始 receipt 保留。

Agentic 交接：`scene_observe_get`、`design_stage_plan_get`、`critic_report_get`、`visual_evidence_bundle_get` 仍为 Runtime-owned read-only projection；`session_create_or_resume`、`checkpoint_prepare`、`checkpoint_restore_prepare` 是受批准的 durable prepare，`session_get`、`checkpoint_get` 是只读 readback。`scripts/probe_agentic_runtime.py` 先读取 `ponytail-preflight@0.1.0`，再验证空 reference fail closed、blocked actions、project binding、SQLite/CAS 重启读回和无用户持久数据触碰；`scripts/check_agentic_runtime_receipt.py` 校验 durable 公开合同；`scripts/check_agentic_projection_receipt.py` 校验真实 Runtime 的 scene/stage 嵌套只读 projection producer/consumer conformance。当前 `AgenticSceneObserveResult@1` 仍是 source transport envelope，durable/reference/DesignSpec 完整 producer、单动作 orchestrator 和 Repair 应用仍未完成。证据：`docs/evidence/mcp010f/agentic-runtime-observe-plan-20260813.json`、`docs/evidence/mcp010f/agentic-runtime-projection-conformance-20260813.json` 与 `docs/evidence/mcp010f/agentic-runtime-session-checkpoint-20260813.json`。

<!-- forgecad-stage0: schemas=402 schema_set_sha256=2f31c744134257e4d455cfc801a9be9e5c38ae81b1bc7fabd80d2002f968c4c7 read_tools=90 write_tools=69 total_tools=159 task=FGC-MCP010F observation=QUALITY_TARGET_NOT_MET eligibility=BLOCKED_INCOMPLETE_BINDING evidence=INCOMPLETE_TRUTH_BINDING camera=MISMATCH packaged=PASS_CURRENT_COHORT_BOUND_READ_MODEL latest_attempt=real-codex-cli-current-20260815-b37-complete-auto-v3.json latest_completed=real-codex-cli-current-20260815-b37-complete-auto-v3.json -->
本轮轮廓优先修正已完成 source/真实 Codex transport 验证：Runtime 自动 mask→contour 改为有向栅格边界追踪，最大外环重建回归 IoU > 0.94；有 15 个 intake landmarks 的同 cohort receipt `docs/evidence/mcp010f/real-codex-cli-landmark-aware-20260813.json` 已完成 target/camera/Rig/fit/compare/九 AOV/review/quality，但最终 IoU 0.685417、boundary F1 0.272115、landmark coverage 0.666667、NME 0.134407，与 attempt35 provisional observation 对照更低，仍 `QUALITY_TARGET_NOT_MET`。它证明地标已经进入 Runtime 的有界排序输入，不解锁材质、confirm/export、人评或 360。

本轮轮廓优先增量已接入当前 F source slice：Runtime/MCP 新增 `reference_mask_prepare`、`reference_mask_refine_prepare`、`silhouette_target_get`、`camera_fit_prepare`、`silhouette_fit_prepare`、`part_contour_fit_prepare`、`silhouette_candidate_compare`、`boundary_error_get` 和只读 `silhouette_part_error_get`。后者按同一 candidate/target hash 返回所有声明 Part 的局部 envelope、质心/宽高比、边界误差、状态和推荐修正 Part；多 Part 合成回归与真实 chest-shell source probe 已通过。target/mask 使用 CAS hash 绑定参考与项目；camera fit 现在运行 37 个覆盖 yaw/pitch/FOV/distance/roll/target-offset/global-scale 的粗候选并对前三名各做 9 个局部探针（硬预算 64 次真实渲染）；boundary error 返回最多 64 个径向方向段。新增的 `SilhouetteTarget.parts` 现在要求唯一、非重叠且在 contour 范围内的区间，`part_contour_fit_prepare` 会把选中 Part-ID 的边界映射到对应 target slice；无 observed slice 时才回退整图诊断，不猜隐藏轮廓。Runtime/MCP focused target round-trip、bounded camera fit、Part-slice validation 和 dispatch 已通过，现有 `script/test_mcp010f.sh` 也纳入 source tests。attempt1–27 的失败/诊断 receipt 继续保留；attempt28 已通过可恢复的 12-turn source-built reference→mask/target→camera/Rig/fit→compare→boundary→九 AOV→review/quality transport，但仍为 `PASS_WITH_QUALITY_TARGET_NOT_MET`，不是 likeness PASS。下一轮应携带 image-derived intake，先读取 per-Part error table，只改一个 silhouette-bearing Part，再重复比较。

> MCP010B/C/D/E/F reconciliation：MCP006 的 44-contract 与已保存的 50/52-contract、3c/f488/d9 receipts是历史或结构事实。当前源码共 191 contracts、21/21 active operators、54 read + 36 opt-in write = 90 tools；MCP010C 的固定 renderer、九 AOV、candidate-bound comparison、MCP image block、Codex/human review 已通过 source raw Gate，当前同 cohort 真实 Codex CLI 也已完成六 turn/32 MCP 调用；D/E/F 当前 catalog 为 21 entries/21 active，包含产品自有 Manifold C ABI/FFI bounded same-Part Boolean 与 product-owned Authoring Mesh，同 cohort packaged D raw strict-readback probe，以及 E 的离线 AssetPack、512px UV atlas、固定 mikktspace、embedded PBR/九 AOV raw Gate也已通过，当前 packaged E 结构性用户参考传输也已通过；F 的只读 Viewer、contour-first target/`CameraCalibrationRef@1`、Runtime-owned bounded Primary Form search、Rig/SDF/Part/candidate compare/boundary、Mechanical pose source slice、Boolean Operand Lineage source slice、同 cohort packaged CLI read-model、原生窗口结构探针和 Computer Use 核心控件 AX smoke 已通过各自 source/结构 Gate；最新真实 Codex receipt 进一步完成 current-cohort Viewer exact project/candidate/artifact/reference/render-set/comparison lineage read-model binding。Mechanical pose、Authoring Mesh、Boolean evaluated-face lineage 与 Agentic observe/plan projection均不等于 orchestrator、Armature/skin、视觉、Viewer UI/accessibility/image-block consumption 或 packaged UI E2E 通过。attempt35 的 26 Parts/4704 triangles linework 只作为 provisional retained observation，其视觉状态为 `QUALITY_TARGET_NOT_MET`、benchmark eligibility 为 `BLOCKED_INCOMPLETE_BINDING`；B Darwin OS 总内存硬门、xatlas/Validator、正式 VoiceOver/无障碍、image-block consumption、人评阈值、PBR likeness、export/restart hash/360仍未完成。

> 用户要求的实际 Viewer 轮廓操作已完成：Computer Use 在打包 Viewer 选择 `chest-shell`、绘制并复制 candidate-bound 草图；两份 draft/intent 均通过校验。第二份草图驱动隔离 Runtime 的单 Part `chest-wedge-mild` 试跑，链路到 `quality_get` 完成且 hash 绑定正确，但 `QUALITY_TARGET_NOT_MET`（IoU 0.7399、boundary F1 0.3263、landmark coverage 0.6667、region median 0.8487），相对 attempt35 provisional observation 退化，故未晋级。未运行 confirm/export 或真人评审，未触碰用户持久数据；详细证据见 `docs/evidence/mcp010f/contour-execution-actual-20260812.json`。当前 `part_contour_fit_prepare` 已进一步改为按选中 Part 的局部宽高包围盒和质心偏移生成建议，避免把全身 bbox 误用于胸甲/肩甲；Runtime/MCP 35+66 focused tests 和 F source Gate 全绿，仍未宣称视觉门通过。

> 第二轮真实几何调优仍未改变可见质量门：`asymmetric-stance` 是局部最佳（26 Parts/3368 triangles，silhouette IoU 0.7490、region median IoU 0.8684），但 boundary F1 0.2381、bbox edge error 0.0195、landmark coverage 0.7333，仍未达全局门；`head-wedge`、`tapered-lower`、`asymmetric-armor` 均拒绝。随后 panel@1 的 bevel 网格改为固定四段圆角，得到历史 20-Part/3944-triangle receipt（`docs/evidence/mcp010f/rounded-panel-real-reference.json`）；当前在其上增加六类受限表面线流层，得到 26-Part/4704-triangle source baseline（`docs/evidence/mcp010f/surface-linework-real-reference.json`），并以完整 8-zone surface-zones 配方运行 material-zoned refinement（`docs/evidence/mcp010f/surface-zones-real-reference.json`）；当前正式 armor-shell-zones 候选实际使用其中 7 个区。曲面 Operator 相容法线平滑只改善固定 beauty pass 的连续着色，不改变 silhouette/quality 指标（receipt：`docs/evidence/mcp010f/smooth-curved-normals-real-reference.json`）。旧 3368-triangle receipt保留为历史，当前仍不能 confirm/export。

> 继续的肩甲轮廓校准也没有替换基线：较大幅度的显式左右 shoulder shell 拆分把 landmark NME 从 0.1345 降到 0.0700、region median IoU 提升到 0.8764，但 boundary F1 从 0.3288 降到 0.3275；更小幅度变体同样未恢复 boundary F1，均保留为 `NOT_PROMOTED/REJECTED` 证据。当前正式候选仍是 unrotated surface-linework + armor-shell-zones，不能因局部 landmark 改善而 confirm/export。
> 组合 `curved-tapered` 也已用当前同级 Worker/AssetPack/九 AOV 实跑：20 Parts/3320 triangles，strict readback 通过，但 silhouette IoU 0.7359、boundary F1 0.2786、region median IoU 0.8553 均不如 26-Part/4704-triangle linework 基线，beauty 仍是 blockout-like。该 receipt 仅作为负向证据保留在 `docs/evidence/mcp010f/experiment-curved-tapered-bb5c01.json`，默认路线不变。

> 真实用户 PNG 的完整 Part-sink `three-quarter` yaw 也已验证：20 Parts/3944 triangles、`-0.30 rad` transform@2 变体的 silhouette IoU `0.6578`、boundary F1 `0.2821`、landmark coverage `0.6000`，整体退化并被拒绝；当前保留 calibrated camera + unrotated model。

> 最新开发包 `d9c23b…ac0bd` 在 primitive-blockout 知识分支修正后重新构建并安装：ad-hoc/package、隔离 Ready/project、raw V2 和真实 Codex CLI V2 structural 均 PASS，`primitive-blockout@0.2.0` 为 active，候选仍未确认（12 Parts/896 triangles/161104-byte GLB）。用户完整重启后 d9 已成为当前 live Desktop cohort，已重新验证 32 工具、Ready、cohort/catalog/hash、active Skill 和临时项目 readback；该结构证据不宣称视觉/PBR/人评/360。

> 2026-08-10 真实用户授权图片演练已另外记录：`project-822d5513ad41499290910913cbc2bfd1` / `reference-35bc12fe88f349c9ba3590b271fb9130` / `candidate-205dce23763d4fdb98f7860699cd78b4`。V2 strict geometry/readback 通过（23 Parts、9964 triangles、1,592,884-byte GLB、完整 coverage/零 integrity errors），但 limited aspect quality 为 `0.5466 < 0.55`，candidate 未确认；这不是视觉相似度或 PBR PASS。完整脱敏 receipt 见 `docs/evidence/mcp010b/real-reference-robot-structural-run.json`。

> Codex 当前单图调用 checklist 见 `docs/CODEX_SINGLE_REFERENCE_OPERATING_GUIDE.md`。它只编排已激活的 catalog/GeometryProgram@2/readback 路线，并明确 limited quality、unknown region、confirm/export 和 C–F 交接停止条件；它不是新的运行时 Skill 或视觉质量证据。

> 同一授权参考的第二次 detail blockout 记录在 `docs/evidence/mcp010b/real-reference-robot-detail-blockout.json`：51 Parts、16,496 triangles、2,658,940-byte GLB 的 strict readback 通过，但 limited aspect proxy 为 `0.4604 < 0.55`。这次负向结果证明“增加 primitive 数量”不能替代固定相机、silhouette 和 region 比较；candidate 未确认、未创建 version/export。

> MCP010C source Gate：当前源码已提供固定 512×512 perspective/z-buffer renderer、九个 PNG AOV、本地 reference mask/metrics、`render_pass_get` image block、Codex typed review 和 HumanVisualReview receipt。`script/test_mcp010c.sh`、raw stdio receipt 与同 cohort 真实 Codex CLI C receipt 均已通过结构/传输门；同一 candidate 的五次 MCP comparison determinism 也已通过。CLI receipt 和早期固定相机机器人 receipt 保留 IoU `0.5132`、boundary F1 `0.1441` 的历史事实。最新同 cohort CLI 回归已验证默认 camera auto-fit 与视觉指标 CAS round-trip，IoU `0.6623`、boundary F1 `0.2418`、bbox edge error `0.0566`、centroid error `0.0135`，但仍为 `QUALITY_TARGET_NOT_MET`。Viewer compare source implementation 与同 cohort packaged CLI read-model 已通过只读 IPC/构建/投影测试；Tauri packaged Viewer UI/C、人评阈值、真实 PBR likeness/纹理审美、export/restart hash 和 HQ_360 仍 NOT_RUN/BLOCKED，详见 `docs/MCP010C_READINESS_AUDIT.md`。

> 最新 packaged contour receipt 还暴露了一个编排问题而非 Runtime 故障：该次 `ReferenceViewSpec@1` 没有任何 image-derived landmarks 或 visible regions，所以 `landmark_coverage=0`、`region_median_iou=0` 只能记为 `incomplete_visual_intake`；真正的首要失败项仍是 boundary F1 `0.2418`、silhouette IoU `0.6623` 和 bbox edge error `0.0566`。`scripts/build_mcp010f_contour_correction.py` 已生成 hash-only 下一轮计划，要求先补 normalized observed/inferred/unknown intake，再只修改一个 contour-bearing Part/Operator，轮廓和 form 门通过前不得进入 surface/UV/PBR。证据见 `docs/evidence/mcp010f/contour-correction-current-20260812.json`；它不写 Runtime/CAS，也不升级质量状态。

> 带真实 intake 的第二次 packaged Codex CLI 已完成：`real-codex-cli-current-20260812-packaged-intake.json` 绑定同一 PNG 的 15 个 landmark 和 8 个 visible region，消除了“没有标注”的歧义。区域填充已达到 region median IoU `0.8466`、critical-region minimum `0.5216`，而 silhouette IoU `0.6623`、boundary F1 `0.2418`、bbox edge error `0.0566`、landmark coverage `0.4667`、landmark NME `0.3367` 仍失败；下一轮只允许一个轮廓 Part/Operator 变化，计划见 `contour-correction-current-20260812-intake.json`。

> 历史 attempt35 receipt `docs/evidence/mcp010f/real-codex-cli-silhouette-first-20260813-attempt35-detail-camera-ref.json` 已完成完整 11-turn detail transport：reference/mask、V2 geometry hash/prepare、target、camera fit、Runtime-owned Rig hash、silhouette fit、compare、boundary error、九 AOV、typed review 和 quality 均完成，26 Parts/4704 triangles/validator passed，MCP/Runtime/Worker cohort `74749cc7…e1c3` 一致。它携带同一授权 PNG 的 15 个 image-derived landmarks/8 个 visible regions。结果是 `QUALITY_TARGET_NOT_MET`（IoU `0.741047`、boundary F1 `0.328765`、bbox edge error `0.007813`、centroid error `0.007878`、landmark coverage `0.733333`、landmark NME `0.134536`、region median `0.869403`、critical region min `0.666289`），只作为 provisional retained observation；camera-fit 选中 `354caf27…f95788`，reference comparison 实际使用 `8cd20605…a535`，故 benchmark eligibility 为 `BLOCKED_INCOMPLETE_BINDING`。Runtime fit proposal 为 `no_improvement`、IoU `0.698340`，仍是 read-only 建议，不是新 candidate。没有 same-observation packaged Viewer、human/PBR/confirm/export/360 证据，用户持久数据未改变；它不是 best 或合格 benchmark。
> 历史 CameraCalibrationRef 里程碑 `docs/evidence/mcp010f/real-codex-cli-camera-ref-20260813-attempt5.json` 在同一授权 PNG 上使用同 cohort `e968c9ef…6980`，通过 `camera_hash + canonical_sha256 + target_sha256` 让 Runtime 重新解析精确 CameraCalibration，完成 13-turn detail/九 AOV/typed review/quality transport。26 Parts/4704 triangles 的结果仍为 `PASS_WITH_QUALITY_TARGET_NOT_MET`（IoU `0.698465`、boundary F1 `0.281074`、bbox edge error `0.037109`、centroid error `0.049908`、landmark coverage `0.666667`、landmark NME `0.201432`、region median `0.771619`、critical region min `0.675106`）。它不是当前最新完成传输，也不是 benchmark；材质、confirm/export、human review 和 360 继续锁定，未触碰用户持久数据。
> 新的同 cohort rig-fit receipt `docs/evidence/mcp010f/real-codex-cli-rig-fit-20260813.json` 已证明 `silhouette_fit_prepare` 会对候选绑定 V2 GeometryProgram 做真实 Worker 编译/严格回读/固定相机渲染的受限几何变体搜索（8 次总评估），并返回 `geometry_evaluations` 与 `parameter_deltas`。本次选定 fit IoU `0.700586`、Boundary F1 `0.278263`，最终 candidate comparison IoU `0.698465`、Boundary F1 `0.281074`，仍 `QUALITY_TARGET_NOT_MET`；它是只读诊断证据，与 attempt35 provisional observation 均不具 benchmark eligibility，也不解锁材质、confirm、export、人评或 360。

> Rig 物化随后改为 DAG-aware：12 个头/胸/肩甲/上臂/前臂/骨盆/大腿/小腿宽高参数会沿 `part_outputs[].input_node_ids` 追踪到真正的源几何节点，镜像/数组/part-output 不再被误当作可编辑源，变换与 profile/panel/vent/joint 参数由固定 Worker 重新编译和回读。Runtime 回归和 source build 已通过；`real-codex-cli-rig-fit-expanded-20260813.json` 的真实 Codex transport 已走完几何试算、compare、九 AOV 和 quality，但 review turn 额外尝试 human-review 工具而被保守记为 `BLOCKED`。这不是视觉质量通过，也不覆盖 attempt35 provisional observation。
> 随后的同 cohort review-recovery 回归 `docs/evidence/mcp010f/real-codex-cli-rig-fit-review-recovery-20260813.json` 已修复该 transport 误阻断：reference/target/camera、12 参数 Rig 试算、compare、九 AOV、`visual_review_submit` 和 `quality_get` 均完成；失败的 human-review 尝试仍保留为拒绝证据，未被当作用户批准。结果 IoU `0.698465`、boundary F1 `0.281074`、bbox edge error `0.037109`、centroid error `0.049908`、landmark coverage `0.666667`、region median `0.771619`，与 attempt35 provisional observation 对照更低，故仍为 `PASS_WITH_QUALITY_TARGET_NOT_MET`，不确认、不导出、不解锁 PBR 或 360。
> 随后的 attempt36 单变量 `chest-wedge-mild` 实验没有产生视觉质量结果：read-only 沙箱在 `project_create` 即失败，workspace-write 重试在大体量 detail authoring turn 超过 bounded 等待后被中止；MCP/Runtime/Worker 均无崩溃，未写用户持久数据，receipt `docs/evidence/mcp010f/real-codex-cli-silhouette-first-20260813-attempt36-chest-wedge-local-fit.json` 明确为 `BLOCKED`，不改变 attempt35 provisional observation。
> 新增的轻量 source probe `scripts/probe_mcp010f_part_correction.py` 绕过大体量 Codex detail authoring：使用实际 `chest-shell` Viewer 草图建立局部 target，调用 `part_contour_fit_prepare`，再用四个 bounded 单变量候选执行 `geometry_prepare → reference_compare_prepare → silhouette_candidate_compare`。它将当前 surface-linework baseline 的 IoU 从 `0.741047` 提升到最高 `0.745895`（Runtime 加权 loss winner 为 IoU `0.745135`、Boundary F1 `0.340045`），但仍远低于 `VISIBLE_SILHOUETTE_PASS`；证据 `docs/evidence/mcp010f/part-correction-source-20260813.json` 只证明 staged transport/best-so-far，不是 likeness、confirm 或 export。

> Runtime 源码和 focused tests 已将固定 Part-ID anchor 接入 camera fit、Rig/geometry trials、candidate comparison 与 persisted reference comparison 的瞬时目标；但最新完成 receipt `docs/evidence/mcp010f/real-codex-cli-semantic-landmark-compare-20260813.json` 本身只验证 persisted reference-comparison 的语义指标 checkpoint，没有运行 camera fit、Rig/geometry trials 或 candidate comparison。该 checkpoint 结果仍为 `QUALITY_TARGET_NOT_MET`（IoU `0.741047`、Boundary F1 `0.328765`、semantic landmark coverage `0.139498`、NME `0.087015`）。landmark coverage/NME 已切换为更严格的语义 Part-ID 指标，不能与 attempt35 的旧语义直接排序；该 receipt 未晋升为模型或 benchmark。最新尝试 `docs/evidence/mcp010f/real-codex-cli-semantic-aligned-fast-20260813.json` 在声明的三次有界 operator discovery 后 `BLOCKED`，但 compact receipt 缺少 raw transcript/exit codes/turn count/`recorded_at`，其 host、尝试次数和 cohort provenance 均未验证，也没有 quality metrics 或视觉晋升。完整顺序见 `docs/evidence/mcp010f/real-codex-run-inventory.json`。

> Runtime 现已把 `silhouette_fit_prepare` 的 Rig proposal 绑定到选定相机的 Part-ID 局部 envelope：有 typed target Part slice 时，匹配 `part_id` 的 width/height/scale/offset 使用局部包络和质心，未标注参数保持全身 fallback；无 Part slice 不额外渲染。合成局部优先回归、完整 Runtime（67 passed，10 isolated ignored）和新的 source probe 均通过。它只改善参数归因，不能把 `0.745895` 写成视觉门通过；没有 confirm/export，用户持久数据未改变。

> `silhouette_part_error_get` 进一步把同一 Part-ID 证据展开为多 Part 误差表；它是只读的 hash-bound 诊断工具，支持 Luna 选择最大局部边界误差的 Part，不会创建候选、确认版本或放宽 `QUALITY_TARGET_NOT_MET`。

> 当前 Agentic observation 已把上述 Part-error 结果直接嵌入 `primary_form_directive.part_error`，同时返回 Runtime 的 `focus_part_id`/visibility 和 RepairIntent 的单一 Part scope。Codex 不需要先读取聚合观察、再单独调用 Part-error 诊断才能知道下一处边界焦点；没有 typed Part slices 时仍返回 `null/unknown`，不制造归因。

> 同 cohort 的 D/E 细节路径也已实际复测：`real-detail-assetpack-current-20260812-baa39a.json` 通过 26 Parts/4704 triangles、7 个 MaterialZone、9 AOV、ArtifactReadback@2 和离线 AssetPack 传输，指标为 silhouette IoU `0.7410`、boundary F1 `0.3288`、bbox edge error `0.0078`、landmark coverage `0.7333`、region median IoU `0.8694`。它相较 primitive-only Codex 结果明显更好，但仍 `QUALITY_TARGET_NOT_MET`，embedded texture URI count 为 0，不能确认或导出为高质量模型。

> 真实 Codex 已进一步通过 `scripts/probe_mcp010c_codex_cli.py --geometry-route detail --geometry-variant surface-linework --material-variant armor-shell-zones` 走完整 detail authoring/compare/review 路线，证据为 `real-codex-cli-current-20260812-packaged-detail-intake.json`。它在 live catalog 后实际提交 profile/panel/vent/revolve/joint/mirror/transform/tube/array 组合，得到 26 Parts/4704 triangles 和与 raw D/E 相同的比较指标；结果仍为 `QUALITY_TARGET_NOT_MET`，因此 Codex Operator 编排已通过，视觉 likeness、材质保真和确认导出仍未通过。
> 当前又新增了 `scripts/build_mcp010f_surface_lineflow_plan.py`：它绑定同一参考 inventory、实时 OperatorCatalog 和离线 AssetPack，按失败门生成最多五个带 `line_flow`、活动 Operator 和 MaterialZone 的单 Part 意图。用户图当前只解锁 `silhouette-blockout`，五个动作均不含几何参数；下肢/脚部因参考裁切 deferred，材质/UV/PBR 继续锁定。证据为 `docs/evidence/mcp010f/surface-lineflow-plan-20260812.json`，仅属 Codex 编排，不改变质量状态。
> Geometry 写入前的 Codex 编排现在还有一个确定性 inventory gate：`scripts/validate_mcp010f_reference_inventory.py` 对当前用户参考记录验证 10 个 detail、8 observed、1 inferred、1 unknown、7 个 active Operator 和 AssetPack 材质 ID；故意引用 `boolean@1` 会 fail closed。该 receipt 只验证编排元数据，不替代 Runtime hash/readback 或视觉质量。

> Packaged C/E/F update：当前 Dev.app 安装/包验证/隔离探针、九 AOV raw renderer、packaged Codex CLI compare/review transport、同 cohort E 的用户参考→D detail→AssetPack→compare 结构传输，以及 F 的 packaged Viewer CLI read-only projection 与原生窗口结构探针均已通过，receipt 位于 `docs/evidence/mcp010c/`、`docs/evidence/mcp010e/` 与 `docs/evidence/mcp010f/`；CLI projection/窗口探针不等于 packaged Viewer 控件交互、无障碍、人评或 likeness PASS。机器人质量状态仍为 `QUALITY_TARGET_NOT_MET`，Viewer UI、PBR likeness、export/restart hash 和 HQ_360 继续 `NOT_RUN/BLOCKED`。
> 最新 E PBR renderer Dev.app cohort `77d4bff5…f2a73` 已通过 ad-hoc/package/isolated user-reference probe；固定 beauty 已采样嵌入 normal/metallic-roughness/AO/emissive，但 Desktop restart、视觉 PBR likeness、人评和 export/restart hash 仍未运行，用户机器人 compare 仍为 `QUALITY_TARGET_NOT_MET`。

## 1. 本次范围决策

产品按个人单用户 MVP 收口，不再让生产签名、后台服务治理或第三方插件市场阻塞真实 3D：

- 运行时只有 `forgecad-mcp`、`forgecad-runtime`、按 Job 启动的 typed workers 和可选 read-only Viewer；
- Runtime 用 OS 文件锁单写者，无 TTL/heartbeat；MCP initialize 不等 Runtime，Runtime 异常最多一次简单重启；
- MCP004 作为 candidate/approval/version/restore 的事务基座关闭；
- MVP 主线 MCP005 reference → MCP006 typed contracts/Skills → MCP007 geometry → MCP008 PBR/render/Viewer → MCP009 quality/change/version/export 已完成 focused Gate；真实 Codex CLI 的授权图片到 CAS GLB 十二调用 host golden path 也已 PASS；当前只剩真人/像素相似度、Desktop write 和正式分发 Gate，不再扩展基础设施；
- 当前工具、Skill 和 GitHub 候选的可执行边界统一见 `docs/MVP_TOOL_CATALOG.md`；没有 `accepted` 第三方依赖，不得因为 GitHub 链接而修改 lockfile；
- MCP010A–F 负责首个硬表面参考质量产品化；复杂 Job recovery/GC 属 MCP011，通用第三方 Skill/AssetPack 生命周期属 MCP012，signing/notarization、packaged Desktop、升级回滚和跨类别真人门属 MCP013。

范围调整没有把未完成能力改成 PASS；`docs/evidence/mcp004/manifest.json` 保留历史 `in_progress`、BLOCKED 和 NOT_RUN 现场。

## 2. 当前已实现

- MCP001：旧 Provider/Agent/App Server/工作台硬切，新 Rust/Tauri/Worker/contracts 骨架；旧用户 Library 未修改；
- MCP002：Project/Candidate/Version/Snapshot/Job/Audit/CAS contracts、SQLite V1、atomic CAS、backup/restore、OS writer lock、authenticated Unix IPC；
- MCP003：MCP 2025-era stdio/resources/read-only tools，Codex `2025-06-18` compatibility，Desktop/CLI required read-only evidence，CLI modern mismatch fail closed；
- MCP004：candidate prepare、Job/audit、quality/approval/idempotent confirm/reject、restore-as-new-version、path-free diagnostic export、authenticated IPC、显式 opt-in MCP writes；
- `forgecad-mcp` 拥有 stdio并异步启动/连接同一数据根的共享 Runtime；短时 launcher flock 只做启动选主，Runtime 持有的 `runtime.writer.lock` 才是最终唯一写者。正常适配器退出不终止已经 Ready 的 Runtime，显式 shutdown/update 才停止；缺失/失败/ready 后 crash 时 stdio 存活，依赖调用返回 `RUNTIME_UNAVAILABLE`，最多一次 restart。最终源码的 MCP 26/26 与 shared lifecycle Gate 已 PASS；同 cohort Dev.app 已重建、隔离验证，并通过第二次 Desktop live Gate；
- 真实 Codex CLI 完成 diagnostic project/create/confirm/restore/export；Viewer 通过 authenticated IPC 读回同一项目/版本；
- MCP005 已完成 PNG/JPEG admission、ReferenceEvidence/CAS readback、authorized-root/symlink/path/hash/MIME negative tests 和真实 Codex CLI image-attachment E2E；证据位于 `docs/evidence/mcp005/`；原图路径/字节未进入仓库或 receipt；
- MCP006 已完成历史范围：44 个合同、十项 first-party registry、十个独立 `bundles/<skill_id>/0.1.0`、`skill_list/get`、Skill resource、trust hash、安全 allowlist、Recipe canonical hash、DAG/单位/finite/预算 validator、合成正/负 fixture、每 Bundle benchmark receipt、LICENSE/NOTICE、SPDX SBOM 和 provenance 已落地；MCP010B 当前源码另有 `primitive-blockout@0.2.0`，MCP010D 又新增经过完整 integrity/benchmark/provenance 验证的 `hard-surface-detail@0.2.0`，其 11 个 D Operator 均由固定 Worker 实际消费后才标为 active；`scripts/check_mcp006_skills.py`、`script/test_mcp010d.sh` 与 Runtime/MCP focused tests 已通过；正式 distribution signature 仍延后到 MCP012–013；
- 真实 Codex CLI 已完成只读 `capabilities_get → skill_list → skill_get`，证据见 `docs/evidence/mcp006/codex-cli-skill-registry-e2e.json`；它只证明 registry metadata 传输，不证明几何/渲染/质量；
- MCP007 已完成：product-owned bounded GeometryProgram compiler、box/cylinder/sphere、14 个语义机器人 Part fixture、finite/index/budget/lineage 检查、确定性 GLB、`ArtifactReadback@1`、authenticated MCP geometry/readback、Viewer candidate/artifact read model。`npm run mcp007:test` PASS；真实 Codex CLI 已用用户授权 PNG 完成 `project_create → reference_import → geometry_prepare → artifact_readback_get`，14 parts/516 triangles/validator passed，证据见 `docs/evidence/mcp007/`；它只证明 typed geometry host slice，不单独证明外观或视觉相似度；
- MCP008 已完成：`AppearanceProgram@1` hash-bound material zones、UV/tangent、glTF PBR、四个兼容 PNG pass、Runtime readback、Three.js GLB canvas 和 `npm run mcp008:test`；MCP010C 在不改写该历史 RenderSet@1 的前提下新增 RenderSet@2 九 AOV/reference comparison/review source path；MCP010E 已补齐受限 `AppearanceProgram@2`、离线 AssetPack、嵌入纹理与固定 PBR beauty sampling；纹理烘焙、UDIM、第三方 Validator 和真实 PBR likeness 仍未完成；
- MCP009 已完成 MVP host golden path：`quality_get`（limited aspect compare）、`version_diff`、`change_prepare`、immutable confirm/reject/restore、`mvp-glb` CAS export receipt；`npm run mcp009:test` 的 24 Runtime tests + 16 MCP tests PASS；真实 Codex CLI 已完成十二调用 reference→geometry→appearance→quality→confirm→version→CAS GLB export，证据见 `docs/evidence/mcp009/`；
- `npm run release:mcp004`、44/50/52-contract、3c/f488/bfa56/d9 Dev.app 以及其 Codex structural receipts均按历史范围保留，不能改写成 current C/D/E source。当前源码 191 contracts、21/21 active operators、54 read + 36 opt-in write = 90 tools；`script/test_mcp010c.sh`、D/E 各 Gate 与 `script/test_mcp010f.sh` F Viewer/轮廓目标/`CameraCalibrationRef@1`/Runtime-owned Primary Form/Rig/SDF/Part/compare/boundary/Mechanical pose/Authoring Mesh source Gate均 PASS。Mechanical pose、Authoring Mesh 与 Agentic projection 不能写成 Armature/skin、orchestrator、Repair 或视觉 PASS；C/E/F synthetic/CLI receipt 与首次真实机器人 receipt 均不构成 likeness PASS。attempt35 指标只作 provisional retained observation，benchmark eligibility 为 `BLOCKED_INCOMPLETE_BINDING`；最新真实 RepairIntentRun 在同 cohort 完成 render/compare/evaluate 后仍按 `QUALITY_TARGET_NOT_MET` blocked；Viewer/UI/accessibility、人评、PBR likeness、export/restart hash 和 360仍保持 NOT_RUN/BLOCKED。
- MCP010A 可恢复旧代码清理已 PASS：恢复包 `20260809-mcp010a-legacy-cleanup` 的 Git bundle、tracked/local archive、worktree patch 和 SHA-256 清单均已验证；旧 Provider/Planner/CSG evaluation、packaged Agent sidecar、Gate07 配置、5 个孤儿 Python Agent、旧虚拟环境/Host/Proxy/生产包/日志/缓存/stale endpoint 已退出当前树或进入私有隔离区；无用 Rust API/依赖和旧 Tauri resource protocol 已移除。两份 standalone Host receipt 只保留为 `docs/evidence/archive/` 下的 `SUPERSEDED` 历史；`output/`、`WushenForgeLibrary`、Runtime V1 和 Codex 历史未修改。

## 3. 当前没有实现或不能宣称

- 像素级 silhouette IoU、landmark/region compare、Codex typed visual review 和真人评分仍属于 MCP010C–F 证据层；MCP009 的 `quality_get` 只返回 limited aspect-ratio evidence，不能替代这些视觉或真人门；
- 通用 mesh-delta、只重编受影响 DAG 的优化；`change_prepare` 是稳定 Part + 新 typed program 的有界重编入口；
- 任意本机路径/生产安装包导出；当前 `mvp-glb` 是 CAS-backed hash receipt，文件系统/签名导出留 MCP013；
- 同 hash Viewer/Runtime/restart 的真实 Codex host visual E2E；
- Developer ID/notarization/clean install/upgrade rollback；
- 通用跨类别高质量与独立真人门。

因此当前软件已经可以在开发构建中运行“用户授权图片 → typed geometry/appearance → GLB/render → candidate → confirm → CAS-only MVP GLB export”的真实 Codex MVP 闭环；仍不能把 aspect-ratio limited evidence 写成像素级相似度、真人高质量验收或通用图生 3D。

## 4. 当前证据摘要

| Gate | 状态 | 边界 |
|---|---|---|
| contracts/store/runtime/IPC focused | PASS | MCP002/MCP004 基座 |
| MCP protocol + required Desktop/CLI read-only | PASS | 不含附件或视觉写入 |
| MCP004 lifecycle regression | PASS（最终修复源码） | MCP 26/26 与 shared lifecycle Gate PASS；覆盖 stdio survival、一次 restart、RUNTIME_BUSY、无只读副作用、共享 Runtime idle owner/passive takeover 与 rogue pre-auth client 隔离 |
| Codex CLI diagnostic write | PASS | 非视觉 contract-only candidate |
| Viewer authenticated read model + GLB canvas | PASS focused | 项目/候选/GLB bytes/版本投影；Three.js scene 只读、无 Runtime 写入 |
| `npm run release:mcp004` | PASS | 当前基座 aggregate；不等于 MVP |
| Reference attachment | PASS（CLI）/ NOT_RUN（Desktop bridge） | MCP005 evidence；Desktop 不能证明 attachment bytes |
| Geometry | PASS focused + real Codex CLI | MCP007 worker/Runtime/MCP/Viewer focused PASS；real CLI 14-part/516-triangle receipt PASS；完整 Desktop 3D write 仍未运行 |
| Appearance/Render | PASS focused + real Codex CLI | MCP008 bounded UV/tangent/PBR + four fixed passes；MCP009 receipt含真实 appearance/readback |
| Quality/Change/Version/Export | PASS MVP host golden path | MCP009 limited quality + approval/version + CAS-backed mvp-glb；pixel similarity/human gate NOT_RUN |
| MCP010A authority/dev activation | DONE / DESKTOP ATTEMPT 1 FAIL RETAINED / ATTEMPT 2 PASS | 第一次完整重启只见 17 个只读工具，失败 receipt 保留；修复后第二次完整重启观察到 30 个工具、Runtime `Ready`、`doctor` ready、临时 `project_create`/readback 和相同 build cohort，成功 receipt 已保存 |
| MCP010B V2 structural truth | BLOCKED/DEFERRED（source Gate PASS；Darwin OS memory hard cap NOT_RUN） | V2 geometry/readback、Skill integrity、Worker isolation、restore hardening 和 closed GLB profile已通过；不把受限预算/peak-RSS后验门当成OS预防式硬上限 |
| MCP010C fixed render/compare/review | SOURCE-FOCUSED PASS_WITH_UNRUN_VISUAL_GATES | 当前源码 191 contracts、21/21 active operators、54 read + 36 opt-in write = 90；隔离 Render Worker、512×512 perspective/z-buffer、九 AOV、candidate-bound comparison、MCP image block、Codex/human review、deterministic raw stdio、轮廓 target/`CameraCalibrationRef@1`/Rig/Part/compare source tests 和真实 Codex CLI silhouette-first detail transport仅按结构/传输范围 PASS；Mechanical pose/Authoring Mesh/Agentic projection 的独立 receipt 不升级 C 视觉状态；attempt35 仅为 `provisional retained observation`，`QUALITY_TARGET_NOT_MET` 且 benchmark eligibility `BLOCKED_INCOMPLETE_BINDING`；Tauri packaged Viewer UI/accessibility、独立人评、PBR likeness、export/restart hash、HQ_360 NOT_RUN/BLOCKED |
| MCP010D hard-surface operators | SOURCE-FOCUSED + PACKAGED STRUCTURAL PASS_WITH_DEFERRED_SUBGATES | 当前 19-entry catalog（19 active，含 bounded Manifold union/difference/intersection）、hard-surface-detail@0.2.0 active、strict lineage/readback、fixed sibling Worker isolation、raw stdio/negative gates and same-cohort packaged D raw probe PASS；任意 mesh Boolean、visual/PBR/360 NOT_RUN/BLOCKED |
| MCP010E offline AssetPack/PBR | SOURCE-FOCUSED + PACKAGED STRUCTURAL PASS_WITH_DEFERRED_SUBGATES | 65-contract checker、`forgecad-hard-surface-robot@1.0.0` provenance、`uv-pbr@0.2.0` active、512px bounded atlas、fixed `mikktspace@0.3.0`、embedded PNG PBR/clearcoat/emissive、nine AOV/raw stdio，以及同 cohort packaged 用户参考结构探针 PASS；xatlas/Khronos Validator、real PBR likeness、Viewer/human/export/restart/360 NOT_RUN/BLOCKED |
| MCP010F Viewer source | SOURCE + PACKAGED READ-MODEL STRUCTURAL PASS_WITH_UNRUN_UI_AND_HUMAN_GATES | 只读 Viewer projection、九 AOV、reference/render split/overlay/flicker、Part/MaterialZone 筛选、临时爆炸图/差异热图辅助、轮廓画布、与 Runtime mask-2 同源的 flood-fill reference-contour aid、candidate-bound QualityReport-only visual gate source、TypeScript/Vite/Tauri build、隔离 Vite browser DOM smoke、同 cohort frontmost native-window smoke、IPC write-boundary negative 和同 cohort packaged CLI read-model PASS；正式 Tauri UI、accessibility、真实机器人闭环、人评、export/restart hash、HQ_360 NOT_RUN/BLOCKED |
| signed/notarized packaged Desktop | BLOCKED / NOT_RUN | MCP013；历史 codesign 为 `errSecInternalComponent` |
| IDE/其他 MCP Client/official transport conformance | OPTIONAL_NOT_IN_SCOPE | 不阻塞个人 MVP |

## 5. 最近任务：FGC-MCP010C–F

本轮新增 V1 AppearanceProgram@1 兼容基线：材质区、固定渲染元数据、UV/tangent 和 GLB 回读通过，但 limited aspect proxy 为 0.4662 < 0.55；候选已拒绝，未创建 version/export。它不能被升级解释为 V2 PBR 或视觉质量证据；详见 `docs/evidence/mcp010b/real-reference-v1-appearance-baseline.json`。

用户已明确领取 FGC-MCP010C，随后领取 FGC-MCP010D/E/F。C 已完成 source-focused implementation，并有真实 Codex CLI C transport receipt：固定 renderer、九 AOV、reference mask/metrics、MCP image block 和 Codex/human review；Viewer compare source implementation 已通过本地 IPC/构建测试。D 已完成 source-focused implementation：Operator Catalog、GeometryProgram@2 typed DAG、11 个真实高细节 Operator、hard-surface-detail@0.2.0、固定同级 Worker isolation、strict readback/lineage 和负向 Gate 均通过，并有同 cohort packaged D raw structural receipt；E 已完成 source-focused implementation：离线 CC0-derived AssetPack、512px UV atlas、fixed `mikktspace@0.3.0`、embedded PNG PBR、nine AOV/raw Gate 均通过，并有当前 packaged E 结构性用户参考传输 PASS；F 已完成 source-focused Viewer slice：九 AOV、reference/render split/overlay/flicker、Part/MaterialZone 筛选、临时爆炸图和差异热图辅助，并通过 TypeScript/Vite/Tauri 与只读 IPC 边界 Gate，同 cohort packaged CLI read-model 也已通过。Manifold/xatlas/Validator、Tauri packaged Viewer UI、live C/F、视觉/PBR likeness、真人评审和 360 仍未运行。不得把 C/D/E/F structural/raw 结果升级成视觉质量或 PBR likeness 结论：

1. V1 `GeometryProgram@1` / `ArtifactReadback@1` 是 MCP007–009 的过渡兼容路径：历史 candidate/version 不迁移、不改写；在 `AppearanceProgram@2` 到位前，现有 MVP appearance golden path 仍可显式使用 V1，但不得把它写成 MCP010B 的 V2 高质量结果；
2. V2 必须有封闭 operator 参数 Schema、真实 DAG inputs、米/弧度单位、显式 Part outputs、operator catalog hash 和完整预算；`GeometryProgram@2.project_id` 必须与外层 `geometry_prepare.project_id` 完全相同，坐标范围为 ±10 m、dimension/height 不超过 10 m、radius/radii 不超过 5 m；
3. Runtime 必须读取 GLB JSON/BIN/accessor，而非相信 compiler extras；invalid index、non-finite、退化面、boundary/non-manifold、winding、Part/Material/source coverage、UV 和 tangent 失败必须 fail closed；
4. sphere 极点、cylinder 端盖与椭球 normal 的现有问题必须修复；不得继续写硬编码 `passed`；
5. 当前 registry/Bundle 不得因文档或 planned operator 宣称 active；不存在的 operator 必须保持 `partial/unavailable + missing_operator_ids`；
6. 只可新增受控 Schema、Rust worker/Runtime/MCP tests、first-party V2 catalog/evidence 和调用指引；Manifold/xatlas/Validator adoption、远程服务和可执行插件都不属于已完成的 D/E source Gate；E 仅采用固定 `mikktspace@0.3.0` 与逐资产 CC0 provenance，不建设通用安装器；
7. 退出前完成 schema/negative → worker → Runtime/MCP → focused → aggregate 的适用 Gate，并将 PASS、FAIL、BLOCKED、NOT_RUN 分开写入 `docs/evidence/mcp010b/manifest.json`。V2 动态工具数量、真实参考视觉相似度和 360°一律不提前宣称。

当前 worktree 的 V2 路径已通过结构验证：`operator_catalog_get`、`forgecad://operators/catalog` 与 capability digest 一致；Codex 只能把 `operator_catalog_get` 返回的 digest 填入无 hash draft，再调用 `geometry_program_hash` 取得 compiler-owned `canonical_sha256`，最后调用 `geometry_prepare`。hash 工具不编译、不创建 candidate/Job，也不写 Store/CAS；它是 resource 的可调用镜像而非第二套 catalog 真值。catalog 节点仍是 closed leaf `primitive@2`，但真实图边是 `part_outputs[].input_node_ids` 的有序 semantic-Part sink：每个 source 必须恰好消费一次并保留逐 source readback binding。V2 target-project binding、10 m/5 m physical envelope、`ArtifactReadback@2` integrity、candidate-bound reread、confirm-time revalidation 和 Runtime-derived Skill availability 都有 source-built Gate。Codex/Luna 仍必须按 [Codex Geometry V2 工作流](CODEX_GEOMETRY_V2_WORKFLOW.md) 只以 JSON/BIN/accessor readback 判定结构硬门；未通过或未运行时不 confirm。

本次已运行 MCP010B structural Gate 作为 C 前置事实；C 新增的 `script/test_mcp010c.sh` 已通过其 59-contract source subtotal、Worker renderer、Runtime candidate-bound review unit、MCP existing suite、raw stdio compare/review/image-block/determinism Gate；E 的 `script/test_mcp010e.sh` 已通过 65-contract checker、离线 AssetPack/provenance、固定 Worker MikkTSpace/UV/PBR、九 AOV 和 MCP image block，并通过同 cohort packaged E 用户参考结构探针；F 的 `script/test_mcp010f.sh` 已通过 Viewer source checker、TypeScript/Vite、Tauri workspace compile、只读 IPC/write-boundary negative check，并补充同 cohort packaged Viewer CLI read-model projection。Viewer source 现在还按 `reference-canvas → silhouette-blockout → landmark-structure → semantic-part-fill → surface-detail → uv-pbr` 顺序显示只读阶段门提示，并把第一处失败门转换为带失败指标、单 Part/单 MaterialZone 约束的 Codex correction queue；它只从 candidate-bound metrics 推导提示，不解锁写入或替代 Runtime QualityReport。真实 Codex CLI C 还在同 cohort 隔离 Runtime 中完成六 turn/32 MCP 调用，证据见 `docs/evidence/mcp010c/real-codex-cli-current-20260812-attempt2.json`；真实机器人结果为 `QUALITY_TARGET_NOT_MET`，未确认 candidate、未写入用户持久数据。Tauri packaged Viewer UI/C/D/F、xatlas/Validator、独立人评、真实 PBR likeness、export/restart hash 和 HQ_360保持 `NOT_RUN/BLOCKED`。

F 的本地 `build_mcp010f_fit_plan.py` 已对用户机器人 PNG 的真实 candidate-bound comparison 运行。旧的三轮 receipt 保留为 region-mask 修复前历史；当前 `region-mask-iou-v2` receipt 按 `reference-canvas → silhouette-blockout → landmark-structure → semantic-part-fill → surface-detail → uv-pbr → final` 生成门控修正队列：轮廓门未通过时只输出 silhouette 单部件动作，region 子门不会绕过全局轮廓门，surface/material 保持锁定。它只验证已有 hash、输出单部件意图和活动 Operator 提示，不写 Runtime/CAS、不改变 QualityReport。脱敏 receipt 为 `docs/evidence/mcp010f/fit-plan-real-reference-region-fix.json`。

本轮把当前可见视图解锁门进一步收紧并统一：Runtime、Viewer 和新生成的 Codex fit-plan 使用 silhouette IoU/Boundary F1 `>=0.90`、bbox/centroid `<=0.02`、landmark coverage `>=0.80`、NME `<=0.03`、region/critical IoU `>=0.85`。此前保存的 0.72/0.75 exploratory receipt 不改写，只作为历史失败或诊断证据；任何新 candidate 必须通过严格门后才可进入结构细化、材质或确认流程。

### 当前 MCP010B 的 V2 authoring 已补齐，但任务未完成

`geometry_program_hash` 现在是默认只读 MCP 工具：输入 `GeometryProgramHashRequest@1`（严格、无 `canonical_sha256` 的 `GeometryProgram@2` draft），输出 `GeometryProgramHashResult@1`（Runtime/Worker-owned canonical hash、catalog hash、schema 和 `validation_status=passed`）。hash 工具对未知字段、V1、预填 hash 和 catalog mismatch fail closed；把返回 hash 填回 V2 program 后，`geometry_prepare` 再对 outer target project mismatch fail closed，且拒绝发生在编译/持久化之前。`operator_catalog_get` 是同一 Runtime-owned `OperatorCatalog@1` 的可调用读取面，必须与 resource/capability/artifact/readback digest 相等。两项能力和固定同级 Worker process isolation 已通过 raw/真实 Codex V2 structural Gate；它们不证明 Darwin 512 MiB OS 总内存硬门，也不打开后续 C–F 能力。

MCP010A 的文档/安装 Gate 仍是历史 PASS；MCP010B 的当前 `d9c23b…ac0bd` Dev.app evidence（install/ad-hoc package verify/isolated Ready/project/V2 raw/real Codex CLI structural probe/live Desktop structural activation）已写入其 manifest，不能借用 010A 或 MCP007–009 的历史 PASS。live activation 的 mismatch attempt 与成功 receipt 均保留；成功只覆盖结构工具链，不替代视觉、PBR 或后续 C–F 门。

详细的 A–F owned paths、目标合同、质量阈值和 011–013 分界见 `docs/MCP010_HIGH_QUALITY_HARD_SURFACE_PLAN.md`。当前单张参考最多支持 `PARTIAL_VISIBLE_VIEW_PASS`；补齐 front/back/left/right/rear-three-quarter 全身参考前，`HQ_360_PASS=BLOCKED_REFERENCE_COVERAGE`。

MCP007–009 仍禁止顺带接入 BlenderMCP、Python CAD、远程 image-to-3D、资产下载或生产签名；外部几何库只能按 adoption receipt 进入隔离 benchmark。

## 6. 工作树保护

当前仓库已有大量未提交修改和未跟踪 MCP004 文件，均视为用户工作。不得 `git reset --hard`、`git clean`、checkout 覆盖或删除 `WushenForgeLibrary`；除非用户明确要求，不 commit/push/merge。

每轮基线：

```bash
git status -sb
git diff --check
npm run release:docs-walkthrough
npm run repository:integrity
npm run release:safety-scope
npm run release:secrets-files
npm run release:license-sbom
```

完成定义、外部项目流程和 Goal 状态句分别见 `CODEX_DEFINITION_OF_DONE.md`、`EXTERNAL_PROJECT_ADOPTION.md` 和 `LUNA_GOAL_EXECUTION_GUIDE.md`。

## 6. 2026-08-13 F 子任务交接

本轮完成 `FGC-MCP010F` 的 Viewer candidate binding 原子修复。`RuntimeViewer.tsx` 现在只使用同一 `project_id + candidate_id` 的 artifact、reference、RenderSet 和 `ViewerVisualEvidence`；没有完整绑定时保持未运行，禁止跨候选 artifact 回退。receipt：`docs/evidence/mcp010f/viewer-candidate-binding-source-20260813.json`。

已通过 Viewer source checker、same/cross/missing binding fixtures、TypeScript、Vite build、Tauri workspace check、Viewer unavailable unit、repository integrity、safety scope、secrets、license/SBOM、Stage 0 truth 和 docs walkthrough。authenticated IPC geometry fixture 因测试环境缺少 geometry Worker 为 `BLOCKED_GEOMETRY_WORKER_UNAVAILABLE`。本轮没有 Runtime/MCP 写入、Schema/Skill registry 变更，也没有改变 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、packaged Viewer、人评、PBR、export/restart 或 360 状态。

并行 Luna 复核随后发现并已修正一个合同对齐问题：`QualityReport@2` 没有 `project_id` 字段，Viewer 不再读取不存在的 `quality_report.project_id`；项目绑定改由 Runtime 的 `ViewerVisualEvidence@1` 外层 envelope、read model candidate/reference 投影，以及同 candidate 的 RenderSet/ComparisonReport/artifact/reference hash 共同校验。新增正向 fixture（无 QualityReport project_id）和跨候选 RenderSet、缺 reference hash 负向 fixture均通过，receipt 与 Stage 0 truth 已更新。该修正仍是 Viewer 只读 source-focused 变更，不代表 packaged、人评、PBR、export/restart、360 或视觉质量通过。

## 7. 2026-08-13 F 单部件修正与前置顺序

按本轮交接要求，`scripts/probe_mcp010f_part_correction.py` 已在任何设计工具调用前读取 `ponytail-preflight@0.1.0`，并校验 Skill manifest 与 knowledge canonical hash。首次真实探针保留了 Runtime 返回的 `PONYTAIL_PREFLIGHT_REQUIRED`；修正后的探针顺序、静态顺序回归和 `script/test_mcp010f.sh` 均通过。这个前置步骤只证明编排顺序，不解锁几何、视觉质量或确认。

新的隔离 Runtime 探针使用用户授权参考字节、Viewer 的 `chest-shell` contour、`silhouette_part_error_get` 的推荐 Part 和有界单变量候选，完成 `part_contour_fit_prepare → geometry_prepare → reference_compare_prepare → silhouette_candidate_compare`。receipt 为 `docs/evidence/mcp010f/part-correction-source-20260813-followup.json`，SHA-256 为 `1d6c38eaf41dbdd749064244e6503e9e1b35bf1f898a68b46361984245490d15`。五个候选全部保持 `QUALITY_TARGET_NOT_MET`；最高原始 IoU 为 `0.745895`、Boundary F1 为 `0.330265`，Runtime loss winner 为 IoU `0.745135`、Boundary F1 `0.340045`，均低于当前 `0.90` 轮廓门。该 receipt 的状态是 `PASS_TRANSPORT_WITH_METRICS`，`persistent_user_data_touched=false`，没有 `candidate_confirm`、version 或 export。

随后扩展 `scripts/probe_mcp010f_part_correction.py`，保留胸甲路由并新增 `shoulder-armor-left/right` 的 typed 节点映射和 `shoulder-*` 参数命名。使用 `shoulder-contour-mild` 与用户授权参考的右肩局部 contour，Runtime 成功返回 `shoulder-armor-right` Part-error、宽高/偏移 proposal，并完成五个候选比较。receipt 为 `docs/evidence/mcp010f/part-correction-source-20260813-shoulder-right.json`，SHA-256 为 `c6c4b59ae77c32c4c8c559206d55860aba375ff8d6425adcfd405182bc802d0c`；最佳全局 IoU `0.744471`、Boundary F1 `0.327606`，低于胸甲 follow-up，也未达到严格门。它证明了多 Part 归因和真正的单肩修正 transport，不替换当前 baseline，不确认、不导出。

随后沿同一顺序选择 `shoulder-armor-left`，使用图像派生的左肩 contour 和 `shoulder-contour-mild` 完成局部 Part-error、proposal 与五候选比较。receipt 为 `docs/evidence/mcp010f/part-correction-source-20260813-shoulder-left.json`，SHA-256 为 `e133be0ec79d80172a5feb54605f604a7bdeb44d3586cea4b1d823dd225b0be6`；最佳全局 IoU `0.742468`、Boundary F1 `0.327530`，没有改善肩甲基线，全部保持 `QUALITY_TARGET_NOT_MET`。左肩 draft 仍为 transient annotation；没有 confirm、version、export 或持久用户数据写入。

因此 F 仍为唯一 `in_progress`。下一轮继续沿“图像派生 intake → 读取 Part 误差表 → 一次只改一个轮廓 Part → 同相机/同 reference 重新比较”的路径；不得把局部 IoU 改善、Skill preflight、typed review 或 packaged read-model 解释为 likeness PASS。camera `MISMATCH`、`INCOMPLETE_TRUTH_BINDING`、packaged Viewer binding、人评、PBR、export/restart 和 360 状态保持原记录。

2026-08-14 Agentic observation binding regression：`agentic_scene_observe` now fails closed when a project has multiple candidates but neither an explicit candidate ID nor an active snapshot. A single candidate keeps the bounded compatibility fallback; no candidates remain unknown. The focused Runtime regression passes 2/2, with no persistent user data touched. This repairs canonical observation lineage only; it does not execute Repair, promote a candidate, alter Viewer quality authority, or change the current visual benchmark status. Receipt: `docs/evidence/mcp010f/agentic-observation-binding-20260814.json`.
