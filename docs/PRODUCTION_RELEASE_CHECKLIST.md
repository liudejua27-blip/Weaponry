# ForgeCAD 生产发布清单

版本：2026-07-13
当前结论：阻断，不可外部分发

只有全部必需项通过，并且证据对应同一个发布 commit，才能把 ForgeCAD 标记为生产候选。

## 1. 产品与状态

- [ ] `ActiveDesignSnapshot` 已实现并完成 Agent 回退/前进、完整并发与所有兼容 UI 的唯一工作台真值验收；
- [ ] 未知领域进入澄清，不默认武器包；
- [ ] 选择、版本、质量、撤销和导出引用同一 Agent 资产；
- [ ] legacy 项目只读或显式转换；
- [ ] 用户指南与当前功能一致。

当前：未通过。

## 2. 核心功能

- [ ] 四领域明确 Brief 均能生成完整 blockout；
- [ ] 方向预览不产生正式版本；
- [ ] 分件确认创建 v1；
- [ ] 部件修改、材质和组件替换经过预览/确认；
- [ ] 重启恢复正确版本和选择；
- [ ] 质量与导出绑定活动版本；
- [ ] Agent GLB 在独立查看器回读成功。

当前：后端纵向 slice、Snapshot 核心链路、不可变 Agent 回退/前进与浏览器 E2E 已通过；完整并发和原生安装 E2E 未通过/未完成。

## 3. 测试与质量

- [ ] G1–G7 全部进入 CI；
- [ ] 工作台 E2E 稳定通过；
- [x] Python 单元测试（13 个基础合同测试）；覆盖率阈值仍未设置；
- [x] Rust 单元测试（Provider 配置与 health URL，4 个测试）；集成测试仍未完成；
- [x] npm、Python、Rust 漏洞审计在 CI 产出 artifact 且高危阻断；
- [ ] 30 分钟内存和单 WebGL renderer 测试；
- [ ] 零基础用户可用性测试；
- [ ] 备份/恢复演练。

当前：部分通过。常规基础单元测试已存在且纳入 CI；npm/Python/Rust 审计本机均无高危漏洞并已配置 CI artifact。生产候选仍被 packaged sidecar、安装 E2E、真实 Provider、性能和签名发布阻断。

## 4. Provider

- [ ] Provider 配置只存 Keychain/安全 secret file；
- [ ] 认证、限流、超时、坏输出和断网恢复；
- [ ] 四领域真实 Provider truth set；
- [ ] token、费用和失败原因报告；
- [ ] Prompt/模型版本可追溯；
- [ ] 密钥不进入日志、数据库或导出。

当前：安全配置路径存在，真实质量评测未完成。

## 5. 数据与恢复

- [ ] 所有迁移在旧库副本上验证；
- [ ] SQLite integrity/foreign key 检查；
- [ ] 对象引用和 hash 校验；
- [ ] 备份对象枚举覆盖 `agent_imported_glbs.object_path`；
- [ ] 备份 manifest 可验证；
- [ ] 新目录恢复后可启动并读取活动版本；
- [ ] 升级失败可回滚且不丢用户数据。

当前：已有备份/恢复工具和 smoke，但外部导入 GLB 对象尚未被备份枚举覆盖，需要针对新 Agent Snapshot 扩充。

## 6. 资产与许可证

- [ ] 默认资产来源声明完整；
- [ ] 独立 reviewer 与 creator 不同；
- [ ] 正式 review/attestation 通过；
- [ ] 质量失败、受限或待审资产不进入正式默认候选；
- [ ] SBOM 和第三方许可证报告无阻断；
- [ ] 资产 hash 与发布包一致。

当前：“刘邦”已指派，但正式审阅尚未完成。

## 7. 安装包

- [ ] 每个平台 sidecar 非空且格式正确；
- [ ] supervisor 报告 `mode=packaged-sidecar`；
- [ ] 用户机器不需要 Python、仓库或 `.venv`；
- [ ] macOS/Windows 新用户账户安装测试；
- [ ] 首次初始化、Provider、工作台、导出和重启恢复；
- [ ] 升级和卸载测试；
- [ ] 包大小、启动时间和资源使用符合预算。

当前：`release:packaging-readiness` 因空 sidecar 明确失败。

## 8. 安全与隐私

- [ ] CSP 和本地端口边界通过；
- [ ] 错误服务拒绝；
- [ ] 文件访问不越过允许目录；
- [ ] 外部 GLB 预算、访问器和外部资源拒绝；
- [ ] 日志和崩溃报告脱敏；
- [ ] 密钥轮换说明；
- [ ] 删除/导出用户数据流程。

当前：基础门通过，仍需安装包和崩溃报告验证。

## 9. 签名与分发

- [ ] macOS Developer ID 签名和公证；
- [ ] Windows 代码签名；
- [ ] 发布渠道权限和 CI 密钥最小化；
- [ ] 签名工件 hash 与发布记录一致；
- [ ] 回滚版本可用。

当前：本机测试阶段可以暂缓；外部发布前强制完成。

## 10. 最终批准

- [ ] 产品负责人确认当前能力和限制；
- [ ] 工程负责人确认测试、数据和回滚；
- [ ] 资产 reviewer 确认默认资产；
- [ ] 发布负责人确认安装、签名和工件；
- [ ] 同一 commit 的全部必需 CI 绿色；
- [ ] 工作区干净，发布 tag 指向该 commit。

任何必需项未勾选时，发布结论只能是 `blocked`。
