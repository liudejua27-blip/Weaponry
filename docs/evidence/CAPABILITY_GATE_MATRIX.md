# ForgeCAD 当前能力—Gate 矩阵

版本：2026-08-01
状态：当前能力与阻断的唯一总表

| 能力 | 当前状态 | 当前证据/Gate | 未完成边界 |
| --- | --- | --- | --- |
| FGC-U004-W4 集成验收 | 部分实现 | `7758a01 → 5ec59ed → 0aab030 → 28373ed` 按 W1→W2→W3 顺序合并；Rust bridge、Tauri `cargo check`、repository integrity、等价 docs/safety/secrets/provider-policy/contracts、Agent compile 和 diff check 通过；完整状态见 [W4 manifest](U004_W4_INTEGRATION_EVIDENCE_MANIFEST.md) | 聚合脚本受缺少 worktree `.venv`/`tsc`/`playwright-core` 和 Node 26 undefined-stream smoke 错误阻断；真实 DeepSeek→GLB/readback/capture 为 FAIL，真实千问/未见输入/真人评分为 NOT RUN，packaged app 为 KNOWN FAIL；U004 不得标 done |
| U004 P4.1 local hard-surface Hybrid 图片闭环 | 部分实现 | desktop:u004-universal-image-bridge-e2e 新增 sealed PNG + pack_unclassified Hybrid fixture：同一 UAS@2 资产包含程序化主壳和受限 lattice 饰条，完成同 renderer capture、一次性 Qwen-compatible evaluate、preview→confirm→version→export、确认幂等；persisted source 保持 Hybrid，隐藏形变特征不进入比较，worker/readback 支持多输出 zones | fixture 不联网且不调用真实千问；仍缺 packaged GPU、真实 Qwen 相似度、任意 mesh/角色/生物/布料表示、未见输入和真人质量 |
| U004 P0.2 通用 sealed-image bridge 闭环 | 部分实现 | `desktop:u004-universal-image-bridge-e2e`；Rust bridge 使用真实 CAS PNG、`pack_unclassified`、UAS@2、同 renderer 八视图/五通道 capture、一次性 Qwen-compatible fixture，完成 evaluate→preview→compat confirm→版本化→export；unavailable `mesh_seed.generic_v1` limitation 验证 worker/preview/Snapshot/version/export 均为零 | fixture 不联网且不调用真实千问；真实千问相似度、packaged GPU、照片级质量、未见输入和角色/生物/跨类别质量仍未运行 |
| U004 P0.3 通用图片主路径与 GPU render provenance | 部分实现 | 工作台只提交 Universal `author_context`；Rust 在 SubjectProfile 产生后把面板只读 `VisualEvidenceGraph@1` 按 macro/meso/micro 投影为绑定 request/profile 的 `VisualEvidenceGraph@2`；Universal candidate 不再与 Legacy multimodal source 同时进入比较。`CandidatePbrCaptureSession@1` 进一步封存 code-owned visual environment ID/hash、render manifest、sRGB/ACES contract，Tauri bridge 对每张 beauty/auxiliary capture 回传值逐项重验；完整 candidate PBR Gate、Tauri bridge 三条 focused E2E、真实浏览器五通道 GPU capture、contracts、desktop typecheck 和 diff check 通过 | 旧 `@1` 仍是兼容输入，不是 UAS@2 真值；真实 DeepSeek/千问、packaged GPU、未见输入、照片级和跨类别视觉质量仍未运行 |
| Rust-owned Agent 与产品状态 | 已实现（Alpha） | K001–K003；`agent:k001-app-server-smoke`、`k003:layered-gate` | 正式签名、跨平台安装、广泛并发 |
| `ActiveDesignSnapshot@1` 单一真值 | 已实现（Alpha） | FGC-S001–S008；`agent:s1-active-design-snapshot-smoke`、`desktop:r3-concept-workbench-smoke` | legacy UI 完全退出、多客户端压力 |
| 唯一结果、preview→confirm、不可变版本 | 已实现（Alpha） | V003、F026、PV005；工作台与连续资产 Gate | 普遍视觉质量未证明 |
| 单 renderer `docked | focus` | 已实现（Alpha） | F026 规格；`desktop:f026-codex-workbench-smoke` | 原生长时间资源测试 |
| 受限程序化几何与 CSG | 已实现（Alpha） | G801–G826、Manifold Python、GLB feature history/readback Gate | 不是 B-Rep、自由曲面或任意代码执行 |
| GLB/PBR/Material Zone/readback/export | 已实现（Alpha） | M108A、G826、A005、PV005/PV008 | 自动事实门不等于真人外观质量 |
| 参考证据与多模态合同 | 部分实现 | R007A/B、PV006A/B；exact-lineage 工程 Gate | 当前 Project 真实图片组合、视觉相似度、跨类别正式 run |
| `ForgeVisualProgram@2` / 1+1 | 部分实现 | VP201–VP204；typed `cylinder/capsule/wedge/radial_array/bevel_approx/surface_panel/shell/lattice_deform` lowering；shell Python/Rust/native validator 与 GLB/readback tests；E005-R1/R2/R3 Core Gate | 通用 Assembly/Surface、真实四模态、完整时间和真人质量 |
| 当前机械臂生成路径 | 部分实现 | 真实 DeepSeek 文本→Rust→GLB→preview/confirm/Snapshot/export 工程闭环 | 画面低于目标参考；sealed comparison 和独立真人 4/5 未完成 |
| 类别开放通用入口 | 部分实现 | U002；`agent:u002-universal-author-contract-gate`、`desktop:u002-universal-author-workbench-smoke`、U004 focused author/build/capture/VP204 patch tests、`u004_compiled_universal_source_creates_confirmable_preview_without_legacy_plan`、`desktop:u004-universal-image-bridge-e2e`；新增本机 `local_open_categories_produce_distinct_validated_visual_sources` 覆盖猫、车辆、建筑并验证 UAS@2；Core `u004_generic_visual_exterior_accepts_open_category_without_provider_trait` 与 native animal/quadruped route regression | Rust 现在除了机械臂和 `procedural.generic_hard_surface_v1`，还可用开发期 `procedural.generic_visual_exterior_v1` 为车辆、家具、建筑、植物、动物、角色、机械设备、虚构游戏道具和未知对象生成不同的轻量外观代理，且不要求 Provider 额外自报 `visual_exterior` 标签；仍未证明照片级相似度、真实千问、packaged E2E、未见输入或真人质量，复杂有机对象仍是 proxy/可能受限 |
| 通用外观候选到工作台 PBR 捕获 | 部分实现 | Rust 独立发布 `build_universal_visual_exterior`、`universal_local_lattice` 和 `universal_local_hybrid` pending route；前端 `CandidatePbrCapturePending@1` 已接收这些路由并沿同一 renderer capture/resume bridge 继续；`u004_generic_visual_exterior_builds_uas_v2_then_requires_desktop_pbr_capture` 已实际验证 author→UAS@2→native geometry→GLB/readback→capture pending，`generic_visual_exterior_capture_resume_keeps_visual_repair_route` 验证通用修复 route；app-server 281 tests、F002、U002 workbench smoke、desktop typecheck 和 animal/quadruped eligibility regression 通过 | 仍需解锁后的真实 DeepSeek 端到端 author、真实千问比较、packaged GPU、跨类别高保真和真人 4/5；通用外观当前仍是轻量 proxy，失败时必须返回 typed limitation |

补充证据（2026-08-01）：本机通用代理新增 `local_visual_prompt_changes_bounded_geometry_not_only_program_seed`。同类别不同描述会改变受限部件节点，而不是只改变 seed；仍属于开发期轻量 proxy，不提升为真实 DeepSeek/Qwen 视觉理解或高保真能力。
补充证据（2026-08-01）：本机通用代理新增 `local_visual_prompt_changes_reviewed_materials_and_structural_patterns` 与 `local_visual_parts_keep_non_mechanical_semantic_patterns`。材质词按部件语义作用，专用玻璃/橡胶/发光区不被全局串色；车辆、角色、四足动物、建筑和植物等输入可生成受限 array/radial-array 结构并通过 lowering。该证据只扩大本机确定性候选多样性，不提升为真实 Provider 视觉相似度、照片级质量或正式跨类别真人门。
| `UniversalAssetSource` 与通用外观绑定 | 已实现（当前程序化切片） | U003；`agent:u003-universal-asset-source-gate`；Rust author→build→preview/版本 exact-lineage；U004 P2.3 `ProjectionCameraBinding@1` 将 candidate GLB/readback bounds/turntable view 绑定到同一 renderer capture session；P2.4 `CoreRepository` 仅从同 Project sealed PNG CAS relation 构造并重验 `ReferenceCameraUvRasterBake@2`；P2.6 已补同一 retained Material Zone 最多两份 V2 camera raster 的受限融合；UAS@2 non-game preview 有独立 universal provenance，确认时将 exact source/hash 写入同一 AssemblyGraph；游戏 delivery 已有双工件原子持久化、receipt-verified export/package、有效 GLB desktop E2E 及 native compatibility ChangeSet `:preview → :preview.glb → :confirm` 回归 | 没有通用多图拼接、用户图片投影入口、完整 ActionLoop pixel-bake wiring 或照片 PBR 恢复；P2.6 仅为同区双视图 bounded capability；游戏 delivery 仍缺 packaged 设备、真实千问和用户质量证据 |
| U004 P2.5 二阶段参考外观烘焙 | 部分实现 | `ReferenceAppearanceBinding@1`、generic sealed-image app-server integration、Core U003/U004 source/raster tests | geometry-first、单次 1024² PBR bake 与 geometry-drift Gate 已实现；仍缺多视图融合、真实 Provider、packaged E2E 与跨类别质量 |
| U004 P2.6 同区受限多视图参考融合 | 部分实现 | `ReferenceCameraUvRasterFusionReceipt@3`、同区最多两份 V2 camera raster、固定 canonical 顺序等权 texel 融合、并集 unobserved mask；Python projection/executor focused tests 与 Core/app-server 全量回归 | 仅融合同一 retained Material Zone 的两份 sealed camera raster；仍缺任意视图拼接、相机/UV 自动求解、完整 ActionLoop/工作台图片投影入口、真实千问、packaged E2E 与照片/跨类别质量 |
| U004 P2.7 参考外观 PBR 回执闭合 | 部分实现 | Rust bridge 校验 Worker 原始 `schema_version`、材质区/纹理、source/camera/matrix lineage 和 fusion order；UAS@2 写入 `ReferenceAppearanceProjectionReceipt@1` 并在 build projection 暴露 hash/count；missing-receipt 与 U004 source-lineage focused tests、Python projection tests、core/app-server/desktop cargo check | 证明参考像素被接受的 PBR artifact 使用并可追溯，不证明视觉相似度或照片级质量；完整 ActionLoop/工作台入口、真实千问、packaged E2E、角色/生物/跨类别质量仍缺 |
| U004 P2.12 最终 GLB 像素真值 | 部分实现 | Rust bridge 从 exact final GLB 独立解析 `material → baseColorTexture → image → bufferView`，重新校验 base-color/mask PNG 实际 SHA-256、字节数和尺寸；核心仅允许 base-color `imported_reference/unknown`，其余 PBR 通道继续 builtin；U004 通用图片 preview→confirm→export 和 stale-PBR focused regression 通过 | 关闭“receipt/manifest 成功但最终 GLB 没有参考像素”的工程假阳性；仍不证明照片级相似度、真实千问、packaged GPU、未见输入或跨类别质量 |
| U004 P2.13 低维轮廓拟合 | 部分实现 | Rust 从 sealed image bytes 与同一 GPU auxiliary silhouette tile 派生 16-bin occupancy profile，并将其绑定到候选相机拟合；profile mismatch、非法长度/值 fail-closed，缺失内容兼容 bounds-only legacy fixture；core/app-server focused tests 通过 | 提升离散相机选择和后续外观编译输入的稳定性；仍不证明像素级视觉相似度、真实千问、packaged GPU、未见输入或跨类别质量 |
| U004 P2.14 参考/候选低维视觉指标 | 部分实现 | Rust `RustReferenceVisualMetrics@1` 对 exact sealed reference 与同一 GPU/PBR beauty/auxiliary capture 计算 profile error、foreground bounds IoU、dominant color overlap、brightness/edge agreement；可用摘要 hash 绑定 `VisualReferenceConvergenceEvidence`，明显偏差 fail-closed，旧最小 PNG 仅 `not_available` | 指标是低维确定性补充，不是 Qwen 语义比较、照片级相似度、真人评分、packaged GPU 或跨类别质量证据 |
| U004 P2.8 Provider 合同投影 | 部分实现 | `author_universal_asset` 由公共 UniversalAuthor/SubjectProfile/VisualFeature/RepresentationPlan/Limitation schema 内联生成三类 typed outcome；Provider context 获得 Rust-owned capability manifest/hash 与 available/unavailable 分支；Product Tool SHA-256 validator 回归；product_tools 103 项、app-server 266 项、U004 candidate PBR contract Gate | 这是理解与合同边界，不是新几何能力；Rust 仍最终校验，角色/生物/植物/任意 mesh 仍 limitation；真实 DeepSeek/千问、packaged E2E、跨类别视觉质量仍未运行 |
| U004 P2.9 工作台 universal image author transport | 部分实现 | 新增 `buildAgentTurnRequestPayload` 单一桌面 payload builder；sealed 图片只以 evidence ID、用户角色和 view hint 进入 `author_context`，完整只读视觉图保留用于 Rust 归一化；Universal Turn 不再同时携带 legacy `multimodal_context`，app-server-protocol 对双来源请求 fail-closed；payload smoke 验证不携带客户端自报 hash、Project、Turn 或 capability，`desktop:u002-universal-author-workbench-smoke`、F026 单视口回归与 protocol 42 tests 通过 | 该 Gate 只证明图片从工作台进入 Rust universal author Turn 的传输边界，不证明用户界面的最终像素投影、真实 DeepSeek/千问、packaged GPU、照片级质量、未见输入或跨类别视觉质量；Rust 二阶段 UV/PBR bake 与回执仍需完整 ActionLoop/正式工作台结果继续验证 |
| 游戏资产交付（LOD/碰撞/socket/UV 密度） | 部分实现（本地受限链路） | `GameAssetProfile@1`、新增 `GameAssetDeliveryRequest@1`、Core `meshopt@0.6.2` deterministic/error-bound LOD tests、`compile_game_asset_lod_delivery` / `verify_game_asset_lod_delivery_glb`、`derive_game_asset_delivery_bindings`、`compile_game_asset_delivery` / `verify_game_asset_delivery_glb`、`GameAssetTexelDensityReadback` LOD0 POSITION/UV0/embedded PNG measurement tests、UAS@2 source→delivery receipt/preview byte lineage、双工件 atomic repository/runtime export、`ForgeAssetPackage@2` source/delivery manifest focused tests、`u004_game_delivery_valid_glb_preview_confirm_export_and_package_round_trip` | 工作台提供轻量/标准 bounded profile selector；StartAgentTurn、Rust digest/idempotency、Product Tool binding 和 provider-profile override fail-closed 已通过，collision proxy IDs 由实际 executable Part bindings 派生且 socket 初始为空。已有真实 `MSFT_lod`、off-scene AABB collision mesh、source/profile/UAS Part binding、topology readback 和逐材料实测有效 texel density；确定性有效 GLB fixture 已完成 source capture→preview→confirm→export/package，receipt 篡改验证、确认重放幂等、source/delivery CAS 存储漂移拒绝以及“确认后 ChangeSet 编辑仍保留 source/delivery 双工件”回归通过；native compatibility `:preview → :preview.glb → :confirm` 全路径也已通过。仍缺 packaged 设备 loader、真实千问和真实用户质量证据；不达目标密度绝不放行 |
| DeepSeek/千问唯一 AI Provider | 已实现（Alpha） | U004A；`release:ai-provider-policy`、Rust 非法 endpoint/model 反向测试、desktop build、F026 smoke | 真实组合 E2E 与视觉质量仍须单独验证；第三个 Provider 禁止 |
| 同一工作台 PBR 瞬态采集与开放图片比较合同 | 部分实现 | `WorkbenchPbrVisualCapture@1`、固定 `640×640` same-renderer capture、实际 camera world/projection/target/frame pose hash、Core `CandidatePbrCaptureSession/Evidence@1`、Rust app-server one-time registry（PNG IHDR dimensions 重检）、`waiting_for_capture` lifecycle、Tauri 受限 `issue/submit/resume` bridge、一次性工作台授权卡与 Tauri `authorize_candidate_pbr_visual_comparison`、UAS@2 `GenericHardSurfaceAppearanceCompilation@2` 对 1–8 个真实材质区的 sealed normal/roughness/conditional-emissive five-channel bake、ActionLoop 同 Turn evaluate→唯一 typed patch→重新 capture continuation、已保存 Agent 资产也通过同一工作台 renderer 生成四张 GPU/PBR 概念图、`VisualEvidenceGraph@2` 到只读 comparison-claim projection（request/profile/feature/plan/V2 graph/program hash 绑定）、`VisualConvergence@2` 单 patch Gate、Rust `CandidatePbrAuxiliaryQuality@1` 五 pass/八视图 semantic gate、Rust-issued `CandidatePbrCaptureSession@1` 环境 ID/hash 与 `VisualReferenceRenderContract@1`（renderer/manifest/environment/color/tone）绑定；2026-07-31 P0.4 又在 macOS/Tauri 概念图入口提前锁定 `pbrRendererId=forgecad-workbench-pbr@1`，旧软件光栅/ShapeProgram fallback 不会进入采集 | 未点击授权卡不联网且不产生 preview/version；续跑重算 exact scope，漂移、无 grant、环境身份缺失或已失效 grant 全部 fail-closed，patch 清除旧 grant；桌面主路径不再把 `forgecad-agent-software-raster@1` 作为质量展示，浏览器兼容路径和历史诊断仍保留；Appearance Compiler 不等同 UV 投影或照片材质恢复；辅助指标只证明同一 renderer 的非空 GPU 语义证据，不替代 Qwen 相似度；真实千问调用、packaged 八视图工作台 E2E、未见输入质量与跨类别表示尚未完成；重启会显式终止内存 continuation |
| Deformable、Local Hybrid 与 bounded local mesh patch | 部分实现（仅受限本地硬表面执行分支） | U004 `deformable.local_lattice_shell_v1` 使用固定 `2×2×2` cage、每轴 `±25%` AABB offset；`mesh_seed.local_patch_v1` 只对已审查 ShapeProgram mesh 追加一个 `local_mesh_patch` terminal，center/radius/offset 均由 Rust/Python/Worker bounded 校验，且保持拓扑、材质与 Part/Zone/readback lineage。两者都有 VP203 lowering、UAS@2 source binding、author/build/capture/一次 VP204 patch 路由；Skill manifest/schema/generated registry/Rust 上限与 20 个 runtime operation 已闭合，`agent:g819-runtime-operation-manifest-smoke` 覆盖 lattice 与 local patch 的真实 GLB 编译和未知操作拒绝 | 仅审查过的机械硬表面程序化源和生成后局部补丁可用；没有任意导入 mesh、角色、生物、布料、通用 mesh seed 或神经混合。真实千问、packaged E2E、confirm/version/Snapshot/export、跨类别净质量与真人门仍缺，不能承诺用户质量 |
| 跨类别正式质量 | blocked | U005 测试策略已冻结 | 八类真实输入、首轮/一次 patch、时间/成本和独立真人盲评均 NOT RUN |
| Packaged 本机 Alpha / 生产发布 | 部分实现 / blocked | `release:packaging-readiness-smoke`、P008、真实 arm64 frozen sidecar、Tauri `.app` LaunchServices、K002/K003 原生双启动和 Rust ownership 均通过；已验证 sidecar 健康、临时 Library、受限几何归属、Rust-owned 产品状态、GLB/readback/render package、重启语义 hash 和 `provider_calls=0` | 签名、公证、全新机安装/升级、跨平台 sidecar、同 commit 发布证据和真实视觉质量仍缺；`release:packaging-readiness` 对未准备的平台目标保持阻断 |
| 特征驱动 Appearance Compiler | 部分实现 | U004 `GenericHardSurfaceAppearanceCompilation@2`；Rust 从 sealed feature description/salience/channel 与 material traits 确定性选择 `SurfaceLayerProgram@1` 的 normal/roughness/emissive motif；Core focused tests 与 Python Worker PBR motif test | 仍限于已声明的 generic hard-surface lowerings 和最多八个真实材质区；不等同 UV/照片纹理恢复、真实 Qwen 比较、角色/生物表示或跨类别视觉 4/5 |
| 可见证据驱动 decal lowering | 部分实现 | U004 P1.2；Rust 仅从 Observed/Inferred + BaseColor 特征选择 bounded decal motif、颜色、固定 token、UV anchor 和 symmetry；Core hidden-evidence negative test 与 Python base-color output test | 最多一个受限 decal/zone；无任意文本、外部贴图、照片投影或真实 Qwen/跨类别质量证据 |
| 可见特征驱动 vector path lowering | 部分实现 | U004 P1.3；Rust 仅从 Observed/Inferred + BaseColor/Normal/Roughness/Metallic 特征选择 bounded VectorPath；Core visible/hidden test 与 Python retained-PBR byte test | 每 zone 最多一个固定路径；仅 retained PBR 纹理层，不是 SVG/CAD/任意曲线、多视图融合、照片投影或真实 Qwen/跨类别质量证据 |
| 六面 surface-panel lowering | 部分实现 | U004 P1.4；`surface_panel` 支持 `±X/±Y/±Z` 六个局部轴向面，Rust VP203/app-server/Python validator/Worker 共享 face-axis 与面内偏移合同；实际 GLB 编译、法向自动附着、Part/Zone/readback lineage；Python 30 tests、app-server focused validator、forgecad-core VP203 12 tests | 仍是轴向盒体/倒角盒体局部附着；不支持任意曲面投影、自由 mesh 编辑、照片材质恢复、角色/生物表示或跨类别质量 |
| 倒角盒体 shell lowering | 部分实现 | U004 P1.5；`shell` 可接收直接 `box` 或已验证 `bevel_approx`，Rust VP203/app-server/Python validator/Worker 共享 radius、segments、源尺寸与内腔净空合同；本地 Manifold CSG 生成闭合薄壁 GLB，保留 Part/Zone/material/readback lineage；beveled-shell deterministic/readback、Core VP203 与 native executor focused tests | 仅限盒体/倒角盒体和受限厚度/半径；不支持通用 CAD offset、任意 CSG、自由 mesh、照片材质恢复、角色/生物表示或跨类别质量 |
| 受限 face-groove lowering | 部分实现 | U004 P1.6；高层 `Groove` 由 Rust 校验六面轴向源、面内尺寸/偏移和深度，并确定性 lowering 为 bounded cutter box + `subtract`；Python Worker 真实编译浅凹 GLB/readback；schema/生成 registry、VP204 node lineage、Core lowering、Python deterministic/readback 与 G819 manifest tests | 仅限盒体/倒角盒体轴向浅凹；不支持任意 boolean、曲面雕刻、自由 mesh、照片材质恢复、角色/生物表示或跨类别质量 |
| 高层 20 操作合同与静态姿态 | 部分实现 | U004 P1.7/P3.1；公开 Schema、生成类型/registry、Rust high-level lowering 和受限 Worker 对齐 20 个已登记操作；operation-coverage fixture、`contracts:types:check`、VP203 15 tests、3 个 GLB/readback fixture、local patch lowering 与 rotation focused test 通过；基础体/profile 节点的静态 Euler rotation 由 Rust 限制在 `[-π,π]` | 仅暴露已登记且有界的 procedural hard-surface/local-lattice/local-patch；不支持任意导入 mesh、自由曲面、角色/生物/布料、照片重建、真实 Qwen 质量或跨类别 4/5 |
| bounded 外观颜色语义 | 部分实现 | U004 P1.8；`SurfaceLayerProgram@1` 和 retained PBR lowering 允许六个 Rust-owned `base_color_token`；sealed feature/material text 可确定性选择银、白陶瓷、枪灰、石墨、铜、警示红，Python 五通道 bake 将受限 tint 写入真实 base-color PNG；Rust Core 6、native executor 1、Python 35、contracts generate/check 通过 | 仅是 reviewed base material 的 bounded tint；不接受自由 RGB、任意贴图或照片材质恢复，不证明真实 Qwen、角色/生物或跨类别真人 4/5 |
| bounded 表面 finish 语义 | 部分实现 | U004 P1.9；`surface_finish_token` 仅允许八个 Rust-owned finish；sealed feature/material text 与 reviewed base material 可选择 `brushed_metal`/`polished_metal`/`ceramic_coat`/`glossy_coat`/`matte_coat`/`rubberized`/`dark_glass`/`emissive_trim`，Python 五通道 bake 将其实际写入 metallic/roughness，Rust/Python/native executor focused tests 通过 | 仍是受限参数混合，不是任意 shader、任意 scalar、照片材质恢复、真实 Qwen、角色/生物或跨类别真人 4/5 |
| sealed reference surface facts → appearance compiler | 部分实现 | U004 P2.15；Rust `ReferenceSurfaceAppearanceBinding@1` 绑定 same-project/evidence hash、Rust-derived foreground color buckets、brightness/edge facts 与 bounded color/finish/roughness fallback；绑定 hash 进入 `GenericHardSurfaceAppearanceCompilation@2`，程序化/local lattice/Hybrid/local mesh patch/typed patch 统一重编译；Core UAS、app-server U004 14 项和 generated contracts regression 通过 | 低维事实不是照片纹理或真实材质测量；不接受自由 RGB/Provider 自报；仍缺真实 Qwen、packaged GPU、未见输入、角色/生物表示和真人 4/5 |
| U004 P2.16 参考 fallback 作用域 | 部分实现 | Rust 按 semantic role、reviewed base material 和 sealed feature/material text 限制参考颜色/finish/roughness 只进入兼容外壳/装甲/外部面板；structural/accent/special-material 负向测试通过，显式 black/gray 词映射已闭合 | 仍是全局低维参考事实的受限补充，不能推断逐材质区照片颜色或背面材质；真实 Qwen、packaged GPU、未见输入、角色/生物和真人 4/5 仍缺 |
| U004 P2.17 exact observed feature-to-zone appearance scope | 部分实现 | Rust 复用 `ReferenceAppearanceBinding@1`，要求 sealed observed appearance feature region 与 Subject Part/Material Zone 精确一致后才允许 `ReferenceSurfaceAppearanceBinding@1` 的颜色/finish/roughness fallback；无 observed region 的 sibling zone 不继承整图事实；Core UAS 7 项 focused tests 通过 | 仍是低维受限 fallback，不是逐区照片材质恢复、真实 Qwen、packaged GPU、未见输入、角色/生物或真人 4/5 |
| U004 P2.10 相机拟合门控与二次参考外观编译 | 部分实现 | 首次 UAS@2 candidate 只编译几何；Rust 从同一工作台辅助 silhouette capture 拟合 sealed reference camera 后，按拟合 view 重开 UAS、重新生成 `ReferenceCameraUvRasterBake@2`、重新编译 GLB/readback/game-delivery 并返回 `capture_required`；拟合失败 fail-closed；valid/Hybrid/limitation bridge、candidate PBR focused 和 app-server cargo check 通过 | 只关闭默认/未求解相机导致的投影谱系漂移；仍缺完整 ActionLoop 端到端真实 Provider、packaged GPU、任意视图求解、照片级相似度、未见输入和跨类别视觉 4/5 |
| U004 P2.11 两阶段实际桥接与 PBR 材质完整性门 | 部分实现 | valid sealed-image bridge 通过实际 `AppServerBridge::resume_candidate_pbr_capture` 验证 `capture_required → 重新采集 → authorization_required`；工作台 identity 拒绝不完整五通道 PBR、少于五张嵌入纹理、色彩空间错误和采样错误；U004 workbench smoke、GPU/PBR Playwright、desktop typecheck 通过 | 仍缺真实千问、packaged GPU、照片级相似度、未见输入、角色/生物/跨类别质量和真人 4/5 |
| U004 P0.5 packaged GPU/PBR eight-view evidence | 部分实现 | 既有 `desktop:c111b-packaged-agent-webgl` 由 TS/Rust 双侧绑定 `forgecad-workbench-pbr@1`、固定 render manifest、`env_forgecad_room_studio_v2`、sRGB/ACES、至少五张嵌入纹理；每个八视图的 `960×640` silhouette/normal/depth/part-ID/material-ID auxiliary PNG 与 beauty PNG 一起进入 Rust capture command，由 Rust 重读 IHDR、计算 SHA-256、写固定 `.auxiliary.png` 工件并重验 dimensions/pass IDs/字节/hash。开发 build、Tauri app rebuild、logic smoke、Rust focused test、U004 PBR smoke、typecheck/contracts 通过 | 本轮真实 packaged LaunchServices QA 被锁屏前置检查阻断（`C111B_PACKAGED_SCREEN_LOCKED`），未取得 packaged GPU 八视图实证；不证明真实千问、照片级相似度、未见输入或跨类别视觉 4/5 |
| 在线 author 高层几何编写规范 | 部分实现 | Rust `ValidatedUniversalAuthorContext::provider_projection` 投影 capability manifest/hash、sealed evidence ledger、`geometry_authoring_playbook`；`forgecad-app-server` 上下文/兼容图 focused tests 通过；规范覆盖 canonical chains、profile/sweep/loft、局部面板/凹槽、disjoint output graph、预算和 macro→meso→micro 优先级 | 只是 DeepSeek 的只读合同指南，不是对象模板或新执行权限；仍缺真实在线首轮完整通过、GLB/readback、真实千问比较、未见输入和跨类别视觉 4/5 |
| 逐部件程序化能力组合 | 部分实现 | `UniversalAuthorOutcome` 与 `UniversalAssetSourceV2` 允许同一受限 `ForgeVisualGeometryProgram@2` 按部件组合 `generic_hard_surface` 与 `generic_visual_exterior`；混合 domain、单一 UAS/GLB/PBR lineage；Core composition test、native U004 17 tests、recovery tests 通过 | 只组合当前已实现的程序化 visual capabilities；不开放机械臂混入、通用 deformable、任意 mesh seed 或神经重建；真实 DeepSeek、真实千问和跨类别视觉 4/5 仍缺 |
| Weapon/Concept 兼容 | 兼容基线 | `COMPATIBILITY_MIGRATION.md`、版本化 OpenAPI/migrations | M5/M6 未退出；不得作为新产品能力 |
| 在线 universal-author 合同恢复与兼容证据展示 | 部分实现 | Rust projection 明确 SubjectProfile parts、VFC requirements、RepresentationPlan rows 的 exact closed-set/bidirectional mapping；ActionLoop 对 `VISUAL_FEATURE_REQUIREMENTS_INCOMPLETE`、`REPRESENTATION_PARTS_INCOMPLETE`、`REPRESENTATION_PART_PLAN_INVALID` 和未登记 rounded geometry 提供一次窄恢复；前端仅在实际 `plan_complete_concept` item 存在时显示兼容证据；app-server 282、U002、desktop smoke/build 通过 | 真实 DeepSeek→GLB/readback 复测仍受当前 macOS 锁屏阻断；不证明照片级质量、真实千问、未见输入或跨类别 4/5；Rust 仍不自动修改 Provider 语义或生成机械臂替代模板 |
| 在线 author profile/loft 合同窄恢复 | 部分实现 | ActionLoop 对 `FORGE_VISUAL_VP203_PROFILE_BOUNDS`、`PROFILE_SELF_INTERSECTION`、`PROFILE_WINDING_OR_DEGENERATE`、`SECTION_SET_INVALID`、`SECTION_RESAMPLE_MISMATCH`、`SECTION_CAP_INVALID` 和缺失几何引用提供一次 typed recovery；author playbook 要求 generic_visual_exterior 有机对象优先 capsule/box/cylinder，并明确统一 profile 采样计数；app-server 282/282 与工作台 smoke 通过 | 真实 DeepSeek 已实际调用但最新猫 Turn 在自动锁屏时仍为 `running`，尚无 GLB/readback 成功证据；不证明照片级质量、真实千问、未见输入或跨类别 4/5 |
| 在线 SubjectProfile feature 合同窄恢复 | 部分实现 | `SUBJECT_FEATURE_INVALID` 现在有一次 typed recovery，要求 feature_id 唯一、feature.part_id 精确引用声明的 SubjectProfile.parts，并区分 SubjectProfile 与 VFC affected_part_ids；app-server 282/282、U002 smoke、desktop typecheck、最新 Tauri bundle 通过 | 最新修复尚未再次调用真实 Provider；先前真实猫 author 仍未形成 GLB/readback，真实千问、照片级质量、未见输入和跨类别 4/5 仍缺 |
| 在线 SubjectProfile author 提示边界加固 | 部分实现 | UniversalAuthorContext 明确 `SubjectProfile.features` 的扁平字段、唯一 feature ID、声明部件逐字引用和单部件 macro/meso/micro 最小样例，并禁止 VFC/RepresentationPlan 字段混入；projection focused、本机跨类别 provider 1/1、PBR capture smoke、contracts、desktop typecheck、packaged QA logic、最新 Tauri bundle 和 diff check 通过 | 只降低真实 DeepSeek 的合同误解概率，不替代真实在线 author→GLB/readback；macOS 锁屏仍阻断下一次真实请求，真实千问、packaged GPU、照片级/跨类别质量和真人 4/5 仍缺 |
| 工作台类别开放入口与通用材质域 | 部分实现 | 侧栏/新建设计示例改为类别开放对象；GLB 导入默认 `pack_unclassified`，不再从文件名猜 Domain Pack；`pack_unclassified` 映射到 `generic_visual_exterior`，通用 PBR 预设可达；F002、F026、U002 smoke、typecheck、Tauri bundle 和 diff check 通过 | 入口修复不等于专用角色/生物表示或照片级质量；真实 DeepSeek→GLB/readback、千问、packaged GPU、跨类别盲测和真人 4/5 仍缺 |

| U004 在线 author Rust canonicalization | 部分实现 | Rust 在 `author_universal_asset` 的 lineage 绑定前，对重复镜像 feature/VFC 行执行按部件拆分，对明显反向 profile、有限唯一 loft section 顺序/cap/resample 和跨输出 mirror ancestor fan-out 执行有界规范化；预算下限按实际节点/部件/材质/输出/操作数提升但不超过 reviewed ceilings，VP203 真实 triangle estimate 仍最终裁决。新增 2 个 native focused tests；`cargo check -p forgecad-app-server`、app-server 284 tests 通过 | 只处理可证明的合同/元数据等价变换；非法轮廓、wrapper/part 重复、未知几何、越界或不确定拓扑继续 fail-closed；源代码尚未在解锁 packaged 工作台进行新的真实 DeepSeek→GLB/readback/capture，真实千问、照片级和跨类别真人质量仍缺 |

| U004 当前运行验证 | 部分实现 | canonicalization 后 app-server 全量 284/284、workspace cargo check、U002 contract、contracts/docs/integrity/safety/secrets/provider policy、desktop typecheck/build、F026、U002 workbench smoke、最新 Tauri bundle 和 diff check 通过 | 最新 R3 smoke 在 GLB 导入后等待当前 UI 的 `分件候选/导入参考模型 v1` 超时；U004 image bridge 本轮首次 Tauri 重链超过 4 分钟无输出后终止，均不计为通过。macOS 锁屏仍阻断新的真实 DeepSeek→GLB/readback/capture，真实千问、照片级和跨类别真人质量仍缺 |

| U004 兼容入口修复与 R3 恢复 | 部分实现 | Python test oracle 接受 Rust category-open 只读参考值 `pack_unclassified`；R3 smoke 已通过 GLB 导入、Agent-first 工作台、重启恢复和导出边界；Python `agent:unit` 183 项、agent/provider policy、docs/integrity/safety/secrets/contracts/diff Gate 通过 | 仅修复测试/兼容入口与错误可观测性；没有创建新的通用几何能力。真实 DeepSeek→GLB/readback/capture、packaged GPU、千问比较、照片级和跨类别真人质量仍缺 |

状态定义：`已实现` 必须有当前代码与 Gate；`部分实现` 必须列出缺口；`目标设计` 没有用户可用承诺；`blocked` 表示退出证据明确缺失。任何目标名称、截图、模型自评或单次 Provider 成功都不能改变状态。
