# ForgeCAD 能力—Gate 矩阵

> 2026-08-28 `FPS-FORM-04BE-F`：closed failure-diagnostic contracts=`PASS_2`；默认只读 MCP tool=`PASS`；exact 04BE-E durable evidence/GeometryProgram binding=`PASS`；Runtime restart canonical equality=`PASS`；logical SQLite/CAS zero-write=`PASS`；left boundary regression=`CONFIRMED`；right owner intrusion increase=`CONFIRMED`；rear3q owner attribution=`CALIBRATION_CONFLICT`；side trigger aperture=`SEALED_LEFT_RIGHT`；line-flow=`INDEPENDENTLY_UNRESOLVED`；new geometry repair=`NOT_AUTHORIZED`；FormQualityV2=`NOT_CREATED`；secondary/confirm/version/export=`NO`；quality=`QUALITY_TARGET_NOT_MET`；commercial=`NOT_PROVEN`。当前 **579 schemas / 128 read + 94 opt-in write = 222 tools**。

> 2026-08-28 `FPS-FORM-04BE-D`：closed repair-plan contracts=`PASS_2`；默认只读 MCP tool=`PASS`；mandatory preflight=`PASS`；exact 04BE-C evidence/GeometryProgram binding=`PASS`；Runtime restart exact plan equality=`PASS`；logical SQLite/CAS zero-write=`PASS`；registered strategy=`rear-stock-owner-void-half-y-flat-z@1`；repair execution=`NOT_RUN`；left boundary/owner-void/rear3q attribution=`FAIL_RETAINED`；FormQualityV2=`NOT_CREATED`；secondary/confirm/version/export=`NO`；human/engine=`NOT_RUN`；quality=`QUALITY_TARGET_NOT_MET`；commercial=`NOT_PROVEN`。当前 **577 schemas / 127 read + 94 opt-in write = 221 tools**。

> 2026-08-28 `FPS-FORM-04BE-C`：typed composite evidence sidecar=`PASS_REAL_D1`；same-cohort 6 views × 9 AOV=`PASS`；CrossView/FormArt durable CAS/Store/restart GET=`PASS`；6-view Part-ID=`observed`；no-regression=`FAIL_LEFT_VIEW`；strict owner-void=`FAIL_LEFT_RIGHT_REAR3Q`；FormArt ready=`false`；FormQualityV2=`NOT_CREATED`；Stage=`camera-calibrated`；secondary/confirm/version/export=`NO`；human/engine=`NOT_RUN`；quality=`QUALITY_TARGET_NOT_MET`；commercial=`NOT_PROVEN`。当前 **575 schemas / 126 read + 94 opt-in write = 220 tools**。

> 2026-08-28 `FPS-FORM-04BE-B`：typed original/current/final composite=`PASS`；Runtime-only candidate/Store/CAS/receipt=`PASS_REAL_D1`；23-Part / 1916-triangle strict GLB readback=`PASS`；Runtime restart GET hash equality=`PASS`；exact six views × 9 AOV=`NOT_RUN_04BE_C`；candidate-bound FormArt/Part-ID/negative-space/line-flow/strict owner-void/no-regression=`NOT_RUN`；FormQualityV2=`NOT_RUN`；secondary=`NOT_CREATED`；Stage=`camera-calibrated`；confirm/version/export=`NO`；human/engine=`NOT_RUN`；quality=`QUALITY_TARGET_NOT_MET`；commercial=`NOT_PROVEN`。当前 **572 schemas / 125 read + 93 opt-in write = 218 tools**。

> 2026-08-27 增量：`FPS-FORM-04AV` source/compile Gate=`PASS_SOURCE_COMPILE_READ_ONLY_NON_PROMOTING`。`ProductionWeaponOwnerReviewedVoidCalibrationProjection@1` 与默认只读 GET 已从 durable FormArt/fresh baseline/RigV2/Part-ID/depth/批准相机派生三视图 owner calibration，并将 calibration eligibility 与 strict owner-void 零侵入门分离。contracts=`PASS_568`，compile=`PASS_WITH_EXISTING_WARNINGS`，real-D1 projection=`NOT_RUN_SOURCE_DATABASE_UNAVAILABLE`，binding=`NOT_PROVEN`，new-shape=`NO`，secondary=`NOT_CREATED`，formal-high=`BLOCKED_SECONDARY_FORM_APPROVAL`，low/uv/bake/fps/engine/human=`NOT_RUN`，commercial=`NOT_PROVEN`。当前 **568 schemas / 124 read + 92 opt-in write = 216 tools**。

> 2026-08-27 `FPS-FORM-04AS`：`FormQualityV2` 新鲜基线适配器已在 Contracts、MCP、Runtime 与 Store 收口。它明确分离 source scope（当前 Stage head、CameraLock、same-cohort fresh baseline、registration lineage、RigV2）与 evaluation scope（distinct proposal candidate、proposal CrossView、proposal-side Part-ID/negative-space/line-flow）；所有调用字段均由 Runtime 从 durable evidence 重派生并由 Store 独立回读验证，legacy 模式不得夹带 proposal scope。538-schema checker、Contracts/Store/Runtime/MCP compile、四组件 same-cohort build identity 均 PASS，source cohort=`acf10c3b…173`。这只是 source/compile gate：当前真实 D1 `candidate-9127…fdc8b` 仍为 `REJECTED_REGRESSION`，未用新 adapter 重跑，Stage=`camera-calibrated`、secondary=`NOT_CREATED`、quality=`QUALITY_TARGET_NOT_MET`，无 confirm/version/export。下一原子是基于批准相机设计新的 bounded `rear-stock` art-shape，只有 proposal evidence=`READY` 且 fresh FormQualityV2 真实运行通过后才允许推进 Stage。证据：`docs/evidence/mcp010f/production-weapon-form-quality-v2-fresh-baseline-adapter-source-gate-04as-20260827.json`。

> 2026-08-27 `FPS-FORM-04AR`：real D1 user orientation lineage/restart=`PASS`；execution cohort `55c50eb6…876e1f` fresh 6-view/9-AOV baseline/restart=`PASS`；620×680 notch half-edge child/GLB strict readback/proposal FormArt Store readback=`PASS_STRUCTURAL_DURABLE`；Part-ID all views=`PASS`；negative-space all views=`FAIL`；line-flow all views=`FAIL`；strict owner-void all views=`FAIL`；CrossView=`NOT_IMPROVED / REJECTED_REGRESSION`；secondary=`NOT_CREATED`；Stage=`camera-calibrated`；quality=`QUALITY_TARGET_NOT_MET`；confirm/version/export=`NO`。post-run closed-schema sync/current four-component build cohort=`07c53f8c…797ca`、538 contracts/build=`PASS`，未重跑 real D1。

> 2026-08-27 `FPS-FORM-04AP/04AQ`：batch immediate abort=`PASS_SOURCE`；same-key pre-render single-flight=`PASS_SOURCE`；typed exact orientation approval context=`PASS_SOURCE_COMPILE`；post-render pre-commit/replay source revalidation=`PASS_SOURCE`；temporary same-hash canonical metadata=`PASS_FOCUSED`；GET restart truth=`PASS_SOURCE`；contracts=`PASS_538`；Stage0=`PASS_WITH_QUALITY_TARGET_NOT_MET`；real D1 lineage/baseline/notch=`NOT_RUN_BLOCKED_EXACT_USER_APPROVAL`；secondary=`NOT_CREATED`；visual=`QUALITY_TARGET_NOT_MET`。

> 2026-08-27 `FPS-FORM-04AL` 当前增量：Runtime-owned durable fresh six-view baseline producer 已接通合同、Store、Runtime 与 MCP `prepare/get`；每个视图绑定 approved registration lineage / RigV2、fresh same-cohort 512×512 九 AOV、camera/mask/compare/quality 与完整 CAS reachability，并以单事务持久化。精确状态为 `PASS_SOURCE_COMPILE_DURABLE_PRODUCER_NOT_RUN_REAL_D1`；真实 D1、orientation approval、fresh baseline、notch、secondary、Stage/confirm/version/export 均未执行。当前公共面 **538 schemas / 118 read + 88 opt-in write = 206 tools**，视觉仍 `QUALITY_TARGET_NOT_MET`。

> `FPS-FORM-04AK`：fresh-baseline preflight source/compile=`PASS_ZERO_WRITE`；lineage/RigV2/six-view validation=`PASS_SOURCE`；materializer=`UNAVAILABLE`；真实 D1 lineage/baseline/notch=`NOT_RUN`；secondary=`NOT_CREATED`；visual=`QUALITY_TARGET_NOT_MET`。当前 **533 schemas / 117 read + 87 write = 204 tools**。

> `FPS-FORM-04AJ`：real-D1 620/680 readonly proposal=`PASS_ZERO_WRITE`；historical FormArt immutable readback=`PASS`；same-cohort active baseline=`BLOCKED_REFRESH_REQUIRED`；orientation approval=`BLOCKED_REQUIRED`；durable notch/fresh six-view=`NOT_RUN`；secondary=`NOT_CREATED`；visual=`QUALITY_TARGET_NOT_MET`。该轮为 531/116+87；当前由 04AK 更新为 **533 schemas / 117 read + 87 write = 204 tools**。

> `FPS-FORM-04AI`：successor contracts/Runtime/MCP compile=`PASS`；真实 D1 Runtime-derived camera projection=`PASS_ZERO_WRITE / READY_FOR_USER_REVIEW`；CAS `1509→1509`、lineage `0→0`；orientation-specific user approval=`BLOCKED_REQUIRED`；durable lineage/restart=`NOT_RUN`；secondary=`NOT_CREATED`；visual=`QUALITY_TARGET_NOT_MET`。该轮为 531/116+87；当前为 **533 schemas / 117 read + 87 write = 204 tools**。

> `FPS-FORM-04AH`：rear3q semantic-camera orientation source seam=`PASS_SOURCE_COMPILE`；orientation-specific user receipt=`BLOCKED_REQUIRED`；real D1 durable lineage/restart=`NOT_RUN`；secondary form=`NOT_CREATED`；visual=`QUALITY_TARGET_NOT_MET`。不得把该 source PASS 写成 camera/visual/human PASS。

> 2026-08-26 `FPS-FORM-04AC-OPEN-FRAME-NOTCH`：Contracts/compile=`PASS`；closed-manifold U-frame kernel=`PASS`；真实 D1 durable child/restart/strict GLB=`PASS_STRUCTURAL_EXECUTION`；fresh proposal FormArt=`PASS_TRANSPORT`；six-view non-regression=`FAIL`；candidate=`REJECTED_BASELINE_RETAINED`；secondary=`NOT_CREATED`；quality=`QUALITY_TARGET_NOT_MET`；High/Low/UV/Bake/Material/FPS/Engine/Human=`NOT_PASSED/NOT_RUN`。合同面 529 schemas，工具面仍 202。

> 2026-08-26 `FPS-AUTHORING-MESH-V2-03` + `FPS-FORM-04AG` durable slice：当前 source=**528 schemas / 28 operators / 115 read + 87 opt-in write = 202 tools**。real D1 child/lower/readback/six-view=`PASS_STRUCTURAL_EXECUTION`，proposal FormArt CAS/Store/replay/restart=`PASS_DURABLE_TRANSPORT`；Pareto diagnostic=`REVIEWABLE_TRADEOFF`，实际 owner/open-void/negative-space/line-flow 内容门=`BLOCKED_PROPOSAL_FORM_ART_EVIDENCE`；secondary=`NOT_CREATED`；High/Low/UV/Bake/Material/FPS/Engine/Human=`NOT_PASSED/NOT_RUN`；总体=`QUALITY_TARGET_NOT_MET / commercial=NOT_PROVEN`。

> `FPS-FORM-04AG` durable evidence row：`ProductionWeaponFormArtProposalEvidence@1` schema/Store/same-key replay/Runtime restart readback=`PASS`；hash-only receipt 见 `docs/evidence/mcp010f/production-weapon-formart-proposal-owner-evidence-durable-store-04ag-20260826.json`。六视图 Part-ID=`observed`，owner/open-void、negative-space、line-flow 内容门=`BLOCKED_PROPOSAL_FORM_ART_EVIDENCE`，secondary=`NOT_CREATED`，quality=`QUALITY_TARGET_NOT_MET`；durable PASS、`REVIEWABLE_TRADEOFF` 或 diagnostic `PASS_*` 均不是质量或 Stage PASS。

> 2026-08-26 `FPS-AUTHORING-MESH-V2-02`：现行 source=**527 schemas / 28 operators / 114 read + 86 opt-in write = 200 tools**。真实 D1 rear-stock candidate-owned source→8V/6Q AuthoringMesh V2 genesis→CAS/Store→Runtime restart get=`PASS_STRUCTURAL_SOURCE_BOUND`；typed revision 保留 source/material/transform binding。Native High V2 bridge 与 `FaceExtrude` 仅 source compile。`MoveVertices`、proposal、六视图、secondary、High/Low/UV/Bake/Material/FPS/Engine/Human 均未通过；总体仍 `QUALITY_TARGET_NOT_MET / commercial=NOT_PROVEN`。

> 2026-08-26 `FPS-FORM-04AF`：现行 source=**527 schemas / 28 operators / 114 read + 85 opt-in write = 199 tools**。real D1 rear-stock source repair=`PASS_EXECUTION / FAIL_VISUAL_NON_REGRESSION / REJECTED`；front/back/top 不回退，left/right/rear-three-quarter 回退，无 confirm/version/export。`AuthoringMesh@2` genesis→split-edge→restart=`PASS_RUNTIME_DURABLE_STRUCTURAL_ONLY`；real weapon binding/High/UV/Bake=`NOT_PROVEN`。CameraLock orientation preflight=`BLOCKED_ORIENTATION_SPECIFIC_USER_RECEIPT`。综合状态仍为 `QUALITY_TARGET_NOT_MET / commercial=NOT_PROVEN`。

> 2026-08-26 `FPS-FORM-04AE` 当前权威口径：**525 schemas / 28 operator entries / 112 read + 84 opt-in write = 196 MCP tools**；CameraLock、AuthoringMesh V2、Native High evaluator、High↔Low↔Cage/bake validator、MaterialLayerGraph plan=`PASS_SOURCE_COMPILE_ONLY`；Manifold feature=`PASS_COMPILE_LINK_ONLY`；OpenSubdiv=`UNAVAILABLE_NOT_ADOPTED`；real D1 child instance/restart=`NOT_RUN`；rear3q authored orientation=`BLOCKED_REQUIRED`；secondary Form=`NOT_CREATED`；正式 High/Low/UV/Bake、FPS、Engine、Human 均未通过/未运行。不得由研究结论、source compile 或目标合同提升能力状态。

> 2026-08-26 `FPS-FORM-04AD` 权威增量：当前合同面为 **518 schemas / 111 read + 83 opt-in write = 194 tools**。新增 `ProductionWeaponSemanticLandmarkOrdering@1` 只表达 Runtime-derived 的 3D source/subject-axis 顺序，明确 `target_landmark_arrays_present=false / metrics=NOT_PRESENT`，不得冒充 2D landmark；`ProductionWeaponAuthoredViewOrientation@1` 将诊断变换与用户方向回执分开；`RegisteredCameraRigCalibration@2` 只有绑定 promotable authored rear3q receipt 才能物化。定向 Contracts/Runtime/MCP compile 与 518-schema checker PASS。真实 D1 尚无 orientation-specific user receipt，因此保持 `BLOCKED_AUTHORED_REAR_THREE_QUARTER_ORIENTATION`、Stage=`camera-calibrated`、secondary=`NOT_CREATED`、quality=`QUALITY_TARGET_NOT_MET`，不 confirm/version/export。旧 `@1` 保持历史真值；durable 落点采用 CameraLock 的 additive child lineage，不复制/自动升级整张旧记录。

> 2026-08-26 `FPS-FORM-04AB`：durable real-D1 rebuild=`PASS_1_OF_1`；registered camera replay/FormEvidence/FormArt/CrossView=`PASS`；hash-bound live raster attribution=`PASS_UNIQUE_SOURCE`。`left.open-stock-void` 最大源为 `rear-stock/548px`、次高 `receiver-main/114px`、muzzle-shroud=0，repair eligibility=`UNIQUE_HIGHEST_IMPACT_SOURCE_OBSERVED`。只解锁一个 rear-stock RepairIntent；Stage/confirm/version/export仍零写，secondary/visual/human/engine不变。V2 durable semantic camera lineage是商业晋级前置。证据：`production-weapon-real-d1-semantic-camera-relock-source-attribution-pass-20260826.json`。

> 2026-08-26 商业 Gate 口径：新增 `PASS_COMPILE / PASS_SOURCE / PASS_ASSET / PASS_ENGINE / PASS_HUMAN_ART_REVIEW` 分层；前两层永远不能提升 Hero Asset 终态。静态 Hero 与 premium FPS experience 分开审：动画/VFX/audio/gameplay beats 未通过时，即使静态模型通过也不能称为完整商业皮肤体验。

2026-08-26 supplemental：durable FormArt evidence→pixel→triangle→semantic Part/source node attribution=`PASS_SOURCE_HASH_BOUND / REAL_D1_EXECUTED / UNIQUE_REAR_STOCK_SOURCE`；Runtime-derived binding覆盖 candidate/artifact/readback、reference target、CameraLock/rig、FormEvidence view、RenderSet、mask与triangle/source-table hashes，caller camera/mask fail closed。只解锁一个 rear-stock repair；Formal High raw negative transport=`PASS`，reservation cleanup=`PASS_1_OF_1`，positive prepare/replay/drop-reopen get=`NOT_RUN`。这些状态不升级 Form、High、visual、human、engine、package 或 commercial Gate。

2026-08-26 Formal High public-source 行：Contracts=`PASS_4_CLOSED_WRAPPERS`，Runtime adapter/IPC=`PASS_COMPILE`，MCP=`PASS_SOURCE_EXPOSED_GET_PREPARE`，Store public idempotency=`PASS_SCOPED_UNIQUE_REPLAY_CONFLICT`，Store tests=`3/3 PASS`，MCP module tests=`4/4 PASS`，positive/cleanup/full restart=`NOT_RUN`，real D1=`ZERO_WRITE_BLOCKED_FORM_SOURCE`，visual=`QUALITY_TARGET_NOT_MET`，commercial=`NOT_PROVEN`。当前计数 **518 schemas / 28 operators / 111 read + 83 write = 194 tools**。

2026-08-26 source gate 增量：Form Stage evidence/head mapping 与 Formal High factory/Store/MCP/Runtime seam 已 `PASS_SOURCE_COMPILE_FOCUSED`。Formal High full positive restart/cleanup=`NOT_RUN`、真实 D1 Stage=`camera-calibrated`、secondary=`NOT_CREATED`、visual=`QUALITY_TARGET_NOT_MET`、human/engine/distribution=`NOT_RUN`、commercial=`NOT_PROVEN`。本条不产生 Stage/confirm/version/export。

2026-08-26 Cage/Bake gate：Store 七子表、固定 exact-topology Cage、8-map Bake 与 8-texel dilation=`PASS_SOURCE`；两候选 High resolver=`PASS_COMPILE_FOCUSED_REAL_FIELD_RESOLUTION`，且无条件 blocker 已移除。Runtime-owned distinct derived High candidate + 既有 `ProductionWeaponHighArtifact@1` durable/restart positive materialization=`NOT_RUN`；正式 High positive receipt=`NOT_RUN`。真实 D1 因缺少派生 High candidate，public prepare 仍为 `FORMAL_HIGH_STAGE_SOURCE_LINEAGE_UNAVAILABLE / PRODUCTION_WEAPON_HIGH_LOW_BAKE_PRODUCER_UNAVAILABLE / ZERO_WRITE`；formal Cage/Bake positive receipt=`NOT_RUN`，quality=`NOT_PASSED`。不得提升 Stage、视觉、人评、引擎、分发或商业结论。


当前新增行：Form raster attribution=`PASS_RUNTIME_FORM_ART_HASH_BOUND_SOURCE / REAL_D1_UNIQUE_REAR_STOCK_SOURCE / ZERO_WRITE`；Low quad durable=`PASS_SOURCE_CONTRACT_STORE_RUNTIME_MCP / RESTART_POSITIVE_1_OF_1_PASS / GLOBAL_CAS_LINKED_GC_PASS / DRAFT_UNREVIEWED / CANDIDATE_BOUND_EXACT_PROVENANCE`；Hero UV durable=`PASS_SOURCE_STORE_RUNTIME_MCP_PUBLIC / REAL_RESTART_REPLAY_1_OF_1_PASS / HERO_CAS_ROOTS_4_LINKED_GC / STRUCTURAL_ONLY`。Form 行只解锁一个 rear-stock repair；这些行均不得映射成 artist unwrap、visual、human、engine、commercial 或 packaged PASS，也不推进 Stage、confirm、version 或 export；唯一 `in_progress` 仍为 `FGC-MCP010F`。

> 2026-08-26 最新权威 source 口径（取代下方 2026-08-25 的“最新/当前”计数）：**518 schemas / 28 operator entries / 111 read + 83 opt-in write = 194 MCP tools**。Low quad draft 已接入 Contracts、Store、Runtime 与公共 `low_quad_draft_durable_get/prepare`，当前为 candidate-bound exact provenance 且仍 `DRAFT_UNREVIEWED / structural_only / promotion_eligible=false`，正向 prepare→同键重放→Runtime drop/重启→get **1/1 PASS**，六个 Low CAS root 已纳入全局 linked/GC 判定；packaged 与 artist edge-flow review 仍 `NOT_RUN`。Hero UV 的 `hero_uv_durable_get/prepare` 已完成 Store→Runtime→MCP 公共链，真实 prepare→replay→drop/reopen→get **1/1 PASS**，四个 Hero CAS root 已 linked/reachability/GC；该结果仅 structural/source，不是 artist unwrap、visual、human、engine、commercial 或 packaged PASS。Render Core 的当前 Runtime Form 诊断仍未绑定 exact candidate/reference/camera/hash。以上均不推进 Stage、confirm、version 或 export；`FPS-HIGH-05=NOT_PASSED`、Stage=`camera-calibrated`、visual=`QUALITY_TARGET_NOT_MET`、human/engine/distribution=`NOT_RUN`、commercial=`NOT_PROVEN`、packaged acceptance=`NOT_RUN`、HQ360=`BLOCKED_REFERENCE_COVERAGE`。证据：`docs/evidence/mcp010f/commercial-weapon-hero-uv-durable-restart-source-gate-20260826.json`。

> 2026-08-25 历史快照（已由上方 2026-08-26 权威口径取代）—矩阵口径：**499 schemas / 28 operator entries / 107 read + 79 opt-in write = 186 tools**。Native High=`PASS_SOURCE_DURABLE_RESTART_MCP`（6/6 + 7/7 + restart 1/1）；Low quad draft=`PASS_WORKER_TYPED_PRODUCER_READBACK_ONLY`；Hero UV=`PASS_WORKER_SOURCE_PRODUCER_ONLY`；Viewer Art Director matrix=`PASS_SOURCE_TYPECHECK_AND_BUILD`。后三者不产生 durable/quality PASS；package、candidate quality、visual/human/engine/distribution 未通过，`FPS-HIGH-05=NOT_PASSED`。

2026-08-25 `CQ-02-TYPED-TOPOLOGY-IDENTITY-LINEAGE`：`authoring_mesh_edit_preview → authoring_mesh_edit_prepare` 的 `split_edge / collapse_edge / dissolve_edge` proof 仍保持 source-element-only；下游 Runtime 现在只从 Store 的 exact candidate→idempotency response 恢复该 proof，并把 parent source identity 物化为 durable `AuthoringMeshIdentityLineage@1` child IDs、单调 tombstone 及 one-to-many/many-to-one relation，不接受 caller identity/proof arrays。真实 split/collapse/dissolve 已分别完成各自独立的完整持久化与 Runtime drop/reopen/get 重启链路，合计 **3/3 PASS**；Store `authoring_mesh_` **12/12**、MCP IdentityLineage **3/3**、490-schema checker与 Contracts/Store/Runtime/MCP 联合 compile PASS，工具数仍 **106 read + 78 write = 184**。general correspondence、evaluated retarget、完整 selection/undo history 与产品级 cross-version editor仍 `NOT_PROVEN`。Stage 保持 `camera-calibrated`，视觉=`QUALITY_TARGET_NOT_MET`，human/engine/distribution=`NOT_RUN`，HQ360=`BLOCKED_REFERENCE_COVERAGE`。新回执：`docs/evidence/mcp010f/authoring-mesh-typed-topology-identity-lineage-materialization-source-gate-20260825.json`；原 source-proof 回执继续作为上游证据。

2026-08-25 Native High 较早 source/transport row：6/6/bridge-only 已由顶部 source durable/MCP receipt 取代。package/candidate quality/Stage 仍 `NOT_RUN/NOT_PASSED`，proposal=`registered:false`；其余质量状态不变。

> 2026-08-25 历史机器口径（当前口径见顶部）：**499 schemas / 28 operator catalog entries / 107 read + 79 write = 186 tools**。IdentityLineage split/collapse/dissolve 独立 full-chain **3/3 PASS**；High source durable restart 1/1；当时 Low quad draft与 Hero UV仅 Worker source producer；Art Director matrix仅 Viewer read model。视觉、人评、引擎和发布状态未改变。

> 2026-08-25 商业质量审计历史总行：High 23 Parts / 2,280 triangles；Low/Cage 1,000 triangles；Low 为 deterministic triangle edge collapse，非 artist-editable quad retopo；bake coverage `0.3625035285949707`，miss `45,386`、nearest fallback `107,063`、cross-part `3,982`、padding `0`；4 MaterialZones / 6 embedded 2K fixed-formula textures；Three.js consumer smoke PASS，商业引擎与独立人审 `NOT_RUN`。这些 coverage/miss/fallback/cross-part/padding 数值只是 standalone 失败诊断，不是当前 formal positive receipt。当前 structural=`PARTIAL_SOURCE_PASS`，visual=`QUALITY_TARGET_NOT_MET`，commercial=`NOT_PROVEN`，human/engine/distribution=`NOT_RUN`，HQ360=`BLOCKED_REFERENCE_COVERAGE`。目标 Gate 与晋级顺序见 `COMMERCIAL_GAME_WEAPON_QUALITY_PLAN.md`。

## 商业 Hero Weapon 阶段矩阵

| 阶段 | 当前证据 | 商业 Gate |
|---|---|---|
| Brief / Art Direction / ReferenceViewSet | 用户确认的可见轮廓与相机证据；depth 仍 unknown | `NOT_PROVEN`；五核心视图不完整 |
| Form / secondary approval | Stage=`camera-calibrated` | `secondary-form-approved=NOT_CREATED`；`QUALITY_TARGET_NOT_MET` |
| AuthoringMesh | durable structural slices 与受限 identity lineage | 完整编辑器、general correspondence、evaluated retarget `NOT_PROVEN` |
| Native High | source durable/restart/MCP evidence | `FPS-HIGH-05=NOT_PASSED`；proposal=`registered=false` |
| editable Low / Retopo | candidate-bound durable draft | `DRAFT_UNREVIEWED / structural_only / promotion_eligible=false` |
| Hero UV | public durable replay/restart；四个 Hero CAS roots linked | artist seam/unwrap review、packaged acceptance `NOT_RUN` |
| topology-correspondent Cage / Bake | fixed Worker exact-topology Cage、8 maps、8-texel dilation；Store 七记录 atomic/replay/reachability/restart；public preflight/get/prepare source seam | producer=`UNAVAILABLE_ZERO_WRITE`、formal positive receipt=`NOT_RUN`、quality=`NOT_PASSED`；旧 0.3625 coverage 仅历史失败诊断 |
| Material Layer / PBR | 4 zones / 6 fixed-formula textures | Layer/Mask/Generator/Decal/Wear/Microdetail `NOT_PROVEN` |
| FPS / world presentation / LOD / collision / sockets | 分散 structural/VFX/consumer slices | 完整 hip/ADS/inspect/equip/reload/recoil 与预算 Gate `NOT_RUN` |
| commercial engine | Three.js consumer smoke；历史 evidence-only Godot slice | 同候选 Unreal/Unity 或目标引擎正式 round-trip `NOT_RUN` |
| independent Hero Art Review / delivery | Codex typed review 不具批准权 | human/distribution `NOT_RUN`；无 confirm/version/export |

> `FPS-FORM-04Y/04Z` supplemental row：04Y=`CURRENT_UNIQUE_REGISTRATION_ELIGIBLE`、formal owner gate=false；04Z station 0/1 directions=`STOP_04Z_STATION_DIRECTION`，six-view non-regression=`NOT_RUN`。聚合 fixture 在诊断输出后中止，故 aggregate=`NOT_PASS_RECORDED`。这两项不新增 capability PASS，不改变 `QUALITY_TARGET_NOT_MET`、`camera-calibrated`、`secondary-form-approved=NOT_CREATED` 或任何 commercial/human/engine Gate。

2026-08-25 `FPS-FORM-04H-SUBJECT-FRAME-REGISTRATION-SOURCE` Gate：private typed/read-only source projection **PASS**（synthetic 7/7；正确真实 D1 1/1，0.05s）。Gate 覆盖 program content-hash revalidation、exact PartOutput/source anchors、muzzle/stock X 与 side-left/right Z parity、identity/yaw-180 closed transform、camera rehash、tamper/ambiguous fail-closed、7-sink stability，以及 Runtime/Worker/CAS/SQLite zero-write。回执 `docs/evidence/mcp010f/production-weapon-subject-frame-registration-source-supplemental-20260825.json`。public/durable camera lineage、rear-three-quarter reference registration、owner-void、real FormQuality/human/secondary 均 `NOT_RUN/BLOCKED`，不可由本 Gate 晋级。

2026-08-25 `FPS-FORM-04G-REGION-PART-ID-BINDING` supplemental diagnostic Gate：真实 D1 **1/1 PASS（371.07s）**；receipt `docs/evidence/mcp010f/production-weapon-form-region-part-binding-supplemental-20260824.json` 的三视图均有 baseline/trial 一致的唯一 transform 候选（left/right horizontal flip，rear-three-quarter vertical flip）。由于没有 authored orientation/registration contract 且 discovery threshold 非 acceptance，Gate 结果为 `BLOCKED_AUTHORED_ORIENTATION_OR_REGISTRATION`，仅诊断、不可晋级。FormArt/contours/depth/stage/confirm/version/export 均未改变；下一项是 `FPS-FORM-04H-AUTHORED-VIEW-ORIENTATION-REGISTRATION`。

2026-08-24 `FPS-FORM-PART-ID-AUDIT-04F` supplemental diagnostic Gate：receipt `docs/evidence/mcp010f/production-weapon-form-part-id-audit-supplemental-20260824.json` 为 `BLOCKED_OWNER_BINDING`。stock mutator/Part-ID changed（owner=`rear-stock`，left/right/rear-three-quarter），但 reviewed region↔Part binding 未 ready；FormArt canonical unchanged、depth=`UNKNOWN`、baseline retained。该 Gate 仅 evidence-only，不是 FormQuality/secondary-form-approved PASS；不推进 stage/confirm/version/export，不改 schema/MCP public contracts/manifest/Stage0 hashes。下一项是 `FPS-FORM-04G-REGION-PART-ID-BINDING`。

2026-08-24 `FPS-STOCK-OPEN-FRAME-04E` supplemental diagnostic Gate：同一 user-confirmed target/crop、camera object/hash、view/reference 和 Render Worker cohort 下，`stock-open-frame-clearance=0.22 m` 与 `stock-open-frame-angle=0.12 rad` 的 baseline/trial 均完成六视图外轮廓 + left/right/rear3q exact open-stock void 对比。两者内孔四项指标都无严格改善，外轮廓又有回退，故 fail-closed `accepted=false`、baseline retained。该 Gate 仅是 ephemeral evidence-only，无新 MCP/Contract，无 Stage/confirm/version/export；不是 Part-ID-aware 质量 PASS，下一项为 reviewed region/Part-ID 绑定审计。

2026-08-24 `FPS-FORM-EVIDENCE-04A`：真实参考 CameraLock、`camera-calibrated` Stage、六视图/54 AOV FormEvidence、reviewed-structure FormArt、同 candidate CrossView、durable structural-only legacy FormQuality 与只读 5/7 typed sink registry replay/restart **PASS（结构/传输）**。CrossView 六视图均`QUALITY_TARGET_NOT_MET`且 hard gate=false；FormQuality@2 preflight 五项 ready，仅 CrossView hard gate 与 FormArt target observation 两项 `PREFLIGHT_BLOCKED_ZERO_WRITE`，无secondary head。Low/Cage source bundle虽durable，仍source-only/non-promoting，无artist quad/edge-flow/formal High-Low-Cage/Bake；Hero UV/human/engine/export=`NOT_RUN`。

The earlier source-intake and seven-crop/six-contour fixtures remain historical prerequisites. The current replay includes a durable structural-only legacy FormQuality parent and a real same-candidate CrossView bundle, but every CrossView remains `QUALITY_TARGET_NOT_MET`. FormQuality@2 preflight is read-only and restart-stable; only the failed CrossView hard gate and blocked FormArt target observation remain blockers, so `secondary-form-approved` remains `BLOCKED` and no visual/commercial quality claim is made.

2026-08-22 `CandidateAnimationVfxQuality@2` source/structural gate：Contracts **402**；Store focused **3/3**、Store full **112/112**；Runtime focused **6/6**、同源码同 cohort Runtime full **354 passed / 0 failed / 22 ignored**（376 total，115.40s）；MCP focused **4/4**、full **152 passed / 0 failed / 0 ignored**（2.49s）；contracts/runtime/store/MCP joint cargo check **PASS**。旧 `GEOMETRY_WORKER_PROTOCOL` 报告来自 stale Worker binary，已由同源码 Geometry/Render Worker 重建后清除。尚无真实 `Attachment@3 + Quality@2` public full-chain positive fixture，durable end-to-end=`NOT_RUN`/`BLOCKED_FIXTURE_CHAIN`；当前仅 `structural_only`，visual/artistic/commercial FPS=`NOT_PROVEN`，human/engine=`NOT_RUN`，不推进 stage/confirm/version/export。证据：`docs/evidence/mcp010f/candidate-animation-vfx-quality-v2-durable-source-gate-20260822.json`。

2026-08-22 `CandidateMaterialSurfaceQuality@1` public positive fixture：`Geometry → CandidateTopologyQuality@1 → AppearanceProgram@3 → TextureBuild@2 → SurfaceBake@1 → AppearanceSourceLineage@1 → CandidateMaterialSurfaceQuality@1` 的 `prepare → same-key replay → get → Runtime drop/reopen → restart get` 通过 **1/1（111.72s）**；Runtime focused **5/5**、Store full **74/74**、Contracts **350**。CAS inventory unchanged；stable `artifact_id` 与 GLB object SHA-256、MaterialPack CAS kind 精确区分，合法 UV/tangent rebuild 不计入 geometry-preservation 漂移。该结果仅为 `structural_only`；V2 animated-socket-particles 仍无完整 public `prepare → Store → restart get`，durable end-to-end=`NOT_RUN`/`BLOCKED_FIXTURE_CHAIN`；visual/commercial=`NOT_PROVEN`，human/engine=`NOT_RUN`，stage/confirm/version/export=false。证据：`docs/evidence/mcp010f/candidate-material-surface-quality-public-positive-source-gate-20260822.json`。

最终同 cohort 修订口径：强制 build cohort `724278fe8f6777c8b3d07bc5058208aee90aa5c700db5f9284d6297126fa79f6` 下 material focused **5/5（112.63s）**；Runtime full **310 passed / 0 failed / 20 ignored**（330 total，201.91s），且 public material fixture 明确在该 full run 内执行。此前 **111.72s** 仅为 public fixture 单测时长；两者都只支持 `structural_only`，不提升 visual/commercial、human/engine 或 stage/confirm/version/export 状态。

2026-08-22 `FictionalEnergyVfxAnimatedSocketParticlesSequence@2` 双候选 source slice：Contracts **350**；Store V2 focused **2/2**、Store full **74/74**；Runtime V2 仅低层 focused **6/6**、cargo check **PASS**；MCP V2 **3/3**；同 cohort `724278fe8f6777c8b3d07bc5058208aee90aa5c700db5f9284d6297126fa79f6` Runtime full **309 passed / 0 failed / 20 ignored**（191.06s）、MCP full **128 passed / 0 failed / 0 ignored**（1.93s），这些是全量回归，不是 V2 public `prepare → Store → restart get` 正向 fixture。V1/V2 隔离；V2 仅证明 1..16 frame、geometry/appearance 双 candidate/delivery/AnchorSet bridge 以及 Store FK/reachability/idempotence/conflict/rollback 的结构面。完整双候选 public Runtime `prepare → Store → restart get` 正向 fixture 尚不存在，durable end-to-end=`NOT_RUN` / `BLOCKED_FIXTURE_CHAIN`，不能声称正向 durable。该 slice 为 `structural_only`；visual/commercial=`NOT_PROVEN`，human/engine=`NOT_RUN`，stage/confirm/version/export=false。证据：`docs/evidence/mcp010f/fictional-energy-vfx-animated-socket-particles-v2-dual-candidate-source-gate-20260822.json`。

2026-08-22 Animated GLB socket transform projection source/runtime Gate：同 cohort `9cc7c11b8309ee7cc76df3d67794c4692c81ff0e8ef064248955ad511b8ca388` 的 Runtime-owned `game_weapon_animated_socket_transform_projection_prepare/get` 通过连续 5/5 replay；MechanicalAnimationClip direct-link readback、AnchorSet sorted Part inventory 与 GLB node-order set validation、真实 GLB node order projection、stable f32 shortest-decimal CAS 和排除 `created_at` 的 frame readback hash 均已固定。Runtime full lib **307/307**（0 failed，20 ignored）、binary **1/1**，合计 **308 pass**；Contracts **344**、MCP **79 read + 59 write = 138**。这是 structural-only、可重放/重启只读证据，不代表 animated-socket Particles/Trails/TrailsBloom 正向集成；视觉/商业 FPS=`NOT_PROVEN`、人评/引擎=`NOT_RUN`、`QUALITY_TARGET_NOT_MET` / `INCOMPLETE_TRUTH_BINDING` / camera `MISMATCH` 保持，stage/confirm/version/export 不推进。receipt：`docs/evidence/mcp010f/game-weapon-animated-glb-socket-transform-projection-source-gate-20260822.json`。

2026-08-21 `GAME-ENGINE-IMPORT-13` evidence-only Godot importer Gate：真实 Godot `4.7.2.stable.official.ed1daf0bf` headless import 聚合 4 个 GLB scene（LOD0/1/2/animated），LOD triangles `304 > 176 > 112`，每 scene 5 meshes、相同 materials、6 non-rendering sockets 的 parent/local TRS exact；animated 10 glTF channels→2 Godot semantic tracks，cross-loader t0/half-duration TRS exact，两个 named socket follow；5 个 `BoxShape3D` collision sidecar rows readback exact。static/animated 输入来自独立 Runtime source cohorts；无 Runtime durable Godot link。commercial engine=false；Unity/Unreal、physics/hitbox、visual/human、confirm/export `NOT_RUN/false`，quality `structural_only`。当前 **291 schemas / 69 read + 49 write = 118 tools**；总体 `QUALITY_TARGET_NOT_MET` / `INCOMPLETE_TRUTH_BINDING` / camera `MISMATCH` / `BLOCKED_REFERENCE_COVERAGE` 不变。

2026-08-21 Game weapon animated GLB socket materialization source/runtime/MCP Gate：`game_weapon_animated_glb_socket_prepare/get` 精确绑定 candidate/state、delivery LOD0、`MechanicalAnimationGlbReceipt@1` animated artifact/receipt 与 AnchorSet，在 derived animated LOD0 GLB 中物化六个稳定命名 empty socket nodes。源/派生 animation projection、animations/channels/samplers、source static/renderable projection 与 BIN bytes exact；Runtime-owned CAS 恰为两个（derived animated socket GLB + new materialization receipt），inline readback digest 不计为 CAS/FK。Three.js r185 `GLTFLoader` source/derived parse、10-track clip projection 与 half-duration 两个 socket parent-follow PASS。状态为 `PASS_SOURCE_RUNTIME_DURABLE_ANIMATED_GLTF_SOCKET_THREEJS_10_TRACKS_TWO_SOCKET_FOLLOW_WITH_ENGINE_AND_QUALITY_GAPS`；这是 structural-only。Unity/Unreal/Godot、runtime pivot、functional weapon semantics、人评、confirm/version/export 与 visual quality `NOT_RUN/false`，总体 `QUALITY_TARGET_NOT_MET` / `INCOMPLETE_TRUTH_BINDING` / camera `MISMATCH` / `BLOCKED_REFERENCE_COVERAGE` 保持。receipt：`docs/evidence/mcp010f/game-weapon-animated-glb-socket-materialization-source-gate-20260821.json`、`docs/evidence/mcp010f/threejs-game-weapon-animated-glb-socket-consumer-20260821.json`。

2026-08-21 Game weapon GLB socket materialization source/runtime Gate：固定对 LOD0/1/2 各派生一份 GLB，每份 exact six stable named empty nodes；owner Part/scene parenting 与 local TRS、源 renderable projection、BIN byte-exact、CAS/SQLite durable parent+children、幂等/冲突/回滚、restart source+derived reparse 均 PASS。Three.js r185 `GLTFLoader` 已真实解析三份派生 GLB，状态为 `PASS_SOURCE_RUNTIME_DURABLE_THREEJS_GLTFLOADER_THREE_LOD_SIX_NODE_WITH_RETAINED_COMMERCIAL_ENGINE_AND_QUALITY_GAPS`。这仍是 structural-only；Unity/Unreal/Godot、动画 socket、runtime pivot、视觉质量、人评、确认/版本/导出未运行或未通过。receipt：`docs/evidence/mcp010f/game-weapon-glb-socket-materialization-source-gate-20260821.json`。

2026-08-21 Fictional energy weapon typed TrailsBloom source/runtime Gate：精确绑定现有 typed trails、base frame、原 HDR Bloom、particle history、AnchorSet、camera/profile/cohort/node/owner/inventory 后，以固定 profile 输出独立 `trail-emissive-source` / `trail-bloom-contribution`；原 trail 三 pass、基础九 AOV、原 Bloom 与 particles 全部 byte-exact。Render Core/Worker、CAS/SQLite durable prepare/get、幂等、冲突、reservation/GC 与 Runtime restart PASS，状态为 `PASS_SOURCE_RUNTIME_DURABLE_SAME_COHORT_TYPED_TRAILS_BLOOM_WITH_RETAINED_FOREIGN_COHORT_AND_ENGINE_GAPS`。这是 RGBA8 HDR-profile-derived review evidence，不是 FP16；sidecar 不是 GLB socket，foreign cohort、Unity/Unreal/Godot、人评、确认/版本/导出与视觉质量未运行或未通过。receipt：`docs/evidence/mcp010f/fictional-energy-vfx-trails-bloom-source-gate-20260821.json`。

2026-08-21 Fictional energy weapon typed trails source/runtime Gate：1–4 个严格更早的 durable particle frames 与 current frame 生成两条固定 typed trails，独立输出 `trail-color` / `trail-id` / `trail-depth`；current base/Bloom 用于显示和 opaque depth，历史 frames 保留各自 tick 的 base/Bloom lineage，并强制同 candidate/artifact/camera/profile/cohort/AnchorSet/node/owner/policy。Render Core/Worker、同 cohort 隔离 Worker、CAS/SQLite durable prepare/get、非法字段与 future-history 零写入、幂等和 Runtime 重启均 PASS；基础九 AOV、Bloom 与 particle passes 前后 byte-exact。状态为 `PASS_SOURCE_RUNTIME_DURABLE_SAME_COHORT_TYPED_TRAILS_WITH_RETAINED_FOREIGN_COHORT_GAP`。V1 `bloom_input=false`，segments 共享 parent trail ID；sidecar 不是 GLB socket，foreign-cohort trails、Unity/Unreal/Godot、人评与视觉质量未运行或未通过。receipt：`docs/evidence/mcp010f/fictional-energy-vfx-typed-trails-source-gate-20260821.json`。

2026-08-20 Fictional energy weapon `bevel@2` source Gate：closed GeometryProgram/Worker grammar、active OperatorCatalog、direct `authoring-mesh@1`、单 stable source edge、closed convex triangle/quad、端点 valence=3、1–4 segment/profile/width/clamp、16/24-triangle strict GLB、solid、determinism 与 boundary/non-manifold/multi-edge/oversize/unknown executable field fail-closed 均 PASS。当前 195 schemas / 26/26 operators / 54+36=90 tools。该结果仅为 `PASS_SOURCE_STRUCTURAL_BEVEL_V2`；不进入 Modifier/Agentic/Repair，当前武器 candidate 未重编译，visual/PBR/human/package/live/export-restart/HQ360 均未推进。

2026-08-20 Fictional energy weapon `energy-core@1` source Gate：closed GeometryProgram/Worker component grammar、active OperatorCatalog、Runtime/MCP/Skill/Agentic/Modifier/Profile/PDK 全入口、四种 closed 同心轴组件、Part/source/material exact mapping、负/微小非零 inner radius 与关系/预算负门、strict GLB readback、lineage、determinism 与 Skill trust 均 PASS。该 slice 当时为 195 schemas / 25/25 operators / 54+36=90 tools。该结果仅为 `PASS_SOURCE_STRUCTURAL_ENERGY_CORE_V1`；当前武器 candidate 未重编译，visual/PBR/human/package/live/export-restart/HQ360 均未推进。

2026-08-20 Fictional energy weapon `vent-array@2` source Gate：闭合 GeometryProgram/Worker 参数、active OperatorCatalog、Runtime/MCP/Skill allowlist、connected/watertight 贯穿开槽面壳、前后 bevel 深度环、同一 Part 内 closed backing sub-solid、逐槽/component/312-triangle 精确预算、strict GLB readback、lineage、determinism、v1 regression、Skill bundle trust 均 PASS。该 slice 当时为 195 schemas / 23/23 operators / 54+36=90 tools。该结果仅为 `PASS_SOURCE_STRUCTURAL_VENT_ARRAY_V2`；backing 不是独立 semantic Part，当前武器 candidate 未重编译，visual/PBR/human/package/live/export-restart/HQ360 均未推进。

2026-08-20 Fictional energy weapon `panel@2` source Gate：闭合 GeometryProgram/Worker 参数、active OperatorCatalog、Runtime/MCP/Skill allowlist、实际 recessed floor/border/分段 bevel/双组 support-loop 几何、strict GLB readback、lineage、预算/关系/未知字段负门、Skill bundle trust 与 license/SBOM 均 PASS。该 slice 当时为 195 schemas / 22/22 operators / 54+36=90 tools。该结果仅为 `PASS_SOURCE_STRUCTURAL_PANEL_V2`；当前武器 candidate 未重编译、未渲染、未多视图比较，PBR/human/package/live/export-restart/HQ360 均未推进。

2026-08-20 Mechanical Animation Viewer discrete-frame source Gate：认证只读 Tauri bridge 对 immutable schedule 单 tick 调用既有 Runtime `mechanical_animation_clip_preview_get`，前端完整重验 candidate/artifact/clip/rest/action/cohort/tick、transient artifact 与双 Worker exact replay；GLB Part owner 必须 exact-one、identity、非嵌套，embedded animation、Bone/SkinnedMesh、未知/重复 owner 与异步旧响应均 fail closed。Viewer 只投影 Runtime delta，不本地求值、不连续播放、不写状态。状态为 `PASS_SOURCE_READ_ONLY_VERIFIED_DISCRETE_RIGID_FRAME`；package/live/VoiceOver/human/visual/HQ360 不变。

2026-08-19 Blender Authoring Mesh Edit Prepare source Gate：两个 closed contracts、显式 write opt-in/requiresConfirmation、preview exact 重算、current-head/scope、actual fixed Worker same-cohort、strict GLB/ArtifactReadback/Quality/Evidence、Store 原子 candidate/Job/event/audit/idempotency、精确 replay、key reuse/stale/foreign/forbidden-field/temporary-CAS rollback 负门 PASS。状态仅为 `PASS_SOURCE_STRUCTURAL_APPROVAL_GATED_STAGED_CANDIDATE`；不 confirm/version/export，不产生 package/live/visual/human/PBR/HQ360 PASS。

2026-08-19 Blender Authoring Mesh source Gate：active product-owned `authoring-mesh@1`、closed GeometryProgram parameter schema、explicit V/E/Loop/F stable opaque IDs、triangle/quad winding、open-boundary manifold validation、duplicate/dangling/unused/degenerate/non-manifold/unknown-executable rejection、deterministic fixed-Worker GLB、strict `ArtifactReadback@2`、Agentic/Skill/catalog cohort 和 Runtime/MCP raw stdio PASS。状态仅为 `PASS_SOURCE_STRUCTURAL_AUTHORING_MESH_OPERATOR_ONLY`；AuthoringTopology read model、translate/extrude、selection/history、arbitrary BMesh、Blender Python/plugin、package/live/visual/human/HQ360 未完成。receipt：`docs/evidence/mcp010f/blender-authoring-mesh-source-gate-20260819.json`。

历史数值口径：本段的 **291 schemas / 26/26 active operators / 69 read + 49 write = 118 tools** 以及 290/118、284/116、271/112、264/110、257/108、231/100、229/99、227/98、221/96、215/94、210/92、204/91、201/90、197/90、195/90、193/90、191/90、187/89、177/84、175/83、173/82、170/80、168/79、166/78、164/78、162/77、160/76 仅作 historical prior slice 保留；现行口径只认本文顶部与 Stage0 marker。

2026-08-20 Viewer Provenance Graph source Gate：Tauri-only 只读命令对 exact project/candidate/candidate-state/artifact 完整绑定，固定 64 nodes / 128 edges / 1 MiB，投影 Geometry/Operator DAG/Artifact/readback/quality 与可验证的 Visual/九 AOV/MechanicalAnimation 分支；跨候选、旧状态、错误 artifact、悬空/重复/循环边和异步旧响应均 fail closed。真实 fixed-renderer 失败 fixture 保留 `QUALITY_TARGET_NOT_MET` 与 `BLOCKED_REFERENCE_COVERAGE`，不升级 `quality_status=structural_only`。MCP 仍为 54 read + 36 write = 90；package/live/VoiceOver/human/PBR/export-restart/HQ360 仍为独立门。

2026-08-19 Blender Render Evidence Replay source Gate：两个 closed contracts、默认只读 MCP、exact candidate/integrity 绑定、current strict GLB readback、actual fixed Render Worker same-cohort 双重放、RenderProfile 一致、九 AOV persisted/first/repeat raw PNG bytes 与 decoded RGBA8 pixels exact、restart/deterministic/no-write/1-MiB Gate PASS。状态仅为 `PASS_SOURCE_SAME_COHORT_REPEAT_BYTE_EXACT_STRUCTURAL_REPLAY`；authored AppearanceProgram lineage、跨平台 determinism、Cycles/EEVEE/OCIO、package/live、visual/human/PBR/export/HQ360 仍未通过。

2026-08-19 historical Blender Mechanical Pose Geometry Preview source Gate：175-schema closed contracts、默认只读 `mechanical_pose_geometry_preview`、durable candidate cohort/rest/action exact binding、per-Part relative delta、纯 Part output ownership、transient fixed-Worker compile、strict GLB readback、determinism/no-write/duplicate-source/gimbal/1-MiB tests PASS。OperatorCatalog 仍 20/20，该 slice 当时工具为 49 read + 34 write = 83；现行总量为 54+36=90；状态仅为 `PASS_SOURCE_TRANSIENT_AUTHORED_RIG_GEOMETRY_PREVIEW`。caller-authored rest frame不证明原资产 rig/pivot，package/live/Viewer/visual/human/360与 Blender Armature/skin/animation parity 未通过。

2026-08-19 Blender Subdivision artifact-lineage sidecar source Gate：三个 closed contracts、Runtime-only immutable CAS sidecar、独立 SQLite candidate/node link、显式 opt-in prepare、默认只读 getter、exact request/evidence/artifact/readback/lineage hash binding、idempotency/restart/no-write/tamper/cross-candidate/1 MiB Gate PASS。Link 与 CAS reachable 在同一 SQLite transaction 提交，失败回滚仅作用于本次 created-new temporary unlinked sidecar；不把 filesystem CAS staging + SQLite 冒充跨介质原子事务，通用 GC 仍为 MCP011。状态仅为 `PASS_SOURCE_RUNTIME_OWNED_IMMUTABLE_CAS_SIDECAR`；candidate-local lineage仍无跨版本或 glTF V/E/C identity，不产生 package/live/visual/human/PBR/export/360 PASS。receipt：`docs/evidence/mcp010f/blender-subdivision-artifact-lineage-sidecar-source-gate-20260819.json`。

2026-08-19 Blender Subdivision artifact-lineage source Gate：closed request/projection、default read-only MCP、durable V2 evidence/strict readback、fixed-Worker full-GLB byte replay、唯一 direct source primitive、root quad → primitive-local triangle ranges、restart/no-write/stale/tamper/1 MiB focused Gate PASS。实际 artifact-bound 大样例为 14×14 level2、5,408 triangles、17,162 lineage elements；16×16 仅为 topology preview。状态仅为 `PASS_SOURCE_STRUCTURAL_RECONSTRUCTED_ARTIFACT_BINDING`；独立 CAS sidecar/durable link、glTF V/E/C ID、跨版本、package/live/visual/human/PBR/export/360 未完成。receipt：`docs/evidence/mcp010f/blender-subdivision-artifact-lineage-source-gate-20260819.json`。

2026-08-19 historical Blender Subdivision root-lineage source Gate（当时 168/79 cohort）：新增两个 closed contracts 与默认只读 `subdivision_topology_lineage_preview`。fixed Geometry Worker 复用真实 `subd-cage@2` evaluator 产生 control vertex/edge/quad root → 最终 evaluated quad topology 映射；Runtime 独立重验 program/catalog/node、V/E/Q/T 计数、coverage/range/crease chain、canonical、25,000-element 与完整 MCP wire 1 MiB。最大 16×16 level2 为 22,802 elements；预算不足整体拒绝。该 Gate 只证明 declared root-lineage preview 完整，不提供 corner/per-level child path/influence weights、artifact/readback/GLB identity、跨版本稳定性、package/live 或视觉质量；现行 artifact-bound projection 口径见本文顶部。

2026-08-19 historical Blender crease-aware Subdivision source Gate：该 slice 当时为 166-schema closed contracts、20/20 OperatorCatalog、product-owned `subd-cage@2` Worker、request-bound Runtime/MCP authoring projection、真实 `geometry_prepare → ArtifactReadback@2`、determinism/golden/negative/no-version/1-MiB tests PASS。状态仅为 bounded regular-grid integer-edge-crease structural capability；Modifier Stack、arbitrary topology、fractional/vertex crease、limit/adaptive/FVar UV、OpenSubdiv parity、package/live/render/visual/human/360 均未通过。receipt：`docs/evidence/mcp010f/blender-subdivision-crease-source-gate-20260819.json`。

2026-08-19 Blender Boolean Operand Lineage source Gate：164-schema closed contracts、默认只读 `boolean_operand_lineage_preview`、fixed Geometry Worker、Runtime 独立重算 operation/左右输入/source lineage、Runtime/MCP determinism/semantic-forgery/wire-budget/no-write PASS；OperatorCatalog 仍 19/19，无新 Skill/operator/dependency。状态仅为 evaluated-face operand structural lineage；原始 authoring face、GLB 持久 lineage、package/live/visual/human 均未证明。receipt：`docs/evidence/mcp010f/blender-boolean-operand-lineage-source-gate-20260819.json`。

Render Evidence Integrity 终审补强：reference/GLB 原始 CAS bytes、ArtifactReadback artifact ID、Quality/comparison 八项派生关系、封闭 threshold rows 与 JSON object 1 MiB 上限已进入 source Gate；不修复 historical attempt35，不推断视觉、人评或 package/live PASS。

2026-08-19 Blender Render Evidence Integrity source Gate：162-schema closed contracts、默认只读 `render_evidence_integrity_get`、Runtime restart/determinism/stale-camera negative、ArtifactReadback/camera/RenderSet/comparison/quality object/canonical lineage、九 AOV/mask exact bytes/PNG/color semantics、threshold gate 与 no-write PASS。OperatorCatalog 仍 19/19，工具为 44 read + 33 write = 77；无新 operator/Skill/Worker/dependency。状态仅为 current-source structural integrity；package/live/visual/human/PBR/export-restart/360 未升级。

2026-08-19 Blender Mechanical Pose Sequence Preview source Gate：160-schema closed contracts、Runtime candidate/artifact/readback/rest/action/policy exact binding、现有默认只读 `mechanical_pose_evaluate` oneOf branch、0/500/1000 ordered sample equivalence/determinism/semantic tamper/unsorted/over-budget/no-write tests PASS。OperatorCatalog 仍 19/19，工具仍 43 read + 33 write = 76；无新 tool/operator/Skill/Worker/dependency。结果固定 `structural_only` / `not-materialized`；Armature/skin/timeline/animation asset/package/live/visual/human/360 未实现或未运行。

2026-08-18 Blender Parametric Group v2 source Gate：158-schema closed contracts、Runtime immutable template/catalog producer、现有只读 `geometry_program_hash` branch、Geometry Worker program validation、三模板确定性、same-template/different-instance、semantic tamper、dynamic field/script/path/URL 负向与 candidate/version/CAS no-write tests PASS。OperatorCatalog 仍 19/19，工具仍 43 read + 33 write = 76；无新 tool/operator/Skill/dependency。结果固定 `structural_only`；package/live/visual/human/360 未升级。

2026-08-18 Blender Mechanical pose historical source Gate：该 slice 当时 156-schema closed contracts、Runtime candidate/artifact/readback/Part/source-node producer、authenticated read IPC、默认只读 `mechanical_pose_evaluate` 与 hierarchy/action/determinism/no-write/negative tests PASS。OperatorCatalog 仍 19/19；工具为 43 read + 33 write = 76。结果固定为 `not-materialized` / `not-run-runtime-read-only-projection`；Armature、skin、IK/NLA/F-Curve、Worker、Viewer time-line、package/live/visual/human/360 未实现或未运行。

2026-08-18 Blender bounded Subdivision evaluation v2 historical source Gate：该 slice 当时的 151-schema closed contract、Runtime policy/topology/budget producer、MCP 第五只读 oneOf branch 与 0/1/2-level exact-count/Worker-compile/determinism/hash/no-persistence/negative tests PASS。OperatorCatalog 仍 19/19，该 slice 当时工具为 42 read + 33 write = 75。该 Gate 仅覆盖 regular rectangular open quad cage；crease/adaptive/limit surface/face-varying UV/per-element lineage/OpenSubdiv/package/live/render/visual/human 均未实现或未运行。

2026-08-18 Blender Modifier evaluation v2 historical source Gate：该 slice 当时的 149-schema closed contract、Runtime producer/full output validator、MCP fourth read-only oneOf branch、initial/reuse/effective-change/disabled-trace/reorder/tamper/scope/hash/no-persistence tests PASS。`reusable` 只代表 canonical semantic decision；没有持久 mesh cache、真实 GLB/readback、Runtime/Worker build-cohort、package/live 或 visual/human Gate。OperatorCatalog 仍 19/19，该 slice 当时工具为 42 read + 33 write = 75。

2026-08-18 Blender Bevel/Normal historical source Gate：active `bevel@1` / `normal-policy@1` 已进入该 slice 当时的 146-schema closed GeometryProgram/Modifier Stack、19/19 OperatorCatalog、Worker/Runtime/MCP/Agentic set 与 first-party Skill。direct rotated box → 12-edge rounded topology → corner area×angle normal → UV/MikkTSpace → 300-triangle GLB strict readback 两次字节一致；非 direct source 与 overlap without clamp 负向 PASS。工具为 42 read + 33 write = 75；package/live/render-AOV/visual/human/360 未由本 Gate 升级。

2026-08-18 Blender TopologySnapshot historical source Gate：closed `TopologySnapshotRequest@1` / `TopologySnapshot@1`、Runtime producer/output validator/authenticated IPC、read-only `topology_snapshot_get` 与 primitive/SubD/Boolean/negative focused tests PASS。预算为单 Part 512 faces、1536 V/E/corner、1 MiB response，超限不截断；输出只声明 evaluated GLB triangle topology、artifact-bound IDs、partial operator-node lineage与 `structural_only`。该 slice 当时为 146 schemas、17 active operators、42 read + 33 opt-in write = 75 tools；当前 catalog 为上一段 19/19。package/live/visual/human/360 均未由本 Gate运行或升级。

2026-08-17 OperatorCatalog historical Gate：Geometry Worker 与 MCP raw stdio probe 已对该 cohort canonical catalog 做 exact-set 校验，16 entries 全部 `active`；同 cohort `906c8decb7aec90e8854bb0b7eb4d650dd3895851b556a62a1c8d16557aa17c8` 的 bounded Boolean union/difference/intersection、strict ArtifactReadback、9 semantic Parts/588 triangles、lineage 与负向 future-DAG/unsupported-shape 均 PASS。当前源码为 19 active source operators；旧 16、17 与 13/12 receipt 均作为 historical cohort，不被重写；该 Gate 不升级视觉/PBR、人评、export/restart、live restart 或 360。receipt：`docs/evidence/mcp010f/current-operator-catalog-reconciliation-20260817.json`。

2026-08-17 typed error classification Gate：MCP 对 Runtime 的 `GEOMETRY_PROGRAM_HASH_REJECTED` 保留非重试 typed code，避免 malformed DAG/Boolean 输入被错误转为 `RUNTIME_UNAVAILABLE`；IPC mapping regression PASS。该 Gate 只影响诊断/重试，不代表任何几何视觉或 likeness 通过。

2026-08-17 `FGC-MCP010F` orchestrator dispatch Gate：Runtime typed IPC 已覆盖 `design_stage_run_prepare`/`design_composition_prepare`，并以 Runtime regression test 保证这些方法不再落入 generic `INVALID_INPUT`；同 cohort `906c8decb7aec90e8854bb0b7eb4d650dd3895851b556a62a1c8d16557aa17c8` 的真实参考隔离 stage batch 通过 1 个 child ActionRun、`view_spec` 转发、Runtime parameter patch 与 replay hash equality，source candidate 未变、version=0。Gate 状态为 `PASS_ACTION_RUN_RUNTIME_AUTO_PARAMETER_PATCH_STAGE_BATCH`，但 child quality=`QUALITY_TARGET_NOT_MET`，故不能升级为视觉/PBR/人评/360 PASS；live restart、真实多视图与 export/restart 仍 `NOT_RUN/BLOCKED`。receipt：`docs/evidence/mcp010f/dev-app-install-orchestrator-dispatch-fix-20260817.json`、`docs/evidence/mcp010f/current-source-real-reference-orchestrator-dispatch-fix-20260817.json`。

2026-08-17 historical package-cohort raw manifest/OptimizationJob recheck：最终 Dev.app cohort `ce45110e3a5e6eaa5b5283e61f430e2338c7f06a2d09f4e75d4a21cb924f6a86` 的隔离 raw stdio probe 已验证 MCP/Runtime cohort exact-match、`tools/list` 与生成 source manifest exact-name equality（41 read + 33 opt-in write = 74，manifest canonical `7ced259e…c526b609`），并完成 39-evaluation `OptimizationJob`（32 coarse + 4 mid + 3 final），Job/result=`succeeded/done`、proposal=`blocked-no-improvement`、`strict_improvement=false`，未 confirm/version/export。receipt：`docs/evidence/mcp010f/optimization-raw-manifest-dynamic-20260817-receipt-v2.json`。该项只证明该历史 package cohort 的结构/IPC/预算边界，不升级真实 likeness、PBR、人评、export/restart、live restart 或 360。

2026-08-17 P2 typed surface-finish package：`hard-surface-finish-v1` 随当前源码安装为 Dev.app cohort `ce45110e3a5e6eaa5b5283e61f430e2338c7f06a2d09f4e75d4a21cb924f6a86`；同 cohort package/probe、Runtime Ready、Ponytail preflight、project readback 与无持久数据副作用 PASS。该项为 source/package/isolated transport，live Desktop restart、真实 finish likeness/PBR、人评、export/restart 与 HQ_360 仍 `NOT_RUN/BLOCKED`。receipts：`docs/evidence/mcp010f/dev-app-install-parametric-surface-finish-20260817.json`、`docs/evidence/mcp010f/dev-app-probe-parametric-surface-finish-20260817.json`。

2026-08-17 P2 camera/observation binding follow-up：RenderSet@2 相机 CAS 已绑定到 RenderSet 本体，RepairIntent 生产者/真实探针已绑定 durable observation；cohort `18c9fb86cafc7e7baf2356e2efe9db404e6530fdeae54d33c0a9beba94fbae40` 的真实授权参考隔离回归 PASS 到 render/compare/evaluate，随后因 `QUALITY_TARGET_NOT_MET` blocked。source candidate 未变，confirm/version/export 未运行。receipt：`docs/evidence/mcp010f/real-reference-repair-intent-run-observation-cas-20260817.json`。

2026-08-17 RepairIntent run source Gate：`repair_intent_run_prepare` 已纳入 33 个显式 write 工具，MCP/Runtime 验证 CAS `RepairIntent@1` 与 observation/evidence/reference/camera/candidate exact binding，并只生成 staged candidate；source/contract/full Gate 与最终 Dev.app packaged positive transport PASS，真实参考在 `CAMERA_EVIDENCE_UNAVAILABLE` 处 blocked；receipt：`docs/evidence/mcp010f/repair-intent-run-source-gate-20260817.json`。Repair apply/confirm、live restart、likeness/PBR、人评/export/360 仍未运行或阻断。

2026-08-17 ReferenceCanvas per-view lineage + package alignment Gate：view 级 `ReferenceViewSpec`、target/mask、camera canonical hash 与 cross-view supplied-kind exact coverage 已由 Runtime/MCP/source Gate 校验；每个 supplied kind 恰好对应一个 authored view card，duplicate/extra/missing kind 均有负向 fixture；最新 Dev.app cohort `820a542fabe3503f0b8cf6c86e84f578e7b074d79d07401a9a3f865dc60709b4` 的 install/probe PASS，receipt 为 `docs/evidence/mcp010f/reference-coverage-exact-binding-capability-package-20260817.json`。该 Gate 只证明 source→package structural/readback boundary，Codex Desktop restart、真实多视图、视觉/PBR、人评、export/restart、VoiceOver/360 仍 NOT_RUN/BLOCKED。

2026-08-17 capability-surface Gate：当 packaged MCP 携带 cohort 时，Runtime cohort 缺失或不一致会使 `tools/list` 与 `capabilities_get` 的写工具面 fail-closed，并标记 `BUILD_COHORT_MISMATCH`；matching cohort 才会恢复显式 write opt-in。最终 package cohort `820a542fabe3503f0b8cf6c86e84f578e7b074d79d07401a9a3f865dc60709b4` 的 source regression、MCP010F full Gate、Dev.app install/probe 均 PASS；隔离 mismatch transport 实测 `tools/list=41`、`write_count=0`、`mcp_write_tools_enabled=false`。当前旧 Codex Desktop session 仍未重启，live diagnostic 继续 BLOCKED；receipt：`docs/evidence/mcp010f/capability-surface-fail-closed-20260817.json`。

2026-08-17 explicit-authoring action boundary Gate：默认 session 的 unbound ReferenceCanvas/DesignSpec 被限制在 observation-only；显式 authoring action 在 Runtime 编译/比较前必须验证 session-bound target/mask、ViewSpec canonical 与 per-view camera。正向 bounded action、默认 fallback fail-closed negative regression、MCP010F full source Gate 与最新 package/probe PASS；不宣称 Repair 通用执行或视觉质量通过。

2026-08-17 Viewer/export boundary Gate：Viewer source checker 对前端与 Tauri 的 command allowlist、candidate/reference/render/quality exact binding、local snapshot no-write boundary 全部 PASS；显式 test-geometry-worker-fallback 下 Tauri Viewer tests 6/6 PASS。该 Gate 只证明只读 projection 与本地 compare 辅助的边界，不证明正式 packaged UI、artifact export、VoiceOver、人评、PBR、restart hash 或 360。

2026-08-17 typed pose-control Gate：`SilhouetteRig@1`/`SilhouetteRigHashRequest@1` 已支持 `rotation_x/y/z`（`radian`、±2π），Runtime 最终输出只写 `forgecad.geometry.transform@2`，并由 Primary Form typed action 暴露；源 Part、镜像拓扑和持久候选不被隐式改写。focused Runtime/MCP/Agentic tests、合同 checker、tool manifest 与 `script/test_mcp010f.sh` source Gate PASS。默认 36-control Rig 的 40-geometry budget 保持不变，姿态控制尚未进入默认自动搜索。该 Gate 不产生真实 likeness/HQ 证据，当前仍 `QUALITY_TARGET_NOT_MET`、历史 Stage 0 `camera=MISMATCH`/`BLOCKED_INCOMPLETE_BINDING`，PBR、人评、export/restart、360 仍未运行或阻断。

2026-08-16 Part namespace/pelvis bounded repair Gate：Part correction probe 已从四个旧硬编码 Part 扩展为 detail fixture 的显式 bounded namespace，解除 `pelvis` 的错误前置 blocker。current cohort `125df766029d3568c4a5724838ea342a18f29651566b625758cc6188d25059b6` 的 v2 receipt `docs/evidence/mcp010f/supplemental/part-correction-camera-binding-fix-pelvis-v2-20260816.json` 完成 Runtime-owned 64-evaluation fit、strict same-camera acceptance 与 staged readback；IoU `0.741047132807 → 0.745160673820`、Boundary F1 `0.328765122610 → 0.334696163978`，camera exact-match，`QUALITY_TARGET_NOT_MET`，未 confirm/version/export。旧 no-namespace receipt 保留为失败证据；该 Gate 不升级 likeness/HQ，也不替换 Stage 0 `BT006_CAMERA_BINDING=FAIL`。

2026-08-16 hip-pair bounded repair Gate：namespace 修复后 `hip-pair` 在同一 current cohort 完成 64-evaluation Runtime-owned fit 与 strict same-camera acceptance，IoU `0.741047132807 → 0.742876384452`、Boundary F1 `0.328765122610 → 0.330228088626`，camera exact-match，candidate staged、`QUALITY_TARGET_NOT_MET`、无 confirm/version/export。该独立 trial 只证明局部 Part correction transport，不产生 likeness/HQ PASS 或 candidate promotion。

2026-08-16 upper-arm-pair bounded repair Gate：同一 current cohort 完成 64-evaluation Runtime-owned fit 与 strict same-camera acceptance；IoU `0.741047132807 → 0.741042819691` 轻微回退、Boundary F1 `0.328765122610 → 0.330420176973`，camera exact-match，candidate staged、`QUALITY_TARGET_NOT_MET`，未晋升该 trial。该 Gate 只证明 Runtime/Worker/Render transport 与退化回退边界，不产生 likeness/HQ PASS。

2026-08-16 shoulder-armor-pair bounded repair Gate：同一 current cohort 完成 64-evaluation Runtime-owned fit 与 strict same-camera acceptance；IoU `0.741047132807 → 0.744097146194`、Boundary F1 `0.328765122610 → 0.338944948938`，camera exact-match，candidate staged、`QUALITY_TARGET_NOT_MET`，未 confirm/version/export。该 Gate 只证明局部 Part correction transport，不产生 likeness/HQ PASS。

2026-08-16 Primary Form canonical-camera acceptance Gate：源码 cohort `125df766029d3568c4a5724838ea342a18f29651566b625758cc6188d25059b6` 的隔离 `chest-shell` repair 已证明最终 `PrimaryFormAcceptance@1`、VisualEvidence 与 comparison 统一使用 canonical observation camera `2cd35f435839f80e364a15a6930ffe37c2145925d6a5b455ccffd30873953736`；探索性 fit camera `04e27c1b…58ad0a` 不再泄漏到最终比较。receipt `docs/evidence/mcp010f/supplemental/part-correction-camera-binding-fix-20260816.json`：64 evaluations、strict accepted、camera exact-match，IoU `0.748146396048`、Boundary F1 `0.350344578024`、`QUALITY_TARGET_NOT_MET`，staged candidate、无 confirm/version/export。此为新的 source/transport boundary PASS，不替换 Stage 0 历史 `BT006_CAMERA_BINDING=FAIL`/`MISMATCH`；full Codex replay、正式 UI/VoiceOver、人评、PBR/export-restart/360 仍未运行或阻断。

2026-08-16 latest current-source real Codex ReferenceCanvas/Observation/Primary Form Gate：MCP/Runtime/Geometry Worker/Render Worker 四个可执行文件 cohort exact-match 为 `d52261ffdf44e07a2a4762b16c80f433a824080f0530fad64bbd32a153eb9c04`。真实用户授权 PNG 已完成 durable `ReferenceCanvas@1`/`DesignSpec@1` readback、candidate-bound `AgenticSceneObserveResult@1`、Rig hash 与 observation hash exact-match；receipt `docs/evidence/mcp010f/supplemental/real-codex-cli-current-20260816-primary-form-multiview-capable-v1.json` 为 `PASS_BOUNDARY_ONLY`。同一 lineage 的 `pelvis → upper-arm-pair` 两步 Primary Form repair 均 `accepted + strict_improvement`，各 64 evaluations，proposal loss `0.463981471487 → 0.460480624038 → 0.458125428229`；最终 IoU `0.752731669182`、Boundary F1 `0.355021780326`、bbox `0.00390625`、centroid `0.001753436389`。该 Gate 证明当前源码的真实 ReferenceCanvas/Observation transport 与 Runtime-owned bounded Primary Form continuation 已落地，不证明 likeness/HQ PASS：`silhouette_gate=QUALITY_TARGET_NOT_MET`、`quality_visual_status=NOT_RUN`、无 confirm/version/export、AOV/review/PBR/人评未运行，且只有 perspective，`front/back/left/right/rear-three-quarter` 缺失，360 为 `BLOCKED_REFERENCE_COVERAGE`。

2026-08-16 current-source supplemental Primary Form convergence Gate：同 cohort 重试 receipt `docs/evidence/mcp010f/supplemental/real-codex-cli-current-20260816-primary-form-chest-shoulder-v3-retry.json` 完成 `chest-shell → shoulder-armor-pair` 两步 async repair，2/2 `accepted + strict_improvement`，每步 64 evaluations；Geometry/Render Worker、9 AOV、candidate-bound typed review/quality transport 均实际完成。相对上述 boundary-only receipt，最终 IoU 从 `0.752731669182` 提升到 `0.754862679391`，Boundary F1 从 `0.355021780326` 提升到 `0.369927879976`；bbox 保持 `0.00390625`，centroid 为 `0.002345583377`。该 Gate 仍为 `PASS_WITH_QUALITY_TARGET_NOT_MET`，`hard_gate=false`、review=`needs_revision`、visual claim 未声明、无 confirm/version/export；单视图 coverage 仍 blocked，不升级 likeness/HQ/360。

下方旧 cohort 条目保留为历史快照；最新 current-source 状态以上述 `d52261ff…` receipt 为准。

2026-08-16 Agentic authoring-context Gate：真实 Codex visual probe 的 source orchestration 已固定 `ReferenceCanvas@1`/`DesignSpec@1`、Runtime CAS producer、单次 durable session readback 与 canonical observation hash equality；Agentic 11-schema、ForgeCAD 125-schema、Visual Surface contract、Python syntax 与 `script/test_mcp010f.sh` 通过。live boundary-only probe 未返回 typed authoring result：read-only 调试在 `project_create` 被取消，workspace-write 隔离 host 后续超时并清理，故 transport 记为 `BLOCKED/NOT_RUN`，不替换 Stage 0 `QUALITY_TARGET_NOT_MET`、`camera=MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`，也不升级 PBR、人评、export/restart 或 360。

2026-08-16 unified real-reference CADFit/Surface/Manifold Gate：同一隔离 cohort 的真实机器人参考完成 39 次 `128→256→512` 多保真 `OptimizationJob`，camera producer round-trip 与 optimization/compare camera hash exact-match；`surface-shell@1` 多个 `surface_control_point` 已实际材料化，Manifold C ABI/FFI Boolean lane `[1..9]` 与 residual evidence 绑定通过。current-quality ledger 已校验 receipt `docs/evidence/mcp010f/supplemental/optimization-job-real-reference-20260816-unified-objective-manifold-surface.json`；`status=PASS` 仅代表结构/执行/证据绑定，视觉仍 `QUALITY_TARGET_NOT_MET`（IoU `0.756284113105`、Boundary F1 `0.331999375494`），proposal `blocked-no-improvement`，无 confirm/version/export。该 Gate 不升级 likeness/HQ、PBR、人评、export/restart 或 360，也不覆盖 Stage 0 历史 `camera=MISMATCH`/`BLOCKED_INCOMPLETE_BINDING`。

2026-08-16 Viewer current-cohort read-model Gate：同 cohort Viewer 对隔离 Runtime 实际读回 `ForgeCADViewerReadModel@1`，candidate/artifact/reference/RenderSet/comparison/QualityReport exact-bound，1 project/1 candidate/0 version；Runtime `QUALITY_TARGET_NOT_MET`、hard gate=false，Viewer read-only/no persistent side effect。receipt `docs/evidence/mcp010f/supplemental/viewer-read-model-real-reference-20260816.json` 已由 current-quality ledger 校验；formal packaged UI E2E、VoiceOver、人评、export/restart、360 仍未运行或阻断。

2026-08-16 Manifold Boolean structural Gate：当前 Geometry Worker 通过真实 vendored Manifold C ABI/FFI 执行 bounded same-Part `union`、`difference`、`intersection`；MCP010D raw receipt 记录 catalog `16 entries / 16 active`、9 semantic Parts、588 triangles，curved-mesh lineage 与 residual tangent focused tests 通过。该 Gate 不等于任意 mesh Boolean 或视觉/PBR likeness、export/restart、真人、360 PASS；历史 deferred receipt 保留为历史快照。

2026-08-16 CADFit/Render Worker multi-fidelity Gate：真实认证 Runtime IPC 已分发 `optimization_job_prepare/get/resume`，Render Worker fit batch 已支持 `256px` mid stage；隔离 raw receipt `docs/evidence/mcp010f/supplemental/optimization-job-raw-20260816-mid-resolution.json` 通过 `32 coarse + 4 mid + 3 final(control included)=39` evaluations、逐步 checkpoint、final best-so-far 与 fail-closed `blocked-no-improvement` proposal boundary。该 Gate 是合成 reference 的 source/transport/预算/readback PASS，不是机器人 likeness、高质量、PBR、人评、export/restart 或 360 PASS；Stage 0 `QUALITY_TARGET_NOT_MET`、`camera=MISMATCH`、`BLOCKED_INCOMPLETE_BINDING` 保持不变。

2026-08-15 Primary Form 512px fit/formal renderer alignment Gate：提交 `220d6f0a` 后，Render Core 的 512px transient fit 与 formal 512px compare 共用 1024 sample grid，128px exploratory path 保持半分辨率；Geometry Worker exact silhouette/part-id PNG regression、MCP010F full Gate、source/package install 通过。same-source Dev.app cohort 为 `3e6debd…c53b34`，安装 receipt 为 `docs/evidence/mcp010f/supplemental/primary-form-fit-raster-alignment-dev-app-install-20260815.json`；真实 receipt 为 `docs/evidence/mcp010f/supplemental/real-codex-cli-current-20260815-primary-form-fit-raster-alignment.json`。该 receipt 的 Primary Form fit IoU/F1 为 `0.752731669182`/`0.355021780326`，顶层 final compare IoU/F1 为 `0.745042364012`/`0.334537343601`；差异由 `silhouette_fit_camera_hash` 与最终 `silhouette_camera_hash` 不同且 `canonical_compare_in_silhouette=false` 解释，不能写成 likeness 或 HQ PASS。当前仍 `QUALITY_TARGET_NOT_MET`、`camera=MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`，hard gate false；PBR、人评、export/restart 与 360 仍未运行或阻断，live MCP 仍需重绑。

2026-08-15 Primary Form canonical ReferenceViewSpec binding Gate：`silhouette_fit_prepare` 与 `primary_form_repair_prepare` 可接收 hash-bound `ReferenceViewSpec@1`，Runtime 校验目标 reference/project scope，并把同一 landmarks/regions 投影同时用于 bounded transient ranking 与最终 same-camera staged compare；MCP canonicalizer 保留 image width/height 的整数契约并恢复其余连续数值后再校验 canonical hash。提交 `1788f242` 后 clean Dev.app 安装与真实 Codex receipt 均通过同 cohort `4c12eac9…edd5b`；receipt `docs/evidence/mcp010f/supplemental/real-codex-cli-current-20260815-primary-form-view-spec.json` 记录 15 landmarks、8 regions、`view_spec_sha256=79bbb0d8…8b65`，两步 bounded repair 均 strict accepted，最终 compare IoU `0.745151573065`、Boundary F1 `0.334457989922`，仍 `QUALITY_TARGET_NOT_MET`。该 Gate 修复观察与搜索目标不一致，不产生 likeness/PBR/human/export-restart/360 PASS；Stage 0 benchmark 的 `camera=MISMATCH`、`BLOCKED_INCOMPLETE_BINDING` 保持不变。

2026-08-15 Primary Form serial composition real Gate：在同 cohort `78d03f2b…1a808` 的 Dev.app direct MCP 隔离运行中，真实用户授权 PNG 完成 `pelvis → upper-arm-pair` 两步 serial `primary_form_repair_prepare`。Runtime/Render Worker 各步均执行 64 次 bounded evaluation，source→staged candidate 均为 `accepted + strict_improvement`，生成 `ForgeCADPrimaryFormCompositionLineage@1`（2 steps、2 accepted、最终 candidate `candidate-65de4eba7341405fa448dafb24dbca84`）。最终 `silhouette_iou=0.751411042945`、`boundary_f1_4px=0.34965309767`、`bbox_edge_error=0.00390625`、`centroid_error=0.001862755047`，仍 `QUALITY_TARGET_NOT_MET`、`hard_gate=false`、`visual_review=needs_revision`；九 AOV/typed review transport 完成，PBR 为 `NOT_RUN`、人评为 `NOT_RUN`、360 为 `BLOCKED_REFERENCE_COVERAGE`。receipt 为 `docs/evidence/mcp010f/supplemental/real-codex-cli-current-20260815-primary-form-sequence-pelvis-upper-arm.json`。该 Gate 证明多 Part candidate/observation/target/camera/Rig/intent lineage 与 Runtime-owned search 已真实落地，不证明 likeness/high-quality，也不替换 Stage 0 benchmark；前一轮重试压缩误判保留在 `...-blocked-retry-gate.json`。

2026-08-15 real Primary Form/Render Worker Gate：在同 cohort `78d03f2b…1a808` 的 Dev.app direct MCP 隔离运行中，真实用户授权 PNG 完成 detail + silhouette-first + Runtime-owned Primary Form repair；64 bounded evaluations、26 Parts、4704 triangles、九 AOV 与 candidate-bound review/quality transport 均完成，source→staged candidate 严格改善。最终 `boundary_f1_4px=0.414236535223`、`silhouette_iou=0.74637831219`、`bbox_edge_error=0.03515625`、`centroid_error=0.007345230528`，仍 `QUALITY_TARGET_NOT_MET`、`hard_gate=false`、`visual_review=needs_revision`。receipt `docs/evidence/mcp010f/supplemental/real-codex-cli-current-20260815-primary-form-runtime-replay.json`；这证明连续参数搜索已由 Runtime/Render Worker 承担并真实落地，不证明 likeness/PBR、人评、confirm/export 或 360，通过后仍需独立质量门。

2026-08-15 Agentic durable design-document readback Gate：`AgenticSessionResult@1` 的 create/resume/get/lookup 均从 Runtime-owned CAS hash 读回 `ReferenceCanvas@1` 与 `DesignSpec@1`，canonical/schema/project/reference/canvas binding exact-match；missing/drifted object fail closed。focused session/action regression 与 source Gate 通过，receipt 为 `docs/evidence/mcp010f/agentic-design-documents-readback-20260815.json`。这是观察上下文完整性 Gate，不产生 likeness/PBR/human/export-restart/360 PASS，当前仍 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`。

2026-08-15 clean package Gate：提交 `cf329215` 后按干净源码重建的 Dev.app，Runtime/MCP/Geometry Worker/Render Worker cohort `aa0d6dcb…5808e` exact-match，`source_worktree_dirty=false`；ad-hoc deep-strict、隔离 Ready/project/preflight 与 packaged MCP010B raw stdio structural Gate PASS。该 Gate 不代表 live MCP 已重绑，也不产生 likeness/PBR/human/export-restart/360 PASS；当前仍 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`。

2026-08-15 current-source packaged structural Gate：`a153dc02` 同源码 Dev.app 的 Runtime/MCP/Geometry Worker/Render Worker cohort `04bbb4d6…ce81f3` exact-match；ad-hoc deep-strict、isolated Ready/project/preflight 与 MCP010B raw stdio PASS，checked-in Skill registry 12 项、工具面 37 read + 24 opt-in write = 61 且 manifest hash exact-match，GeometryProgram@2 strict readback/candidate binding 和 Part sink negative Gate 均通过。raw probe 已修正 preflight-first 顺序与过期的固定 11-skill 假设。该 Gate 只证明 packaged/source transport 和结构契约；当前 Codex live MCP 仍旧 cohort `7f9e4c…ee518`、需重启，未产生新的 likeness/PBR/human/export-restart/360 证据，质量仍 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`。

2026-08-15 Primary Form adaptive local line-search Gate：Runtime 在固定 `40 geometry + 15 initial camera + 9 winner refit` 上限内，针对 36-control Rig 的 joint failure schedule 改为 `joint + 0.5 whole-proposal backtrack + 0.5/1.0 dominant-Part coupled recovery + 36/36 scalar coverage`；小 Rig 动态保留 `0.5/0.25` backtrack 与单 coupled recovery。focused Runtime `22 passed`，证据 `docs/evidence/mcp010f/primary-form-adaptive-line-search-20260815.json`。这是 source/convergence Gate，未产生新的真实 likeness、PBR、人评、export/restart 或 360 证据；当前仍 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`。

2026-08-15 Primary Form recovery coverage Gate：joint proposal 失败时，Runtime 只执行一个 boundary-dominant Part 的 coupled recovery，再保证 36-control detail Rig 的 `36/36` scalar probe coverage；总预算仍固定为 `40 geometry + 15 initial camera + 9 winner refit`。focused Runtime/Agentic tests 与 `script/test_mcp010f.sh` full Gate PASS，证据为 `docs/evidence/mcp010f/primary-form-recovery-coverage-20260815.json`。该 Gate 只证明 Runtime-owned bounded convergence boundary，未产生新的 likeness/PBR/human/export-restart/360 证据；package/live MCP 尚未重建到本源码，当前仍 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`。

2026-08-15 live/package cohort Gate：`abae43f3` 同 revision 的 Dev.app 已安装，MCP/Runtime/Geometry Worker/Render Worker cohort `5a1f108a…e2dd2f` exact-match；deep-strict package、resource allowlist、隔离 Runtime Ready/project/preflight probe PASS，未触碰持久用户数据。新包工具面为 37 read + 24 opt-in write。当前 Codex MCP 会话仍返回旧 `7f9e4c…ee518`/旧 `05fca3…d4d0a` surface，故 `live_session_rebind=BLOCKED_RESTART_REQUIRED`，不能把 package PASS 误写成 live visual authoring PASS；无新 likeness receipt，当前仍 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`。

2026-08-15 Primary Form resolution-consistent fit Gate：`silhouette_fit_prepare` 的 Primary Form camera/geometry batch 统一为 512×512 isolated Render Worker，与 same-camera acceptance 共享分辨率；非 Primary Form camera 粗搜继续使用 128×128。源码/Runtime focused regression、MCP010F 聚合门与 product workspace check 已通过；该模块不产生 likeness/PBR/human/export-restart/360 PASS，当前仍 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`。

版本：2026-08-13
状态：当前唯一能力与阻断总表；ADR-0026 Agentic Design Runtime 与废弃隔离规则已纳入目标/治理能力，不改变 MCP010F 质量状态

2026-08-15 Viewer Agentic evidence binding Gate：`visualEvidenceBound=true` 时，Viewer Agentic normalizer 对 artifact/reference/render-set/comparison/QualityReport 五个 hash 采用 exact-match；缺失或漂移直接为 unavailable，不显示 Runtime quality gate 的 ready 状态。无视觉绑定时仍保持结构 projection 的 unknown 语义。Viewer source Gate、Node exact-hash regression、TypeScript/build 与 MCP010F full Gate 通过；该 Gate 不产生 likeness/PBR/human/export-restart/360 PASS，当前仍 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`。

2026-08-15 Render Worker fail-closed test boundary Gate：Runtime `render-core` fallback 已从 `cfg(test)` 隐式路径改为独立 `test-render-worker-fallback`；无 feature 的 product `cargo check` 通过，source ownership checker 与显式 fallback Runtime `122 passed / 0 failed / 12 ignored`、MCP `56 passed / 0 failed`、MCP010C/F full Gate 通过。该 Gate 证明测试不会在 Worker 缺失时伪造 Render Worker 证据；它不产生 likeness/PBR/human/export-restart/360 PASS，当前仍 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`。

2026-08-15 Agentic canonical observation MCP transport Gate：`InProcess` adapter 已把 stage/critic/visual-evidence follow-up 统一转发到 Runtime bound projection，并严格校验 `observation_sha256`；stale/missing observation 回归通过，MCP 全量 `56 passed / 0 failed`。该 Gate 只证明 transport lineage 一致，不产生真实 likeness、人评、PBR、export/restart 或 360 证据；质量仍 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`。

2026-08-15 Primary Form output-level offset sink Gate：Runtime 对 mirror/array Part 的 camera-plane offset/scale 在完整 typed output graph 后追加并复用 `forgecad.geometry.transform@2`，source geometry 不再承载会被 mirror 反向复制的整体位移。新增 mirror/direct-output materialization regressions 与 Stage 0 source-truth freshness 通过；无新的真实 likeness、人评、PBR、export/restart 或 360 证据，仍为 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`。

2026-08-15 Primary Form exact evaluation ledger Gate：Runtime 将 64 上限的 phase split 对齐到实际 schedule：`40 geometry + 15 initial camera + 9 winner refit`；同一 source/proposal Render Worker batch 的每个 refit row 都计入执行账本，即使 strict same-camera gate 保留 source。focused budget regression、Primary Form fixture 与 Stage 0 source-truth freshness 通过；这只修复 Runtime-owned bounded search 的预算/证据一致性，不产生真实 likeness、人评、PBR、export/restart 或 360 证据，仍为 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`INCOMPLETE_TRUTH_BINDING`/`BLOCKED_INCOMPLETE_BINDING`。

2026-08-15 Primary Form 单 Part Runtime-owner Gate：`scripts/probe_mcp010f_part_correction.py` 现在只执行 authored baseline、Runtime camera/observation/Rig hash 和一次 `primary_form_repair_prepare`；连续搜索不再由 Codex/Python 负责。`part_contour_rig_draft` 的 offset 控制使用 meter 单位，Runtime 返回的 `PrimaryFormRepairPrepareResult@1` 是唯一 staged/no-improvement authority；脚本不再生成 fraction candidates、patch GeometryProgram、调用 `silhouette_candidate_compare` 或做本地非回归接受。MCP010F full Gate、contracts、Viewer source、desktop build 和 focused Runtime tests PASS；无新的真实参考/likeness receipt，故仍为 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`INCOMPLETE_TRUTH_BINDING`、`BLOCKED_INCOMPLETE_BINDING`，人评/PBR/export-restart/360 未运行或阻断。

2026-08-15 Primary Form profile-loft height sink Gate：Runtime 将 `height` proposal 映射到 `forgecad.geometry.profile-loft@1` 的 `profiles[*].height_m` station span，保持首站锚点并不修改 profile point depth；`profile-extrude` 既有行为回归通过。7 个 materialization tests、Runtime `122 passed / 0 failed / 12 ignored`、MCP010F full Gate 与 Stage 0 source-truth checker PASS。该 Gate 只修复 typed geometry sink，不产生新的真实 likeness/quality receipt；`QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`、人评/PBR/export-restart/360 状态保持不变。

2026-08-15 Durable Agentic lineage Gate：session/checkpoint/action 的 observation claims 现在必须同时匹配 current candidate/reference 的 ID、candidate canonical hash、reference object hash 与 reference canonical hash；缺失或漂移返回 fail-closed binding error。exact-lineage unit、Agentic projection 与 bounded action regressions PASS；不新增 Schema/tool/CAS，不产生 Repair/confirm/version/export 或视觉质量结论，仍为 `QUALITY_TARGET_NOT_MET` / `MISMATCH` / `BLOCKED_INCOMPLETE_BINDING`。

2026-08-15 Agentic observation cache Gate：Runtime 进程内按 canonical observation hash 缓存完整只读 projection；bound plan/critic/evidence/action 在同一 Runtime 会话消费原对象，缓存丢失时执行重建 + hash/scope fail-closed 校验。focused Runtime cache/ambiguity 与 bounded action tests PASS；无新增 Schema、tool、CAS 用户数据或视觉质量结论，仍为 `QUALITY_TARGET_NOT_MET` / `MISMATCH` / `BLOCKED_INCOMPLETE_BINDING`。

2026-08-15 Primary Form composition-lineage Gate：CLI 的 `ForgeCADPrimaryFormCompositionLineage@1` 对 2–3 个 serial repair steps 做 candidate/observation/target/camera/Rig/intent hash binding，第二步起在 candidate advancement 前实际消费已校验的 prefix projection，并只接受 Runtime `prepared + strict_improvement`；source drift、target drift、hash 缺失和错误状态均 fail closed。该 Gate 是 orchestration projection/source Gate，不是 Runtime/CAS durable write、视觉 likeness 或高质量 PASS；当前仍 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING`，真实 composition 因授权 PNG 缺失 `NOT_RUN/BLOCKED_REFERENCE_BYTES_UNAVAILABLE`。

2026-08-15 Primary Form bilateral landmark-mask Gate：Runtime 的 landmark evidence decoder 现在按固定 `rig_part_matches_observed_part` alias 将 Worker `knee-left`/`knee-right` 等 concrete Part-ID 合并到 `knee-pair` 语义 mask；双侧 anchor 覆盖与 NME 不再静默退回 whole-body fallback。新增 Runtime regression；该 Gate 只修复 Codex observation 的 Part-owned projection，不产生真实 likeness、人评、PBR、export/restart 或 360 证据，当前仍为 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING`。

2026-08-15 Primary Form bilateral local-envelope Gate：Runtime 将 target 与 Render Worker 的 concrete `*-left`/`*-right` envelope 通过固定 alias 合并到 authoring Rig 的 `*-pair` 控制，再计算局部 width/height/offset proposal；此前 exact lookup 会让 bilateral Part 退回 whole-body bbox。新增 merge regression 与 Primary Form focused tests 通过；该 Gate 只修复收敛输入，不产生新的真实 likeness receipt，`QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING` 与人评/PBR/export-restart/360 状态保持不变。

2026-08-15 Primary Form bilateral boundary ranking Gate：Runtime 将 Render Worker 的 concrete `*-left`/`*-right` Part-ID 通过固定 alias 聚合到 authoring Rig 的 `*-pair` 控制，再按 candidate-bound boundary score 排序 bounded probes；此前 exact-string lookup 会使 bilateral control 丢失边界优先级。新增 regression、Primary Form 相关 21 个 Runtime tests 与 MCP010F full Gate 通过；该 Gate 只修复收敛排序输入，不产生新的真实 likeness receipt，`QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING` 与人评/PBR/export-restart/360 状态保持不变。

2026-08-15 Stage 0 Runtime source snapshot synchronization：`current-benchmark-truth.json` 的 Runtime visible-view policy hash 已同步到当前已提交源码，`check_mcp010f_stage0_truth.py` 通过。同步只修复 source-truth freshness，不改变 8 项 visible-view thresholds、`camera=MISMATCH`、`QUALITY_TARGET_NOT_MET`、`BLOCKED_INCOMPLETE_BINDING` 或 Viewer Runtime-only quality authority；真实组合仍因授权 PNG 字节不可用而 `NOT_RUN/BLOCKED_REFERENCE_BYTES_UNAVAILABLE`。

2026-08-15 Primary Form hip-pair bounded candidate Gate：r21 在 cohort `726153a3…42ab5c` 完成 60 次 Runtime-owned fit，source/proposal loss `0.426350916959`/`0.426265642648` 严格改善，生成 staged `candidate-2dc5728da7124288b6ff79a0fb6b4f00`，camera `PASS_SILHOUETTE_FIT_TO_COMPARE`、九 AOV/readback 与 no-persistent-side-effect 通过。全局 compare 仍为 IoU `0.743473474034`、Boundary F1 `0.301145366407`、bbox `0.0234375`、centroid `0.021831234764`、`QUALITY_TARGET_NOT_MET`、hard gate false；未 confirm/version/export。该 Gate 与 r19/r20 是相互独立的 bounded candidate/transport PASS_WITH_QUALITY_TARGET_NOT_MET，不是 likeness/high-quality PASS，也不替换 Stage 0 provisional truth。

2026-08-15 Primary Form chest-shell bounded candidate Gate：r20 在 cohort `726153a3…42ab5c` 完成 61 次 Runtime-owned fit，source/proposal loss `0.426350916959`/`0.416406210214` 严格改善，生成 staged `candidate-d9d8cb24b1c5475d85b773c3ceb032c7`，camera `PASS_SILHOUETTE_FIT_TO_COMPARE`、九 AOV/readback 与 no-persistent-side-effect 通过。全局 compare 仍为 IoU `0.739507057324`、Boundary F1 `0.314765400977`、bbox `0.013671875`、centroid `0.020515796935`、`QUALITY_TARGET_NOT_MET`、hard gate false；未 confirm/version/export。该 Gate 为 bounded candidate/transport PASS_WITH_QUALITY_TARGET_NOT_MET，不是 likeness/high-quality PASS，也不替换 Stage 0 provisional truth。

2026-08-15 Primary Form upper-arm-right bounded candidate Gate：r19 在 cohort `726153a3…42ab5c` 完成 63 次 Runtime-owned fit，source/proposal loss `0.426350916959`/`0.423630597587` 严格改善，生成 staged `candidate-f1123cb13b624cb69493f825398b4423`，camera `PASS_SILHOUETTE_FIT_TO_COMPARE`、九 AOV/readback 与 no-persistent-side-effect 通过。全局 compare 仍为 IoU `0.744938326895`、Boundary F1 `0.307109823312`、bbox `0.021484375`、centroid `0.021832396893`、`QUALITY_TARGET_NOT_MET`、hard gate false；未 confirm/version/export。该 Gate 为 bounded candidate/transport PASS_WITH_QUALITY_TARGET_NOT_MET，不是 likeness/high-quality PASS，也不替换 Stage 0 provisional truth。

2026-08-15 Primary Form pelvis trial / camera-handoff Gate：r16 因瞬时 `reference_get` IPC 失败而 `BLOCKED`，r17 保留了 probe 对 `no_improvement` 未采用 proposal camera 的错误期望；修复后 r18 `docs/evidence/mcp010f/supplemental/real-codex-cli-current-20260815-canonical-observation-pelvis-r18.json` 通过 `PASS_CAMERA_FIT_TO_COMPARE`，canonical observation 返回 26 inferred Parts、`boundary_error_get=0`。pelvis 55 次 Runtime-owned bounded fit 未改善，source/proposal loss `0.435730135362`/`0.438891599478`，`retained_source`/`no_improvement`，最终 `QUALITY_TARGET_NOT_MET`，无 candidate/version/confirm/export。该 Gate 是 CLI/Runtime camera lineage 与 strict retention PASS_WITH_QUALITY_TARGET_NOT_MET，不是 likeness/high-quality PASS，也不替换 Stage 0 provisional truth。

2026-08-15 Canonical observation + inferred Part attribution Gate：Runtime 对 automatic target 的空 `parts` 生成 candidate-bound `SilhouettePartErrorResult@1`，并由同一 baseline compare 后的 `AgenticSceneObserveResult@1` 携带完整 26-Part table；真实 r15 supplemental `docs/evidence/mcp010f/supplemental/real-codex-cli-current-20260815-canonical-part-observation-r15.json` 使用 cohort `726153a3…42ab5c`，`boundary_error.source=canonical_observation`、`boundary_error_get=0`，推荐 Part 可供下一次单 Part bounded repair。该 Gate 为 Runtime/CLI observation consolidation PASS_WITH_QUALITY_TARGET_NOT_MET；最终 IoU `0.739507057324`、Boundary F1 `0.314765400977`、hard gate false，未 confirm/version/export，不替代 likeness、人评、PBR、export/restart、360 或 Stage 0 provisional truth。

2026-08-15 Primary Form right-shoulder bounded candidate trial Gate：current packaged cohort `26354e2f…a2029` 的真实 r11 supplemental receipt 对 `shoulder-armor-right` 完成 Runtime-owned exact Part bounded fit（63 evaluations）与同 camera source/proposal acceptance，loss `0.426350916959`→`0.409541676181`，生成 staged candidate。全局视觉门仍失败：IoU `0.749072115206`、Boundary F1 `0.326405552646`、`QUALITY_TARGET_NOT_MET`、`quality_hard_gate_passed=false`、`visual_review=needs_revision`；没有 confirm/version/export，未改变 Stage 0 provisional benchmark truth。该 Gate 为 current-cohort transport + bounded acceptance PASS_WITH_QUALITY_TARGET_NOT_MET，不是 likeness/high-quality PASS。

2026-08-15 Primary Form exact-side sink alias Gate：Runtime `materialize_rig_geometry_program` 仅按固定 `*-armor-left/right ↔ *-left/right` 规则做 side-aware projection；exact `shoulder-armor-left` 不会因输出 sink 使用 `shoulder-left` 而零应用或污染右侧。alias/materialization、bilateral 与 exact-side scope regressions 通过；该 source Gate 不产生新真实 likeness、人评、PBR、export/restart 或 360 证据，`QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING` 保持不变。

2026-08-15 Primary Form bounded single-Part candidate trial Gate：Runtime 在既有 `primary_form_repair_prepare`/Job 上接受可选 `part_id`，从 bilateral Rig 生成 exact Part scope，连续搜索不再包含其他 Part；source/proposal 仍使用同一 Runtime camera、Geometry/Render Worker 和严格 non-regression acceptance。r10 current-cohort receipt `docs/evidence/mcp010f/supplemental/real-codex-cli-current-20260815-part-contour-trial-r10.json` 对 `shoulder-armor-left` 实际返回 `retained_source`/`no_improvement`，source/proposal loss `0.426350916959`/`0.425568832154`，未创建 candidate/version，Part contour readback `proposal_ready`。顶层 `BLOCKED` 只来自 probe 无关副作用，quality `NOT_RUN`；该 Gate 是 bounded trial/retention PASS，不是 likeness 或 high-quality PASS。

2026-08-15 Primary Form single-Part contour/asymmetry Gate：Runtime `part_contour_fit_prepare` 对 bilateral alias 做确定性聚合，同时保留 exact single-Part mask/error/parameter scope；`hip-pair` 可读取 `hip-left`/`hip-right`，显式 `shoulder-armor-left` 只影响左侧。新增 alias projection 与 asymmetric materialization regressions，真实 r8 supplemental `real-codex-cli-current-20260815-part-contour-r8.json` 在 cohort `dd78d216…e5088` 通过 `silhouette_rig_hash → part_contour_fit_prepare`，返回 `proposal_ready`、26 Parts、`PASS_BOUNDARY_ONLY`，persistent/unrelated side effects 均为 false。该 Gate 只证明 Runtime-owned typed/read-only 归因，不产生 likeness、人评、PBR、export/restart 或 360 证据；r8 quality 为 `NOT_RUN`，历史 `QUALITY_TARGET_NOT_MET`、`BLOCKED_INCOMPLETE_BINDING` 保持不变。

2026-08-15 QualityReport/packaged F Gate：Runtime `AppearancePrepare` 现在严格输出 `visual_status=not-run`、`hard_gate_passed=false`；同一 packaged cohort `fee79807…` raw F probe 通过 detail geometry/readback、7 material zones、九 AOV、embedded textures、Render Worker same-cohort binding 和 Viewer read-model。该 Gate 是 structural/read-model PASS，不是视觉 likeness PASS，故真实 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING` 与人评/PBR/export/restart/360 的 `NOT_RUN/BLOCKED` 状态保持不变。

2026-08-15 Primary Form 多尺度 geometry convergence Gate：Runtime 在固定 64 evaluation ceiling 内把 `GeometryProgram` bounded search 分配为 `40 geometry + 15 initial camera + 9 geometry-winner refit`；coordinate probe 后续 pass 采用 `1.0 → 0.5 → 0.25` deterministic scales，使 26-control detail Rig 能在完整 evidence pass 后继续 reverse/fine refinement。focused budget/scale regression 通过；该 Gate 只证明 Runtime-owned 收敛搜索结构，不产生新的真实 likeness、人评、PBR、export/restart 或 360 证据，现有 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING` 保持不变。

2026-08-15 Runtime camera-fit→compare handoff Gate：同一 Runtime 进程内，`camera_fit_prepare` 的 selected camera 现在按 `project/candidate/target` 三元组缓存；后续 `reference_compare_prepare` 未显式携带 camera 时复用该 exact calibration，并跳过 default framing 的二次推导。无 exact cache 或无 target 时才使用原有 bounded fallback；新增 Runtime regression 通过。该 Gate 只修复 source-level camera lineage，不替代历史真实 receipt 的重新运行，不改变当前 `camera=MISMATCH`、`QUALITY_TARGET_NOT_MET`、`BLOCKED_INCOMPLETE_BINDING` 或 human/PBR/export/restart/360 状态。

2026-08-15 Primary Form bounded continuation Gate：`primary_form_repair_prepare` 与异步 Job 在固定 64 evaluation ceiling 内最多执行两轮 Runtime-owned camera/geometry continuation；第二轮围绕第一轮 incumbent，不把连续参数轨迹交还 Codex。optimizer normalization regression 与端到端 fixture 通过（`iterations=2`、63–64 evaluations）；该 Gate 只增强 bounded convergence path，不产生新的真实 likeness receipt，现有 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING` 与未运行的人评/PBR/export/restart/360 状态保持不变。

2026-08-15 Agentic single-observation slice Gate：`scene_observe_get` 现在是 Agentic follow-up 的唯一现场来源；`design_stage_plan_get`、`critic_report_get` 和 `visual_evidence_bundle_get` 必须提交并通过同一 `observation_sha256`，Runtime 只返回该 observation 内的 stage/critic/`VisualEvidenceBundle@1` slice，stale/missing hash fail closed。Viewer `ViewerVisualEvidence@1` 仍保持独立 read-only transport；focused Runtime/MCP/source tests 通过。该 Gate 只证明观察一致性，不证明 orchestrator、Repair、真实 likeness、人评、PBR、export/restart 或 360，`QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING` 保持不变。

2026-08-15 Render Worker cohort-to-RenderSet Gate：Runtime 保留 isolated sibling Render Worker 返回的 `build_cohort_sha256`，并将其写入 `RenderSet@2.render_worker_build_cohort_sha256` 与 `render_worker_binding_status`；当前 Runtime cohort 不匹配或字段/status 不一致时，RenderSet、VisualEvidence 和 Agentic observation fail closed。Viewer 只读显示 `same_cohort_verified` / `cohort_unavailable`，不从该字段或 comparison metrics 重算质量门。Runtime C fixed-renderer regression、contracts、Viewer TypeScript、MCP010C/E probe syntax 通过；该 Gate 只关闭 Worker identity/evidence 断点，不产生新的 likeness、人评、PBR、export/restart 或 360 证据，现有 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING` 保持不变。

2026-08-15 canonical observation durable binding Gate：`DesignSession@1`、`DesignCheckpoint@1` 和 bounded action 现在共享同一 `AgenticSceneObserveResult@1.canonical_sha256`；Store payload/schema 校验以及 session get、checkpoint prepare/get/restore、action run 的 Runtime stale/mismatch checks 均 fail closed。隔离 Runtime/MCP restart probe 证明持久化 session/checkpoint 与 nested projection 的 observation hash 相同；contracts、receipt checker、projection checker 和 `forgecad-runtime` full Gate（`105 passed / 12 ignored`）通过。该 Gate 只证明观察 lineage、durable readback 和 Runtime authority，不替代完整 Agentic producer/orchestrator、Repair、真实 Codex loop、likeness、人评、PBR、export/restart 或 360；`QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING` 保持不变。

2026-08-15 RepairIntent observation binding Gate：`RepairIntent@1.observation_sha256` 必须等于 durable session/checkpoint 使用的同一 `AgenticSceneObserveResult@1.canonical_sha256`；Runtime 在 `checkpoint_restore_prepare` 生成该字段，schema/receipt checker 拒绝缺失或不一致值，隔离重启 probe 已验证 Runtime-owned readback。该 Gate 只证明 Repair intent 的观察 lineage，不证明 Repair 已执行、完整 orchestrator、真实 likeness、PBR、export/restart 或 360；现有质量失败与不完整绑定状态保持不变。

2026-08-15 Primary Form local-group convergence Gate：Runtime 在 joint proposal 失败时按 boundary-priority 组成同 Part 的耦合 width/height/offset candidate，候选通过同一 typed Geometry/Render Worker 和 Runtime-owned metric ordering 后才更新 incumbent；随后才使用剩余预算进行单坐标 refinement。group ordering/composition regression 与 Primary Form focused Gate 通过；该 Gate 只证明 Runtime 搜索结构更接近可收敛的 coarse-to-fine loop，不证明真实 likeness、人评、PBR、export/restart 或 360，`QUALITY_TARGET_NOT_MET`、`MISMATCH`、`BLOCKED_INCOMPLETE_BINDING` 保持不变。

2026-08-15 Primary Form ranking snapshot Gate：Runtime camera/geometry/refit 每次 render 同时形成五项 public contour metrics 和 candidate-bound transient landmark ranking snapshot；baseline、winner、geometry local incumbent 与最终 strict-improvement 使用同一 ranking projection，selected-camera geometry trial 不再拿 base-camera raw metrics 直接比较。公开 `SilhouetteFitResult@1`、MCP manifest 和 Viewer quality authority 不变；snapshot regression 与 Runtime full Gate（`105 passed / 12 ignored`）通过。该 Gate 只修复 Runtime 收敛排序，不产生新的真实 likeness 证据，仍为 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING`，人评/PBR/export-restart/360 未运行或阻断。

2026-08-15 Agentic observation/camera handoff Gate：MCP `design_action_run_prepare` 必须接收 Codex 消费的 `observation_sha256`，并把它绑定进 `input_sha256`；Runtime 只接受当前 candidate-bound `AgenticSceneObserveResult@1` 的同 hash，stale observation fail closed。Primary Form action 的 `base_camera` 由 session `camera_hash` 在同一 candidate/target-bound Runtime camera-fit evidence 中解析，未命中返回 camera binding mismatch，不回退 default camera。MCP schema/negative fixture、Runtime stale-observation/action round-trip focused Gate 通过；该 Gate 只证明 observation/camera lineage，不替代 orchestrator、Repair、真实 Codex loop、likeness、人评或 Viewer 质量权威。

2026-08-15 Runtime quality-authority Gate：`visible_view_gate_checks` 统一定义 visible-view metric 的方向、阈值和状态；`visible_view_gate_passes` 与 Agentic critic 的 `failed_metrics` 共用同一 Runtime projection，避免 Agentic/Viewer 层重新推导质量门。focused regression、Runtime 全量 `104 passed / 12 ignored` 和 MCP010F source Gate 通过；该 Gate 只证明质量权威边界，不是 likeness PASS，仍为 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING`，人评/PBR/export-restart/360 未运行或阻断。

2026-08-15 Primary Form metric-priority Gate：Runtime camera/geometry/refit/acceptance/candidate compare 共用确定性 priority ordering：`boundary_f1_4px` → `silhouette_iou` → `bbox_edge_error/centroid_error` → landmark coverage/NME → regions；scalar loss 只在全部高优先级项相等时 tie-break。新增回归证明明显 boundary 改善不会被 IoU/Chamfer 交易掉，Primary Form focused 14 passed。该 Gate 是 Runtime 收敛判定修复，不是视觉 likeness PASS；没有新的真实 receipt，不改变 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING` 或人评/PBR/export-restart/360 未运行状态。

2026-08-15 Primary Form 双向边界证据 Gate：Runtime 在固定 bounded budget 内同时生成 target→model 与 model→target boundary correspondences，并在同一 candidate/camera/Part-ID 作用域下保留模型独有或位移后的边缘；既有 per-Part coverage 与最多 64 条 deterministic retention 继续作为 Codex observation projection。新增 model-owned excess edge regression，`forgecad-runtime` 全量 `103 passed / 12 ignored`。该 Gate 只改善 Runtime-owned Primary Form 收敛信号，不能替代真实 likeness、独立人评、PBR、export/restart 或 360；`QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING` 保持不变。

2026-08-15 Render Worker ownership Gate：`geometry_worker.rs` 仅暴露通用 `execute_sibling_worker`，`render_worker.rs` 独占 `forgecad-render-worker` binary identity 和 fixed/perspective/batch response adapter；Geometry Worker 与 Render Worker 的产品身份和协议 projection 不再交叉。新增真实 sibling isolated conformance tests 覆盖九 AOV、固定 512×512 PNG、重复确定性与 GeometryProgram payload rejection；source boundary checker、Runtime full Gate（`102 passed / 12 ignored`）通过。该 Gate 只证明 Runtime 模块/进程边界，不能替代真实 likeness、独立人评、PBR、export/restart 或 360，现有 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING` 保持不变。

2026-08-15 Primary Form evidence-magnitude Gate：Runtime 第一轮 bounded coordinate probe 对尚未达到同一 boundary proposal 的坐标复用其 evidence-derived magnitude，单步受 authored Rig span 50% 上限约束；proposal 已到达和后续反向 pass 仍用原有小步。focused regression 与完整 `forgecad-runtime` Gate 通过（102 passed、10 ignored）；该 Gate 只改善 Runtime 内部有限搜索的有效步长，不是视觉 likeness PASS，仍为 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING`，human/PBR/export-restart/360 未运行或阻断。

2026-08-15 Primary Form boundary evidence coverage Gate：Runtime 的 candidate-bound boundary segments 在 `max_segments<=64` 内先为每个有 `part_id` 的可见 Part 保留一条最高误差记录，再按全局距离填充，最后按距离降序稳定输出；新增 regression 与完整 `forgecad-runtime` Gate 通过（101 passed、10 ignored）。该 Gate 关闭“单一 dominant Part 占满观察表、secondary Part 不可见”的证据覆盖缺口，但不宣称视觉 likeness 或高质量 PASS；没有新的真实 receipt，仍为 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING`，human/PBR/export-restart/360 未运行或阻断。

2026-08-15 Primary Form proposal-direction retention Gate：Runtime 将完整 boundary projection 保留为 Runtime-owned coordinate direction source，同时仅把 dominant Part 的投影值用于 probe zero；secondary Part 仍按自身 boundary evidence 进入有界坐标探测，零改变量才使用稳定 fallback。显式 `-1/0/+1` regression、完整 Runtime Gate 通过（100 passed、10 ignored）；无新的真实 likeness receipt，仍为 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING`，human/PBR/export-restart/360 未运行或阻断。

2026-08-15 Primary Form bounded multi-Part sweep Gate：Runtime 保留 candidate-bound dominant Part 的局部 seed，但不再把后续 coordinate probes 限制在该 Part；固定 bounded schedule 会按 boundary score、proposal delta 和稳定 ID 排序后覆盖完整 typed Rig。新增 seed/sweep coverage regression 与完整 `forgecad-runtime` Gate 通过（100 passed、10 ignored）；无新的真实 likeness receipt，仍为 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING`，human/PBR/export-restart/360 未运行或阻断。该 Gate 不改变 Agentic action-run 当前单 Part scope。

2026-08-15 Primary Form local Part offset calibration Gate：Runtime 局部 Part envelope proposal 的米制 `offset_x/offset_y` 使用 candidate fit 的 `CameraCalibration@1` world-per-normalized-screen scale，`offset_y` 与 landmark/boundary projection 统一 camera-plane up 符号；无 camera 的 legacy proposal 对米制 offset fail-safe 为 neutral。focused regression 与 `forgecad-runtime` 全单测通过；无 Schema/工具数变化、无新的真实 likeness receipt，仍为 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING`，human/PBR/export-restart/360 未运行或阻断。

2026-08-15 Primary Form same-camera retention Gate：Runtime 在 `geometry_prepare` 前用同一最终 camera、reference target、512px Render Worker 和 weighted loss 对 source/proposal 做 non-persisted compare；只有 strict improvement 才创建 staged candidate，失败返回 `PrimaryFormAcceptance@1.status=retained_source` 并保留 authored source。合同、Runtime focused regression、candidate/version no-write boundary 通过；无新授权机器人视觉 receipt，仍为 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING`，human/PBR/export-restart/360 仍未运行或阻断。

2026-08-15 Agentic observation→action binding Gate：`DesignActionRun@1.observation_sha256` 必须等于本次 `design_action_run_prepare` 内 Runtime 生成并通过 binding 校验的 `AgenticSceneObserveResult@1.canonical_sha256`；Store/CAS readback、schema fixture（含缺字段 fail-closed）和 Runtime action round-trip 通过。该 Gate 只证明观察与动作 receipt 的 lineage，不能替代 orchestrator、Repair、真实 Codex loop 或视觉/人评 Gate。

2026-08-15 Runtime Render Worker adapter Gate：`forgecad-runtime/src/render_worker.rs` 现在是 fixed/perspective/batch Render Worker response 的唯一 Runtime typed adapter；Geometry Worker 仅提供 Geometry compile 与固定 sibling launcher，Primary Form 的 framing/full-resolution/refit 统一通过 adapter 进入 isolated Render Worker。source ownership checker、Runtime check 和 focused Primary Form/action/camera regressions 通过；不产生新的真实参考视觉证据，仍为 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`，人评/PBR/export-restart/360 仍未运行或阻断。

2026-08-15 Primary Form automatic-target Part Gate：`part_contour_fit_prepare` 现在可从同一 Render Worker silhouette + Part-ID boundary evidence 投影无显式 Part slice 的 automatic target，单 Part probe 支持 `shin-pair → shin-left` 的 bounded typed patch、同 cohort identity 和 baseline retention。真实 receipt `docs/evidence/mcp010f/part-correction-current-20260815-shin-auto.json` 通过 transport/compare，但五个候选均未同时避免 IoU/Boundary/bbox/centroid 回归，故 authored baseline 保留；该 Gate 是局部归因/回退 PASS，不是 likeness 或 high-quality PASS，仍为 `QUALITY_TARGET_NOT_MET`，不改变 camera `MISMATCH`、benchmark incomplete binding、人评/PBR/export-restart/360 状态。

2026-08-15 Primary Form action budget Gate：修复前 real-Codex receipt 证明 `primary_form_repair_prepare` 把 detail 请求的 `max_evaluations=64` 静默截为 24；Runtime 现恢复固定 64 cap，并以 helper 统一约束 `max_iterations<=2`。端到端 Runtime fixture 实际消费 63–64 次 bounded fit、两轮 continuation，focused/full source Gate 通过；修复后 real-Codex 复跑在 authoring/hash/prepare 阶段 `BLOCKED`，没有新的 compare/likeness 结果。该 Gate 是 Runtime 收敛预算与 Codex 单动作边界 PASS，不是视觉质量 PASS；仍保留 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark incomplete binding、人评/PBR/export-restart/360 未运行。

2026-08-15 packaged Render Worker landing Gate：同一 current source cohort 的 Dev.app Resource allowlist 精确包含 `forgecad-mcp`、`forgecad-runtime`、`forgecad-geometry-worker`、`forgecad-render-worker`；ad-hoc deep-strict package verify 通过，packaged Runtime 通过 sibling Render Worker 完成九 AOV/固定 512px render/两次 deterministic hash/compare/image-block transport，隔离 probe 无持久用户数据写入。该 Gate 是 packaged process/resource/protocol PASS；输入为 synthetic reference，`structural_visual_claim=NOT_CLAIMED`，不改变 `QUALITY_TARGET_NOT_MET`、human/PBR/export-restart/360 未运行状态。

2026-08-14 Primary Form 联合 proposal 回退 Gate：Runtime 对 dominant Part 的一次性联合 proposal 在严格未改善时固定重试 50% 与 25% authored-baseline interpolation，再进入 deterministic coordinate probes；回退不扩大 bounds、不交给 Codex 连续搜索。focused/full Runtime、contracts、Render Worker boundary PASS；无新的视觉 receipt，仍为 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING`。

2026-08-14 Primary Form bilateral Part-ID projection Gate：Runtime 将 `*-left`/`*-right` Worker Part-ID 归并到对应 `*-pair` Rig Part 后计算局部 boundary envelope proposal；pair alias regression、full Runtime、MCP010C/F source 与 Render Worker boundary PASS。该 Gate 只证明收敛输入修复，没有新的视觉 receipt，仍为 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING`。

2026-08-14 Primary Form 单 Part action Gate：Runtime 将主导 Part 的 candidate-bound boundary score 绑定为本次 repair 的唯一 mutable scope，并将 critic projection 的 repair operation 固定为 `primary_form_repair_prepare`；非聚焦参数回到 authored baseline，避免 Codex 重新接管连续搜索。focused/full Runtime、Agentic contract、MCP010C/F source 与 Render Worker boundary PASS；没有新视觉 receipt，故仍为 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、benchmark `BLOCKED_INCOMPLETE_BINDING`。

2026-08-14 Primary Form Part-priority Gate：Runtime 使用同 candidate Part-ID boundary evidence 为 bounded Rig probes 建立主导 Part 优先级，并保留无 Part evidence 的 deterministic fallback；focused/full Runtime regression 与 MCP010F source Gate PASS。当前没有新的授权参考图视觉 receipt，故仍为 source/convergence PASS、`QUALITY_TARGET_NOT_MET`，不改变 camera `MISMATCH`、benchmark incomplete binding、人评/PBR/export-restart/360 状态。

2026-08-14 Primary Form 全控制覆盖 Gate：26-control detail Rig 的 CLI `silhouette_fit_prepare` budget 已提升为 64；Runtime GeometryProgram schedule 固定为 `32 geometry + 16 initial-camera + 16 geometry-winner-camera-refit`，首轮逐控制覆盖通过 focused/full Runtime regression。该 Gate 只证明 Runtime-owned bounded coverage 与 Codex 单次 typed intent 边界；没有新的真实 likeness receipt，当前 `QUALITY_TARGET_NOT_MET`、camera `MISMATCH`、`BLOCKED_INCOMPLETE_BINDING`、human/PBR/export-restart/360 状态不变。

2026-08-14 Primary Form Runtime-owned action Gate：current-cohort real Codex receipt `real-codex-cli-current-20260814-primary-form-runtime-owned-r3.json` 验证一次聚合 observation 后仅有一个 `primary_form_repair_prepare`，其内部完成 Geometry Worker → Render Worker → compare，CLI 消费同一 candidate-bound visual evidence。该 Gate 为 transport/orchestration PASS_WITH_QUALITY_TARGET_NOT_MET；IoU `0.749122`、Boundary F1 `0.347623` 未过视觉门，不能计作 likeness/high-quality PASS。

FGC-MCP010F 增量：隔离 Vite browser DOM smoke 已验证 Viewer 控件和无 metrics 的 `reference-canvas`/空 correction queue 语义；同 cohort Dev.app 的 Computer Use AX smoke 又实际操作了 AOV、Home/End、overlay/flicker、轮廓画布、差异热图和爆炸图；System Events 仍未暴露 WebKit 子树，因此正式 VoiceOver/无障碍、视觉 likeness 或人评状态不升级。

Stage 0 Gate 历史段口径：本段 195/25/90 是对应旧 cohort；2026-08-26 现行 source 为 **518 schemas / 28 operator entries / 111 read + 83 opt-in write = 194 tools**，唯一 `in_progress` 仍是 `FGC-MCP010F`；机器证据入口为 `docs/evidence/mcp010f/current-benchmark-truth.json`。Mechanical pose、Agentic scene/stage/critic/evidence 均是只读 projection；durable session/checkpoint/RepairIntent 与 Primary Form 单动作不等于完整 producer/orchestrator/Repair。attempt35 仍为 `QUALITY_TARGET_NOT_MET + INCOMPLETE_TRUTH_BINDING`、camera `MISMATCH`；UI/accessibility、人评与 packaged UI E2E 未运行。

2026-08-14 Primary Form schedule regression：Runtime camera coordinate-descent now continues after a non-improving local batch and consumes the declared bounded camera remainder, preventing later roll/FOV/distance/target-offset/global-scale axes from being silently skipped. The focused Runtime fixture consumes all 8 camera evaluations in its bounded budget. This is a source/convergence PASS only; it does not promote the current observation, alter Viewer quality authority, or change `QUALITY_TARGET_NOT_MET`, `MISMATCH`, `BLOCKED_INCOMPLETE_BINDING`, human/PBR/export-restart, or 360 status.

2026-08-14 Agentic visual evidence/critic consolidation：`target_sha256` now remains candidate/reference-bound from Runtime visual evidence through Viewer and Agentic observation；critic emits one fixed-priority Runtime-owned Primary Form directive and does not expose continuous parameter search to Codex. Target-bound round-trip and `script/test_mcp010f.sh` passed；this does not change Viewer quality authority or the existing `QUALITY_TARGET_NOT_MET` / `MISMATCH` / `BLOCKED_INCOMPLETE_BINDING` state.

| Blender reference-only Modifier Stack lowering（historical 144-schema cohort） | PASS_SOURCE_FOCUSED | `GeometryModifierStackRequest@1` / `GeometryModifierStackProgram@1`；Runtime deterministic/no-persistence 2 tests；MCP read-only round-trip；当时 144-schema check | 当时仅 transform/mirror/array；当前 Bevel/Normal 见下一行；package/live/visual/human NOT_RUN |
| Blender clean-room bounded Bevel/Normal（historical 146-schema slice） | PASS_SOURCE_STRUCTURAL_ONLY | historical 146 schemas；19/19 catalog；35/35 Geometry Worker；Runtime/MCP/Skill focused；300 triangles strict readback deterministic；source-box lineage exact | Bevel 仅 direct box；Normal 仅 corner face-area×face-angle；Runtime auto-prepare/Agentic typed params/package/live/render AOV/visual/human NOT_RUN |
| Blender clean-room bounded Subdivision evaluation v2 | PASS_SOURCE_STRUCTURAL_ONLY | historical 151-schema slice；19/19 catalog；`geometry_program_hash` fifth branch；0/1/2 level 8/32/128 triangles；Runtime/MCP/Worker focused and no-persistence | regular open quad grid only；crease/adaptive/limit surface/FVar UV/element lineage/OpenSubdiv/package/live/visual/human NOT_RUN |
| ForgeCAD fixed RenderProfile/AOV lineage | PASS_SOURCE_STRUCTURAL_ONLY（historical 152-schema slice） | Render Worker/Runtime exact profile/hash binding；Schema exact field/order/color alignment；PNG exact RGBA8 decode；ID palette indices 0..255 fail closed | package/live/visual/human NOT_RUN；no Blender/Cycles/EEVEE/OCIO/GPU/EXR/custom-AOV parity |

<!-- forgecad-stage0: schemas=579 schema_set_sha256=2f959a94d11392f851b9b276d6b699bc250d900a74fd2e88ff3bb1c19cb764b1 read_tools=128 write_tools=94 total_tools=222 task=FGC-MCP010F observation=QUALITY_TARGET_NOT_MET eligibility=BLOCKED_INCOMPLETE_BINDING evidence=INCOMPLETE_TRUTH_BINDING camera=MISMATCH packaged=PASS_CURRENT_COHORT_BOUND_READ_MODEL latest_attempt=real-codex-cli-current-20260815-b37-complete-auto-v3.json latest_completed=real-codex-cli-current-20260815-b37-complete-auto-v3.json -->
<!-- forgecad-reference-source: input=ENV_AUTHORIZED_PNG original_sha256=1964704a62ed7a841b4d49c370b8d46f4626e201daad29092a9c39a40b4c4109 intake=PASS_SOURCE_SIX_REFERENCE_EVIDENCE_CAS views=6 worker=PASS_SAME_COHORT_SIX_FIXED_VIEWS target=USER_REFINED_USER_CONFIRMED_REVIEWED_STRUCTURE user_confirmed_crop=PASS_USER_CONFIRMED_SEVEN_CROPS contour=PASS_USER_CONFIRMED_SIX_IDENTITY_CONTOURS negative_space=BOUNDING_REGIONS_CONFIRMED_EXACT_SUBTRACT_UNKNOWN line_flow=EXPECTED_ROWS_DURABLE_MATCH_NOT_PROVEN camera_lock_fixture=PASS_REAL_DURABLE_REPLAY_RESTART form_art_fixture=PASS_REAL_DURABLE_NOT_PROVEN form_quality_v2_fixture=BLOCKED_ZERO_WRITE_MISSING_LEGACY_CROSS_VIEW secondary_form_approved=NOT_CREATED fixture=PASS_REAL_1_OF_1_108.07S -->

MCP010B/C/D/E/F reconciliation：历史 MCP006 为 44 contracts；保存的历史 receipts 按当时范围保持原样。当前源码为 490 contracts、28 operator entries、106 read + 78 opt-in write = 184 tools，C/D/E/F 的 renderer、operator、AssetPack、Viewer/contour、Mechanical pose、crease-aware Subdivision 与 Authoring Mesh 各自 source Gate PASS；Mechanical pose、Authoring Mesh 和 Agentic projection 都不等于 Armature/skin、orchestrator、Repair、Viewer UI/accessibility、image-block consumption 或视觉门通过。用户机器人 provisional retained observation 因 camera `MISMATCH` 仍为 `QUALITY_TARGET_NOT_MET + INCOMPLETE_TRUTH_BINDING`，benchmark eligibility 为 `BLOCKED_INCOMPLETE_BINDING`。Darwin OS memory cap、xatlas、Khronos Validator、packaged Viewer UI E2E、真实 user likeness、PBR likeness、human threshold、export/restart hash/360仍 `NOT_RUN`/blocked。

| 能力 | 当前状态 | 实现 Gate | 集成 Gate | 生产 Gate |
|---|---|---|---|---|
| 文档权威重置 / Dev 激活 | MCP010A done / Desktop attempt 1 FAIL retained / attempt 2 PASS | MCP010A 权威链、真实 Codex CLI、config activation、共享 Runtime/IPC 修复 tests、current `release:mvp`、同 cohort package PASS | 第二次 Desktop 重启观察到 30 个工具、Runtime/doctor `Ready`、capabilities cohort match、临时 project_create/project_get readback PASS；成功 receipt 已保存 | 不适用 |
| 安全硬切 | 已实现 | 旧产品路径删除、contracts/viewer 骨架、Rust workspace 和 release:mcp002 | reset archive/Library hash 保留；后续 packaged hardening 在 MCP013 | clean packaged build |
| Runtime 单写者 | 已实现（MCP002）；共享启动协调与 Desktop activation PASS | migration 前 OS 独占 `runtime.writer.lock`、第二实例 `RUNTIME_BUSY`、事务 rollback 是已有 Gate；短时 launcher flock 只做选主，不能替代 writer lock | normal adapter exit 后复用 Ready Runtime、idle owner/passive takeover、rogue pre-auth client 隔离回归和第二次 Desktop live Gate PASS；packaged kill-9/upgrade 在 MCP011/013 | backup/upgrade/packaged |
| Runtime CAS | 已实现（MCP002 范围） | SHA-256、atomic write、capacity/missing/corruption/hash mismatch、backup/restore | reachability/GC 在 MCP011 | packaged/disaster recovery |
| authenticated local IPC | 已实现（MCP002 范围） | 0600 Unix socket、token hash/constant-time auth、错误 token 拒绝 | MCP013 packaged wiring | signed install |
| MCP stdio/resources | 已实现（MVP host golden path + MCP010C/E/F source raw） | MCP 2025-11-25 canonical + Codex 2025-06-18 compatibility initialize；当前源码 manifest 为 208 tools（119 read + 89 explicit opt-in write），新增 `weapon_foundation_asset_get/prepare` 只传 typed identity/hash/status，拒绝 path/URL/source bytes。`mechanical_pose_evaluate` 等读取 Runtime evidence；轮廓 mask、Agentic durable prepare 与 foundation import prepare 均显式 write opt-in；URI/contract fail closed | C/E/F raw source PASS；foundation importer source/runtime replay PASS；同一 provisional observation 的 packaged Viewer E2E/live C/F 与 packaged foundation import 尚未运行 | signed install/config |
| Codex 附件导入 | 已实现（MCP005，CLI） | PNG/JPEG MIME/path/hash/decode-limit/adversarial | 真实 Codex CLI bytes/CAS readback PASS；Desktop bridge `NOT_RUN / unavailable` | clean install E2E |
| Candidate/approval/version | 已实现（MVP host golden path） | prepare/Job/approval/confirm/reject/restore-as-new-version/diagnostic-export/idempotency/stale/hash/quality/expiry、MCP009 stable-Part `change_prepare` PASS | 真实 Codex CLI candidate/quality/version/CAS export PASS；MCP010A Desktop attempt 1 在 write tool 可见性前 FAIL、无项目写入，故 Desktop 写事务仍 NOT_RUN；change/restore/restart hash 与 packaged 待后续 Gate | packaged multi-turn |
| Geometry Compiler | 部分实现（MCP007 done） | canonical GeometryProgram；bounded box/cylinder/sphere；finite/index/budget/lineage；deterministic GLB/readback；unknown operator/no-version-on-failure PASS | Runtime/MCP authenticated geometry prepare + Viewer candidate/artifact read model PASS；real Codex CLI geometry/readback slice PASS（14 parts/516 triangles）；MCP009 full host appearance链已单独 PASS | full profile/boolean/bevel, local edit, cross-category/performance/human |
| Geometry V2 / detail | MCP010B structural + MCP010D source + packaged structural PASS | `GeometryProgram@2`/OperatorCatalog/`geometry_program_hash`/真实 GLB readback；D 的 profile/revolve/sweep/loft/transform/mirror/array/panel/vent/joint/part-output 与 bounded same-Part Boolean；`GeometryQualityReport@2`/`GeometryCandidateEvidence@1` 绑定 strict quality 与 confirm/restore provenance | 52-contract B/Worker isolation/restore/closed profile/D operator Gate 与同 cohort packaged D raw strict-readback probe PASS；Darwin OS 512 MiB hard cap、任意 mesh Boolean、visual gate仍 NOT_RUN | PBR/reference/visual/human/360 |
| Authoring Topology / transient mesh edit | MCP010F partial structural；three-object restart PASS；IdentityLineage Runtime restart/basic cross-candidate PASS | exact V/E/source-loop/F；typed split/collapse/dissolve 独立 full-chain 3/3；单调 tombstone/non-reuse | bridge/inset/general correspondence、evaluated retarget、persistent selection/undo history、package/live/Viewer UI、BMesh/Python/plugin parity NOT_PROVEN | visual/human/PBR/final export-restart/HQ360 NOT_RUN/BLOCKED |
| FPS foundation typed importer | SOURCE/RUNTIME STRUCTURAL PASS；promotion blocked | 三个固定 hash 内嵌 GLB；strict GLB2、累计 TRS、asset-specific right-handed meter mapping、退化面 sanitation、compact topology/socket/rig/PBR/animation inventory、CAS durable link、MCP hash-only get/prepare | Pichuliru west、WRAD arms、Lightning 真实 bytes 解析 PASS；首次 prepare、幂等 replay、get hash revalidation PASS；`FpsPresentationPackage@1=DRAFT_UNREVIEWED` | `AUTHORING_MESH_MATERIALIZATION_PENDING`；WRAD/Lightning 方向人工确认、完整 clips/events、High→Low→UV→Bake、engine/human/HQ360 NOT_RUN/BLOCKED |
| Appearance Compiler | 已实现（MCP008 bounded） | hash-bound UV/tangent/PBR/MaterialZone/GLB readback PASS；完整 glTF validator/texture bake NOT_RUN | Runtime fixed artifact + Viewer canvas PASS；真实 Codex appearance/readback 在 MCP009 receipt PASS | engine roundtrip/human |
| 离线 AssetPack / PBR V2 | MCP010E source-focused + packaged structural PASS_WITH_UNRUN_EXTERNAL_AND_VISUAL_GATES | `forgecad-hard-surface-robot@1.0.0`、逐资产 provenance、512px bounded UV atlas、固定 `mikktspace@0.3.0`、颜色空间/纹理预算、无 external URI、clearcoat/emissive strength | `script/test_mcp010e.sh` raw source Gate 与同 cohort packaged E 结构传输 PASS；xatlas/Khronos Validator adoption、真实视觉/PBR likeness、export/restart hash仍 NOT_RUN | signed package/license review |
| Render Evidence | MCP010C source-focused PASS | deterministic MCP008 compatibility passes + C perspective/z-buffer nine AOV PNG + camera/renderer hash PASS；`render_fixed` 已收紧为 Geometry Worker 编译 GLB → Render Worker 消费的 typed boundary，并有 compiled-GLB 负向回归；无状态 renderer source 已归属 `apps/render-core`，Render Worker 不再依赖 Geometry Worker crate，source ownership checker PASS | F Viewer source AOV/compare projection and local build/IPC boundary PASS；packaged Viewer/cross-GPU/human 仍未运行 | packaged Viewer/cross-GPU/human |
| Reference compare V2 | MCP010C source-focused PASS_WITH_UNRUN_VISUAL_GATES（非当前 `in_progress`） | perspective/z-buffer、九 AOV、local mask/IoU/F1/bbox/centroid/landmark/region、typed review/human receipt schema | 历史 r7 54+36 工具 cohort 的 comparison/Viewer transport 结构通过但仍为 `QUALITY_TARGET_NOT_MET`；现行总工具面 110+82 不改变视觉结论；真实 user likeness、同基线 packaged Viewer E2E、人评阈值仍 NOT_RUN | cross-category blind review |
| Quality Compiler | 部分实现（MCP009 limited） | candidate/GLB/PBR/fixed-render checks PASS；reference aspect comparison explicitly limited | `quality_get`/`version_diff` and functional tests PASS；typed visual review/real Codex/user score NOT_RUN | independent cross-category blind review |
| Skill Bundle | MCP006 done（development-only）+ MCP010B/D/E/F truthful overlays | MCP006 历史 44 contracts，当前源码 490；历史 first-party registry + active Bundles，canonical/trust hash、operator/validator allowlist、Recipe DAG/单位/finite/预算、AssetPack LICENSE/NOTICE/SPDX SBOM/provenance、无网络/无动态代码/无模型调用 | D/E/F source Gates PASS；Mechanical pose 与 Authoring Mesh 不新增 executable Skill 或插件；xatlas/Validator、packaged/visual benchmark NOT_RUN | distribution signature/revocation/third-party registry |
| Agentic Design Runtime | projection source/read-only PASS；nested projection conformance PASS；durable prepare/readback PASS；Primary Form bounded action-run PASS；通用 orchestrator/Repair NOT_IMPLEMENTED | Agentic contract family 已进入 manifest；4 个 read-only projection tool、5 个 durable session/checkpoint prepare/readback tool、Runtime SQLite/CAS、Viewer lookup、preflight/isolated restart probe 已通过各自范围；`design_action_run_prepare`/`design_action_run_get` 另通过单 Part `primary-form` action 的 bounded compile/readback/render/evaluate、幂等和 immutable readback focused test；结果不允许 confirm/export 或 candidate/version mutation | `docs/evidence/mcp010f/agentic-runtime-projection-conformance-20260813.json` 证明真实 Runtime scene/stage 嵌套只读 projection producer/consumer conformance；当前 Runtime regression 证明 action receipt 与 Part-error/RepairIntent scope 共用 candidate/target/render lineage，不证明 durable/reference/DesignSpec 完整 producer、通用单动作 orchestrator、Repair 应用、packaged、视觉或真人门 | durable/reference/DesignSpec producer conformance、通用 action orchestrator、Repair execution、real Codex loop、human/package |
| 废弃隔离 | 已实现（当前树审计范围） | 旧 Provider/Agent/workbench 已清理；standalone Host receipt 在 archive；三个 superseded Skill Bundle 已移入 `packages/forgecad-skills/archive/superseded/`，archive manifest 固定 tree hash，Runtime build 只嵌入 registry 声明的 Bundle，Skill checker 拒绝未注册版本留在 `bundles/**` | archive 不计 active capability；后续废弃项仍需按 `DEPRECATED_ISOLATION_PLAN.md` 记录和验证 | 发布前还需完整 package/SBOM/archive 审计 |
| 局部修改 | 已实现（MVP bounded） | stable ID + allowlisted `change_prepare` + base head/readback PASS；不声称 mesh-delta | Runtime focused PASS；真实 Codex/Viewer selection E2E NOT_RUN | full undo/explode/a11y |
| 回退 | 已实现（MVP） | restore-as-new-version PASS | Runtime focused PASS；restart/packaged recovery NOT_RUN | packaged recovery |
| 爆炸图 | MCP010F source-focused PASS | stable Part/lineage metadata + ephemeral offset rule | Viewer source toggle/readability + packaged Computer Use AX core-control smoke | formal VoiceOver/human |
| 导出 | 已实现（CAS-backed MVP GLB） | `manifest-json/diagnostic` 与 `glb/mvp-glb` prepare/confirm；confirmed quality GLB、output hash、approval/idempotency PASS；不写任意路径 | MCP009 functional PASS；filesystem/package target NOT_RUN | engine roundtrip |
| 外部项目/资产 | evaluation allowed | adoption receipt/pin/license/SBOM/security | MVP benchmark before `accepted` | legal/distribution approval |
| 首个硬表面 MVP | host golden path PASS / MCP010A done / MCP010B deferred / MCP010C source-focused done / MCP010D/E source-focused PASS / MCP010F source-focused in_progress / visual benchmark OPEN | MCP005–MCP009 focused PASS；B structural Gate PASS但 Darwin OS hard cap NOT_RUN；C fixed renderer/九 AOV/reference compare/review raw Gate、D 19-entry/19-active source catalog（历史 hard-surface raw Gate 为 16-entry/16-active）、E AssetPack/UV/PBR/MikkTSpace raw Gate与F Viewer source Gate PASS；comparison-sheet/fit-plan 仅为本地编排辅助；fit-plan 固定 reference-canvas→silhouette→structure→form→surface/material 解锁顺序；Viewer source 另显示同一累计门的只读阶段提示与 Runtime mask-2 同源 flood-fill contour aid，不改变 Runtime QualityReport；region-mask-iou-v2 已修正区域比较真值；2026-08-12 同 cohort 外形 sweep（sleek/tapered/long-limbs/helmet-visor）已实跑但全部未过质量门 | real Codex CLI full V1 chain PASS；C source synthetic/raw PASS；D 当前 source catalog 19-entry/19-active；历史 raw 16-entry/16-active/strict readback PASS；E 的历史 65-contract raw receipt按当时范围保留；F Viewer source AOV/compare/selection/explosion/heatmap/fit-plan/contour-first panel/flood-fill aid 与 packaged Computer Use AX core-control smoke 仅按 source/结构 smoke 记账，不是 packaged E2E；正式 VoiceOver、真实 user likeness、同一 provisional observation 的 packaged Viewer binding、人评/PBR likeness/360 NOT_RUN；attempt35 仍为 `QUALITY_TARGET_NOT_MET + INCOMPLETE_TRUTH_BINDING`，benchmark eligibility 为 `BLOCKED_INCOMPLETE_BINDING`；单图最高 `PARTIAL_VISIBLE_VIEW_PASS`，且需先通过全部阈值，360 `BLOCKED_REFERENCE_COVERAGE` | 不要求签名分发 |
| 生产发布 | blocked | MCP001–MCP012 | full packaged Codex E2E | MCP013 + cross-category human gate |

旧 U004/Provider/F026/C111/E005 Gate 不映射为新能力 PASS。只有当前合同、当前工作树、真实运行和对应发布包证据才能升级状态。

2026-08-14 F Agentic observation binding：`scene_observe_get` 对无 active snapshot 的多候选项目改为 fail closed，阻止 Runtime/MCP/Viewer 通过隐式列表顺序拼接错误候选上下文；单候选兼容、零候选 unknown 保持。focused Runtime regression 2/2 PASS，receipt `docs/evidence/mcp010f/agentic-observation-binding-20260814.json`。状态仅为 source binding PASS，视觉质量仍为 `QUALITY_TARGET_NOT_MET`，benchmark eligibility 仍为 `BLOCKED_INCOMPLETE_BINDING`。
