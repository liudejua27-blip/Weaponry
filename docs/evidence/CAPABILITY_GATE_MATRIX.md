# ForgeCAD 能力—Gate 矩阵

2026-08-15 Stage Batch Runtime auto-patch Gate：真实 receipt `docs/evidence/mcp010f/design-action-run-real-reference-stage-view-spec-20260815-b37.json` 通过四组件 cohort、entry-level `view_spec` forward、子 ActionRun 到 evaluate、独立 proposal candidate、batch stop/checkpoint/replay 和 source immutability；Gate 标记为 `PASS_ACTION_RUN_RUNTIME_AUTO_PARAMETER_PATCH_STAGE_BATCH_WITH_QUALITY_TARGET_NOT_MET`。它只证明 staged transport 与 fail-closed boundary；proposal/compare 仍 `QUALITY_TARGET_NOT_MET`，没有 confirm/version/human/PBR/360，也不证明 Critic 自动生成 Repair 或完整多动作 orchestrator。

2026-08-15 高层 ActionRun 自动参数补丁 Gate：真实回执 `docs/evidence/mcp010f/design-action-run-real-reference-runtime-auto-parameter-patch-20260815.json` 使用授权参考 `b9cb687e…c1cadd` 与四组件 cohort `a21e448f…f057106`，证明调用方只提交 typed `parameter_changes` + 外层 `ReferenceViewSpec` 时，Runtime 可选择 `surface-control-points-v1`、物化 `RuntimeParameterPatch@1`/`RepairIntent`/review candidate，并完成 `prepare → compile → readback → render → evaluate`。`caller_supplied_full_proposal=false`、source candidate unchanged、proposal `rejected-regression`、视觉 `QUALITY_TARGET_NOT_MET`、无 confirm/version；Gate 级别为 `PASS_ACTION_RUN_RUNTIME_AUTO_PARAMETER_PATCH_WITH_QUALITY_TARGET_NOT_MET`，不升级完整 Agentic Runtime、Repair、likeness、通用 Boolean 或高质量状态。

2026-08-15 SurfaceRig 多控制点 CADFit Gate：真实回执 `docs/evidence/mcp010f/optimization-job-real-reference-surface-control-groups-v15.json` 证明当前源 cohort 上 `surface-shell@1` chest-shell 的 8 个 `surface_control_point` 参数被 Runtime 校验、成组搜索、编译、回读并按同一 camera 比较；39 次 `32/4/3` 评估中有 30 个非 baseline candidate 改变多个控制点，Manifold residual 候选 `[1..9]` 仍受同一 Part scope 限制。全局 loss `0.379948070031 → 0.379344569175` 不足以通过 Part strict objective，最终为 `blocked_global_or_part_objective`/`blocked-no-improvement`，无 proposal/confirm/version/export。Gate 级别为 `PASS_TYPED_MULTI_CONTROL_SEARCH_WITH_QUALITY_TARGET_NOT_MET`，不升级 likeness、PBR、人评、export/restart 或 360。

2026-08-15 img2threejs adoption boundary：staged intake/spec/pass/review 思想已登记为 `reference-only / first-party reimplementation`；Visual Surface 已进入 bounded active source capability：`visual_surface_get` 可回读同一 candidate GLB 的 `triangle-dihedral@1` curvature proxy 与 `boundary-and-crease-edge@1` feature-line summary。`surface-patch@1` typed open-surface、`surface-shell@1` bounded watertight-shell 与 `subd-cage@1` bounded editable regular-quad cage source Gate 均已通过，但仍需 arbitrary-topology SubD/NURBS/曲面语义、same-candidate/same-camera 真实参考、视觉和人评 Gate，不能写成 Visual Surface quality PASS；既有真实视觉 transport receipt 仍属于 SubD 激活前 source cohort。

2026-08-15 当前 cohort CADFit Gate：`docs/evidence/mcp010f/real-codex-cadfit-real-reference-20260815-rebuilt-rig-rebind.json` 在同一真实授权参考上完成 39 次单 `chest-shell` 多保真评估（32/4/3），`baseline_loss=0.466875204210`、`best_loss=0.462318253808`，`strict_improvement=true`、proposal 已生成；MCP/Runtime/Worker cohort 为 `675df14b5e24c02a4dbf463098894ac59a612b00d4e7c6436fc3e64b80b0035f`，Runtime optimization 10/10、MCP 61/61。整体视觉状态仍 `PASS_WITH_QUALITY_TARGET_NOT_MET`（IoU `0.529998`、Boundary F1 `0.073554`），candidate 未 confirm/version/export；该 Gate 只证明当前源批次的 CADFit transport/proposal，不是 likeness、高质量、PBR、人评或 360 PASS。

2026-08-15 视觉候选排序 Gate：`silhouette_candidate_compare` 已统一到 Boundary F1 → Silhouette IoU → bbox → centroid → SDF 的 contour-priority 排序，复合 loss 只作 tie-break，winner/runner-up delta 方向也已固定；当前授权参考回执 `docs/evidence/mcp010f/part-correction-real-reference-20260815-b37-priority.json` 通过真实 5 候选比较。该 Gate 只修正局部选择语义，回执仍为 `QUALITY_TARGET_NOT_MET`，不升级 likeness、PBR、人评、confirm/export 或 360。

2026-08-15 Critic→PartError→CADFit/Manifold Gate：真实回执 `docs/evidence/mcp010f/optimization-job-real-reference-20260815-critic-part-bool-b37.json` 验证显式目标哈希进入 `critic_report_get`，Runtime 产出同候选绑定的 Critic/PartError 哈希，并由同一 `chest-shell` residual 驱动 product-owned Manifold C-ABI 的 32/4/3 CADFit 多保真评估；`strict_improvement=true`、`proposal_status=proposed`，但质量仍 `QUALITY_TARGET_NOT_MET`，无 confirm/version。该 Gate 只证明 scoped diagnostic/Boolean residual/readback，不升级通用 mesh Boolean、likeness、PBR、人评、export/restart 或 360。

2026-08-15 Agentic stage orchestrator real stdio Gate：`docs/evidence/mcp010f/design-action-run-orchestrator-real-reference-20260815-b37.json` 证明同 cohort 真实 MCP stdio 已调用 `design_stage_run_prepare`，在第一个 ActionRun 的 render/evaluate 质量门处停止；RuntimeJob failed 但结果 CAS checkpoint、source candidate unchanged、zero version 和 exact replay hash 均通过。该 Gate 只升级真实 stage batch dispatch/stop/checkpoint/replay，不升级完整 orchestrator、Repair、likeness、PBR、人评、export/restart 或 360。

2026-08-15 CADFit v4 / Manifold residual Gate：`docs/evidence/mcp010f/design-action-run-cadfit-real-reference-20260815-v4-diverse-fixed4-b37.json` 证明真实 ActionRun-bound CADFit 完成 32/4/3 多保真搜索并到达 render。顶层父 ActionRun 的 `run_status=blocked`、`repair_probe_status=blocked`、`proposal=null` 只表示直接 Repair 探针被质量门阻断；嵌套 CADFit child 已通过显式 continuation 物化独立 review candidate，但视觉状态仍 `QUALITY_TARGET_NOT_MET`、不可 confirm、未晋级 version。`docs/evidence/mcp010f/optimization-job-real-reference-20260815-boolean-residual-v20-fixed.json` 证明 `[1..9]` 非 baseline 候选均经过同一 Part 的 product-owned Manifold C-ABI Boolean lane，baseline/best loss `0.612457433696/0.610821480247`，strict gate 为 `proposed`；该回执没有 build cohort 字段，已在 additive ledger 中禁止向当前 source cohort 合并。它仍不等于通用 mesh Boolean、likeness、PBR、人评、export/restart 或 360 PASS。

2026-08-15 ActionRun→CADFit handoff Gate：真实授权 PNG 已通过父 `DesignActionRun@1` → child `OptimizationJob` 的 hash-bound scope/Part/stage/camera 绑定；回执 `docs/evidence/mcp010f/design-action-run-cadfit-handoff-real-reference-20260815.json` 完成 38 次（32/4/2）搜索并正确停在 `blocked-no-improvement`，source candidate/version/持久数据均未改变。该 Gate 只记为 `PASS_ACTION_RUN_CADFIT_HANDOFF`，不升级完整 orchestrator、Repair execution、视觉 likeness、PBR、人评、export/restart 或 360。

2026-08-15 CADFit/Manifold v19c 局部绑定修正：`SilhouetteTarget@1.part.region` 现在允许 Runtime 消费由参考图像标注的归一化 ROI；`target_part_region_mask` 将 Runtime-owned 全身 mask 裁剪到该 ROI，CADFit `part_region`、Part contour error 和 Part fit envelope 优先使用它，缺少 ROI 时才回退到声明的 contour slice。真实回执 `docs/evidence/mcp010f/optimization-job-real-reference-20260815-boolean-residual-v19c.json` 在同一授权参考上通过 32/4/2 共 38 次评估，`part_target_binding=PASS_IMAGE_DERIVED_REGION_BOUNDED_REFERENCE_MASK`，`chest-armor` ROI 为 `x=0.292,y=0.285,width=0.435,height=0.285`；candidate 0 无 Boolean，候选 `[1..9]` 进入 product-owned Manifold Boolean lane，baseline/best loss `0.472832854888/0.470494645212`，但 `strict_improvement=false`、`proposal_status=blocked-no-improvement`。ROI 是显式图像区域，不是语义分割或 likeness 通过；没有 confirm/version/持久用户数据写入，仍 `NO_LIKENESS_PASS_CLAIM`、`QUALITY_TARGET_NOT_MET`。

2026-08-15 Boolean residual family v3 增量：真实回执 `docs/evidence/mcp010f/optimization-job-real-reference-20260815-boolean-residual-v18.json` 固定 candidate 0 为 unmodified baseline，并在候选 `[1..9]` 运行 `residual-chest-sphere-boolean` 的 product-owned Manifold C-ABI family；完成 32 coarse、4 mid、2 final 评估，baseline/best loss `0.480812392901/0.478756028517`。Boolean lane 被真实搜索但最终 winner 为无 Boolean shape-only candidate；`strict_improvement=false`、`proposal_status=blocked-no-improvement`，没有 confirm/version，Gate 级别为 `PASS_BOUNDED_BOOLEAN_RESIDUAL_SEARCH_WITH_QUALITY_TARGET_NOT_MET`。该证据不升级 semantic Part likeness、通用 mesh Boolean、PBR、人评、export/restart 或 360。

2026-08-15 最新源代码覆盖：当前为 120 Schema、38 read + 30 opt-in write = 68 tools。只读 `visual_surface_get`/VisualSurfaceResult@1 已加入 Runtime/MCP source surface，backend 为 `NOT_RUN`；单视图 `repair_apply_confirm` 已加入 Runtime/MCP/Store source surface；`design_action_optimization_proposal_prepare` 只加入 ActionRun→CADFit 的独立 review-candidate continuation，严格改进不足或视觉门失败时保持 blocked，不自动 Repair/Confirm。合同/negative gate、Rust focused test 与 Stage 0 truth 已通过；多视图仍由 `cross_view_promotion_confirm` 消费。该 source increment 不升级视觉、packaged、PBR、人评、export/restart 或 360 Gate。

计数校正：下方 reconciliation 行中的 115 Schema、37 read + 28 opt-in write = 65 是历史快照；当前 Gate 矩阵统一采用 120 / 38 + 30 = 68。

版本：2026-08-13
状态：当前唯一能力与阻断总表；ADR-0026 Agentic Design Runtime 与废弃隔离规则已纳入目标/治理能力，不改变 MCP010F 质量状态

FGC-MCP010F 增量：隔离 Vite browser DOM smoke 已验证 Viewer 控件和无 metrics 的 `reference-canvas`/空 correction queue 语义；同 cohort Dev.app 的 Computer Use AX smoke 又实际操作了 AOV、Home/End、overlay/flicker、轮廓画布、差异热图和爆炸图；System Events 仍未暴露 WebKit 子树，因此正式 VoiceOver/无障碍、视觉 likeness 或人评状态不升级。

Stage 0 Gate 口径：当前为 118 Schema、37 read + 30 opt-in write = 67 tools，唯一 `in_progress` 是 `FGC-MCP010F`；机器证据入口为 `docs/evidence/mcp010f/current-benchmark-truth.json`。Agentic projection 的隔离证据为 `docs/evidence/mcp010f/agentic-runtime-observe-plan-20260813.json`；真实 Runtime scene/stage 嵌套只读 projection conformance 证据为 `docs/evidence/mcp010f/agentic-runtime-projection-conformance-20260813.json`，由 `scripts/check_agentic_projection_receipt.py` 校验；durable session/checkpoint/RepairIntent prepare/readback 的隔离重启证据为 `docs/evidence/mcp010f/agentic-runtime-session-checkpoint-20260813.json`，由 `scripts/check_agentic_runtime_receipt.py` 校验；bounded independent stage batch 与 2–6 项线性 `design_composition_prepare` 由 Runtime/MCP focused tests 证明 approval/hash/scope/stop/replay 边界，新增的 `cumulative-program` merge-chain validation、`repair_apply_prepare` CAS-backed apply-intent boundary、`repair_apply_confirm` fixture/runtime transaction 和 `design_action_optimization_proposal_prepare` strict-improvement continuation 也由 focused fixture 覆盖，bounded cross-view evidence 另见 `docs/evidence/mcp010f/cross-view-evidence-boundary-20260814.json`。组合 receipt 见 `docs/evidence/mcp010f/agentic-composition-proposal-20260814.json`；这些证据不等于 durable/reference/DesignSpec 完整 producer、positive merge、真实参考 Repair 实际应用或 promotion。attempt35 只是 provisional retained observation，状态为 `QUALITY_TARGET_NOT_MET + INCOMPLETE_TRUTH_BINDING`，benchmark eligibility 为 `BLOCKED_INCOMPLETE_BINDING`，camera-fit `354caf27…f95788` 与 reference-compare `8cd20605…a535` 为 `MISMATCH`。最新真实 Codex v3 receipt 完成当前 cohort 的 camera-fit→compare 与 9 AOV/image-block transport，但不等于 packaged Viewer 绑定；packaged Viewer CLI read-model 是独立的 current-cohort lineage receipt。UI/accessibility、真人评审与 packaged UI E2E 仍未运行，source/raw/transport/build/AX smoke 不得计作视觉、人评或 packaged UI PASS。

CADFit 相机交接补充：真实授权参考的 `optimization-job-real-reference-20260815-v8.json` 已验证 `silhouette_fit_prepare` winner camera `27d180d2…c0c` 被完整交给 38-evaluation OptimizationJob，绑定状态为 `PASS_SILHOUETTE_FIT_TO_OPTIMIZATION`；严格多目标改善为 false，proposal 仍 fail closed 为 `blocked-no-improvement`，未 confirm/version。该证据只升级 CADFit transport 的相机绑定，不升级本段 Stage 0 的视觉 benchmark、likeness 或 packaged/human Gate。

CADFit Boolean residual 补充：`optimization-job-real-reference-20260815-boolean-residual-v14.json` 已在真实授权参考上完成 hash-bound Critic/PartError → `OptimizationResidual@1` → product-owned Manifold C-ABI → 38 次多保真评估；`boolean_node_ids`、同一 Part、candidate program/readback 和 `candidate_confirmed=false/version_count=0` 均可回执核对。严格多目标 gate 返回 `blocked-no-improvement`，所以这项为 `PASS_BOUNDED_RESIDUAL_TRANSPORT_WITH_QUALITY_TARGET_NOT_MET`，不是 visual likeness PASS。camera canonical rebind、profile-extrude Boolean raw union/difference/intersection、薄三角形 UV/tangent regression 也已通过；通用 mesh Boolean、packaged D、PBR、人评、export/restart、360 仍保持 `NOT_RUN/BLOCKED`。

<!-- forgecad-stage0: schemas=123 schema_set_sha256=583fd0d2615f09e66d16c58fca8d4ab60f1856d1de427b5b9e390c8c8b137f67 read_tools=40 write_tools=30 total_tools=70 task=FGC-MCP010F observation=QUALITY_TARGET_NOT_MET eligibility=BLOCKED_INCOMPLETE_BINDING evidence=INCOMPLETE_TRUTH_BINDING camera=MISMATCH packaged=PASS_CURRENT_COHORT_BOUND_READ_MODEL latest_attempt=real-codex-cli-current-20260815-b37-complete-auto-v3.json latest_completed=real-codex-cli-current-20260815-b37-complete-auto-v3.json -->

2026-08-14 Cross-view correction: `CrossViewEvidenceBundle@1` and view-keyed evidence storage are source/validator implemented; `candidate_confirm` remains fail-closed with `CROSS_VIEW_PROMOTION_REQUIRED`. This does not upgrade the marker's `QUALITY_TARGET_NOT_MET`, incomplete binding, human, packaged UI or 360 state.

MCP010B/C/D/E/F reconciliation：历史 MCP006 为 44 contracts；保存的 50/52-contract、3c/f488/bfa56/d9 Dev.app receipts按当时范围保持原样。当前源码有 115 contracts（含 Agentic contract family 与 Boolean request/result），C 的 fixed renderer/九 AOV/reference comparison/MCP image block/Codex-human review source Gate、D 的 hard-surface Operator/Skill source Gate、product-owned Manifold bounded Boolean Worker 与 strict raw readback、E 的离线 AssetPack/UV/PBR/MikkTSpace source Gate与同 cohort packaged E structural probe、F 的哈希绑定轮廓目标/Runtime-owned camera ref/37 个覆盖全局尺度的粗候选加局部探针 camera fit/方向性 boundary error 与只读 Viewer source Gate PASS；F 另要求 `SilhouetteTarget.parts` 为唯一非重叠 contour slices，并将有声明的 Part-ID 边界映射到对应 target slice。Agentic projection、逐视图 evidence inventory、durable session/checkpoint/RepairIntent prepare/readback、bounded independent stage batch 和 bounded cross-view evidence 另有 source/transport/restart/focused PASS；最新真实 Codex receipt 已完成 current-cohort packaged Viewer CLI read-model exact lineage binding，但完整组合式 orchestrator、Repair、Viewer UI/accessibility、image-block consumption 和视觉门仍未运行。用户机器人当前 provisional retained observation 指针为 unrotated surface-linework + armor-shell-zones，26 Parts/4704 triangles（silhouette IoU 0.741047、boundary F1 0.328765、region median IoU 0.869403、critical region min 0.666289），但因 camera `MISMATCH` 仍为 `QUALITY_TARGET_NOT_MET + INCOMPLETE_TRUTH_BINDING`，benchmark eligibility 为 `BLOCKED_INCOMPLETE_BINDING`；8-zone/7-zone MaterialZone 只改善材质-ID/表面可读性，曲面 Operator 的兼容法线平滑仍只改善 beauty shading、不改变指标，左右肩甲单部件 correction transport 已执行但最佳 IoU/F1 仍未晋级。Darwin OS 512 MiB total-memory cap、current packaged Manifold Boolean D、xatlas、Khronos Validator、packaged Viewer UI E2E、真实 user likeness、PBR likeness、human threshold、export/restart hash/360仍 `NOT_RUN`/blocked。

| 能力 | 当前状态 | 实现 Gate | 集成 Gate | 生产 Gate |
|---|---|---|---|---|
| 文档权威重置 / Dev 激活 | MCP010A done / Desktop attempt 1 FAIL retained / attempt 2 PASS | MCP010A 权威链、真实 Codex CLI、config activation、共享 Runtime/IPC 修复 tests、current `release:mvp`、同 cohort package PASS | 第二次 Desktop 重启观察到 30 个工具、Runtime/doctor `Ready`、capabilities cohort match、临时 project_create/project_get readback PASS；成功 receipt 已保存 | 不适用 |
| 安全硬切 | 已实现 | 旧产品路径删除、contracts/viewer 骨架、Rust workspace 和 release:mcp002 | reset archive/Library hash 保留；后续 packaged hardening 在 MCP013 | clean packaged build |
| Runtime 单写者 | 已实现（MCP002）；共享启动协调与 Desktop activation PASS | migration 前 OS 独占 `runtime.writer.lock`、第二实例 `RUNTIME_BUSY`、事务 rollback 是已有 Gate；短时 launcher flock 只做选主，不能替代 writer lock | normal adapter exit 后复用 Ready Runtime、idle owner/passive takeover、rogue pre-auth client 隔离回归和第二次 Desktop live Gate PASS；packaged kill-9/upgrade 在 MCP011/013 | backup/upgrade/packaged |
| Runtime CAS | 已实现（MCP002 范围） | SHA-256、atomic write、capacity/missing/corruption/hash mismatch、backup/restore | reachability/GC 在 MCP011 | packaged/disaster recovery |
| authenticated local IPC | 已实现（MCP002 范围） | 0600 Unix socket、token hash/constant-time auth、错误 token 拒绝 | MCP013 packaged wiring | signed install |
| MCP stdio/resources | 已实现（MVP host golden path + MCP010C/E/F source raw） | MCP 2025-11-25 canonical + Codex 2025-06-18 compatibility initialize；当前源码默认 37 个只读 tools，authenticated/dynamic IPC + explicit env opt-in 时 67 个 tools（37 read + 30 write）。`material_pack_get`/`render_pass_get`/`silhouette_target_get`/`camera_fit_prepare`/`silhouette_part_error_get`/`boundary_error_get`/`session_get`/`checkpoint_get` 只读 CAS/manifest/evidence；轮廓 mask、Agentic durable prepare、`design_stage_run_prepare`、`design_composition_prepare`、`repair_apply_prepare`、`repair_apply_confirm` 与 `design_action_optimization_proposal_prepare` 显式 write opt-in；URI/contract fail closed | C/E/F raw stdio discovery/compare/review/image block/AssetPack/PBR/contour-target source PASS；Agentic session/checkpoint/RepairIntent prepare/readback isolated restart PASS；stage batch、composition merge-chain、Repair apply prepare/confirm 与 CADFit continuation focused approval/hash/stop/replay PASS；同一 provisional observation 的 packaged Viewer E2E/live C/F 尚未运行，IDE/其他 Client非阻塞未来范围 | signed install/config |
| Codex 附件导入 | 已实现（MCP005，CLI） | PNG/JPEG MIME/path/hash/decode-limit/adversarial | 真实 Codex CLI bytes/CAS readback PASS；Desktop bridge `NOT_RUN / unavailable` | clean install E2E |
| Candidate/approval/version | 已实现（MVP host golden path） | prepare/Job/approval/confirm/reject/restore-as-new-version/diagnostic-export/idempotency/stale/hash/quality/expiry、MCP009 stable-Part `change_prepare` PASS | 真实 Codex CLI candidate/quality/version/CAS export PASS；MCP010A Desktop attempt 1 在 write tool 可见性前 FAIL、无项目写入，故 Desktop 写事务仍 NOT_RUN；change/restore/restart hash 与 packaged 待后续 Gate | packaged multi-turn |
| Geometry Compiler | 部分实现（MCP007 done） | canonical GeometryProgram；bounded box/cylinder/sphere；finite/index/budget/lineage；deterministic GLB/readback；unknown operator/no-version-on-failure PASS | Runtime/MCP authenticated geometry prepare + Viewer candidate/artifact read model PASS；real Codex CLI geometry/readback slice PASS（14 parts/516 triangles）；MCP009 full host appearance链已单独 PASS | full profile/boolean/bevel, local edit, cross-category/performance/human |
| Geometry V2 / detail | MCP010B structural + MCP010D source + packaged structural PASS | `GeometryProgram@2`/OperatorCatalog/`geometry_program_hash`/真实 GLB readback；D 的 profile/revolve/sweep/loft/transform/mirror/array/panel/vent/joint/part-output；`GeometryQualityReport@2`/`GeometryCandidateEvidence@1` 绑定 strict quality 与 confirm/restore provenance | 52-contract B/Worker isolation/restore/closed profile/D operator Gate 与同 cohort packaged D raw strict-readback probe PASS；Darwin OS 512 MiB hard cap、Manifold、visual gate仍 NOT_RUN | PBR/reference/visual/human/360 |
| Appearance Compiler | 已实现（MCP008 bounded） | hash-bound UV/tangent/PBR/MaterialZone/GLB readback PASS；完整 glTF validator/texture bake NOT_RUN | Runtime fixed artifact + Viewer canvas PASS；真实 Codex appearance/readback 在 MCP009 receipt PASS | engine roundtrip/human |
| 离线 AssetPack / PBR V2 | MCP010E source-focused + packaged structural PASS_WITH_UNRUN_EXTERNAL_AND_VISUAL_GATES | `forgecad-hard-surface-robot@1.0.0`、逐资产 provenance、512px bounded UV atlas、固定 `mikktspace@0.3.0`、颜色空间/纹理预算、无 external URI、clearcoat/emissive strength | `script/test_mcp010e.sh` raw source Gate 与同 cohort packaged E 结构传输 PASS；xatlas/Khronos Validator adoption、真实视觉/PBR likeness、export/restart hash仍 NOT_RUN | signed package/license review |
| Render Evidence | MCP010C source-focused PASS | deterministic MCP008 compatibility passes + C perspective/z-buffer nine AOV PNG + camera/renderer hash PASS | F Viewer source AOV/compare projection and local build/IPC boundary PASS | packaged Viewer/cross-GPU/human |
| Reference compare V2 | MCP010C source-focused PASS_WITH_UNRUN_VISUAL_GATES（非当前 `in_progress`） | perspective/z-buffer、九 AOV、local mask/IoU/F1/bbox/centroid/landmark/region、typed review/human receipt schema | 当前 source tools 37 read + 28 opt-in write = 65；raw synthetic comparison/image block/review Gate 仅按 source/transport 范围 PASS；attempt35 为 `QUALITY_TARGET_NOT_MET + INCOMPLETE_TRUTH_BINDING`，真实 user likeness、同基线 packaged Viewer E2E、人评阈值仍 NOT_RUN | cross-category blind review |
| Quality Compiler | 部分实现（MCP009 limited） | candidate/GLB/PBR/fixed-render checks PASS；reference aspect comparison explicitly limited | `quality_get`/`version_diff` and functional tests PASS；typed visual review/real Codex/user score NOT_RUN | independent cross-category blind review |
| Skill Bundle | MCP006 done（development-only）+ MCP010B/D/E/F truthful overlays | MCP006 历史 44 contracts，当前源码 115；历史 first-party registry + `primitive-blockout@0.2.0`、`hard-surface-detail@0.2.0`、`uv-pbr@0.2.0` active Bundle，canonical/trust hash、operator/validator allowlist、Recipe DAG/单位/finite/预算、AssetPack LICENSE/NOTICE/SPDX SBOM/provenance、无网络/无动态代码/无模型调用 | D Operator、product-owned Manifold Boolean Worker、E AssetPack/MikkTSpace 与 F contour-target/`CameraCalibrationRef@1`/camera-fit/boundary source Gate PASS；Agentic projection/逐视图 evidence inventory/durable prepare/readback/stage batch/CrossViewEvidence/composition merge-chain/Repair apply prepare source/restart/focused Gate PASS；xatlas/Validator、packaged/visual benchmark NOT_RUN | distribution signature/revocation/third-party registry |
| Agentic Design Runtime | projection source/read-only PASS；nested projection conformance PASS；bounded authoring/readback PASS；geometry-stage single-Part proposal PASS；independent stage batch PASS；ordered composition proposal + cumulative merge-chain validation PASS_WITH_FAIL_CLOSED_MERGE；single-view `repair_apply_confirm` fixture/runtime transaction PASS；positive merge/MaterialZone/cross-view Repair NOT_RUN | Agentic contract family 已进入 manifest；4 个 read-only projection tool、5 个 durable session/checkpoint prepare/readback tool、默认或显式 ReferenceCanvas/DesignSpec CAS producer、逐视图 evidence inventory、geometry-stage ActionRun、最多 6 个同阶段独立 ActionRun 的 RuntimeJob/event batch、2–6 项线性依赖的 `design_composition_prepare`、`cumulative-program` parent/program hash validation、Runtime SQLite/CAS、Viewer lookup、preflight/isolated restart probe 已通过各自范围；单视图 `repair_apply_confirm` 已接入合同/Runtime/MCP/Store source surface，`cargo check --workspace`、focused test 与 `cargo test --workspace` 已通过 | `docs/evidence/mcp010f/agentic-runtime-projection-conformance-20260813.json` 证明真实 Runtime scene/stage 嵌套只读 projection producer/consumer conformance；`docs/evidence/mcp010f/agentic-runtime-session-checkpoint-20260813.json`、`docs/evidence/mcp010f/agentic-runtime-authoring-context-action-boundary-20260814.json`、`docs/evidence/mcp010f/agentic-composition-proposal-20260814.json` 和 `docs/evidence/mcp010f/repair-apply-confirm-transaction-20260815.json` 分别证明 bounded authoring/session/checkpoint/geometry proposal/evidence inventory/composition stop-replay-promotion boundary 与 Repair confirm fixture/runtime transaction；不证明 positive merge、跨视图 Visual Evidence conformance、MaterialZone/UV-PBR/Repair 运行时应用、packaged、视觉或真人门 | cross-view quality evaluation、positive composition merge/promotion、MaterialZone/UV-PBR/Repair execution、real Codex loop、human/package |
| 废弃隔离 | 已实现（当前树审计范围） | 旧 Provider/Agent/workbench 已清理；standalone Host receipt 在 archive；三个 superseded Skill Bundle 已移入 `packages/forgecad-skills/archive/superseded/`，archive manifest 固定 tree hash，Runtime build 只嵌入 registry 声明的 Bundle，Skill checker 拒绝未注册版本留在 `bundles/**` | archive 不计 active capability；后续废弃项仍需按 `DEPRECATED_ISOLATION_PLAN.md` 记录和验证 | 发布前还需完整 package/SBOM/archive 审计 |
| 局部修改 | 已实现（MVP bounded） | stable ID + allowlisted `change_prepare` + base head/readback PASS；不声称 mesh-delta | Runtime focused PASS；真实 Codex/Viewer selection E2E NOT_RUN | full undo/explode/a11y |
| 回退 | 已实现（MVP） | restore-as-new-version PASS | Runtime focused PASS；restart/packaged recovery NOT_RUN | packaged recovery |
| 爆炸图 | MCP010F source-focused PASS | stable Part/lineage metadata + ephemeral offset rule | Viewer source toggle/readability + packaged Computer Use AX core-control smoke | formal VoiceOver/human |
| 导出 | 已实现（CAS-backed MVP GLB） | `manifest-json/diagnostic` 与 `glb/mvp-glb` prepare/confirm；confirmed quality GLB、output hash、approval/idempotency PASS；不写任意路径 | MCP009 functional PASS；filesystem/package target NOT_RUN | engine roundtrip |
| 外部项目/资产 | evaluation allowed | adoption receipt/pin/license/SBOM/security | MVP benchmark before `accepted` | legal/distribution approval |
| 首个硬表面 MVP | host golden path PASS / MCP010A done / MCP010B deferred / MCP010C source-focused done / MCP010D/E source-focused PASS / MCP010F source-focused in_progress / visual benchmark OPEN | MCP005–MCP009 focused PASS；B structural Gate PASS但 Darwin OS hard cap NOT_RUN；C fixed renderer/九 AOV/reference compare/review raw Gate、D hard-surface operator/Skill raw Gate、E AssetPack/UV/PBR/MikkTSpace raw Gate与F Viewer source Gate PASS；comparison-sheet/fit-plan 仅为本地编排辅助；fit-plan 固定 reference-canvas→silhouette→structure→form→surface/material 解锁顺序；Viewer source 另显示同一累计门的只读阶段提示与 Runtime mask-2 同源 flood-fill contour aid，不改变 Runtime QualityReport；region-mask-iou-v2 已修正区域比较真值；2026-08-12 同 cohort 外形 sweep（sleek/tapered/long-limbs/helmet-visor）已实跑但全部未过质量门 | real Codex CLI full V1 chain PASS；C source synthetic/raw PASS；D raw 13-entry catalog/12-active/strict readback PASS；E 的历史 65-contract raw receipt按当时范围保留；F Viewer source AOV/compare/selection/explosion/heatmap/fit-plan/contour-first panel/flood-fill aid 与 packaged Computer Use AX core-control smoke 仅按 source/结构 smoke 记账，不是 packaged E2E；正式 VoiceOver、真实 user likeness、同一 provisional observation 的 packaged Viewer binding、人评/PBR likeness/360 NOT_RUN；attempt35 仍为 `QUALITY_TARGET_NOT_MET + INCOMPLETE_TRUTH_BINDING`，benchmark eligibility 为 `BLOCKED_INCOMPLETE_BINDING`；单图最高 `PARTIAL_VISIBLE_VIEW_PASS`，且需先通过全部阈值，360 `BLOCKED_REFERENCE_COVERAGE` | 不要求签名分发 |
| 生产发布 | blocked | MCP001–MCP012 | full packaged Codex E2E | MCP013 + cross-category human gate |

旧 U004/Provider/F026/C111/E005 Gate 不映射为新能力 PASS。只有当前合同、当前工作树、真实运行和对应发布包证据才能升级状态。
