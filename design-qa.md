# ForgeCAD CAD 工作台设计 QA

- 日期：2026-07-10
- 参考图：`/Users/liuchongjiang/Desktop/e9d4239c-ee36-44de-9161-5020d2fcb329.png`
- 实现入口：`http://127.0.0.1:1420/#/cad`
- 实现文件：`apps/desktop/src/features/cad-workbench/CadWorkbenchPanel.tsx`
- 目标视口：1536 × 1024
- 默认状态：设计模式、选择工具、等轴视图、网格开启、`v5`、主体组件、STEP
- 最终实现截图：`artifacts/design-qa/cad-workbench-final-1536x1024.png`
- 最终并排对照：`artifacts/design-qa/comparison-final.png`

## 对照过程

| Pass | 发现 | 修复 | 结果 |
| --- | --- | --- | --- |
| 1 | 武器模型占比过大、顶部瞄具裁切、选中状态过蓝、滚动条过亮 | 调整相机/目标点、缩小瞄具、改为中性金属选中态、增加深色滚动条 | 进入 Pass 2 |
| 2 | 模型缩得过小且金属表面过暗，和参考图的中心视觉权重不一致 | 调整等轴相机距离与角度，增加环境/主光 | 进入 Pass 3 |
| 3 | 模型主体仍缺少参考图中的侧面层次与红色特征 | 增加侧板、导轨、枪管护罩、螺钉、边线和红色通风特征 | 通过 |

## 最终视觉检查

| Surface | 证据与结论 |
| --- | --- |
| 布局 | 顶部命令栏、左侧项目/AI/参数、中部视口、底部组件库、右侧属性/DFM/导出、底部状态栏均与参考九区结构一致。 |
| 间距与密度 | 1536 × 1024 下无面板重叠、按钮裁切或主区溢出；工作台保持桌面 CAD 的高密度信息层级。 |
| 字体 | 使用 Inter / SF Pro / PingFang SC 系统栈；标题、面板标题、参数和状态栏形成稳定层级，中文无异常换行。 |
| 颜色 | 深海军蓝表面、低对比边框、蓝色主状态、绿色通过和琥珀建议与参考语义一致。 |
| 图标 | 全部采用同一 Phosphor 图标族，没有 emoji、手绘 SVG 或文本符号替代。 |
| 三维资产 | 使用可交互的参数化 Three.js 模块化武器原型，包含主体、枪管、握把、导轨、瞄具、供弹与枪口；它匹配参考主体类别，但明确不冒充 R3 之后的权威 B-Rep。 |
| 可访问性 | 核心按钮有语义名称，表单由 label 关联，视口有 aria-label；键盘可到达主要按钮和输入。 |
| 目标设备 | 产品是 Tauri 桌面工作台；1536 × 1024 为参考/主验收视口，1280 × 720 交互烟测未出现核心控件重叠。移动端不在当前产品范围。 |

## 核心交互验证

应用内浏览器实际执行并验证：

- 模式切换：`设计 → 分析`；
- 视口工具：`选择 → 测量`，状态提示更新；
- 组件分类：选择“枪管”后只显示 2 个枪管组件；
- 组件选择：选择 `barrel-01` 后属性面板和状态栏同步；
- 参数修改：整体长度 `230 → 245 mm`；
- AI 输入：提交“将上导轨延长 12 mm”，助手生成 ChangeSet 风格提示；
- 导出格式：`STEP → 3MF`；
- 浏览器 console error/warn：0。

## 验证命令

```bash
npm run desktop:typecheck
npm run desktop:build
npm run desktop:p0-context-continuity-smoke
```

三项均通过；生产构建只有既有的 Vite 大 chunk 警告，不属于运行错误。

final result: passed
